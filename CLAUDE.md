# CLAUDE.md — 대청부 수련회 총무팀 통합 관리 시스템

Claude Code가 이 프로젝트에서 작업할 때 **매번 먼저 읽어야 하는 유일한 기준 문서**입니다.
세션이 끊겨도 이 문서만 읽으면 목적·구조·확정된 결정을 그대로 이어받을 수 있어야 합니다.

문서를 둘로 나누면 반드시 어긋나므로 배경·결정·스펙을 이 한 파일에 모았습니다.
**설계가 바뀌면 코드보다 이 문서를 먼저 고칩니다.**

`docs/mockups/` 의 HTML 3개는 이 문서와 같은 효력을 갖는 **시각 스펙**입니다. 글로 적기 어려운 배치·간격·상태 표현은 목업이 기준입니다.

| 파일 | 대상 화면 |
|---|---|
| `retreat-board-v4.html` | 준비 단계 보드 |
| `retreat-live.html` | 수련회 진행 |
| `retreat-setup.html` | 새 회차 세팅 마법사 |

---

## 0. 목적과 판단 기준

**무엇을 만드는가** — 대학청년부(대청부) 수련회를 준비·진행·정산하는 웹 시스템. 총무팀과 각 부서 리더가 함께 씁니다.

**왜 만드는가** — 담당 전도사와 총무팀장이 매 회차 바뀝니다. 잘 챙기는 사람이 맡으면 굴러가고 아니면 구멍이 생기는 구조를 없애는 것이 목적입니다.

**모든 설계 판단의 기준** — "누가 놓쳐도 시스템이 대신 알아차리는가?" 이 질문에 답하지 못하는 기능은 우선순위가 낮습니다.

이 목적에서 파생된 두 가지 원칙:

1. **아무것도 삭제하지 않는다.** 이번 회차에 안 하는 업무도 "미실행"으로 기록만 바꿉니다. 기록이 쌓여야 판단 근거가 생깁니다.
2. **지식은 사람이 아니라 기록에 남긴다.** 논의 내역은 회차를 넘어 따라갑니다. "명찰 사이즈를 스트랩 재고 때문에 바꿨다" 같은 것이 다음 담당자에게 전달되는 유일한 경로입니다.

**작업 방식** — 사용자는 Phase 단위로 맡기고 완성된 결과물만 리뷰합니다. Claude Code는 구현 → 실행 검증 → 자체 수정까지 마친 뒤 보고합니다. 사소한 판단은 묻지 않되, 아래 확정 사항과 충돌하면 반드시 확인합니다. 완료 보고에는 직접 눌러볼 수 있는 실행 방법을 포함합니다.

---

## 1. 확정된 결정사항

변경하려면 사용자 확인이 필요합니다.

| 항목 | 결정 |
|---|---|
| 로그인 | **초대 링크** (총무팀이 발급, 카카오톡으로 전달). SMS 인증은 접었습니다 — 4-12 |
| 회차 생성 | 자동 없음. 총무팀이 수동 실행 |
| 시간 축 | D-주차 단위. 개회일 직전 일요일이 D-1주차 |
| 일 단위 전환 | D-2주차부터 (여기서부터 하루 단위로 표시) |
| 알림 | 웹 푸시(PWA)만. 카카오톡은 보류 |
| 식대 1인당 상한 | 회차 설정값. 하드코딩 금지 (여름수련회 8,000원 / 소그룹지원비 5,000~7,000원 사례) |
| 결산 양식 | 미확정. 사용자 제공 시 반영, 그전까지 범용 엑셀 |
| 소속 외 표시 | 숨기지 않고 흐리게. 존재는 인지되어야 함 |
| STT 벤더 | 네이버 CLOVA Speech (화자분리·장시간 파일 지원, 한국어 정확도 기준) |
| 배포 | 집 윈도우 서버 + Cloudflare Tunnel. 링크 하나로 접속, 설치 불필요 — 11장 |

---

## 2. 부서

봉사팀 공통은 **해체했습니다.** 실제로는 특정 팀이 주관하는데 "공통"으로 묶여 있어 담당이 모호했습니다.

| ID | 이름 | 색 |
|---|---|---|
| `chongmuM` | 1 총무M | `#2F4858` |
| `chongmu` | 1 총무팀 | `#77848F` |
| `seongyo` | 3 선교사회 | `#3B6EA5` |
| `sketch` | 4 스케치 | `#B95A83` |
| `hebron` | 5 헤브론 | `#4A8A5C` |
| `koram` | 6 코람데오 | `#B44B42` |
| `jaejeong` | 7 재정 | `#8A6A4F` |
| `gaegija` | 8 개기자 | `#7A5BA6` |
| `saechingu` | 9 새친구팀 | `#A98A1E` |

**재배정 기준** — 실무진 답사·선발대 운영·집회 진행요원·큐시트·봉사자 시간표는 총무M, 장비 관련은 헤브론(관련팀 코람데오), 객원 모집은 코람데오, 성찬은 선교사회. 전원이 참석하는 일정(중간점검·봉사자 기도회 등)은 부서에 배정하지 않고 공통 일정으로 둡니다.

**부서 소속은 키로 비교합니다.** `Department` 행은 회차마다 새로 만들어지므로 `id` 로 비교하면 새 회차가 열리는 순간 모든 부서 리더가 자기 부서 업무조차 손대지 못합니다. 회차를 넘어 같은 부서임을 알아보는 것은 `key` 뿐입니다.

**담당팀과 관련팀은 다릅니다.** 담당팀은 하나, 관련팀은 여럿입니다. 관련팀은 "이 업무의 결과물을 받아야 다음 일을 할 수 있는 팀"만 지정합니다.

**업무 사이의 관계는 셋이고, 섞으면 안 됩니다.**

| 관계 | 방향 | 저장 | 뜻 |
|---|---|---|---|
| 관련업무 | 없음 | 양방향 (`relatedLibraryIds`) | 서로 참고할 사이. 보드의 고스트 바와 연결 강조가 쓴다 |
| 선행업무 | 있음 | **단방향** (`prerequisiteLibraryIds`) | 저쪽이 끝나야 이쪽을 시작할 수 있다. 진단 패널의 '진행 불가' 근거 |
| 상위-하위 | 포함 | `parentLibraryId` | 나를 이루는 단계. **앞을 막지 않는다** (4-10) |

선행은 **가진 쪽에만 적습니다.** A 가 B 를 선행으로 가질 때 B 에는 아무것도 쓰지 않고,
"A 가 나를 기다린다"(후속)는 물어볼 때 계산합니다. 양쪽에 적으면 한쪽만 지워졌을 때
어느 쪽이 맞는지 알 수 없습니다.

---

## 3. 화면 구성

상단 탭 4개 + 별도 세팅 마법사.

```
회의록 | 준비 단계 | 수련회 진행 | 재정          [새 회차 만들기]
```

준비와 진행을 나눈 이유가 중요합니다. 준비 기간에는 "몇 주에 걸친 업무가 어떻게 얽혔나"를 봐야 하고, 진행 중에는 "지금 뭘 해야 하나"만 필요합니다. **같은 데이터를 다른 축으로 보는 두 화면**이며 UI를 공유하지 않습니다.

---

## 4. 준비 단계 보드

시각 스펙: `retreat-board-v4.html`

### 4-0. UI 방향 — 무채색 기조

2026-08-31 에 화면을 노션 쪽으로 옮겼습니다. **기능과 구성은 그대로 두고 색·여백·글자·테두리만 바꿨습니다.**
목업 3개도 같은 방향으로 다시 만들었습니다 — 15장이 목업부터 읽으라고 하므로, 목업이
옛 화면인 채로 남으면 앞으로 모든 작업이 옛 기준에서 시작합니다.

| 값 | 쓰는 곳 |
|---|---|
| 잉크 `#37352F` | 본문. **순검정을 쓰지 않습니다** |
| 보조 `#6E6D6A` | 부차적인 글자. **9장의 4.5:1 을 실제로 넘깁니다**(5.17:1) |
| 흐림 `#83827F` | **옆의 값이 뜻을 다 지고 있는 자리에만** — 개수·아이콘·속성 라벨, 그리고 *흐린 것이 곧 뜻*인 것(완료된 점·이 달 밖 날짜·고스트 바). 목록은 `tests/test_typescale.py` 에 |
| 선 `rgba(55,53,47,.09)` | 전부 헤어라인 |
| 사이드바 면 `#F7F7F5` · 호버 `rgba(55,53,47,.06)` | |
| 선택 `#2383E2` · 지연 `#C4554D` · 비침 `#FAF1D2` | 뜻이 있는 색은 이 셋뿐 |

글자는 시스템 폰트입니다(`ui-sans-serif, -apple-system, "Segoe UI", Pretendard, "Apple SD Gothic Neo", "Malgun Gothic"`).
굵기를 남발하지 않습니다. 웹폰트를 받아오지 않으므로 첫 화면이 늦게 뜨지 않습니다.

**크기는 `--fz` 하나가 정합니다.** 2026-09-03 에 "프로그램 전체적으로 글자가
작다" 는 말을 듣고 세어 봤더니 **24가지가 286곳에 숫자로 박혀** 있었습니다
(9 · 9.5 · 10 · 10.5 · 11 · 11.5 · 12 · 12.5 · 13 · 13.5 · 14 …).
0.5px 차이가 위계를 만들지 못하면서 **손댈 곳만 늘렸습니다** — 어딘가를
키우면 다음에 또 다른 곳이 작습니다. 그래서 다섯 단으로 줄이고 전부 눈금에서
끌어옵니다. 다시 키울 일이 생기면 `--fz` 하나만 올립니다.

| 단 | 값 | 쓰는 곳 |
|---|---|---|
| `--fz-xs` | 12 | **장식** — 배지·단위·캐럿처럼 옆의 값이 뜻을 다 지는 자리 |
| `--fz-sm` | 13 | **보조** — 메타·날짜·작성자·부속 숫자 |
| `--fz-md` | 14 | **목록** — 바·점·사이드바·칩 |
| `--fz-base` | 15 | **본문** — body·입력·논의·왼쪽 라벨 |
| `--fz-lg` 16 · `--fz-xl` 17 · `--fz-2xl` 20 · `--fz-3xl` 23 · `--fz-4xl` 26 | | 제목 |

**하한이 둘입니다** — 혼자 뜻을 지는 글자는 **14px**, 장식은 **12px**.
9장이 대비에 하한을 둔 것과 같은 이유로 크기에도 둡니다.
`tests/test_typescale.py` 가 숫자가 다시 새어 들어오는 것을 막습니다.

**칸 높이도 함께 올렸습니다.** 글자만 키우고 칸이 그대로면 더 답답해집니다 —
바 안에서 위아래가 붙습니다. 한 줄이 4px 커지고(팀 42 · Main 38 · 하위 33),
부서를 다 편 96행이 4,245px → 4,789px 로 **544px** 길어집니다. 한 화면에
보이는 줄은 18 → 16 입니다. 보드는 원래 한 화면에 안 들어오고 **부서를 접어
보는 화면**이므로(4-1) 스크롤이 받습니다.

**셋째 색(비침)은 나중에 들였습니다.** 달력에서 점에 마우스를 올렸을 때
그 업무의 기간이 비치는 자리입니다(4-13). 처음에는 무채색으로 두었는데
**안 보였습니다** — 옅어서만이 아니라 `.out`(이 달 밖) 칸이 이미 회색이라
**회색 비침이 '이 달 밖' 으로 읽혔습니다.** 잰 값으로 ΔE 4.9, 사실상 구분이
안 되는 거리였습니다. **안 보이는 표시는 없는 것과 같으므로** 여기서만
이 규칙을 깼습니다. 붉은 계열은 지연과, 파랑은 선택과 갈리지 않고, 초록은
이 화면에서 이미 '완료·진행 가능'(`--go`)을 뜻해서 **앰버**입니다.
값은 `--span` 한 곳에 있고 왜 그 값인지도 거기 적혀 있습니다.

**부서 색은 유지하되 면을 채우지 않습니다.** 왼쪽 점과 바 테두리에만 씁니다.
화면 전체가 색으로 덮이면 정작 눈에 띄어야 할 **선택 테두리와 지연 배지가 묻힙니다** —
색을 아껴야 색이 뜻을 갖습니다. 4-3 의 상태 표현 4종은 그대로 살렸고, 진행중을 팀 색
20% 로 채우던 것만 **왼쪽 3px 팀색 마개**로 바꿨습니다(채움이 아니라 테두리의 연장).
완료는 여전히 회색 채움으로 눈에 띄지 않습니다.

**가로 격자선은 넣지 않습니다.** 세로 격자선만 남깁니다. 가로선을 넣어봤으나 화면이
답답해져 뺐습니다 — 행은 여백과 호버로 갈립니다. 날짜 헤더 아래 한 줄만 남기는데,
그것은 격자선이 아니라 sticky 경계입니다(없으면 스크롤할 때 바가 헤더 위를 지나가는 것처럼 보입니다).

**상단 탭 줄을 없앴습니다.** 화면 이동은 왼쪽 사이드바로만 합니다 — 같은 목록이 두
군데 있을 이유가 없습니다. 4-9 에서 논의 입력칸을 두고 하단 버튼을 없앤 것과 같은 판단입니다.

**사이드바는 접었다 펼 수 있고 기본은 접힘입니다.** 여는 길이 둘이고 뜻이 다릅니다 —
왼쪽 위 토글은 **고정해서 계속 열고**(본문이 그만큼 밀립니다), 화면 왼쪽 가장자리에
마우스를 대면 **잠깐 떠서 나옵니다**(본문은 그대로고 위에 겹칩니다). 들춰 보기가 있어야
하는 이유는 이동 수단이 이제 여기 하나뿐이기 때문입니다 — 다른 화면으로 가려고 매번
고정했다 푸는 것은 이동 한 번에 조작이 셋입니다. 고정 여부는 기기에 남깁니다.

> **대비에 관해** — 9장은 보조 텍스트 4.5:1 을 요구합니다. 보조 `#6E6D6A` 는
> 흰 바탕 5.17:1 · `#F7F7F5` 위 4.82:1 로 **넘깁니다.** 흐림 `#83827F` 는
> 3.84:1 로 넘기지 않으므로 **속성 라벨처럼 옆의 값이 뜻을 다 지고 있는
> 자리에만** 쓰고, 혼자 뜻을 지는 글자에는 보조 이상을 씁니다.
>
> **규칙만 적어 두면 새어 들어옵니다.** 2026-09-03 에 세어 봤더니 흐림이
> **107곳**에 쓰이고 있었고 그중 **69곳이 혼자 뜻을 지는 글자**였습니다 —
> 사이드바 묶음 제목(`실무`·`회차 준비`)·담당자 이름·논의 날짜·달력 요일
> 머리·범례처럼 **그 글자가 없으면 아무것도 안 남는** 자리들입니다.
> 그 69곳을 보조로 옮기고, 남은 38곳을 `tests/test_typescale.py` 의
> `흐려도_되는곳` 에 이름으로 적었습니다. **거기 없는 자리에 흐림을 쓰면
> 빨개집니다** — 규칙을 글이 아니라 목록으로 지킵니다.
> (2026-09-02 이전에는 `#787774`/`#9B9A97` 였고 각각 4.48:1 · 2.81:1 이라
> **기준을 못 넘는 값이 기준으로 쓰이고** 있었습니다 — 9장에 자세히.) 팀 색 틴트 위에 글자를 얹던 자리가 사라져 9장의 합성 대비 문제는 함께 없어졌습니다.

### 4-1. 축

- **가로** — D-13주 ~ D-3주는 주 단위, D-2주부터 개회일까지는 일 단위, 마지막에 수련회 기간 칸. 단위가 바뀌는 지점에 굵은 세로선.
  D-13주는 **기본 시작점일 뿐**입니다. 그보다 이른 업무가 있으면 보드가 그 주까지 늘어납니다(최대 D-40주).
  기획은 개회 반년 전부터 돌기 때문입니다 — 장소 탐방·견적, 주제 논의 같은 것들이 D-13주보다 훨씬 앞에 있습니다.
- **세로** — 부서 → Main 업무 → 하위 업무 순의 트리. 부서 헤더를 눌러 접고 펼칩니다.

주 단위를 쓰는 이유는 봉사자들이 주말에만 모이기 때문입니다. 노션의 D-주차 마커가 전부 일요일이었던 것으로 확인했습니다. 주 단위는 편의가 아니라 실제 리듬입니다.

### 4-2. 업무 3분류

기존 노션의 `업무속성`을 정리한 것입니다.

| 분류 | 정의 | 표현 |
|---|---|---|
| Main | 산출물이 남는 단위. 논의가 쌓임 | 두꺼운 바 |
| 하위 | Main을 완성하기 위한 단계 | 얇은 바 |
| 일정 | 논의 없이 날짜만 지키면 되는 것 | 왼쪽에 굵은 선이 있는 칩 |

**재분류 원칙** — 산출물이 없고 참석만 하면 되는 것은 일정입니다(예: 총 리허설). "확인"과 "확인완료 및 전달"처럼 순서가 있으면 앞이 하위, 뒤가 상위가 아니라 그 반대입니다. 같은 의뢰건이 여러 Main으로 흩어져 있으면 하나로 묶습니다.

### 4-3. 상태

| 상태 | 표현 |
|---|---|
| 대기 | 흰 바탕 + 팀 색 테두리 |
| 진행중 | 팀 색 20% 농도 채움 |
| 완료 | 회색 채움 |
| 지연 | 옅은 붉은색 + 빨간 테두리 + `지연` 배지 |

부서를 접고 펴는 것은 **사용자가 정한 것이 우선**입니다. 소속 기준 자동 접기가 매번 다시 적용되면, 남의 팀 그룹을 펴서 상태를 하나 바꿀 때마다 그룹이 닫혀 일을 할 수가 없습니다.

**완료를 눈에 띄게 하지 않습니다.** 목적이 구멍 방지라면 시선은 미완료로 가야 합니다. 일반적인 간트와 반대 방향입니다.

> **메모 (아직 고치지 않음)** — `대기·진행중·완료` 는 진척이고 `지연` 만 기한이라,
> 성격이 다른 값이 한 칸에 있습니다. `지연` 을 고르는 순간 진척 정보가 지워지고,
> `TaskRun.startedAt` 은 그 손실을 우회한 것이지 없앤 것이 아닙니다. 기한 초과를
> 날짜로 계산하게 된 지금 저장된 `지연` 은 할 일이 없습니다 — 패널에도
> "마감에서 12일 경과"(계산)와 "담당자가 지연으로 표시함"(수동)이 나란히 뜨는데
> 같은 사실의 출처가 둘입니다.
> 정리한다면 상태를 셋으로 줄이고 `지연` 은 `overdueOf` 로 칠하는 표시로만 둡니다.
> 위의 상태 표현 4종은 그대로 살고 `hasStarted` 의 보정도 필요 없어집니다.
> 상태를 다시 건드릴 일이 생길 때 함께 합니다.

### 4-4. 관련업무 표시

담당팀이 아닌 관련팀의 행에도 그 업무가 **점선 고스트 바**로 나타납니다. 왼쪽 목록에는 `↳ 업무명` 과 담당팀 표시(작은 색 점 + 회색 글씨)가 붙습니다. 담당팀 표시가 업무명보다 강하면 안 됩니다.

### 4-5. 연결 강조

업무를 클릭하면 관련 업무로 **파란 점선이 곡선으로 이어집니다.** 선택한 업무는 파란 테두리, 관련 업무는 검은 테두리입니다.

주의할 구현 지점:

- 관련 업무가 하나도 없어도 **선택 표시는 반드시 나타나야 합니다.**
- 같은 업무가 관련팀에 고스트로 중복 존재하므로, 선의 출발점은 **화면에 실제로 보이는 원본 바**여야 합니다. 첫 번째 일치 요소를 쓰면 접힌 부서의 고스트가 잡혀 선이 그려지지 않습니다.

### 4-6. 바를 끌어 날짜 옮기기

바를 잡아 좌우로 끌면 그 회차의 시작일이 바뀌고 기간은 유지됩니다. 칸에 물려 떨어집니다 — 주 단위 구간에서는 그 주로, 일 단위 구간에서는 그 날로. 주 단위 칸에 놓을 때는 **원래 요일을 지켜** 그 주 안에 놓습니다.

**어느 칸에 놓을지는 '가장 가까운' 칸으로 정합니다.** 바의 왼쪽 끝이 들어간 칸으로 정하면 좌우가 비대칭이 됩니다 — 시작할 때 왼쪽 끝이 이미 경계에 붙어 있어서 왼쪽으로는 1px 만 움직여도 넘어가고, 오른쪽으로는 한 칸 폭을 다 가야 합니다. 칸 왼쪽 모서리와의 거리로 고르면 양쪽 모두 **반 칸**을 움직여야 넘어갑니다. 끈 거리는 매번 원래 칸을 기준으로 다시 재서 오차가 쌓이지 않게 합니다.

기간(마감일)은 상세 패널에서 시작일·마감일을 직접 골라 바꿉니다. 드래그는 통째로 옮기는 것이고, 늘리고 줄이는 것은 정확한 날짜를 봐야 하는 일이라 입력으로 받습니다.

**위아래로는 움직이지 않습니다.** 행을 옮기는 것은 담당 부서를 바꾸는 것인데, 날짜를 고치는 일과 전혀 다른 결정이라 드래그로 일어나서는 안 됩니다.

바뀌는 것은 `TaskRun` 뿐입니다. 라이브러리의 기본 D-주차는 그대로 둡니다 — 한 회차에서 일정을 당겼다고 다음 회차의 기준까지 따라 움직이면 안 됩니다. 내 부서의 업무만 끌 수 있고, 고스트 바는 끌 수 없습니다(원본에서만).

### 4-7. 이동과 정렬

왼쪽 목록의 업무명을 클릭하면 그 업무 위치로 이동합니다. 순서는 **담당 부서 펼치기 → 강조 → 상세 열기 → 스크롤**입니다. 부서가 접혀 있으면 대상이 화면에 없어 위치 계산이 어긋나므로 펼치기가 먼저입니다.

스크롤 목표는 업무 시작 칸이 아니라 **한 칸 앞 날짜**입니다. 가로·세로를 한 번에 지정해야 합니다. 나눠 호출하면 뒤 명령이 앞 애니메이션을 취소합니다.

**소속을 고르면 그 부서 제목이 스크롤 맨 위로 옵니다.** 날짜 헤더가 sticky 로 위를 덮으므로 그 높이만큼 빼야 제목이 가려지지 않습니다. 목록 아래쪽 부서는 그 아래에 스크롤할 내용이 없어 올라가지 못하므로, 시트 바깥(보드 안)에 **모자란 만큼만** 여백을 만듭니다. 시트 안에 넣으면 격자선이 빈 공간까지 번집니다.

자동 스크롤은 **소속 선택에서만** 일어납니다. 부서 헤더를 직접 접었다 펴는 것은 이동이 아니라 그 자리에서 보는 동작이므로 스크롤을 건드리지 않습니다. 여기에 스크롤을 붙이면 어느 부서를 눌러도 '내 소속' 자리로 끌려가 보고 있던 위치를 잃습니다.

### 4-8. 날짜 선택 필터

상단 날짜 칸을 클릭하면 **그 날짜가 업무 기간에 포함된 업무만** 남습니다. 해당 업무가 있는 부서만 보이고 그 안에서도 걸린 업무만 펼쳐집니다. 주 단위 칸은 그 주 7일 전체가 기준입니다.

### 4-9. 상세 패널

오른쪽에서 열립니다. 열리면 **본문이 밀려서 보드가 가려지지 않아야 합니다.** 왼쪽 가장자리를 드래그해 폭을 조절합니다.

- 헤더 — 담당팀·분류·상태 칩. 상태 칩을 누르면 바로 변경.
- 기간·담당팀·담당자 — 여기서 바로 고칩니다. **담당자는 모든 업무에 답니다.** 팀만 적혀 있으면 결국 아무도 안 합니다. 고를 수 있는 사람은 그 부서 소속과 총무팀입니다
- 담당팀을 옮기면 업무가 **다른 부서의 줄로 통째로 옮겨가므로** 보드를 다시 그립니다. 넘긴 팀 사람이 담당자로 남아 있으면 뜻이 맞지 않으므로 함께 비웁니다. 넘기고 나면 넘긴 쪽은 더 이상 그 업무를 고칠 수 없습니다 — 그게 넘긴다는 뜻입니다
- 편집 칸은 **평소에 글자처럼 보입니다.** 테두리는 마우스를 올렸을 때만 나타납니다. 상시 입력처럼 보이면 읽기 위한 패널이 입력 폼으로 읽힙니다. 날짜에는 달력 아이콘을 두지 않고 **날짜를 누르면 달력이 열립니다**
- 탭 — **업무 규칙** / 논의 내역 / 달력 / 연결된 업무 / 첨부파일. 열면 업무 규칙이 먼저 보입니다.
  첨부파일은 **맨 끝**이고 개수를 함께 표시합니다
- 업무 규칙 — 이 업무를 **어떻게 진행하는지**. 논의가 이번 회차의 사정이라면, 규칙은 매번 같은
  방식으로 하기 위한 것입니다. 그래서 회차가 아니라 **라이브러리(`TaskLibrary.rules`)에 붙습니다.**
  담당자가 바뀌어도 "이건 이렇게 한다"가 사람이 아니라 기록에 남는 자리입니다
- 논의 내역 — 기존 내용에 취소선, 아래에 수정 내용을 붙이는 방식(노션에서 쓰던 관례 유지).
  **말을 바꾸는 것과 잘못 쓴 것을 고치는 것은 다릅니다.** 결정이 뒤집힌 것은 취소선 + 후속 기록으로
  남기고, 오타·오기는 그 자리에서 고칩니다. 자기가 쓴 기록만 고칠 수 있고 총무팀은 전부 고칠 수 있습니다.
  지난 회차에서 따라온 기록은 그 회차의 사실이므로 손대지 않습니다.
  대체된 기록과 후속 내용은 **한 항목 안에** 들어갑니다 — 취소선 다음에 들여쓴 `.fix` 블록.
  따로 떨어진 두 항목으로 그리면 무엇이 무엇을 고친 것인지 읽히지 않습니다.
  **날짜와 작성자는 본문 위에 한 줄로 두고, 본문은 그 아래 왼쪽 끝에서 시작합니다.**
  날짜 옆에서 시작하면 번호 매긴 목록의 첫 줄만 날짜 폭만큼 밀려 둘째 줄부터와 어긋납니다
- 논의 **입력칸은 늘 열려 있습니다.** 접어 두고 버튼으로 여는 방식은 버튼이 두 자리에
  생기고(하단 + 입력칸 안) 어느 쪽을 눌러야 할지 알 수 없게 됩니다. 그래서 하단의 `논의 추가`는
  없애고 입력칸을 상시 노출로 두었습니다. 입력칸 안의 버튼은 `저장`·`취소` 뿐입니다
- 입력칸은 **노션처럼 씁니다.** `1.` 로 시작하고 엔터를 치면 다음 번호가 자동으로 붙고,
  빈 항목에서 엔터를 치면 목록을 빠져나옵니다. `-` 뒤에 스페이스를 치면 글머리표(`•`)가 되고,
  탭으로 한 단계 들어가고(`◦` → `▪`) 시프트+탭으로 나옵니다. 업무 규칙 입력칸도 같습니다.
  **줄바꿈은 저장 후에도 그대로 보입니다** — 화면 쪽은 `white-space: pre-wrap`
- 첨부파일 — **이번 회차의 것입니다.** `TaskRun` 에 붙으므로 새 회차를 열 때 업무는
  따라와도 파일은 따라오지 않습니다. **논의 내역과 같은 취급이고, 업무 규칙과 다릅니다** —
  규칙은 "이 업무를 어떻게 하는가"라 회차를 넘어가지만(라이브러리), 첨부는 이번 회차의
  시안·견적·명단이라 다음 회차에 그대로 따라가면 **틀린 자료를 맞는 것처럼 보여줍니다.**
  탭 아래에 "이번 회차에만 남습니다. 다음 회차에는 따라가지 않습니다."를 적어 둡니다.
  파일마다 확장자·크기·올린 사람·날짜를 보이고, 마우스를 올리면 내려받기·이름변경·삭제
  메뉴가 나옵니다. 탭 안에 **끌어다 놓는 자리**가 있습니다.
  권한은 **그 업무를 고칠 수 있는 사람과 같고**, 부서는 키로 비교합니다(2장). 보는 것과
  내려받는 것은 회차를 볼 수 있는 누구나 — 논의 내역과 같은 범위입니다.
  **파일 이름을 그대로 디스크에 쓰지 않습니다** — 올린 이름은 DB 에만 남기고 실제 파일은
  임의의 이름으로 저장합니다. 경로 조작(`../`), 같은 이름끼리의 덮어쓰기, 한글 인코딩이
  한꺼번에 사라집니다. 용량과 확장자에 상한을 두고, **넘으면 무엇 때문인지 말합니다** —
  상한이 없으면 디스크가 찰 때까지 받다가 어느 날 아무 설명 없이 실패합니다.
  경로와 상한은 `app/config.py` 한 곳에 모읍니다(`ATTACHMENT_DIR` · `MAX_ATTACHMENT_BYTES` ·
  `ALLOWED_ATTACHMENT_EXTS`). 파일은 DB 밖에 쌓이므로 **백업 대상입니다** (11-2)
- 상한은 **200MB** 이고, 그 숫자는 짐작이 아니라 실제로 오간 것에서 나왔습니다.
  25MB 로 두었더니 실제로 올리는 것 — 이미지 원본·PDF·PPT — 이 막혔고 **가장 큰 것이
  164MB** 였습니다. 확장자도 그에 맞춰 늘렸습니다(RAW·TIFF·7z 등, 44가지).
  `DCB_MAX_ATTACHMENT_MB` 로 바꿉니다.

  **숫자만 올리면 안 됩니다.** 큰 파일은 세 곳에 함께 닿습니다.

  | 함께 손본 곳 | 안 하면 |
  |---|---|
  | 백업 (`scripts/backup.py`) | 매일 새벽 200MB 를 통째로 다시 묶고 30벌을 남겨 디스크가 금세 찹니다. **바뀐 것만** 복사하고(안 바뀌면 지난 zip 에 하드링크), 개수(30)뿐 아니라 **총합(`MAX_TOTAL_BYTES`)** 으로도 오래된 것부터 지웁니다 |
  | 디스크 (`scripts/healthcheck.py`) | 찰 때까지 받다가 어느 날 아무 설명 없이 실패합니다. 자가진단이 남은 공간을 보고, 올릴 때도 여유가 없으면 **이유를 말하고 거절합니다**(`DISK_FREE_FLOOR_BYTES` · `DISK_FREE_WARN_BYTES`) |
  | 올리는 동안의 화면 | 164MB 는 몇 분이 걸립니다. 아무 반응이 없으면 사람이 창을 닫습니다. 목록 **맨 위**에 퍼센트·남은 시간·취소를 띄웁니다 |

  받는 것도 **조각으로 받아 바로 디스크에 씁니다.** 200MB 를 통째로 메모리에 올리지
  않고, **중간에 끊기면 반쯤 올라간 것을 남기지 않습니다** — 취소를 누르면 브라우저가
  연결을 끊는데, 그때 남은 조각은 아무도 지우지 않습니다.

  > **터널이 그만큼을 못 실어 나릅니다 — 걸리는 것은 크기입니다.**
  > Cloudflare 무료·Pro 요금제는 **요청 본문을 100MB 로 제한**하고 Tunnel 도 같은
  > 제한을 받습니다. 넘으면 파일이 서버에 닿기도 전에 **413** 이 돌아오므로
  > 우리 응답이 아니고 `detail` 도 없습니다 — 화면이 그것을 따로 알아보고
  > 사람 말로 옮깁니다. 상한을 넘는지 **보내기 전에** 보는 것도 그래서입니다.
  >
  > **내려받기에는 이 제한이 없습니다.** 큰 것은 서버가 있는 곳에서 올려 두면
  > 바깥에서 받는 것은 됩니다. 첨부 탭에 그렇게 적어 둡니다.
  >
  > 앱 자체는 200MB 를 문제없이 받고 돌려줍니다(로컬에서 164MB 왕복 확인).
  > 그래서 상한은 200MB 로 두고, 95MB(`TUNNEL_MAX_BYTES`)를 넘으면 **막지 않고
  > 경고만** 합니다 — 집 안 회선에서는 올라가므로 거절하면 되는 것을 못 하게
  > 만듭니다.
  >
  > > 처음에는 근거를 "30초를 넘기면 끊긴다 · 2MB/s · 60MB 언저리" 로 적었습니다.
  > > 그 측정은 **로그인하지 않은 요청**이라 서버가 본문을 다 받기 전에 로그인
  > > 화면으로 답해 버린 것일 수 있어, 시간이 원인이라고 단정할 수 없었습니다.
  > > 시간으로 적어 두면 다음 사람이 상한을 60 으로 내립니다.

- **링크도 붙입니다.** 영상 원본이나 드라이브 폴더처럼 **올릴 것이 아니라 가리킬 것**이
  있기 때문입니다. `TaskAttachment.url` 이 있으면 링크이고 없으면 파일입니다 —
  같은 표에 둡니다.

  **파일과 한 목록에 섞습니다.** 나누면 "저건 어디 있더라" 를 두 번 찾게 되는데,
  담당자에게 둘은 그냥 자료입니다. 정렬도 종류를 가리지 않고 **올린 순서(최근이 위)** 입니다.

  **그러나 같아 보이면 안 됩니다.** 링크는 우리 서버에 없어서 지워지거나 권한이
  막히면 안 열리는데 **그건 우리가 어쩔 수 없습니다.** 그 차이가 눈에 보여야 하므로
  **점선 테두리와 `↗`** 로 그립니다. 둘째 줄에는 크기 대신 **도메인만** 냅니다 —
  `youtube.com` 인지 `drive.google.com` 인지가 보이면 누를지 말지 판단이 됩니다.
  전체 주소는 넣지 않습니다. 목록에서 잘려 아무 소용이 없습니다.

  **설명("무엇인가요")을 비워 둘 수 없습니다.** `drive.google.com/drive/folders/1aB3xY...`
  만 보고 아는 사람은 없습니다 — 주소만 있는 링크는 목록에 남아도 아무도 열지 않습니다.
  주소는 `http`·`https` 만 받고, 아니면 **붙는 자리 바로 아래**에 이유를 적습니다.
  멀리 적으면 어느 칸이 틀렸는지 알 수 없어 사람이 둘 다 지우고 다시 씁니다.

  회차별인 것·누가 언제 넣었는지 남는 것·지우는 권한은 **파일과 같습니다.**
  용량을 차지하지 않으므로 상한과 디스크 검사만 지나가지 않습니다.

- 탭 이름은 드로어 폭(440px)에서 다섯이 넘쳐 `논의 내역`·`연결된 업무` 를
  **`논의`·`연결`** 로 줄였습니다. **첨부파일이 맨 끝이라 잘리면 안 되므로**
  줄일 곳은 가운데입니다.
- 하단 버튼 — `하위 업무 추가` 하나뿐입니다(Phase 2 이므로 비활성 + 사유를 툴팁에).
  **`파일 첨부` 버튼은 없앴습니다** — 첨부파일 탭 안에 올리는 자리가 생겼으므로 같은 기능이
  두 군데 있게 됩니다. 논의 입력칸을 상시 노출로 바꾸면서 하단 `논의 추가`를 없앤 것과
  같은 이유입니다: 버튼이 두 자리에 있으면 어느 쪽을 눌러야 할지 알 수 없습니다
- 달력 — 업무 기간이 걸친 달을 표시. 연결된 업무의 일정은 날짜 아래 점으로.
  **이것은 업무 하나의 기간을 보는 탭입니다** — 여러 업무를 한 달력에 놓는
  화면은 따로 있습니다(4-13). 이름이 같지만 자리가 달라 헷갈리지 않습니다.
  **칸 크기는 고정입니다(34px).** 패널 폭에 따라 늘어나면 패널을 넓힐수록 달력이 커져
  한 화면에 안 들어옵니다 — 넓힌 이유가 더 보기 위해서인데 반대로 갑니다
- 연결된 업무 — **선행 / 후속 / 관련** 세 구획입니다. 각 항목을 클릭하면 **열기 / 이동** 선택 메뉴.
  선행과 후속은 여기서 바로 고칩니다. 관련은 방향이 없으므로 여기서는 보기만 합니다.
  **날짜는 회차별(`TaskRun`)이지만 관계는 라이브러리에 붙습니다** — 업무 규칙과 같은 성격이라
  다음 회차에도 그대로 따라갑니다. 그래서 "다음 회차에도 적용됩니다"를 한 줄로 밝힙니다.
  고칠 수 있는 사람은 **선행을 가진 쪽** 업무의 담당 부서와 총무팀입니다. A(스케치)가
  B(헤브론)를 기다린다고 적는 것은 A 쪽의 판단이므로 스케치가 적습니다
- 하단 고정 — 진단 패널 (4-10)

### 4-10. 진단 패널

보드 전체를 훑어 이 업무가 왜 시작되지 못하는지 판단합니다. 상태·회의록·돌발이슈로 변수가 계속 바뀌므로 주기적으로 다시 계산합니다.

**패널 이름이 판정 결과입니다.** 위에서부터 순서대로 끊습니다.

| 순서 | 조건 | 이름 |
|---|---|---|
| 1 | `status == '완료'` | **완료** |
| 2 | 분류가 `일정` | 판정하지 않음 |
| 3 | 미완료 선행이 없음 | **진행 가능** |
| 4 | 미완료 선행 있음 + 미착수 | **진행 불가** |
| 5 | 미완료 선행 있음 + 착수함 | **일부 진행 가능** |

**완료를 맨 위에서 끊는 것이 중요합니다.** 위에서부터 평가하지 않으면 선행이 미완료인
채로 끝낸 업무가 '진행 불가'로 찍힙니다.

**판정에 쓰는 값은 전부 계산값입니다.**

- 미완료 선행 = `blockedByRunIds` 가 가리키는 run 중 아직 끝나지 않은 것.
  **날짜가 지난 `일정` 선행은 끝난 것으로 봅니다** — `일정`은 산출물 없이 날짜만
  지키면 되는 것이라(4-2) 아무도 완료를 누르지 않습니다. 그러지 않으면 총 리허설을
  선행으로 둔 업무가 리허설이 지나간 뒤에도 영원히 '진행 불가'입니다
- 착수 여부는 **`TaskRun.startedAt`** 으로 봅니다. 상태로 보면 담당자가 '진행중'으로
  바꾸는 것만으로 판정이 움직입니다 — 막는 요인은 그대로인데 자기 신고로 판정이
  바뀌면 총무팀이 볼 이유가 없어집니다.
  `startedAt` 이 없던 시절의 기록만 상태로 보정하되 **인정하는 값은 '진행중'·'완료'
  뿐입니다.** '지연'은 착수 여부를 알려주지 않으므로 — 그게 `startedAt` 을 만든
  이유입니다 — 미착수 쪽에 둡니다. 그러지 않으면 기한을 넘겼고 선행도 안 끝났고
  손도 안 댄 업무, 즉 가장 위험한 조합이 '일부 진행 가능'으로 읽힙니다.
  **모르는 것은 문제를 감추는 방향이 아니라 드러내는 방향에 둡니다**
- 기한 초과는 **날짜에서 계산합니다** (`endDate < 오늘` 이고 미완료). 저장된
  `status='지연'` 은 담당자가 손으로 눌러야만 붙으므로 판정에 넣지 않고, 근거에
  "담당자가 지연으로 표시함"으로만 보여줍니다. 놓친 사람이 직접 눌러야 시스템이
  알아차리는 구조는 이 문서 0장의 판단 기준과 정면으로 부딪힙니다

**기한이 지났어도 막는 요인이 없으면 진행 가능입니다.** 진행 불가는 남을 기다리는 상태,
진행 가능인데 안 된 건 우리가 안 한 상태입니다. 총무팀의 대응이 완전히 다릅니다.
기한 초과는 판정을 바꾸지 않고 근거로만 나갑니다.

**판정하지 않는 두 경우**

- **`일정` 분류** — 산출물 없이 날짜만 지키면 되는 것(총 리허설 등)이라 "왜 시작되지
  못하는가"라는 질문이 맞지 않습니다. 판정 대신 "날짜만 지키면 되는 업무입니다"와
  남은 날짜를 보여줍니다
- **종료된 회차** — 지난 회차를 열면 모든 업무의 마감이 과거라 전부 기한 초과로
  보입니다. `endDate` 가 오늘보다 이르거나 `isArchived` 면 판정하지 않고
  "종료된 회차입니다"와 최종 상태만 보여줍니다

**근거의 정의**

| 근거 | 정의 |
|---|---|
| 선행 미완료 업무 | 제목·담당팀·상태. 선행이 기한 초과면 그것도 함께 |
| 빠진 선행 | 회차를 연 뒤 선행이 빠져 링크가 끊긴 것. **막는 것으로 치지 않고** 근거로만 |
| 날짜로 끝난 선행 | 완료가 아니라 날짜가 지나 끝난 것으로 본 `일정` 선행. 막지 않되 **그 판단이 있었다는 사실은 남깁니다** — 실제로 진행됐는지 취소됐는지는 확인된 적이 없습니다 |
| 상위 업무 상태 | 표시만. 판정에 넣지 않음 |
| 하위 업무 상태 | "하위 3건 중 1건 진행 불가". 표시만 |
| 기한 경과일수 | 계산된 `overdueDays` |
| 연쇄되는 후속 | `blocksRunIds` 는 한 홉이므로 **깊이 3까지** 따라가며, 방문한 run 을 기억해 고리에서 멈춤 |
| 부서 업무 집중도 | 같은 부서에서 기간이 겹치는 **미완료** 업무 건수 |
| 논의 번복 횟수 | `supersedesEntryId` 가 붙은 기록 수. **2회 이상**일 때만 |
| 마지막 논의 후 경과일 | 이번 회차 기록의 최신 `authoredAt` 부터. **21일 이상**이고 미완료일 때만 |
| 이번 회차 기록 없음 | 지난 회차에서 따라온 기록만 있고 이번 회차 기록이 하나도 없을 때 |

**완료와 종료된 회차에서는 연쇄 근거를 내지 않습니다.** 요약은 "후속 업무를 막고 있지
않습니다"인데 근거에 "지연 시 4건이 연쇄로 밀립니다"가 함께 뜨면 한 패널 안에서 서로
부정합니다.

**빠진 선행을 조용히 삼키지 않습니다.** 회차를 연 뒤 선행 업무가 빠지면 링크가 가리킬
곳이 없어집니다. 그냥 걸러 버리면 그 업무가 '진행 가능'으로 바뀌는데, 막는 요인이
없어져서가 아니라 안 보이게 돼서입니다.
묻는 방식이 중요합니다 — **"끊긴 링크가 있는가"가 아니라 "라이브러리의 선행 중 이번
회차에 대응하는 run 이 없는 것이 있는가"로 묻습니다.** 앞쪽으로 물으면 링크가 *애초에
만들어지지 않은* 경우(회차를 연 뒤 추가한 업무 등)가 통과해 버립니다. 그래서 회차를
연 뒤 업무를 추가할 때도 라이브러리의 선행을 run 링크로 다시 맞춥니다.

주의: **상위 업무는 선행 조건이 아닙니다.** 나를 포함하는 관계지 앞을 막는 관계가
아닙니다. 이미 완료된 후속 업무를 "연쇄 지연" 경고에 넣어서도 안 됩니다.

**논의 맥락의 절반은 계산으로 들어갔습니다.** 번복 횟수·마지막 논의 후 경과일·이번
회차 기록 없음 셋은 문장을 읽지 않고 세기만 하면 되므로 결정적으로 계산되고 테스트됩니다.
특히 **마지막 논의 후 경과일이 "'진행 가능'인데 방치된 것"과 "'진행 불가'"를 가릅니다** —
둘은 총무팀의 대응이 전혀 다른데 판정만으로는 구분되지 않습니다.

**남은 절반 — 문장을 실제로 읽는 것 — 만 LLM 후속 작업입니다.** 붙일 때 지킬 조건 넷:

1. **판정에 넣지 않습니다.** 근거로만 나갑니다. 판정이 결정적이어야 테스트가 됩니다
2. **논의 본문의 해시로 캐시합니다.** 패널을 열 때마다 돌리면 비용도 지연도 감당이 안 됩니다
3. **근거가 된 `DiscussionEntry.id` 를 함께 반환합니다.** 원문으로 갈 수 없는 요약은
   확인할 수 없고, 확인할 수 없는 근거는 신뢰를 잃습니다 (6-3과 같은 원칙)
4. **할 말이 없으면 빈 값을 냅니다.** 억지로 만들면 근거 없는 제안이 됩니다
5. **어디까지 읽었는지 화면에 적습니다.** 조건 2의 해시 캐시 때문에 새 논의가
   올라와도 요약은 옛것이 남는데, 오래된 요약과 방금 만든 요약이 화면에서 똑같이
   생겼습니다. "N월 N일 기록까지 읽었습니다"를 함께 적습니다
6. **지난 회차 기록과 이번 회차 기록을 섞지 않습니다.** `carriedFromRunId` 가 붙은
   항목은 그 회차의 사실이고 4-9에서 손대지 않기로 한 것입니다. 한 덩어리로 던지면
   작년 결정을 올해 결정처럼 말합니다 — 이 시스템에서 가장 값비싼 오류입니다.
   이번 회차 기록만 읽히거나, 최소한 어느 회차 것인지 문장에 붙어야 합니다
7. **판정 단어를 출력에서 금지합니다.** 조건 1은 코드 경로 이야기입니다. 요약이
   "진행 불가로 보입니다"라고 쓰면 코드가 판정에 넣지 않아도 사람은 판정으로
   읽습니다. 패널 이름이 판정 결과인 화면이라 더 그렇습니다
8. **실패해도 패널이 살아 있어야 합니다.** 진단 패널은 상세 패널 하단 고정이라
   업무를 열 때마다 부릅니다. API 가 느리거나 죽었을 때 근거 한 줄이 비는 것으로
   끝나야 합니다 — 판정과 나머지 근거까지 사라지면, 결정적인 절반을 애써 분리한
   이유가 없어집니다

**논의 요약의 열람 범위는 원문과 같습니다.** 요약은 흩어진 것을 한 문단으로 모아
주므로 원문이 보인다는 것과 성격이 다릅니다.

### 4-11. 알림 (웹 푸시)

여기까지 만든 것은 전부 **사람이 화면을 열었을 때** 답하는 것이었습니다. 푸시는
시스템이 먼저 말을 겁니다. 그래서 잘못 만들면 되돌리기 어렵습니다 — 틀린 알림을
세 번 받은 사람은 그 뒤 알림을 보지 않고, **한 번 무시하기 시작하면 돌아오지
않습니다.** 보내지 않는 쪽으로 기울여 만듭니다. 놓치는 것보다 신뢰를 잃는 쪽이 비쌉니다.

**판정은 4-10 을 그대로 씁니다.** 푸시 전용 판정 규칙을 새로 쓰면 두 벌이 되어
화면과 알림이 어긋납니다.

**받는 사람은 담당자 한 사람입니다.** 부서 리더나 총무팀에 함께 보내지 않습니다 —
셋 다 받으면 아무도 자기 일로 받지 않습니다. 담당자가 없는 업무만 예외로 그 부서의
`dept_lead`, 그마저 없으면 총무팀으로 올립니다. **그 리더를 찾을 때 부서를 `key` 로
봅니다**(2장) — `Department.id` 로 찾으면 새 회차가 열리는 순간 리더를 못 찾고 조용히
총무팀으로 떨어져, 담당자 미지정 업무가 한 사람에게 몰립니다. 하지 않기로 한 바로 그
상태인데 아무 오류도 나지 않습니다.

**'진행 불가'는 그 담당자에게 보내지 않습니다.** 기다리는 중인데 재촉받는 것이
되기 때문입니다. 대신 **선행을 쥔 업무의 담당자**가 받습니다 — "'명찰 제작'(스케치팀)이
이 업무를 기다리고 있습니다". 기다리는 사람을 재촉하면 알림 전체의 신뢰가 깨집니다.

**매일 아침, 사람당 한 통입니다.** 업무마다 따로 보내지 않습니다.

| 종류 | 조건 |
|---|---|
| 방치 | 판정이 `진행 가능`이고 미착수인 채로 시작 예정일에서 7일 경과. **막는 요인이 없다는 것이 확인된 상태라 가장 정확한 신호입니다** |
| 기한 임박 | 미완료이고 마감까지 3일 이하. 판정과 무관 |
| 기한 초과 | `overdueOf` 로 계산. 저장된 '지연'은 보지 않습니다 |
| 선행 재촉 | 내 업무가 남의 업무를 막고 있고 그쪽 판정이 `진행 불가`일 때 |

보내지 않는 것 — 종료·보관된 회차, `일정` 분류, 완료된 업무, 그리고 **`진행 불가`인
업무를 그 담당자에게**.

**총무팀에 함께 보내는 경우는 하나뿐입니다** — 기다리는 쪽이 기한을 넘겼을 때.
`진행 불가`는 그 담당자에게 가지 않으므로, 선행 재촉 문구에 기한이 없으면 아무도
급한 줄 모릅니다. 그래서 "…이 업무를 기다리고 있습니다 — 그쪽 마감이 5일 지났습니다"
로 적고, 이때만 총무팀에도 같은 항목을 올립니다. 나머지는 담당자 한 사람 원칙 그대로입니다.

**같은 말을 반복하지 않습니다.** 같은 (사람, 업무, 종류)는 7일 안에 다시 보내지
않되, 그 업무의 상태나 판정이 바뀌면 간격을 초기화합니다 — 사정이 달라진 것이므로
같은 말이 아닙니다. 한 통에 담는 것은 5건까지이고 넘으면 "외 N건"으로 자릅니다.
스무 줄짜리 알림은 아무도 읽지 않습니다. **보낼 것이 없으면 보내지 않습니다** —
"오늘은 없습니다"를 보내지 않습니다.

**문구는 재촉하지 않습니다.** "아직 안 하셨습니다"가 아니라 "막는 요인이 없습니다 —
시작할 수 있습니다". 판정 단어는 화면과 같은 말을 쓰고 다른 이름을 지어내지 않습니다.

**스케줄러를 앱 안에 넣지 않습니다.** 하루 한 번 `POST /admin/notify/run` 을 바깥
스케줄러가 부릅니다. 앱이 시간을 재기 시작하면 껐다 켤 때마다 동작이 달라집니다.
`GET /admin/notify/preview` 는 **보내지 않고** 오늘 누구에게 무엇이 갈지만 보여줍니다 —
첫 회차에는 이것만 켜고 문구와 기준을 다듬습니다. 몇 주 동안 매일 볼 화면이라 **사람이
읽는 화면**이고, 받는 사람별로 **실제로 갈 문구 그대로** 보여줍니다. 구독자가 0명이면
"지금 실행해도 아무에게도 가지 않습니다"를 눈에 띄게 적습니다. 기계가 읽을 것은
`?format=json` 입니다.

**구독은 기본이 꺼짐이고 사용자가 켜야 받습니다.** VAPID 키가 없으면 푸시만 꺼진 채로
앱이 뜹니다 — 키가 없다고 서버가 죽으면 안 됩니다. 알림을 누르면 그 업무의 상세 패널이
열린 보드로 갑니다.

**"보냈는가"와 "구독을 지울까"는 다른 질문입니다.** 전송 결과를 셋으로 나눕니다 —
성공 / 일시 실패(구독 유지) / 만료(410·404, 구독 삭제). 둘을 한 값에 담으면 서버가
다 죽어 있어도 관리자 화면에 "실제 발송 12명"이라고 나옵니다. **일시 실패는 보낸 것으로
세지 않고, 보내지 못한 묶음은 기록하지 않습니다** — 기록하면 그 항목들이 7일간
침묵해서, 구독자가 0명일 때 한 번 잘못 부르면 그 주의 알림이 통째로 소진됩니다.

> **HTTPS 가 있어야 켜집니다** — 웹 푸시는 브라우저 규격상 **HTTPS 또는 localhost**
> 에서만 동작합니다. Cloudflare Tunnel 이 HTTPS 를 붙여 주므로(11장) 배포한 뒤에는
> 휴대폰에서도 구독이 됩니다. 다만 같은 와이파이의 `http://192.168.x.x` 로 들어오면
> 여전히 구독되지 않으니, **터널 주소로 접속하도록 안내**합니다.
>
> 발송은 **당장 켜지 않습니다.** 작업 스케줄러에 등록만 해 두고 '사용 안 함' 으로 둔 뒤,
> 몇 주 동안 미리보기로 문구와 기준을 지켜본 다음 켭니다.

> **VAPID 키는 백업 대상입니다.** 키 파일이 없으면 새로 만드는데, 데이터 폴더를
> 옮기거나 잃으면 **새 키가 생기고 기존 구독이 전부 조용히 죽습니다** — 아무 오류도
> 나지 않고 그냥 안 갑니다. SQLite 파일과 함께 백업합니다.

**앱 알림함과 묶음은 별개 경로입니다.** 이전 설계의 `Notification`·`push_notifications`
는 알림함에 쌓인 것을 보내는 길이고, 4-11 의 묶음은 그 자리에서 계산해 바로 보냅니다 —
저장할 이유가 없기 때문입니다. 다만 그래서 **전송에 실패하면 사람이 확인할 흔적이
남지 않습니다.** 어느 쪽으로 정리할지는 실제로 배포해 본 뒤에 정합니다. 지금 합치지
않습니다.

### 4-12. 계정과 초대

**SMS 를 접은 이유** — 발신번호 사전등록에 서류와 승인 기간이 필요한데, 그 일정이
**2027년 8월 여름수련회** 준비에 맞지 않았습니다. 쓸 사람이 19명이고 총무팀이 그분들 연락처를
이미 알고 있으므로, 인증 절차를 하나 더 두는 것보다 총무팀이 직접 링크를 보내는 편이
빠르고 덜 막힙니다. 1장 확정사항을 이에 맞춰 고쳤습니다.

**총무팀이 계정을 만들고 1회용 링크를 발급합니다.** `/admin/users` 에서 하고,
카카오톡으로 직접 전달합니다. 링크를 열면 계정이 활성화되고 그 기기에 **90일 세션**이
붙습니다. 기기를 잃으면 총무팀이 재발급합니다.

**링크는 완성된 채로 나옵니다. 자리표시자를 남기지 않습니다.** 전에는 주소 앞부분을
자리표시자로 찍어 두고 사람이 매번 손으로 갈아 끼웠는데, 그러다 **토큰까지 건드려
링크가 깨졌습니다** — 하루에 대여섯 번씩 그랬습니다. 그때마다 재발급하면 앞의 링크가
죽으므로 **어느 것이 살아 있는지도 알 수 없게 됩니다.**

- 바깥 주소는 `DCB_BASE_URL` 로 정하고, 없으면 `app/config.py` 의 기본값을 씁니다.
  앱은 `127.0.0.1` 에만 열려 있어서 서버가 자기 주소로 아는 값(`request.base_url`)은
  바깥 주소가 아닙니다 (11-2) — 그걸 복사해 보내면 받는 사람 브라우저에서 열리지 않습니다
- 주소를 만드는 곳은 `domain/auth.invite_url()` **하나**입니다. 화면과 스크립트가
  같이 씁니다 — 두 곳에서 만들면 한쪽만 고쳐집니다
- **발급하면 그 사람의 옛 링크는 죽습니다.** 원래도 그랬지만 화면에 그 말이 없어서
  총무팀이 "아까 보낸 링크로 들어가세요" 라고 안내하게 됐습니다. 그래서 발급할 때마다
  **"이전에 발급한 링크는 이제 쓸 수 없습니다"** 를 함께 적습니다 — 화면과 스크립트 둘 다

**토큰 원문을 저장하지 않습니다.** `secrets.token_urlsafe` 로 만들고 **해시만** 남기며,
원문은 발급 화면에서 한 번만 보여줍니다. DB 파일이 새면 링크가 그대로 새는 구조를
만들지 않기 위해서입니다 — 비밀번호를 평문으로 두지 않는 것과 같은 이유입니다.
그래서 "지난번 그 링크 다시 보여주세요"가 안 되고, 재발급만 됩니다.

**유효기간 7일, 한 번 쓰면 만료.** 재발급하면 남아 있던 링크는 함께 취소됩니다 —
그러지 않으면 "한 번 쓰면 만료"가 뜻을 잃습니다.

**삭제는 두지 않고 비활성화만 둡니다.** 지난 회차의 논의와 지출에 그 사람이 작성자로
남아 있어서, 계정을 지우면 그 기록이 "누가 썼는지 모르는 것"이 됩니다. 비활성화하면
로그인만 막히고 기록은 그대로 남으며 살아 있던 링크도 함께 취소됩니다.

**다만 비활성 계정이 연락처를 붙들면 정리를 끝낼 수 없습니다.** 중복을 정리한 뒤
남긴 계정에 실제 번호를 넣으려는데 비활성화한 쪽이 그 번호를 쥐고 있으면 막힙니다.
그렇다고 삭제로 풀면 위의 이유가 무너집니다 — **막힌 것은 삭제가 없어서가 아니라
비활성 계정이 번호를 놓을 방법이 없어서였습니다.**

**로그인은 초대 링크로 하지 번호로 하지 않으므로, 비활성 계정에게 번호는 필요
없습니다.** 그래서 비활성화할 때 번호를 놓고 `retired_phone` 에 옮겨 둡니다.

- **되살릴 때 그 사이 아무도 안 쓰고 있으면 돌려줍니다.** 누가 쓰고 있으면
  되돌리지 않고 **누가 쓰고 있는지 말합니다** — 조용히 빈 채로 남으면 왜 없어졌는지
  아무도 설명하지 못합니다
- **화면에는 원래 번호를 흐리게 남깁니다**(`010-XXXX-XXXX (반납)`). 빈 칸으로 두면
  그 계정이 누구였는지 알 수 없습니다. 고치려면 먼저 되살려야 합니다
- **겹침 검사는 번호를 쥔 계정끼리만 합니다.** 비활성 계정은 쥐고 있지 않습니다
- **비활성화·활성화가 활동 기록에 번호까지 남습니다** —
  `박민준: 비활성 · 연락처 010-XXXX-XXXX 반납` /
  `박민준: 활성 · 연락처 010-XXXX-XXXX 되돌림` / `… 되돌리지 못함(박서진 사용 중)`
- 번호가 비어도 **초대 링크는 멀쩡합니다** — 링크는 계정에 붙습니다

> **DB 쪽** — `phone_number` 는 쓰던 SQLite 파일에서 `NOT NULL` 이라 빈 문자열로
> 놓습니다(표를 다시 만들지 않기 위해서). 전체 유니크 인덱스는 **번호를 쥔 행만**
> 보는 조건부 인덱스(`ix_users_phone_held`)로 바꿉니다 — 겹치면 안 되는 것은
> 번호를 실제로 쥔 계정끼리이지 "번호가 없는 계정" 끼리가 아닙니다.
> 이 규칙이 생기기 전에 비활성화된 계정들은 앱이 뜰 때 **한 번 정리**되고,
> 무엇을 옮겼는지 로그에 남습니다.

**부서는 키로 붙입니다**(2장). 화면의 선택지도 키를 값으로 씁니다 — `Department.id`
로 붙이면 새 회차가 열리는 순간 소속이 끊깁니다.

**연락처도 화면에서 고칩니다.** 잘못 들어간 번호를 바로잡을 길이 없으면 계정을
새로 만들게 되고, 그것이 중복의 또 다른 입구입니다.

다만 **연락처는 계정을 구분하는 열쇠입니다.** 겹치면 **조용히 엉뚱한 계정에 링크가
갑니다** — 총무팀은 이름을 보고 발급하는데 시스템은 번호로 사람을 찾기 때문입니다.
그래서 고칠 수는 있되 이렇게 막습니다.

- **이미 다른 계정이 쓰는 번호로는 바꿀 수 없습니다.** 거절하면서 **누구 것인지**
  말합니다. **비활성 계정이 쓰고 있어도 마찬가지입니다** — 지금은 로그인이 막혀
  있어도 나중에 되살리면 그때 겹치고, 그때는 왜 겹쳤는지 아무도 기억하지 못합니다
- **빈 값으로 바꿀 수 없습니다.** 연락처가 없으면 나중에 그 계정을 찾을 길이 없습니다
- **부서·권한과 같은 저장 버튼**으로 함께 저장합니다. 칸을 따로 두면 어느 것이
  저장됐는지 헷갈립니다
- **바뀐 것만 활동 기록에 남깁니다.** 안 바뀐 연락처가 기록에 남으면 나중에
  "이때 번호를 건드렸나" 를 다시 따져야 합니다
- **바꿔도 살아 있는 초대 링크는 죽지 않습니다** — 링크는 계정에 붙지 번호에 붙지
  않습니다. 번호를 고쳤다고 이미 보낸 링크가 막히면 또 재발급하게 됩니다

**세션 서명키는 고정합니다.** 매번 새로 만들면 서버를 재시작할 때마다 전원이
로그아웃됩니다. `DCB_SECRET_KEY` 가 있으면 그것을, 없으면 데이터 폴더의
`secret_key.txt` 를 씁니다. 이 파일도 백업 대상입니다 (11-2).

**첫 관리자만 예외입니다.** `/admin/users` 에 들어가려면 이미 관리자여야 하므로,
맨 처음 한 사람은 `scripts/create_admin.py` 로 만듭니다.

**링크가 필요할 뿐인데 계정을 만들게 하면 중복이 쌓입니다.**

실제로 관리자 계정이 넷이 됐고 그중 셋이 같은 이름이었습니다. 초대 링크는 **한 번
쓰면 만료**되므로 재발급이 자주 필요한데, **링크만 다시 받는 길이 눈에 띄지 않으면
사람은 계정을 다시 만듭니다.** 안내서의 예시 번호(`01012345678`)가 그대로 들어가
계정이 하나 더 생기기도 했습니다 — 그래서 문서와 도움말에서 예시 번호를 뺐습니다.

**중복 계정은 조용한 문제가 아닙니다.** 총무팀 에스컬레이션은 `admin` 전원에게
가므로(4-11) 같은 사람에게 알림이 그 수만큼 갑니다. 담당자를 고를 때도 같은 이름이
여럿 뜹니다.

그래서 셋을 둡니다.

- **같은 연락처면 새로 만들지 않고** 링크만 다시 발급합니다
- **같은 이름의 관리자가 있으면 멈추고** 기존 계정을 보여줍니다.
  동명이인이 있을 수 있으므로 `--force` 로는 만들 수 있습니다
- **`--reissue <연락처>` 로 계정을 만들지 않고 링크만 받습니다.**
  화면 안내에도 이 길을 적습니다 — 보이지 않으면 없는 것과 같습니다

**이미 생긴 중복은 `scripts/merge_users.py` 로 정리합니다.** 기본은 미리보기이고,
`--keep <id> --apply` 로 남길 계정을 정하면 나머지를 **비활성화만** 합니다.

- **지우지 않습니다** — 지난 회차의 논의와 지출에 작성자로 남아 있습니다
- **기록을 옮기지 않습니다.** 옮기면 "누가 썼는가" 가 사실과 달라집니다 —
  그건 정리가 아니라 조작입니다. 어느 계정에 무엇이 달려 있는지는 세어서
  보여주기만 하고, 남길 계정을 고르는 것은 사람이 합니다

**자가진단이 중복을 알립니다.** 같은 이름의 관리자가 둘 이상이면 `[문제]` 로
냅니다 — 예전에는 정확히 이 상태에서 `[정상]` 이 떴습니다.

### 4-13. 달력 보기

구현: `app/domain/calendar.py` · `app/routers/calendar.py` · `templates/calendar.html`

준비 보드는 D-주차 타임라인이라 **"전체가 어떻게 흘러가나"** 를 봅니다.
담당자가 **"이번 주에 내가 뭘 해야 하나"** 를 보기에는 불편합니다.
그 자리를 이 화면이 맡습니다. 사이드바의 `준비 단계` 바로 아래입니다.

> **4-9 의 `달력` 탭과 다릅니다.** 그것은 상세 패널 안에서 **업무 하나의 기간**을
> 보는 것이고, 이 화면은 **여러 업무를 한 달력에 놓는 것**입니다. 이름은 같지만
> 자리가 달라 헷갈리지 않습니다 — 하나는 패널 안의 탭, 하나는 사이드바의 화면입니다.

> **패널은 보드와 같은 것을 씁니다** — 화면도 코드도 한 벌입니다. 아래 "점을 누르면" 을 보세요.

#### 마감일에 점 하나. 기간 띠가 아닙니다

**기간 전체를 띠로 그리지 않습니다.** 포스터 제작이 15일짜리인데 띠로 그리면
한 주에 몇 개만 있어도 달력이 꽉 차서, 정작 무엇이 급한지 안 보입니다.
그리고 담당자에게 중요한 것은 기간이 아니라 **언제까지 끝내야 하는가**입니다.
기간을 보고 싶으면 보드가 그 일을 이미 합니다 — 두 화면이 같은 것을 두 방식으로
보여줄 이유가 없습니다.

- 준비 업무(`TaskRun`)만 놓습니다. 수련회 프로그램은 `/live` 가 합니다
- `included=false` 는 놓지 않습니다
- **`일정` 분류도 놓습니다.** 날짜만 지키면 되는 것이지만 날짜는 있습니다
- 한 날에 여럿이면 세로로 쌓고, **3건을 넘으면 `외 N건` 으로 접습니다.**
  다 펼치면 그 주만 세로로 길어져 달력으로 읽히지 않습니다

#### 날짜 없는 업무를 따로 보여줍니다

**마감일이 없는 업무는 달력 아래에 모아 둡니다.** 놓을 자리가 없다고 조용히
빼면 **그것이 정확히 놓치는 지점**입니다 — 날짜가 없어서 아무 화면에도 안 뜨는
업무야말로 아무도 안 챙깁니다. `날짜가 없는 업무 N건` 으로 접어 두고, 펴면
목록과 함께 "보드에서 기간을 정해 주세요" 를 적습니다.

4-10 에서 빠진 선행을 조용히 삼키지 않기로 한 것과 같은 자리입니다.

#### 점의 생김새는 보드의 규칙을 그대로 씁니다

`board.paint_of()` 를 그대로 부릅니다 — 색·테두리·기한 초과·툴팁 문장이
전부 거기서 나옵니다.
**두 벌이 되면 반드시 어긋나고, 어긋난 쪽을 아무도 눈치채지 못합니다.**

- 부서 색은 왼쪽 3px 마개에만 씁니다 (4-0)
- 상태 4종을 구분합니다 (4-3). 완료는 회색에 취소선으로 **눈에 띄지 않게**
- **기한 초과는 날짜에서 계산합니다** — 저장된 `status='지연'` 이 아니라
  `overdue_of` 입니다. 놓친 사람이 직접 눌러야 알아차리는 구조를 만들지
  않습니다 (4-10)
- 오늘 칸에 파란 테두리
- **점에 마우스를 올리면 그 업무의 기간이 비칩니다.** 마감일에 점 하나는
  그대로입니다 — 기간을 늘 띠로 그리면 달력이 꽉 차서 무엇이 급한지 안
  보이는데, 마감일만으로는 "언제부터 하는 일인지" 를 알 수 없습니다.
  한 번에 하나만 뜨고 손을 떼면 사라지므로 위의 판단에 걸리지 않습니다.
  색은 **무채색**입니다 (4-0) — 부서 색으로 면을 채우면 정작 눈에 띄어야 할
  선택 테두리와 지연 배지가 묻히는데, 비침이 뜨는 순간이 바로 그 둘을 봐야
  하는 순간입니다. 좁은 화면에서는 하지 않습니다(날짜 격자도 마우스도 없습니다).

  > **점을 놓는 것과 비추는 것은 범위가 다릅니다.** 점은 **이 달**에만
  > 놓습니다(`:not(.out)`) — 옆 달 칸에 점을 놓으면 같은 업무가 두 달에
  > 두 번 보입니다. 그런데 비침은 **격자에 보이는 칸까지** 칠합니다.
  > 10월 격자의 왼쪽 끝에 9월 말 며칠이 보이는데, 기간이 거기 걸쳐 있을 때
  > 그 칸만 비워 두면 **띠가 중간에 끊겨 고장난 것처럼 읽힙니다.**
  > 격자를 넘어가는 쪽은 끝을 각지게 두어 잘렸다는 것을 보이고, 정확한
  > 날짜는 점의 툴팁에 적습니다.

**점을 누르면 그 자리에서 상세 패널이 열립니다.** 달력은 그대로 있고 오른쪽에
겹쳐 뜹니다. 닫으면 보던 달·범위 칩·`미완료만` 이 그대로입니다.
`/calendar?task=<run_id>` 로 들어오면 열린 채로 시작합니다.

전에는 `/board?task=` 로 넘겨 보냈습니다. 그 판단의 이유는 게을러서가 아니라
**패널을 한 벌만 두려는 것**이었습니다 — 두 벌이 되면 논의·상태·첨부·선후행이
두 곳에서 갈리고, **갈린 쪽을 아무도 눈치채지 못합니다.** 이 프로젝트에서 가장
자주 고쳐 온 문제입니다.

그런데 넘겨 보내면 **보던 달을 잃습니다.** 담당자가 "이번 주에 내가 뭘 해야 하나"
를 보려고 연 화면인데, 하나 확인할 때마다 보드로 갔다가 뒤로 돌아와야 합니다.

**그래서 패널을 옮기지 않고 나눴습니다.** 여전히 한 벌입니다.

| 한 벌인 것 | 어디 |
|---|---|
| 패널의 화면 | `templates/partials/drawer.html` — 보드와 달력이 함께 include |
| 패널을 움직이는 코드 | `static/js/drawer.js` — 탭·상태·논의·첨부·선후행·진단 전부 |

화면마다 다른 것은 **자기 화면을 어떻게 고쳐 그리는가** 뿐이고, 그것만
`Drawer.init({...})` 로 알려 줍니다 — 보드는 바를, 달력은 점을 고쳐 칠합니다.
상태를 바꾸면 **다시 불러오지 않고** 점만 바뀝니다. 다시 불러오면 보던 달을 잃습니다.

- 달력에는 **`이동` 메뉴를 내지 않습니다** — 스크롤해서 갈 자리가 없습니다.
  눌러도 아무 일이 없는 항목을 보여줄 이유가 없습니다
- 좁은 화면에서는 패널이 전체를 덮습니다 (보드에서 하던 방식 그대로)
- 점의 `href` 는 그대로 두었습니다 — 가운데 버튼으로 새 탭에 열거나 자바스크립트가
  죽었을 때 보드로 가는 길이 남습니다

**`docs/checks/drawer.js` 를 두 화면에서 각각 돌립니다.** 패널이 한 벌이면 양쪽에서
같이 통과해야 합니다 — **한쪽만 통과하면 실제로는 한 벌이 아니라는 뜻입니다.**
스크립트가 어느 화면인지 알아보고, 여는 자리(바/점)와 "보던 자리"(부서 그룹/보던 달)를
그 화면에 맞춰 봅니다.

#### 범위 — **보드와 같은 것을 씁니다**

`내 것` · `전체` · **그 회차의 부서 전부**를 고릅니다. **기본은 `내 것`** —
담당자가 "내가 뭘 해야 하나" 를 보려고 여는 화면이기 때문입니다.

> 처음에는 `내 것 / 우리 부서 / 전체` **칩 셋**이었습니다. 그런데 **어느
> 부서를 고를 수가 없었습니다** — 총무팀장이 스케치팀 것만 보려 해도 방법이
> 없었습니다. 보드는 이미 드롭다운으로 어느 부서든 골랐으므로, **새로 만들지
> 않고 그것을 꺼내 둘이 함께 씁니다** (`templates/partials/deptpick.html`).
> 두 벌이 되면 한쪽만 고쳐집니다.

- **부서 키가 그대로 범위 값입니다** (2장). `Department.id` 로 두면 새 회차가
  열리는 순간 고른 것이 엉뚱한 부서를 가리킵니다 — 값 자체를 키로 두면 그
  실수를 할 자리가 없어집니다
- **옛 값(`dept`)은 내 부서 키로 옮겨 줍니다.** 쿠키에 남아 있을 수 있는데,
  모르는 값이라고 `내 것` 으로 떨어뜨리면 어제까지 우리 부서를 보던 사람이
  오늘 갑자기 자기 것만 보게 되고 **왜인지 알 수 없습니다**
- 고르면 **서버에 다시 물어봅니다.** 보드는 그 자리에서 흐리게 하면 되지만
  달력은 `외 N건`·위쪽 건수·날짜 없는 업무 수를 **다시 세어야** 합니다 —
  화면에서 감추기만 하면 그 숫자들이 옛것으로 남습니다
- `미완료만` 은 **칩으로 남깁니다.** 고르는 축이 다릅니다
- **고른 것을 기기에 남깁니다.** 담당자는 늘 같은 것을 보러 오므로 매번
  다시 고르게 하지 않습니다. 달을 넘기는 링크에도 지금 값을 달고 다닙니다 —
  쿠키에만 기대면 링크를 복사해 준 사람이 다른 화면을 봅니다

#### 달과 이동

월 단위입니다. 처음 열면 **오늘이 든 달**이고, 오늘이 회차 기간 밖이면
**회차가 시작하는 달**입니다 — 지난 회차를 열었는데 빈 달이 뜨면
"업무가 없나" 로 읽힙니다. **회차 기간을 벗어난 달로도 갑니다. 막지 않습니다.**

> 달 값은 `2026-11` 도 `2026-11-01` 도 받습니다. 화면이 내보내는 것은
> 뒤엣것이라, 앞엣것만 받으면 **이전/다음 달이 조용히 기본 달로 돌아옵니다.**

#### 휴대폰

좁은 화면(820px 아래)에서는 **주 단위 목록**으로 바꿉니다 — 날짜별로 업무를
세로로 쌓습니다. 7칸 달력에 글자를 억지로 우겨넣지 않습니다. 봉사자 시간표가
같은 표를 가로로 넘기게 둔 것(5-8)과는 반대 판단인데, 그쪽은 **같은 표를 보는
것이 목적**이고 이쪽은 **내 할 일을 읽는 것이 목적**이라 그렇습니다.

---

## 5. 수련회 진행

시각 스펙: `retreat-live.html` · 화면 `/live` · 구현 `app/domain/live.py` · `app/routers/live.py`

### 5-0. 이 화면만 조건이 다릅니다 — 휴대폰

**준비 보드는 몇 달에 걸쳐 천천히 보는 것이지만, 이 화면은 현장에서 휴대폰으로
급히 보는 것입니다.** 사람이 뛰어다니면서 한 손으로 누릅니다. 그 전제로 만들었고,
다른 화면의 판단을 그대로 옮겨오면 안 되는 자리가 몇 군데 있습니다.

- **좁은 화면(820px 아래)에서는 프로그램 목록과 체크리스트를 한 번에 하나씩** 보여줍니다.
  좌우 2단이 들어가지 않습니다. 프로그램을 누르면 체크리스트로 넘어가고 위에 고정된
  `← 프로그램 목록` 으로 돌아옵니다
- **체크 버튼은 44px 입니다.** 뛰면서 누르는 것이라 글머리표 크기로는 안 됩니다
- **체크는 한 번 누르면 바로 반영됩니다.** 저장 버튼을 두지 않습니다
- **누른 것을 화면에 먼저 반영하고 서버에는 뒤따라 보냅니다.** 현장 네트워크가 느려도
  손이 멈추지 않아야 하기 때문입니다. 실패하면 **되돌리고 화면에 말합니다** —
  조용히 삼키면 눌렀다고 믿은 채로 넘어갑니다. 현장에서는 콘솔을 아무도 안 봅니다

### 5-1. 구조

- 상단 — 일자 탭(선발대·1일차·2일차·폐회) + 당일 진행률
- 왼쪽 — 시각순 프로그램 목록. 현재 시각 위치에 `NOW` 선. 각 프로그램에 완료 현황(`3/8`)
- 오른쪽 — 선택한 프로그램의 **전 / 중 / 후** 체크리스트

화면을 열면 진행 중인 프로그램이 자동 선택됩니다. 진행 중인 것이 없으면(사이 시간이거나
**오늘이 회차 기간 밖이면**) 첫 프로그램을 고릅니다 — 기간이 지난 회차를 열어도
화면이 죽으면 안 됩니다.

**일자의 날짜를 저장하지 않습니다.** `day` 이름만 남기고 회차의 개회일에서 매번
계산합니다 — 라이브러리가 절대 날짜를 갖지 않는 것과 같은 이유로, 개회일이 바뀌면
프로그램표도 따라 움직여야 합니다.

```
선발대 = 개회일 하루 전 · N일차 = 개회일부터 폐회 전날까지 · 폐회 = 폐회일
```

회차 길이가 줄어 목록에 없는 이름이 남으면 **버리지 않고 뒤에 붙입니다.**
그 날의 프로그램이 조용히 사라지면 아무도 알아차리지 못합니다.

**프로그램을 만들 때 셋을 정합니다.** 이 셋이 봉사자 시간표(5-8)에서 그
프로그램이 **어느 칸에 어떻게 서는지**를 정합니다. 화면(`/live`)의 프로그램
추가·수정 창에서 고르고, 각 값의 뜻을 옆에 한 줄씩 적어 둡니다 — `staff` 를
보고 무엇인지 아는 사람은 이걸 만든 사람뿐입니다.

| 값 | 고르는 것 | 기본 |
|---|---|---|
| `audience` | 참가자와 함께(`all`) / 봉사자만(`staff`) | `all` |
| `track` | 정규일정(`main`) / 총무팀 작업(`ops`) | `main` |
| `parallel` | 나란히 도는 프로그램인가 (새친구 등) | 꺼짐 |

**가장 흔한 경우가 기본입니다** — 참가자와 함께하는 정규일정. 대부분의
프로그램이 그것이라, 기본이 그쪽이어야 대부분의 경우 아무것도 고르지 않아도
맞습니다. 반대로 두면 매번 두 번씩 눌러야 하고, 안 누르면 조용히 틀립니다.

**모르는 값을 조용히 기본값으로 바꾸지 않습니다.** 그러면 화면에서 고른 것과
저장된 것이 달라지는데 아무 표시도 나지 않습니다. 값이 이상하면 거절합니다.

**지난 회차에서 복사해 올 때 셋이 따라옵니다**(5-5). 처음엔 빠뜨려서
복사해 온 회차의 표가 통째로 틀렸는데, 값이 기본값으로 채워지는 바람에
**아무 오류도 나지 않았습니다.** `end_time` 도 함께 옮깁니다.


### 5-2. 전 / 중 / 후

| 구간 | 의미 |
|---|---|
| 준비(전) | 프로그램 시작 전에 끝나 있어야 하는 것 |
| 진행(중) | 프로그램이 도는 동안 챙길 것 |
| 정리(후) | 끝나고 반드시 처리할 것 |

### 5-2-1. 그 안에서 다시 — 팀 단위 / 개인 단위

각 구간 안에서 **팀이 통째로 움직이는 것**과 **개인에게 붙는 것**을 나눕니다.

```
[준비]
  팀   · 코람데오 리허설 시작 (18:00)
  팀   · 송출시작 & 18:50 스탠바이
  개인 · 나윤 — 진행자 물 세팅
  개인 · 전체 — 의자정렬
```

**구간이 먼저고 범위가 나중입니다.** 현장에서 먼저 묻는 것은 "지금 뭘 해야 하나"
이지 "이게 팀 일인가 내 일인가" 가 아닙니다. 범위를 바깥 묶음으로 두면 준비·진행·정리가
흩어져서, 프로그램이 시작되기 전에 끝나 있어야 할 것을 한눈에 볼 수 없습니다.

- **팀 단위를 위, 개인 단위를 아래에** 둡니다 — 팀이 움직여야 개인 일이 돌아갑니다
- 팀 단위는 담당 이름을 뒤에 붙이고, 개인 단위는 **이름을 앞세웁니다**.
  팀은 "누가 움직이나", 개인은 "누구 일인가" 가 먼저입니다
- 덩어리 라벨은 작게 둡니다. 구간 제목보다 커지면 무엇이 먼저인지 뒤집힙니다(4-0)
- **한쪽이 비면 그 라벨도 내지 않습니다.** 라벨만 있는 빈 칸은 잡음입니다

**파트 칩과 별도로 범위 칩(`전체` · `팀 단위만` · `내 것만`)이 있고 둘은 함께
걸립니다.** 파트는 "무슨 일인가", 범위는 "팀이 움직이는가 내가 하는가" 라 서로 다른
축이라서 한 줄에 섞지 않습니다. 헤브론 리더는 칩 하나로 팀 움직임만 볼 수 있고,
총무팀원은 자기 파트의 개인 항목만 볼 수 있습니다. 두 종류가 다 있을 때만 칩을 냅니다.

**왼쪽 프로그램 목록의 `3/8` 은 범위로 나누지 않고 전체로 셉니다.** 그 숫자는
"이 프로그램이 얼마나 준비됐나" 이지 "내 것이 얼마나 남았나" 가 아닙니다.

> **`scope` 를 파트에서 계산하지 않고 컬럼으로 둡니다.**
> 헤브론·코람데오가 팀이고 총무팀 파트가 개인인 것은 **대체로** 맞지만 예외가
> 있습니다 — 총무팀 항목에도 "강당 의자 세팅_전체" 처럼 팀 단위가 섞이고,
> 봉사팀도 개인에게 붙는 일이 생깁니다. 계산으로 두면 **그 예외를 표현할 방법이
> 없고 나중에 화면에서 고칠 수도 없습니다.**
>
> 대신 넣을 때는 추측합니다 — 파트가 봉사팀이면 팀, 담당이 비었거나
> 전체·총무팀·봉사자 같은 묶음 이름이면 팀, 개인 이름이면 개인.
> **추측이지 규칙이 아니므로** 총무팀이 항목마다 그 자리에서 바꿀 수 있고,
> 가져오기는 몇 건을 추측했는지 따로 알려줍니다.

**정리 항목을 눈에 띄게 둡니다.** 정리는 다음 프로그램 준비와 겹치면 누락됩니다.
그런데 **다음 프로그램으로 넘어가면 앞 프로그램의 정리 항목은 화면에서 사라지는데,
그것이 정확히 누락이 생기는 지점입니다.** 그래서 세 가지로 붙잡습니다 —
정리 구간에 세로선을 두어 구분하고, 끝났는데 남아 있으면 **왼쪽 목록에도
`정리 N건 남음`** 을 띄우고, 그 프로그램을 열면 상세 위쪽에 다시 한 번 말합니다.

프로그램이 이미 시작됐는데 준비 항목이 안 끝났으면 `지연` 배지가 붙습니다.

**시작 여부는 저장된 값이 아니라 시각에서 계산합니다.** 4-10 에서 기한 초과를
날짜로 계산하기로 한 것과 같은 이유입니다 — 아무도 누르지 않아도 시스템이
알아차려야 합니다. 현장에는 상태를 눌러 줄 사람이 특히 없습니다.
그래서 `Program` 에는 상태 컬럼이 없고 `ProgramItem` 에도 '지연' 컬럼이 없습니다.

```
day_date < 오늘                     → done
day_date > 오늘                     → todo
같은 날: 다음 프로그램 시작 시각 ≤ 지금 → done
        내 시작 시각 ≤ 지금          → live
        그 외                        → todo
```

**이미 지나간 프로그램의 미완료 준비 항목에도 `지연` 이 붙습니다.** 진행 중일
때만 붙이면, 지나갔는데 준비가 안 끝난 상태 — 더 나쁜 쪽 — 가 조용해집니다.

> **시각에 기대는 판정은 `now` 를 인자로 받습니다** (`app/domain/live.py`).
> 기본값으로 `datetime.now()` 를 부르지 않는 이유는 둘입니다 — 테스트가 실행
> 시각에 따라 갈리면 안 되고, 한 화면을 그리는 동안 여러 계산이 서로 다른
> '지금'을 보면 안 됩니다(자정을 넘기면 일자 탭과 NOW 선이 어긋납니다).

### 5-3. 총무팀 파트 · 봉사자

실제 일자별 시트의 열 구성 그대로입니다. 각 실행 항목에 파트와 담당자가 붙고, 상단 칩으로 자기 파트만 볼 수 있습니다.

`행정` `현장관리` `비품` `음식` `재정` `교역자` — 여기에 봉사팀(`헤브론` `코람데오`)이 맡는 항목이 더해집니다.

**시트의 두 열은 성격이 다릅니다. 이 구분은 시트에서 온 것이지 우리가 만든 것이
아닙니다.**

| 열 | 적히는 모양 | 범위 |
|---|---|---|
| 봉사자 | "헤브론 집합" · "코람데오 리허설 시작" · "송출시작 & 18:50 스탠바이" | 팀이 통째로 움직입니다. 언제 모여서 무엇을 준비하는지가 전부입니다 |
| 총무팀 파트 | "인원계수_온" · "진행자 물 세팅_나윤" · "강사비 계좌이체" | 개인 이름까지 붙습니다 |

둘을 한 목록에 섞으면 **현장에서 헤브론 리더는 자기와 상관없는 개인 항목 사이에서
자기 것을 찾아야 하고, 총무팀원은 팀 전체 움직임과 자기 할 일을 같은 무게로 봅니다.**
그래서 5-2-1 처럼 구간 안에서 다시 갈라 놓고 범위 칩을 둡니다.

**로그인한 사람의 부서가 파트와 맞으면 그 파트를 기본으로 잡습니다** — 부서는 키로
봅니다(2장). `hebron`→헤브론, `koram`→코람데오, `jaejeong`→재정. 총무팀 파트는
총무팀 **안의** 나눔이라 부서와 1:1 이 아니므로 `전체` 로 두고 직접 고르게 합니다.
그 날 실제로 쓰이지 않은 파트는 칩에 내지 않고 기본값으로도 잡지 않습니다.

**담당자 이름은 계정과 잇지 않은 문자열입니다** — 현장에 계정 없는 사람이 섞이기
때문입니다(8장 `assigneeName`). **그 대가로 표기가 갈립니다** — 같은 사람이
`민준` 과 `민준M` 두 가지로 적히고, 계정이 없으니 시스템이 같은 사람인 줄 모릅니다.

`scripts/normalize_names.py` 로 맞춥니다.

```
.venv\Scripts\python.exe scripts/normalize_names.py --retreat "2026 여름수련회 Belong"
.venv\Scripts\python.exe scripts/normalize_names.py --retreat "..." --apply
```

- **`--apply` 없이 돌리면 보여만 줍니다.** 기본이 미리보기입니다 — 이름을 잘못
  바꾸면 누가 무엇을 맡았는지가 틀어지는데, 그건 되돌릴 근거가 화면에 남지 않습니다
- **짝을 코드에 박지 않고 회차 자료에서 찾습니다.** `X` 와 `XM` 이 둘 다 쓰인
  것만 `X` → `XM` 으로 맞춥니다. 다음 회차에 다른 이름이 갈려도 같은 스크립트가 잡습니다
- **`XM` 이 없으면 건드리지 않습니다.** `하람`·`나윤`·`온` 처럼 M 없이만 쓰는
  사람에게 M 을 붙이면 사람 이름이 틀어집니다. 있는 짝만 맞춥니다
- `서윤·나윤` 처럼 한 칸에 여럿이 든 것은 구분자로 쪼개 각각 보고 다시 합칩니다.
  **구분자는 원래 쓰던 것을 그대로 둡니다**
- 바꾸면 `log_activity` 에 남깁니다(`담당자_표기_통일`) — 화면에서 하나씩 고치면
  180건 중 무엇을 고쳤는지 알 수 없습니다

> **회차를 넘기기 전에 돌리는 것이 좋습니다.** 프로그램표를 복사해 오면(5-5)
> 갈린 표기가 **그대로 따라갑니다.** 겨울 회차로 넘긴 뒤에 고치면 두 회차를
> 각각 고쳐야 합니다.

### 5-4. 권한

- **체크는 로그인한 사람 누구나** 합니다. 현장에서는 옆 사람 것도 대신 눌러야 하고,
  손이 비는 사람이 누르는 것이 맞습니다. 남의 부서 항목이어도 됩니다
- 누가 눌렀는지는 `done_by_id`, 언제 눌렀는지는 `done_at` 으로 남습니다.
  **한 번 찍힌 시각은 다시 눌러도 덮어쓰지 않습니다** — 처음 누른 때가 사실입니다
- **프로그램과 항목을 만들고 고치고 지우는 것은 총무팀(admin)만** 합니다.
  그건 현장에서 급히 하는 일이 아니라 미리 짜 두는 일입니다

### 5-5. 프로그램표를 채워 넣는 길

프로그램표는 매 회차 새로 만듭니다. **손으로 하나씩 넣게 두면 아무도 안 씁니다.**

- 총무팀이 `/live` 화면에서 프로그램과 항목을 추가·수정·삭제합니다
- **지난 회차에서 복사해 오기** — 회차를 고르면 그 회차의 프로그램표를 통째로
  가져옵니다. 프로그램이 하나도 없을 때 화면 한가운데에 이 자리가 나옵니다.
  날짜는 저장하지 않으므로 옮길 것이 없고, `day` 이름만 따라가 **새 회차의
  개회일에서 다시 계산**됩니다
- **체크 상태는 가져오지 않습니다.** 지난 회차의 사실이지 이번 회차의 것이
  아닙니다 — 진행 상태가 전부 대기로 돌아가는 것(6-10)과 같은 원칙입니다
- 프로그램표가 없는 회차를 고르면 "그 회차에는 프로그램표가 없습니다" 로 막고
  기존 것을 지우지 않습니다
- **목업의 예시 프로그램을 seed 에 넣지 않습니다.** 그것은 실제 이력이 아니라
  시각 스펙용 예시입니다 — 지어낸 기록을 심지 않는다는 6-9 와 같은 자리입니다

**구글시트에서 옮겨 오기** — `scripts/import_programs.py` 가 일자별 시트를 옮긴
JSON 을 넣습니다. 2026 여름수련회(프로그램 61개 · 항목 157건)를 이것으로 넣었습니다.

```
.venv\Scripts\python.exe scripts/import_programs.py data/2026여름_프로그램표.json `
    --retreat "2026 여름수련회 Belong"
```

- **회차는 이름으로 정확히 찾습니다.** 비슷한 이름을 골라 넣지 않고, 없으면 있는
  회차 목록을 보여주고 멈춥니다 — 엉뚱한 회차에 61개를 넣고 나면 어느 것이 원래
  것인지 알 수 없습니다
- **이미 프로그램이 있으면 그냥 덮어쓰지 않습니다.** 몇 건이 지워질지 먼저 말하고
  `--replace` 를 줬을 때만 지웁니다. 실수로 한 번 더 돌려서 날아가면 되돌릴 방법이 없습니다
- 모르는 파트·구간·일자, 이상한 시각이 있으면 **어느 줄인지 말하고 멈춥니다.**
  하나라도 이상하면 아무것도 넣지 않습니다 — 절반만 들어간 프로그램표는 비어 있는
  것보다 나쁩니다
- **`done_at` · `done_by_id` 를 넣지 않습니다.** 이 파일은 무엇을 했는지의 기록이
  아니라 무엇을 하기로 했는지의 표입니다
- 담당자는 계정과 잇지 않으므로 오타가 그대로 남습니다. 넣은 뒤 **비슷한 이름
  짝**(`민준` / `민준M` 같은)을 짚어 주되 고치지는 않습니다
- **`scope` 가 없으면 `person` 으로 몰지 않고 계산합니다**(5-2-1의 기준). 그렇게
  하지 않으면 범위가 없던 시절의 파일에서 봉사자 열이 통째로 개인 일이 됩니다.
  `team` · `person` 이 아닌 값은 어느 줄인지 말하고 멈춥니다.
  **몇 건을 추측했는지 따로 알려줍니다** — 추측이지 규칙이 아니기 때문입니다

### 5-6. 시스템 밖에서 진행한 회차

지난 회차에 프로그램표만 있고 체크가 하나도 없으면 화면이 **"아무것도 안 했다"로
읽힙니다.** 실제로는 다 했는데 **그때 이 시스템이 없었을 뿐**입니다.
없는 기록을 지어내지도 않고(6-9), 안 한 것처럼 보이게 두지도 않습니다.

**종료된 회차 + 체크 0건이면 진행률 대신 한 줄을 보여줍니다** —
"이 회차는 시스템 밖에서 진행했습니다 — 프로그램표만 옮겨온 것입니다".
**0% 막대를 그리지 않습니다.**

판단 근거는 그 둘뿐입니다. **회차에 표시를 따로 달지 않습니다** — 표시를 달면
누군가 그것을 켜고 꺼야 하고, 안 켜면 또 조용히 틀립니다. 0장의 판단 기준과
같은 자리입니다.

- **종료**는 폐회일이 오늘보다 이르거나 `is_archived` 입니다 (4-10 의 '종료된 회차'와 같은 뜻)
- **체크는 회차 전체로 셉니다.** 그 날만 보면 진행 중인 회차에서 아직 아무도
  누르지 않은 날이 같은 문구를 달게 됩니다
- 체크가 하나라도 생기면 그때부터 보통의 진행률로 돌아갑니다

**`지연` 과 `정리 N건 남음` 도 붙이지 않습니다.** 둘 다 "안 끝났다"는 말인데,
그 회차는 안 끝난 게 아니라 **누른 적이 없는** 것입니다. 위에 사정을 적어 놓고
아래를 빨간 배지로 덮으면 한 화면이 서로를 부정합니다 — 4-10 에서 완료와 연쇄
경고를 함께 내지 않기로 한 것과 같은 자리입니다. **판정 자체는 그대로 두고
표시만 가립니다** — 체크가 하나 생기는 순간 전부 원래대로 돌아옵니다.

### 5-8. 봉사팀 보기 — 화면과 내려받기를 같은 표로

원래 쓰던 구글시트의 '봉사자 시간표'를 그대로 옮긴 것입니다. 세로가 시각,
가로가 일자이고, 일자마다 **왼쪽이 봉사자 칸 · 오른쪽이 전체일정 칸**입니다.
`/live/staff` 에서 보고 `/live/staff.xlsx` 로 내려받습니다.

**표를 만드는 곳은 `app/domain/staff_sheet.py` 하나입니다.** 화면과 파일이 같은
`build()` 의 결과를 그립니다. 저쪽은 놓기만 하고 판정·색·병합을 다시 계산하지
않습니다. 따로 만들면 반드시 어긋나고, **어긋난 쪽이 파일이면 그 사실을 아무도
모른 채 카카오톡으로 돌아다닙니다.** 이 프로젝트에서 가장 자주 고쳐 온 문제가
"같은 것이 두 곳에 있는 것"이라 여기서는 처음부터 하나로 두었습니다.

**로그인한 사람 누구나 봅니다** — 봉사팀도 총무팀도. 소속이 헤브론·코람데오면
`/live` 로 들어와도 이 화면으로 옵니다(`?stay=1` 로 진행 화면에 머뭅니다).
총무팀은 진행 화면이 기본이고 위의 탭으로 건너갑니다.

**체크를 두지 않습니다.** 이 표는 읽는 것입니다 — 체크는 진행 화면에서 합니다.
휴대폰에서도 **가로로 넘겨 같은 표를 봅니다.** 세로로 접거나 다른 모양으로
바꾸지 않습니다. 같은 표를 보는 것이 이 화면의 목적이기 때문입니다.

**담기는 것과 담기지 않는 것**

| 담긴다 | 담기지 않는다 |
|---|---|
| 봉사자 프로그램 · 전체일정 프로그램 | `ops` 프로그램 (뒤에서 도는 일) |
| 봉사팀(헤브론·코람데오) 항목 | 총무팀 개인 항목 |
| | 체크 상태 (`done_at`) |

거르는 것이 이 표의 이유입니다. 봉사팀이 보는 것은 "우리가 언제 모여 무엇을
하나"이지 총무팀의 실행 목록이 아닙니다.

**전체일정이 하나도 없는 날은 그 칸 자체를 만들지 않습니다.** 빈 칸을 두면
"왜 비었지"를 묻게 됩니다 — 선발대가 그렇습니다.

**색은 파트 순서대로 배정합니다.** 목록을 코드에 박지 않습니다 —
`PROGRAM_PARTS` 에서 봉사팀 파트만 추려 순서대로 `TEAM_PART_FILLS` 를 물립니다.
파트가 늘면 색도 함께 늘어야 하고, 목록을 박아 두면 새 팀이 조용히 무색이 됩니다.
**섞인 칸은 어느 팀의 것도 아닙니다** — 프로그램과 봉사팀 항목이 같은 시각에
합쳐지면 첫째 것의 색을 쓸 수 없습니다. 첫째가 무엇인지에 따라 같은 칸이
파랬다 노랬다 하므로, 색이 갈리면 봉사자 공통색으로 떨어뜨립니다.

#### 표에 적는 말

화면으로 보고 나서 정한 것들입니다. 셋 다 **같은 말이 두 번 나오거나, 알 필요
없는 말이 자리를 차지하는** 문제였습니다.

**프로그램과 그 프로그램에 붙은 봉사팀 항목이 같은 말이면 한 번만 냅니다.**
둘 다 봉사자 칸에 들어가므로 나란히 찍힙니다 — `골든벨 준비` 가 두 줄,
`음향 및 무대설치` 와 `음향 및 무대설치 (3.5h)` 가 두 줄로 나왔습니다.

- **같고 다름은 이름을 견주지 않고 한쪽이 다른 쪽 안에 들어 있는지로 봅니다**
  (`same_thing`). 띄어쓰기와 `·` 를 지우고 견주므로 `복귀 짐 패킹 · 상차 · 이동`
  과 `헤브론 복귀 짐 패킹·상차·이동` 도 같은 말로 잡힙니다
- **항목 쪽에 괄호 정보(`3.5h`)가 더 있으면 그쪽을 쓰고**, 없으면 프로그램 쪽을
  남깁니다. 애매하면 한쪽을 반드시 고르되 기본은 원래 시간표의 이름입니다 —
  빠지는 것보다 겹치는 것이 눈에 더 거슬립니다
- **버리는 쪽의 담당과 색은 잃지 않습니다.** `하차 (헤브론)` 를 버리고
  `본당 도착 · 하차 (전체)` 만 남기면 헤브론이 관련된 사실이 사라지는데,
  이 표를 보는 사람이 정확히 그것을 보러 옵니다
- **프로그램이 전체일정이면 합치지 않습니다.** 프로그램은 오른쪽 칸,
  항목은 왼쪽 칸이라 애초에 나란히 찍히지 않습니다

**담당이 `총무팀`·`전체` 처럼 특정인이 아니면 괄호를 아예 내지 않습니다**
(`who_of`). 봉사팀에게 보내는 표인데 `아침식사 (총무팀)` 이 거의 모든 칸에
붙어서 정작 사람 이름이 묻혔습니다. 사람 이름과 봉사팀 이름만 남기고,
**장소는 그대로 냅니다** — 그건 봉사팀에게 쓸모가 있습니다.
버릴 것이 하나도 없으면 **원문을 그대로** 돌려줍니다. 구분자를 다시 짜맞추면
`하람·나윤` 이 `하람 · 나윤` 으로 바뀌어, 고칠 이유가 없는 표기가 흔들립니다.
버릴 말의 목록은 `TEAM_WORDS` 에서 봉사팀 이름만 뺀 것입니다 — 따로 적으면
파트가 늘 때 한쪽만 고쳐집니다.

#### 칸의 가로세로 비율

**원본 시트에서 읽은 값입니다.** 세로가 시간인 표라 비율 자체가 뜻을 가집니다 —
납작하면 같은 한 시간이 좁아 보여 일정이 실제보다 촘촘하게 느껴집니다.

| 원본에서 읽은 것 | 값 |
|---|---|
| 시각 열 | 8.43 |
| 내용 칸 (두 열) | 11.63 + 8.43 = 20.06 |
| 자료 한 줄 | 28.5pt → 한 시각(두 줄)은 57pt |

**한 시각의 높이 : 한 칸의 너비 = 57 : 20.06 ≈ 2.84** 입니다.
다만 두 수의 **단위가 다릅니다** — 포인트와 엑셀 열 너비 단위입니다. 그래서
2.84 는 화면에서 눈에 보이는 세로/가로 비가 아니라 원본이 적어 둔 두 값의
비이고, px 로 환산하면 0.5 쯤입니다(76px : 150px). 어느 쪽으로 재든
**원본과 같아지는 것이 목적**이라 네 값을 `staff_sheet` 에 두고 전부 거기서
끌어옵니다.

- 파일은 원본 값을 **그대로** 씁니다(열 너비 8.43·11.63, 줄 높이 28.5pt)
- 화면은 같은 값을 px 로 환산해 씁니다(줄 38px, 칸 86+64px)
- **xlsx 쪽에도 CSS 에도 너비·높이를 적지 않습니다.** 두 곳에서 정하면
  화면과 파일의 비율이 어긋나고, 어긋난 쪽이 파일이면 아무도 모릅니다.
  화면의 열 너비는 `<colgroup>` 이 구조에서 받아 씁니다

**비율만 바꾸는 것이지 글 양에 따라 늘리는 것으로 되돌리는 것이 아닙니다** —
한 시각의 높이는 여전히 어디서나 같습니다.

**칸 크기와 글자 크기는 같이 정합니다.** 하나만 바꾸면 어긋납니다 — 실제로
칸 비율만 원본에 맞추고 글자를 그대로 두었더니 **여백만 넓어져서** 내용이
적은 날(폐회)이 휑해 보였습니다. 그래서 글자도 줄 높이에서 끌어옵니다.

| | 화면 | 파일 |
|---|---|---|
| 본문 | 16px | 12pt |
| 머리줄·시각 열 | 18px 굵게 | 13.2pt 굵게 |
| 넘치는 칸(`dense`) | 13px | 9.8pt |

`FONT_TO_ROW = 0.42` 하나가 셋을 다 정합니다 — 글자 : 줄 높이 = 0.42 로
화면과 파일이 **같은 비율**입니다. 원본은 10pt : 28.5pt ≈ 0.35 인데 우리는
그보다 조금 큽니다. **원본은 한 줄이 한 칸이지만 우리는 여러 줄에 걸친 칸이
많고 글이 위에 붙어서**, 같은 비율로는 아래가 비어 보이기 때문입니다.

- **머리줄과 시각 열은 본문보다 크고 굵습니다** — 시각은 이 표에서 가장
  자주 보는 글자입니다
- **`dense` 도 기본이 커진 만큼 함께 커집니다**(`DENSE_FONT_SCALE`).
  따로 두면 기본만 커지고 작은 글씨는 그대로여서 한 칸만 유난히 작아집니다
- **`dense` 로도 넘치면 넘치게 둡니다. 줄을 늘리지 않습니다** — 한 시각의
  높이가 어디서나 같아야 시각 눈금이 맞습니다
- 글자 폭도 글자 크기에서 셉니다(`NARROW_CHAR_RATIO`). 폭을 숫자로 박아 두면
  글자를 키운 뒤에도 옛 폭으로 세어 **넘치는 칸을 못 잡습니다**
- **CSS 에도 xlsx 에도 크기를 적지 않습니다.** 화면은 표가 실어 보낸
  `--fs`·`--fs-dense`·`--fs-head` 를 쓰고, 파일은 `sheet["font"]` 를 씁니다

2026 여름수련회 기준으로 **57칸 중 5칸**이 작은 글씨입니다. 절반을 넘으면
기본이 너무 큰 것이므로 `FONT_TO_ROW` 를 내립니다.


#### 한 시각의 높이는 어디서나 같습니다

**시간표는 세로가 시간이어야 읽힙니다.** 처음에는 칸이 글 양대로 늘어나서
06:00 은 한 줄, 11:00 은 세 줄 높이가 되고, 13:00 옆에 14:00·15:00 이 몰려
붙었습니다. 왼쪽 시각 눈금이 오른쪽 내용과 어긋나면 시간표가 아닙니다.

- 높이는 `ROW_HEIGHT_PX` 하나가 정하고 **화면(px)과 파일(pt)이 그 값에서**
  나옵니다. 파일 쪽에서 글 양으로 줄 높이를 계산하던 것을 걷어냈습니다
- 넘치는 글은 **칸을 늘리는 대신 글자를 줄입니다**(`dense_of`). 그 판단도
  `staff_sheet` 가 하므로 화면과 파일이 같은 칸을 작게 씁니다
- 글자 수가 아니라 **글자 폭**으로 셉니다. 한글은 글자 크기와 폭이 비슷하고
  영문·숫자는 그 절반쯤이라, 글자 수로 세면 `Belong FM 준비 (13:00)` 같은
  줄이 실제보다 길게 잡힙니다
- 화면 쪽은 칸 안의 `.cx` 가 높이를 붙듭니다. **테두리 2px 을 빼야** rowspan 2
  짜리 한 칸과 rowspan 1 짜리 두 칸의 높이가 같아집니다 —
  안 빼면 줄마다 2px 씩 어긋나 아래로 갈수록 눈금이 밀립니다
- **시각은 그 시각이 시작하는 자리에 붙입니다.** 가운데 두면 한 시간의
  한복판에 떠서 오른쪽 내용보다 반 칸 아래로 보입니다

#### 비어 있는 것이 정상임을 말해 줍니다

개회일 전체일정은 06:00~13:00 이 통째로 비어 있습니다. 참가자가 아직 오지
않아서 맞는 것인데, 그 큰 공백이 **"뭔가 빠진 것" 처럼** 보입니다.
그 구간에 `참가자 등록 전` 을 옅게 적습니다.

- 칸을 채우려는 것이 아니므로 **색을 넣지 않습니다.** 내용이 있는 것처럼
  보이면 안 됩니다
- **개회일에만 붙입니다.** 폐회일 아침이 비어 있는 것은 참가자가 없어서가
  아닙니다. 개회일은 회차의 개회일과 날짜가 같은 일자로 찾습니다 —
  `1일차` 라고 적어 두지 않습니다

#### 세 가지 함정

앞서 같은 종류로 헤맨 것들이라 규칙으로 적습니다.

1. **자정을 넘긴 시각을 문자열로 정렬하지 않습니다.** `00:00 광고`가 `06:00`
   위에 서면 하루가 통째로 뒤집힙니다. 06시 이전은 24시간을 더해 아래로 보냅니다
   (`row_of`). 축은 06:00 ~ 다음날 01:00 이고 **한 시각이 두 줄**입니다
2. **같은 줄에서 시작하는 프로그램을 덮어쓰지 않습니다.** 한 칸에 합쳐 줄바꿈으로
   잇습니다 — 2일차 00:00 의 광고와 야식이 그렇습니다. 그냥 넣으면 나중 것이
   앞엣것을 지웁니다
3. **나란히 도는 것을 시각으로 추측하지 않습니다.** `parallel` 로 적힌 것만
   오른쪽 열로 뺍니다. 시각이 겹친다고 나란히 도는 것은 아닙니다

#### 화면에서 놓치기 쉬운 것

시각 열과 머리 두 줄을 `position:sticky` 로 붙이면서 두 번 걸렸습니다.

- **붙는 자리와 머리 줄 높이는 같은 값 하나여야 합니다.** 둘째 머리 줄을
  `top:30px` 로 적어 두고 실제 높이가 26px 이면, 그 4px 틈으로 본문이 비쳐
  지나갑니다. `--headh` 하나를 높이와 `top` 에 함께 씁니다
- **모서리만 가로·세로 둘 다 붙습니다.** `.c-head:first-child` 로 적으면
  둘째 머리 줄의 첫 칸(첫 부서의 `봉사자`)까지 왼쪽에 붙어 시각 열을 덮습니다.
  첫 줄로 한정합니다
- **`table-layout:fixed` 는 첫 줄의 칸에서 열 너비를 가져갑니다.** 시각 열의
  너비는 `.c-time` 이 아니라 첫 줄의 머리 칸이 정하므로 양쪽 끝을 함께 적습니다
- 표가 컨테이너에 맞춰 찌그러지지 않도록 `width:max-content`. 그래야 좁은
  화면에서 열 너비를 지킨 채 **가로로 넘어갑니다**

#### 파일

**저장 전에 병합이 겹치는지 봅니다** (`check_merges`). 겹치면 엑셀이 "읽을 수
없는 내용이 있습니다"를 띄우는데 **openpyxl 은 아무 말도 하지 않습니다.**
손상된 파일을 만들어 놓고 성공했다고 하면 안 되므로, 겹치면 저장하지 않고
무엇이 어디서 겹쳤는지 말하며 멈춥니다.

**글꼴은 맑은 고딕 10 입니다.** 원본 시트는 Arial 이지만 한글 글자가 없어 대체
글꼴로 떨어지고, 그러면 열 너비에 맞춰 잡아 둔 자간이 어긋나 글이 잘립니다.
**원본 재현보다 읽히는 것이 먼저입니다.** 엑셀은 병합된 칸을 자동으로 늘려주지
않으므로 줄 높이도 글 양에서 계산해 그 줄들이 나눠 갖게 합니다.

**두 번째 시트에 '이 파일에 대해'를 붙입니다** — 회차, 내려받은 시각, 보는 법,
그리고 "이 파일은 내려받은 시점의 사본입니다. 바뀌면 다시 내려받으세요".
파일은 한 번 나가면 손을 떠나 카카오톡으로 돌아다니고 사흘 뒤에도 누군가
그걸 보고 있습니다. **언제 것인지 파일이 스스로 말하지 않으면 그것이 곧
틀린 정보가 됩니다.** 시각은 **벽시계**로 적습니다 — UTC 로 적으면 새벽에
하루 전이 됩니다(첨부파일에서 겪은 그 문제).

---

## 6. 업무 라이브러리와 회차 세팅

시각 스펙: `retreat-setup.html`

### 6-1. 핵심 구조

업무는 회차에 속하지 않고 **라이브러리에 영속**합니다. 회차는 그중 무엇을 실행했는지를 기록할 뿐입니다.

```
TaskLibrary  ──< TaskRun >── Retreat
   (영속)         (회차별 실행 기록)
```

이번 회차에 빼도 삭제되지 않고 `included: false`로 기록됩니다. 이 기록이 다음 회차 분류의 입력값이 됩니다.

### 6-2. 자동 분류

사람이 지정하지 않고 실행 이력에서 계산합니다. **다만 쌓인 회차 수를 넘어서는 말을 하지 않습니다.**
한 회차 기록으로 "최근 3회 모두 실행"이라고 할 수는 없기 때문입니다.

| 쌓인 회차 | 분류 |
|---|---|
| 0 | 이력 없음 |
| 1 | 지난 회차 실행 / 지난 회차 미실행 |
| 2 | 2회 모두 실행 / 2회 중 1회 / 2회 모두 미실행 |
| 3 이상 | 필수 / 추천 / 후순위 (최근 3회차 기준) |

기본 선택은 "빠짐없이 실행" 쪽만 켜고 "모두 미실행"은 끕니다. 마법사 3단계에 **분류 근거가 몇 회차인지** 표시하며, 3회차가 쌓이면 그 안내가 저절로 사라집니다.

각 업무에 회차별 실행 이력을 작은 막대로 표시합니다. 살아 있는 관행인지 사라진 관행인지 한눈에 드러나야 합니다.

**수동 필수 지정** — 자동 분류는 이력이 얕을 때 힘이 없습니다. `TaskLibrary.always_required` 로 총무팀이 "이건 매 회차 반드시"를 직접 표시할 수 있고, `/library` 화면에서 관리합니다. 구멍 방지 경고는 **수동 지정과 자동 판정 중 하나라도 해당하면** 뜹니다 — 이력이 하나도 없는 첫 회차에도 경고가 작동해야 하기 때문입니다.

### 6-3. Claude 제안

이력에 없는 업무를 제안합니다. **반드시 근거를 함께 표시합니다.** 근거 없는 제안은 신뢰를 잃습니다.

근거의 출처 두 가지:

- **지난 회차에서 실제로 문제가 됐던 것** — 논의 기록과 지연 이력에서 도출. 예: 홍보영상이 출연자 일정으로 미진행된 이력 → "영상 출연자 섭외·일정 확정"을 선행 업무로 분리 제안
- **이번 회차의 조건이 달라서 필요한 것** — 예: 여름 회차를 복제해 겨울에 하므로 방한·난방 물품 점검이 목록에 아예 없음

두 번째가 특히 중요합니다. 복제만 하면 **없는 항목은 빠졌다는 사실조차 모릅니다.**

### 6-4. 날짜 재계산

D-주차 상대 위치를 유지하고 새 개회일 기준으로 날짜만 옮깁니다.

```
D-1주차 = 개회일 직전 일요일
D-N주차 = D-1주차 - (N-1) × 7일
```

### 6-5. 명절 충돌 감지

계산된 D-주차가 성탄·연말·신정과 겹치면 경고합니다. 겨울 회차는 D-4주가 성탄 주간, D-3주가 연말 주간에 걸립니다. 논의와 검토가 주말에 이뤄지는데 그 주말이 막히므로 실질적인 문제입니다.

### 6-6. 세팅 마법사 4단계

1. **회차 정보** — 개회일 입력 → D-주차 자동 계산 + 명절 충돌 표시
2. **부서 확정** — 직전 회차 목록에서 제외하거나 **새 부서를 추가**합니다. 새 부서에는 서버가 회차를 넘어 쓸 키(`team1`, `team2` …)를 발급합니다 — 한글 이름으로는 안전한 키를 만들 수 없고, 그 키가 다음 회차에서도 같은 부서임을 알아보는 유일한 근거입니다
3. **업무 라이브러리** — 분류별 필터, 선택, 이력 막대. 목록은 **구조가 보이게** 놓습니다:
   - 성격이 다른 셋을 갈라 놓습니다 — `Main 업무` / `일정`(날짜만 지키면 되는 별도 업무) / `Claude 제안`
   - **진행 순서**로 정렬하고(시작일 순), 시작 주차가 바뀌는 곳에 D-주차 눈금을 둡니다
   - Main 아래에 **하위 업무를 들여쓰기로** 보여줍니다. 고르는 단위는 상위지만
     무엇이 딸려 오는지 보이지 않으면 고를 수가 없습니다. 상위를 끄면 하위도 함께 흐려집니다
   - 기간은 **보드와 같은 눈금**으로 고릅니다(D-3주까지 주 단위, 이후 하루 단위, 마지막에 수련회 기간).
     시작과 마감이 같은 목록이라 며칠짜리인지 세지 않아도 됩니다. 놓을 수 있는 범위는 **D-26주**까지 열려 있습니다
   - 상위 업무는 **가나다순**으로 놓고 이름으로 좁혀 찾습니다. 50건이 넘으면 눈으로 훑을 수 없습니다
   - 여기서 **업무를 새로 만들거나 고칠 수 있습니다.** 고른 목록을 보다가 빠진 것을 발견하는
     자리가 여기이기 때문입니다. 라이브러리를 고쳐도 **지난 회차의 실행 기록 날짜는 건드리지
     않습니다** — 그때 실제로 언제 했는지는 사실이므로 바뀌면 안 됩니다
4. **확인 및 생성** — 요약 + 경고

### 6-7. 팀별 수집 — 각 팀이 고르고 총무팀이 연다

총무팀장이 50여 건을 혼자 훑어 고르면 각 팀의 사정이 반영되지 않습니다. 마법사 2단계에서 **각 팀에 업무 선택 요청**을 누르면 회차 정보와 부서만 정해진 **초안**이 열리고 부서마다 칸이 하나씩 생깁니다.

```
RetreatDraft (수집중)  ──<  DraftSubmission  >──  부서
        └ 모두 제출되면 총무팀이 마법사 3·4단계로 회차를 연다
```

- 부서 리더는 `/draft` 에서 **자기 칸만** 채웁니다. 총무팀은 모든 칸을 볼 수 있습니다.
- **임시저장**은 몇 번이든, **제출**은 "우리 팀은 다 골랐다"는 표시입니다. 회차를 열기 전까지는 제출한 뒤에도 고칠 수 있습니다.
- 마법사 3단계는 **제출된 것만** 모읍니다. 작성 중인 초안은 아직 그 팀의 답이 아닙니다. 제출하지 않은 팀이 있으면 "지금 만들면 그 팀의 업무는 빠집니다"라고 알립니다.
- 초안은 한 번에 하나만 엽니다. 새로 열면 앞의 것은 접힙니다 — 두 개가 열려 있으면 팀이 어느 쪽을 채워야 할지 모릅니다.
- 총무팀에 전할 말을 한 줄 남길 수 있습니다. 선택의 이유가 사람이 아니라 기록에 남습니다.

**회차를 연 뒤에도 업무는 늘어납니다.** 보드의 `+ 업무 추가` 에서 라이브러리에 있는 것을 넣거나 새로 만듭니다. 새로 만든 것도 라이브러리에 남아 다음 회차의 후보가 됩니다. 미실행으로 기록돼 있던 업무를 다시 넣으면 그 기록이 되살아납니다.

### 6-8. 구멍 방지 경고

4단계에서 반드시 확인합니다.

- 빠뜨리면 안 될 업무가 빠졌을 때 — 수동 필수 지정 또는 기록된 모든 회차에서 실행한 것. 경고에는 그 업무가 왜 대상인지(근거)를 함께 적습니다
- 선행 업무가 이번 회차에서 빠졌을 때 — 넣은 업무가 기다려야 할 업무가 목록에 없으면
  그 업무는 시작할 수 없습니다. 링크를 만들지 않고 경고로만 알립니다
- 제외한 부서가 맡던 업무에 담당이 없을 때
- 명절 주간에 업무가 배정될 때
- 미실행으로 기록될 건수 안내

### 6-9. 첫 세팅

복제할 직전 회차가 없으므로 2026 여름수련회(Belong)를 라이브러리 초기값으로 올립니다. 노션에서 정리한 준비 업무와 시간표에서 뽑은 실행 항목이 그 재료입니다.

**실제 실행 이력은 이 한 회차뿐입니다.** 분류를 그럴듯하게 만들려고 과거 회차를 지어내지 않습니다 — 지어낸 이력이 "필수 23건" 같은 판단의 근거가 되면 시스템 전체의 신뢰가 무너집니다. 이력이 얕은 동안에는 6-2의 수동 필수 지정이 구멍 방지를 맡습니다.

마스터를 처음부터 완벽하게 만들려 하지 않습니다. **두 회차를 겪은 뒤 교집합에서 자동으로 드러납니다.**

### 6-10. 회차를 넘어가는 것과 아닌 것

| 넘어감 | 안 넘어감 |
|---|---|
| 업무명, 계층, 연결 관계 | 담당자 개인 이름 |
| 업무 규칙 (진행 방식) | 파일 첨부 |
| 담당 부서, 관련팀 | 진행 상태 (전부 대기로) |
| 선후행 관계 | 회차별 선행 링크 (`blockedByRunIds` 는 매번 다시 계산) |
| 전/중/후 실행 항목 | 절대 날짜 (D-주차로 재계산) |
| 지난 회차 논의 내역 (접힌 상태) | |

---

## 7. 재정

아직 목업 없음. 아래는 실제 파일에서 검증된 사실입니다.

### 7-1. 검증된 컬럼 구조

`Belong 예산.xlsx` → "지출 상세내역" 시트 실측 (2026-08-27):

```
구분 | 항목 | (병합) | 세부항목-1 | 세부항목-2 | 세부항목-3 |
예산 사용액 | 합계금액 | (병합) | 영수증번호 | 지출일자 | 금액 |
지원금액 | 비고 | 지급여부 | 지급일 | 지출자 | 지출자 계좌
```

### 7-2. 식대 정산

수련회 본행사 식비가 아니라 **"그 외 > 수련회 준비지원 > [부서명] > 모임 식사비-N"** 패턴입니다. 준비 기간에 부서별로 반복 발생하는 회식비입니다.

공식을 실제 숫자로 검증했습니다.

```
지원금액 = min(금액, 인원수 × 1인당 상한)
```

- 12명 · 130,600원 → 96,000원 (상한 적용)
- 9명 · 68,900원 → 68,900원 (상한 미만이라 전액)

**인원수와 명단이 현재 "비고" 칸에 자유 텍스트로 들어가 있습니다.** `9명 박민준 남시우 노서윤...` 형태입니다. 이것을 전용 필드로 분리하는 것이 이 모듈의 핵심입니다.

### 7-3. 필요한 기능

- 예산 카테고리 CRUD, 예산 대비 지출 실시간 집계
- 지출 등록 + 영수증 파일 첨부
- 식대 전용 입력 폼: 인원수 / 명단 / 결제자 / 계좌번호 → 지원금액·개인부담액 자동 계산
- 반복 등록이 잦으므로 직전 입력값을 기본값으로 제안
- 환급 대상자 리스트 (이름·계좌·지원금액·지급여부)
- 엑셀 export

---

## 8. 데이터 모델

```
Retreat {
  id, name, openDate, closeDate,
  mealSubsidyPerPerson,        // 회차별 설정
  clonedFromRetreatId?
}

Department { id, retreatId, name, color, sortOrder }

User { id, phoneNumber, name, departmentId?, role }
// role: 'admin' | 'dept_lead' | 'member' | 'viewer'

/* ── 업무: 라이브러리(영속) + 회차별 실행 기록 ── */
TaskLibrary {
  id, title, kind,                    // 'main' | 'sub' | 'schedule'
  parentLibraryId?,
  defaultDepartmentId, defaultDWeek,
  relatedLibraryIds[],                // 관련업무 — 방향 없음. 양쪽에 서로 적는다
  prerequisiteLibraryIds[],           // 선행업무 — 방향 있음. 가진 쪽에만 적는다.
                                      // 후속("나를 기다리는 업무")은 저장하지 않고 조회할 때 계산한다
  relatedDepartmentIds[],             // 관련팀 (고스트 표시 대상)
  alwaysRequired: boolean,            // 총무팀이 손으로 지정한 '매 회차 반드시'
  rules?,                             // 업무 규칙 — 회차를 넘어가는 진행 방식 (4-9)
  origin,                             // 'history' | 'claude_suggestion'
  suggestionRationale?,               // 제안 근거 (origin이 제안일 때 필수)
  createdAt, archivedAt?
}

TaskRun {
  id, libraryId, retreatId,
  included: boolean,                  // false여도 삭제하지 않고 기록
  departmentId, assigneeId?,
  dWeek, startDate, endDate,
  status,                             // '대기'|'진행중'|'완료'|'지연'  (models.RUN_STATUSES)
  startedAt?,                         // 처음 '대기'를 벗어난 날. 되돌려도 지우지 않는다 (4-10)
  completedAt?,                       // 실제로 끝난 날. '완료'를 벗어나면 지운다
                                      // (착수는 사실이지만 완료는 취소된다)
  blockedByRunIds[]
}

DiscussionEntry {
  id, runId, authoredAt, body,
  supersedesEntryId?,                 // 취소선 처리될 이전 항목
  carriedFromRunId?                   // 이전 회차에서 참고용으로 따라온 것
}

TaskAttachment {
  id, runId,                          // 회차별 — TaskRun 에 붙는다. 새 회차로 따라가지 않는다
  originalName,                       // 올린 이름. 표시에만 쓴다
  storedName,                         // 디스크의 이름. 임의로 만든다 —
                                      // 올린 이름을 그대로 쓰면 경로 조작·덮어쓰기·
                                      // 한글 인코딩이 한꺼번에 따라온다
  sizeBytes,
  uploadedById?, uploadedByName?, uploadedAt
}
// 파일 자체는 DB 밖(config.ATTACHMENT_DIR)에 있다. 백업 대상이다 (11-2)

/* ── 수련회 진행 (5장) ── */
Program {
  id, retreatId,                      // 회차별 — TaskRun 처럼 회차에 붙는다
  day,                                // '선발대'|'1일차'|'2일차'|'폐회'
                                      // **날짜를 저장하지 않는다.** 개회일에서 계산한다 (5-1)
  startTime,                          // "HH:MM"
  name, host, place?, note?, sortOrder
}
// 상태 컬럼이 없다 — done/live/todo 는 시각에서 계산한다 (5-2)

ProgramItem {
  id, programId, phase,               // 'pre' | 'mid' | 'post'
  partKey,                            // 행정|현장관리|비품|음식|재정|교역자|헤브론|코람데오
  assigneeName,                       // **이름 문자열이다. User 로 잇지 않는다** —
                                      // 현장에는 계정 없는 사람이 섞이고(`하람` `서윤` `전체`)
                                      // 실제 시트가 그렇게 쓰여 있다.
                                      // 그 대가로 표기가 갈린다(`민준` / `민준M`) —
                                      // scripts/normalize_names.py 로 맞춘다 (5-3)
  scope,                              // 'team' | 'person' (5-2-1). 기본 person.
                                      // **파트에서 계산하지 않고 컬럼으로 둔다** —
                                      // "강당 의자 세팅_전체" 처럼 총무팀 파트에도
                                      // 팀 단위가 섞이고, 계산으로 두면 그 예외를
                                      // 표현할 방법도 화면에서 고칠 방법도 없다.
                                      // ALTER 로 붙어 기존 행은 NULL — 읽는 자리는
                                      // 늘 `scope_key`('person' 으로 본다)
  text, sortOrder,
  doneAt?,                            // 실제로 누른 시각. 체크 여부만으로는
                                      // "정리 항목이 몇 시에 처리됐나" 를 볼 수 없다
  doneById?                           // 누가 눌렀는지 (누구나 누를 수 있다 — 5-4)
}
// '지연' 컬럼이 없다 — 시각에서 계산한다 (5-2)

/* ── 재정 ── */
BudgetCategory { id, retreatId, level1, level2, level3a?, level3b?, level3c?, plannedAmount }
ExpenseEntry {
  id, retreatId, budgetCategoryId,
  receiptNumber, expenseDate, amount,
  payerId?, payerName, payerAccount,
  paid, paidDate?, note, receiptFileUrl?,
  isMealExpense,
  mealHeadcount?, mealAttendeeNames?[],
  subsidyAmount, personalBurdenAmount  // 자동 계산
}

/* ── 회차 준비 초안 (6-7) ── */
RetreatDraft {
  id, name, openDate, closeDate, mealSubsidyPerPerson,
  departmentKeys[], status,           // '수집중' | '생성완료' | '취소'
  createdRetreatId?
}

DraftSubmission {
  id, draftId, departmentKey,
  libraryIds[], adoptedTitles[], note?,
  savedAt?, submittedAt?              // 임시저장은 savedAt 만, 제출하면 submittedAt
}

/* ── 알림 (4-11) ── */
PushSubscription {
  id, userId, endpoint, p256dh, auth,
  userAgent?, createdAt, lastFailedAt?      // 한 사람이 기기를 여러 개 가질 수 있다
}

NotificationLog {
  id, userId, runId, kind,                  // '방치'|'기한 임박'|'기한 초과'|'선행 재촉'
  sentOn, payload                           // payload 에 그때의 상태·판정을 남긴다.
                                            // 사정이 바뀌었는지 알려면 그때 무엇이었는지가 있어야 한다
}

ActivityLog {
  id, retreatId, actorType, actorId?,  // actorType: 'user' | 'claude'
  action, targetType, targetId, before?, after?, createdAt
}
```

---

## 9. UI 공통 규칙

목업에 이미 반영되어 있습니다. 재구현할 때 놓치기 쉬운 것들입니다.

**대비** — 혼자 뜻을 지는 글자는 배경 대비 4.5:1 이상이어야 합니다. 처음엔 2.8:1이라 화면 전체가 흐렸습니다. 색을 진하게 해도 투명도가 걸려 있으면 소용없으므로 둘 다 확인합니다.

> **2026-08-31 무채색 기조로 바꾸면서 이 항목이 달라졌습니다** (4-0).
> 팀 색 틴트 위에 글자를 얹던 자리가 사라져 합성 대비 문제 자체가 없어졌습니다 —
> 이제 배경은 흰색 아니면 `#F7F7F5` 뿐입니다.
> **2026-09-02 에 값을 내렸습니다.** 그전에는 `--ink-2: #787774` 가 흰 바탕에서
> **4.48:1 로 이 문단의 기준(4.5:1)에 못 미치면서 기준으로 쓰이고** 있었고,
> `--ink-3: #9B9A97` 는 2.81:1 이었습니다. 사이드바·보조 글자가 화면 전체에서
> 흐리다는 말을 들었고, 재 보니 문서가 자기 기준을 어기고 있었습니다.
>
> | | 흰 바탕 | `#F7F7F5` 위 |
> |---|---|---|
> | `--ink-2` `#787774` → **`#6E6D6A`** | 4.48 → **5.17** | 4.17 → **4.82** |
> | `--ink-3` `#9B9A97` → **`#83827F`** | 2.81 → **3.84** | 2.62 → **3.58** |
> | `--ink` `#37352F` (그대로) | 12.26 | 11.43 |
>
> 색상은 그대로 두고 명도만 내렸습니다 — 무채색 기조(4-0)를 벗어나지 않고
> 검정으로도 가지 않습니다. **`--ink-3` 은 4.5:1 을 넘기지 않습니다** —
> 넘기면 보조와 구별되지 않아 위계가 무너집니다. 대신 **장식 자리에만**
> 쓴다는 규칙은 그대로이고(속성 라벨·메타처럼 옆의 값이 뜻을 다 지고 있는
> 자리), 혼자 뜻을 지는 글자에는 `--ink-2` 이상을 씁니다. 2.8:1 은 장식
> 자리에도 너무 옅어서 3.5:1 위로 올렸습니다.
>
> 아래의 옛 기록은 어떤 실패를 겪었는지 남겨 둡니다:
> 목업의 `--ink-3: #69726D` 가 팀 색 20% 틴트 위에서 3.4:1 밖에 나오지 않아
> `#4E5551` 로 진하게 고쳤던 일 — 색으로 면을 채우면 그 위의 글자가 따라서 어두워져야 합니다.

**바 라벨** — 바 안에서 시작해 길면 그대로 밖으로 흘러나갑니다. 바 위와 밖의 배경이 다르므로 같은 글자를 두 겹으로 겹쳐 경계에서 잘라 이어붙입니다.

> 폭은 캔버스 `measureText` 로 잽니다. 요소의 `scrollWidth` 는 `지연` 배지 유무에 따라 달라져 항목마다 결과가 갈립니다. 글꼴 로딩 전에 재면 값이 틀리므로 `document.fonts.ready` 이후 다시 계산하고, 부서를 접거나 필터가 바뀔 때도 다시 잽니다. 두 겹은 같은 글자를 같은 좌표에 깔고 `clip-path` 로 안쪽은 바 경계까지, 바깥쪽은 그 뒤부터만 보이게 잘라 이어붙입니다.

**사람에 대한 평가는 이 시스템에 넣지 않습니다.** 회의에서 나왔더라도.

0장의 "지식은 사람이 아니라 기록에 남긴다" 는 **업무 지식**을 말한 것입니다 —
"명찰 사이즈를 스트랩 재고 때문에 바꿨다" 같은 것. **사람 품평이 아닙니다.**

실제로 옮겨 온 회의록에 그런 대목이 있습니다. 2026-06-21 총무팀 회의는 여덟
명의 MBTI 와 개인 평가입니다(`체력 약함`, `추진력 약함`, `사람 쪼는거 못함`).
역할을 나누려고 그 자리에서 적은 것이지만, 회차를 넘어 따라다니는 기록이
되면 뜻이 완전히 달라집니다 — 그때의 메모가 **다음 담당자에게는 인사 자료로
읽힙니다.**

- 옮기는 스크립트는 **자동으로 빼지 않고 미리보기에서 짚어 줍니다.**
  기계가 사람 평가와 업무 메모를 가릴 수 없습니다 — `재정에 익숙` 은 평가이고
  `재정 담당 양준서` 은 업무입니다. **사람이 보고 정합니다**
- **나중에 전사(STT) 파일을 넣기 시작할 때 이 줄이 기준입니다.** 회의 녹취에는
  이런 말이 훨씬 많이 들어 있습니다

**화면이 값을 `data-*` 에 캐시할 때는 훅의 주어와 같은 이름을 씁니다.**
담당자를 `data-assignee` 에 넣어 두고 나중에 다시 읽는다면, 그 값을 갱신해
주는 것은 `onAssignee` 입니다 — 이름이 같아야 **"안 쓴다고 적어 두고 그
주어를 캐시하고 있다"** 를 기계가 잡습니다(`test_u_16b`). `data-owner` 처럼
다른 이름을 쓰면 그 시험이 조용히 지나갑니다. 지금 잡히는 것은 마침 이름이
같아서이지 규칙이 있어서가 아니었으므로, 여기 규칙으로 적습니다.
`data-run` 처럼 훅이 없는 것은 해당 없습니다 — 바뀌지 않는 값입니다.

**겹침 순서** — 날짜 헤더 > 왼쪽 라벨 열 > 선택·연결된 바 > 일반 바. 선택된 바가 라벨 열보다 위에 오면 안 됩니다.

**드래그 후 클릭** — 폭 조절 드래그가 끝나면서 발생하는 클릭이 "빈 영역 클릭"으로 잡힙니다. 드래그 직후 짧은 시간의 클릭은 무시합니다.

**빈 영역 클릭의 예외 범위** — 상세 패널을 닫지 않을 대상은 **바와 왼쪽 업무명뿐**입니다. 행 전체를 예외로 두면 행 안의 빈 레인까지 예외가 되어 패널이 영영 닫히지 않습니다. 필터 때문에 행에도 `data-run` 을 달았다면 선택자를 `.bar[data-run]` 으로 좁혀야 합니다. (목업은 `data-t` 를 바에만 달아 이 문제가 없었습니다.)

**상태 배지** — 배지를 다시 누르면 상태를 바꾸지 않고 목록만 닫힙니다. 목업에는 없던 동작이지만 드롭다운으로서 당연히 기대되는 것입니다.

**축 밖으로 나간 날짜** — 업무를 축의 시작보다 앞으로 옮기면 그릴 칸이 없습니다. 이때 "못 찾았으니 마지막 칸"으로 처리하면 **앞으로 당길수록 뒤로 밀립니다.** 범위 밖은 가까운 쪽 끝(앞이면 첫 칸)에 붙이고, 축보다 앞으로 나갔으면 보드를 다시 그려 축을 늘립니다.

**좁은 폭의 날짜** — 상세 패널을 최소(320px)로 줄여도 `2026-05-17` 이 잘리면 안 됩니다.
`input[type=date]` 는 내용에 맞춰 줄지 않으므로 자간을 좁히고(`letter-spacing:-.055em`)
최소 폭을 직접 줍니다. 담고 있는 줄은 `flex-wrap:wrap` 으로 두어 넘칠 때 접히게 합니다.

**격자 좌표** — 서브그리드를 쓸 때 열 인덱스가 한 칸 밀리기 쉽습니다. 실제 날짜와 화면 위치가 맞는지 몇 개 지점을 계산해 확인합니다. 주 단위 첫 칸·일 단위 전환 지점·수련회 칸 세 곳은 `tests/test_board_web.py` 가 지키고 있습니다.

**모바일** — 820px 아래에서는 24칸 간트 대신 **D-주차 → 부서** 순 목록으로 바꿉니다. 부서를 바깥 묶음으로 두면 "이번 주에 뭐가 있나"가 보이지 않습니다. 소속 외 업무는 여기서도 숨기지 않고 흐리게 둡니다.

---

## 10. 화면 변경을 끝냈다고 말하기 전에

이 두 가지를 놓쳐 같은 종류의 버그를 반복했습니다. 규칙으로 못박습니다.

**속성이 아니라 화면을 확인합니다.** `el.hidden === true` 는 "숨겨졌다"는 뜻이 아닙니다. `display` 를 준 author 규칙이 `[hidden]` 의 `display:none` 을 이깁니다. 판정은 항상 `getComputedStyle(el).display` 나 `offsetParent` 로 합니다. (`[hidden]{display:none!important}` 를 넣어 두었지만, 확인 방법 자체가 틀리면 다음에 또 놓칩니다.)

**한 동작을 확인할 때 주변이 그대로인지도 같이 확인합니다.** "편집창이 열렸는가"만 보고 "드로어가 아직 열려 있는가"를 보지 않아 두 번 놓쳤습니다. 보드에서 무엇을 누르든 아래가 유지되어야 합니다.

| 누른 뒤에도 | 이유 |
|---|---|
| 드로어가 열려 있다 | 패널 안의 버튼은 패널을 닫지 않는다 |
| 부서 그룹이 펼쳐진 채다 | 보던 자리를 잃지 않는다 |
| 스크롤 위치가 그대로다 | 이동은 명시적으로 요청했을 때만 |
| 콘솔에 오류가 없다 | |

**DOM 을 갈아끼우는 핸들러는 판정을 먼저 해 둡니다.** `innerHTML` 로 다시 그리면 눌린 요소가 트리에서 떨어져 나가고, 그 뒤 문서 핸들러가 `closest()` 로 물으면 전부 `null` 이 나와 "바깥 클릭"으로 오판합니다. 클릭이 어디서 일어났는지는 **캡처 단계에서** 기록해 두고 그 값을 씁니다.

**글자를 찾는 시험은 코드와 설명을 못 가립니다** — 지금까지 **네 번** 당했습니다
(`code_only` · `test_v_09` · `_token` · `test_y_04`). "그 말이 없어야 한다" 는
시험을 쓸 때, 찾는 말이 **주석이나 시험 자신의 단언문 안에** 있으면 고쳐 놓고도
빨갛거나 안 고쳤는데 초록입니다. 주석·독스트링·문자열을 걷어내고 보거나,
함수 이름만 모아서 봅니다.

**파일 단위로도 같습니다.** 어떤 것을 금지하는 도구는 그것을 설명하려고
스스로 담게 됩니다 — 2026-09-04 에 실명 검사가 **자기 주석 셋 · 넘김
목록 머리 · 남은곳 목록 · 검토 보고** 다섯에 걸렸습니다.
**"이 도구가 금지하는 것을 설명하려면 무엇을 적어야 하는가" 를 만들기
전에 묻고, 주석만 적힌 상태에서 자기 검사를 한 번 돌립니다** — 다 만들고
돌리면 다섯 번 빨개진 뒤에 압니다.

**자리를 세어 두지 않습니다.** 처음에 "거의 늘 넷" 이라고 적었는데 같은
작업 안에서 다섯이 됐습니다. 그때 없던 파일이 새로 생겼기 때문입니다 —
**도구가 자라면 자리도 늘어납니다.**

**그리고 자리를 아는 것과 예외로 두는 것은 다릅니다.** 검토 보고는
자리이지만 **예외가 아닙니다** — 운영 자료에서 온 것을 계속 담을 파일이라
빼면 정확히 새는 자리를 안 보게 됩니다. 가려서 적습니다.

**pytest 는 화면을 못 봅니다** — 지금까지 **세 번** 당했습니다
(`.mlist` 이름 충돌로 목록이 통째로 안 보임 · `class="mrow{{"` 이름 바꾸기
누락 · 새 업무 제안에 누를 수 없는 단추). 셋 다 **시험은 초록이었고 브라우저로
열어서 알았습니다.** 화면을 바꿨으면 열어 봅니다 (11-3 의 2단계).

**규칙을 지켰는지 스크립트로 돌립니다** — `docs/checks/drawer.js`. 보드를 열고 콘솔에 붙여넣으면 상세 패널의 모든 조작을 눌러 보고, 매번 위 표의 항목이 유지되는지 확인합니다. 반대편(빈 영역 클릭으로는 **닫혀야** 한다)도 함께 봅니다 — 규칙이 과하게 걸려 정작 닫혀야 할 때 안 닫히면 그것도 버그입니다.

> 2026-08-31 UI 를 노션 방향으로 옮기면서(4-0) 다시 돌렸습니다. **기존 26항목은
> 선택자를 하나도 고치지 않고 그대로 통과했습니다** — 상단 탭 줄을 없애고 사이드바를
> 붙이고 색·여백을 바꾼 것이 상세 패널의 조작에는 닿지 않았기 때문입니다.
> 규칙이 더 이상 맞지 않게 된 항목은 없어서 지우거나 바꾼 것도 없습니다.
> 대신 새로 생긴 자리를 같은 기준으로 덮도록 항목을 더했습니다 — 첨부파일 탭(맨 끝인가 ·
> 올리는 자리가 권한과 맞는가 · 하단에 `파일 첨부` 버튼이 없는가), 사이드바(접힌 채로
> 시작하는가 · 토글은 본문을 밀고 가장자리 호버는 겹쳐 뜨는가), 그리고 가로 격자선이
> 없고 세로선만 남았는가. 지금은 **보드 61항목 · 달력 62항목**입니다
> (화면마다 볼 것이 달라 수가 다릅니다 — 달력에는 점·비침·보던 달이,
> 보드에는 격자선과 왼쪽 라벨 열이 있습니다).

---

## 11. 기술 스택

| 영역 | 선택 |
|---|---|
| 프런트엔드 | 반응형 웹앱 + PWA (모바일 우선) |
| 백엔드/DB | 실제 DB. 150명 이상, 동시 편집, 권한 분리 |
| 파일 저장 | **서버 디스크** (`config.ATTACHMENT_DIR`). DB 밖이라 백업 대상 — 11-2 |
| 인증 | **초대 링크** (총무팀 발급 · 카카오톡 전달). SMS 는 접었습니다 — 4-12 |
| 알림 | 웹 푸시 |
| 회의록 읽기 | **Claude API** (`app/domain/llm.py`). 키는 `data/anthropic_key.txt` — 저장소 밖. **없어도 앱은 뜨고** 낱말 겹침으로 물러섭니다. 넣는 법은 `docs/배포-안내.md` 14장 |
| 배포 | 집 윈도우 서버 + Cloudflare Tunnel (무료). 절차는 `docs/배포-안내.md` |

---

## 11-2. 운영

**앱은 `127.0.0.1:8000` 에만 엽니다.** 바깥은 Cloudflare Tunnel 로만 들어옵니다 —
공유기 포트를 열지 않습니다. 앱·터널·정기 작업은 윈도우 **작업 스케줄러**가 띄웁니다.
별도의 서비스 도구를 깔지 않는 이유는 관리할 것을 하나로 줄이기 위해서입니다.

**절차는 `docs/배포-안내.md`** 에 있습니다. 명령어를 아는 사람이 아니라 처음 하는
사람 기준으로 썼고, 각 단계에 "이렇게 나오면 성공"을 함께 적었습니다.

**문서에 적는 명령은 PowerShell 로 씁니다.** 이 노트북이 개발기이자 운영
서버이고, 작업 스케줄러와 터널과 재시작 확인이 전부 PowerShell 입니다 —
명령을 두 껍데기로 적으면 붙여 넣는 사람이 어느 창에서 돌릴지 알 수 없습니다.
줄을 잇는 것은 `` ` ``(백틱)이고, 환경변수는 `$env:NAME` 이며,
경로를 옮기는 것은 `cd "경로"` 입니다 — `/d` 스위치는 없습니다.
그리고 **PowerShell 이 같은 이름의 별칭으로 가리는 명령은 `.exe` 를**
붙입니다 — `sc.exe` · `curl.exe` · `where.exe`.

> **cmd 문법을 PowerShell 창에 붙여 넣으면 「붙여넣었는데 명령이 안 돈다」**
> 가 됩니다. 눈에 띄게 오류가 나기도 하고, **파싱은 되고 실행만 실패하거나**
> **엉뚱한 것이 돌기도** 합니다 — `cd /d` 는 오류가 나고, `sc` 는 아무것도
> 안 나온 채 `query` 라는 파일을 만듭니다.
>
> 재 본 것 (Windows PowerShell 5.1):
>
>     잇기 한 줄만 넣으면   -> 인자로 딸려 들어간다: ['data/x.json', '~']  exit 0
>     두 줄을 한꺼번에      -> 파스 단계에서 실패 (첫 줄도 안 돈다)
>     cd /d "..."           -> 파싱은 통과하고 실행이 실패한다
>     sc                    -> Set-Content 별칭이라 파일이 만들어진다
>     curl                  -> Invoke-WebRequest 별칭이라 HTML 이 안 나온다
>     curl.exe · sc.exe     -> 원래 것이 그대로 돈다
>
> (위의 `~` 는 이 문서가 스스로 검사에 걸리지 않게 바꿔 적은 것입니다.
> 실제로는 cmd 의 줄 잇기 글자입니다.)
>
> **대화형 콘솔에 두 줄을 한 번에 붙여넣는 경우는 재지 않았습니다** — 호스트와
> PSReadLine 판에 따라 갈립니다. 그래서 「첫 줄이 이미 실행된 뒤에 오류를 본다」
> 고 적지 않습니다. **잰 것만 적습니다.**
>
> **예외는 `.bat` 뿐입니다** — 그건 cmd 가 읽습니다. **이름을 세지 않습니다**
> (10장) — `git ls-files "*.bat"` 로 찾습니다.
>
> **`scripts/*.py` 의 모듈 독스트링은 규칙 안입니다.** 코드처럼 보이지만
> 하는 일은 문서입니다 — argparse 의 `description` 과 이어져 있지 않아
> `--help` 로도 안 나오고, **사람이 파일을 열어 긁어 가는 사용법**입니다.
> 실제로 `import_programs.py` 의 그 줄은 **5-5 에 적힌 명령과 같은 것**이라,
> 한쪽만 고치면 저장소에 같은 명령이 두 판으로 남습니다.

`tests/test_docs_shell.py` 가 문서의 코드 블록을 매번 봅니다.

### 백업

| 대상 | 잃으면 |
|---|---|
| `data/app.db` | 모든 기록. 재정·논의가 들어간 뒤에는 되돌릴 방법이 없습니다 |
| `data/uploads/` | 올린 **파일이 전부** 사라집니다 — 첨부파일(4-9)과 영수증. **DB 밖에 있어서 `app.db` 만 되돌리면 목록에는 있는데 열리지 않습니다** |
| `data/vapid_private.pem` | 알림 구독이 **전부 조용히 죽습니다** — 아무 오류도 나지 않습니다 (4-11) |
| `data/secret_key.txt` | 전원이 로그아웃됩니다. 다시 초대 링크를 보내야 합니다 (4-12) |

`scripts/backup.py` 를 **매일 새벽 3시**에 작업 스케줄러가 부릅니다.
`data/backups/` 에 날짜를 붙여 쌓이고 **30개**까지 남깁니다 —
`app-날짜.db` · `uploads-날짜.zip` · `vapid-날짜.pem` 을 같은 날짜로 묶어 함께 지웁니다.

**파일 복사가 아니라 `VACUUM INTO` 를 씁니다.** 누군가 쓰는 중에 복사하면 반쯤 쓰인
페이지가 섞인 파일이 남는데, 그 파일은 **열어 보기 전까지 멀쩡해 보입니다.**
`VACUUM INTO` 는 SQLite 가 일관된 스냅샷을 직접 만들어 줍니다.

**업로드 폴더는 zip 하나로 묶습니다.** 올라온 파일이 하나도 없으면 만들지 않습니다 —
빈 zip 이 30개 쌓여 있으면 "백업에 파일이 있다"와 "원래 없었다"를 구별할 수 없습니다.

**되돌리기** — 서버를 끄고 **같은 날짜끼리 셋을 함께** 되돌립니다.
`app-날짜.db` → `data/app.db`, `uploads-날짜.zip` → `data/uploads/`,
`vapid-날짜.pem` → `data/vapid_private.pem`. 날짜를 섞으면 DB 의 목록과 실제 파일이
어긋나고, VAPID 를 빼먹으면 구독이 살아나지 않습니다.

**백업도 같은 컴퓨터에 있습니다.** 집 컴퓨터가 고장 나면 함께 사라지므로 가끔
밖으로 복사해 둡니다. 다만 **폴더째 통째로 복사하면 안 됩니다.**

업로드 zip 은 내용이 안 바뀌면 다시 묶지 않고 **지난 것과 한 파일을 같이
가리키게** 해 둡니다(하드링크, 4-9). 그래서 이 컴퓨터에서는 30벌이 몇 벌
크기밖에 안 씁니다. 그런데 **USB(exFAT)·구글 드라이브·원드라이브는 그 이어짐을
모릅니다** — 복사하는 순간 전부 실체로 펼쳐집니다. 업로드가 1GB 면 30벌이
**30GB** 가 됩니다.

`MAX_TOTAL_BYTES`(10GB)는 **중복을 뺀 크기**라 복사본 크기를 전혀 묶어 주지
못합니다. 그러므로 **최근 몇 벌만 골라 복사합니다** — 실제 명령은
`data/backups/읽어보기.txt` 와 `docs/배포-안내.md` 에 적어 두었습니다.
그 안내는 백업 폴더 안에 두는 것이 맞습니다. 복사하려고 폴더를 연 사람이
그 자리에서 보게 되기 때문입니다.

**옛 zip 을 열어 고쳐 저장하지 않습니다.** 같은 파일을 가리키던 다른 날짜의
zip 도 함께 바뀝니다. 들여다보는 것은 괜찮고, 고쳐 저장하는 것만 안 됩니다.

### 저장소와 실명

**코드는 GitHub 의 공개 저장소에 있습니다** — `Jun9293/daecheongbu-retreat`.
2026-09-01 에 공개로 바꿨습니다. 그래서 **저장소에는 실명이 하나도
없습니다** — 공개 전에도 그렇게 해 두었기 때문에 바꾸는 데 따로 손댈 것이
없었습니다. 앞으로도 실명이 든 파일을 새로 만들어 커밋할 때만 신경 쓰면 됩니다.

| 저장소에 있는 것 | 저장소 밖에 있는 것 (gitignore) |
|---|---|
| 가명 (박민준·정하윤·최도현 …) | `data/app.db` — 진짜 기록 전부 |
| 가명으로 바꾼 프로그램표 | `data/*.real.json` — 실명이 든 원본 |
| | `data/anonymize-map.json` — 실명↔가명 대응표 |
| | `data/secret_key.txt` · `vapid_private.pem` · `uploads/` |

**대응표를 저장소에 올리면 안 됩니다.** 실명과 가명을 나란히 적어 둔 것만으로
원래 이름이 드러나서, 바꿔 놓은 의미가 없어집니다.

**평소에 쓰는 동안에는 아무것도 안 해도 됩니다.** 화면에서 입력한 것은 전부
`app.db` 로 가고 그 파일은 애초에 올라가지 않습니다.

**커밋할 때마다 `scripts/check_names.py` 가 봅니다** (11-3 1단계).
전에는 *"실명이 든 파일을 새로 만들어 커밋할 때만 신경 쓰면 된다"* 고
적어 두었는데, **그 문장이 틀렸습니다** — 생성 스크립트가 운영 DB 를
읽어 그 파일을 매번 만들어 냈습니다. 사람이 언제 신경 쓸지를 정하는
규칙이 아니라, **매번 도는 검사**여야 합니다.

바꾸는 것은 `scripts/anonymize.py` 입니다
(`--apply` 없이 돌리면 무엇이 바뀔지 보여주기만 합니다).

> **한 이름이 두 사람을 가리키고 있었습니다.** 성이 다른 두 사람이 있는데
> 대응표에는 이름만 적힌 한 줄뿐이라, 성이 붙은 쪽은 애초에 안 바뀌고
> 이름만 적힌 쪽만 바뀌었습니다. **그래서 지금까지 커밋된 그 가명은
> 전부 한 사람(성이 없는 쪽)이고 맞습니다** — 섞인 파일이 없으므로
> 되돌릴 것도 없습니다. 2026-09-04 에 대응표를 **한 줄 = 한 사람**으로
> 바꾸고(`{"가명": …, "표기": [ … ]}`), 한 표기가 두 사람에게 있으면
> **읽기가 거절**하게 했습니다. 옛 모양 `[실명, 가명]` 도 그대로
> 읽으므로 표기가 하나뿐인 사람은 옮기지 않았습니다.
>
> **2026-09-04 에 새어 나갔습니다.** 생성 스크립트가 운영 DB 를 읽어
> `docs/review/` 에 바로 썼고, 커밋 전 실명 검사는 대응표 구조를 잘못
> 읽어 **29개 중 0개를 보고 있었습니다.** 회의록에 적힌 봉사자 이름
> 몇이 공개 커밋에 남았습니다(연락처·계정·토큰은 아닙니다).
> 히스토리는 고치지 않았습니다 — 되돌린다는 보장이 없는데 지금까지의
> 검토 주소가 전부 죽습니다. 지금부터 막는 쪽으로 정했습니다.
>
> **같은 날 하나가 더 나왔습니다.** 대응표에 **아예 없던 이름**이라
> 검사가 못 봤습니다 — `check_names.py` 는 **대응표에 있는 표기만**
> 찾기 때문입니다. "아무것도 안 보는 것은 통과가 아니다" 는 막았는데
> **"볼 목록이 불완전하다" 는 안 막혀 있었습니다.** 사람이 원본과
> 공개본을 손으로 대조해서야 찾았습니다(셋 중 둘은 바뀌었는데 하나만
> 남아 있었습니다). 그래서 `check_names.py` 가 **운영 자료의 담당자
> 칸에 있는데 대응표에 없는 이름**을 세어 말합니다 — 막지 않고
> 말만 합니다. 사람 이름인지는 사람만 알기 때문입니다.
> 히스토리는 이것도 고치지 않았습니다.

**생성물은 `data/` 에 씁니다.** 운영 DB 를 읽어 글을 만드는 것은
`data/*.real.md` 로 나가고 그 이름은 gitignore 됩니다 — `data/*.real.json`
과 같은 규칙입니다. **채점·검토는 그 파일로 합니다**(실명이 보여야 합니다).
저장소에는 `--공개` 로 만든 익명화 사본만 올립니다.

**바꾸는 것과 찾는 것을 가릅니다.**

| | 하는 일 | 경계 |
|---|---|---|
| `scripts/anonymize.py` | **바꾼다** | 엄격 — 앞뒤가 한글·영문·숫자면 건너뜀. 안 그러면 `필요한`·`진행` 이 깨집니다 |
| `scripts/check_names.py` | **찾기만 한다** | 느슨 — 글자가 들어 있으면 전부 짚습니다 |

엄격한 규칙이 놓치는 자리가 실제로 둘 있었습니다 — **성이 붙은 이름**
(`최○○`: 앞이 한글이라 건너뜀)과 **`M` 뒤에 조사가 붙은 것**
(`○○M으로`: 뒤가 한글이라 건너뜀). 둘 다 공개 커밋에 남았습니다.
**규칙을 느슨하게 하지 않습니다** — 그러면 `진행` 이 깨집니다. 대신
느슨하게 *찾는* 도구를 따로 두고, 오탐은 `docs/이름-확인됨.txt` 에
**왜 넘기는지와 함께** 적습니다. 오탐은 사람이 한 번 보고 넘기면 되지만,
못 찾은 실명은 공개 저장소로 나갑니다.

**`docs/` 로 무언가를 커밋하기 전에 `check_names.py` 를 돌립니다.**
읽은 실명이 0개면 **통과가 아니라 실패**입니다 — 아무것도 안 보고 초록을
내는 것이 이 사고의 절반이었습니다. `tests/test_check_names.py` 가 그것을
지킵니다.

> **바꿀 때 지킨 것** — 글자 수와 `M` 접미사를 그대로 뒀습니다. 화면 폭·정렬이
> 그대로 시험되고, `이름`/`이름M` 짝을 맞추는 `normalize_names.py`(5-3)의 시험도
> 그대로 성립합니다. 부서(`총무M`)·역할(`담당M`)·파트 이름은 사람 이름이 아니라
> 건드리지 않았습니다.
>
> **앞뒤가 한글·영문·숫자가 아닐 때만 바꿉니다.** 이름을 그냥 문자열로 바꿨다가
> `필요한`·`중요한`·`진행` 같은 낱말이 함께 깨진 적이 있습니다.

### 서버를 켜 둔 채 코드를 고치지 않습니다

파이썬은 **켤 때 읽은 코드를 그대로 들고 있습니다.** 그래서 서버를 켜 둔 채
코드를 고치면, 그 프로세스 안에서는 옛 코드가 계속 돕니다.

전에는 화면(템플릿)만 요청마다 디스크에서 다시 읽어서 **새 화면 + 옛 코드**가
짝지어졌고, 화면이 라우터가 아직 안 넘기는 값을 읽어 500 이 났습니다.
`/live` 와 `/live/staff` 가 이 조합으로 세 번 죽었습니다 — **코드는 맞았고
테스트도 통과했는데 돌던 서버만 옛것이었습니다.**

그래서 **운영에서는 템플릿도 켤 때 한 번만 읽습니다**(`app/templating.py` 의
`auto_reload`). 둘이 함께 낡으므로 화면이 잠깐 옛것일 수는 있어도 서로 어긋나지는
않습니다. **고친 것을 반영하려면 서버를 다시 켜야 합니다.**

**그런데 껐다고 생각했는데 안 꺼집니다.** 작업 스케줄러에서 작업을 멈추면
**작업만 멈추고 그 아래 파이썬은 남습니다** — 프로세스가
`cmd.exe`(serve.bat) → `python.exe` → `python.exe`(8000 을 쥔 쪽) 로 이어져
있어서 맨 위만 없어집니다. 포트는 계속 열려 있으니 겉으로는 재시작된 것처럼
보이는데 **어제 뜬 프로세스가 그대로 답합니다.** 2026-09-02 에 이것으로
상세 패널이 `Drawer is not defined` 로 안 열렸습니다 — 또 새 화면 + 옛 코드입니다.

**그래서 재시작은 포트를 쥔 프로세스를 직접 끝내고, PID 가 바뀌었는지로
확인합니다.** 포트가 열려 있는 것으로는 알 수 없습니다 — 안 꺼진 옛 프로세스도
포트를 쥐고 똑같이 답하기 때문입니다. 절차는 `docs/배포-안내.md` 12장에
붙여넣을 수 있는 형태로 있습니다.

> 개발 서버(`scripts/devserve.bat`)는 `--reload` 를 켜 두었지만 **믿을 만하지
> 않습니다** — 몇 번 반응하다 변경을 놓친 적이 있습니다. 화면이 이상하면 먼저
> 껐다 켜 보세요.
>
> **`.bat` 는 cp949 로 저장합니다.** UTF-8 로 두면 한글 주석이 깨져 `set` 줄까지
> 파싱이 틀어집니다. 실제로 개발 서버가 운영 DB 를 읽은 적이 있습니다.

### 자가진단

`scripts/healthcheck.py` 가 넷을 봅니다 — DB 연결, VAPID 준비, 관리자 계정,
바깥에서의 접속. 하나라도 아니면 무엇을 하면 되는지 한국어로 말합니다.

---

## 11-3. 작업을 마칠 때

**작업마다 똑같이 하는 넷입니다.** 지시문에 매번 옮겨 적으면 언젠가
한 번은 빠지므로 여기 둡니다. 앞으로 지시문에는 **"마무리는
CLAUDE.md 11-3 대로"** 한 줄이면 됩니다.

**막혀서 중간에 멈출 때도 4단계는 합니다.** 남에게 물어보고 싶은
때가 바로 그때입니다 — 자세한 것은 4단계에.

맨 아래 **「마치기 전에 되짚는 것」** 은 하는 일이 아니라 목록입니다 —
넷을 마친 뒤 읽습니다.

### 1. 커밋하고 푸시한다

저장소는 **공개**입니다 (11-2 의 '저장소와 실명').

- `git add -A` 뒤 **커밋하기 전에** `git status --short` 를 먼저 봅니다.
  `data/` 안의 것(`app.db` · `uploads/` · `backups/` · `secret_key.txt` ·
  `vapid_private.pem` · `server.log`)이나 `testdata/` · `link.txt` 가
  하나라도 잡히면 **멈추고 알립니다.** 밀어 넣지 않습니다
- **실명은 `scripts/check_names.py` 로 봅니다.** 손으로 쓰는 코드로
  확인하지 않습니다 — 2026-09-04 에 그렇게 하다 대응표를
  `list(m.keys())` 로 읽어 **실명 29개 중 0개를 보면서 초록**을
  냈습니다. 두 번의 푸시가 그 초록을 믿고 나갔습니다.
  **아무것도 안 보는 검사는 통과가 아닙니다.**
  **`걸린 것 없음` 은 0건이 아닙니다.** 끝의
  `아직 안 고친 곳 N곳` 을 함께 봅니다 — 그 목록
  (`docs/이름-남은곳.txt`)이 다음에 할 일입니다.
  **0곳이면 0곳이라고 말합니다** — 안 찍히면 그 줄이 무엇을
  가리키는지 흐려지고, 목록이 있다는 것조차 잊힙니다
- **`대응표에 없는 담당자 표기 N개` 도 함께 봅니다.** 검사는 대응표에
  **있는** 표기만 찾으므로, 표에 없는 이름은 그 줄에만 나옵니다 —
  실제로 그 틈으로 이름 하나가 공개 커밋에 남았습니다 (11-2).
  **막지 않고 말만 합니다.** 사람 이름인지는 사람만 압니다
- 실제 연락처·초대 토큰 원문이 새로 들어가지 않았는지 함께 봅니다
- **운영 DB 를 읽어 파일을 쓰는 것은 `data/*.real.*` 로 나갑니다.**
  저장소로 갈 때는 익명화를 거친 사본만 갑니다 (11-2). 생성
  스크립트가 공개 폴더에 바로 쓰면 **돌릴 때마다 실명이 다시
  들어옵니다** — 한 번의 실수가 아니라 열린 길입니다
- **작업 단계별로 나눠 커밋합니다.** 한 덩어리로 뭉치면 나중에 무엇 때문에
  무엇이 바뀌었는지 읽히지 않습니다
- 각 메시지에 **왜 그렇게 했는가**를 넣습니다. 무엇을 바꿨는지는 diff 가
  이미 말합니다. 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- `git push origin main` 뒤 `git log --oneline -6` 과 `git status` 를 봅니다

### 2. 재시작하고, **돌고 있는 서버에서** 확인한다

절차는 `docs/배포-안내.md` **12-1** 입니다. 그대로 붙여넣습니다.

- **`Stop-ScheduledTask` 만으로는 안 꺼집니다** — 그 아래 파이썬이 남습니다.
  프로세스가 `cmd.exe`(serve.bat) → `python.exe` → `python.exe`(8000 을 쥔
  쪽)로 이어져 있어 맨 위만 없어집니다
- **성공 판정은 PID 가 바뀌는 것입니다.** 포트가 열린 것으로는 알 수
  없습니다 — 안 꺼진 옛 프로세스도 포트를 쥐고 똑같이 답합니다
- `!! 아직 …이(가) 포트를 쥐고 있습니다` 가 뜨면 **관리자 권한이 필요합니다.**
  멈추고 사용자에게 알립니다. 억지로 밀지 않습니다

올라온 뒤 `http://127.0.0.1:8000` 을 **실제로 받아서** 이번 작업이 반영됐는지
봅니다. 무엇으로 볼지는 그 작업에 맞게 정합니다 — 화면의 스크립트 주소가
내용 해시인지, 응답 헤더가 바뀌었는지, 그 화면에 새 요소가 있는지.

> **"테스트가 통과했다" 로 끝내지 않습니다.** 이 규칙은 실제로 한 번 당한
> 뒤에 생겼습니다 — 통과로 보고한 기능이 사용자 화면에는 없었습니다.
> 코드는 맞았고 pytest 도 초록이었는데 **브라우저가 옛 파일을 쓰고
> 있었습니다**(Cloudflare 가 `/static` 에 4시간 캐시를 붙이는데 주소가
> `?v=1` 로 고정이었습니다). 그전에는 서버가 안 꺼져서 옛 코드가 답한
> 적도 있습니다. **돌고 있는 서버에서 확인한 것만 확인입니다.**

재시작 전후 PID 와 확인한 결과를 보고에 적습니다.

**서버가 읽는 것이 하나도 안 바뀌었으면 건너뜁니다** — 문서·시험·
스크립트만 고쳤을 때입니다. `app/` · `templates/` · `static/` 이
하나라도 바뀌었으면 건너뛰지 않습니다. **건너뛰었으면 그 사실과
이유를 보고에 한 줄 적습니다** — 안 적으면 빠뜨린 것과 구별되지
않습니다.

**커밋 기준으로 봅니다.** 판에서 안 바뀌었어도 이번에 내는 커밋에
`app/` · `templates/` · `static/` 이 들어 있으면 건너뛰지 않습니다 —
앞선 판에서 확인했더라도, **그 커밋을 여는 사람은 판을 모릅니다.**
앞서 확인한 것이면 **그 PID 와 시각을 다시 적습니다.**

### 3. CLAUDE.md 에 옛말이 남아 있지 않은지 되짚는다

**이 문서는 "매번 먼저 읽어야 하는 유일한 기준 문서" 입니다.** 그런데 한 곳을
고치고 그 사실을 적어 둔 다른 곳을 안 고치면, 다음 사람이 **옛 기준에서
시작합니다.** `__unused` 의 이유가 낡는 것과 정확히 같은 모양입니다.

- 이번에 바꾼 동작의 **옛 표현을 그대로 찾아봅니다.** 예컨대 달력의 점을
  누르는 동작을 바꿨다면 `/board?task=` 를, 상한을 바꿨다면 옛 숫자를
- 12장 로드맵은 특히 낡습니다. 각 항목에 "완료. 규칙은 N-N" 하고 **동작 요약을
  같이 적어 두었기 때문**입니다 — 그 요약이 본문과 갈립니다
- **왜 이 단계가 생겼나** — 4-13 을 "그 자리에서 열린다" 로 고치면서 12장에
  남아 있던 "`/board?task=` 로 보드의 상세 패널이 열립니다" 를 놓쳤습니다.
  같은 회차에 11장 기술 스택 표(파일 저장 `Google Drive API`, 인증 `SMS`)와
  10장의 항목 수도 낡아 있었습니다. **셋 다 한참 전에 바뀐 것들입니다**

### 4. 채팅에 붙여넣을 것을 만든다

**하나의 코드블록**으로 냅니다 — 사람이 복사 버튼 한 번으로 통째로 가져가
다른 Claude 채팅에 붙여넣습니다. 덩어리 밖에 설명을 덧붙이지 않습니다.

**어디부터 어디까지가 이쪽이 쓴 것인지 덩어리가 스스로 말합니다.**
사람이 그 덩어리에 자기 의견을 덧붙이므로, 받는 쪽이 **누가 한 말인지
갈라 읽어야** 하기 때문입니다.

```
━━━ Claude Code 여기부터 ━━━ 커밋 <짧은해시> · 브랜치 <이름> · origin: O
(덩어리 내용)
━━━ Claude Code 여기까지 ━━━
```

- **첫 줄은 상태 표기와 한 줄로 합칩니다.** 줄이 늘면 언젠가 하나가 빠집니다
- **끝 표시를 반드시 냅니다.** 시작만 있으면 뒤에 붙은 것이 사람 말인지
  이쪽이 이어 쓴 것인지 여전히 알 수 없어, 표시한 뜻이 없어집니다
- 커밋 전에 멈춰서 낼 때는 첫 줄을 이렇게 적습니다 —
  `━━━ Claude Code 여기부터 ━━━ 커밋 <해시> · 브랜치 <이름> · origin: X — 저장소를 열지 마세요, 옛것입니다`
- **덩어리 밖에는 아무것도 쓰지 않습니다.** 밖은 사람의 자리입니다.
  덧붙일 말은 덩어리 **안**에 씁니다

**모든 지시문에 붙입니다. 끝났을 때만이 아닙니다.** 2026-09-04 에 이것으로
한 번 빠뜨렸습니다 — 시험 하나가 빨개 커밋 전에 멈췄고, 이 단계가 "마무리"
안에 있어서 **안 끝났으니 안 만들었습니다.** 그런데 남에게 물어보고 싶은
때가 바로 그때입니다. 막혀서 멈춘 것·커밋하지 않은 것·조사만 한 것도
전부 만듭니다. **담는 내용이 달라질 뿐 덩어리는 언제나 있습니다.**

| 그때의 상태 | 덩어리에 담는 것 |
|---|---|
| 다 끝나고 푸시함 | 아래 서식 그대로 (커밋 해시·diff 주소·바뀐 파일) |
| **막혀서 멈춤** | 어디까지 했나 · **무엇이 막았나** · 내민 선택지 |
| 커밋하지 않음 | 주소가 없으므로 **바뀐 내용을 덩어리 본문에** 담고, 왜 커밋하지 않았는지 한 줄 |
| 조사·검토만 | 무엇을 봤나 · 알아낸 것 · 판단이 갈린 자리 |

**푸시하지 않았으면 주소를 지어내지 않습니다.** 푸시하지 않았으면
저장소는 **고치기 전 것을 그대로 보여줍니다** — 받은 사람은 그게
최신인 줄 알고 읽습니다. **링크가 없는 것보다 나쁩니다.**
주소 대신 **내용을 덩어리에 담습니다.**

주소는 **방금 푸시한 커밋의 전체 해시**(`git rev-parse HEAD`)를 씁니다.
`main` 을 쓰면 다음 푸시 뒤에 다른 것이 열립니다. 파일 목록은 이번에 실제로
바뀐 것만 넣습니다(`git diff --name-only <이전>..HEAD`) — 문서와 시험도
포함합니다.

**`main` 에 못 넣을 때는 검토 브랜치 하나로 갑니다.** 공개 저장소에
나가는 일이거나 pytest 가 빨간 채여서 `main` 에 밀 수 없을 때,
**채팅이 읽을 길이 없어집니다.** 그러면 우회로가 여럿 생깁니다 —
덩어리에 본문 담기 · 바탕화면 폴더 · raw 주소. **같은 것이 세 곳에
있는 상태**이고, 이 프로젝트가 가장 자주 고쳐 온 모양입니다.

길은 하나입니다 — `review/<날짜>-<주제>` 로 푸시하고 덩어리 주소를
그 브랜치로 줍니다. `raw.githubusercontent.com/<repo>/<브랜치>/<경로>`
는 브랜치 이름으로 그대로 열립니다. 공개 저장소에 나가는 것은 같지만
**`main` 이 아니라는 것이 이름에 적혀 있습니다.**
합친 뒤에도 **브랜치를 지우지 않습니다** — 지난 검토 주소가 그
커밋들을 가리킵니다.

**`compare/` 주소는 사람이 볼 것입니다.** 채팅은 그것을 못 엽니다
(GitHub 이 막습니다). 채팅에게 필요한 것은
`raw.githubusercontent.com` 쪽이므로 **바뀐 파일마다 raw 주소를
빠짐없이 답니다.**

```
코드 리뷰를 부탁합니다. 저장소가 공개이니 아래 주소를 직접 열어서 읽어 주세요.

이번 변경 diff
https://github.com/Jun9293/daecheongbu-retreat/compare/<이전>...<이번>

이번에 바뀐 파일
https://raw.githubusercontent.com/Jun9293/daecheongbu-retreat/<이번>/<경로>
(바뀐 파일마다 한 줄)

검토용 보고
https://raw.githubusercontent.com/Jun9293/daecheongbu-retreat/<이번>/docs/review/%EC%B5%9C%EA%B7%BC.md

무엇을 했나 — (단계별로 한 줄씩)

특히 봐 주셨으면 하는 것 — (판단이 갈린 자리, 확신이 없는 자리)
```

`docs/review/최근.md` 는 **매번 이번 작업으로 덮어씁니다. 빠뜨리지 않습니다** —
한 번 빠뜨렸더니 수용 기준의 통과 여부를 사람이 확인할 길이 없었습니다.
코드를 다 고치고 나면 이 파일이 남았는지부터 봅니다. 머리 세 줄은
`작성 시각: … (KST)` · `커밋: <짧은 해시>` · `작업: …` 이고, 그 아래에 한
것 · 수용 기준별 통과 여부와 테스트 함수명 · 요청받은 출력 · pytest 마지막
줄 · `git diff --name-only` · 판단이 들어간 자리의 발췌를 담습니다.
2000줄을 넘기지 않습니다.

### 마치기 전에 되짚는 것 (하는 일이 아닙니다)

**막는 코드에는 막히는 쪽 시험이 함께 있습니다.** 시험은 셋이 한
벌입니다 — ① 막혀야 할 것이 막히는가 ② **막히면 안 되는 것이
통과하는가** ③ **검사가 볼 것을 실제로 보고 있는가.**
③ 이 없으면 아무것도 안 보는 검사가 초록을 냅니다 — **센 것이 0이면
성공이 아니라 실패입니다.**
2026-09-04 에 넷이 걸렸습니다. 실명 검사가 29개 중 0개를 보며 초록,
개수 검사가 안내 줄까지 세어 20을 21로, 덮어쓰기 가드가 다섯 자리 중
둘만, 그리고 **대응표 겹침 거절 경로는 아예 한 번도 실행된 적이
없어 밟는 순간 `NameError`** 였습니다. **막는 코드에 시험이 없으면
그 경로를 처음 밟는 것은 실제로 막아야 할 순간입니다.**

**문서의 값을 시험에 박지 않습니다.** 박으면 문서를 고칠 때마다
시험이 막습니다 — 성적표의 산수를 고치려는데 `"14/20"` 을 찾는
시험이 빨개졌습니다. **재는 것은 박고 잰 것은 박지 않습니다** —
표본 네 회의는 바뀌면 견줄 수 없으므로 박고, 점수는 문서가 말할
값이므로 박지 않습니다. 값 대신 **값끼리 맞는지** 봅니다
(분자·분모의 합이 합계와 같은가). 그랬으면 그 어긋남을 시험이
잡았을 것입니다.

**언제 조심할지를 정하는 규칙은 그 '언제' 가 아닌 때에 조용히
뚫립니다.** 11-2 가 "실명이 든 파일을 **새로 만들어 커밋할 때 뿐**"
이라고 적어 두었는데, 생성 스크립트가 그 파일을 **매번** 만들어
내면서 규칙 밖이 됐습니다. **사람이 조심할 때를 정하지 말고 매번
도는 검사로 둡니다.**

> 같은 모양이 2026-09-04 에 **다섯 번째**입니다 — 앞의 넷은
> 코드였고(위 문단) 이건 문서입니다.

**`pytest` 는 옵션 없이 돌립니다.** `pytest.ini` 에 이미 `-q` 가
있어서 `-q` 를 또 주면 `-qq` 가 되고, 그 수준에서는 `852 passed …`
줄이 **아예 출력되지 않습니다.** 처음에는 `tail` 이 자른 줄 알고
엉뚱한 곳을 고쳤습니다.

**고치지 않기로 한 것은 `docs/봐둘것.md` 에 있습니다.** 무게가
「봐 둘 것」 이거나, 지금 새는 곳이 없어 미룬 것들입니다.
**그 언저리를 손댈 때 먼저 엽니다** — 미룬 것을 다시 만나고도
모르면 같은 것을 두 번 고칩니다.

> 가리키는 곳이 여기여야 하는 이유가 있습니다. 전에는 그 목록을
> 가리키는 곳이 `docs/review/최근.md` 하나뿐이었는데, 그 파일은
> 11-3 이 **매번 덮어쓴다**고 정한 것이라 다음 판이 덮는 순간
> 길이 사라집니다. `docs/이름-남은곳.txt` 가 살아남은 것은 목록이라서가
> 아니라 **`check_names` 가 돌 때마다 그 이름을 찍기 때문**입니다.
> 실명 검사에 성격이 다른 것을 얹지 않는 대신, **안 덮이고 마칠 때마다
> 읽는 자리**인 여기서 가리킵니다.

---

## 12. 로드맵

### Phase 1 — 기반
- 로그인·권한
- 회차 생성 + 세팅 마법사 (업무 라이브러리, 날짜 재계산, 구멍 경고)
- 준비 단계 보드 (트리 + 타임라인, 상태, 상세 패널, 논의 내역)
- 연결 강조와 관련업무 고스트 — 보드의 행 구조라 여기서 함께 만들었습니다
- 업무 라이브러리 관리 화면 (수동 필수 지정)
- 팀별 업무 수집 (초안 → 임시저장·제출 → 총무팀 개설) + 회차 개설 후 업무 추가
- 보드에서 바를 끌어 날짜 옮기기
- ~~달력 보기 (마감일 기준)~~ — 완료. 규칙은 4-13. 화면은 `/calendar`.
  **마감일에 점 하나**이고 기간 띠가 아닙니다. 점의 색·상태·기한 초과는
  보드의 `paint_of`·`overdue_of` 를 그대로 쓰고, 점을 누르면 **그 자리에서**
  상세 패널이 열립니다 — 달력은 그대로 있고 오른쪽에 겹쳐 뜹니다.
  보드로 넘겨 보내지 않습니다(그러면 보던 달을 잃습니다). 그래도 패널은
  여전히 **한 벌**입니다 — 화면은 `partials/drawer.html`, 코드는
  `static/js/drawer.js` 하나이고 각 화면은 "내 화면은 이렇게 고쳐 그린다"만
  알려 줍니다 (4-13)

### Phase 2 — 안전망
- ~~진단 패널 (진행 가능/불가 판정)~~ — 완료. 판정과 근거는 4-10
- ~~수련회 진행 화면 (프로그램 전/중/후)~~ — 완료. 규칙은 5장. 화면은 `/live`.
  **이 화면만 조건이 다릅니다** — 현장에서 휴대폰으로 급히 보는 것이라 좁은 화면에서는
  목록과 체크리스트를 한 번에 하나씩 보여주고, 체크 버튼은 44px 이며, 실패하면
  되돌리고 화면에 말합니다(5-0). `지연` 과 진행 여부는 저장하지 않고 **시각에서
  계산합니다**(5-2). 프로그램표는 지난 회차에서 통째로 복사해 옵니다(5-5)
- ~~웹 푸시~~ — 완료. 규칙은 4-11.
  받는 사람은 **총무팀이 아니라 담당자**이고, `진행 불가`는 본인이 아니라
  **선행을 쥔 쪽**이 받습니다. 담당자가 없는 업무만 부서 리더 → 총무팀으로 올립니다.
  다만 **배포 전까지는 실사용이 안 됩니다** — 웹 푸시가 HTTPS·localhost 에서만
  동작해서, HTTPS 를 붙이기 전에는 서버를 띄운 컴퓨터에서만 구독됩니다.
  그동안은 미리보기 화면으로 문구와 기준을 다듬습니다
- ~~봉사팀 보기 (화면·엑셀 한 표)~~ — 완료. 규칙은 5-8.
  **표를 만드는 곳은 `staff_sheet.build()` 하나**이고 화면과 파일이 그것을 같이 씁니다.
  따로 만들면 어긋나는데, 어긋난 쪽이 파일이면 아무도 모른 채 돌아다닙니다

> 원래 여기 있던 **연결 강조와 관련업무 고스트는 Phase 1 에서 구현했습니다.**
> 보드의 행 구조 자체라 빼면 목업과 다른 화면이 되기 때문입니다.

### Phase 3 — 확장
- 재정 (예산·식대 정산·환급 리스트·엑셀)
- 회의록 + 액션아이템 → 업무 전환 — 화면과 옮기기는 만들었고(`/meetings` ·
  `scripts/import_meetings.py`), **제안이 남았습니다.**
  지금은 **낱말 겹침**으로 고릅니다. 2026-09-03 에 사람이 표본 21개를 눈으로
  채점했습니다(`docs/review/제안-성적표.md`) — **14/21 (67%)**, 논의 8/13 ·
  새 업무 6/8. 틀린 일곱 중 **넷이 같은 모양**입니다: 낱말은 겹쳤는데
  회의록에서 그 얘기를 한 적이 없습니다. `프로그램 자료 헤브론 전달` 하나가
  **세 번** 잘못 나왔는데, `프로그램`·`자료`·`전달` 이 흔한데도 각각 업무 이름
  3개를 안 넘어 **흔한 낱말 거르기에 안 걸립니다.**
  나머지 둘은 결정과 단순 의견을 못 가린 것, 회의록 문장을 그대로 제목으로
  쓴 것입니다. **낱말 겹침은 여기까지입니다** — 67% 가 낮아서가 아니라 남은
  33% 가 전부 문장을 안 읽어서 생긴 것이라 그물을 다듬어서는 못 넘습니다.

  **그래서 Claude API 로 문장을 읽습니다** (`app/domain/llm.py` ·
  `suggest.분석()`). 내는 것이 셋으로 늘었습니다 — **결정사항**(회의록의 그
  줄을 **그대로 인용**) · **논의**(근거가 낱말이 아니라 **문장**) ·
  **새 업무**(제목을 업무 이름답게 다듬고 상위·부서를 함께). 보드를 글로
  펼쳐 함께 보내되 **상태는 그 회의 날로 되돌립니다**(`_상태`) — 저장된
  `status` 를 보여주면 8월에 끝난 것을 알고 6월 회의를 고르게 되어, 잰 것이
  아니라 답을 본 것이 됩니다.
  **두 번 부릅니다** — 1차는 목록+회의록, 2차는 후보의 논의 이력. 2차는
  **볼 것이 있을 때만** 부릅니다(후보에 지난 논의가 있을 때).
  **회의록을 저장하면 뒤에서 돕니다.** 저장은 기다리지 않고, 본문이 안
  바뀌면 다시 부르지 않으며, **적는 동안에는 아예 안 부릅니다** — 저장하고
  3분쯤 잠잠해지면 그때 읽습니다(`DCB_SUGGEST_QUIET_MIN`). 쓰면서 중간중간
  저장하는 것이 자연스러운데 그때마다 부르면 오타 세 번에 세 번 값이
  나갑니다. 당장 보고 싶으면 **`지금 읽기`** 를 누릅니다.
  **시간은 앱이 재지 않습니다**(4-11) — 열려 있는 화면이 물어볼 때 "이제
  됐나" 를 함께 봅니다.
  **키가 없거나 API 가 죽으면 낱말 겹침으로 물러서되 그렇다고 화면에
  적습니다** — 빈 목록으로 두면 "할 말이 없다"(4-10 조건 4의 정상)와
  구별되지 않고, **구별되지 않는 실패가 가장 비쌉니다.**
  **사람 평가가 든 회의록**(9장)에는 표시가 뜨고, 그 대목은 제안에 인용되지
  않습니다 — 보내는 것과 남기는 것은 다릅니다.

  **첫 실제 결과가 기준선보다 나빴고, 그 이유가 우리 쪽이었습니다.**
  표본의 `26.08.09` 이 빈 목록이었는데(낱말 겹침은 5개 중 4개를 맞힌 회의),
  답 원문을 받아 보니 `stop_reason: max_tokens` 였습니다 — **생각(thinking)에
  4,000 중 3,042 를 쓰고 남은 958 로 답을 쓰다 JSON 이 문장 한가운데
  잘렸습니다.** 잘린 JSON 은 `{}` 가 되고 화면에는 "낼 것을 찾지 못했습니다"
  가 떴습니다. **모델이 판단해서 없는 것과 우리가 못 읽은 것이 같은
  모양**이었던 것입니다.
  그래서 상한을 12,000 으로 올리고(`DCB_LLM_MAX_TOKENS`),
  **답은 왔는데 못 읽은 경우를 `실패` 로 나눴습니다.** 거르느라 빈 것도
  그렇다고 말합니다(`사람 평가가 섞여 결정사항 1건을 뺐습니다`).

  **모르는 상태를 '대기' 라고 말하지 않습니다** (6-9). 옮겨 온 자료는
  `started_at`·`completed_at` 이 비어 96건이 전부 '대기' 로 나갔는데,
  구별에 기여하지 않으면서 토큰만 먹고 **8월에 끝난 일도 '대기' 로
  보였습니다.** 이제 모르면 그 칸을 빼고, 빠진 것이 곧 '모름' 이라는 뜻을
  목록 머리말 한 줄이 밝힙니다.

  **값은 어림하지 않고 잽니다.** 처음에 "한글 글자당 0.6~0.75 토큰" 으로
  어림해 회의당 37~43원이라고 적었는데 **실제로는 113원**이었습니다 — 세
  배입니다. 표본 네 회의 실측은 **481원**(회의당 34~193원, 평균 120원)이고
  `usage` 를 그대로 받아 적습니다. 회차마다 회의록이 50건쯤이니 다 읽히면
  6,000원 남짓입니다 (`docs/배포-안내.md` 14-5).

  **2판을 냈습니다** — 표본 네 회의에서 **27개**(1판은 21개),
  `docs/review/제안-2판.md`. **견주는 것은 종류별입니다** — 2판에 `결정사항`
  이 새로 생겼고 그것은 회의록 줄을 그대로 인용하는 것이라 잘 맞을 수밖에
  없어서, 합계에 섞으면 그 12개가 비율을 끌어올립니다. 논의는 논의끼리,
  새 업무는 새 업무끼리 견주고 결정사항은 따로 적습니다.
  **채점은 사람이 합니다.** 그때는 숫자만이 아니라
  **근거도 함께 봅니다** — 맞혔는데 근거가 낱말 하나면 나아진 것이 아닙니다
  (6-3). 성적표에 **"놓친 것" 칸**을 더했습니다. 없으면 **적게 내는 쪽으로
  점수를 올릴 수 있습니다**
- ~~업무 첨부파일~~ — 완료. 규칙은 4-9.
  **회차별입니다** — `TaskRun` 에 붙고 새 회차로 따라가지 않습니다. 논의 내역과 같은
  취급이고 업무 규칙(라이브러리)과 다릅니다. 상세 패널의 마지막 탭이고, 하단의
  `파일 첨부` 버튼은 같은 기능이 두 군데 있게 되므로 없앴습니다.
  업로드 폴더는 백업 대상에 들어갔습니다 (11-2)
- 부서 간 확인 요청 (이전 설계의 `/reviews` 화면을 이 스펙에 맞춰 다시)
- CLOVA Speech 연동 (녹취 → 회의록 초안)
- 결산 리포트 정식 양식
- 관리자용 자연어 설정 편집

**Phase 1 완료 기준** — 2026-08-29 전부 통과

- [x] 새 회차를 마법사로 생성하고 업무 라이브러리에서 선택해 보드가 채워진다
- [x] 개회일을 바꾸면 모든 업무 날짜가 D-주차를 유지한 채 이동한다 (마법사 1단계 · 설정의 회차 수정)
- [x] 필수 업무를 빼면 경고가 뜬다
- [x] 부서 리더 계정으로 자기 부서만 선명하게 보인다 (나머지는 숨기지 않고 0.6 투명도)
- [x] 업무 상세에서 논의를 추가하고 상태를 바꿀 수 있다
- [x] 모바일에서 깨지지 않는다 (820px 아래에서는 타임라인 대신 기간이 붙은 목록)

---

## 13. 원본 데이터

- **Belong 예산** (Google Drive 스프레드시트) — "예산(실시간)", "지출 상세내역" 시트
- **2026 여름수련회 일정표_Belong** — "본시간표", "봉사자 시간표", 일자별 총무팀 시트(`260820_목_선발대` 등). 진행 단계 데이터의 원본
- **Notion 공용 워크스페이스** — 수련회 업무 DB (`업무속성`, `담당팀`, `관련 팀`, `행사 전후`, `진행 상황`, `관련업무` 속성). 준비 단계 데이터의 원본
- 식대 상한 근거 — 소그룹지원비 결재 문서

---

## 14. 구현 현황

Phase 1 은 기존 FastAPI + SQLAlchemy + Jinja 앱 위에 얹었습니다. 어디를 고쳐야 하는지 —

| 하는 일 | 파일 |
|---|---|
| D-주차 계산 · 명절 충돌 | `app/domain/dweek.py` |
| 자동 분류 · 회차 생성 · 날짜 재계산 | `app/domain/library.py` |
| Claude 제안과 근거 | `app/domain/suggestions.py` |
| 보드 축·행·바 계산 · 기한 초과 계산 | `app/domain/board.py` |
| 바·점의 생김새 (색·테두리) | `app/domain/board.py` 의 `paint_of` — **여기 하나다** |
| 툴팁 문장 만들기 (4-13) | `app/domain/board.py` 의 `tooltip_of` — **문장을 만드는 곳은 여기 하나다.** 매크로도 화면도 받아 쓰기만 한다 |
| 노션 회의록 자르기 | `app/domain/meeting_import.py` (`cut` · `people_notes`) |
| 회의록 옮기기 (일회성) | `scripts/import_meetings.py` — 미리보기가 기본. `--until` 로 시뮬레이션, `--undo` 로 되돌림 |
| 회의록 화면 | `app/routers/meetings.py` · `templates/meetings.html` · `meeting_detail.html` |
| 회의록을 읽고 제안하기 | `app/domain/suggest.py` — **읽고 제안하는 창구는 여기 하나다** (화면이든 채팅이든) |
| Claude API 로 나가기 | `app/domain/llm.py` — **문은 여기 하나다.** 키·모델·요금이 두 곳에 있으면 갈리고, 갈린 쪽을 아무도 모른다 |
| 회의록을 문장으로 읽기 | `app/domain/suggest.py` 의 `분석()` — 1차(목록+회의록) → 2차(후보의 논의 이력). **2차는 볼 것이 있을 때만** |
| 보드를 글로 펼치기 | `app/domain/suggest.py` 의 `catalog()` · `board_as_of()` — **상태는 그 회의 날로 되돌린다** (`_상태`) |
| 분석을 뒤에서 돌리기 | `app/routers/meetings.py` 의 `분석_한번` · `분석_걸어둔다` · `다시_읽어야_하나` · `때가_됐나` — **저장은 기다리지 않고, 적는 동안에는 안 부른다** |
| 답을 못 읽은 것을 알아보기 | `app/domain/suggest.py` 의 `못읽었나` — `stop_reason` 이 `max_tokens` 거나 JSON 이 아니면 **`실패`다.** 모델이 없다고 한 것과 갈라야 한다 |
| 표본 네 회의 다시 내기 (성적) | `scripts/suggest_sample.py`. **나가는 곳은 `data/*.real.md`** 이고 `--공개` 를 줘야 익명화 사본이 `docs/review/` 로 간다 (11-2). 문장으로 읽는 판은 키가 없으면 **아무것도 안 쓴다**; **`--낱말` 은 키 없이 돌아** 1판 목록(`제안-1판.md`)을 낸다 — 낱말 겹침은 Claude 를 부르지 않는다 |
| 채점표를 덮어쓰지 않기 | `scripts/suggest_sample.py` 의 `막는다` · `채운곳` — 나갈 파일이 있으면 `--replace` 를 줘야 쓰고, **사람이 채운 판정이 있으면 `--replace` 로도 막는다.** 사람이 채운 27개는 되돌릴 수 없는 유일본이다 |
| 제안을 골라 반영하기 | `routers/meetings.py` 의 `/suggestions` · `/suggestions/apply` · `static/js/meeting.js` — **고른 것만** 들어가고 출처가 남는다 |
| 논의로 남을 문장 만들기 | `routers/meetings.py` 의 `discussion_body` — **여기 하나다.** 누르기 전 미리보기와 실제 저장이 같은 함수를 쓴다. 두 벌이면 보여준 것과 남는 것이 갈리고, 갈린 쪽을 아무도 눈치채지 못한다 |
| 노션처럼 쓰는 입력칸 | `app/static/js/listinput.js` — **여기 한 벌**. 논의 입력칸과 회의록 본문이 함께 쓴다 |
| 대응표 읽기 | `scripts/anonymize.py` 의 `표기들()` · `가명()` · `가명수()` — **읽는 곳은 여기 하나다.** 두 곳에서 읽으면 구조가 바뀔 때 한쪽만 고쳐진다. 원본 모양을 돌려주지 않고 **물음 단위로만** 낸다 |
| 정적 주소에 내용 해시 (11-2) | `app/templating.py` 의 `static` · `app/main.py` 의 `HashedStatic` |
| 달력 보기 (4-13) | `app/domain/calendar.py` · `routers/calendar.py` · `templates/calendar.html` · `static/js/calendar.js` |
| **상세 패널 — 보드·달력 공용** (4-13) | `templates/partials/drawer.html` · `static/js/drawer.js` — **여기 한 벌뿐이다** |
| 진단 패널 판정과 근거 | `app/domain/diagnosis.py` |
| 알림 묶음 만들기 (4-11) | `app/domain/notify.py` · `routers/notify_admin.py` · `templates/notify_preview.html` · `app/push.py` |
| 부서를 키로 찾기 | `app/domain/departments.py` (`department_key_of` · `users_in_department`) |
| 보드 화면 | `app/routers/board.py` · `templates/board.html` · `static/js/board.js` |
| 수련회 진행 (5장) | `app/domain/live.py` · `app/routers/live.py` · `templates/live.html` · `static/js/live.js` |
| 진행 화면의 시각 판정 | `app/domain/live.py` (`program_state` · `late_items` · `leftover_post` — 전부 `now` 를 받는다) |
| 프로그램표 복사 | `app/domain/live.py` (`copy_programs`) · `/live/copy` |
| 구글시트 프로그램표 가져오기 (5-5) | `scripts/import_programs.py` |
| 담당자 표기 통일 (5-3) | `scripts/normalize_names.py` — 회차를 넘기기 전에 돌린다 |
| 첫 관리자 · 링크 재발급 (4-12) | `scripts/create_admin.py` (`--reissue` · `--force`) |
| 중복 계정 정리 (4-12) | `scripts/merge_users.py` — 비활성화만 한다 |
| 시스템 밖에서 진행한 회차 (5-6) | `app/domain/live.py` (`carried_only`) |
| 봉사자 시간표 만들기 (5-8) | `app/domain/staff_sheet.py` — **화면과 xlsx 가 같이 쓰는 유일한 표** |
| 봉사자 시간표 xlsx 쓰기 | `app/domain/staff_xlsx.py` (`write` · `SheetBroken`) |
| 봉사팀 보기 화면 | `app/routers/live.py` 의 `/live/staff` · `/live/staff.xlsx` · `templates/staff_sheet.html` |
| 팀 단위 / 개인 단위 (5-2-1) | `app/domain/live.py` (`guess_scope` · `scopes_in`) · `static/js/live.js` 의 `renderDetail` |
| 세팅 마법사 | `app/routers/setup.py` · `templates/setup.html` · `static/js/setup.js` |
| 목업의 디자인 시스템 (무채색 기조 · 4-0) | `app/static/css/retreat.css` |
| 글자 눈금 (4-0) | `app/static/css/retreat.css` 의 `--fz` — **크기를 정하는 곳은 여기 하나다.** 숫자로 박으면 `tests/test_typescale.py` 가 빨개진다 |
| 부서 고르기 — 보드·달력 공용 (4-13) | `templates/partials/deptpick.html` — **여기 한 벌뿐이다.** 고른 뒤에 무엇을 하는가만 화면마다 다르다 |
| 사이드바 (상단 탭 줄 대신) | `templates/retreat_base.html` · `static/js/sidenav.js` |
| 잠깐 떴다 사라지는 안내 | `app/static/js/flash.js` (`base.html` · `retreat_base.html` 이 함께 씁니다) |
| 첨부파일·링크 (4-9) | `app/routers/attachments.py` · `partials/drawer.html` 의 `p-files` · `static/js/drawer.js` 의 `renderFiles`·`putFile` |
| 업로드 상한·디스크 여유 (4-9) | `app/config.py` (`MAX_ATTACHMENT_BYTES` · `DISK_FREE_FLOOR_BYTES` · `DISK_FREE_WARN_BYTES`) |
| 백업 — 바뀐 것만·크기 제한 (11-2) | `scripts/backup.py` (`copy_uploads` · `prune` · `disk_used`) |
| 초대 주소 만들기 (4-12) | `app/domain/auth.py` 의 `invite_url` — **주소를 만드는 곳은 여기 하나다** |
| 업로드 경로와 상한 | `app/config.py` (`ATTACHMENT_DIR` · `MAX_ATTACHMENT_BYTES` · `ALLOWED_ATTACHMENT_EXTS`) |
| 라이브러리 관리 (필수 지정 · 선후행) | `app/routers/library.py` · `templates/library.html` · `static/js/library.js` |
| 선후행 검증 · 평평한 목록 · 후보 제안 | `app/domain/library.py` (`validate_prerequisite` · `flat_catalog` · `prerequisite_proposals`) |
| 팀별 수집 (초안·제출) | `app/domain/drafts.py` · `routers/drafts.py` · `templates/draft.html` · `static/js/draft.js` |
| 회차 개설 후 업무 추가 | `routers/board.py` 의 `/board/add` · `templates/board_add.html` |
| 라이브러리 초기값 (2026 여름수련회) | `seed_library_data.py` · `seed_library.py` |

**날짜의 저장 방식** — 라이브러리는 절대 날짜를 갖지 않습니다.
`date_anchor`(week/open) + `default_d_week` + `default_offset_days`(그 주 일요일로부터 며칠) +
`default_span_days` 네 개만 저장하고, 회차의 개회일로 매번 다시 계산합니다.
요일 오프셋을 따로 두는 이유는 개회일 요일이 회차마다 달라도 **업무의 요일이 유지되어야** 하기 때문입니다.

**스키마 변경** — 마이그레이션 도구는 아직 없습니다. `app/db.py` 의 `_ADDED_COLUMNS` 에 (테이블, 컬럼, DDL)을 더하면 기존 SQLite 파일에 그 컬럼이 자동으로 추가됩니다. **추가만** 합니다 — 이름 변경·삭제는 여기서 하지 않습니다.

**아직 없는 것** — 재정(7장)은 Phase 3입니다.
진단 패널(4-10)은 만들었습니다 — 근거 중 '논의 기록에서 온 맥락'만 후속으로 남았습니다.
수련회 진행(5장)은 만들었습니다 — 사이드바의 `수련회 진행` 은 `/live` 로 갑니다.
**회의록은 새 껍데기로 옮겼습니다** — 회의록 2단계. 사이드바의 `재정` 은
이전 설계에서 만든 화면으로 연결되어 있습니다.
동작은 하지만 이 문서의 스펙과는 다른 화면이며, 각 Phase 에서 다시 만듭니다.
**그 화면들은 아직 옛 껍데기(`base.html` · `app.css`)를 씁니다** — 4-0 의 무채색 기조는
`retreat_base.html` 쪽(보드·진행·마법사·라이브러리·초안·계정·알림 미리보기)에
적용했습니다. 각 Phase 에서 다시 만들 때 함께 옮깁니다.
두 껍데기가 공유하는 것은 `flash.js` 하나입니다.

> **옛 `/schedule` 화면은 아직 남아 있습니다.** 이전 설계의 '일정표'로,
> `base.html` 쪽 사이드바에서만 갈 수 있습니다. 5장 화면(`/live`)과 데이터가
> 다르므로(`ScheduleDay`·`ScheduleItem` vs `Program`·`ProgramItem`) 섞이지
> 않지만, **같은 성격의 것이 두 곳에 있는 상태**입니다. 회의록·재정을 다시
> 만들면서 옛 껍데기를 걷어낼 때 함께 정리합니다.

---

## 15. 시작할 때

1. 이 문서와 `docs/mockups/` 의 HTML 3개를 확인
2. 프로젝트 스캐폴딩 (프레임워크·DB는 표준적이고 유지보수 쉬운 조합으로 자율 결정)
3. Phase 1을 로그인·권한 → 회차 세팅 → 보드 순서로 구현
4. 완료 기준 통과 후 실행 방법과 함께 보고
