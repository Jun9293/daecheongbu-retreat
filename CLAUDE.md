# CLAUDE.md — 대청부 수련회 총무팀 통합 관리 시스템

이 파일은 Claude Code가 이 프로젝트에서 작업할 때마다 먼저 읽어야 하는 기준 문서입니다.
세션이 끊기거나 새로 시작해도, 이 파일을 읽으면 프로젝트의 목적·구조·이미 확정된 결정사항을 그대로 이어받을 수 있어야 합니다.

전체 배경과 논의 과정은 `docs/설계문서_v1.md`에 있습니다. 이 CLAUDE.md는 그중 **지금 당장 실행 가능한 형태로 정리한 버전**입니다.

---

## 0. 프로젝트 정체성 & 운영 원칙

**무엇을 만드는가**: 대학청년부(대청부) 수련회 총무팀이 쓰는 웹 기반 통합 관리 시스템. 일정/할일 관리 + 예산 관리를 핵심으로 하고, 이후 파일 협업·회의록·알림·권한 관리로 확장.

**왜 만드는가**: 담당자(총무 선교사, 부서 리더)가 매번 바뀌어도 준비 과정에 구멍이 생기지 않게 하기 위함. → **모든 기능 설계는 "누가 놓쳐도 시스템이 대신 알아차리는가?"를 기준으로 판단할 것.**

**작업 방식 (중요)**: 사용자는 Phase 단위로 작업을 맡기고, 완료된 결과물(실행 가능한 형태)만 리뷰합니다.
- Claude Code는 각 Phase의 기능을 스스로 구현 → 실행해서 검증 → 발견된 문제를 스스로 수정하는 사이클을 거친 뒤에 "완료"로 보고합니다.
- 구현 중간에 사소한 판단(변수명, 컴포넌트 분리 방식 등)은 묻지 않고 진행하되, **아래 "확정된 결정사항"과 충돌하는 선택**을 해야 하는 경우에만 사용자에게 확인합니다.
- 각 Phase 완료 시 커밋을 정리하고, 무엇을 만들었는지 / 어떻게 확인(테스트, 직접 실행)했는지 / 남은 이슈가 있는지를 짧게 보고합니다.
- Phase 완료 보고에는 반드시 **직접 눌러볼 수 있는 실행 방법**(로컬 실행 명령어 또는 배포 링크)을 포함합니다.

---

## 1. 확정된 결정사항 (변경 시 사용자 확인 필요)

| 항목 | 결정 |
|---|---|
| 로그인 방식 | 전화번호 인증 (SMS 인증코드) |
| 부서/예산 카테고리 | `Retreat`(수련회 회차) 단위로 매번 재설정. 새 회차 생성 시 직전 회차 복제 옵션 제공 |
| 회차 생성 | 자동 생성 없음. 총무팀이 "새 회차 만들기"를 수동 실행 |
| 알림 | **Phase 1~2는 웹 푸시(PWA)만 구현.** 카카오톡 연동은 보류 — 나중에 별도로 진행 |
| 결산 리포트 양식 | 아직 미확정. Phase 3에서 사용자가 제공하는 양식에 맞춰 구현 (그 전까지는 범용 엑셀 export로 충분) |
| 로드맵 순서 | 아래 4장(Phase 1 → 2 → 3) 순서 그대로 |
| 소속 기반 UI | 로그인 계정의 부서와 무관한 Task/일정은 숨기지 않고 **흐리게 표시** (완전히 숨기지 않는 것이 원칙 — 존재는 인지되어야 함) |
| 식대 정산 1인당 상한 | 기본값 있으나 **회차/규정별로 다름** (여름수련회 8,000원, 소그룹지원비 규정은 5,000~7,000원 사례 확인됨) → 하드코딩 금지, 반드시 설정값으로 관리 |

---

## 2. 데이터 모델 (Phase 1 기준 스키마 초안)

```
Retreat {
  id, name (예: "2026 여름수련회 Belong"), startDate, endDate,
  clonedFromRetreatId?  // 직전 회차 복제 시
  mealSubsidyPerPerson  // 식대 1인당 지원 상한 (원). 회차별 설정
}

Department {
  id, retreatId, name, colorTag?
}

User {
  id, phoneNumber, name, departmentId?, role  // 'admin' | 'dept_lead' | 'member' | 'viewer'
}

BudgetCategory {
  id, retreatId, level1(구분), level2(항목), level3(세부항목)?, plannedAmount
}

ExpenseEntry {
  id, retreatId, budgetCategoryId,
  level1(구분), level2(항목), level3a(세부항목-1), level3b(세부항목-2), level3c(세부항목-3), // 기존 시트의 3단 세부항목 구조 그대로
  receiptNumber,       // 영수증번호 (기존: 정수 시퀀스)
  expenseDate,         // 지출일자
  amount,              // 금액 (영수증 실제 금액)
  payerId, payerName, payerAccount,   // 지출자 / 지출자 계좌 (많은 항목이 "수련회계좌"라는 특수 지출자로 기록됨)
  paid: boolean,       // 지급여부
  paidDate?,           // 지급일
  note,                // 비고 (자유 텍스트 — 기존엔 식사 인원/명단을 여기 욱여넣었음, Phase 1에서 구조화된 필드로 대체)
  receiptFileUrl?,

  isMealExpense: boolean,
  // isMealExpense == true 일 때만 사용 — "수련회 준비지원 > [부서] > 모임 식사비-N" 패턴에 해당
  mealHeadcount?,          // 기존엔 비고에 "9명"처럼 텍스트로만 존재했음 → 숫자 필드로 분리
  mealAttendeeNames?[],    // 기존엔 비고에 "박민준 홍성헌 민주아..." 텍스트로만 존재 → 배열로 분리 (회원 검색select 또는 수기입력)
  subsidyAmount,           // 자동계산 = min(amount, mealHeadcount * retreat.mealSubsidyPerPerson) ← 기존 파일에서 실측 검증된 공식
  personalBurdenAmount     // 자동계산 = amount - subsidyAmount
}

ScheduleDay { id, retreatId, label(예: "1일차"), date, sortOrder }

ScheduleItem { id, scheduleDayId, title, startTime, endTime, location? }

Task {
  id, retreatId, title, description?,
  departmentId, assigneeId?,
  scheduleItemId?,     // 특정 일정에 종속된 할일이면 연결, 없으면 null(독립 할일)
  dueDate?,
  status,              // '대기' | '진행중' | '피드백요청' | '완료' | '지연'
  blockedByTaskIds[],  // 선행 작업
  relatedDepartmentIds[] // 확인 요청을 보낼 관련 부서 (Phase 2)
}

ActivityLog {
  id, retreatId, actorType('user'|'claude'), actorId?, action, targetType, targetId, beforeValue?, afterValue?, createdAt
}
```

> ✅ **검증 완료** (사용자가 업로드한 `Belong 예산.xlsx`의 "지출 상세내역" 시트 실측 확인, 2026-08-27):
> - 실제 컬럼 순서: `구분 | 항목 | (병합) | 세부항목-1 | 세부항목-2 | 세부항목-3 | 예산 사용액 | 합계금액 | (병합) | 영수증번호 | 지출일자 | 금액 | 지원금액 | 비고 | 지급여부 | 지급일 | 지출자 | 지출자 계좌`
> - **식대 정산은 "그 외 > 수련회 준비지원 > [부서명] > 모임 식사비-N" 패턴으로, 수련회 본행사 식비(아침/점심/저녁 자율배식)와는 별개로 부서별 준비모임 회식비에 적용됨.** 여러 번(모임 식사비-1, -2, -3...) 반복 발생.
> - 공식 실측 검증: `지원금액 = min(금액, 인원수 × 8,000원)` — 예) 인원 12명 금액 130,600원 → 지원금액 96,000원(=12×8,000, 캡 적용) / 인원 9명 금액 68,900원 → 지원금액 68,900원 그대로(캡 미만이라 전액 지원). Phase 1의 자동계산 로직은 이 공식을 그대로 구현하면 됨.
> - **인원수·명단은 현재 구조화된 컬럼이 아니라 "비고" 칸에 "9명 박민준 홍성헌 민주아..." 식 자유 텍스트로 들어가 있음** — 이게 바로 사용자가 시스템화하고 싶어했던 지점. Phase 1에서 `mealHeadcount`/`mealAttendeeNames` 전용 필드로 분리해서 입력받고 자동계산.

---

## 3. Phase 1 — MVP 상세 스펙

### 3-1. 일정 & 할 일 관리
- `Retreat` 생성/복제 (부서 목록, 예산 카테고리 포함 복제)
- `ScheduleDay` / `ScheduleItem` CRUD, Day별 탭 UI
- `Task` CRUD, 상태 변경, 담당 부서·담당자 지정
- 로그인 계정 기준 "내 부서 오늘 할 일" 뷰 (기본 화면)
- 타 부서 Task는 흐리게 표시 (완전 비노출 금지)
- 선후행 의존성(`blockedByTaskIds`)은 **데이터 구조만 마련**, UI/자동 상태 전환 로직은 Phase 2

### 3-2. 예산 관리
- `BudgetCategory` CRUD (구분/항목/세부항목, 예산금액)
- `ExpenseEntry` 등록 (영수증 파일 업로드 포함), 예산 대비 지출 실시간 집계
- **식대 지출 등록 시** (부서별 "모임 식사비" 패턴 — 준비 기간 중 여러 번 반복 발생): 인원수/명단/결제자/계좌번호 입력 폼 + 1인당 상한 기준 자동 지원금액(`min(금액, 인원수×상한)`)/개인부담액 계산. 같은 부서가 "모임 식사비-1, -2, -3..."처럼 반복 등록하는 흐름이므로 직전 입력값(부서, 결제자, 참석자 명단 일부)을 다음 등록 시 기본값으로 제안하면 입력 부담이 줄어듦
- 환급 대상자 리스트 뷰 (이름/계좌번호/지원금액/지급여부, 필터 가능)
- 엑셀 export (범용 포맷 — 정식 결산 양식은 Phase 3)

### 3-3. 로그인 & 권한
- 전화번호 + SMS 인증코드 로그인
- 역할: `admin`(총무팀) / `dept_lead`(부서리더) / `member`(부서원) / `viewer`(열람전용)
- 권한별 CRUD 제한: admin은 전체, dept_lead/member는 자기 부서 Task·예산만 편집, viewer는 읽기 전용

### Definition of Done (Phase 1)
- [ ] 총무팀 계정으로 새 `Retreat` 생성 → 부서 3개 이상, 예산 카테고리 5개 이상 등록 가능
- [ ] 부서 리더 계정으로 로그인 시 자기 부서 Task만 선명하게, 타 부서는 흐리게 보임
- [ ] 예산 카테고리에 지출 등록 → 대시보드에 예산 대비 진행률이 실시간 반영
- [ ] 식대 지출 등록 시 인원수 입력하면 지원금액/개인부담액이 자동 계산되어 표시
- [ ] 엑셀로 지출 내역 export 가능
- [ ] 모바일 화면에서 위 기능이 깨지지 않고 정상 동작

---

## 4. Phase 2 / Phase 3 (참고용 — 아직 착수 안 함)

**Phase 2 — 안전망 강화**
- Task 선후행 의존성의 자동 상태 전환 + 담당자 미지정/기한 임박 시 총무팀 에스컬레이션 알림 (웹 푸시)
- 파일 업로드 + 부서 간 "확인 요청" 플로우
- 체크리스트, 회의록(+ 액션아이템 → Task 자동 생성)

**Phase 3 — 확장**
- 카카오톡 친구톡 연동 (사용자가 카카오 채널 개설 후 진행 — 현재 보류 상태)
- 정식 결산 리포트 양식 출력 (사용자가 양식 제공하면 반영)
- 회의 녹취 음성 → STT 변환 → Claude 분석 → 회의록/액션아이템 초안. **STT 벤더: 네이버 CLOVA Speech (Naver Cloud Platform API)로 확정.** 비용 무관·화자분리 필수·한국어 정확도 최우선이라는 기준에서, 한국어 인식률이 가장 높다고 평가되고 장시간 음성 파일 업로드 + 화자분리를 API로 공식 지원하는 CLOVA Speech가 가장 적합. (CSR이라는 유사 서비스도 있지만 1분 이내 명령어 인식용이라 회의 녹취에는 부적합 — CLOVA Speech만 해당)
- 관리자용 자연어 설정 편집 채팅 (Claude API 연동)

---

## 5. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프런트엔드 | 반응형 웹앱, PWA | 모바일 우선, 링크 하나로 접속 |
| 백엔드/DB | 실제 DB 필요 (Sheets 아님) | 150명+, 동시 편집, 권한 분리 때문 |
| 파일 저장 | Google Drive API | 기존 협업 환경 재사용 |
| 인증 | 전화번호 SMS 인증 | 국내 SMS 인증 API 벤더는 Claude Code가 구현 시점에 후보 제시 |
| 알림 | 웹 푸시(PWA) | 카카오는 Phase 3 이후 |
| 배포 | Claude Code가 적합한 호스팅 제안 (예: Vercel 등) | 무료/저비용, 소규모 트래픽 기준 |

---

## 6. 참고 원본 데이터 (Google Drive, 필요 시 재조회)

- 예산 구조 원본: "Belong 예산" 스프레드시트 — "예산(실시간)" 시트(구분/항목/세부항목/예산금액/결산금액), **"지출 상세내역" 시트(실제 ExpenseEntry 스키마의 근거, 2장 참고)**
- 부서별 일정 참고: "2026 여름수련회 일정표_Belong" (총무팀 전체 시간표 시트)
- 식대 상한 규정 근거: "대학청년부... 소그룹지원비 항목... 검토요청" 결재 문서 (1인당 5,000~7,000원 사례)
- 대청부 업무 관리: Notion 공용 워크스페이스 (연결 완료 — 필요 시 "업무리스트" 등 페이지 재조회 가능)

---

## 7. 지금 시작할 때 첫 번째로 할 일

1. 이 문서와 `docs/설계문서_v1.md`를 읽고 이해했는지 확인
2. 프로젝트 스캐폴딩 생성 (프레임워크/DB 선택은 표준적이고 유지보수 쉬운 조합으로 자율 결정 가능)
3. Phase 1의 3-3(로그인/권한) → 3-1(일정/할일) → 3-2(예산) 순서로 구현 진행 (권한 체계가 먼저 있어야 나머지 기능의 접근 제어를 처음부터 올바르게 만들 수 있음)
4. Definition of Done 체크리스트를 모두 통과하면 Phase 1 완료로 보고
