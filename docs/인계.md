# 대청부 수련회 시스템 — 미결 목록 (새 채팅이 먼저 읽는다)

갱신: 2026-09-15 노션 대조 1판 — **값 없이 건수만 맞춰 봤다. 아무것도 고치거나 들여오지 않았다.** 업무는 노션 223(구분 있는 줄) · 앱 102 → 노션만 143 · 앱만 22 · 둘 다 제목 다름 30 · 제목 같음 50, 짝 안에서 구분 다름 33 · 담당 다름 12 · 시작일 다름 11 · 마감일 다름 15. 회의록은 페이지 10 이 앱 출처 10 과 맞는다. **앱 업무는 노션에서 들여온 것이 아니라 목업에서 추린 시드다**(`730541a`). 다음 판의 갈래(ㄱ 다시 들여오기 · ㄴ 어긋난 것만 · ㄷ 노션 닫기)와 까닭은 `docs/review/최근.md` 6장 — **사람이 정한다.** 값은 `data/노션대조.real.md` 에만 있다. 화면 점검 마무리 판 보고는 `docs/review/2026-09-15-점검마무리.md` 로 얼렸다.
그 전 갱신: 2026-09-15 화면 점검 마무리 판 — **재정 줄기 완전히 끝남 · 화면으로도 확인됨(2026-09-15) · 다음은 회의록이거나 사람이 다시 정한다.** 사람이 정할 것 넷을 닫았다 — 이 창의 화면 점검은 **참고 기록**(정식 11-3 점검 아님) · 계좌번호 칸은 **총무님이 개발 화면에서 직접 입력해 뒷자리 표시와 복사까지 확인** · 운영 로그인은 **재시작 뒤 유지 확인됨(총무님 직접)** · 부서 리더 개발 계정은 급하지 않은 후속(남은 것 표). 화면 점검 · 쿠키 이름 판 보고는 `docs/review/2026-09-15-쿠키이름.md` 로 얼렸다.
그 전 갱신: 2026-09-15 화면 점검 · 쿠키 이름 판 — **화면 점검이 돌았다 — 관리자 계정으로 drawer.js 세 화면 · phone.js(400px 틀) 실패 0, 재정 화면은 저장 경로가 전부 됐고 계좌번호 칸만 못 넣었다(→ 다음 판에 총무님이 직접 확인), 깨진 화면 없음**(재정 절 「화면 점검」 · 한계가 붙은 점검). 개발과 운영의 세션 쿠키 이름을 갈랐다(CLAUDE.md 11-2) — **개발 서버를 다시 켠 뒤부터** 갈리고 그때 개발에는 다시 로그인한다. 운영 로그인은 서명키 지문과 쿠키 이름이 그대로라 **유지될 것으로 보이나 사람이 아직 확인하지 않았다.** → **다음 판에 확인됨(총무님 직접 · 2026-09-15)**. 화면 점검 판 보고는 `docs/review/2026-09-15-화면점검.md` 로 얼렸다.
그 전 갱신: 2026-09-15 화면 점검 판 — **개발 DB 다시 만들기가 닫혔다**(총무님이 실행 · 첫 관리자 로그인 확인). **화면 점검(`drawer.js` · `phone.js`)은 시도했으나 못 돌렸다** — 이 창이 붙는 Chrome 에 개발 서버 세션이 없었다(재정 절 「화면 점검 시도」). 재정 줄기는 실제 화면에서 아직 확인되지 않았다. 개발 DB 안내 판 보고는 `docs/review/2026-09-15-개발DB안내.md` 로 얼렸다.
그 전 갱신: 2026-09-15 개발 DB 안내 판 — **계좌 게이트 판부터 넘어온 「개발 DB 다시 만들기」 안내가 준비됐다**(재정 절 「개발 DB 다시 만들기 — 안내」).
총무님이 실행하고 알려 주시면 그 줄을 닫는다. 재정 줄기 마감 판 보고는 `docs/review/2026-09-15-재정마감.md` 로 얼렸다.
그 전 갱신: 2026-09-15 재정 줄기 마감 판 — **재정 줄기 완전히 끝남(2026-09-15) · 다음은 회의록이거나 사람이 다시 정한다.** 사람이 09-13 사고 직전 판을 지키는판에 넣기로 정해 넣었고(넘김에서 뗌), 재정에 남은 정할 것은 없다. 줄기를 닫은 뒤에도 남는 일(재정에서 뒤로 미룬 차례 7 · 8 과 다음 시트 때 볼 것 포함)은 「재정 — 지금 어디까지」 머리의 남은 것 표에 있다. 재정 줄기 마무리 판 보고는 `docs/review/2026-09-15-재정마무리.md` 로 얼렸다.
그 전 갱신: 2026-09-15 재정 줄기 마무리 판 — **재정 차례 1~6 이 전부 닫혔다.** 사람이 「문서가 되돌리는 자리로 가리키는 사본은
`지키는판` 에 넣는다」 를 정해 CLAUDE.md 11-2 백업 절에 적었고 `tests/test_stage52.py` 가 그 짝을 본다. 남은 것은 아래
「재정 — 지금 어디까지」 머리 한 자리에 모았다. **재정 관련 다음 지시문이 없으면 다음 큰 줄기는 회의록이거나 사람이 다시 정한다.**
짝잇기 운영 실행 판 보고는 `docs/review/2026-09-15-짝잇기실행.md` 로 얼렸다.
그 전 갱신: 2026-09-15 짝잇기 운영 실행 판 — **재정짝표 30건을 시트 결산 식으로 운영에 채웠다**(사람이 「그대로 해」 · 이 창이 미리보기를
다시 재어 앞 판과 같은 것을 보고 `--실행` · 직전 사본 `data/backups/app-20260915-144929.db` · 예산 항목 미지정 30 → 0 · 두 번째는
「채울 것이 없습니다」). 사람이 둘을 더 정했다 — 덮인 옛 시트 글자는 활동 기록에 안 남긴다(봐둘것 BB-h) · 다음 시트부터 들여오기가
처음부터 결산 식으로 잇고 식 파서가 Excel 기본 범위 꼴을 받는다(BB-i · 이 판에서 코드까지 함). **다음은 16번(구매목록)이거나 사람이
정하는 것.** 결산 식 잇기 판 보고는 `docs/review/2026-09-15-결산식잇기.md` 로 얼렸다.
그 전 갱신: 2026-09-15 결산 식 잇기 판 — **재정짝표 30건은 시트 결산 식으로 다시 잇는다**(사람이 정함). 총무님이 화면에서 이은 것은
0 건이었다. `scripts/재정짝잇기.py` 가 섰고 운영 미리보기(채울 30 · 못 채움 0)까지 냈다 — **`--실행` 은 총무님이 미리보기 수를
보고 「그대로 해」 한 뒤 다음 판에서.** 안내문 `docs/재정-잇기-안내.md` 는 닫았다(지우지 않음). 다음 시트(2027)를 처음부터 결산
식으로 이을지는 봐둘것 BB-i. 잇기 안내 판 보고는 `docs/review/2026-09-15-잇기안내.md` 로 얼렸다.
그 전 갱신: 2026-09-15 잇기 안내 판 — **재정짝표 30건 — 총무님이 화면에서 잇는 중.** 사람이 16번보다 이것을 먼저 정했다.
이 창은 차례 5 의 고치기가 「예산 항목 없는 지출에 항목을 골라 저장」 으로 도는지 앱을 그 자리에서 만드는 시험으로 쟀고
(`tests/test_stage50.py` · 도는 서버에는 로그인하지 않는다 11-2), 따라 할 순서를 `docs/재정-잇기-안내.md` 에 두었다.
다 이으면 총무님이 이은 수와 미지정으로 둔 수를 알려 주고, 그때 되돌리는 사본을 지키는판에서 풀지 정한다(봐둘것 BB-h).
운영 실행 판 보고는 `docs/review/2026-09-15-운영실행.md` 로 얼렸다.
그 전 갱신: 2026-09-15 운영 실행 판 — **재정 차례 6 이 끝났다. 시트의 재정이 운영에 들어갔다**(사람이 「그대로 해」 · 이 창이 `--실행` ·
직전 사본 `data/backups/app-20260915-002352.db` · 다시 센 수가 계획과 같음 · 두 번째는 멈춤). 예산 항목을 못 이은 지출 30 건은
지출 화면 「예산 항목 미지정」 묶음에서 사람이 고쳐 잇는다(봐둘것 BB-h). **다음은 16번(구매목록)이거나 사람이 정하는 것.**
계좌 꼴 게이트 판 보고는 `docs/review/2026-09-14-계좌게이트.md` 로 얼렸다.
그 전 갱신: 2026-09-14 계좌 꼴 게이트 판 — **계좌 꼴 게이트가 섰다(봐둘것 BB-c 닫음). 운영 실행은 총무님이 운영 미리보기 수를
직접 보고 「그대로 해」 라고 채팅에 말하기를 기다린다 — 그다음 판에서 이 창이 `--실행` 을 돌린다.** 사람이 정할 것 넷을
정했다(실행은 게이트 뒤 · 식대 두 줄은 그대로 · 게이트는 실행 전 · 비고 88 줄은 안 들임 — 봐둘것 BB-h). 규칙은 CLAUDE.md
7-4 · 7-5 · 차례 6 판 보고는 `docs/review/2026-09-14-차례6.md` 로 얼렸다.
그 전 갱신: 2026-09-14 재정 차례 6 판 — **시트 들여오기 스크립트가 섰고 미리보기까지 냈다. 운영 실행은 사람이 미리보기를 보고
「그대로 해」 라고 한 다음 판에서 이 창이 돈다.** 규칙은 CLAUDE.md 7-5 · 짝 표 건수는 봐둘것 BB-h · 차례 5 닫기 판 보고는
`docs/review/2026-09-14-차례5닫기.md` 로 얼렸다. 사람이 식대 인원은 비고에서 뽑는다고 마지막 갈림을 정했다(BB-a).
그 전 갱신: 2026-09-14 차례 5 닫기 판 — **재정 차례 5 가 끝났다. 다음은 차례 6(시트 들여오기).** 사람이 남은 둘을
정했다 — 지원금액은 금액·인원이 바뀔 때만 다시 센다 · 리더가 계좌 적힌 지출의 지출자만 바꾸면 400. 둘 다 CLAUDE.md 7-4.
차례 5 판 보고는 `docs/review/2026-09-14-차례5.md` 로 얼렸다. 사람에게 받을 것은 없다.
그 전 갱신: 2026-09-14 재정 차례 5 판 — **고치는 길이 섰다. 다음은 차례 6(시트 들여오기).** 지출(일자·금액·예산 항목·
지출자·계좌 셋·비고·원본 번호) · 예산 항목(취소된 것은 409) · 수입(새로) 을 고친다. 계좌 가르기 규칙은
`budget.split_account` 하나, 목록 계좌는 뒤 네 자리 + 복사, 내 정보 계좌를 바꾸면 칸 이름만 기록(옛 11번의 미룬 둘 ·
봐둘것 BB-c). 차례 6 으로 넘긴 것은 봐둘것 BB-g. 차례 4 닫기 판 보고는 `docs/review/2026-09-14-차례4닫기.md` 로 얼렸다.
그 전 갱신: 2026-09-14 차례 4 닫기 판 — **재정 차례 4 가 끝났다. 다음은 차례 5(고치는 길).** 사람이 남은 둘을 정했다 —
남의 부서 영수증 막기에서 총무팀은 통과(봐둘것 BB-f) · 칩의 「지출 N건」 은 취소된 지출도 센다(BB-e). 둘 다 CLAUDE.md 7-4.
차례 4 손질 판 보고는 `docs/review/2026-09-14-차례4손질.md` 로 얼렸다. 사람에게 받을 것은 없다.
그 전 갱신: 2026-09-14 재정 차례 4 판 — **계좌 셋 · 영수증 잇기 표 · 원본 영수증 번호를 한 판에 했다**(11 · 12 를 닫았다 · 29번).
사람이 시트 아홉 갈래와 물음 셋에 답했다(봐둘것 BB-a). 운영의 영수증 12장은
**이 창이 `scripts/영수증잇기옮기기.py` 로 이었다**(2026-09-14 손질 판 · 사람이 11-2 대로 정함) — 창은 닫혔다.
손질 판이 취소된 지출의 잇기·떼기(409)와 남의 부서 영수증 잇기(403)도 막았다(CLAUDE.md 7-4). 시트 모양 판 보고는
`docs/review/2026-09-14-시트모양.md` 로 얼렸다. 다음은 차례 5(고치는 길).
그 전 갱신: 2026-09-14 시트 모양 판 — **사람이 준 시트 둘을 값 없이 모양만 읽었다.** 들여오기 전에 사람이 정할 갈래가
`docs/review/최근.md`(다음 판이 `2026-09-14-시트모양.md` 로 얼린다) 4장에 있다. 사람이 셋을 더 정했다(봐둘것 BA-f —
집행률이 오르는 모양을 둔다 · 세는 시험은 지금대로 · 부팅이 칸을 붙이는 것은 읽기만의 밖이고 값을 채우는 것은 안).
지우는 길 판 보고는 `docs/review/2026-09-14-지우는길.md` 로 얼렸다. **다음 코드 판이 고칠 것 하나** —
`tests/test_stage45.py` 의 `b01b` 독스트링이 「사람이 정한다」 로 남았다(이제 「둔다」 로 정해짐).
그 전 갱신: 2026-09-14 지우는 길·세는 자리 판 — **재정 순서의 차례 3 을 했다.** 예산 항목 · 수입은 지우지 않고
취소 표시이고, 재정 숫자를 세는 곳을 `domain/budget.py` 하나로 모았다. 사람이 정한 여덟 갈래와 물음 둘은
봐둘것 BA-f. 재정 보기 판 보고는 `docs/review/2026-09-13-재정보기.md` 로 얼렸다. 다음은 차례 4(표 모양) —
「재정 — 지금 어디까지」. **아직 사람에게 받을 것: 두 시트의 xlsx.**
그 전 갱신: 2026-09-13 재정 보기 판 — **아무것도 안 만들고 재정을 봤다.** 사람이 둘을 정했다: 검토 뒤 고침을
다시 보이는 것은 한 번(CLAUDE.md 11-3 4단계) · 재정의 지우는 길은 이 판에서 봤다 — **둘 다 행을 지우고
0장과 부딪힌다.** 운영의 2026 재정 행은 시트에서 들여온 것이 아니라 **`seed_data.py` 로 들어왔다**(그 값이
시트 그대로인지는 못 가림). 순서와 사람이 정할 것은 이 판 보고 5장에 있다(`docs/review/최근.md` — 다음 판이
`2026-09-13-재정보기.md` 로 얼린다). 넷째 판 보고는 `docs/review/2026-09-13-흔적과비밀번호.md` 로 얼렸다.
**사람에게 받을 것: 「Belong 예산」 과 비품 구매 탭의 xlsx** — 무엇을 왜인지는 보고 3장.
그 전 갱신: 2026-09-13 넷째 판 — **앱 밖 자리가 흔적을 안 남기고(28번), 이 창은 로그인 칸에 비밀번호를
넣지 않는다는 것이 규칙이 됐다(CLAUDE.md 11-2).** 그래서 phone.js 의 안 심은 자가시험 둘은 이 창 혼자서는 못
돌리고 사람이 들어가 준 창에서 돌린다 — 그때까지 총무팀이 휴대폰으로 동작만 본다(22번).
**남은 것: 사역자 열 분의 계정(18번) · 개기자 주소(8번) · 재정.** 재정이 다음 큰 줄기이고 지금 서 있는
것은 「재정 — 지금 어디까지」 에 적었다. 22 · 26 은 그 옆에 열려 있다.
그 전 갱신: 2026-09-13 셋째 판 — 사고의 실타래(23·24·25)를 닫았다. DB 가 비어도
`되살아나나.ps1` 이 빈 판을 말하고, 백업 알림은 푸시로 안 나가며, pytest 앞의 `-p` 는 conftest 가 멈춘다.
그 전 갱신: 2026-09-13 후속 판 — 빈 판 경고가 총무팀 앱 알림에 서고 의심 판에 상한이 생겼다(25번).
멈출 프로세스는 작업 폴더로 가른다(CLAUDE.md 11-3 · 봐둘것 AZ-e).
그 전 갱신: 2026-09-13 앞 판 커밋·빈 백업 판 — 앞 판(23번)을 커밋했고, 빈 백업이 성한 판을 밀어내지 않게 했다
그 전 갱신: 2026-09-12 휴대폰 판 — **20번은 닫혔다(전원을 내렸다 올려도 셋 다 섰다). 22번이 반쯤 열려 있다.**
**2027 회차(9번)는 서 있고 날짜도 2027-08-20~22 로 정해졌다** — 만들 일이 아니다.
**2026 을 보관으로 넘기지 않기로 사람이 정했다(2026-09-12) — 길도 안 만든다.**
사람이 할 일은 둘이다 — 사역자 열 분의 계정(18번)과 개기자 주소 넣기(8번).
17번(비품)은 닫혔고 「끝난 것」 에 있다. 15·16 은 앞자리가 생긴 뒤다

**읽는 법** — 새 채팅은 이 파일과 `CLAUDE.md` 와 `docs/review/최근.md` 를
읽고 시작한다. **채팅을 옮길 때마다 이 파일을 갱신한다.**

항목마다 **무엇 · 재료가 어디 · 미정** 셋을 적는다. 셋 중 하나가 비면
그 자리가 다음 사람이 막히는 자리다.

---

## 사람이 정한 넷과 회차 넘나드는 시험 — 지금 어디 있나 (2026-09-13 셋째 판 · 넷째 판에서 화면 시험자 줄 고침)

| 무엇 | 어디 서 있나 | 남은 것 |
|---|---|---|
| 지시문 대조 | 보고마다 표 · `scripts/check_orders.py` 가 모양을 본다(CLAUDE.md 11-3 4단계) | 번호가 지시문과 맞는지는 기계가 못 본다 — 사람과 검토자 |
| 고치기 전 훑기 | CLAUDE.md 11-3 0단계 · 보고에 「훑기」 장 | 기계 검사 없음 — 사람과 검토자가 본다 |
| 되살아나는지 | 닫힘(20번) · `scripts/되살아나나.ps1` · 이제 빈 백업 판도 말한다 | 없음 |
| 화면 시험자 | 반쯤(22번) · `docs/checks/phone.js` · `drawer.js` | 스스로 도는 자리 없음(봐둘것 AY-a) · 820~1280 띠(AY-d) · phone.js 의 안 심은 자가시험 둘 — 이 창 혼자서는 못 돌리고 사람이 들어가 준 창에서 돌릴 수 있다. 그때까지 총무팀이 휴대폰으로 동작만 본다(22번) |
| 회차를 넘나드는 시험 | 대표 넷으로 섰다(21번) · `tests/test_stage41.py` | 지출·예산·회의록(26번) |

## 진행 중

1) **데이터 동기화**
   - 무엇: 앱의 2026 여름수련회 자료를 원본과 맞춘다. 도막 0 이 끝났다.
     다음은 **외부 id 칸**(노션 페이지 id · 시트 행 열쇠)을 만들고 **그
     열쇠로 들여오는** 도막이다. 앱의 2026 자료는 짝짓기 열쇠가 없어
     **대조가 아니라 다시 들여오기**다. 앱에만 있는 행은 지우지 않고
     **원본에 없음** 표시로 남긴다. 반영은 **노션 먼저 시트 나중.**
   - 재료가 어디: 앱 쪽 셈은 `docs/review/2026-09-09-도막0.md`
     (도막 1 의 셈은 `docs/review/2026-09-09-도막1.md`).
     원본 수 — 업무 313 · 일정 160 · 예산 34 + 수입 5 · 지출 97 · 영수증 89
     **노션 대조 1판(2026-09-15)** — 노션 업무 DB 를 읽어 보니 257줄이라 위 수와 다르다(까닭 모름).
     네 갈래 셈 · 원인 짐작 · 다음 갈래는 그 판 보고(다음 판이 날짜 이름으로 얼린다)에
   - 미정: 업무를 ㄱ · ㄴ · ㄷ 중 어느 길로 맞출지(노션 대조 1판 6장). 외부 id 를 어느 표의 어느 칸에 둘지. 「원본에 없음」 을 어떻게
     표시할지(칸인지 상태인지)

## 할 것 — **재정 줄기 완전히 끝남(2026-09-15) · 다음은 회의록이거나 사람이 다시 정한다(맨 위 갱신 · 「재정 — 지금 어디까지」 머리).** 그 전 머리: 20 은 닫혔고 22 를 사람이 앞당겼다(2026-09-12) (21 은 2026-09-13 에 섰다) — 사람이 화면에서 할 일 둘(18·8)은 그와 나란히. 비품에 남은 15·16 은 앞자리가 생긴 뒤다


26) **회차를 넘나드는 시험 넓히기 — 지출·예산·회의록** (2026-09-13)
   - 무엇: `tests/test_stage41.py` 는 비품·업무·부서·봉사팀의 **대표 자리 넷**만 잰다.
     지출·예산·회의록도 회차마다 서는 자리인데 아직 이 눈 밖이다
   - 재료가 어디: `docs/review/2026-09-13-회차넘나듦.md` 5장(무엇을 어느 길로 쟀나 ·
     엔드포인트로 쓸 것과 도메인 함수로 쓸 것을 가른 표)
   - 미정: 어느 판에서 넓힐지 — 재정 차례 4 판(2026-09-14)은 넓히지 않았고 **영수증 잇기의 회차 경계 하나만**
     잰다(`test46_c03`). 지출·예산 전체는 여전히 이 눈 밖이다

22) **화면 시험자 — 반쯤 열려 있다** (2026-09-12 에 사람이 순서를 앞당겼다)
   - 무엇: 화면을 눌러 보는 자리를 사람이 아니라 도구가 맡는다.
     **휴대폰 폭에서 실제로 누르는 눈은 생겼다** — `docs/checks/phone.js`
     (메뉴 단추 · 사이드바에서 화면 옮기기 · 닫는 길 둘 · 가장자리 띠).
     휴대폰에서 아무 데도 못 가던 고장이 아무 눈에도 안 걸려서 앞당겼다
   - **남은 것: 스스로 도는 자리가 없다.** phone.js 도 drawer.js 도 사람이
     브라우저에 붙여넣어야 돈다 — 봐둘것 AY-a
   - **사람이 챙길 것(총무팀)** — 휴대폰으로 들어가 **사이드바가 처음에 접혀 있는지, 메뉴를 열어 그 안의
     링크를 누르면 화면이 옮겨지는지** 본다. phone.js 의 안 심은 자가시험 둘이 재려던 **동작**이고, 이 창은
     로그인 칸에 비밀번호를 안 넣어 혼자서는 못 잰다(CLAUDE.md 11-2 · 봐둘것 AY-a). 점검이 새는지를
     재는 자가시험은 사람이 개발 서버에 들어가 준 창에서 따로 돌린다
   - **그리고 820~1280 띠에는 아직 눈이 없다** — 봐둘것 AY-d. 아래는
     phone.js, 위는 drawer.js 가 재고 그 사이는 아무도 안 잰다
   - 재료가 어디: `docs/checks/phone.js`(자가시험까지 있다) ·
     `docs/checks/drawer.js` · 규칙은 CLAUDE.md 11-3 2단계
   - 미정: 무엇이 그것을 돌릴지. 옮겨 담을 때 **자가시험까지** 옮겨야
     「눈이 살아 있다」 가 함께 따라간다

18) **사역자 열 명 계정 — 사람이 화면에서 만든다** (2026-09-11)
   - **2027 회차는 새로 만들지 않는다 — 이미 있다.** 9번을 보라. 이 줄이
     한때 「만든다」 였던 것은 **운영을 안 읽고 적었기 때문**이고,
     커밋 전 검토가 잡았다
   - 무엇: 사역자 열 분이 **2027 회차 선교사회**의 일반 계정으로 들어온다
     (사람이 2026-09-11 에 정했다). 설정 › 사용자에서 만들기 → 부서
     선교사회 → 아이디 → 첫 비밀번호 발급.
     **이 창은 만들지 않았다** — 실명을 저장소에 안 적기 때문이고, 화면이
     그 일을 견디는지만 **가명 열 명으로 실제로 해 봤다**(`test35_f01`).
     끊기는 자리는 없었다
   - **회차를 만드는 길은 마법사 하나다**(설정 › 회차 관리 → 「새 회차
     만들기」 → `/setup`). 개발 자료로 끝까지 가 봤고 부서 아홉이 키 그대로
     섰다 — 손으로 아홉을 만드는 일이 아니다. 잰 값은 그때의 검토 보고에.
     **다음에 회차를 하나 더 열 때 쓰는 길이고, 2027 에는 쓸 일이 없다**
   - **소속은 회차가 아니라 키에 붙는다**(4-12). 그래서 **회차마다 다시
     붙일 일이 아니다** — 어느 회차의 선교사회에 붙이든 그 사람은 모든
     회차에서 선교사회이고, 2027 업무의 담당자 후보에 그대로 뜬다
     (`test36_c02` 가 화면이 답하는 자리로 잰다). 지시문이 한때 「2026 에
     붙이면 2027 에 다시 붙여야 한다」 고 적었는데 **그것이 틀렸다** —
     2026-09-12 판이 재어 바로잡았다
   - **뗄 때도 회차를 안 가린다** — 2027 폼에서 어떤 사람의 선교사회
     체크를 끄면 2026 쪽 소속도 함께 사라진다(봐둘것 AV-c). 사역자를
     붙이는 자리가 바로 그 화면이라 적어 둔다
   - **아이폰 쓰는 분께 한 가지만 부탁한다** (2026-09-13). 첫 로그인
     때다: **왼쪽 위 ☰ 로 메뉴를 연 뒤, 메뉴 밖(본문)을 한 번 눌러 메뉴가
     닫히는지** 봐 달라고 한다. 안 닫히면 ☰ 를 다시 누르면 닫히므로
     갇히지는 않는다 — **닫히는지 아닌지만** 알려 주면 된다.
     까닭은 봐둘것 AY-c 에 있다(사파리는 단추가 아닌 것의 누름을 document
     까지 안 올릴 수 있어 코드에서 먼저 막아 두었고, **실기로는 못 쟀다**).
     사역자에게 보낼 글(`docs/로그인-안내문.md`)에는 안 적는다 — 총무팀이
     챙길 일이지 받는 사람이 읽을 안내가 아니다
   - **업무 100건은 그대로 둔다** (2026-09-12 에 사람이 정함). 사역자가
     처음 보는 목록이 백 건이지만, 부서 드롭다운의 기본이 「내 부서」 라
     선교사회만 보인다(4-14). 줄이려면 `included` 를 꺼야 하는데 **회차를
     연 뒤 업무를 빼는 길이 화면에 아직 없다**(12장이 적어 둔 그 사실)
   - 비품·프로그램(봉사팀)·예산은 애초에 안 따라왔다 —
     `domain/library.py` 에 그 이름들이 아예 없다. 프로그램표가 필요하면
     `/live` 의 「지난 회차에서 복사해 오기」 로 따로 한다(5-5)
   - 재료가 어디: 보낼 글은 `docs/로그인-안내문.md`(비울 자리 넷과 무엇을
     채우는지가 그 파일 위에 표로 있다). 아이디는 **이름의 로마자**이고
     겹치면 화면이 누구 것인지 말한다. 첫 비밀번호는 **발급한 그 자리에서
     한 번만** 보인다 — 놓치면 다시 발급해야 하고 앞의 것은 죽는다
   - 미정: **실명과 아이디를 누가 정하는지** — 총무팀이 정한다. 저장소에는
     안 적는다. 그리고 그분들이 **이번 회차에 붙을지 다음 회차에 붙을지** —
     부서는 회차마다 서므로(2장) 어느 회차의 선교사회에 붙일지가 갈린다

8) **개기자 운영 앱 관리자 웹 링크** — 자리는 도막 4 에서 만들었다
   - 무엇: 설정 › 바깥 링크(총무팀만)에 이름·주소를 넣으면 사이드바 끝에
     새 창으로 여는 항목이 뜬다. **주소는 DB 에만 있고 저장소에는 없다** —
     사람이 넣는다
   - 재료가 어디: **설정 › 바깥 링크에 사람이 넣는다.** 주소 자체는 이 파일에서
     뺐다(옛말 검사가 잡는다). git log 의 이 파일 옛 판에는 남아 있다
   - 미정: 없음 — 사람이 주소를 넣는 것만 남았다

9) **2027 여름회차** — **서 있고 날짜도 정해졌다** (2026-09-12)
   - 무엇: 운영에 회차 id 2 「2027 여름수련회」 가 2026-08-30 에 만들어져
     있었고, **2026-09-11 에 사람이 개회일을 2027-08-20~22 로 고쳤다.**
     읽어서 확인했다 — 활동 기록에 `회차_수정` 한 줄이 있고, 업무 100건의
     날짜가 **D-주차를 지킨 채 함께 움직였다**(어긋난 것 0). 움직이지 않은
     여섯은 **뺀 업무**라 원래 날짜가 없다
   - **기본 회차도 이것이다** — `app/deps.py` 가 「보관 안 된 것 중 개회일이
     가장 늦은 것」 을 고른다. 사역자가 들어오면 이 회차를 본다
   - 재료가 어디: 운영을 읽어 센 값은
     `docs/review/2026-09-12-2027확인.md` 1장 — **덮이는 `최근.md` 가
     아니라 얼린 판을 가리킨다**(0장 · 11-3 4단계).
     고치는 자리는 설정 › 회차 관리 → 그 회차 → 「수정」
   - 미정: 없음. **2026 을 보관으로 넘기지 않기로 사람이 정했다**
     (2026-09-12). **길도 만들지 않는다** — 지금 새는 곳이 없다:
     기본 회차를 고르는 기준이 「개회일이 가장 늦은 것」 이라 2027 이
     이미 이기고, 2026 은 폐회일이 지나 `period.is_over` 도 이미 참이다.
     재어 둔 것은 봐둘것 AW-a 에 그대로 있다 — 회차가 셋째로 설 때 쓴다

10) **Claude 창구**
    - 무엇: 회의록 → 재정(영수증 사진 + 조건 → 지출·결산) → 채팅(업무
      추가·변경 지시 → 생성·편집·정리) 순으로 연다. **Claude 는 제안만
      하고 사람이 고른다.** 고르는 과정이 **아주 쉽고 짧아야** 한다.
      반영된 항목에는 **출처를 기록**한다
    - 재료가 어디: 회의록 쪽은 이미 있다 — `app/domain/suggest.py` ·
      `app/domain/llm.py`(문은 여기 하나)
    - 미정: 재정 쪽 입력(사진)을 무엇으로 읽을지. 「조건」 을 어디에 적을지

## 재정 — 지금 어디까지 (2026-09-15 재정 줄기 마감 판)

> **차례 1~6 전부 닫힘 · 30건 시트 결산 식으로 운영에 실제 반영됨(2026-09-15).** 7 · 8 은 뒤로 미룬 채 남아 있다.
> **재정 줄기 완전히 끝남 · 화면으로도 확인됨(2026-09-15) · 다음은 회의록이거나 사람이 다시 정한다.** 재정 화면 넷(계좌 셋 입력 · 영수증 잇기 · 고치기 · 취소)이 개발 서버에서 저장까지 됐다 — 계좌번호 칸은 총무님이 직접, 나머지는 이 창의 참고 점검(아래 「화면 점검」). 정식 11-3 점검(보이는 탭 · 두 계정)은 아직이다.
>
> **재정에 남은 정할 것: 없음**(2026-09-15 · 마지막이던 09-13 사고 직전 판은 사람이 지키는판에 넣기로 정해 넣었다). 지금 정할 것은 없다. 아래 표는 줄기를 닫은 뒤에도 남는 일이다 — 재정과 무관한 일(점검 스크립트 · 개발 DB 는 2026-09-15 에 닫힘)과, 재정에서 뒤로 미뤄 그때 가서(다음 회차나 다음 시트) 사람이 정할 일(차례 7 · 8 · BB-i)이 함께 있다.
>
> **남은 것 — 한 자리에 모음**
>
> | 무엇 | 누가 · 언제 | 어디 |
> |---|---|---|
> | `docs/checks/drawer.js` · `phone.js` 정식 점검 | 2026-09-15 의 것은 참고 기록(탭 hidden · DOM 클릭 · 400px 틀 — 정식 11-3 점검 아님 · 사람이 정함). 남은 것은 보이는 탭에서 다시 · 실제 휴대폰 | 아래 「화면 점검」 · CLAUDE.md 11-2 · 11-3 2단계 |
> | 부서 리더 개발 계정 만들기 | 급하지 않은 후속(2026-09-15 사람이 정함) · 비밀번호는 사람이 넣는다 — 만들면 정식 점검의 건너뜀 12 쪽을 잰다 | 아래 「화면 점검」 |
> | 16번 구매목록 | 재정과 별개 판 | 이 문서 「할 것」 16 · 순서표 8 |
> | 지난 회차 예산 가져오기 | 다음 회차를 열 때 · 사람이 정함 | 순서표 7 · 봐둘것 BA-c |
> | 결산 식 파서가 열을 안 봄 · 글자로 못 잇는 줄은 식이 틀어져도 짝 표에 목록이 안 남음 | 다음 시트를 들이기 전에 사람이 정함 | 봐둘것 BB-i 머리 |

### 개발 DB 다시 만들기 — 안내 (2026-09-15 · 총무님이 직접) — **닫힘 · 실행 완료(2026-09-15 · 총무님이 다시 만들고 첫 관리자로 로그인까지 확인)**

> 아래는 그때의 안내 원문이다(「무엇이 막혀 있나」 는 그때의 사정이고 지금은 풀렸다). 다시 할 일이 생기면 이 순서를 쓴다.

**무엇이 막혀 있나** — 개발 서버(`scripts/devserve.bat`)가 쓰는 개발 DB 에 옛 seed 가 넣은 지어낸 계좌 5칸(`expense_entries.payer_account`)이
남아 `scripts/check_dev_db.py` 가 서버를 안 띄운다(2026-09-15 에 이 창이 그 검사를 돌려 확인 · 값은 안 찍음).

**지울 파일은 하나다** — `C:\Users\user\AppData\Local\Temp\dcb-dev\app.db`(= `$env:TEMP\dcb-dev\app.db`).
운영 DB 는 `C:\Users\user\Desktop\대청부-수련회-시스템\data\app.db` 로 **폴더부터 다르다.** 개발 서버가 그 폴더를 고르는 것은
`devserve.bat` 의 `DCB_DATA_DIR` 한 줄이고, 그것을 덮어쓸 수 있는 `DCB_DATABASE_URL` 은 이 노트북의 사용자·시스템 환경변수에
**없다**(2026-09-15 확인). 같은 폴더의 `secret_key.txt` · `vapid_private.pem` · `uploads` 는 안 지운다.

**새 seed 가 안전한 근거** — `devserve.bat` 이 부르는 기본 `seed.py`(인자 없음)는 회차 · 부서 · 업무 라이브러리만 만들고 **계정도 지출도 안 만든다**
(`--demo` 갈래에서만 지출이 선다). 그 갈래에서도 `seed_data.py` 의 식대 지출 계좌는 전부 999 머리(지어낸 표시)이고 `seed.py` 는 그 칸을
`_account` 로 받아 **DB 에 쓰지 않는다.** 옛 개발 DB 를 `--demo` 로 만들었다면 그 화면 자료(할 일 · 회의록 · 지출)는 다시 만든 뒤 비어 있다. 네 seed 파일에서 `app/account_shape.py` 의 계좌 꼴에 걸리는 글은 0 이다.
이 창이 이 창의 임시 폴더에 새로 seed 를 만들어 `check_dev_db.py` 를 돌리니 통과(exit 0)했다 — 개발 DB 와 운영 DB 는 안 건드렸다.

1. 개발 서버가 켜져 있으면 그 창에서 Ctrl+C 로 끈다(켜진 채로는 파일이 잠긴다)
2. 저장소 폴더에서 PowerShell 로 지운다

   ```powershell
   Remove-Item "$env:TEMP\dcb-dev\app.db"
   ```

3. 개발 서버를 켠다 — DB 가 없으면 `devserve.bat` 이 `seed.py` 로 새로 만들고 곧바로 뜬다(seed 명령을 따로 칠 필요 없음)

   ```powershell
   .\scripts\devserve.bat
   ```

4. **떴는지 보기** — 그 창에 `Uvicorn running on http://127.0.0.1:8001` 이 나오면 떴다. **새로 만든 DB 는 `devserve.bat` 이 검사를 건너뛰고 켜므로**
   (DB 가 있을 때만 검사한다) 검사는 따로 돌린다. 다른 PowerShell 창을 **저장소 폴더에서** 열고 아래 둘이 차례로 `0` · `200` 이면 끝이다

   ```powershell
   .venv\Scripts\python.exe scripts\check_dev_db.py; $LASTEXITCODE
   (Invoke-WebRequest http://127.0.0.1:8001/login -UseBasicParsing).StatusCode
   ```

5. 개발 서버에 로그인하려면 첫 관리자가 필요하다 — 새 seed 는 계정을 안 만든다. **비밀번호가 창에 찍히므로 총무님이 직접**
   (CLAUDE.md 11-2). **저장소 폴더에서** 친다. 첫 줄을 빼면 운영 폴더를 보게 되니 반드시 함께 치고, 끝나면 마지막 줄로 되돌린다.
   devserve 창에 seed 가 찍어 주는 계정 명령에는 첫 줄이 없으니 **그 줄을 긁어 쓰지 않는다**

   ```powershell
   $env:DCB_DATA_DIR = "$env:TEMP\dcb-dev"
   .venv\Scripts\python.exe scripts/계정문열기.py --첫관리자 "이름" 아이디
   Remove-Item Env:DCB_DATA_DIR
   ```

   (운영에 관리자가 있는 동안에는 첫 줄을 빠뜨려도 스크립트가 만들지 않고 멈춘다 — 운영 계정이 빈 날에는 그 막이 없으니 늘 첫 줄부터 친다)

### 화면 점검 (2026-09-15 · 개발 서버 8001) — **참고 기록 · 정식 11-3 점검 아님** · 재정 화면 넷 전부 확인 완료(2026-09-15)

> **사람이 정함 (2026-09-15)** — 이 점검은 탭이 가려진 채(hidden) · DOM 클릭 · 400px 틀로 쟀다. 타이머에 기대는 항목이 배경 탭에서 제대로 재졌는지 확신할 수 없어 정식 11-3 2단계 점검으로 치지 않고 참고 기록으로만 둔다.

**확인 9 · 실패 0 · 못 함 0**(관리자 계정 · 2026 회차) — 확인은 drawer.js 세 화면(256항목) · phone.js(11항목) · 재정 넷(예산 항목 추가는 곁가지로 셈 안 함) · 계좌번호 칸(총무님 직접). 깨진 화면: **없음**(실제로 누른 뒤의 없음 — 다만 아래 한계 안에서).

| 무엇 | 결과 |
|---|---|
| `drawer.js` 목록 | 93항목 통과 · 건너뜀 5 · 못 쟀음 0 · 완주 |
| `drawer.js` 보드 | 80항목 통과 · 건너뜀 3 · 못 쟀음 0 · 완주 |
| `drawer.js` 달력(8월 · 범위 전체) | 83항목 통과 · 건너뜀 4 · 못 쟀음 0 · 완주 |
| `phone.js`(400px 틀) | 11항목 통과 · 건너뜀 2(손가락 기기 조건 · 호버 없는 기기 조건) · 못 쟀음 0 · 완주 |
| 재정 — 예산 항목 추가 | 저장됨(총액에 반영) |
| 재정 — 지출 등록 · 계좌 셋 | 은행 · 예금주 저장 · 목록에 보임 |
| 재정 — 계좌번호 칸 | **됨(총무님 직접 확인 · 2026-09-15)** — 개발 화면에 999 로 시작하는 지어낸 번호를 넣어 저장했고 뒷자리만 보이는 표시와 「복사」 단추가 작동했다. 이 창은 넣지 않았다(이 창이 따르는 바깥 작업 규칙 · CLAUDE.md 규칙 아님) |
| 재정 — 영수증 잇기 | 메모 영수증을 단 지출의 번호를 다른 지출에서 「번호로 잇기」 → 두 행에 「지출 2건」 |
| 재정 — 고치기 | 금액 · 비고를 고쳐 저장 · 은행 · 예금주는 그대로 |
| 재정 — 취소 · 되살림 | 취소하면 흐리게(투명도 0.45) 남고 합계에서 빠짐 · 잇기 단추가 사라짐 · 되살리면 합계로 돌아옴 · 콘솔 오류 0 (확인창은 그 클릭 동안 자동 수락이라 뜨는지는 안 봄) |

개발 DB 를 읽기 전용으로 다시 세어 저장을 확인했다(산 예산 항목 1 · 지출 2 · 영수증 1 · 끊기지 않은 잇기 2 · 고친 금액 1 · 지출 활동 기록 6). **개발 DB 에 점검용 행이 남아 있다 — 지우지 않았다**(사람이 정함). 이름에 「점검」 이 붙어 있다 — 예산 항목(구분 「점검」) · 지출 「화면점검 A」 · 「화면점검 B」(지출자 「점검지출자」 · 은행 「점검은행」 · 메모 영수증 1장) · 총무님이 넣은 999 로 시작하는 계좌번호. 개발 화면을 여는 사람은 그것을 실제 자료로 읽지 않는다.

**어떻게 쟀나 · 한계** — 확장이 붙은 Chrome 창이 다른 창 뒤에 있어(탭이 hidden) 좌표 클릭이 안 닿았다. 그래서 점검 스크립트는 이 창만 쓰는 임시 파일 서버에서 받아 페이지 안에서 돌렸고(바이트 수를 파일과 견줌), 재정 화면은 **폼과 단추를 DOM 에서 눌렀다**(`button.click()` — 브라우저의 보통 폼 제출). 휴대폰 폭은 창 크기를 못 줄여 **같은 페이지 안 400px 틀(iframe)** 에서 쟀다 — 폭 · 미디어 쿼리는 틀을 따르지만 호버 · 터치는 데스크톱 기기를 따른다. **11-3 2단계의 전제에서 벗어났다** — 탭이 hidden 이었다(그 절은 탭이 보여야 한다고 적는다 · 완주하고 못 쟀음 0 이었지만 시간에 기대는 항목이 배경 탭에서 제대로 재졌는지는 모른다). 폭은 1707px 로 1280 위였다. DOM `click()` 은 제출 경로를 타지만 좌표 판정을 건너뛰어 **사람 손으로 눌리는지(덮여 있지 않은지)는 못 봤다.** 건너뜀 12(5 · 3 · 4)는 개발 DB 에 부서 리더 계정이 없는 몫으로 보이나 줄마다 까닭을 옮기지는 않았다.

**앞선 시도(같은 날)** — 총무님이 처음 로그인한 브라우저가 웨일이라 확장이 붙은 Chrome 에 세션이 없어 못 돌렸다. 그 뒤 개발 쿠키 이름을 운영과 갈랐다(CLAUDE.md 11-2) — 그 판 시점에 떠 있던 개발 서버는 환경변수 없이 뜬 옛 프로세스라 **아직 운영과 같은 이름을 썼다**(그 뒤 다시 켰는지는 기록이 없다). 개발 서버를 다시 켠 뒤부터 갈리고, 그때 개발에는 다시 로그인해야 한다.

다음 판이 이 위에서 시작한다. 규칙은 CLAUDE.md 7장(7-1 에 2026-09-14 시트 실측). **사람이 정한 것은 봐둘것 BA-f**
(여덟 갈래 · 물음 둘 · 셋 — 되묻지 않는다). 순서·까닭은 얼린 판 `docs/review/2026-09-13-재정보기.md` 5장,
차례 3 은 `docs/review/2026-09-14-지우는길.md`, 시트 모양과 들여오기 전에 정할 갈래는 `docs/review/최근.md`
(다음 판이 `2026-09-14-시트모양.md` 로 얼린다) 4장.

**순서(재정 보기 판 5-1 · 사람이 아-ㄱ 으로 정함)에서 어디까지 왔나**

| 차례 | 무엇 | 지금 |
|---|---|---|
| 1 | 시트 두 개를 받는다 | **받았고 모양을 읽었다**(2026-09-14 · `data/Belong예산.real.xlsx` · `data/비품구매.real.xlsx` — 저장소 밖). 들여오기 전에 정할 갈래(시트-가 ~ 시트-자)와 물음이 사람에게 가 있다 |
| 2 | seed 재정을 어떻게 할지 | **정함**(BA-f 의 가-ㄷ) — 들여올 때 취소 표시로 내리고 지금은 그대로 |
| 3 | 지우는 길 둘을 취소 표시로 · 세는 자리를 모은다 | **했다**(2026-09-14) |
| 4 | 표 모양 넷을 한 판에 — 계좌 셋 · 영수증 잇는 표 · 원본 영수증 번호 칸 · (취소 칸은 3 에서 섰다) | **끝났다**(2026-09-14 · 29번 · CLAUDE.md 7-4). 운영의 영수증 12장은 이 창이 스크립트로 이었고(손질 판), 남은 정할 것 둘도 닫혔다(닫기 판 · 봐둘것 BB-e · BB-f) |
| 5 | 고치는 길 — 수입 · 지출(BA-f 의 바-ㄱ) | **끝났다**(2026-09-14 · CLAUDE.md 7-3 · 7-4). 계좌 가르기 규칙은 `budget.split_account` 로 올렸다 · 남은 정할 것 둘도 닫혔다(닫기 판) · 넘긴 것은 봐둘것 BB-g |
| 6 | 시트 들여오기 | **끝남 · 운영 반영됨**(2026-09-15 · CLAUDE.md 7-1 · 7-5 · `scripts/재정들여오기.py` · 되돌리는 사본 `data/backups/app-20260915-002352.db`). **재정짝표 30건 — 시트 결산 식으로 운영에 채움**(2026-09-15 · `scripts/재정짝잇기.py` · 직전 사본 `data/backups/app-20260915-144929.db` · 미지정 0 · 손 안내문은 닫음 · 다음 시트부터 들여오기가 결산 식 기본) · 짝 표는 봐둘것 BB-h |
| 7 | 지난 회차 예산 가져오기(BA-f 의 사-ㄴ · 봐둘것 BA-c) | 6 뒤 |
| 8 | 비품 구매 탭 → 지출 후보(16) | 4 · 5 뒤 |

- **예산 항목 · 수입은 지우지 않는다** — 취소 표시(`canceled_at` · 지출과 같은 모양)이고 기록에 그때의 값이 남는다.
  걸린 지출이 있어도 취소되고, 그 지출의 집행은 「(취소된 예산 항목에 걸린 지출)」 줄로 결산 합에 든다(7-3)
- **재정 숫자를 세는 곳은 `app/domain/budget.py` 하나다** — 지출 화면의 지표 넷(`list_totals` · 필터에 걸린 행
  기준이라 summary 와 따로) · 필터 조건(`filter_entries`) · 엑셀 환급 시트(`refund_sheet`) · 수입 금액 식
  (`income_amount_of`) · 회차 목록의 지출 건수·합(`expense_stats` — 재정 보기 판이 못 센 자리) · 결산 파일의
  취소 거름(`live_entries`)을 모았다. `tests/test_stage45.py` 가 밖에서 **합·사칙 셈**으로 다시 세는지 끌어낸
  이름으로 잰다 — 필터 조건과 건수는 못 본다
- **걸린 지출이 있는 항목을 취소하면 집행률이 오른다**(예산은 빠지고 집행은 남음 · 7-3). **그 모양을 둔다**
  (2026-09-14 에 사람이 정함 · BA-f)
- **운영의 2026 재정 행(예산 9 · 지출 12 · 영수증 12)은 `seed_data.py` 로 들어왔고, 그 금액은 시트에서 온 한 조각이다**
  (사람이 답함 · BA-f). 계좌 넷은 자리표시자다. 이 판이 운영 사본에서 옛 식과 새 함수를 견줘 같았다

| 자리 | 서 있는 것 | 없는 것 |
|---|---|---|
| 숫자 | `app/domain/budget.py` — `build_budget_summary`(산 예산 항목 · 취소된 항목 · 구분 소계 · 미지정 · 취소된 항목에 걸린 지출 · 산 수입과 잔액 · `unspent_*`) · 위 다섯 함수 · 다음 영수증 번호. 홈 · 회차 목록·상세 · 예산 · 지출 · 엑셀이 **이것만 부른다** | 결산 정식 양식(사용자 제공 전 · 7장) |
| 예산 화면 `/budget` | 목록 · 항목 만들기·고치기(단가×명수×횟수) · 수입 넣기 · **예산 항목·수입 취소/되살림** | (수입 고치기 · 취소된 항목 고치기 막음은 차례 5 에서 섰다) |
| 지출 화면 `/expenses` | 등록 · 영수증 붙이기 · 번호로 잇기 · 떼기 · 지급 표시 · 취소/되살림 · 필터(식대·미지급·환급·영수증 없음) · `/refunds` 는 301 | 부서 · 식대 여부는 못 고친다(취소하고 새로 · 7-4) · (지출 고치기는 차례 5, 영수증 1장에 지출 여럿 · 계좌 셋 · 원본 번호는 차례 4 에서 섰다) |
| 엑셀 | `/expenses.xlsx` 하나 — 시트 셋(지출 상세 · 예산 대비 집행 · 환급 대상자). 계좌 칸은 볼 수 있는 사람에게만. 취소된 예산 항목·수입은 빠지고 그 항목에 걸린 지출은 따로 한 줄 | 예산만 따로 내려받는 길 |
| 운영 자료 (읽기 전용으로 셈) | 2026 회차에 예산 항목 9 · 지출 12 · 영수증 12 · 수입 0 · 취소 0 — **전부 `seed_data.py` 로 들어온 행**(재정 보기 판). 2027 회차는 전부 0 | 영수증 12 가 전부 파일도 메모도 없다(옛 12번의 그 12행 · 29번이 잇는다) · 시트를 들여오는 길로 들어온 행은 없고, 수도 원본(1번)에 한참 못 미친다 |

**인계 15·16 이 재정에 붙는가** — **16 은 붙는다**(구매 탭 → 지출 후보 — 지출 쪽 자리가 먼저 있어야
한다). **15 는 안 붙는다** — 옛 회차 탭을 비품 라이브러리에 들여오는 일이고 기다리는 것은 봐둘것 AO-b
(라이브러리 품목을 이번 회차에 담는 자리)이지 재정이 아니다.

15) **비품 — 옛 회차 탭 셋** (AO-b 뒤)
   - 무엇: 총무 시트의 지난 회차 탭 셋을 **라이브러리에만** 들여온다. 그 회차의
     `EquipmentRun` 은 만들지 않으므로 지금 화면에는 안 보인다 — 라이브러리의 품목을
     이번 회차에 담는 자리(봐둘것 AO-b)가 생긴 뒤에 해야 사람이 그것을 쓸 수 있다
   - 재료가 어디: 원본 「수련회 비품 목록_Belong」 의 옛 회차 탭 셋. 들여오는 길은
     `scripts/비품들여오기.py` 를 그대로 쓰되 run 을 안 만드는 갈래가 필요하다
   - 미정: 옛 회차 탭의 품목을 이번 회차 품목과 같은 것으로 볼지(같은 (팀, 이름)이면
     합쳐진다 — 그것이 맞는지) · 화면에 안 보이는 품목을 사람이 어떻게 찾는지

16) **비품 구매 탭 → 지출 후보** (지출 쪽 자리가 생긴 뒤 — 재정 차례 6 이 2026-09-15 에 운영에 반영돼 **이제 할 수 있다** · 사람이 다른 것을 먼저 정할 수 있다)
   - 무엇: 총무 시트의 구매 탭을 지출(7-4)의 후보로 잇는다. 지금은 비품과 지출을
     잇는 칸이 없어 담을 자리가 없다
   - 재료가 어디: 원본 「수련회 비품 목록_Belong」 의 구매 탭
   - 미정: 품목과 지출을 어느 칸으로 잇는지(run 에 지출 id 인지 지출에 품목 id 인지) ·
     구매 탭의 금액을 예산 라인(7-3)과 어떻게 맞추는지
---

## 끝난 것

**29) 재정 차례 4 — 계좌 셋 · 영수증 잇기 표 · 원본 영수증 번호 (2026-09-14).** 옛 11 · 12 를 닫았다.
계좌는 은행 · 계좌번호 · 예금주 셋(지출 · 사람)이고 옛 한 칸은 남기되 안 읽고 값도 안 옮겼다(다-ㄷ).
영수증은 종이 한 장이 한 행이고 잇기 표(`expense_receipt_links`)로 지출과 잇는다 — 번호로 잇기 · 떼기(영수증은
남음). 원본 영수증 번호 칸은 글자 · 겹칠 수 있음. 새 칸은 전부 NULL 로 붙였다(11-2). 지금 있는 영수증은
`scripts/영수증잇기옮기기.py` 가 잇는다 — 운영에서 이 창이 돌려 12장이 전부 처음 붙은 지출에 이어졌다(손질 판 · 직전 사본 app-20260914-164215.db). 차례 4 판 보고는 `docs/review/2026-09-14-차례4.md` 로 얼렸다.
11 에 적혀 있던 「게이트에 계좌 꼴을 더한다」 는 이 판 지시문 밖이라 봐둘것 BB-c 로 옮겼다.

11) **계좌 셋으로 나누기 — 닫혔다 (2026-09-14 · 29번).** 옛 값은 옮기지 않았다(다-ㄷ) · 게이트의 계좌 꼴은 봐둘것 BB-c

12) **영수증 1장에 지출 여럿 — 닫혔다 (2026-09-14 · 29번).** 잇기 표 · 금액은 지출마다 · 운영 12장은 이 창이 스크립트로 이었다

**28) 앱 밖 자리가 흔적을 안 남긴다 · 비밀번호 칸 규칙 (2026-09-13 넷째 판).** `backup.py --살핀다` 가
`app.config`(읽히는 순간 폴더를 만들고 서명키를 쓴다) 대신 `app/paths.py` 에서 경로만 받는다. `data/` 에
백업만 남은 자리에서 불러도 전후가 같고 말은 성한 자리와 글자까지 같다(`test44_h01`·`h02`). 이 창은
로그인 칸에 비밀번호를 넣지 않는다 — 운영이든 개발이든(CLAUDE.md 11-2). 셋째 판(27번)의 보고는
`docs/review/2026-09-13-앱밖자리.md` 로 얼렸다

**27) 사고의 실타래를 닫는다 — 앱 밖 자리 · 새벽 푸시 끔 · `-p` 금지 (2026-09-13 셋째 판).**
DB 가 계정까지 비면 앱 안 알림은 못 선다. 그래서 `scripts/되살아나나.ps1` 이 백업 폴더만 읽어
빈 판을 말한다(`backup.py --살핀다`) — 앱 안과 같은 `backup.의심말들` 을 부른다. 다만 그 스크립트를
여는 것은 사고 뒤라 늦다(CLAUDE.md 11-2). 백업 알림은 푸시로 안 나가고, 앞으로 알림의 푸시는
「그 순간 사람이 움직일 일인가」 로 정한다. pytest 앞의 `-p` 는 conftest 의 `pytest_configure` 가
예외 없이 멈춘다 — conftest 보다 먼저 도는 것(PYTHONSTARTUP 등)은 기계가 못 본다(11-3).

**25) 빈 백업이 성한 판을 밀어내지 않는다 (2026-09-13).** 운영이 빈 날 새벽 백업이
빈 판을 떴고, 개수(30)로 지우면 빈 판이 하루 한 칸씩 성한 판을 밀어낸다.
`scripts/backup.py` 가 뜰 때 표마다 행 수를 옆 파일(app-날짜.rows.json)에 남기고,
회차가 0 이거나 행이 가운데 판(중앙값)의 `SUSPECT_RATIO` 미만인 판은 **성한 칸에 안 센다**.
의심 판은 따로 `SUSPECT_KEEP` 까지 남기고 크기가 넘치면 먼저 지우며, 사고의 증거 판은
`지키는판` 이 붙든다. 빈 판이 뜨면 총무팀 앱 알림에 한 번 선다(2026-09-13 후속 판). 되돌리는 절차(CLAUDE.md 11-2 · 배포-안내)에
「먼저 행 수를 본다」. 그날 있던 백업 18벌 중 빈 판은 하나였다 — 그 판의 보고에.
그리고 운영을 직접 여는 표본 시험은 **읽기 전용 엔진**으로 못박았다(봐둘것 AZ-b)

**23) 앞 판(휴대폰 마무리 · 아이폰 · 회차 넘나듦) — 커밋했다 (2026-09-13).** 그 판이
시험을 돌리다 운영 DB 를 비워 커밋 전에 멈췄던 것을, 수습 판 뒤에 따로 커밋했다.
그 판의 보고는 `docs/review/2026-09-13-회차넘나듦.md` 로 얼렸다(12장이 사고 기록 —
수습은 24번)

**21) 회차를 넘나드는 시험 — 대표 자리 넷으로 섰다 (2026-09-13).** `tests/test_stage41.py`
가 비품·업무·부서·봉사팀에서 「A 를 고치면 B 가 안 바뀐다」 와 공유될 것이 공유된
채인지를 잰다. **지출·예산·회의록은 아직 이 눈에 없다** — 26번으로 열어 두었다

**24) 시험이 비운 운영 DB — 되돌렸고 다시는 못 지우게 막았다 (2026-09-13).**
`data/app.db` 를 15:18 백업으로 되돌렸고 표 37개의 행 수가 백업과 같다(되돌리기
전·복사 직후·앱이 뜬 뒤). 그 뒤 아홉 시간 동안 입력한 것이 없다고 사람이
확인했다. 막은 곳은 `tests/conftest.py` 의 `_시험DB가_아니면_멈춘다` — 입구가
아니라 `drop_all` 앞이다. 기록은 봐둘것 AZ-a, 같은 기계에서 시험을 돌린다는
사실은 AZ-b(사람이 정할 것). 비운 채의 사본은 증거로 남겼다. 잰 값은
`docs/review/2026-09-13-사고수습.md`. **주의 — 그날 03:00 정기 백업은 빈 DB 를
떴다**(AZ-a 의 표)

**20) 되살아나는지 — 닫혔다 (2026-09-12).** 사람이 전원을 통째로 내렸다
올렸고 **셋 다 스스로 섰다** — 부팅 21:05:55 · 앱이 21:06:20 에 트리거=Boot 로
뜸 · 터널 커넥터가 21:06:33 에 붙음 · 휴대폰에서 밖으로 열려 로그인까지 됐다.
「앱이 부팅으로 스스로 떴다는 자국」 이 그 판의 미정이었는데, 이번 부팅에서
작업 이력으로 실제로 봤다. 재는 것은 `scripts/되살아나나.ps1`, 절차는
`docs/배포-안내.md` 12-4.

19) **보드에서 손으로 옮긴 자리가 개회일이 바뀌어도 산다** — **닫힘 · 2026-09-12**
   - 무엇: run 이 「그때 셈한 자리에서 며칠」(`moved_offset_days`)과 「그 자리를
     정한 때」(`moved_at`)를 갖는다. 개회일이 바뀌면 **안 옮긴 것은 다시 세고
     옮긴 것은 같은 간격만큼 함께 민다.** 켠 채 날짜가 빈 업무는 **빈 채**다
     (AW-c 도 같이 닫았다). 라이브러리의 상대 위치를 고친 때
     (`TaskLibrary.dates_changed_at`)와 부딪히면 **나중 것이 이기고**, 어느 쪽이
     이겼는지는 상세 패널에 한 줄로 뜨며 **그 자리에서 되돌린다.**
     저장하기 전에 갈래마다 몇 건인지가 뜬다(6-4 의 표).
     규칙은 CLAUDE.md 6-4 한 곳
   - 재료가 어디: `app/domain/library.py`(`hand_wins` · `dates_for` · `_plan`) ·
     `app/routers/board.py`(`move_dates` · `moved_state` · `/dates/follow`) ·
     `tests/test_stage39.py`. 봐둘것 AW-b · AW-c 는 원문을 남긴 채 닫았다
   - 미정: 없음. **이미 선 업무는 「안 옮김」 으로 시작한다** — 부팅은 칸을
     붙이기만 한다. 운영에는 라이브러리 셈과 날짜가 다른 run 이 **회차 1 에 둘**
     있고(2026-09-12 에 읽기 전용으로 셌다) 그 둘은 채워 넣지 않았다.
     다음에 회차 1 의 개회일을 바꾸면 라이브러리 셈으로 돌아간다


17) **비품 — 시트도 비고도 들어갔고 약 묶음도 합쳐졌다** — **닫힘 · 2026-09-11**
   - 무엇: **두 시트가 다 들어갔다.** 코람데오 28줄은 2026 여름수련회 Belong(id 1)
     에 있고, 총무 시트도 그 뒤에 들어갔다 — 도막 4 가 운영을 읽기로 세어 확인했다
     (품목 174 · run 188 · 총무 시트의 묶음이 실제로 서 있다. 도막 3 직후에는 58/58
     이었다). **언제 무엇으로 돌렸는지는 앱 밖이라 기록이 없다** — 그 수를 믿고
     무엇을 세지 않는다
   - **비고도 회차로 옮겨졌다** — 사람이 운영에서 `scripts/비품비고옮기기.py` 를
     돌렸다(2026-09-11 · 사본 `app-20260911-155104.db` · 품목 62 → run 70 ·
     두 번째 판 0건). 이제 비고는 `EquipmentRun.note` 이고 `/equipment` 가 보인다
   - **갈린 약 묶음도 합쳐졌다** (2026-09-11). 운영을 읽기 전용으로 세어 확인했다 —
     묶음 35 → **25**, `의약품` 10 → **23**, `소화제`·`감기약`·`연고`·`통제` 는
     **0줄**이다. 얼린 판(`docs/review/2026-09-11-비품도막5.md` 6장)이 미리 적어 둔
     예측과 정확히 같다. 사본 `app-20260911-171608.db` 에는 아직 갈린 채로 남아
     있으므로 **합치기는 그 뒤에 돌았다** — 다만 그 파일이 합치기의 사본인지
     새벽 백업인지는 이름이 같아 가릴 수 없다(둘 다 `app-<때>.db`)
   - 재료가 어디: `data/비품-총무.real.tsv`(저장소 밖) · `scripts/비품들여오기.py`.
     **`--회차 1` 을 꼭 붙인다** — 회차가 둘이라 안 붙이면 멈춘다


13) **README 가 전화번호 인증 시절 그대로** — **닫았다** (2026-09-11 · 로그인 판)
   - README 의 「처음 로그인하기」 와 화면 표, 환경변수 설명을 아이디·비밀번호로
     고쳤다. 옛말 검사가 「초대 링크」 를 잡으므로 다시 낡으면 빨개진다
   - 그 판의 셈과 잰 값은 `docs/review/2026-09-11-로그인판.md` 에 있다


14) **비품 시트 들여오는 도구** — 넣는 일은 17번 (2026-09-11)
   - 무엇: `scripts/비품들여오기.py` 로 TSV 를 읽어 비품 표에 넣는다. 머리글은 이름으로
     찾고, 합친 칸은 위 값을 물려받으며, 묶음 이름·위치·비고 세 자리는 **허락한 것만**
     넣는다(사람 이름과 전화가 섞여 있다). 규칙은 CLAUDE.md 4-18
   - 재료가 어디: `docs/review/2026-09-11-비품시트.md`. 재료 파일은
     `data/비품-*.real.tsv` 로 **저장소 밖**이다(`.gitignore` 의 `data/*.real.*`)
   - 미정: 없음 — **도구만** 끝났다. 코람데오는 들어갔고(회차 1) **총무 시트는 17번**,
     남은 탭은 15·16번

7) **회차별 비품 목록 — 표 둘 · 옮기기 · 화면** (2026-09-10)
   - 무엇: `EquipmentItem`(라이브러리 · 팀 키 · 묶음 · 이름) / `EquipmentRun`
     (회차별 · 수량 · 위치 · 체크 · `included`). `scripts/비품옮기기.py` 로 옛
     체크리스트 다섯을 옮기고(`moved_at`), `/equipment` 화면과 사이드바 항목.
     규칙은 CLAUDE.md 4-18. 도막 2 에서 묶음을 품목에서 회차(run)로 옮겼다 —
     `scripts/비품묶음옮기기.py`
   - 재료가 어디: `docs/review/2026-09-10-비품.md` · `docs/review/2026-09-10-비품2.md`.
     도막 1 의 옮기기(`비품옮기기.py --실행`)는 **2026-09-10 17:27 에 운영에서 돌았다**
     (사본 `app-20260910-172758.db` · 체크리스트 5 → 품목 30 · run 30). 도막 2 의
     묶음옮기기도 **끝났다** — 2026-09-11 에 운영을 읽어 보니 `equipment_items` 에
     묶음 칸이 없다(옛 모양이면 부팅이 말을 한다 · `app/db.py`). 순서는 4-18 의
     그 사슬이다
   - 미정: 없음 — 시트 들여오기는 14번

4) **앱 UI 전체를 눈으로 보기** — UI 정리 판 1·2 (2026-09-10)
   - 무엇: 홈·회의록·설정·알림·보드·목록·달력·체크리스트·예산·봉사팀을 봤고,
     고칠 것은 판 1(예산·봉사팀·소속 줄·/draft)과 판 2(회의록 폼 접기·끝난 회차의
     알림·배지=화면)에서 처리했다
   - 재료가 어디: 잰 값은 `docs/review/2026-09-10-UI정리판1.md` ·
     `docs/review/2026-09-10-UI정리판2.md`
   - 미정: 없음 — 다음은 실제로 써 본 사람이 어디서 막히는가(12장)

5) **예산 화면** — UI 정리 판 1 (2026-09-10)
   - 무엇: 구분 제목 줄(이름·세부항목 수·소계·접기) · 항목은 첫 세부항목의
     라벨 · 단가·명수·횟수 칸은 늘 보이고 없으면 「–」 · 수입이 없으면 잔액은
     「수입 미입력」. 지출 화면(`expenses.html`)은 이번 판에 안 봤다
   - 재료가 어디: `templates/budget.html` · `static/js/budget.js` · CLAUDE.md 7-3.
     엑셀 「예산 대비 집행」 은 안 바뀌었다(`tests/golden/`)
   - 미정: 지출 화면을 시트 꼴로 얼마나 따라갈지 — 4번의 다음 판

6) **봉사팀 보기 화면** — UI 정리 판 1 (2026-09-10)
   - 무엇: 화면 CSS 를 앱 토큰으로 다시 썼다(엑셀 흉내 걷음). 표를 만드는
     함수는 0-d(반 줄이 둘로 갈라지던 것) 한 곳만 고쳤고 그래서 파일의 병합
     둘이 함께 바뀌었다 — 그 판 보고에
   - 재료가 어디: `app/domain/staff_sheet.py` · `templates/staff_sheet.html` ·
     CLAUDE.md 5-8 「그 색은 파일의 것입니다」
   - 미정: 없음

3) **한 사람이 여러 부서·역할** — 도막 4 (2026-09-10)
   - 무엇: `user_departments`(부서마다 리더/팀원) 로 옮기고 `users.department_id`
     를 지웠다. `users.role` 은 `admin · general · viewer`. 소속을 읽고 쓰는
     곳은 `app/domain/permissions.py` 하나다. 사람이 정한 것 다섯(admin 도
     부서 줄 · 소속 없는 일반은 열람 전용 · /draft 는 리더인 부서 첫째 ·
     드롭다운 기본은 내 부서 전부 · 리더 둘이면 둘 다 알림)은 CLAUDE.md 4-12
   - 재료가 어디: 도막 3 의 셈에 「고쳤다/안 고쳐도 된다」 를 단 점검표는
     `docs/review/2026-09-10-도막4.md`. 운영 DB 는 `scripts/부서옮기기.py --실행`
     으로 옮겼다 — 사본 파일명은 그 보고에
   - 미정: 없음. **되돌리려면** 서버를 끄고 `data/backups/` 의 사본을
     `data/app.db` 로 되돌린다(11-2)

2) **계정 정리** — 도막 2 (2026-09-09)
   - 무엇: 사람이 고른 계정(id 1) 하나만 남기고 나머지를 지웠다.
     `scripts/계정정리.py` — 기본은 미리보기, `--실행` 으로만 지운다.
     지우기 직전에 `VACUUM INTO` 사본을 뜨고, FK 가 없는
     `activity_logs.actor_id` 는 스크립트가 직접 비운다(`actor_name` 은 남긴다)
   - 재료가 어디: 지우기 전 셈과 지운 뒤 셈은 `docs/review/2026-09-09-도막2.md`.
     FK 표는 `docs/review/2026-09-09-도막1.md` 의 0-(e)
   - 미정: 없음. **되돌리려면** 서버를 끄고 `data/backups/` 의 사본을
     `data/app.db` 로 되돌린다(11-2) — 파일명은 도막 2 보고에

## 재 보고 안 만든 것

- **업무 여럿을 한 번에 다른 부서로 옮기는 자리는 화면에 없습니다**
  (2026-09-11 에 확인). 담당팀을 바꾸는 길은 **드로어 하나뿐**이고
  한 건씩입니다(`POST /board/task/{run_id}/department` · `drawer.js`).
  `run_ids` 를 여럿 받는 자리는 선행 잇기와 회의록 제안 반영뿐이라 부서와
  상관이 없습니다. 지금 목록의 할 일에 부서가 섞여 있어 한 번에 옮기고
  싶어지는데, **만들지 않았습니다** — 옮기면 담당자도 함께 비워야 하고
  (4-9) 그 판단이 건마다 다릅니다. 만들 때 그 줄을 먼저 봅니다

## 사람이 챙길 재료

- **개기자 관리자 주소를 설정 › 바깥 링크에 넣는다** (사람이 — 저장소에는 없다)
- **사역자 열 분의 실명과 아이디** (18번 — 저장소에는 안 적는다)
- **`docs/로그인-안내문.md` 의 비운 자리 넷** — 이름 · 로그인 주소 · 아이디 ·
  첫 비밀번호. 주소는 이 저장소가 공개라 여기 안 적는다 (11-2)
