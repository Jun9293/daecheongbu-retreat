# 대청부 수련회 시스템 — 미결 목록 (새 채팅이 먼저 읽는다)

갱신: 2026-09-27 **4단계 완전히 마무리 — 계좌 팝업을 눌러서도 연다**.
`exppop.js` 의 `click` 핸들러에 `.payer.acct` 갈래를 더했다 — **올림과 누름이 둘 다** 같은 `계좌팝` 을 부르고, 닫는 규칙은 긴 내용 · 영수증과 **한 벌** 그대로다. 손가락에는 호버가 없어 올림으로만 열면 **그 기기에서 계좌를 볼 길이 통째로 없었다**(7-4 는 그때도 「눌러서 열고」 라고 적고 있었다 — 글과 코드가 어긋난 자리였다).
**편집 중 막기는 계좌에 해당 없다** — 막기의 축은 「저장이 일어날 수 있나」 이고 **계좌 팝업에는 저장도 화면 다시 읽기도 없다.** 그래서 `누른줄` 에 계좌를 안 넣고, `test74_h06` 이 **넣지 않았는지**를 잰다.
**좁은 폭 375px 창에서 실제로 눌러 열었다** — 지출 26 항목 · 빨강 0 · 완주. 자가시험은 **잡음 9 · 놓침 0 · 못심음 6** 이고 새 갈래 ⑮(누름으로는 안 열린다)도 잡혔다.

**4단계는 이것으로 닫힌다.** 남은 셋은 **4단계를 막지 않는다** — 달력의 터치 두 단계(사람이 손가락 기기로 볼 자리) · 자료가 없어 못 심는 갈래 둘 · 부서 리더 계정의 좁은 폭(창이 하나뿐이라 못 돌렸다 · 까닭과 근거는 봐둘것 **BJ-i**).

**남은 단계는 5단계(수입 — 입금 캡처 자동 분류가 걸린 별도 판)다.**

**「남은 것」 표에는 다음 판이 이어받아야 할 것만 올린다** (2026-09-24 사람이 정함) —
못 한 것 · 사람 손이 필요한 것 · 정할 것. **봐 둘 것 후보는 `docs/봐둘것.md` 에만**
적는다: 둘을 섞으면 이 표가 길어져 정작 이어받을 것이 묻힌다.

> **남은 것 — 한 자리에 모음**
>
> | 무엇 | 누가 · 언제 | 어디 |
> |---|---|---|
> | **5단계 — 수입 화면** (입금 캡처 자동 분류는 기술 선택이 걸린 별도 판) | 다음 판 | — |
> | **달력의 터치 두 단계** — 두 점검 파일 어디에서도 못 잰다(넓은 폭은 호버가 있고, 좁은 폭은 주 단위 목록이라 칠함이 없다) | 사람 · 손가락 달린 기기에서 | 봐둘것 **BJ-i** |
> | **지연 칩 `!`(달력 ㉚) · 영수증 없는 줄(재정 ⑬)** — 이 회차에 그런 줄이 없어 고장을 못 심는다. 번호는 **화면 이름과 함께** 적는다(같은 기호가 보드에도 있다) | 자료가 생기는 판 | 봐둘것 **BJ-i** |
> | **`내프로세스.py --멈춤` 이 자기를 부른 셸까지 멈춘다** — 11-3 이 「빼야 한다」 고 적어 둔 자리인데 그대로 났다. 그때까지는 PID 를 직접 끝낸다 | 다음 판 | 봐둘것 BI-l |
> | 16번 구매목록 | 재정과 별개 판 | `docs/인계-지난.md` 의 「할 것」 16 · 순서표 8 |
> | 지난 회차 예산 가져오기 | 다음 회차를 열 때 · 사람이 정함 | 순서표 7 · 봐둘것 BA-c |

지난 기록은 **`docs/인계-지난.md`** 에 있다. **두 파일을 어떻게 나누는지는
CLAUDE.md 11-3 이 정본이고 여기 옮겨 적지 않는다** — 같은 규약이 두 곳에 있으면
갈린다(2026-09-24 검토가 잡음).
