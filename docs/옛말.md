# 옛말 — 바꾼 말과 그 낡은 표기들

**설계를 바꾸면 그 말을 적어 둔 자리도 함께 바꿔야 합니다** (11-3 3단계).
한 곳을 고치고 다른 곳을 안 고치면 다음 사람이 **옛 기준에서 시작합니다.**

지금까지는 사람이 그때그때 낱말을 골라 grep 했는데, **낱말을 고르는 그
자리가 새는 곳**이었습니다 — 2026-09-08 에 「업무 이름을 누르면」 하나로
찾아 네 곳(CSS 머리 주석 · drawer.js · 점검 스크립트 · 시험 이름)을
놓쳤고, 보고에는 「0곳」 이 적혔습니다. **검사를 했다는 기록이 있는데
검사가 못 본 것이라 안 한 것보다 나쁩니다.**

그래서 낡은 말마다 **표기를 여럿** 적어 두고 `scripts/check_stale.py` 가
그 전부를 찾습니다.

## 무엇을 적는가 — **낱말이 아니라 「낡은 주장」**

`작업 파일`·`삭제 단추` 같은 **주제어를 적으면 안 됩니다.** 그 말은 지금
규칙을 설명할 때도 나옵니다(「작업 파일은 사이드바에 없다」 ·
「삭제 단추는 없습니다」) — 그러면 검사가 맞는 문장을 붙잡고, 넘김 목록만
늘다가 아무도 안 읽게 됩니다(4-11 의 그 자리).

**옛 주장일 때만 나오는 말**을 적습니다. 「이름을 누르면 펼쳐」 는 지금
규칙에서는 쓸 수 없는 말이지만, 「이름」 은 얼마든지 쓸 수 있습니다.

## 적는 법

    ## <지금 말>
    - 바뀐 때: <YYYY-MM-DD> · <어디에 정해져 있나>
    - 낡은 표기: `표기1` · `표기2` · `표기3`
    - 왜: <한 줄>

**넘기는 자리**는 `docs/옛말-넘김.txt` 에 `파일 | 왜 넘기는가` 로 적습니다
(이 파일 자신과 검사·시험처럼 **그 말을 설명하려고 담는 자리**).
10장이 「어떤 것을 금지하는 도구는 그것을 설명하려고 스스로 담게 된다」 고
적어 둔 그 자리입니다.

---

## 행 아무 데나 눌러 열기 (목록)

- 바뀐 때: 2026-09-08 · CLAUDE.md 4-14
- 낡은 표기: `이름을 누르면 펼쳐` · `이름 클릭이 여는` · `이름 클릭으로 열` ·
  `여는 자리(이름·캐럿)` · `이름을_눌러` · `이름과 캐럿이 그 일을` ·
  `'.nm, .caretc'` · `이름 클릭 ·`
- 왜: 이름만 여는 자리였을 때는 행의 대부분이 죽은 면이었다. 지금은
  행 전체가 열리고 예외는 「안 여는 자리」 목록뿐이다

## 총무팀 일정 (수련회 진행 화면)

- 바뀐 때: 2026-09-07 · CLAUDE.md 5장 · `domain/live.SCREEN_TITLE`
- 낡은 표기: `진행 화면의 이름`
- 왜: 사이드바 라벨과 화면 제목이 한 곳에서 나가야 한다

## ?task= (펼친 업무를 주소에 남기기)

- 바뀐 때: 2026-09 · CLAUDE.md 4-13 · 4-14
- 낡은 표기: `?open=` · `open=<run_id>` ·
  `/board?task= 로 보드의 상세` · `알림의 바로가기는 /board?task=`
- 왜: 보드·달력·목록이 같은 이름을 쓴다 — 알림 바로가기가 어디로 와도
  같은 것이 열린다. **알림은 `/tasks?task=` 로 간다**(4-11 · 4-16):
  휴대폰에서 보드는 무겁다. (4-13 이 이력으로 `/board?task=` 를 적는
  자리가 있어 표기를 그 주장이 실릴 때만 걸리게 좁혔다)

## 첨부 탭 (업무의 자료)

- 바뀐 때: 2026-09 · CLAUDE.md 4-9
- 낡은 표기: `작업 파일 탭` · `사이드바의 작업 파일` · `파일 첨부 버튼` ·
  `/files 화면으로`
- 왜: 사이드바의 「작업 파일」 을 없애고 드로어의 첨부 탭이 그 자리다.
  (`/board/task/{id}/files` 는 **지금 쓰는 주소**라 옛말이 아니다)

## 수련회 준비 (목록·보드·달력)

- 바뀐 때: 2026-09 · CLAUDE.md 3장
- 낡은 표기: `준비 단계 탭` · `준비 단계」 화면` · `준비 단계 보드` ·
  `준비 단계 ·` · `준비 단계 데이터`
- 왜: 「준비 단계」 라는 이름을 쓰지 않는다 — 같은 TaskRun 을 세 축으로
  보는 화면이다.

  > **표기 둘로는 모자랐다.** 2026-09-08 에 보드를 브라우저로 열었더니
  > **탭 제목이 「준비 단계 · 2026 여름수련회 Belong」** 이었다 —
  > 검사가 초록인 채로 **사용자가 보는 이름**이 옛 이름이었다.
  > 함께 남아 있던 곳: `board.html`(탭 제목) · `domain/board.py` ·
  > `routers/board.py` · `models.py` · `retreat.css` · `board.js` ·
  > `seed.py` · `test_board_web.py` · `docs/현황-브리핑.md` ·
  > CLAUDE.md 13장. **개수는 적지 않는다** — 세어 두면 하나 더 나올
  > 때마다 그 숫자도 고쳐야 하고, 안 고치면 목록과 숫자가 어긋난다
  > (10장). 실제로 처음에 수를 적었는데 나열한 것보다 적었다.
  > 낱말 하나(`준비 단계`)로 넓히지 않는 이유는 CLAUDE.md 3장이 「이
  > 이름을 쓰지 않는다」 고 말하려면 그 낱말을 담아야 하기 때문이다
  > (10장) — 대신 **그 뒤에 오는 말**을 함께 적는다

## 폐회일 (회차가 끝나는 날)

- 바뀐 때: 2026-09 · CLAUDE.md 4-10 · 5-1
- 낡은 표기: `개회일 다음 날` · `개회일+1 이면 종료`
- 왜: 종료 판정은 `period.is_over` 하나이고 폐회일을 본다

## 만드는 곳은 create_run 하나 (일회성 업무)

- 바뀐 때: 2026-09-08 · CLAUDE.md 14장
- 낡은 표기: `/board/add/new)와 같은 모양` · `같은 모양(라이브러리 + run`
- 왜: 베낀 모양이 아니라 **같은 함수**다

## 쪽지 읽기는 한 곳 (`방식|말`)

- 바뀐 때: 2026-09-08 · CLAUDE.md 14장 · `domain/suggest.쪽지읽기`
- 낡은 표기: `suggest_note or "").partition` · `startswith("낱말|")`
- 왜: 라우터가 자리마다 쪼개던 것을 한 규칙으로 모았다.
  (`쪽지읽기` 안의 `partition` 은 그 규칙 자신이라 옛말이 아니다)

## 하한이 셋 · 보조급자리 (글자 눈금)

- 바뀐 때: 2026-09-08 · CLAUDE.md 4-0 · `tests/test_typescale.py`
- 낡은 표기: `하한이 둘입니다` · `장식이어도_되는곳` ·
  `장식 단에 두는 예외` · `정렬 토글」(4-14) 은 장식` ·
  `배지가 쓰는 장식 단(--fz-xs)`
- 왜: 정렬 토글은 12px 예외가 아니라 **보조 급(13px)** 이다. 예외를
  파지 않고 급을 하나 두었으므로 하한이 셋이다

## 글 검사는 한 자리에서 (`scripts/글검사.py`)

- 바뀐 때: 2026-09-08 · CLAUDE.md 11-3 1단계 · 14장
- 낡은 표기: `를 돌리고 결과를 보고에 적습니다` · `도 돌립니다 — 축이 둘이어야` ·
  `셋을 다 돌린다` · `check_names · check_stale · check_named 를 돌린다` ·
  `scripts/검사.py` · `검사.py 를` · `검사.py 의`
- 왜: 검사 이름을 문서에 세어 두면 하나 늘 때 그 검사만 조용히 안 돈다.
  더할 곳은 `검사들` 한 줄이다.

  > **이름이 곧 경계입니다.** 처음에는 `검사.py` 였고 「글 검사만
  > 돈다」 를 독스트링에만 적었는데, **문구는 사람이 안 읽고 이름은
  > 부를 때마다 읽습니다** — 끝의 「전부 통과했습니다」 가 1단계의
  > 문이 다 닫힌 것으로 읽혔습니다(`git status` 훑기는 여전히 사람의
  > 일입니다). `글검사.py` 로 바꾸니 같은 줄이 「글이 통과했다」 로
  > 읽힙니다

## 넘김은 이유가 있어야 넘김이다

- 바뀐 때: 2026-09-08 · CLAUDE.md 11-3 3단계 · `scripts/넘김.py`
- 낡은 표기: `이유 칸이 비면 그 줄을 버린다` · `이유가 없으면 그냥 건너뛴다`
- 왜: 세 검사가 같은 모양을 저마다 읽으며 셋 다 이유 없는 줄을 조용히
  버렸다 — 적은 사람은 넘긴 줄 알고 검사는 안 넘긴 채로 돈다.
  읽는 곳은 `넘김.읽는다` 하나이고, 이유가 없으면 0 이 아닌 값으로 끝난다

## 기다리지 말고 본다 (점검 스크립트)

- 바뀐 때: 2026-09-08 · `docs/checks/drawer.js` · CLAUDE.md 11-3 2단계
- 낡은 표기: `await sleep(250)` · `await sleep(400)` · `await sleep(700)` ·
  `await sleep(900)` · `{wait: 900}` · `{wait: 120}` · `250ms 기다린`
- 왜: 시간을 어림으로 박으면 **검사가 흔들린다** — 세 판 연속 그랬다.
  이제 「메뉴가 닫힌 것을 보고 간다」 이고, 상한에 걸리면 ✗ 가 아니라
  **「못 쟀음」** 이다

## 뺀 것도 센다 (회의록 제안이 이미 만든 업무)

- 바뀐 때: 2026-09-08 · CLAUDE.md 12장 · `domain/tasks.made_from_meeting`
- 낡은 표기: `「지금 있는」 업무` · `은 세지 않습니다` ·
  `되살리는 길은 보드의` · `뺀 것은 세지 않` ·
  `라이브러리 항목이 하나 더`
- 왜: 안 세면 제안을 다시 눌러 **같은 제목의 라이브러리가 둘**이 되고
  6-2 의 실행 이력이 두 줄로 갈린다. 「가리킬 곳이 없다」 는 화면이
  푼다 — 「빼 둔 업무입니다」 와 되살리기

## 못 바꾸는 사람에게도 그 칸은 행을 연다 (목록 상태 칸)

- 바뀐 때: 2026-09-08 · CLAUDE.md 4-14 · `static/js/tasks.js` 의 `안여는곳`
- 낡은 표기: `그 칸은 아무 일도 하지 않습니다` ·
  `상태 칸은 「안 여는 자리」 라서` · `.cell.st, a, button` ·
  `cell.st{cursor:default`
- 왜: 예외는 「누르면 뭔가 되는 것」 이지 「누를 수 없는 사람에게도
  예외」 가 아니다. 넓게 두면 그 사람에게만 행 오른쪽 끝이 죽은 면이 된다

## 상태 배지에서 바로 바꾼다 (목록)

- 바뀐 때: 2026-09-08 · CLAUDE.md 4-14
- 낡은 표기: `배지는 표시일 뿐` · `상태 배지를 눌러도 안 열림` ·
  `목록 배지는 지금 아무 조작도`
- 왜: 목록 배지가 드로어의 상태 메뉴를 그 자리에서 연다 — 죽은 면이 아니다

## 넘김의 이유에는 그 말을 다시 쓰지 않는다

- 바뀐 때: 2026-09-09 · CLAUDE.md 11-3 3단계 · `scripts/넘김.py` 의 `인용했나`
- 낡은 표기: `이유 칸이 비어 있지 않으면 통과` ·
  `이유 없는 줄은 넘기지 **않는다.**` · `(이유 없는 줄은 넘김이 아닙니다)` ·
  `이유없음: list[tuple[int, str]]` · `이유 없는 줄을 사람 말로 낸다` ·
  `이유 없는 넘김은 넘김이 아니` · `이유 없는 줄:` · `이유 없는 줄`
- 왜: 이유가 넘기는 그 말을 인용하면 **그 줄이 다시 걸려 또 넘김이
  필요해진다.** 이제 안 되는 줄은 둘이다 — 이유가 비었거나, 이유가 그
  말을 다시 쓴 것

## 탭 수를 글로 박지 않는다 (드로어)

- 바뀐 때: 2026-09-09 · CLAUDE.md 4-9
- 낡은 표기: `다섯이 넘쳐` · `탭 다섯이` · `탭 다섯` · `다섯 탭`
- 왜: 탭은 여섯인데 문서와 점검이 다섯이라 말하고 있었다. 4-10 의
  「조건 넷」 과 같은 모양이고, 세는 말 검사는 `곳`·`군데`·`자리` 만
  보므로 이 모양을 못 잡는다 — 수를 지우면 하나 늘어도 안 틀린다

## 목업 목록은 표가 말한다

- 바뀐 때: 2026-09-09 · CLAUDE.md 0장 · 15장
- 낡은 표기: `HTML 4개`
- 왜: 폴더에는 다섯이 있었고 표에도 하나가 빠져 있었다. 수를 지우고
  표를 목록으로 삼는다

## ✗ 를 찍는 길은 하나다 (점검 스크립트)

- 바뀐 때: 2026-09-10 · `docs/checks/drawer.js` 의 `잰다` · `tests/test_stage20.py`
- 낡은 표기: `직접 판정 줄` · `errors.push(label` · `errors.push 가 붙지` ·
  `찍는 자리와 세는 자리가 다르면`
- 왜: 찍는 것과 세는 것을 따로 두면 손으로 맞춰야 하고, 실제로 셋이
  어긋나 ✗ 인데도 「통과」 였다. 이제 길이 하나뿐이라 어긋날 자리가 없다

## 「단」 은 쓰임의 급 (글자 눈금)

- 바뀐 때: 2026-09-10 · CLAUDE.md 4-0
- 낡은 표기: `--fz-* 토큰은 아홉` · `크기 단으로 읽으면`
- 왜: 표의 다섯 줄이 그 다섯이고 토큰은 그보다 많다. 크기 단으로 세면
  틀린 수가 된다 — 그 한 줄을 4-0 에 적어 두었다

## 자가시험은 세 화면 · 두 계정

- 바뀐 때: 2026-09-09 · CLAUDE.md 11-3 2단계 · `docs/checks/drawer.js` 의 `고장들`
- 낡은 표기: `자가시험도 두 계정으로` · `한 화면 한 계정이면 됩니다` ·
  `자가시험이 아직 보드에서만` · `보드 · 관리자 하나로 돌렸`
- 왜: 화면에만 있는 판정 줄(목록의 상태 칸 · 달력의 비침)은 그 화면에서
  돌려야 재진다. 한 화면으로만 돌리면 「잡았다」 고 적어 두게 되는데
  실은 거기서 재 본 적이 없다

## 점검이 끝까지 돌았는지를 출력이 말한다

- 바뀐 때: 2026-09-09 · `docs/checks/drawer.js` 의 `완주`
- 낡은 표기: `통과: errors.length === 0, 실패` · `재는 검사가 없습니다 —`
- 왜: 한복판에서 죽으면 그때까지 잰 것만 남아 「전부 통과」 로 보인다.
  이제 `완주` 가 붙고 죽은 자리가 ✗ 로도 남는다

## 건너뛰는 길도 심어 본다

- 바뀐 때: 2026-09-09 · `docs/checks/drawer.js` 의 `건너뜀나와야`
- 낡은 표기: `브라우저에서 심어 본 적은 없습니다` · `심어 보지 않는다`
- 왜: 죽어 있거나 없는 요소를 누르면 ✗ 가 아니라 **건너뜀**으로 세야
  한다 — 그것이 그쪽의 정답이고, 이제 심어서 잰다

## 「모두 읽음」 은 목록 위 한 줄

- 바뀐 때: 2026-09-09 · CLAUDE.md 4-16 · `app/templates/notifications.html`
- 낡은 표기: `모두 읽음 (` · `대기 중 요청만) / 모두 읽음`
- 왜: 칩 줄 끝에 두면 고르는 것들 사이에 섞인다. 세 자리로 쌓인 사람이
  먼저 할 일은 「어디부터 볼까」 가 아니라 한 번에 지우는 것이다

## 메뉴를 여는 자리는 pickcell (상태 칸 · 담당자 칸)

- 바뀐 때: 2026-09-09 · `app/static/js/drawer.js` 의 `originOf`
- 낡은 표기: `statchip: at(` · `의 statchip)` · `statchip 이 그 자리`
- **백틱을 두른 꼴은 이 목록으로 못 잡습니다** — 표기는 백틱 사이에서
  읽으므로(`check_stale.표기`) 표기 안에 백틱을 넣을 수가 없습니다.
  실제로 CLAUDE.md 14장의 「`originOf` 의 `statchip`」 한 줄이 그래서
  안 걸렸고 검토가 눈으로 잡았습니다. 이름 검사도 못 잡습니다 —
  `#statchip` 은 드로어에 실재하는 요소라 「없는 이름」 이 아닙니다
- 왜: 담당자 칸이 같은 메뉴를 쓰게 되면서 상태 칸 하나를 가리키던 이름이
  거짓이 됐다. `#statchip`(드로어의 그 단추)은 그대로 있다

## 엑셀은 편집자가 받고 계좌 칸만 총무팀 (7-3 · 5-8)

- 바뀐 때: 2026-09-09 · `app/routers/export.py` · `permissions.can_see_account`
- 낡은 표기: `엑셀은 admin 만` · `총무팀만 받습니다 (파일에 계좌` · `엑셀을 통째로 막` · `파일은 통째로 admin`
- 왜: 도막 1 이 파일을 통째로 막았더니 화면은 계좌만 빼고 누구나 보는데
  파일은 아무도 못 받아 같은 표인데 보는 사람이 갈렸다. 막을 것은 표가
  아니라 계좌라, 계좌 칸을 빼고 표는 편집자 이상이 받는다

## 소속은 user_departments 의 줄 여럿 (한 사람에 부서 하나가 아니다)

- 바뀐 때: 2026-09-10 · CLAUDE.md 4-12 「부서 여럿 · 역할 둘」 · `app/domain/permissions.py`
- 낡은 표기: `department_key_of` · `한 사람 한 부서` · `my_dept_key=` · `users.department_id 는`
- 왜: `users.role` 하나에 넷이 섞여 「총무팀이면서 헤브론 리더」 를 적을 칸이
  없었다. 전체 역할(`admin·general·viewer`)과 부서 역할(소속 줄의
  `lead·member`)로 갈랐고 `users.department_id` 칸은 지웠다. 「한 사람 →
  키 하나」 함수는 없고 「내 부서 키 집합」 만 있다

## 예산 표는 구분 제목 줄 + 세부항목 줄 (소계 행·항목 행이 따로 없다)

- 바뀐 때: 2026-09-10 · CLAUDE.md 7-3 · `app/templates/budget.html`
- 낡은 표기: `구분별 소계 행과 총계 행` · `구분 행과 항목 행` · `항목 행은 그보다 약하게` · `항목은 그보다 약한`
- 왜: 소계는 구분 제목 줄에 흡수됐고 항목은 첫 세부항목 줄의 라벨이 됐다.
  「구분 행·항목 행·소계 행」 셋이 따로 서던 표는 없다

## /draft 는 소속이면 채우고 제출한다 (리더만이 아니다)

- 바뀐 때: 2026-09-10 · CLAUDE.md 6-7 · `app/routers/drafts.py` 머리 · 봐둘것 AM-e
- 낡은 표기: `부서 리더는 자기 칸만`
- 왜: 도막 4 부터 저장·제출의 문이 소속(`my_dept_keys`)이었는데 문서와 파일
  머리는 「리더만」 이라고 적혀 있었다. 사람이 「팀원도 채운다」 로 정했다

## 알림 배지와 화면의 줄은 같은 수를 센다

- 바뀐 때: 2026-09-10 · CLAUDE.md 4-16 · `app/notifications.py` 의 `badge_unread_count`
- 낡은 표기: `사이드바 배지의 전 회차 수를 그대로` · `알림 화면은 그 전 것까지 전부 세어`
- 왜: 배지(들어온 뒤 온 시스템 알림 · 전 회차)와 화면의 줄(이 회차 전부)이
  다른 수를 말해 운영에서 36 과 380 이었다. 하나의 함수로 합쳤고 「모두 읽음」 이
  그보다 많이 지울 때는 그 수를 줄에 적는다

## 비품은 체크리스트가 아니라 비품 표에 있다

- 바뀐 때: 2026-09-10 · CLAUDE.md 4-18 · `app/routers/equipment.py`
- 낡은 표기: `비품·준비물 체크리스트` · `체크리스트는 비품·준비물 목록`
- 왜: 체크리스트에 있던 비품 다섯이 유일한 비품 자리였는데 회차를 넘는 층이 없었다.
  품목은 EquipmentItem(라이브러리), 수량·체크·위치는 EquipmentRun(회차별)로 옮겼고
  체크리스트는 업무에 딸린 준비물 자리로 남는다

## 비품의 묶음은 품목이 아니라 회차(run)에 있다

- 바뀐 때: 2026-09-10 (도막 2) · CLAUDE.md 4-18 · `scripts/비품묶음옮기기.py`
- 낡은 표기: `(team_key, group_name, name)` · `묶음이 다르면 같은 이름이 둘 설 수 있어 묶음까지` · `EquipmentItem.group_name`
- 왜: 묶음이 품목의 유니크에 들어 있어 같은 릴선이 묶음마다 딴 품목이 됐다. 품목은
  회차를 넘고 쓰이는 자리는 회차의 것이라 묶음을 run 으로 옮겼고, run 의 유니크는
  (회차, 품목, 묶음) 셋이다

## 회차를 안 주면 알아서 고른다 — **회차가 하나일 때만 맞던 말**

- 바뀐 때: 2026-09-11 · `scripts/비품들여오기.py` 의 `회차고르기`
- 낡은 표기: `안 주면 열려 있는 회차를 쓰고` · `회차 id (안 주면 열려 있는 회차)`
  (**「시작일이 늦은 것」 은 안 적는다** — 무엇을 고쳤는지 설명하는 글에 그대로
  나오는 말이라, 적으면 맞는 문장을 붙잡고 넘김 목록만 는다 — 이 파일 머리의 그 자리)
- 왜: 다음 회차를 미리 열어 두면 「시작일이 늦은 것」 이 곧 **아직 아무것도
  없는 회차**다. 2026-09-11 에 코람데오 28줄이 그리로 들어갔고 사람이 되돌렸다.
  열린 회차가 둘 이상이면 멈추고 id 와 이름을 늘어놓는다

## 찍는 번호가 늘 시트의 행이라는 주장

- 바뀐 때: 2026-09-11 (도막 4) · CLAUDE.md 4-18 · `scripts/비품들여오기.py` 의 `줄뽑기`
- 낡은 표기: `시트의 {시트행}행` · `보는 행 번호**입니다`
  (**주제어를 안 적는다** — 「시트의 행」·「사람이 보는 행 번호」 는 무엇을 왜
  고쳤는지 적을 때도 나오는 말이라, 적으면 맞는 문장을 붙잡고 넘김 목록만 는다.
  남긴 둘은 **옛 주장일 때만** 나오는 꼴이다 — 이 파일 머리의 그 자리)
- 왜: 그 보증은 **파일의 레코드 수와 시트의 행 수가 같을 때만** 참이고, 합친 칸이
  든 시트는 사람이 어떻게 내려받느냐에 따라 깨진다. 재고 있는 것은 파일의 줄이다.
  지금은 「파일의 N번째 줄」 로 말하고 **묶음과 그 안 차례**를 함께 찍는다

## 비품 비고는 품목이 아니라 회차에 있다

- 바뀐 때: 2026-09-11 (도막 4) · CLAUDE.md 4-18 · `scripts/비품비고옮기기.py`
- 낡은 표기: `적혀 있는 메모를 시트가 밀어내지 않습니다` · `이미 있는 품목의 메모는 시트가 밀어내지 않는다`
  (위와 같은 이유로 주제어는 안 적는다 — 그 칸 이름과 「비어 있을 때만」 은
  바뀐 내력을 적은 코드 주석·모델 독스트링·시험에 그대로 나온다)
- 왜: 비고는 그 회차 그 자리에서 쓰는 말인데 품목에 있어 회차를 넘어 따라다녔다.
  「비어 있을 때만」 이라는 조건 자체가 한 칸을 여러 회차가 나눠 쓰던 자국이었다.
  run 은 (회차·품목·묶음)마다 따로라 서로 밀어낼 일이 없다


## 로그인은 아이디와 비밀번호다 — 초대 링크가 아니다

- 바뀐 때: 2026-09-11 (로그인 판) · CLAUDE.md 1장 · 4-12 · `app/domain/login.py`
- 낡은 표기: `로그인은 초대 링크` · `초대 링크로 들어옵니다` · `링크를 열면 계정이 활성화` ·
  `유효기간 7일` · `invites.issue` · `invite_url`
  (**주제어도, 지운 파일 이름도, 옛 규칙을 설명할 때 나오는 말도 안 적는다** —
  「초대 링크」·「토큰」·「한 번 쓰면 만료」 는 무엇을 왜 걷었는지 적을 때 그대로
  나오고, 지운 스크립트의 이름은 그것이 없어졌다는 것을 재는 시험과 대신하는
  스크립트의 머리말에 나온다. 적으면 검사가 맞는 문장을 붙잡고 넘김 목록만 는다 —
  이 파일 머리의 그 자리. 없는 이름을 가리키는지는 둘째 축이 본다.
  **이 괄호 안에는 낱말을 백틱으로 감싸지 않는다** — 감싸면 그것까지 표기로 읽힌다)
- 왜: 휴대폰 홈 화면에 붙인 앱은 쿠키가 사파리와 따로라 사파리에서 연 링크로는
  그 앱 안에서 로그인되지 않고, 그 안에는 링크를 붙여 넣을 주소창도 없다.
  옛 주소는 404 로 두지 않고 로그인 화면으로 보내고, 만드는 길은 코드에서 지웠다.
  표와 행은 남는다(0장)

## 아이디는 이름의 로마자다 — 전화번호가 아니다

- 바뀐 때: 2026-09-11 (로그인 판) · CLAUDE.md 4-12 · `docs/봐둘것.md` AQ-a
- 낡은 표기: `전화번호를 아이디로 그대로` · `번호로 사람을 찾기 때문`
- 왜: 번호를 아이디로 두면 로그인 화면·활동 기록·DB 에 그 번호가 계속 남고,
  번호가 바뀌면 아이디까지 바꿔야 한다. 겹치면 안 되는 것은 아이디이므로
  유니크가 `login_id` 쪽으로 옮겨 갔다

## 운영 명령을 누가 돌리는지는 11-2 가 정한다 — 스크립트마다 적지 않는다

- 바뀐 때: 2026-09-11 (로그인 판 뒤 손질) · CLAUDE.md 11-2 의 그 절
- 낡은 표기: `사람이 돌리는 순서` · `사람이 돌립니다.` · `이고 사람이 돌린다` ·
  `사람이 돌린다(4-18)` · `로 사람이 합친다`
  (**주제어를 안 적는다** — 「사람이 돌린다」 자체는 지금 규칙에도 나온다:
  값이 오가는 것과 재어 본 적 없는 것은 여전히 사람 몫이다. 남긴 것들은
  **옛 주장일 때만** 나오는 꼴이다 — 스크립트 이름 옆에 붙어 「이건 사람
  몫」 이라고 못박던 자리들)
- 왜: 운영 DB 를 바꾸는 것을 전부 사람 몫으로 두었던 기준이 틀렸다. 같은
  노트북의 같은 스크립트이고 사본도 같이 뜬다. 기준은 11-2 한 곳에 있고
  4-18·11-3·코드 주석은 가리키기만 한다 — 자리마다 적으면 갈린다

## 급을 올린 자리가 하나가 아니다 — 둘이고 무게가 다르다

- 바뀐 때: 2026-09-12 (안내 글자 판) · CLAUDE.md 4-0 · `app/static/css/retreat.css`
- 낡은 표기: `이 한 줄만 .hint 보다 한 급 위` · `이 한 줄만 한 급 위입니다` · `올린 자리도 이름으로 적습니다 — 지금 하나입니다` ·
  `그 페이지의 나머지 .hint 는 장식 단 그대로`
- 왜: `.fld .pwwarn` 하나뿐이던 자리에 `.fld .hint.lone` 이 생겼다. 둘은 크기가
  같고 하는 일이 다르다 — 저쪽은 색과 굵기까지 올려 「이 문장이 대가를 감당한다」
  를 말하고, 이쪽은 4-0 의 하한(14px)에 맞출 뿐이다. 「하나」 라고 적힌 자리를
  고치고도 그것을 옮겨 적은 CSS 주석을 안 고쳐 커밋 전 검토가 잡았다

## 개회일이 바뀌면 갈리는 것이 셋이 아니다 — 넷이다

- 바뀐 때: 2026-09-12 (일하는 방식 판) · CLAUDE.md 6-4 · `library.어느쪽`
- 낡은 표기: `셋으로 갈립니다` · `다시 셀 것 · 함께 밀 것 · 날짜가 빈 채 남을 것` ·
  `세 수가 뜬다` · `저장 전에 보이는 세 수`
- 왜: 드로어에서 **마감일만 고쳐도** 같은 엔드포인트로 들어와 「손으로 정한
  자리」 가 된다(4-6). 그것이 「자리를 민 업무」 와 한 수에 들어가 **실제로 민
  것보다 큰 수**를 냈다 — 사람이 그 수를 보고 판단할 수 없다. `기간만` 갈래를
  갈라 넷이 됐다

## 터널은 작업 스케줄러에 없다 — 윈도우 서비스다

- 바뀐 때: 2026-09-12 (되살아나는지 판) · CLAUDE.md 11-2 · `docs/배포-안내.md` 8-3
- 낡은 표기: `앱·터널·정기 작업은 윈도우 **작업 스케줄러**가 띄웁니다` ·
  `앱·터널·정기 작업은 윈도우 작업 스케줄러가 띄웁니다`
- 왜: 터널은 `Cloudflared` 라는 윈도우 서비스로 돕니다(LocalSystem · 시작
  유형 자동). 작업 스케줄러만 열어 보면 **터널이 통째로 안 보이고**, 그것이
  곧 「밖에서 안 열리는데 안에서는 다 되는」 상태를 못 알아채는 자리입니다.
  배포-안내는 처음부터 서비스로 적어 두었고 11-2 만 낡아 있었습니다

## 좁은 화면에서 사이드바를 여는 길이 가장자리 호버뿐이 아니다

- 바뀐 때: 2026-09-12 (휴대폰 판) · CLAUDE.md 4-0 · `app/static/js/sidenav.js`
- 낡은 표기: `1280px 아래에서는 CSS 가 sidepin 을 무시하므로 화면 이동은 들춰 보기로` ·
  `가장자리에 마우스를 대면 잠깐 떠서 나옵니다 (본문은 그대로고 위에 겹칩니다)` ·
  `1280px 아래에서는 sidepin 이 붙어 있어도 접힘 + 가장자리 호버다` ·
  `왼쪽 가장자리에 마우스를 대도 잠깐 열립니다` · `여는 길은 둘이고 뜻이 다르다`
- 왜: 손가락에는 호버가 없다. 여는 길이 가장자리뿐이면 휴대폰에서는 여는
  길이 **아예 없고**, 상단 탭 줄이 없으므로(4-0) 이동 수단이 통째로
  사라진다. 이제 좁은 화면에서는 **토글이 겹쳐 연다**

## 토글 단추의 이름은 「메뉴 열기」 다

- 바뀐 때: 2026-09-13 (휴대폰 마무리 판) · CLAUDE.md 4-0 · `app/templates/retreat_base.html`
- 낡은 표기: `aria-label="사이드바 여닫기"` · `title="사이드바 여닫기` ·
  `aria-label="사이드바 고정"` · `title="사이드바 고정`
- 왜: 「사이드바」 는 그 자리에 있는 것의 이름이 아니라 **만드는 사람의 말**이고,
  앱 안에 「목록」 화면이 따로 있어 「목록 단추」 로 불러도 헷갈린다. 사람이
  2026-09-13 에 「메뉴 열기」 로 정했다. **표기를 속성에 붙여 적는다** — 이름만
  적으면 그 이름을 설명하는 보고와 얼린 판이 통째로 걸려 넘김 목록만 늘어난다
  (10장 「금지하는 도구가 그것을 설명하려고 스스로 담는다」)

## 시험의 DB 보호는 지우는 자리의 검사다 — 순서가 아니다

- 바뀐 때: 2026-09-13 (사고 수습 판) · `tests/conftest.py` 의 `_시험DB가_아니면_멈춘다` · 봐둘것 AZ-a
- 낡은 표기: `테스트가 건드리지 않도록)` · `임시 경로로 잡아 두었으므로`
- 왜: 환경변수를 맨 위에서 바꿔 두는 것은 **conftest 가 app 보다 먼저 읽힐 때만**
  듣는다. `-p` 플러그인이 그 순서를 앞질러 운영 표가 지워졌다. 이제 보호는
  `drop_all` 앞마다 도는 검사이고, 「환경변수를 잡아 두었으니 운영에 안 닿는다」
  는 주장은 틀린 말이다. **얼린 검토 보고 둘에도 같은 주장이 남아 있지만** 그때의
  기록이라 표기로 적지 않았다

## 의심 판은 성한 칸에 안 세고 따로 상한까지 남긴다 — 「안 지운다」 가 아니다

- 바뀐 때: 2026-09-13 (후속 판) · CLAUDE.md 11-2 · `scripts/backup.py` 의 `SUSPECT_KEEP` · `지키는판`
- 낡은 표기: `세지도 지우지도 않` · `개수·크기에 안 세고 지우지도 않습니다` ·
  `의심스러운 판은 개수에도 크기에도 안 세고`
- 왜: 사람이 의심 판에 상한을 두되 성한 판보다 먼저 지우라고 정했다. 「지우지도 않는다」
  가 남아 있으면 다음 사람이 의심 판이 한없이 쌓이는 줄 알거나, 상한을 버그로 읽는다.
  안 지우는 것은 `지키는판` 에 까닭과 함께 적힌 판뿐이다

## 새벽 백업 알림은 푸시로 안 나간다 · pytest 의 `-p` 에는 예외가 없다

- 바뀐 때: 2026-09-13 (셋째 판) · CLAUDE.md 11-2 · 11-3 · `app/notifications.py` 의 `notify(push=)` · `tests/conftest.py` 의 `pytest_configure`
- 낡은 표기: `**웹 푸시로도** 나가려 합니다` · `먼저 AZ-a 를 엽니다`
- 왜: 사람이 둘을 정했다 — 새벽 세 시의 백업 알림은 그 순간 사람이 할 일이 없어 푸시로
  안 보내고, pytest 앞의 `-p` 는 예외를 두지 않는다. 앞엣것이 남으면 다음 사람이 푸시가
  안 가는 것을 고장으로 읽고, 뒤엣것이 남으면 「AZ-a 를 열면 끼워도 된다」 로 읽는다

## 앱 밖 자리는 폴더도 서명키도 안 만든다 · 이 창은 로그인 칸에 비밀번호를 안 넣는다

- 바뀐 때: 2026-09-13 (넷째 판) · CLAUDE.md 11-2 · `app/paths.py` · 봐둘것 AY-a · AZ-c
- 낡은 표기: `폴더를 만들고 서명키를 새로 씁니다`
- 왜: 사람이 둘을 정했다 — 사고 직후에 부르는 자리가 흔적을 남기면 원래 있던 것이 흐려지고, 버리는
  계정이라는 판단은 그때그때의 예외다. 앞엣것이 남으면 다음 사람이 폴더를 만드는 것을 괜찮은 줄 안다.
  뒤엣것은 옛 문장이 「못 했다」 로만 적혀 있어 표기로 잡을 낡은 주장이 없다(AY-a 에 닫음을 붙였다)

## 예산 항목 · 수입은 지우지 않고 취소 표시 · 재정 숫자를 세는 곳은 domain/budget.py 하나

- 바뀐 때: 2026-09-14 · CLAUDE.md 0장 · 4-15 · 7-3 · 8장 · `app/routers/budget.py` · `app/domain/budget.py`
- 낡은 표기: `삭제할 수 없습니다. 먼저 지출을 옮겨주세요` · `**지금은 두 곳입니다**` · `예산 항목을 삭제했습니다` · `수입을 삭제했습니다`
- 왜: 사람이 나-ㄴ 을 정했다 — 걸린 지출이 있어도 취소되고 행은 안 지운다. 옛 가드 문장이 남으면
  다음 사람이 「걸린 지출이 있으면 못 내린다」 로 읽고, 「지금은 두 곳」 이 남으면 모은 뒤에도 필터가
  두 벌인 줄 안다

## 영수증은 잇기 표로 지출과 잇는다 · 계좌는 셋 · 기본값을 단 칸은 값 채우기

- 바뀐 때: 2026-09-14 (재정 차례 4) · CLAUDE.md 7-4 · 8장 · 11-2 · 봐둘것 BA-h · BB-a
- 낡은 표기: `지출 1건에 N개` · `1 지출 : N 영수증` · `지출자 계좌 <input` · `이 창이 둔 임시 규칙`
- 왜: 사람이 시트-가 ㄷ · 시트-라 ㄱ · BA-h 답을 정했다. 옛 말이 남으면 다음 사람이 영수증 한 장에
  지출 하나만 걸리는 줄 알고 새 번호를 또 매기며, 임시 규칙이라는 말이 남으면 NULL 규칙을 걷어도 되는 줄 안다

## 들여오기는 시트 결산 식으로 예산 항목을 잇는다 · 글자는 확인용

- 바뀐 때: 2026-09-15 (짝잇기 운영 실행 판) · CLAUDE.md 7-5 · 봐둘것 BB-i · `scripts/재정들여오기.py`
- 낡은 표기: `처음부터 결산 식으로 이을지는 사람이 정합니다` · `는 그대로 둡니다** — 글자로 이은 줄은 결산 식과` · `글자 그대로 같고 그런` · `탭을 주면 범위의 두 끝에 모두 탭 이름이 있어야 한다`
- 왜: 사람이 BB-i 를 정했다. 옛 말이 남으면 다음 사람이 2027 시트를 글자로 들이고 못 이은 줄을 짝잇기로 또 채우며,
  파서가 탭 이름이 한 번 붙은 범위를 모르는 줄 안다
## 총무님이 표시하는 파일은 엑셀이다 — md 는 표의 정본이고 표시 자리가 아니다

- 바뀐 때: 2026-09-16 (확인용 파일 판) · CLAUDE.md 13장 · 14장 표 · `scripts/업무대조확인표.py`
- 낡은 표기: `에 표시하면 다음 판이` · `에 표시(짝 확인` · `확인용.real.md 에서`
- 왜: 총무님이 여는 것은 엑셀이고 거기서만 고른다. 옛 말이 남으면 다음 사람이 md 만 읽어
  「표시가 하나도 없다」 고 본다

## 총무님 표시를 받아 다음 판이 노션 짝을 잇는다 · 확인용 엑셀이 다음 판의 입력이다
- 바뀐 때: 2026-09-16 (노션 업무 들이기 판) · CLAUDE.md 13장 · 14장 표 · 봐둘것 BC-c · `scripts/노션업무들이기.py`
- 낡은 표기: `실제로 잇는 판` · `표시한 것을 받아` · `다음 판이 엑셀을 읽어`
- 왜: 사람이 짝짓기를 접고 노션을 그대로 옮기는 쪽으로 정했다. 이 말이 남으면 다음 사람이 총무님 표시를
  기다리거나 확인용 엑셀을 입력으로 삼아, 이미 안 쓰기로 한 길을 다시 걷는다

## 무채색은 차갑다 · 팀색이 면에도 온다 · 숫자를 고정폭으로 줄 세우지 않는다 · 하한은 단이다

- 바뀐 때: 2026-09-21 (톤 판) · CLAUDE.md 4-0 · 4-3 · 4-13 · 9장 · 14장 · `tests/test_stage63.py`
- 낡은 표기: `면을 채우지 않는다` · `면을 채우지 않고` · `면을 채우지 않` · `점과 왼쪽 마개에만` · `왼쪽 점과 바 테두리에만` · `팀 색 20% 농도 채움` · `var(--mono)` · `--card-r` · `보조는 13px` · `장식은 **12px**` · `하한이 둘`
- 왜: 사람이 2026-09-21 에 팀색을 면에 쓰기로 정했고 색온도 · 눈금 · 모서리를 함께 옮겼다.
  옛 말이 남으면 다음 사람이 바를 다시 흰 바탕으로 되돌리거나, 숫자를 고정폭으로 줄 세우거나,
  눈금을 올린 뒤에도 px 로 적힌 하한을 그대로 믿는다


## 본문은 흰 종이다 · 구역은 상자가 아니라 선으로 나눈다

- 바뀐 때: 2026-09-21 (흰 바탕 판) · CLAUDE.md 4-0 · `app/static/css/retreat.css` · `tests/test_stage63.py`
- 낡은 표기: `위에 흰 카드` · `회색 위에 흰 카드` · `var(--bg)` · `--bg:` · `면(--bg)` · `카드마다 테두리` · `카드 배경`
- 왜: 사람이 본문을 순백으로 정했고 구역의 네 면 상자를 걷었다. 회색 토큰은 지웠다.
  옛 말이 남으면 다음 사람이 없는 토큰을 본문색으로 읽거나, 구역에 상자를 다시 두른다

## 보드에 가로선을 둔다 · 달력은 다 보인다 · 상태는 세 칸 버튼 · 드로어는 밀고 들어온다

- 바뀐 때: 2026-09-23 (목업 확정분 판 · 1단계) · CLAUDE.md 4-0 · 4-1 · 4-9 · 4-13 · 6-7 · 9장
- 낡은 표기: `가로 격자선은 넣지 않습니다` · `세로 격자선만 남깁니다` · `3건을 넘으면` · `건으로 접습니다` · `상태 칩을 누르면` · `날짜를 누르면 달력이 열립니다` · `사이드바(240px)`
- 왜: 사람이 2026-09-23 목업으로 정했다. 옛 말이 남으면 다음 사람이 보드의 가로선을
  다시 걷거나, 달력을 세 건에서 접거나, 상태를 메뉴로 되돌린다.
  **화면 쪽 말(맞춤 단추 · 달력 코드의 접기 주석)은 그 코드를 고치는
  단계에서 더한다** — 아직 안 고친 코드를 지금 목록에 넣으면 검사가 「고쳐야 할 것」 과
  「아직 그 단계가 아닌 것」 을 못 가른다

## 드로어는 겹치지 않고 밀고 들어온다 · 날짜는 달력 팝업에서 함께 고른다

- 바뀐 때: 2026-09-23 (목업 확정분 1단계) · CLAUDE.md 4-9 · `app/static/js/drawer.js` ·
  `app/static/js/datepick.js` · `app/static/css/retreat.css`
- 낡은 표기: `오른쪽에 겹쳐` · `상태 칩을 누르면 바로 변경` · `날짜 칸을 누르면` ·
  `기간 칸이` · `시작일 칸` · `기간 칸도`
- 왜: 1단계가 실제로 고친 자리다. 드로어는 본문을 밀고 들어오고(폭이 준다), 상태는
  세 칸 버튼이라 메뉴를 안 열며, 날짜는 달력 팝업에서 둘을 함께 골라 「저장」 을 눌러야
  반영된다. 옛 말이 남으면 다음 사람이 패널을 다시 겹치게 두거나, 없는 `#statchip` 을
  선택자로 쓰거나, 칸 하나씩 즉시 저장하던 때의 문제를 다시 들인다

> **낱말이 아니라 「낡은 주장」 을 적는다** (11-3 3단계). 옛 칩의 *이름* 을 그대로
> 목록에 넣어 봤더니, 그 이름이 왜 바뀌었는지 설명하는 주석 넷과 그 이름이 없는지
> 재는 시험 셋이 함께 걸렸다 — 전부 **맞는 글**이다. 이름이 코드에 남았는지는
> `test_stage64` 가 그 자리를 직접 재고, 여기서는 **동작을 말하는 문장**만 잡는다.
> 「위에 겹쳐 뜬다」 도 뺐다: 사이드바는 지금도 겹쳐 뜬다(4-0) — 밀고 들어오는
> 것은 드로어뿐이라 드로어를 가리키는 말만 남긴다.

## 관련팀 알림은 저장마다 다시 간다 · 열람 전용은 안 받는다

- 바뀐 때: 2026-09-24 (목업 확정분 1단계 뒤처리 판) · CLAUDE.md 4-9 ·
  `app/routers/board.py` · `app/static/js/drawer.js`
- 낡은 표기: `영영 한 번` · `영구히 한 번` · `두 번 안 가` ·
  `소속 전원에게` · `그 부서 소속 전원`
- **어간까지 넓혔다** — 처음에 `두 번 안 가는 것은` 이라고 끝까지 적었더니
  `두 번 안 간다` 가 안 걸려 **두 자리가 그대로 남았다**(기준 문서 14장 ·
  인계). 11-3 이 「낱말을 고르는 그 자리가 새는 곳」 이라고 적어 둔 그
  모양이라, 끝을 자르고 어간으로 적는다
- **`안 가는데` 는 넣었다가 뺐다** — `drawer.js` 의 낡은 주석 하나가 그
  말이었는데, **지금 규칙을 말하는 문장도 같은 말을 쓴다**(「같은 저장이
  겹치면 알림은 안 가는데」). 주제어라 검사가 맞는 문장을 붙잡는다 —
  그 주석은 검사가 아니라 **커밋 전 검토가** 잡았고, 그것이 이 목록이
  못 보는 자리의 모양이다
- **`한 번만 간다` 류는 일부러 뺐다** — 그것은 지금 규칙을 말하는 문장이기도
  하다(같은 저장을 두 번 누르면 그렇다). 주제어를 넣으면 검사가 맞는 문장을
  붙잡고 넘김 목록만 는다(이 파일 머리말)
- 왜 낡았나: `dedupe_key` 가 (업무, 팀) 둘이던 시절의 말이다. 셋째 자리에
  **저장 식별값**이 들어가면서, 사람이 다시 저장하고 다시 체크하면 **다시 간다** —
  막는 것은 같은 저장을 두 번 누르거나 재시도한 경우뿐이다. 받는 사람도
  「소속 전원」 이 아니라 **열람 전용을 뺀 사람들**이다. 옛 말을 그대로 두면
  다음 사람이 「왜 또 갔지」 를 고장으로 읽고 되돌린다

## 자가시험 표에 「안 심음」 칸이 없다 · 깨끗한 판을 맨 처음과 맨 끝에 잰다

- 바뀐 때: 2026-09-24 (1단계 브라우저 점검 판) · CLAUDE.md 11-3 2단계 ·
  `docs/checks/drawer.js`
- 낡은 표기: `안 심음 통과` · `안심음 칸` · `안 심음 칸` · `두 칸을 적는다` ·
  `안 심음 / 심음` · `둘 다 맞을 때만 잡은`
- 왜 낡았나: 그 칸은 **갈래마다 다시 재는 것이 아니라 맨 위 깨끗한 판을
  옮겨 적은 것**이라 늘 같은 값이었다 — 두 칸처럼 보이지만 잰 것은 하나다
  (앞 판 커밋 전 검토가 짚었고 사람이 걷기로 정했다). 안 심었을 때
  깨끗한지는 이제 **맨 처음과 맨 끝의 깨끗한 판**이 말하고, 맨 끝이 빨가면
  **그 판의 판정 전체가 무효**다
- **`두 칸` 만으로는 안 적는다** — 드로어 머리의 「두 칸 정의 표」(4-9)가
  같은 말이라 맞는 문장을 붙잡는다. 이 파일 머리말이 말하는 그 자리라
  `두 칸을 적는다` 로 좁혔다
