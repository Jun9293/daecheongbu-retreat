사람이 할 것
노트북에서 직접: 사역자 열 분을 설정 › 사용자에서 만들기 (인계 18번 · 5장)
관리자 권한: 없음 — 이 판은 서버가 읽는 것을 안 고쳤습니다 (6장)
정할 것: `.hint` 에서 올릴 것 — 훑은 목록이 3장에 · 2026 을 보관할지 (9장)
막혀서 못 한 것: 없음

작성 시각: 2026-09-12 05:10 (KST)
커밋: 3b108aa (시험 26c4905 · **문서 3b108aa** — 그 위에 이 보고와 얼린 판)
작업: 사람이 고친 것 확인 · 검토가 올린 셋 · 보관 자리 · 점검이 보는 회차

---

# 0. 한 줄 요약

**사람이 고친 것이 그대로 들어갔습니다.** 2027 개회일이 2027-08-20~22 이고,
업무 100건이 **D-주차를 지킨 채 함께 움직였습니다** — 어긋난 것 **0**.
회차는 여전히 둘입니다. **읽기만 했습니다.**

그 길을 합성 자료로 재다가 **두 가지를 새로 알았습니다** — 손으로 끌어 옮긴
날짜는 개회일이 바뀌면 되돌아가고(AW-b), D-주차가 빈 채 켜진 업무는 없던
날짜를 얻습니다(AW-c). 둘 다 **고치지 않고 봐둘것에** 적었습니다.

검토가 올린 셋에 답했습니다 — `.hint` 를 한 줄씩 갈라 **올릴 값이 있는 넷**을
목록으로 냈고(고치지 않았습니다), `test_ts_02c` 의 범위를 **설정 페이지
전체**로 넓혔고, 이 보고를 스스로 훑었습니다.

---

# 1. 사람이 고친 것을 읽어서 확인했습니다 (지시 1 · 수용 기준 다)

**읽기만 했습니다** — `sqlite3` 를 `mode=ro` 로 열었고 앱을 안 띄웠습니다.

## 1-1. 날짜 (지시 1-a)

| 물은 것 | 답 |
|---|---|
| 2027(id 2)의 개회일·폐회일 | **2027-08-20 ~ 2027-08-22** |
| 사람이 고치기로 한 값 | 2027-08-20 ~ 2027-08-22 |
| 같은가 | **예** |

활동 기록에 `회차_수정 · retreat 2` 가 **2026-09-11 15:45** 에 한 줄
있습니다. 그 뒤로 회차를 건드린 기록은 없습니다.

## 1-2. 업무가 함께 움직였나 (지시 1-b · 수용 기준 가)

D-1주차는 개회일 직전 일요일이므로 **2027-08-15**(개회일이 금요일)입니다.
각 업무의 시작일이 자기 D-주차의 그 주(일~토) 안에 있는지 셌습니다.

| | 2027 (id 2) | 2026 (id 1 · 견줌) |
|---|---|---|
| 업무 run | 106 | 102 |
| **D-주차가 지켜진 것** | **100** | 96 |
| **어긋난 것** | **0** | 0 |
| 시작일이 비어 있는 것 | 6 | 6 |

**안 옮겨진 것은 여섯이고 전부 「뺀 업무」 입니다** — `included=0` 이고
분류는 모두 `main` 입니다. 켠 100건 중에는 날짜가 빈 것이 **하나도**
없습니다. 2026 도 같은 모양(96 켜짐 / 6 뺌)이라 **이 회차만의 사정이
아닙니다.**

> **셈만 적습니다** — 지시대로 업무 제목은 안 적습니다.

`library.reschedule` 이 `if not run.included: continue` 로 뺀 것을 건너뜁니다.
그래서 「안 옮겨진 여섯」 은 고장이 아니라 **그 종류의 성질**입니다.

## 1-3. 회차 수 (지시 1-c)

**둘입니다.** 1 · 2026 여름수련회 Belong, 2 · 2027 여름수련회.
셋이 아니므로 멈추지 않았습니다.

## 1-4. 같은 길을 합성 자료로 재다가 새로 안 것 둘

**`test37_a01` 을 쓰면서 드러났습니다.** 둘 다 운영에서는 지금 안 밟히지만,
밟히는 조건이 분명해서 적어 둡니다.

| | 무엇 | 지금 새나 |
|---|---|---|
| **AW-b** | `reschedule` 은 run 의 옛 날짜를 **미는 것이 아니라 라이브러리에서 다시 셉니다.** 보드에서 끌어 옮긴 날짜(4-6)는 개회일이 바뀌면 **기본 자리로 돌아갑니다** | 끈 것을 따로 남기지 않아 **알 수 없습니다** — 그것이 이 항목의 뿌리입니다 |
| **AW-c** | D-주차가 빈 채 **켜져 있는** 업무는 `d_week or FIRST_D_WEEK` 로 떨어져 **없던 날짜를 얻습니다.** 4-13 의 「날짜 없는 업무」 자리가 조용히 빕니다 | **안 밟힙니다** — 날짜 없는 여섯이 전부 뺀 것이라 위의 `continue` 로 걸러집니다 |

`test37_a01` 이 셋으로 갈라 셉니다 — **옮겨진 3 · 안 건드린 1 · 없던 날짜를
얻은 1.** 고치지 않은 것이지 못 본 것이 아니라는 표시입니다.

`test37_a02` 는 반대쪽입니다 — **이름만 바꾸면 업무가 한 건도 안 움직입니다.**
둘이 있어야 「늘 다시 계산한다」 와 구별됩니다.

---

# 2. 검토가 올린 것 — `test_ts_02c` 의 범위 (지시 2-b · 수용 기준 나)

**설정 탭 전부로 넓혔습니다** — 내 정보 · 회차 관리 · 회차 상세 · 부서 ·
점검 · 사용자 · 탭 줄. 카드 하나(`.fld`)만 보던 것을 **화면들이 실제로
쓰는 클래스**로 가려 그 탭들에 걸리는 규칙 전부를 봅니다.

> **두 번 넓혔습니다.** 처음에는 `settings.html` 한 장만 읽고 「페이지
> 전체」 라고 적었는데, **설정은 템플릿이 여섯**입니다 — 커밋 전 검토가
> 짚었고 그 틈에 회차 상세의 `.kpi .v` 가 있었습니다.

```
쓰는클래스 = settings.html 의 class="…" 에서 읽는다
… 선택자에 나오는 클래스가 전부 그 안에 있으면 이 페이지의 규칙이다
```

**`.calundated .hint` 처럼 같은 `.hint` 를 쓰는 남의 화면은 안 끌어옵니다** —
안 그러면 이 목록이 설정과 상관없는 이름으로 부풀어 뜻을 잃습니다.

**넓히니 본문 단 이상인 자리가 스물하나가 됐습니다.** 그대로 한 목록에
담으면 「급을 올린 자리」 가 **설정 페이지의 크기 목록**이 되어 뜻을
잃습니다. 그래서 **둘로 갈랐습니다.**

| 목록 | 무엇 | 지금 |
|---|---|---|
| `올린자리` | **원래 장식·보조 자리인데 올린 것** — 까닭이 「이 문장이 없으면 지금 할 수 있는 전부가 없어진다」 쯤이어야 합니다 | `.fld .pwwarn` **하나** |
| `눈금이정한자리` | 구조가 원래 그 급인 것 — 지표 숫자 · 구역 이름 · 경고 띠 · 탭 · 단추 · 정의 표의 값 … | 열하나 |

제목과 입력칸·표는 **요소 이름으로** 걸립니다(`제목태그` · `눈금이정한요소`) —
두 곳이 같은 일을 하면 한쪽만 고쳐지므로 목록에는 안 적습니다.
**단추와 지표 숫자는 안 거릅니다** — 크기를 고를 여지가 있는 자리라
이름으로 적어 둡니다.

**심어 봤습니다 — 이번에는 다른 탭에.**

```
.deptchip 을 --fz-md 로 올림  →  ✗ 「목록에 없는 자리가 본문 단 이상이다: '.deptchip'」
되돌린 뒤                     →  14 passed
```

`.deptchip` 은 **회차 상세**(다른 템플릿)의 부서 칩입니다 — 지난 판의
범위로는 아예 안 보이던 자리입니다. **둘을 같은 판에서 봤습니다** —
안 심었을 때 초록인 것과 심었을 때 빨간 것.

---

# 3. `.hint` 를 한 줄씩 갈랐습니다 (지시 2-a) — **고치지 않았습니다**

4-0 의 기준으로 갈랐습니다 — **혼자 뜻을 지는가**(14px 자리),
**옆이나 아래가 뜻의 절반을 지는가**(13px), **옆의 값이 뜻을 다 지는가**(12px).

| 화면 | 문장 | 가름 | 올릴 값 |
|---|---|---|---|
| 설정 › 내 정보 | 「연락처·부서·역할은 총무팀이 사용자 탭에서 고칩니다」 | 위의 `.kv` 에 그 값들이 **읽기 전용으로 떠 있습니다** — 「왜 못 고치나」 를 옆에서 설명 | **약함** |
| 설정 › 비밀번호 | 「규칙은 길이 하나뿐입니다. 바꿔도 이미 들어와 있는 기기는 …」 | 앞 반쪽은 칸 라벨이 이미 말합니다(「8자 이상」). **뒤 반쪽은 혼자 집니다** — 화면 어디에도 그 사실이 없습니다 | **갈라야 할 자리** |
| 설정 › 바깥 링크 | 「이름과 주소가 있으면 사이드바에 … 둘 다 비우면 항목이 사라집니다」 | **혼자 집니다** — 입력칸만 보고는 비웠을 때 무슨 일이 나는지 알 수 없습니다 | **있음** |
| 설정 › 내 정보(푸시) | 「아침에 하루 한 통으로 옵니다 … 보낼 것이 없는 날은 오지 않습니다」 | **혼자 집니다** — 켜고 끄는 단추만 보고는 무엇이 오는지 모릅니다 | **있음** |
| 회차 상세 | **「개회일을 바꾸면 모든 업무 날짜가 D-주차를 유지한 채 함께 움직입니다」** | **혼자 집니다.** 칸만 보고는 알 수 없고 **되돌리기 어려운 결과**입니다 — 이 판에서 사람이 실제로 그 일을 했습니다 | **가장 큼** |
| 회차 상세 | 「고치는 것은 총무팀만 합니다」 · 「부서 이름·색·순서는 부서 …」 | 권한·위치 안내. 옆이 반을 짐 | 없음 |
| 설정 › 부서 | 「회차를 넘어 쓸 키는 마법사 2단계에서 … 여기서 만든 부서는 이 회차에만 붙습니다」 | 뒷문장이 **혼자 집니다**(2장의 함정) | **있음** |
| 설정 › 회차 관리 | 「지난 회차를 지어내지 않습니다 (6-9). 회차명을 누르면 상세로 갑니다」 | 앞은 원칙(혼자 짐), 뒤는 조작 안내(옆이 짐) — **섞여 있습니다** | 가르면 |
| 설정 › 회차 관리 | 「팀별 수집 초안 — 「…」 수집 중 · 제출 n/N · 초안 열기」 | **조작 안내와 상태가 섞였습니다** — 제출 수는 혼자 지고, 「초안 열기」 는 누르는 자리입니다 | 가르면 |
| 설정 › 점검 | 「자세한 진단은 서버에서 …」 | 가리키는 말 | 없음 |

> **열한 줄입니다.** 처음에 열 줄만 적었다가 커밋 전 검토가 빠진 하나
> (회차 관리의 팀별 수집 줄)를 짚었습니다.

**그리고 고를 때 볼 재료가 하나 더 있습니다** — `.hint` 규칙은 CSS 에
**여섯이고 그중 설정(`.fld .hint`)만 12px 입니다.**

| 어디 | 크기 |
|---|---|
| `.linkform .hint` | 13px |
| `footer.wizfoot .hint` · `.ph-h .hint` · `.sheetbar .hint` · `.calundated .hint` | **14px** |
| **`.fld .hint`(설정)** | **12px** |

**설정만 한 급 아래입니다.** 올릴지 정할 때 「다른 화면은 이미 14px 이다」
가 근거가 됩니다 — 올리는 것이 새 규칙을 파는 것이 아니라 **나머지와
맞추는 것**이 됩니다. 검토가 짚어 적습니다.
**올릴 값이 있는 것이 넷이고 그중 하나(회차 상세의 개회일)가 큽니다.**
고치지 않은 까닭 셋:

1. **한 화면의 일이 아닙니다** — 회차 상세·부서·바깥 링크로 흩어져 있고,
   `.hint` 는 그 페이지들이 함께 쓰는 클래스입니다
2. **나누는 자리가 문장 안입니다** — 비밀번호 칸의 그 줄처럼 **반쪽만
   올려야 하는** 것이 있어, 클래스를 바꾸는 것이 아니라 문장을 가르는
   일입니다. 글을 고치는 것은 사람이 볼 일입니다
3. 지시가 「올릴 것이 있으면 목록을 내고 고치지는 않는다」 였습니다

---

# 4. 회차를 보관으로 넘기는 길 (지시 3) — **만들지 않았습니다**

**지금 화면에도 스크립트에도 없습니다.** 칸(`Retreat.is_archived`)은 있고
읽는 곳은 여럿인데 **참으로 만드는 자리가 없습니다** — 8장이 적어 둔 그대로.
어디에 붙고 무엇이 딸려 오는지만 재어 **봐둘것 AW-a** 에 적었습니다.

| 잰 것 | 무엇 |
|---|---|
| 어디에 붙나 | 설정 › 회차 관리의 회차 줄, 또는 회차 상세의 「수정」 옆 — 이름·개회일과 같은 성격(회차 자체의 속성)입니다 |
| 안 보이게 되는 것 | `deps.py` 의 **기본 회차 후보**에서 빠지고, `period.is_over` 가 **참**이 되어 홈이 결산 홈으로(4-15) · 진단 패널이 판정을 멈추고(4-10) · 알림을 안 만들고(`notify`) · 정기 점검이 안 훑고(`main.py`) · **마법사의 바탕에서도 빠집니다**(`latest_retreat`) |
| **일부러 안 딸려 오는 것** | `board.overdue_by_date` 와 `suggestions.late_in_base` 는 **보관과 무관하게** 그때의 사실을 잽니다 — 「보관은 드롭다운에서 치우는 표시지 그때 지연이 없었다는 뜻이 아니다」. **만들 때 이 둘을 건드리면 지난 회차의 지연 이력이 통째로 사라집니다** |
| 스크립트 | `비품들여오기` 는 보관된 회차를 `--회차` 로 고르면 **경고 한 줄만 찍고 그대로 갑니다**(4-18 · 사람이 정함). `비품묶음합치기` 도 같은 `회차머리` 를 부릅니다 |
| **되돌리는 길** | **같이 없습니다.** 참으로 만드는 자리가 없으니 거짓으로 되돌리는 자리도 없습니다 — **만들 때 둘을 함께** 만들어야 합니다. 한쪽만 만들면 지우지 않았는데 되돌릴 수 없는 상태가 되어 0장이 실질적으로 깨집니다 |

**2026 을 보관할지는 사람이 정합니다. 지금 정하지 않았습니다.**

---

# 5. 운영

**읽기만 했습니다. 쓰지 않았습니다.**

| 무엇을 | 어떻게 |
|---|---|
| 2027 의 날짜 · 회차 수 · 업무 106건의 D-주차 | `sqlite3` 를 `mode=ro` 로 · 앱을 안 띄웠습니다 |
| 날짜 없는 여섯이 무엇인지(`included` · `kind`) | 같음 |
| 활동 기록의 `회차_수정` 한 줄 | 같음 |
| **쓴 것** | **없습니다** — `--실행` 이 있는 명령을 하나도 안 돌렸습니다 |

**사역자 열 분의 계정은 사람이 화면에서 만듭니다** — 실명이 오가는 자리라
11-2 의 「사람만 쥘 수 있는 값」 입니다(인계 18번).

---

# 6. 돌고 있는 서버에서 확인한 것

**건너뛰었습니다 — 서버가 읽는 것이 하나도 안 바뀌어서입니다.**

11-3 이 이 경우를 「건너뜁니다 … 건너뛰었으면 그 사실과 이유를 보고에 한 줄
적습니다」 라고 부릅니다. 처음에는 「건너뛴 것이 아니라 고친 것이 없다」 고
적었는데, 그러면 **다음 사람이 「건너뜀」 으로 찾을 때 이 문단을 못
만납니다** — 커밋 전 검토가 짚었습니다. 하는 일은 같고 **낱말을 규칙에
맞춥니다.**

11-3 2단계는 **커밋 기준**으로 봅니다 — 이번에 내는 커밋에 `app/` ·
`templates/` · `static/` 이 하나라도 들어 있으면 건너뛰지 않습니다.
이 판의 커밋을 세어 보면 그 셋이 **하나도 없습니다.**

```
$ git diff --name-only 8eaef17..HEAD
docs/checks/drawer.js      ← 점검 스크립트(저장소 문서 · 서버가 안 읽습니다)
docs/봐둘것.md
docs/인계.md
tests/test_stage37.py
tests/test_typescale.py
```

**`docs/checks/drawer.js` 가 헷갈리기 쉬운 자리라 적어 둡니다** — 이름이
`.js` 이고 이 판에서 실제로 브라우저가 받아 돌렸지만, **서버가 읽는
파일이 아닙니다.** 사람이 콘솔에 붙여넣거나, 받아 돌릴 때는
`app/static/js/` 에 **사본**을 두었다가 끝난 뒤 지웁니다(8-1의 그 자리 ·
`git status` 가 깨끗합니다).

**앞선 판의 PID 를 다시 적습니다**(11-3 — 「앞서 확인한 것이면 그 PID 와
시각을 다시 적습니다」). 지금 운영에서 도는 것은 **PID 23188** 이고,
2026-09-11 에 올라와 그 뒤로 안 바뀌었습니다. 그때 받아 본 것:
`GET /login` 200 · 그 화면의 CSS(`retreat.13f5ef2e.css`)에 `.fld .pwwarn`
과 `button:not(.sbtn)` 이 들었고 옛 `.loginform button{` 은 걷혔습니다.

> **이 판이 운영에서 확인한 것은 따로 있습니다** — DB 를 읽기 전용으로
> 열어 사람이 고친 날짜와 업무 100건을 셌습니다(1장). 그것은 「돌고 있는
> 서버에서 확인」 이 아니라 **자료를 읽은 것**이라 5장에 적었습니다.
> 둘을 섞지 않습니다.

---

# 7. 수용 기준

| 기준 | 어디서 잰다 | 통과 |
|---|---|---|
| 가) 회차 날짜를 옮기면 업무가 D-주차를 지킨 채 따라온다 · 안 움직이는 종류도 **같은 판에서** | `test37_a01` — 셋으로 갈라 셉니다(옮겨진 3 · 안 건드린 1 · 없던 날짜를 얻은 1). 반대쪽은 `a02`, 손으로 끈 것은 `a03` | O |
| 나) `test_ts_02c` 가 페이지 전체를 본다 · **심어서 빨개지는 것과 안 심었을 때 초록인 것을 같은 판에서** | 2장 — `.setting .sub` 를 심어 ✗, 되돌린 뒤 14 passed | O |
| 다) 운영에서 센 값이 보고에 있다 — 회차 수 · 2027 의 날짜 · 옮겨진 업무 수 | 1장 (둘 · 2027-08-20~22 · 100건) | O |
| 라) pytest 옵션 없이 전부 통과 · 글검사 5/5 | 8장 — **원문을 넣은 뒤 다시 돌린 값** | O |
| 마) 검토자 원문이 최근.md 에 그대로 | 11장 | O |

---

# 8. 검사와 시험

```
1417 passed, 3 warnings in 1390.15s (0:23:10)
```

**두 번 돌렸습니다.** 검토 전과, 검토가 짚은 것을 고치고 **보고에 원문까지
넣은 뒤** 다시. 둘 다 1417 이고 새로 빨개진 것이 없습니다 — 11-3 이
「보고를 다 채운 뒤에 검사와 시험을 다시 돌립니다」 라고 못박은 자리이고,
검토자도 [1] 에서 같은 것을 짚었습니다.

> **이 판에서 늘어난 시험은 셋입니다**(`test37_a01`·`a02`·`a03`).
> `test_ts_02c` 는 지난 판에 이미 있었고 이 판은 그 **범위**를 넓혔습니다.

```
━━━ 모아서 ━━━
  통과  check_names (exit 0)
  통과  check_stale (exit 0)
  통과  check_named (exit 0)
  통과  check_counts (exit 0)
  통과  check_handoff (exit 0)

글 검사가 전부 통과했습니다.
```

**새 파일과 이 보고와 얼린 판을 `git add` 한 뒤 돌린 값입니다** —
`check_stale`·`check_counts` 는 `git ls-files` 를 훑으므로 스테이징하지
않은 파일은 아예 안 보입니다.

> **처음에는 이 줄이 거짓이었습니다.** 얼린 판을 뜨기도 전에 「얼린 판을
> `git add` 한 뒤」 라고 적었고 검토가 [4] 로 잡았습니다 — **두 판 연속
> 같은 자리**입니다. 지금은 실제로 뜨고 스테이징한 뒤의 값입니다.

**얼린 판에 넘김 한 줄이 필요했습니다.** `git add` 한 첫 판에서
`check_counts` 가 **검토자 원문 안의 한 줄**을 잡았습니다(그 칸을 읽는
자리를 검토자가 직접 세어 짚은 대목). 한 글자도 못 고치는 파일이라
`docs/세어둠.txt` 에 이유와 함께 적었습니다 — 11-3 이 「얼릴 때마다 그
파일이 넘김 목록에 한 줄 필요할 수 있습니다」 라고 적어 둔 자리입니다.

## 8-1. 상세 패널 점검 — **자가시험까지 돌렸습니다**

**이 판에서 `docs/checks/drawer.js` 를 고쳤으므로**(머리에 「어느 회차를
보는가」 를 적었습니다 · 아래) 11-3 대로 **자가시험을 세 화면 × 두 계정**으로
돌렸습니다. 주석만 고쳐도 돌린다는 것이 지시였습니다.

주소의 해시 자리를 매번 바꾸고 `{cache:'reload'}` 로 받았습니다.
**받은 길이를 찍은 것은 여섯 판 중 넷이고 넷 다 59,730 입니다**(지난 판
59,095 — 늘어난 것이 그 주석입니다).

> **넷입니다. 여섯이 아닙니다.** 처음에 「여섯 판 다 59,730」 이라고
> 적었다가 **이 보고를 스스로 훑다가**(지시 2-c) 잡았습니다 — 관리자
> 달력·목록 두 판은 길이를 안 찍는 모양으로 물어봤습니다. 네 판이 같은
> 값이고 같은 주소 규칙으로 받았으니 나머지 둘도 같을 것이지만, **「같을
> 것」 과 「찍어서 봤다」 는 다릅니다.**
>
> **그런데 그 훑기가 자기 보고의 수용 기준 표는 안 봤습니다.** 8장이
> 자리표시자인 채로 7장의 라) 에 O 가 찍혀 있었고, 10장도 비어
> 있었습니다 — **커밋 전 검토가 잡았습니다.** 지시 2-c 가 바로 그것을
> 훑으라고 한 것인데 **훑는 범위를 스스로 좁혔습니다**: 「값이 틀렸나」
> 만 보고 「값이 아직 없나」 를 안 봤습니다. 네 판째 같은 자리입니다.

| 계정 | 화면 | 잰 갈래 | 못 심은 갈래 | **놓친 갈래** | 깨끗한 판 (항목수 / 건너뜀 / 못쟀음) |
|---|---|---|---|---|---|
| 관리자 | 보드 | 8 | 4 | **0** | 79 / 2 / 0 |
| 관리자 | 달력 | 9 | 3 | **0** | 82 / 3 / 0 |
| 관리자 | 목록 | 10 | 2 | **0** | 97 / 2 / 0 |
| 부서 리더 | 보드 | 6 | 6 | **0** | 62 / 15 / 0 |
| 부서 리더 | 달력 | 9 | 3 | **0** | 82 / 3 / 0 |
| 부서 리더 | 목록 | 10 | 2 | **0** | 92 / 4 / 0 |

**「깨끗한 판」 이 평소 점검입니다** — 자가시험은 심기 전에 그것을 먼저
돌리고 **✗ 가 하나라도 있으면 심지 않고 멈춥니다**(「심기 전부터 ✗ 가
있습니다」). 여섯 판 다 지나갔으므로 평소 점검도 여섯 판 전부 통과입니다.

**못 심은 갈래가 계정·화면마다 다른 것이 두 계정 세 화면으로 도는 이유입니다** —
관리자 보드는 4, 부서 리더 보드는 6 입니다(부서 리더에게는 아예 없는
자리가 있어 고장을 심을 수 없습니다). 목록 전용 갈래 둘과 달력 전용 둘은
그 화면에서만 심어집니다.

**여섯 판 다 개발 서버(8001)에서 돌렸습니다.** 운영을 안 열었습니다 —
이것이 이 판에서 **중요한 자리**가 됐습니다. 검토가 짚기를,
`?retreat_id=` 는 읽기만 하는 길이 아니라 **그 계정의 「보던 회차」 를
바꿔 저장합니다**(`deps.py` 의 `remember_retreat`). 운영에서 돌렸다면
그 계정 둘이 다음에 들어올 때 2026 이 떴을 것이고, 사람이 **왜인지
몰랐을 것**입니다. 그 사실을 스크립트 머리에도 적었습니다 —
「되도록 개발 서버에서 돌린다」 와 「돌린 뒤 되돌려 둔다」.

> **5장의 「쓴 것 없습니다」 는 그대로 참입니다** — 운영에는 아무것도 안
> 썼습니다. 다만 그 문장이 참인 까닭이 「점검이 안 쓰기 때문」 이 아니라
> **「운영에서 안 돌렸기 때문」** 이라는 것을, 검토를 받고서야 정확히
> 적게 됐습니다.

**여섯 판 다 `?retreat_id=1`** 로 열었습니다 — 까닭을 이번에 스크립트 머리에
적었습니다(지시 4). 안 붙이면 기본 회차(지금은 2027)가 열리는데 **그 회차는
열 자리가 적어** 항목 상당수가 건너뜀으로 빠지고, 건너뛴 것도 항목 수에
들어가므로 **「N/N 통과」 가 뜻을 잃습니다.** 언제 바꿀지도 함께 적었습니다 —
**회차 번호가 아니라 건너뜀 수**로 정합니다(2027 쪽 건너뜀이 2026 쪽보다
많지 않아지면 그때입니다). 회차 id 를 스크립트에 박지 않은 까닭도 그것입니다.

> **한 번 헛돌았습니다.** 탭이 숨은 상태에서 돌리다 45초에 걸려 끊겼는데,
> **페이지 안의 검사는 계속 돌고 있었습니다.** 그 위에 두 번째 판을
> 시작해 둘이 같은 DOM 을 만졌고, 자가시험이 「심기 전부터 ✗ 가 있습니다」
> 로 거절했습니다 — **검사가 맞게 거절한 것**입니다. 새로 고치고 탭을
> 보이게 한 뒤 다시 돌려 위의 값을 얻었습니다. 11-3 이 「탭이 보이는
> 상태여야 한다」 고 적어 둔 그 자리입니다.

---

# 9. 판단이 갈린 자리와 사람이 정할 것

0. **검토가 잡은 셋을 고쳤습니다.** ① 보고의 자리표시자 셋과 거기 얹힌
   O(위) ② **봐둘것 AW-a 의 한 줄이 틀렸습니다** — 「보관하면 마법사의
   바탕에서 빠진다」 고 적었는데 `past_retreats` 가 `select(Retreat)` 를
   그대로 써서 **안 빠집니다.** 고치고, 그 표가 「세어 둔 자리」 라는
   경고를 위에 달았습니다(`models.py` 가 이 칸에 대해 바로 그렇게 적어
   두었는데 그대로 밟았습니다) ③ **`test_ts_02c` 가 설정 탭 여섯 중
   하나만 봤습니다** — 이름은 「페이지 전체」 인데 `settings.html` 만
   읽었고, 회차 상세의 `.kpi .v`(제목 급)가 그 틈에 있었습니다.
   **9장 ㄱ 이 약속한 「목록에 한 줄 더하면 되돌리지 못한다」 가 바로 그
   회차 상세에 대해 성립하지 않고 있었습니다**
1. **`.hint` 를 안 고치고 목록만 낸 것.** 지시가 그랬고, 나누는 자리가
   **문장 안**이라(반쪽만 올려야 하는 것이 있습니다) 글을 고치는 일입니다 — 3장
2. **AW-b·AW-c 를 안 고친 것.** 이 판은 「자잘한 것」 이고, 둘 다 고치려면
   run 에 칸을 하나 더하거나 `dweek` 의 기본값 규칙을 바꿔야 합니다 — 1-4
3. **보관을 안 만든 것.** 지시가 「만들지는 않는다」 였습니다 — 4장
4. **`test_ts_02c` 의 범위를 「CSS 전체」 가 아니라 「설정 페이지」 로
   잡은 것.** 전체로 하면 본문 단 이상인 자리가 수십이라 목록이 뜻을
   잃습니다. 페이지 단위가 「급을 올렸다」 가 뜻을 갖는 가장 넓은 범위입니다
5. **점검이 보는 회차를 스크립트에 안 박은 것.** 주석이 말하는 것은
   「1 을 써라」 가 아니라 **「자료가 있는 회차를 골라라」** 이고, 판단
   기준을 건너뜀 수로 적었습니다

## 사람이 정할 것 — 둘

**ㄱ. `.hint` 에서 올릴 것을 고르시겠습니까?** 3장의 표에 넷이 있고 그중
**회차 상세의 「개회일을 바꾸면 …」** 이 가장 큽니다 — 되돌리기 어려운
결과를 미리 말하는 줄인데 12px 장식 단에 있습니다. 고른 것만 `.pwwarn`
쪽으로 옮기면 되고, `test_ts_02c` 의 목록에 한 줄씩 더하면 다음 사람이
그것을 되돌리지 못합니다.

**ㄴ. 2026 회차를 보관으로 넘기시겠습니까?** 지금 그 길이 없어서
**만드는 일부터입니다.** 무엇이 딸려 오는지는 4장과 봐둘것 AW-a 에
재어 뒀습니다 — 특히 **되돌리는 길을 같이 만들어야** 합니다.
넘기지 않아도 지금 당장 새는 곳은 없습니다(기본 회차는 이미 2027 입니다).

---

# 10. 바뀐 파일

```
$ git diff --name-only 8eaef17..HEAD
docs/checks/drawer.js
docs/봐둘것.md
docs/인계.md
tests/test_stage37.py
tests/test_typescale.py
```

| 커밋 | 무엇 |
|---|---|
| `26c4905` | 시험 — 회차 날짜를 옮기는 길 · 급 검사의 범위 |
| `3b108aa` | 문서 — 점검이 보는 회차 · 보관 자리(AW-a) · 인계 |
| **검토 뒤** | 틀린 한 줄(마법사 바탕) · 범위를 설정 탭 전부로 · `?retreat_id=` 가 쓰는 길이라는 것 · 보고와 얼린 판 |

**`app/` 은 이 판에서 한 줄도 안 바뀌었습니다** — 6장.

---

# 11. 커밋 전 검토자

**아래는 커밋 전 검토자의 답을 익명화를 지나 그대로 옮긴 것입니다.**
한 글자도 고치지 않았습니다. 제가 어떻게 답했는지는 그 아래 표에 있습니다.

```
━━━ 검토자 여기부터 ━━━ 범위 8eaef17..3b108aa · 커밋 2개 · 파일 5개 (+ 커밋 안 된 보고 1개)

먼저 직접 재어 확인한 것
  pytest  1417 passed, 3 warnings in 1126.83s (0:18:46)   ← 옵션 없이 돌림
  글검사  5/5 전부 통과 (exit 0)
  바뀐 두 시험 파일만  17 passed (test_stage37 3 + test_typescale 14)
  운영 DB 를 mode=ro 로 읽어 보고 1장의 셈을 그대로 재현했습니다 —
    회차 2개 · id2 개회 2027-08-20 폐회 2027-08-22 · is_archived 0
    id2: run 106 / D-주차 지켜짐 100 / 어긋남 0 / 날짜 없음 6, 그 6 중 included=1 은 0
    id1: run 102 / 96 / 0 / 6, 날짜 없는 것 중 included=1 은 0
    activity_logs 의 회차_수정(target 2) 1건 · 2026-09-11 15:45
  → 수용 기준 다) 는 한 자리도 안 틀립니다. 쓰기는 안 했습니다(아래 5번은 별개).


━━ 막아야 함 ━━

1. 보고에 자리표시자가 셋인데 「11장만 비어 있다」 고 전달됐고,
   그 자리표시자를 근거로 수용 기준 하나가 O 로 찍혀 있습니다.

   docs/review/최근.md:235   PYTEST_마지막줄
   docs/review/최근.md:333   # 10. DIFF_PLACEHOLDER
   docs/review/최근.md:339   REVIEW_PLACEHOLDER
   docs/review/최근.md:227   | 라) pytest 옵션 없이 전부 통과 · 글검사 5/5 | 8장 | O |

   8장이 가리키는 자리에 값이 없는데 7장은 O 입니다. 글검사도 pytest 도
   이 자리를 못 봅니다(제가 돌려 확인했습니다 — 5/5 통과).
   이번 판의 지시 2-c 가 바로 「보고가 아직 없는 것을 있다고 적은 자리를
   스스로 훑는다」 였고, 8-1 의 「넷/여섯」 은 실제로 그렇게 잡았습니다.
   그런데 같은 훑기가 자기 보고의 수용 기준 표를 안 봤습니다.

   실제 값은 위에 적어 두었습니다. 10장의 diff 도 함께 채워야 합니다.


━━ 고쳐야 함 ━━

2. 봐둘것 AW-a 의 「읽어서 세었습니다」 표에 사실과 다른 줄이 하나 있습니다.

   docs/봐둘것.md:2354
     | app/domain/library.py 의 latest_retreat | 마법사의 바탕에서 빠집니다.
       보관 안 된 것 중 개회일이 가장 늦은 것을 고릅니다 |
   docs/review/최근.md:159 도 같은 말(「마법사의 바탕에서도 빠집니다」)

   실제로는 안 빠집니다.
     app/domain/library.py:134-136  latest_retreat → past_retreats 의 마지막
     app/domain/library.py:116-119  past_retreats 는 select(Retreat) 를 그대로
                                    쓰고 is_archived 를 보지 않습니다
   저장소 전체에서 is_archived 를 읽는 곳은 넷뿐입니다 —
     app/deps.py:36 · app/domain/notify.py:178 · app/domain/period.py:29 ·
     app/main.py:69 (그리고 models 의 정의)
   library 는 그 안에 없습니다.

   이 항목의 존재 이유가 「보관을 만들 때 무엇이 딸려 오는지」 한 장이라,
   틀린 한 줄이 다음 사람의 유일한 재료가 됩니다. 나머지 네 줄과
   「일부러 안 딸려 오는 것」(board.overdue_by_date · suggestions.late_in_base)
   은 읽어서 맞는 것을 확인했습니다 — board.py:168-178 에 그 이유가 그대로
   적혀 있습니다.

   덧붙여, app/models.py:59-62 는 바로 이 칸에 대해 「자리를 세어 두지
   않는다(10장)」 고 적어 두었습니다. AW-a 는 자리를 표로 세었고 그중
   하나가 이미 틀렸습니다 — 세지 말라는 이유가 이 판에서 그대로 났습니다.


3. test_ts_02c 의 새 범위가 설정 탭 여섯 중 하나만 봅니다.
   이름과 독스트링과 보고는 「설정 페이지 전체」 라고 말합니다.

   tests/test_typescale.py:169-172
     def _설정화면의_클래스() … (ROOT / "app" / "templates" / "settings.html")

   설정은 템플릿이 여섯입니다 — settings.html · settings_retreats.html ·
   settings_retreat_detail.html · settings_departments.html ·
   settings_checkup.html (그리고 admin_users.html · library.html 탭).
   게다가 탭 줄은 partials/settings_tabs.html 에 있어 클래스(stabs · adm)가
   아예 안 보입니다.

   제가 같은 논리를 다섯 템플릿의 클래스 합집합으로 돌려 재었습니다.
     지금 (settings.html 만): 본 것 7 · 올린 것 3
     다섯 화면 전부      : 본 것 13 · 올린 것 4
     새로 들어오는 여섯: .kpi .l · .kpi .v · .kpi .v small · .kpi .d ·
                        .stbadge · .setting .deptchip
     그중 .kpi .v 는 --fz-4xl 로 **올린 자리인데 목록에 없습니다** —
     범위가 이름대로였다면 오늘 ③ 이 빨개집니다.

   그리고 이 구멍이 이 판의 결론과 바로 부딪힙니다.
     보고 3장이 「가장 큼」 으로 꼽은 문장은
       app/templates/settings_retreat_detail.html:26
       「개회일을 바꾸면 모든 업무 날짜가 D-주차를 유지한 채 함께 움직입니다」
     보고 9장 ㄱ 은 「고른 것만 .pwwarn 쪽으로 옮기고 test_ts_02c 의 목록에
     한 줄씩 더하면 다음 사람이 그것을 되돌리지 못합니다」 라고 적었습니다.
     그 템플릿은 이 검사의 눈 밖이라, 거기서 새 클래스를 써서 올리면
     선택자의 클래스가 쓰는클래스에 없어 조용히 건너뜁니다. 약속이 그
     한 줄에 대해서는 성립하지 않습니다.

   독스트링의 「이 페이지 어느 카드에 둘째가 생겨도 걸립니다」 도 같은 뜻으로
   넓게 읽힙니다. _설정화면의_클래스 의 주석이 「손으로 적으면 칸이 하나 늘 때
   목록만 낡습니다(10장)」 인데, 한 템플릿만 읽는 지금 모양이 정확히 그
   방식으로 낡습니다 — 탭이 하나 늘면 검사는 아무 말도 안 합니다.

   심기 증명 자체는 맞습니다 — .setting .sub 는 옛 `.fld ` 접두 필터로는
   안 걸리던 자리라 넓힌 범위를 실제로 증명합니다.


4. 인계 9번이 「덮이는 최근.md」 를 가리킵니다 — 이 판이 그 줄을 고치면서
   그대로 두었고, 이 판의 얼린 판이 아직 없습니다.

   docs/인계.md:87
     - 재료가 어디: 운영을 읽어 센 값은 `docs/review/최근.md` 1장.

   CLAUDE.md 0장이 「덮이는 최근.md 가 아니라 얼린 판을 가리킵니다(11-3 4단계)」
   라고 못박았고, docs/봐둘것.md 머리말(17-19줄)은 이 저장소가 그 사고를
   한 번 겪었다고 적어 두었습니다. 인계의 형제 줄은 전부 날짜 파일을
   가리킵니다(24 · 25 · 109 · 115 · 148 · 159 · 166 · 177 · 189 · 216 · 226).
   87줄 하나만 최근.md 이고, 그 줄이 이 판의 diff 안에 있습니다(1-0 → 1장).

   그리고 지금 작업 트리에 얼린 판이 없습니다 —
     git status --untracked-files=all 에 ` M docs/review/최근.md` 한 줄뿐
     docs/review/2026-09-12-2027회차.md 는 직전 판(54ea565/8eaef17)의 것
   그런데 보고 8장 250줄은 「이 보고와 얼린 판을 git add 한 뒤 돌린 값입니다」
   라고 적혀 있습니다. 얼린 판이 저장소에도 작업 트리에도 없습니다.

   이 판의 셈(2027 날짜 · 100건 · D-주차 검증)은 11-3 이 말한 「다음 판이
   입력으로 쓸 셈·실측」 바로 그것이라 얼릴 자리입니다.


5. drawer.js 머리글이 소개한 ?retreat_id= 는 읽는 길이 아니라 쓰는 길입니다.
   그 사실이 주석에도 보고에도 없습니다.

   docs/checks/drawer.js:15-17
     「안 붙이면 기본 회차가 열리는데, 그것은 …(app/deps.py)」

   app/deps.py:14-24 를 보면
     19  if retreat_id is not None:
     23      remember_retreat(db, user, retreat)      ← db.commit() 합니다(51줄)
   즉 그 주소로 한 번 열면 그 계정의 user_retreat_states 가 2026 으로 바뀌고
   그 뒤로는 파라미터 없이 들어와도 2026 이 열립니다(우선순위는 26-32줄 —
   URL > 저장된 회차 > 기본). 그래서 둘이 어긋납니다.

   (a) 「안 붙이면 기본 회차가 열린다」 는 저장된 회차가 없는 계정에만 맞습니다.
       주석의 전제가 첫 한 번에만 성립합니다.
   (b) 점검을 여섯 판 돌린 두 계정은 이제 2026 을 기본으로 봅니다. 부서 리더
       계정이 실제 사람의 것이면 다음에 열 때 지난 회차가 뜨고 왜인지 모릅니다.
       되돌리는 말(회차 드롭다운으로 2027 로 돌려 둔다)이 주석에 없습니다.
   (c) 보고 5장은 「쓴 것 — 없습니다」 이고 0장은 「읽기만 했습니다」 입니다.
       사람이 건 조건이 「운영은 읽기만 한다」 였습니다.

   다만 보고 8-1 은 점검을 **어느 서버에서** 돌렸는지 안 적었습니다. 6장은
   운영 PID 23188 얘기를 하고 8-1 은 주소 해시만 말합니다. 운영이면 위의
   (c) 가 사실이고, 개발 서버면 6장과 8-1 이 서로 다른 서버 얘기를 하고
   있는 것이라 그 사실을 한 줄 적어야 합니다. 어느 쪽인지는 제가 정할 일이
   아니라 아래 물음에 뒀습니다.

   (점검 스크립트가 자료를 바꾸는지는 따로 읽었습니다 — 링크 붙이기는 일부러
    틀린 주소를 넣어 저장이 안 되게 해 두었고, 상태·담당자를 실제로 고르는
    자리는 없습니다. 남는 쓰기는 위의 한 줄뿐입니다.)


━━ 봐 둘 것 ━━

6. AW-c 의 방아쇠가 적힌 것보다 넓습니다.
   docs/봐둘것.md:2397-2416 은 dweek.resolve_dates 의 `d_week or FIRST_D_WEEK`
   한 줄을 지목하고 제목도 「D-주차가 빈 채 켜진 업무」 입니다. 실제 조건은
     app/domain/library.py:736-742 가 넘기는 것은 run.d_week 이 아니라
     lib.default_d_week 이고,
     745줄의 비교는 「run 의 날짜가 계산값과 다른가」 이므로
   **켜진 run 의 날짜가 비어 있으면** 라이브러리에 D-주차가 있든 없든 날짜가
   채워집니다. 지금은 create_run 이 날짜 없이 만들 때 상대 위치도 안 넣어서
   (tasks.py:121-125) 둘이 겹치지만, 이름 붙은 한 줄만 고치면 넓은 쪽이
   그대로 남습니다. 「운영에서 안 밟힌다」 는 판정은 맞습니다 — 날짜 없는
   여섯이 두 회차 다 included=0 인 것을 제가 다시 세었습니다.

7. test37_a02 의 판별력이 ⑦ 의 run 하나에서만 나옵니다.
   app/routers/settings.py:381 의 `if retreat.start_date != old_open` 가드를
   떼어도, 같은 개회일로 다시 계산하면 라이브러리 자리에 있는 run 들은
   값이 안 바뀝니다. a02 가 실패하는 이유는 오직 run_no 5(날짜 없이 켜진 것)가
   D-13 날짜를 얻기 때문입니다. 즉 AW-c 를 고치는 순간 a02 는 조용히
   아무것도 안 재게 됩니다. 독스트링이 말하는 「늘 다시 계산한다와 구별된다」
   가 그 run 에 얹혀 있다는 것을 한 줄 적어 두면 다음 사람이 압니다.

8. 올린자리 목록의 이름과 내용이 갈립니다.
   tests/test_typescale.py:158 의 .kv 이유는 「올린 것이 아니라 원래 그 급이다」
   입니다. 목록 위 주석(141-146줄 언저리)은 「올리는 데는 까닭이 있어야 하고,
   그 까닭은 『이 문장이 없으면 지금 할 수 있는 전부가 없어진다』 쯤이어야
   한다」 고 요구합니다. 지금 목록에는 두 종류(정말 올린 것 · 원래 그 급인 것)가
   섞여 있고 검사는 len(이유) > 30 만 봅니다. 3번대로 범위를 넓히면
   .kpi .v 가 세 번째 종류로 들어옵니다.

9. 새로 더한 단언 하나가 CSS 가 아니라 템플릿에 묶여 있습니다.
   tests/test_typescale.py:234
     assert set(올린자리) <= 올린것, "③ 목록에만 있고 CSS 에 없는 자리: …"
   settings.html 에서 loginform 클래스가 빠지면(비밀번호 폼이 자기 클래스를
   갖게 되면) .loginform button:not(.sbtn) 이 범위 밖이 되어 이 단언이
   빨개집니다. 그런데 메시지는 「CSS 에 없다」 고 말하고 CSS 에는 그대로
   있습니다. 방향 자체는 좋은 추가입니다(낡은 줄 막기) — 말만 어긋납니다.

10. 3장의 .hint 가름에 빠진 재료 둘.
    (a) 설정 탭의 .hint 는 열한 줄인데 표는 열 줄을 봅니다. 빠진 것은
        app/templates/settings_retreats.html:17 — 팀별 수집 초안 줄입니다.
        조작 안내 + 상태(제출 n/N)가 섞여 있어 가름이 갈릴 만한 자리입니다.
    (b) CSS 의 .hint 규칙은 여섯인데 .fld .hint 만 12px 입니다 —
        retreat.css:697 .linkform(13) · 885 wizfoot(14) · 1169 .ph-h(14) ·
        1289 .sheetbar(14) · 1490 .calundated(14) · 1691 .fld(12).
        「설정만 장식 단」 이라는 사실은 올릴지 정하는 데 쓸 근거인데
        표에도 9장 ㄱ 에도 없습니다.


━━ 구현자가 물은 다섯에 대한 답 ━━

o AW-b · AW-c 가 맞는가 — 둘 다 맞습니다.
  AW-b: library.py:736-747 이 run 의 옛 날짜를 미는 것이 아니라 lib 의
  default_* 로 다시 셉니다. 4-6 이 「TaskRun 만 바뀐다」 고 정한 것과
  만나면 끈 자리가 표시 없이 사라지는 것 맞습니다.
  AW-c: dweek.py:72 의 `d_week or FIRST_D_WEEK` 맞습니다. 다만 6번.
  「운영에서 안 밟힌다」 는 제가 다시 세어 확인했습니다.

o test_ts_02c 의 새 범위가 새는가 — 두 방향 다 봤습니다.
  다른 화면을 끌어오는 쪽: 안 끌어옵니다. 범위 안에 든 일곱 중
  .loginform label · .loginform button:not(.sbtn) 은 settings.html:42 가
  실제로 그 클래스를 쓰므로 남의 화면이 아니라 이 화면의 규칙이 맞습니다.
  놓치는 쪽: 3번 — 여섯 화면 중 하나만, 파샬은 통째로 못 봅니다.
  (partials/settings_tabs.html 의 .stabs .adm 은 지금 --fz-xs 라 마침
   올린 자리가 아니어서 오늘은 아무 일도 안 납니다.)

o 3장의 .hint 가름이 4-0 을 제대로 쓴 것인가 — 아홉 줄의 가름 자체는
  기준과 맞습니다. 특히 비밀번호 줄을 「앞 반쪽 / 뒤 반쪽」 으로 가른 것과,
  「고치는 것은 총무팀만」 을 옆이 반을 지는 쪽에 둔 것이 4-0 의 세 급을
  바르게 쓴 판단입니다. 고치지 않고 목록만 낸 판단도 맞다고 봅니다 —
  나누는 자리가 문장 안이라 글을 고치는 일이고, 그건 사람 몫입니다.
  다만 10번의 두 재료가 빠져 있어 사람이 고를 때 정보가 모자랍니다.

o 6장을 「고친 것이 없다」 로 적은 것이 맞는가 — 판정은 맞습니다.
  이 범위의 다섯 파일에 app/ · templates/ · static/ 이 없고,
  docs/checks/drawer.js 를 app/ 이나 templates/ 에서 참조하는 곳은 0개입니다
  (grep 으로 확인). 서버가 안 읽는다는 판정 맞습니다.
  말만 한 가지 — CLAUDE.md 11-3 2단계는 이 경우를 「건너뜁니다 …
  건너뛰었으면 그 사실과 이유를 보고에 한 줄 적습니다」 라고 부릅니다.
  보고는 「건너뛴 것이 아니라」 로 시작해 그 말을 부정합니다. 하는 일은
  규칙대로인데 다음 사람이 「건너뜀」 으로 찾으면 이 문단을 못 만납니다.
  (직전 커밋 8eaef17 의 제목은 「건너뛴 재시작을 적는다」 였습니다.)

o 없는 것을 있다고 적은 자리가 남았는가 — 남았습니다. 1번입니다.
  8-1 에서 「넷/여섯」 을 스스로 잡은 것은 정확한 훑기였는데, 그 훑기가
  7장의 수용 기준 표와 8장·10장의 자리표시자에는 안 닿았습니다.


━━ 사람에게 묻는 것 ━━

ㄱ. 점검(docs/checks/drawer.js 여섯 판)을 운영 서버에서 돌렸습니까,
    개발 서버에서 돌렸습니까? 운영이면 두 계정의 「보던 회차」 가 2026 으로
    바뀌어 있으니 2027 로 되돌려 두실지 정해 주세요(5번).

ㄴ. test_ts_02c 의 범위를 설정 탭 여섯 전부로 넓힐까요? 넓히면 .kpi .v 가
    목록에 한 줄 필요하고(회차 상세의 큰 숫자), 그 대신 보고 9장 ㄱ 의
    약속이 회차 상세에도 실제로 걸립니다(3번).

ㄷ. 9장 ㄱ·ㄴ(.hint 에서 올릴 것 · 2026 보관)은 구현자가 이미 올려 둔
    물음이라 다시 묻지 않습니다. 다만 ㄱ 을 고르실 때 10번의 두 재료를
    함께 보시면 좋겠습니다.

━━━ 검토자 여기까지 ━━━
```

## 무엇을 했나 — 열 중 아홉을 고치고 하나를 답했습니다

| 검토 | 어떻게 했나 |
|---|---|
| **[1]** 자리표시자 셋 · 거기 얹힌 O | **채웠습니다.** 그리고 **왜 스스로 못 잡았는지**를 8-1 에 적었습니다 — 「값이 틀렸나」 만 보고 **「값이 아직 없나」 를 안 봤습니다.** 같은 모양이 네 판째입니다 |
| **[2]** AW-a 의 틀린 줄 | **고쳤습니다.** `past_retreats` 가 `select(Retreat)` 를 그대로 써서 **보관해도 마법사 바탕에 남습니다.** 그리고 그 표 위에 **「세어 둔 자리」 경고**를 달았습니다 — `models.py` 가 이 칸에 대해 바로 그렇게 적어 뒀는데 그대로 밟았습니다 |
| **[3]** 범위가 여섯 중 하나 | **설정 탭 전부로 넓혔습니다**(파샬과 사용자 탭 포함). 스물하나가 들어와 **목록을 둘로 갈랐습니다** — `올린자리`(하나)와 `눈금이정한자리`(열하나). 한 목록에 담으면 「급을 올린 자리」 가 **페이지의 크기 목록**이 되어 뜻을 잃습니다. **`.deptchip`(회차 상세)을 심어** 빨개지는 것까지 봤습니다 |
| **[4]** 인계가 덮이는 파일을 가리킴 | **얼린 판 이름으로 바꿨습니다**(`2026-09-12-2027확인.md`). 그 파일을 이 푸시에 함께 냅니다 |
| **[5]** `?retreat_id=` 가 쓰는 길 | **스크립트 머리에 적었습니다** — 「보던 회차를 바꿔 저장한다 · 돌린 뒤 되돌려 둔다 · 되도록 개발 서버에서」. 그리고 **여섯 판을 개발 서버에서 돌렸다는 것**을 8-1 에 적었습니다(검토자의 ㄱ 에 대한 답) |
| **[6]** AW-c 의 방아쇠가 더 넓다 | **적었습니다** — 정확히는 「켜진 run 의 날짜가 비어 있으면」 이고, `dweek` 의 한 줄만 고치면 넓은 쪽이 남습니다 |
| **[7]** a02 의 판별력이 run 하나에 얹힘 | **독스트링에 적었습니다** — AW-c 를 고치는 날 이 줄이 조용히 아무것도 안 재게 됩니다 |
| **[8]** 목록에 두 종류가 섞임 | **[3] 의 가름이 이것을 함께 풉니다** |
| **[9]** 단언 메시지가 어긋남 | 「**목록에만 있고 지금 화면에는 안 걸리는 자리**」 로 고쳤습니다 — CSS 가 아니라 범위의 문제라는 말입니다 |
| **[10]** `.hint` 가름에 빠진 재료 둘 | **3장에 더했습니다** — 빠뜨린 열한 번째 줄과, **설정의 `.hint` 만 12px 이고 다른 다섯은 13~14px** 이라는 사실 |

**검토자가 사람에게 물은 ㄱ 은 제가 답할 수 있는 것이라 답했습니다**
(개발 서버에서 돌렸습니다 — 운영 계정의 보던 회차는 안 건드렸습니다).
**ㄴ 은 물어보지 않고 넓혔습니다** — 지시 2-b 가 「범위를 페이지 전체로
넓힌다」 였고, 안 넓히면 9장 ㄱ 의 약속이 거짓이 되기 때문입니다.

> **6장의 말도 고쳤습니다.** 「건너뛴 것이 아니라」 로 시작해 11-3 의
> 낱말을 부정하고 있었습니다 — 다음 사람이 「건너뜀」 으로 찾으면 그
> 문단을 못 만납니다.

