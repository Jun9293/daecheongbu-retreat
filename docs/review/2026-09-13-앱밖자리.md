사람이 할 것
노트북에서 직접: 없음 — 사역자 열 분 계정(인계 18번)과 개기자 주소(인계 8번)는 그대로 남아 있습니다
관리자 권한: 없음 — 재시작은 이 창에서 됐습니다(7장)
정할 것: 검토자가 물은 둘 — ① 앱 밖 자리가 `app.config` 를 읽어서 `data/` 가 통째로 빈 날 부르면 폴더와 서명키를 새로 쓸 수 있는데 받아들일지(3-3 · 봐둘것 AZ-c) ② 개발 DB 에 스스로 만든 버리는 계정이라도 이 창이 비밀번호를 칸에 넣지 않는 것을 규칙으로 둘지(4-1)
막혀서 못 한 것: phone.js 의 안 심은 고장 둘 — 개발 서버에도 로그인해야 자가시험이 도는데 이 창은 비밀번호를 칸에 넣지 않습니다(4-1)

작성 시각: 2026-09-13 18:30 KST — 검토를 받고 고친 판
커밋: (이 보고와 함께 나갑니다 — 11장)
작업: DB 가 비어도 알 수 있게 · 새벽 푸시 끄기 · 예외 없음

---

# 0. 한 줄 요약

**DB 가 계정까지 비어도 `scripts/되살아나나.ps1` 이 빈 백업 판을 말하고, 백업 알림은 푸시로 안
나가며, pytest 에 `-p` 를 끼우면 conftest 가 예외 없이 멈춥니다.** 사고의 실타래는 이 판으로 닫습니다.

- 판정과 문장은 `backup.의심말들` 하나이고 앱 안(`알린다`)과 앱 밖(`살핀다`)이 같이 부릅니다.
- 앱 밖 자리는 **늦습니다** — 사람이 그 스크립트를 여는 것은 사고를 알아챈 뒤입니다(CLAUDE.md 11-2).
- 가드는 명령줄 · `PYTEST_ADDOPTS` · ini `addopts` 의 `-p` 와 `PYTEST_PLUGINS` 를 봅니다.
  PYTHONSTARTUP · sitecustomize · 설치된 플러그인 entry point 는 못 봅니다(검토 [B] 를 받고 넓힘).

---

# 1. 고치기 전 훑기 (11-3 0단계)

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| `notify` 를 부르는 데 | 앱 안 여럿과 `backup.알린다`. 새 인자 `push` 는 기본이 켬이라 **다른 부르는 곳은 그대로** 푸시를 시도합니다 — `test44_f01` 이 「기한 초과」 로 그것을 같은 판에서 잽니다 |
| 백업 알림의 문장 | 전에는 `알린다` 안에 있었습니다. 앱 밖 자리가 같은 말을 해야 하므로 `의심말들` 로 뺐고 제목 글자가 코드에 한 번만 있는지 `test44_e05` 가 셉니다 |
| `rows_of` · `suspects` 를 부르는 데 | `prune` · `run` · 시험(`test_stage43` · `test_stage44` · `test_backup`). 새 인자 `기록` 은 기본이 참이라 **옆 파일을 쓰던 곳은 그대로** 씁니다. 앱 밖만 거짓 |
| `되살아나나.ps1` 을 적어 둔 문서 | CLAUDE.md 11-2 · 14장 · 배포-안내 12-4. 14장의 「파워셸만 쓴다」 는 줄을 검토가 짚어 고쳤습니다([E]). 배포-안내는 출력 모양을 박아 두지 않아 안 고쳤습니다 |
| 「푸시로도 나간다」 · 「AZ-a 를 열면 끼운다」 를 적어 둔 곳 | CLAUDE.md 11-2 · 11-3 · 봐둘것 AZ-c · 앞 판 보고(덮임). 살아 있는 문서는 고치고 옛말에 표기로 적었습니다. 봐둘것 AZ-c 의 옛 줄은 그때의 기록이고 바로 아래에 이 판의 결론을 붙였습니다 |

---

# 2. 앱 밖 알림 자리 (지시 1)

## 2-1. 어디에 (1-a · 1-b)

**새 자리를 만들지 않고 `scripts/되살아나나.ps1` 에 붙였습니다.** 이미 백업 폴더를 훑는 4장이 있어서
그 끝에 「빈 판」 줄을 더했습니다. 그 줄은 `backup.py --살핀다` 를 부르고, `살핀다` 는

- **DB 를 안 엽니다** — 엔진도 sqlite 연결도 운영 DB 로 안 갑니다. 백업 판 파일만 엽니다
- **옆 파일(rows.json)을 안 씁니다** — `기록=False` 로 세기만 합니다(`test44_e03` · 고장 ②)
- 의심 판이 없으면 「빈 판 없음」, 있으면 앱 안 알림과 같은 제목과 본문을 찍습니다

운영에 돌린 줄(바깥 주소 없음):

```
  손으로 뜬 것까지 합쳐: 18개
  [빈 판 — 백업 폴더만 읽음 · DB 와 무관]
    빈 판 없음
```

「빈 판 없음」 이 맞습니다 — 운영의 빈 판은 증거 판 하나이고 `지키는판` 이라 안 냅니다(앱 안과 같음).
돌리기 전과 뒤에 백업 폴더 파일 수가 같았습니다(검토자도 따로 확인).

## 2-2. 사람이 언제 보나 (1-e)

**늦습니다.** 그 스크립트를 여는 것은 전원을 내렸다 올릴 때(배포-안내 12-4)와 무언가 이상할 때입니다.
사고가 나면 **사고를 알아챈 뒤에** 봅니다 — 먼저 알려 주는 자리가 아니라 알아챈 사람이 어느 판으로
돌아갈지 고르는 자리입니다. CLAUDE.md 11-2 에 그대로 적었습니다.

## 2-3. 빈 DB 에서 잰 것 (1-c · 수용 가)

`test44_e01` — `tmp_path` 에 **스키마만 있고 계정 0 인 DB** 를 실제로 만들고(`Base.metadata.create_all`),
같은 백업 폴더에 성한 판 셋과 빈 판 하나를 둡니다.

1. 빈 DB 에 `알린다` → 알림이 안 서고 까닭이 옵니다(시험의 전제)
2. `backup.py --살핀다` 를 **자식 프로세스로** 부르며 `DCB_DATABASE_URL` 을 그 빈 DB 로 줍니다 → 빈 판을 말합니다
3. 같은 시험 안에서 성한 DB(conftest DB · 관리자와 회차) → `알린다` 가 1 건 세우고, `살핀다` 가 **글자까지 같은 줄**을 냅니다

검토 [D] 가 3 의 단언이 1 의 결과를 다시 쓰고 있다고 짚어, 성한 DB 에 대고 `알린다` 를 다시 받게 고쳤습니다.
`test44_e02` 는 빈 판이 없을 때 「빈 판 없음」 을 봅니다.

## 2-4. 같은 말 · 판정 하나 (1-d · 수용 나)

- `test44_e04` — 알림 표에 선 제목·본문 집합이 `의심말들` 결과와 같고, `살핀다` 의 줄이 그 결과를 그대로 편 것과 같습니다
- `test44_e05` — 제목 글자 「새벽 백업이 비어 있습니다」 가 `app/` 과 `backup.py` 를 통틀어 **한 번**만 있습니다. ps1 에는 `--살핀다` 가 있고 판정 낱말(`retreats` · `SUSPECT`)이 없습니다. BOM 도 봅니다

---

# 3. 새벽 푸시 끄기 (지시 2)

## 3-1. 무엇을 (2-a · 2-b · 2-d)

`app/notifications.notify(..., push: bool = True)` — 참이면 전처럼 `_try_push`, 거짓이면 앱 안에만 섭니다.
`backup.알린다` 만 `push=False` 를 줍니다. **앞으로의 기준 한 줄**(CLAUDE.md 11-2 · notify 독스트링):
「그 순간 사람이 움직일 일인가」. 끈 자리는 `push=False` 로 찾습니다(검토 [H] 를 받고 「하나다」 를 뺌).

## 3-2. 같은 판에서 (2-c · 수용 다)

`test44_f01` — `app.push.push_notifications` 를 가로채 보낸 알림 종류를 모읍니다. 같은 시험에서
`알린다` 를 부르면 「백업」 이 안 모이고, 이어서 `notify` 로 「기한 초과」 를 세우면 모입니다.
하나만 재면 「전부 껐다」 와 구별이 안 되어 둘을 같이 봅니다. 고장 ①(백업도 푸시) · ④(푸시를 늘 끔) 둘 다 빨강.

## 3-3. 검토가 짚은 「읽기만」 의 경계 — 정할 것

`backup.py` 는 `app.config` 를 import 합니다. 그 모듈은 읽힐 때 `data/` · `uploads/` 폴더를 만들고
서명키가 없으면 새로 씁니다. **지금 운영에는 둘 다 있어 아무것도 안 씁니다.** 다만 이 자리가 필요한
날이 「`data/` 가 통째로 비었다」 는 날이면, 그날 부르면 서명키가 새로 섭니다. DB 와 백업은 안 건드리고
새 서명키는 전원이 다시 로그인하는 것뿐이라 **두었습니다** — 받아들일지는 사람칸 「정할 것」 ①.
봐둘것 AZ-c 와 CLAUDE.md 11-2 에 적었습니다.

---

# 4. 예외 없음 (지시 3)

## 4-0. 11-3 에 (3-a)

CLAUDE.md 11-3 의 그 줄 아래에 「예외가 없습니다 — `-p no:cacheprovider` 도, 고장을 심는 판도」 와 까닭
(예외를 두면 그때마다 사람이 가르고 그 가름이 사고의 입구였다 · 지킬 수 없는 예외보다 불편한 금지가 낫다).
「끼워야 할 일이 생기면 AZ-a 를 연다」 는 지웠고 옛말에 적었습니다.

## 4-1. `-p no:cacheprovider` 없이 되나 (3-b)

**됩니다.** 앞 판은 고장을 심은 사본을 `-p no:cacheprovider` 로 돌렸는데, 이 판은 고장 일곱을
**작업 트리에 직접 심고 `-p` 없이** `pytest tests/test_stage44.py -k …` 로 돌린 뒤 파일을 되돌렸습니다
(되돌린 뒤 `cmp` 로 사본과 같음을 확인).

```
① 백업 알림 push=True              -> f01 빨강
② 살핀다가 옆 파일을 씀(기록=True)   -> e03 빨강
③ -p 가드 끔                        -> g01 빨강
④ notify 의 푸시를 늘 끔            -> f01 빨강
⑤ 앱 밖 문장이 본문을 뺌            -> e04 빨강
⑥ ini addopts 를 안 봄              -> g01 빨강
⑦ PYTEST_PLUGINS 를 안 봄           -> g01 빨강
```

**phone.js 의 안 심은 둘(지시 4-a)** — 개발 DB 계정 길을 봤습니다. `scripts/devserve.bat` 은
`%TEMP%` 아래 따로 선 DB 를 쓰므로 운영에는 안 닿지만, 들어가려면 로그인 칸에 비밀번호를 쳐야 합니다.
**이 창은 계정이 누구의 것이든 비밀번호를 칸에 넣지 않아 시도하지 않았습니다.** 세션 쿠키를 서명해
넣는 길은 인증을 돌아가는 것이고, 개발 서버에만 여는 로그인 우회는 그 코드가 곧 열린 문이라 안 했습니다.
봐둘것 AY-a 아래에 적었습니다. 검토 [I] 는 11-2 가 이 넓이로 막지 않는다고 짚었고 — 사람칸 「정할 것」 ②.

## 4-2. 기계가 보나 (3-c)

**`-p` 는 봅니다.** `tests/conftest.py` 의 `pytest_configure` 가 명령줄 · `PYTEST_ADDOPTS` · ini `addopts` 에서
`-p`(붙인 꼴 `-pX` 포함, `--` 로 시작하는 것 제외)를, 그리고 `PYTEST_PLUGINS` 가 비지 않았는지를 보고
`pytest.exit(returncode=4)` 로 멈춥니다. 처음에는 앞의 둘만 봤고 검토 [B] 가 pytest 9.1.1 소스를 읽어 나머지
둘을 짚어 넣었습니다.

**못 보는 것** — PYTHONSTARTUP · sitecustomize · 설치된 플러그인 entry point. conftest 가 읽히기 전에 돌고
conftest 에서 보이는 자국이 없습니다. 그쪽은 11-3 의 글과 `_시험DB가_아니면_멈춘다` 가 지킵니다.

`test44_g01` 은 **`-p` 로 pytest 를 띄우지 않습니다**(그것이 금지) — 훅을 가짜 설정으로 부릅니다. 막히는 쪽
(짧은 꼴 셋 · 붙인 꼴 · `PYTEST_ADDOPTS` · addopts · `PYTEST_PLUGINS`)과 통과하는 쪽(옵션 없음 · `--pdb -q`)을 둘 다 봅니다.

---

# 5. 정리 (지시 4)

- **4-b 인계** — 머리를 「사고의 실타래(23·24·25)는 이 판으로 닫는다 · 남은 것: 사역자 계정 · 개기자 주소 · 재정」 으로 바꾸고, 끝난 것에 27번을 더했습니다
- **4-c 한 곳** — `docs/인계.md` 맨 앞에 표 하나: 지시문 대조 · 고치기 전 훑기 · 되살아나는지 · 화면 시험자 · 회차를 넘나드는 시험이 각각 어디 서 있고 무엇이 남았나
- **검토 뒤에 하나 더 고쳤습니다(검토자가 안 본 것)** — 이 보고에 원문을 싣자 `tests/test_docs_shell.py` 의 `%VAR%` 규칙이 원문 [I] 의 줄바꿈된 줄(`%TEMP%` 로 시작)에 걸렸습니다. 원문은 못 고치므로, 그 규칙 **하나만** 검토자 원문 덩어리(`check_placeholder` 가 쓰는 두 끝 글자) 안을 안 보게 했습니다. `test_껍데기_05c` 가 원문 안은 안 걸리고 같은 줄이 밖이면 걸리며 원문 안의 줄 잇기는 여전히 걸리는지 잽니다. 그 줄을 되돌려 심으면 05c · 07 이 빨강
- **문서(11-3 3단계)** — CLAUDE.md 11-2(푸시 안 나감 · 기준 한 줄 · 앱 밖 자리 · 늦다 · 읽기만의 경계) · 11-3(예외 없음 · 기계가 보는 것과 못 보는 것) · 14장(새 줄 셋 · 되살아나나 줄) · 봐둘것 AZ-c · AY-a · 옛말 한 항목

---

# 6. 글검사 결과

```
━━━ 모아서 ━━━
  통과  check_names · check_stale · check_named · check_counts
  통과  check_handoff · check_placeholder · check_orders

글 검사가 전부 통과했습니다.
```

**이 보고를 다 쓴 뒤에 다시 돌린 결과입니다.** 이 판은 얼린 판을 새로 안 만들었습니다.

---

# 7. 돌고 있는 서버 (11-3 2단계)

`app/notifications.py` 가 바뀌어 배포-안내 12-1 대로 재시작했습니다.

```
멈추기 전 PID: 13836 · 18:21:23
포트가 비었습니다
다시 켠 뒤 PID: 23752 · 18:21:26
성공 — PID 가 바뀌었습니다
GET http://127.0.0.1:8000/login -> 200
```

`app/` 의 마지막 수정(검토 [H] 독스트링)은 재시작 전입니다 — 그 뒤로 `app/` · `templates/` · `static/` 을 안 고쳤습니다.
이번 변경은 화면에 새 요소가 없고(푸시 여부) 새벽 작업이 부르는 길이라, 돌고 있는 서버에서 눈으로 볼 것은 뜨는지뿐입니다.

---

# 8. 수용 기준 — 각 줄이 무엇을 보나

| 기준 | 어디서 잰다 | 통과 |
|---|---|---|
| 가) 빈 DB 에서도 앱 밖이 의심 판을 말함 · 성한 DB 에서도 같은 말 · 같은 판 · 실제로 빈 DB | 2-3 — `test44_e01`(빈 DB 를 만들어 자식 프로세스로 · 같은 시험에서 성한 DB) · `e02` · `e03` · 고장 ② | O |
| 나) 앱 안과 밖이 같은 말 · 판정 하나를 코드에서 끌어내 | 2-4 — `test44_e04`(`의심말들` 결과와 알림 행 · 앱 밖 줄을 견줌) · `e05`(문장이 한 곳) · 고장 ⑤ | O |
| 다) 백업 알림은 푸시 안 되고 다른 알림은 됨 · 같은 판 | 3-2 — `test44_f01` · 고장 ① ④ | O |
| 라) pytest 옵션 없이 전부 · 글검사 7/7 | 9장 · 6장. 얼린 판 없음 | O |
| 마) 검토자 원문 | 13장 | O — **원문을 실은 뒤에 검사와 시험을 돌렸습니다** |

---

# 9. 시험

```
1500 passed, 3 warnings in 1218.83s (0:20:18)
```

**이 보고를 다 쓴 뒤 옵션 없이 처음부터 끝까지 돌린 판입니다.** 그전에 보고보다 먼저 시작한 전체 시험 하나를
`scripts/내프로세스.py "-m pytest" --멈춤` 으로 멈췄습니다 — 고른 다섯이 전부 이 창의 것이었고 밖은 0개였습니다(exit 15).

---

# 10. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| ① | 앱 밖에 알림 자리 · 백업 폴더를 앱 밖에서 읽는다 | **했다** — 2장 |
| ② | 새벽 백업 알림은 푸시로 안 | **했다** — 3장 |
| ③ | 11-3 의 금지에 예외 없음 | **했다** — 4장 |
| 1-a | DB 를 안 열고 백업 폴더만 · 사람이 어떻게 보나 | **했다** — 2-1. 다만 `app.config` 가 폴더·서명키를 만들 수 있음(3-3) |
| 1-b | 늘 지나는 자리 · 되살아나나.ps1 먼저 | **했다** — 새 자리 없이 거기에 |
| 1-c | DB 가 비어도 · 실제 빈 DB 로 · 운영 말고 | **했다** — 2-3 · tmp_path |
| 1-d | 앱 안과 같은 말 · 판정 하나 | **했다** — 2-4 |
| 1-e | 언제 보나 · 사고 뒤면 늦다고 | **했다** — 2-2 · CLAUDE.md 11-2 |
| 2-a · 2-b | 백업 알림만 푸시를 끈다 | **했다** — 3-1 |
| 2-c | 같은 판에서 둘 다 잰다 | **했다** — 3-2 |
| 2-d | 앞으로의 기준 한 줄 | **했다** — 3-1 |
| 3-a | 11-3 에 못박고 왜인지 | **했다** — 4-0 |
| 3-b | `-p no:cacheprovider` 없이 되나 | **했다** — 4-1. 고장 일곱을 `-p` 없이 |
| 3-c | 기계가 보나 · 못 보면 못 본다고 | **했다** — 4-2 |
| 4-a | phone.js 둘 · 개발 DB 계정 길 · 안 되면 봐둘것에 | **다르게 했다** — 계정 길을 실제로 시도하지 않고 로그인 칸에서 막힌다는 것을 확인해 봐둘것 AY-a 에 적음(4-1) |
| 4-b | 인계를 지금 사실로 · 실타래 닫음 | **했다** — 5장 |
| 4-c | 사람이 정한 넷과 회차 넘나드는 시험을 한 곳에 | **했다** — 인계 맨 앞 표 |
| 가)~마) | 수용 기준 | **했다** — 8장 |
| (끝줄) | 덩어리와 보고에 실명·전화·계좌·토큰·비밀번호·바깥 주소 안 적음 · 검토자 원문 그대로 | **했다** |
| (끝줄) | 운영 자료는 읽기만 · 백업을 손으로 안 지움 | **했다** — 운영 DB·백업 폴더에 안 씀. ps1 을 돌린 전후 파일 수 같음 |
| (끝줄) | 앱을 고쳤으면 PID | **했다** — 7장 |
| (끝줄) | 마무리는 11-3 | **했다** — pytest 앞에 아무것도 안 끼움 |

---

# 11. 커밋

커밋은 나눕니다 — 백업 알림 문장·푸시 / 앱 밖 자리 / `-p` 가드 / 시험 / 문서 / 원문의 퍼센트 규칙, 그리고 이 보고.
해시는 푸시한 뒤 덩어리 첫 줄에 적습니다(이 파일은 그 커밋 안에 들어가 자기 해시를 모릅니다).

---

# 13. 검토자 원문

**커밋 전 검토를 받았고 답을 통째로 싣습니다.** 한 글자도 안 가렸습니다.

| 라벨 | 무게 | 어떻게 |
|---|---|---|
| [A] | 고쳐야 함 | 고쳤다 — CLAUDE.md 11-3 이 앞 판의 `-p no:cacheprovider` 를 밝히고 이 판의 고장 일곱을 근거로 삼음(덮이는 보고를 안 가리킴) |
| [B] | 고쳐야 함 | 고쳤다 — 가드에 ini `addopts` · `PYTEST_PLUGINS` · g01 에 두 입구 · 못 보는 목록에 entry point |
| [C] | 봐 둘 것 | 그대로 — 가드 판정이 pytest 의 preparse 와 같은 꼴 |
| [D] | 고쳐야 함 | 고쳤다 — e01 이 성한 DB 에 `알린다` 를 다시 받음 |
| [E] | 고쳐야 함 | 고쳤다 — 14장 되살아나나 줄 |
| [F] | 봐 둘 것 | 고쳤다 — try/finally · 환경변수를 안 건드림 |
| [G] | 봐 둘 것 | 적었다 — 11-2 · AZ-c · 사람칸 「정할 것」 ① |
| [H] | 봐 둘 것 | 고쳤다 — notify 독스트링 |
| [I] | 봐 둘 것 | 사람칸 「정할 것」 ② |
| [J] | 막아야 함(조건부) | 이 보고를 쓰고 글검사·전체 시험을 다시 돌림 · 재시작함 |

```
━━━ 검토자 여기부터 ━━━ 범위 9cf49c1..작업 트리(커밋 안 함) · 커밋 0개 · 파일 9개

돌려 본 것
  pytest tests/test_stage44.py (옵션 없음)  -> 25 passed
  scripts/글검사.py                           -> 7/7 exit 0 (최근.md 는 아직 앞 판의 것이라 참고만)
  powershell -File scripts\되살아나나.ps1     -> 끝까지 돌았음. 빈 판 줄이 나옴.
     data/backups 파일 수는 돌리기 전과 뒤가 같음(38 -> 38). rows.json 이 새로 안 생김
  바탕: pytest 9.1.1 의 _pytest/config/__init__.py 를 읽음(consider_preparse · _preparse)

[A] 고쳐야 함 — CLAUDE.md 가 없는 사실을 말한다 (3-b)
  CLAUDE.md 3764 줄 근처: 「고장을 심는 판도 -p 없이 돌았습니다(그날 ⓪ 판)」
  ⓪ 은 앞 판 보고(docs/review/최근.md 168 · 326 줄)에서 「안 심은 사본」 이다.
  같은 보고 171 · 321~326 줄은 고장을 심은 사본 여섯을 -p no:cacheprovider 로
  돌렸다고 적었다. 그러니 이 문장은 그 보고와 거꾸로 말한다.
  그리고 가리키는 곳이 최근.md 인데, 그 파일은 이 판이 덮는다. 얼린 판도 없다
  (2026-09-13-* 셋 중 ⓪ 이 든 파일이 없음). 11-3 4단계의 「덮이는 판을 가리키지 않는다」 에 걸린다.
  「-p 없이도 된다」 를 받쳐 주는 근거는 이 판 구현자가 한 일이다
  (고장 다섯을 -p 없이 심었다). 그것을 이 판 보고에 적고, 그 보고를 가리켜야 한다.

[B] 고쳐야 함 — 3-c 「기계가 못 보는 것」 목록이 모자란다
  CLAUDE.md 「기계가 보는 것은 -p 하나입니다」 문단과 14장 표의 새 줄,
  tests/conftest.py 75 줄.
  pytest 9.1.1 이 conftest 보다 먼저 플러그인을 읽는 입구가 둘 더 있는데,
  둘 다 conftest 에서 읽을 수 있다. 그러니 「conftest 가 알 길이 없다」 로
  묶이지 않는다.
    - ini 의 addopts (_preparse 1561 줄 · consider_preparse 1576 줄)
      pytest.ini 에 -p 를 적으면 invocation_params.args 에 안 나타난다.
      config.getini("addopts") 로 볼 수 있다
    - PYTEST_PLUGINS 환경변수 (consider_env 872~874 · 1586 줄)
  못 보는 것은 PYTHONSTARTUP · sitecustomize, 그리고 설치된 pytest11
  entry point(1583 줄)다.
  둘 중 하나를 고르면 된다. 가드에 넣어 막거나, 문서의 「못 봅니다」 에 적는다.
  지금은 「명령줄과 PYTEST_ADDOPTS 만 본다」 는 문장만 맞고, 빠진 입구는 아무 데도 안 적혀 있다.

[C] 봐 둘 것 — g01 과 실제 명령줄 파싱이 어긋나는지 (물은 것)
  pytest 자신의 consider_preparse 는 `opt == "-p"` 이거나 `opt.startswith("-p")`
  일 때만 플러그인을 읽는다. conftest 75 줄의 판정도 같은 모양이다.
  - `--plugin` 같은 긴 이름은 pytest 9 에 없다. `--` 로 시작하는 것을 빼도 새는 곳이 없다
  - `-qp x` 처럼 붙여 쓰면 가드가 못 잡는다. 그런데 pytest 도 그 꼴로는
    플러그인을 안 읽으므로 둘이 같은 결론이다
  - 인자 값으로 들어간 "-p…"(예: -k 의 값)는 오탐이 난다. 이것도 pytest 의
    preparse 와 똑같이 오탐하므로, 멈추는 쪽으로 틀리는 것이라 괜찮다
  g01 은 짧은 꼴 셋 · 붙인 꼴 · PYTEST_ADDOPTS 로 막히는지를 보고, 옵션 없음과
  --pdb 로 통과하는지도 본다. ①② 는 갖췄다. ③ 은 이 판에 심은 고장(가드 끔)으로
  잡혔다고 구현자가 적었다. [B] 의 두 입구는 시험에 없다.

[D] 고쳐야 함 — e01 의 「성한 DB」 절반에 낡은 변수가 섞였다 (물은 것)
  tests/test_stage44.py e01 의 성한 쪽 단언: `assert 수 == 0 and any(...)`
  여기서 `수` 는 빈 DB 에 알린다 를 부른 결과가 그대로 남은 것이다. 성한 DB 에
  대해서는 아무것도 재지 않는데, 읽으면 「성한 DB 에서도 앱 안이 0」 처럼 읽힌다.
  그래도 실제로 견주기는 한다. 빈 쪽은 subprocess 에 DCB_DATABASE_URL 을 빈 DB 로
  주고, 성한 쪽은 conftest DB(관리자와 회차가 있음)에서 in-process 로 부른다.
  살핀다 가 DB 를 읽게 망가지면 두 결과가 달라져 잡힌다. 그러니 수용 가) 는
  잰 것이 맞다.
  다만 성한 쪽에서 앱 안 알림이 서는지는 e01 이 안 잰다. e04 · f01 이 잰다.
  e01 docstring 의 「같은 판에서 둘 다 본다」 는 앱 밖 자리만 두고 한 말로 좁히거나,
  `수` 를 성한 DB 에 대고 다시 받는 것이 맞다.

[E] 고쳐야 함 — 한 곳을 고치고 옮겨 적은 곳이 남았다
  CLAUDE.md 4172 줄(14장 되살아나나.ps1 줄):
  「파이썬이 아닌 것은 재는 것이 전부 작업 스케줄러·서비스·포트라서다」
  이 판에서 그 스크립트가 .venv 파이썬으로 backup.py --살핀다 를 부르므로
  이 문장은 이제 틀렸다. 11-2 에는 새 문단을 더했는데 이 줄은 안 고쳤다.

[F] 봐 둘 것 — 되살아나나.ps1 의 인코딩 복원 (물은 것)
  scripts/되살아나나.ps1 131~138 줄
  - $ErrorActionPreference='Continue' 이고 & $py ... 2>&1 이다. 파이썬이 0 이 아닌
    값으로 끝나거나 stderr 를 내도 오류가 스크립트를 끝내지 않으므로 복원 줄까지 간다.
    실행 파일이 없는 경우는 Test-Path 가 먼저 막는다.
    그래서 보통의 실패 경로에서는 복원된다
  - try/finally 가 아니다. Ctrl+C 로 끊거나 스크립트를 끝내는 오류가 나면 콘솔
    코드페이지가 UTF-8 로 남는다. 이 값은 창에 붙은 콘솔 전체의 설정이라
    -File 로 불러도 부른 창에 남는다
  - $env:PYTHONIOENCODING 은 되돌리지 않는다. -File 로 부르면 자식 프로세스 안에서
    끝나지만, 열린 PowerShell 창에서 .\scripts\되살아나나.ps1 로 부르면 그 창의
    세션에 남는다. 머리 주석 3 줄 「아무것도 안 바꿉니다」 와 부딪힌다
  - backup.py 가 이미 stdout 을 utf-8 로 reconfigure 하므로(21~27 줄)
    PYTHONIOENCODING 은 없어도 된다
  고치는 방법: try { ... } finally { 둘 다 되돌림 } 한 겹이면 된다.

[G] 봐 둘 것 — 「DB 를 안 연다 · 읽기만」 이 조금 넓게 말한다
  scripts/backup.py 38 줄은 app.config 를 import 한다. app/config.py 21~24 줄은
  import 할 때 data/ · uploads/ 폴더를 mkdir 하고, 103 줄은 secret_key.txt 가
  없으면 새로 쓴다.
  지금 운영에는 그 파일이 있어 아무것도 안 쓴다(실행 전후를 확인했다).
  그런데 이 자리가 필요한 날은 「data 가 통째로 비었다」 는 날일 수 있다. 그날
  --살핀다 를 부르면 새 서명키가 생긴다.
  DB 는 안 여는 것이 맞다. 옆 파일(rows.json)을 안 쓰는 것도 e03 과 실측으로 맞다.
  CLAUDE.md 11-2 의 「읽기만」 을 문자 그대로 받으면 틀린다.

[H] 봐 둘 것 — 「…하나다」 꼴 (검토-원칙 7 · 10장)
  app/notifications.py notify docstring: 「지금 끈 것은 새벽 백업 알림 하나다」
  push=False 를 쓰는 곳이 하나 늘면 곧바로 거짓이 된다. 끄는 곳은
  `push=False` 로 찾으라고 적는 쪽이 낡지 않는다.

[I] 봐 둘 것 — 4-a 를 못 한 까닭의 경계 (사람이 정할 일)
  docs/봐둘것.md 2897 줄 근처. 적힌 사실은 맞다(scripts/devserve.bat 4~5 줄:
  %TEMP%\dcb-dev · DCB_SECRET_KEY=dev-only-secret).
  다만 「작업하는 창은 계정이 누구의 것이든 비밀번호를 칸에 넣지 않는다」 는
  CLAUDE.md 11-2 에 이 넓이로 적혀 있지 않다. 11-2 가 막는 것은 「사람만 쥘 수 있는
  값」 이다. 개발 DB 에 스스로 만든 버리는 계정의 비밀번호가 여기에 드는지는 해석이 갈린다.
  구현자는 넓게 읽었다. 그 해석을 규칙으로 둘지 사람이 정해야 한다.

[J] 막아야 함 (조건부) — 보고가 아직 없다
  docs/review/최근.md 는 앞 판의 것 그대로다(사람칸 · 지시문 대조 · 「정할 것」
  모두 앞 판 내용). 구현자가 검토 뒤에 쓴다고 했으므로 지금은 결함이 아니다.
  이대로 커밋하면 검토-원칙 8-1 · 9 에 걸린다. 그리고 [A] 가 가리키는 ⓪ 이
  덮여 사라진다.
  보고를 쓴 뒤에는 11-3 에 따라 글검사와 옵션 없는 전체 pytest 를 다시 돌린다.
  app/ 과 scripts/ 가 바뀌었으니 재시작도 한다(app/notifications.py 가 바뀜).

없음으로 확인한 것
  - 판정 한 곳(1-d · 나): 제목 글자는 scripts/backup.py 에 한 번만 있다. 앱 안 알림
    (알린다)과 앱 밖(살핀다)이 둘 다 의심말들 을 부른다. e04 는 알림 행과 살핀다 의
    줄을 의심말들 의 결과와 글자 단위로 견주고, e05 는 문장이 한 곳에만 있는지 센다
  - 푸시(다): f01 은 push.push_notifications 를 가로챈다. 앞은 _try_push 안에서
    import 하므로 가로채기가 실제로 먹는다. 같은 판에서 백업 알림은 안 나가고
    「기한 초과」 는 나가는 것을 둘 다 잰다
  - test_stage42 의 가드 시험과 새 -p 가드가 부딪히지 않는다(subprocess 로 pytest 를 안 띄운다)
  - 인계의 18 · 8 · 21 · 22 · 26 번이 실제로 있다. 봐둘것 AZ-c 는 머리에 「처리함」 이 있고
    본문에 이 판의 결론이 붙었다
  - 옛말.md 에 새로 적은 두 표기는 살아 있는 문서에 남은 곳이 없다. 봐둘것 3117 줄의
    「웹 푸시로도 나가려 한다」 는 시점이 적힌 기록이고, 바로 아래에 이 판의 결론이 있다
  - 실명 · 바깥 주소 · 비밀번호 원문: 새로 들어온 것 없음

사람에게 묻는 것
  1. [B] ini addopts 와 PYTEST_PLUGINS 로 들어오는 플러그인도 conftest 가드로 막을지,
     아니면 「못 본다」 목록에만 적을지
  2. [G] 앱 밖 자리가 app.config 를 import 해 폴더를 만들고, 서명키가 없으면 새로
     쓸 수 있는 것을 받아들일지. 안 받아들이면 살핀다 쪽이 app.config 없이 경로를 잡아야 한다
  3. [I] 「작업하는 창은 개발 DB 의 버리는 계정이라도 비밀번호를 칸에 넣지 않는다」 를
     규칙으로 둘지. 두면 phone.js 의 안 심은 고장 둘은 사람이 들어가 준 창에서만 잴 수 있다
━━━ 검토자 여기까지 ━━━
```

**검토자가 물은 셋 중 1 은 이 창이 가드에 넣어 닫았습니다**([B] · 되돌리기 쉬운 쪽 — 막히는 입구가 늘 뿐). 2 · 3 은 사람칸에.
