사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: 가려진 탭에서 돈 판을 이번 점검으로 받을지 (2장) · 보드의 본문 누름을 목록의 행(button)까지 넓힐지 (봐둘것 BE-d)
막혀서 못 한 것: 없음 — 두 계정 모두 사람이 들어가 준 창에서 돌았다 (2장)

작성 시각: 2026-09-17 19:40 (KST)
커밋: 87a66f1 다음 — 이 판의 커밋들(7장 파일 목록)
작업: phone.js 거짓 실패 수정 — 보드 · 달력에 main 추가 + 점검 스크립트 보강

---

**앱 코드를 고쳤다** — `app/templates/board.html` · `app/templates/calendar.html` · `app/static/css/retreat.css` (3장).
**운영 앱을 다시 켰다** — PID 2776 → 19996 (19:03:17 → 19:03:21 · 12-1 그대로 · 「성공 — PID 가 바뀌었습니다」).
돌고 있는 운영 서버가 새 CSS(`retreat.57af7ac5.css` 에 `.pgmain` 규칙)를 내보내는 것을 받아서 확인했다. 템플릿은 로그인해야 보여서 운영에서는 안 봤다 — 개발 서버에서 봤다(2장).
**운영 DB 는 안 건드렸다** — 수정 시각 15:20:10 · 활동 기록 66 · 지출 109 그대로(재시작 뒤에도).

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | CSS 의 `main` 선택자는 `main.wiz` 뿐이라 새 `main.pgmain` 과 안 겹친다. `board.js` · `calendar.js` · `drawer.js` 는 `main` 을 안 찾고, 구조로 부모를 따라가는 곳(`parentNode` 등)도 이 두 화면의 바깥 틀에는 없다. `docs/checks/phone.js` 가 `main` 을 찾는 자리는 본문 판정과 자가시험 ⑥ 둘이다 |
| 같은 부분 템플릿을 쓰는 화면 | 드로어(`partials/drawer.html`)는 main 밖에 두었다 — 목록은 드로어 자리가 행 안이라 이번과 무관. `tests/test_stage7.py` 의 `test7_c02`(드로어가 `#calswap` 밖) 는 그대로 통과 |
| 그 값을 세어 둔 문서 | 봐둘것 BE-a · CLAUDE.md 13장 · 인계 맨 위와 「남은 것」 표 — 셋 다 이번 판 사실로 맞췄다(4장 대조 표 4a · 4b) |

# 1. 1단계 — 앱에 main

- **1a · 1b** — 두 화면의 본문을 `<main class="pgmain">` 하나로 감쌌다. **보드는 도구줄 · 간트(`#board`) · 좁은 폭 목록(`#mlist`)**, **달력은 `calbar` 부터 날짜 없는 업무까지**. 드로어는 밖이다.
  지시문이 권한 「`div.mlist` 의 태그만 main 으로」 는 **하지 않았다** — 넓은 화면에서는 `.mlist` 가 `display:none` 이라 주 영역이 숨은 요소가 된다. `.mlist` · `#calswap` 의 클래스 · id 는 그대로라 CSS · JS 참조는 안 바뀐다.
- **CSS 한 줄** — `.pgmain{flex:1;min-height:0;display:flex;flex-direction:column}`. body 가 세로 flex 이고 안의 칸들(`.board` · `.mlist` · `.calwrap` 의 `flex:1`)이 그것을 전제하므로, 감싼 main 이 같은 모양을 물려받는다.
  넓은 창(1707px)에서 main 857px · 간트 810px · 달력 격자 813px 로 창 안에 들어왔고 drawer.js 가 전과 같은 수로 통과했다(2장).
- **1c** — `retreat_base.html` 에는 `main` 이 없고 **화면마다 자기 `main` 을 둔다**(설정 · 목록 · 재정 등). 껍데기에 두면 그 화면들의 `main` 과 겹치므로 **안 고쳤다**. `main` 이 없는 화면이 아직 넷 남았다(총무팀 일정 · 봉사자 시간표 · 회의록 목록과 상세) — 봐둘것 BE-c.

# 2. 2 · 3단계 — 점검 보강과 다시 재기

**2a · 2b** — `phone.js` 는 본문을 `본문후보 = ['main', '#mlist', '#calswap']` 순으로 찾고, 무엇을 골랐는지 라벨에 적는다(「본문이 그려졌다 (main)」). 자가시험 ⑥ 은 고른 본문 어디에나 심는다.
모든 판에서 라벨이 **(main)** 이었다 — main 이 여전히 먼저 잡힌다.
**대체 경로도 쟀다** — 부서 리더 창의 400px 틀 안에서 main 을 div 로 바꿔 돌리니 보드는 **(#mlist)**, 달력은 **(#calswap)** 을 골라 실패 0. 다만 `#mlist` 의 행은 `button` 이라 그 경우 보드의 본문 누름은 건너뛴다(main 이 있는 지금은 도구줄의 링크를 잰다).

**어떻게 돌렸나** — 사람이 들어가 준 Chrome 창(localhost:8001)에서, 저장소의 두 파일을 한 줄로 감싼 사본을 임시 정적 서버(127.0.0.1:8765)에서 script 태그로 실었다. phone.js 는 같은 페이지 안의 375 · 400px 틀에서 돌렸다. 개발 서버는 고친 뒤 다시 켰다(8001 을 쥔 PID 10008 · 데이터 폴더는 개발 경로 · psutil).
**탭이 가려져 있었다 — 11-3 의 전제(보이는 탭)를 다 지키지 못했다.** `document.hidden` 을 잰 판과 결과는 이렇다.
- **가려짐으로 잰 판** — 부서 리더의 phone.js 자가시험 세 화면 · 관리자의 drawer.js 세 화면 · 관리자의 phone.js 열두 판 중 여섯과 자가시험 세 화면(앞뒤로 잰 값이 둘 다 가려짐)
- **안 잰 판** — 부서 리더의 drawer.js 세 화면과 phone.js 여섯 판(그때는 재지 않았다 · 가려졌는지 모른다)
- 타이머를 바꿔치기하지 않았다. 가려진 탭에서 자가시험이 몇 분씩 느려졌을 뿐 모든 판이 완주했고 「못 잼」 은 0 이다 — **이것은 끊기지 않았다는 사실이지 가려진 탭이 괜찮았다는 증거가 아니다.** 받을지는 사람칸 「정할 것」

**3a — drawer.js** (1707px · 실패 0 · 못 잼 0 · 전부 완주)

| 계정 | 목록 | 보드 | 달력(7월 · 전체) |
|---|---|---|---|
| 관리자 | 93 · 건너뜀 5 | 80 · 건너뜀 3 | 83 · 건너뜀 4 |
| 부서 리더 | 79 · 건너뜀 17 (부서 전체 · 못 바꾸는 칸 4 항목 ✓) | 63 · 건너뜀 16 | 66 · 건너뜀 17 |

지난 판과 같은 수다 — main 으로 감싼 것이 상세 패널 조작에 닿지 않았다.

**3a · 3b · 3c — phone.js** (두 계정 × 세 화면 × 375 · 400px — 열두 판)

| | 항목 | 건너뜀 | 실패 | 본문 | 본문 링크 |
|---|---|---|---|---|---|
| 열두 판 모두 | 11 | 2 | 0 | ✓ (main) | ✓ 재짐 |

- 본문 링크로 잡힌 것 — 목록은 상태 칩, 보드는 「+ 업무 추가」, 달력은 「‹」. **보드에서 잰 것은 도구줄의 링크 하나이고 좁은 폭 목록의 행(button)은 아니다** — 봐둘것 BE-d
- **지난 판에 한 번도 안 재졌던 「본문의 링크가 눌리는 자리에 있다」 가 보드 · 달력에서 두 계정 모두 ✓** (지난 판 10 항목 · 건너뜀 3 → 11 · 2)
- 건너뜀 둘은 손가락 기기 조건(마우스 기기)이다
- **375px 도 쟀다**(지난 판은 400 만)

**phone.js 자가시험** (400px · 세 화면)

| 계정 | 목록 | 보드 | 달력 |
|---|---|---|---|
| 관리자 | 잡음 7 · 놓침 0 · 못 심음 2 | 잡음 7 · 놓침 0 · 못 심음 2 | 잡음 7 · 놓침 0 · 못 심음 2 |
| 부서 리더 | 잡음 7 · 놓침 0 · 못 심음 2 | 잡음 7 · 놓침 0 · 못 심음 2 | 잡음 7 · 놓침 0 · 못 심음 2 |

- 여섯 판 모두 「안 심으면 초록」 ✓
- **⑥ 「본문의 링크가 덮여 있다」 가 보드 · 달력에서도 심겼고 잡혔다** — 지난 판에는 그 두 화면에서 늘 못 심었다
- 못 심은 둘은 ⑧(손가락 기기 조건)과 ⑨(호버가 있는 기기의 가장자리 띠) — 마우스 기기에서는 그 자리가 없다(11-3 이 말하는 「그 화면에 없는 자리」)

# 3. 바뀐 것 · 시험

| 파일 | 무엇 |
|---|---|
| `app/templates/board.html` | 도구줄 · 간트 · 좁은 폭 목록을 `<main class="pgmain">` 으로 감쌈 |
| `app/templates/calendar.html` | `calbar` ~ 날짜 없는 업무를 `<main class="pgmain">` 으로 감쌈 |
| `app/static/css/retreat.css` | `.pgmain` 한 줄 |
| `docs/checks/phone.js` | `본문후보` · 고른 것을 라벨에 · 자가시험 ⑥ 을 고른 본문에 |
| `tests/test_stage60.py` | 새 시험 5 |

**시험 (`tests/test_stage60.py`)**
- `test60_a01` — 보드의 main 이 하나이고 도구줄 · 간트 · 좁은 폭 목록을 감싸며 드로어는 밖
- `test60_a02` — 달력의 main 이 `calbar` · `#calswap` 을 감싸고 드로어는 밖
- `test60_a03` — `.pgmain` 이 세로 flex 네 조각을 가짐
- `test60_b01` — phone.js 의 후보가 `main` 먼저 · `#mlist` · `#calswap` 을 담고, 옛 `querySelector('main')` 줄이 없음
- `test60_b02` — 자가시험 ⑥ 이 고른 본문에 심김

**고장을 심어 쟀다** — 여섯 다 잡았다(pytest 는 옵션 없이 그 파일 하나).

| 심은 고장 | 결과 |
|---|---|
| 보드의 여는 `<main>` 을 뺌 | 잡음 |
| 달력의 닫는 `</main>` 을 뺌 | 잡음 |
| 보드의 드로어를 main 안으로 | 잡음 |
| `.pgmain` 을 `flex:1` 만으로 | 잡음 |
| 후보를 `['main']` 만으로 | 잡음 |
| ⑥ 을 옛 `main a` 로 | 잡음 |

# 4. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) board.html · calendar.html 에 main 이 있다 | O | 1장 · `test60_a01` · `test60_a02` |
| 나) phone.js 가 두 화면에서 거짓 실패 없이 통과 | O | 2장 표 — 열두 판 실패 0 |
| 다) 본문 링크 덮임 검사가 보드 · 달력에서 실제로 돈다 | O | 2장 — 건너뜀 3 → 2, 항목이 ✓ 로 찍힘 · 자가시험 ⑥ 이 두 화면에서 심김 |
| 라) 375px 에서도 확인 | O | 2장 표 |
| 마) pytest 옵션 없이 전부 통과 · 글검사 7/7 | O | 7장 |
| 바) 검토자 원문이 최근.md 에 그대로 | O | 5장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 1a | 다르게 했다 | 태그를 바꾸지 않고 감싸는 main 을 새로 둠 — `.mlist` 만 main 이면 넓은 화면에서 주 영역이 숨는다 (1장) |
| 1b | 했다 | 클래스 · id 그대로 · CSS 한 줄 · drawer.js 가 같은 수로 통과 |
| 1c | 했다 | 껍데기에는 안 둠(겹침) · 남은 화면은 봐둘것 BE-c |
| 2a | 했다 | `main` → `#mlist` → `#calswap` |
| 2b | 했다 | 모든 판의 라벨이 (main) |
| 2c | 했다 | 보드 · 달력에서 본문 링크 항목이 재짐 |
| 3a | 다르게 했다 | 두 계정 × 세 화면 — drawer.js 실패 0 · phone.js 실패 0. 다만 11-3 이 전제로 둔 보이는 탭을 다 지키지 못했다(2장 · 사람칸) |
| 3b | 했다 | 보드 · 달력 본문 링크 ✓ |
| 3c | 했다 | 375 · 400 둘 다 |
| 4a | 했다 | BE-a 처리함 · 되살리기는 BE-b 로 옮김 · BE-c 새로 |
| 4b | 했다 | CLAUDE.md 13장 · 인계 맨 위와 「남은 것」 표 |
| 앱 코드를 고치면 재시작 · PID 확인 | 했다 | 2776 → 19996 |
| 운영 DB 는 안 건드린다 | 했다 | 머리 |
| 검토자 원문을 그대로 싣는다 | 했다 | 5장 |
| 마무리는 11-3 대로 | 했다 | 앞 판 보고를 `docs/review/2026-09-17-화면점검.md` 로 얼림 |

# 5. 검토자 원문

## 5-1. 커밋 전 검토

```
━━━ 검토자 여기부터 ━━━ 범위 87a66f1..(스테이징) · 커밋 0개(스테이징 1묶음) · 파일 10개

본 것
  git diff --cached 87a66f1 전부, retreat_base.html · retreat.css 의 body/820px/달력 규칙,
  board.js · calendar.js · drawer.js · docs/checks/drawer.js 의 main 참조,
  templates 전체의 <main 유무, tests/test_stage60.py 실행(5 passed · 옵션 없이 그 파일 하나).
  얼린 사본: git show 87a66f1:docs/review/최근.md 와 docs/review/2026-09-17-화면점검.md 가
  diff 로 한 글자도 다르지 않음(확인).

막아야 함
  없음.

고쳐야 함

  1. docs/review/최근.md 125줄 (지시문 대조 3a 「했다」) · 41줄
     11-3 2단계는 「탭이 보이는 상태여야 합니다」 를 점검의 전제로 적는다. 보고 41줄은
     가려진 탭에서 돈 판이 있다고 스스로 적었는데 표는 「했다」 다. 표의 판정이 본문과
     어긋난다(검토-원칙 8-1). 「다르게 했다」 와 까닭이 맞는 모양이다.
     또 41줄의 「부서 리더의 자가시험 · 관리자 판 전부」 가 무엇까지인지 흐리다 —
     관리자의 drawer.js 세 화면도 가려진 탭에서 돌았는지, 가려진 판에서 타이머를
     바꿔치기했는지(11-3 은 「느리다고 타이머를 대체하면 검사가 흔들린다」)를 한 줄로
     적어야 사람이 그 수를 믿을지 가를 수 있다. 「완주 · 못 잼 0」 은 가려진 탭이 괜찮았다는
     증거가 아니라 그 판에서 끊기지 않았다는 사실뿐이다.

  2. CLAUDE.md 3217줄 근처(11-2 「phone.js 에 아직 안 심은 자가시험 둘」) ·
     docs/인계.md 88 · 118 · 170줄
     「안 심은 둘 = 사이드바가 처음에 접혀 있는가 · 그 안의 링크가 눌리는가」 인데,
     phone.js 고장들에는 87a66f1 에도 이미 「③ 사이드바 링크가 안 눌린다」 가 있다
     (docs/checks/phone.js 313줄 · 87a66f1 판 308줄). 안 심은 것은 「처음에 접혀 있다」
     하나로 보인다. 그리고 「이 창 혼자서는 못 돌린다 · 사람이 들어가 준 창에서 돌릴 수
     있다」 는 이미 앞 판과 이번 판에서 그 길로 자가시험을 돌렸다.
     이 판이 만든 어긋남은 아니지만 지시문 4b(「CLAUDE.md·인계를 최종 사실로」)의 범위라
     짚는다. 내가 틀렸을 수 있으니(③ 이 그 문장이 말하는 「링크」 와 다른 것이라면)
     구현자가 근거로 답하면 된다.

봐 둘 것

  3. docs/checks/phone.js 135~141줄 · app/templates/board.html 16~18줄
     보드에서 「본문의 링크」 로 잡히는 것은 도구줄의 「+ 업무 추가」 하나다(보고 58줄이
     정직하게 적음). 좁은 폭 목록의 행은 button 이라 a[href] 로는 안 잡힌다. 그래서
       - 목록 영역만 덮는 회귀(도구줄 아래를 덮는 층)는 보드에서 안 걸린다
       - 열람 전용 계정은 is_readonly 로 그 링크가 없어 보드에서 다시 「건너뜀」 이 된다
     건너뜀은 줄로 찍히고 ⑥ 은 「못 심음」 으로 빠지므로 조용한 초록은 아니다 —
     대체 경로(#mlist)도 같다(행이 전부 button). 다만 「보드에서 본문 누름이 재졌다」 는
     「도구줄의 링크 하나가 재졌다」 이지 목록이 재진 것이 아니다. 후보를 a[href] 밖
     (button 등)으로 넓힐지는 사람이 정할 일.

  4. docs/checks/phone.js 57줄 (본문후보 = main, #mlist, #calswap)
     세 화면 모두 main 이 생겨 #mlist · #calswap 갈래는 지금 앱에서 도는 자리가 없다
     (main 없는 화면은 live · staff_sheet · meetings · meeting_detail 이고 거기엔 둘 다 없다).
     구현자가 main 을 div 로 바꿔 한 번 쟀다고 적었으니 죽은 코드는 아니지만, 다음에
     그 갈래가 깨져도 평소 점검으로는 드러나지 않는다. BE-c 로 main 을 채워 가면 이 목록은
     걷어도 되는 자리다.

  5. tests/test_stage60.py 50~60줄 (b01 · b02)
     phone.js 의 글자를 찾는 시험이라 10장의 「코드와 설명을 못 가린다」 에 걸린다 —
     "본문후보.find(" · "본문후보.map(q => q + ' a')" 는 주석에만 남아도 초록이다.
     pytest 가 JS 를 못 돌리니 실제 지킴이는 자가시험이고, 이 시험은 보조라는 것을
     알고 두면 된다. a01~a03 은 렌더된 화면과 CSS 규칙을 보고 드로어가 밖인 것까지 봐서
     볼 것을 본다(고장 여섯 심기도 보고에 있음).

  6. CSS 판단 — 전과 같게 둔다고 본다(근거)
     body 는 height:100% · 세로 flex · 넓은 폭 overflow:hidden / 820px 아래 overflow:auto
     (retreat.css 115~118 · 936). .pgmain(288줄)이 flex:1 · min-height:0 · 세로 flex 라
       - 보드 넓은 폭: .board{flex:1;overflow:auto} 가 main 안에서 전과 같이 남는 높이를 채운다
       - 보드 좁은 폭: .mlist{flex:1;overflow:auto} 도 같다 · 붙는 머리(.mgroup>h3)는
         .mlist 가 스크롤 상자라 main 에 안 갇힌다
       - 달력: #calgrid 가 block 이라 .calwrap · .calweeks 의 flex:1 은 전에도 뜻이 없었고
         지금도 없다. 좁은 폭에서 내용이 main 상자를 넘치지만 overflow 가 visible 이라
         body 스크롤에 그대로 잡힌다 · 달력에 main 기준으로 붙는 sticky 는 없다
       - 형제 선택자(.toolbar + … 등)·body > 선택자·JS 의 부모 따라가기는 없다(grep)
       - CSS 의 main 선택자는 main.wiz 뿐(761 · 952)
     참고: 넓은 폭 달력은 body 가 overflow:hidden 이라 격자가 창보다 길면 잘리는데,
     이것은 이 판 전부터의 모양이고 이 판이 바꾸지 않았다.

  7. 드로어를 main 밖에 둔 것 · 1a 를 「다르게 했다」 로 둔 것
     둘 다 근거가 맞다. .mlist 는 넓은 폭에서 display:none(428줄)이라 그것만 main 이면
     주 영역이 숨는다. 드로어는 position:fixed(451줄)라 밖에 둬도 배치가 안 바뀌고,
     test7_c02(드로어가 #calswap 밖)와도 맞는다. 까닭이 표에 적혀 있다.

  8. docs/인계.md 3줄 「탭이 가려진 채 돈 판이 있다(보고)」
     「보고」 는 다음 판이 덮는 최근.md 를 가리킨다. 다음 판이 얼리면 그 이름을 적어 주는 것이
     0장·11-3 의 「덮이는 최근.md 가 아니라 얼린 판을 가리킨다」 와 맞다.

  9. docs/review/최근.md 제목 번호
     0 · 1 · 2 · 3 · 4 · 6 · 7 · 8 — 5장이 없다. 빠진 것인지 번호만 건너뛴 것인지 읽는 사람이
     멈춘다. 6 · 7 · 8 의 ⟪…⟫ 자리는 보고가 스스로 「커밋 전에 채운다」 고 했으니 채운 뒤
     grep 으로 0 을 보는 것까지가 그 일이다(check_placeholder 는 이 꼴을 못 본다).

서로 어긋나는가
  봐둘것 BE-a(처리함) · BE-b · BE-c, CLAUDE.md 13장(4457줄), 인계 3 · 269줄, 보고 1~2장이
  서로 같은 사실을 말한다. BE-c 의 「main 이 없는 화면」 목록(총무팀 일정 · 봉사자 시간표 ·
  회의록 목록과 상세)은 templates 를 훑은 결과(live · staff_sheet · meetings ·
  meeting_detail)와 맞고, BE-c 는 개수를 세지 않고 찾는 법으로 적었다.
  CLAUDE.md 14장 phone.js 줄은 「본문이 그려졌는가」 로 적혀 있어 이번 변경과 부딪히지 않는다.
  어긋남은 위 2번(11-2 · 인계의 「안 심은 둘」) 하나.

지시문 대조 표 (검토-원칙 8-1)
  1a~1c · 2a~2c · 3a~3c · 4a · 4b 가 전부 있다. 1a 「다르게 했다」 는 까닭이 있다.
  3a 판정은 위 1번.

사람칸 (검토-원칙 9)
  보고 1~5줄에 다섯 줄이 순서대로 있고 네 값이 비지 않았다.
  덩어리는 검토자가 못 본다 — 확인하지 않았다.

실명 · 계정 값 · 바깥 주소
  diff 와 보고에 실명·비밀번호·계좌·전화·바깥 주소 없음. 나오는 주소는 localhost:8001 ·
  127.0.0.1:8765 뿐이다.

사람에게 묻는 것
  ㄱ. 가려진 탭에서 돈 판(관리자 전부 · 부서 리더 자가시험)을 이번 판의 점검으로 받을지,
      보이는 탭으로 다시 돌릴지 (11-3 이 전제로 적은 조건이다)
  ㄴ. 보드의 본문 누름이 도구줄 링크 하나로만 재지는 것(열람 전용 계정은 건너뜀)으로
      충분한지, 목록의 행(button)까지 재도록 phone.js 를 넓힐지
  ㄷ. 11-2 · 인계의 「안 심은 자가시험 둘」 을 이번 판에서 사실(하나 · 이미 돌린 길)로
      고칠지, 다음 판으로 미룰지
━━━ 검토자 여기까지 ━━━
```

## 5-2. 검토 뒤 고침을 다시 본 것

```
━━━ 검토자 여기부터 ━━━ 검토 뒤 고침 다시 보기 · main 판 · 범위 87a66f1..(스테이징) · 파일 10개

본 것
  git diff --cached 87a66f1 을 다시 봤다(앱 · phone.js · 시험 · CLAUDE.md 는 앞 검토 때와 같다).
  바뀐 것은 docs/review/최근.md(사람칸 · 2장 · 3a · 장 번호 · 6장 표) · docs/봐둘것.md(BE-d 새로) ·
  docs/인계.md(맨 위) 이다. [2] 의 근거는 봐둘것 AY-a 본문과 `git log -S` 로 대조했다.

지적이 닫혔나
  [1] 닫힘. 3a 가 「다르게 했다」 이고, 2장이 가려짐으로 잰 판 · 안 잰 판 · 타이머를 안 바꾼 것을
      나눠 적었으며, 「완주는 괜찮다는 증거가 아니다」 도 적었다. 받을지는 사람칸 「정할 것」 에 있다.
  [2] 안 고친 근거를 받는다 — 내 지적이 틀렸다. AY-a(봐둘것 2868 절)는 안 심은 둘을
      「처음에는 접혀 있다」 · 「사이드바 링크가 눌리는 자리에 있다」 로 이름을 들어 적었고,
      고장 ③ 이 기다리는 줄은 「그 링크를 누르면 화면을 옮기려 한다」 로 다른 항목이다.
      11-2 · 인계의 「안 심은 둘」 은 지금도 맞다.
  [3] · [4] 닫힘. BE-d 에 두 내용이 다 들어갔고, 넓힐지는 사람칸에 올라갔다.
  [5] 받는다(보조 시험이라는 것을 6장에 적었다).
  [8] 인계 맨 위는 고쳤다. 다만 아래 「새로 생긴 것」 2 를 본다.
  [9] 장 번호는 0~7 로 이어졌고, 머리 8줄의 「7장 파일 목록」 과 대조 표의 「5장」 · 「7장」 이
      새 번호와 맞는다.

새로 생긴 것 · 남은 것

고쳐야 함

  1. docs/봐둘것.md 3801줄 BE-d 제목 「사람이 정함 · 낮음」
     본문 끝줄은 「넓힐지는 사람이 정한다」(아직 안 정함)인데, 이 저장소에서 「사람이 정함」 은
     「이미 정했다」 를 뜻한다(같은 파일 3753 · 3754줄). 제목과 본문이 다른 것을 가리키면
     정해진 줄 알고 넘어간다(11-3 3단계 「라벨과 본문이 다른 것을 가리키게 두지 않는다」 · AC-c 의
     그 모양). 「사람이 정할 것」 같은 꼴이면 된다.

  2. docs/review/최근.md 229줄 · 6장 [9] 「채울 자리는 커밋 직전 grep 으로 0 을 본다」
     5-1 에 실은 내 원문 229줄에 「⟪…⟫」 글자가 들어 있다. 그래서 다 채워도 「⟪」 grep 은 0 이
     아니라 1 이 나온다. 원문은 고치면 안 되니, grep 을 5-1 밖으로 좁히거나 「원문 한 줄만 남는다」
     로 적어야 한다. 그러지 않으면 0 을 봤다는 기록과 실제 결과가 갈리거나, 원문을 손대는 쪽으로
     끌린다.

봐 둘 것

  3. docs/봐둘것.md BE-d 둘째 점 「그 판 보고 2장」 · docs/인계.md 3줄 「그 판의 보고 2장 ·
     다음 판이 날짜 이름으로 얼린다」
     인계 쪽은 [8] 을 받아 「얼린다」 까지 적었다. BE-d 는 「그 판 보고」 만 있어 덮이는 최근.md 를
     가리킨다. 다음 판이 얼릴 때 두 자리 모두 얼린 이름으로 바꾸면 된다. 지금 막을 일은 아니다.

  4. docs/봐둘것.md BE-a 처리함 줄 · docs/인계.md 3줄 · CLAUDE.md 13장
     셋 다 「두 계정 · 세 화면 · 실패 0 · 닫았다」 로 적었다. 그런데 그 판들 가운데 가려진 탭에서 돈
     판을 받을지는 사람칸에 열려 있다. 인계는 「가려진 채 돈 판이 있다」 까지는 말하지만, 그것이
     「정할 것」 으로 열려 있다는 말은 없다. 사람이 「안 받는다」 로 정하면 BE-a 의 「처리함」 과
     13장의 「닫았습니다」 를 되짚어야 한다. 인계에 「받을지는 정할 것」 한 마디를 두면 새 채팅이
     놓치지 않는다.

그 밖
  사람칸 다섯 줄은 순서대로 있고, 「정할 것」 에 두 물음이 들어가 비지 않았다.
  지시문 대조 표의 항목은 빠진 것이 없다.
  실명 · 계정 값 · 바깥 주소는 새로 들어오지 않았다.

사람에게 묻는 것
  ㄱ. 가려진 탭에서 돈 판을 이번 점검으로 받을지(받지 않으면 BE-a · 13장 · 인계를 다시 연다)
  ㄴ. 보드의 본문 누름을 목록의 행(button)까지 넓힐지(BE-d)
━━━ 검토자 여기까지 ━━━
```

# 6. 검토 뒤 고침

| 검토 | 한 것 | 파일 |
|---|---|---|
| [1] | 대조 표 3a 를 「다르게 했다」 로. 2장에 가려짐을 잰 판 · 안 잰 판 · 타이머를 안 바꾼 것을 나눠 적고, 받을지를 사람칸 「정할 것」 에 올렸다 | 최근.md |
| [2] | **안 고침 — 근거.** 자가시험 ③(09-12 에 파일과 함께 생김 · `git log -S`)이 기다리는 줄은 「그 링크를 누르면 화면을 옮기려 한다」 이고, 11-2 가 말하는 둘은 「처음에는 접혀 있다」 · 「사이드바 링크가 눌리는 자리에 있다」(봐둘것 AY-a 의 그 절이 이름으로 적어 둠)다. 고장들에 그 둘을 기다리는 갈래는 지금도 없다. 「사람이 들어가 준 창에서 돌리는 길」 은 11-2 가 이미 「남습니다」 로 적어 두었다 | — |
| [3] · [4] | 봐둘것 BE-d 를 새로 두었다(보드의 본문 누름은 도구줄 링크 하나 · 열람 전용은 건너뜀 · 대체 갈래는 평소에 안 돈다). 넓힐지를 사람칸 「정할 것」 에 올렸다. 2장에도 한 줄 | 봐둘것.md · 최근.md |
| [5] | 안 고침 — b01 · b02 는 글자를 보는 보조이고 실제 지킴이는 자가시험이라는 것을 여기 적어 둔다 | — |
| [6] · [7] | 고칠 것 없음 | — |
| [8] | 인계 맨 위의 「(보고)」 를 「그 판의 보고 2장 · 다음 판이 날짜 이름으로 얼린다」 로 | 인계.md |
| [9] | 장 번호를 0 ~ 7 로 이었다(검토자 원문 5 · 이 장 6 · 검사 7). 채울 자리는 커밋 직전 5-1 · 5-2 원문 밖에서 grep 으로 0 을 본다(7장 · 원문에는 그 글자가 인용으로 남는다) |  최근.md |

검토의 「사람에게 묻는 것」 ㄱ · ㄴ 은 사람칸에 올렸다. ㄷ 은 위 [2] 의 근거대로 고칠 사실이 없다.


## 6-1. 두 번째 검토 뒤 고침

세 번째 검토는 없다(11-3).

| 두 번째 검토 | 고친 것 | 파일 |
|---|---|---|
| 새 1 | BE-d 제목을 「사람이 정할 것」 으로 — 「사람이 정함」 은 이미 정한 것으로 읽힌다 | 봐둘것.md |
| 새 2 | 6장 [9] 를 「5-1 · 5-2 원문 밖에서 grep」 으로 — 원문에 그 글자가 인용으로 남는다 | 최근.md |
| 새 4 | 인계 맨 위에 「가려진 탭 판을 받을지는 사람이 정할 것 · 안 받으면 BE-a 와 13장을 다시 연다」 | 인계.md |
| 새 3 | 안 고침 — BE-d · 인계가 가리키는 「그 판의 보고」 는 다음 판이 얼린 이름으로 바꿔 적는다 | — |

# 7. 검사 · 시험

**바뀐 파일** (`git diff --name-only --cached 87a66f1`)

```
CLAUDE.md
app/static/css/retreat.css
app/templates/board.html
app/templates/calendar.html
docs/checks/phone.js
docs/review/2026-09-17-화면점검.md
docs/review/최근.md
docs/봐둘것.md
docs/인계.md
tests/test_stage60.py
```

**글검사** — 7개 전부 통과(check_names · check_stale · check_named · check_counts · check_handoff · check_placeholder · check_orders)

**pytest** (옵션 없이 · 보고를 다 채운 뒤) — `1698 passed, 3 warnings in 604.40s (0:10:04)`
