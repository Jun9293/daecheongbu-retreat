사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: 하나 — 뺀 업무 드로어에도 상태 칩(「지연」)과 진단 패널(「진행 가능 · 마감에서 N일 경과」)이 그대로 뜬다. 한 줄 표시와 겹치지는 않는다. 뺀 run 을 진단·지연 판정에서 뺄지는 4-10 의 일이라 이 판에서 안 고쳤다 — 목록의 뺀 줄에서는 같은 까닭으로 배지를 이미 뺐다는 것도 함께 본다 (4장 · 8장 · 봐둘것 BC-c)
막혀서 못 한 것: 로그인한 화면을 열지 못했다(11-2) — 375px 는 그 자리에서 만든 앱이 낸 실제 JSON 과 저장소의 CSS·JS 를 붙여 쟀다 (4장) · 화면 점검(drawer.js · phone.js)은 이 줄기 전체에서 못 돌았다 — 로그인된 브라우저가 준비되면 한꺼번에 (인계 맨 위)

작성 시각: 2026-09-16 18:40 (KST)
커밋: e2f0e79 다음 — 이 판의 커밋들(5장 파일 목록)
작업: 노션 업무 들여오기 완전 마무리 — 남은 셋

---

**앱 코드를 고쳤고 재시작했다(PID 28816 → 12316). 운영 DB 는 안 건드렸다.**

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `/board/task/{run_id}`(`task_detail`)의 응답을 받는 곳은 `drawer.js` 셋(여는 자리 · 목록의 담당자 메뉴 · 상태 메뉴)이라고 적었는데 **넷이다** — 선행을 저장한 뒤 `task_detail` 을 그대로 돌려주는 자리(`board.py` 의 선행 저장)를 `drawer.js` 가 받아 `detail` 을 바꾼다(커밋 전 검토가 짚음 · 그 자리는 연결 탭만 다시 그려 줄이 안 흔들린다). 새 칸 `included` 는 **더하기만** 했고 읽는 곳은 `renderDrawer` 하나다 — 옛 응답(칸 없음)은 산 것으로 본다. 새 클래스 `.dexcl` 은 저장소에 처음이다 |
| 같은 부분 템플릿을 쓰는 화면 | `partials/drawer.html` 은 **보드 · 달력 · 목록이 함께 쓴다**(4-13). 줄은 처음에 숨어 있고 `included` 가 거짓일 때만 켜지므로, 보드·달력에서 산 업무를 열면 아무것도 안 바뀐다. 뺀 업무를 `?task=` 로 열면 세 화면 모두에서 뜬다 — 같은 한 벌이라 맞는 모양이다 |
| 그 값을 세어 둔 문서 | CLAUDE.md 4-14(뺀 업무 줄) · 13장(노션 줄기) · 14장 목록 줄 · 인계 맨 위 · 봐둘것 BC-c |

# 1. 드로어의 한 줄 (1a–1d)

## 1a. 자리

**칩(`#dkick`) 바로 아래, 제목 위**에 「이번 회차에서 뺀 업무입니다」 한 줄을 둔다(`.dexcl`).

- 진단 패널(4-10 · 「진행 가능/불가」)은 드로어 **맨 아래 고정**이고, 옮긴 자리 줄(`.dmoved` · 6-4)은 **머리 표 아래**다 — 둘 다와 떨어진 자리다
- 닫기 단추(×)는 칩 줄 높이에 떠 있어서, 칩 **위**에 두면 오른쪽 끝이 그 단추 밑으로 들어간다. 칩 **아래**라 안 겹친다(4장에서 잼)
- 모양은 옮긴 자리 줄의 「안 따르는 쪽」(`.dmoved.off`)과 같은 옅은 면 · 헤어라인 · 14px(혼자 뜻을 지는 글자 하한) — 새 색이 없다
- 문장은 **템플릿에 글자로** 있고 JS 는 `hidden` 만 켜고 끈다

## 1b. 고치기는 그대로

`task_detail` 에 `included` 한 칸을 더했을 뿐 권한·편집 길은 안 건드렸다. 뺀 업무의 상태를 바꾸는 요청이 200 인 것을 시험이 잰다(`c01`).

## 1c. 375px

4장.

## 1d. 시험

`tests/test_stage58.py` 의 `c01`(같은 판에서 뺀 업무와 산 업무를 둘 다 열어 `included` 가 갈리는지 · 뺀 업무 상태 고치기가 열려 있는지) · `c02`(줄이 처음에 숨어 있고, 칩과 제목 사이에 있고, JS 가 `included` 하나로 켜고, 14px 인지 — 주석을 걷어내고 본다).

**산 업무에서 안 뜨는 것은 둘로 잰다** — 서버가 참을 내는 것(`c01`)과 줄이 처음에 숨어 있고 서버 값으로만 켜지는 것(`c02`). 브라우저에서 실제로 안 뜨는 것은 4장에서 쟀다(`display:none`).

# 2. 문서 (2a–2c)

- **2a** 봐둘것 BC-c 머리를 「줄기 완전히 마무리」 로 바꾸고, 마무리 판이 올린 정할 것 ①②③ 을 사람이 정한 대로 닫았다. 4장에서 본 「뺀 업무에도 지연 칩·진단 패널이 뜬다」 를 적어만 두는 줄로 더했다(사람칸 「정할 것」)
- **2b** CLAUDE.md 13장에 「이 줄기는 2026-09-16 에 완전히 마무리됐습니다 — 남은 것은 BC-c 에 적어만 둔 것과 한 번도 못 돈 화면 점검 둘」(검토 뒤 「…뿐」 을 고침 · 8장), 4-14 에 드로어 한 줄과 고치기를 안 막는다는 것, 14장 목록 줄에 `.dexcl` 을 적었다. 인계 맨 위에도 같은 말
- **2c** 인계 맨 위 둘째 줄에 「화면 점검(drawer.js · phone.js)은 이 줄기 전체에서 한 번도 못 돌았다 — 로그인된 브라우저가 준비되면 세 화면 · 두 계정으로 한꺼번에」 를 적었다
- 문서에 선택자를 id 표기로 적었다가 이름 검사(`check_named`)에 걸렸다 — 그 검사는 선택자를 글자 그대로 찾는데 저장소에는 `id="dexcl"` 과 `.dexcl` 만 있다. `.dexcl` 로 고쳤다

# 3. 사람이 정한 ① ③

- **①** 16px 은 여백이었다 — 앞 판에서 잰 43px 그대로이고 코드를 안 고쳤다. 이 판의 드로어 줄도 375px 에서 화면 가장자리부터 16px(드로어 안쪽 여백)이다
- **③** 결산 홈 문장 13px 은 그대로 — 코드를 안 고쳤다

# 4. 화면으로 잰 것 — 한계가 붙은 점검

**로그인한 화면을 열지 못했다**(11-2). 그래서 **앱을 그 자리에서 만들어**(임시 DB · 저장소에 적힌 지어낸 계정 — pytest 의 `login_as` 와 같은 조건이라 11-2 의 대상이 아니다) 실제 응답 셋을 떴다 — `/tasks?task=<뺀 id>` 화면 · 뺀 업무의 `/board/task/{id}` JSON · 산 업무의 JSON(뜬 파일에서 `included` 가 각각 `false` · `true`). 그것을 Playwright 로 375×812 에서 열고, 정적 파일은 **지금 저장소의 `app/static` 원본**을 그대로 내주었다. **돌고 있는 서버의 화면이 아니다.** 예시 제목은 지어낸 것이다.

| 잰 것 | 뺀 업무를 열었을 때 | 산 업무를 열었을 때 |
|---|---|---|
| 줄의 `display` | block | **none** |
| 글자 | 「이번 회차에서 뺀 업무입니다」 · **14px** | — |
| 자리(위 · 아래 · 왼 · 오른, px) | 58 · 93 · 16 · 359 (높이 34) | — |
| 칩 줄 | 18 · 46 | — |
| 제목 | 103 · 163 | — |
| 닫기 단추(×) | 16 · 44 · 329 · 357 | — |
| 겹치는 것(닫기 · 칩 · 제목 · 옮긴 자리 · 진단) | **없음** | — |
| 가로 넘침 | 없음 | 없음 |

뺀 업무는 목록에 행이 없어 드로어가 오른쪽 패널로(좁은 화면에서는 화면 전체로) 열렸고, 산 업무는 목록의 그 행 아래로 펼쳐졌다 — 둘 다 원래 동작이다.

**콘솔 오류 둘**은 `manifest.webmanifest` 404 뿐이다 — 측정에서 그 파일을 안 내준 탓이다.

**눈으로 본 것** — 뺀 업무 드로어에 상태 칩 「지연」 과 진단 패널 「진행 가능 · 마감에서 N일 경과」 가 그대로 뜬다. 지시문이 고치기를 막지 말라고 했고 판정은 4-10 의 일이라 안 고쳤다 → 사람칸 「정할 것」 · 봐둘것 BC-c. 스크린샷은 저장소 밖(작업 창의 임시 폴더)에만 있고 올리지 않았다.

**돌고 있는 서버** — 재시작 뒤 `/login` 이 가리키는 CSS 가 `retreat.210ccc3e.css`(지금 파일의 해시와 같음)이고 거기 `.dexcl` 규칙이 있다. `drawer.8b749f60.js`(지금 파일의 해시)가 200 으로 오고 `d.included !== false` 가 들어 있다. 로그인 뒤 화면은 못 받았다.

# 5. 검사 · 시험 · 재시작

## 5-1. 재시작 (배포-안내 12-1)

```
멈추기 전 PID: 28816
포트가 비었습니다
다시 켠 뒤 PID: 12316
성공 — PID 가 바뀌었습니다
```

2026-09-16 18:30:06 · 그 뒤 4장 끝.

## 5-2. 고장을 심어 시험이 빨개지는지

심고 `tests/test_stage58.py` 만 돌린 뒤 바로 되돌렸다(`try/finally`).

| 심은 고장 | 결과 |
|---|---|
| 서버가 `included` 를 안 싣는다 | 1 failed |
| 서버가 늘 참이라 한다 | 1 failed |
| 줄이 처음부터 보인다(`hidden` 없음) | 1 failed |
| JS 가 줄을 안 켠다 | 1 failed |
| JS 가 상태로 다시 가른다 | 1 failed |
| 줄이 제목 아래로 간다 | 1 failed |
| 글자가 장식 단(12px) | 1 failed |
| 뺀 업무 드로어를 막는다(404) | 1 failed |

## 5-3. 글검사 · pytest · 파일

- 커밋 전 `git status --short`: `data/` · `testdata/` · `link.txt` 는 안 잡혔다. 측정 도구가 저장소 안에 만든 로그 폴더(`.playwright-mcp/`)는 이 판이 만든 것이라 지웠다
- 글검사(검토자 원문까지 다 채운 뒤): **7/7 통과**(check_names · check_stale · check_named · check_counts · check_handoff · check_placeholder · check_orders)
- pytest(옵션 없이 전체 · 보고와 검토자 원문을 다 채운 뒤): **1677 passed, 3 warnings in 1967.55s** (exit 0)
- 바뀐 파일(`e2f0e79` 부터): `CLAUDE.md` · `app/routers/board.py` · `app/static/css/retreat.css` · `app/static/js/drawer.js` · `app/templates/partials/drawer.html` · `docs/review/2026-09-16-노션마무리.md`(얼림) · `docs/review/최근.md` · `docs/봐둘것.md` · `docs/인계.md` · `tests/test_stage58.py`

# 6. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 뺀 업무 드로어에 표시가 뜨고 산 업무에는 안 뜬다 | O | 1장 · `test58_c01` · `c02` · 4장(block / none) |
| 나) 375px 에서 실제로 잰 값이 있다 | O | 4장 — 로그인 화면이 아니라 같은 CSS·JS·실제 JSON 으로 잰 값 |
| 다) pytest 옵션 없이 전부 통과 · 글검사 7/7 | O | 5-3 |
| 라) 검토자 원문이 최근.md 에 그대로 | O | 7장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| ① | 했다 | 3장 — 여백 43px 그대로 · 글자 14~15px 유지 · 코드 안 고침 |
| ② | 했다 | 1장 — 한 줄 표시 · 고치기 안 막음 |
| ③ | 했다 | 3장 — 13px 그대로 · 코드 안 고침 |
| 1a | 했다 | 1a — 칩 아래 · 진단 패널·옮긴 자리 줄과 떨어진 자리 |
| 1b | 했다 | 1b · `c01` |
| 1c | 다르게 했다 | 4장 — 로그인 화면 대신 같은 CSS·JS·실제 JSON 을 붙여 Playwright 로 잼(11-2) |
| 1d | 했다 | `c01` · `c02` — 같은 판에서 뺀 것·산 것 둘 다 |
| 2a | 했다 | 2장 |
| 2b | 했다 | 2장 |
| 2c | 했다 | 2장 · 인계 맨 위 둘째 줄 |
| 앱 코드를 고치므로 재시작하고 PID 로 확인 | 했다 | 5-1 |
| 운영 DB 는 안 건드린다 | 했다 | 이 판에서 운영 DB 를 연 곳이 없다 |
| 검토자 원문을 그대로 싣는다 | 했다 | 7장 |
| 마무리는 11-3 대로 | 다르게 했다 | `docs/checks/drawer.js` · `phone.js` 는 로그인이 필요해 못 돌렸다(11-2 · 2c 로 인계에 적음). 나머지는 했다 |

# 7. 검토자 원문

## 7-1. 커밋 전 검토

```
━━━ 검토자 여기부터 ━━━ 범위 e2f0e79..작업 트리 · 커밋 0개(커밋 전) · 파일 10개(새 파일 1 포함)

읽은 것: CLAUDE.md(11-3 되짚기 · 10장 · 4-14 · 13장 · 14장) · docs/검토-원칙.md ·
git diff 전부 · docs/review/최근.md · app/static/js/drawer.js 에서 detail 을 바꾸고
다시 그리는 자리 전부 · app/routers/board.py 의 task_detail 을 부르는 곳 ·
retreat.css 의 [hidden] 규칙.
얼린 판은 HEAD 의 최근.md 와 줄끝(CRLF) 말고는 같다(diff --strip-trailing-cr 로 봄).

─────────────────────────────────────────
막아야 함
─────────────────────────────────────────
없음.

사람칸 다섯 줄은 순서대로 있고 값도 다 채워져 있다.
지시문 대조 표에는 ①②③ · 1a–1d · 2a–2c · 재시작 · 운영 DB · 원문 싣기 ·
11-3 이 모두 있고, 「다르게 했다」 두 줄에는 까닭이 적혀 있다.
7장 원문 자리와 5-3 의 값 칸은 부탁대로 짚지 않았다.

─────────────────────────────────────────
고쳐야 함
─────────────────────────────────────────
1. 「남은 것은 …뿐」 이 실제로 남은 것을 빠뜨렸다 (검토-원칙 7)
   - CLAUDE.md 13장에 더한 줄: "남은 것은 docs/봐둘것.md BC-c 에 적어만 둔 것뿐입니다"
   - docs/인계.md 맨 위 첫 줄도 같은 말로 끝난다: "남은 것은 봐둘것 BC-c 에 … 뿐이다"
   - 그런데 인계의 바로 다음 줄(2c)은 화면 점검(drawer.js · phone.js)이 이 줄기 전체에서
     한 번도 못 돌았다고 적는다. 그 점검은 BC-c 에 없다.
   - 그래서 기준 문서(13장)만 읽는 사람은 남은 것이 BC-c 뿐인 줄 안다.
   - 13장 줄에 「화면 점검이 못 돈 것은 인계 맨 위에」 를 한 마디 더하거나,
     「…뿐」 을 쓰지 않는 쪽이 맞다.

─────────────────────────────────────────
봐 둘 것
─────────────────────────────────────────
2. 0단계 훑기가 응답을 받는 곳 하나를 빠뜨렸다 (보고 0장 첫 줄)
   - 보고는 task_detail 의 응답을 받는 곳을 drawer.js 의 세 곳(89 · 1185 · 1308)이라 적었다.
   - 넷째가 있다. app/routers/board.py:1217 은 선행을 저장한 뒤 task_detail 을 그대로
     돌려주고, drawer.js:541 이 그것으로 detail 을 통째로 바꾼다.
   - 해는 없다. 그 응답에도 included 가 실리고, 그 자리는 renderRel 만 부르므로 줄이 안 바뀐다.
   - 다만 훑기 목록으로는 틀렸다.

3. 짚어 달라고 한 점 — 확인한 것 (고칠 것 없음)
   - 다른 run 으로 다시 열 때: openDrawer(89–94)가 detail 을 새 응답으로 바꾼 뒤
     renderDrawer 를 부르고, renderDrawer 는 매번 hidden 을 다시 정한다. 줄이 남지 않는다.
   - 다른 경로의 다시 그리기(396 관련팀 · 1286 상태 · 1506 날짜)는 detail 의 칸만 고치고
     included 는 그대로 두므로 줄이 흔들리지 않는다.
   - 여는 중(openDrawer 첫머리)에는 줄이 앞 run 의 상태로 잠깐 남는다.
     제목과 같은 동작이라 그대로 둬도 된다.
   - hidden 이 CSS 에 이기는가: retreat.css:138 에 [hidden]{display:none!important} 가 있고,
     .dexcl 에는 display 가 없다. 이긴다.
   - 자리: .dkick 은 flex(margin-bottom 12)이고 .dexcl 은 그 아래 블록이다.
     .dhead 는 보드·달력·목록이 한 벌로 쓰므로 세 화면의 배치가 같다.
     보고 4장의 수치(칩 46 → 줄 58)도 이 계산과 맞는다.
   - included 칸을 더한 것: JSON 에 칸 하나를 더한 것뿐이다. 받는 쪽은 전부 drawer.js 이고,
     칸이 없을 때는 「!== false」 라서 산 업무로 본다. 다른 모양과 부딪히는 곳은 없다.

4. 시험
   - c01: 뺀 업무와 산 업무를 한 판에서 열고, 뺀 업무의 상태 고치기가 200 인지까지 본다.
   - c02: 주석을 걷어낸 뒤 hidden 초기값 · 칩 < 줄 < 제목 순서 · JS 가 included 로 켜는 것 ·
     14px 를 본다.
   - 보고 5-2 에 고장 여덟을 심어 각각 빨개졌다고 적혀 있다(재현은 안 했다).
   - 「산 업무에는 안 뜬다」 를 JS 없이 둘로 나눠 재는 것은 이 저장소의 틀(pytest 는 화면을
     못 봄 · 10장)에서 할 수 있는 만큼이다.
   - 브라우저 쪽 확인은 4장의 block / none 이 맡았다. 그 값은 돌고 있는 서버가 아니라
     그 자리에서 만든 앱에서 쟀다(아래 5).
   - 한 가지 약한 곳: c02 가 JS 를 글자 그대로 찾는다(「d.included !== false」).
     같은 뜻을 다르게 쓰면 빨개진다. 틀리게 초록이 되는 쪽이 아니라 봐 둘 것에 그친다.

5. 1c 를 잰 방식과 11-2
   - 앱을 그 자리에서 만들었고(임시 DB), 로그인 값은 저장소에 적힌 지어낸 값이다.
     11-2 가 적은 두 조건에 들어맞으므로 로그인 규칙 밖이라고 읽힌다.
   - 보고 4장은 「돌고 있는 서버의 화면이 아니다」 를 스스로 밝혔고,
     대조 표도 1c 를 「다르게 했다」 로 두었다. 정직하다.
   - 다만 인계 맨 위의 「같은 CSS·JS·서버가 낸 실제 JSON 으로 쟀다」 는
     「돌고 있는 서버」 로 읽힐 수 있다.
     보고처럼 「그 자리에서 만든 앱이 낸 JSON」 으로 적는 편이 덜 헷갈린다.

6. 재시작 순서 (11-3 2단계)
   - 재시작은 18:30(PID 12316)이고 보고는 18:40 에 썼다.
   - 이 검토 뒤에 app/ · templates/ · static/ 을 하나라도 더 고치면
     재시작을 다시 하고 보고 5-1 과 머리의 PID 를 고친다.
   - 위 1번은 문서만 고치는 것이라 해당하지 않는다.

7. 사람칸 「정할 것」 — 적절하다
   - 뺀 업무 드로어에 지연 칩과 진단 패널이 뜨는 것은 지시문(고치기를 막지 않는다) 밖이고,
     판정은 4-10 의 일이라 사람에게 올린 것이 맞다.
   - BC-c 에 적은 줄과 글이 맞는다.
   - 지연 배지는 목록 줄에서는 이미 빼 두었다(4-14 「배지를 안 붙입니다 — 재촉」).
     드로어 칩만 「지연」 을 내는 것은 그 판단과 결이 다르다.
     사람이 정할 때 이 연결을 함께 보면 좋다.

8. 문서와 코드
   - CLAUDE.md 4-14 · 14장 줄은 코드와 맞는다(`.dexcl` · 14px · 서버 included 하나 · 고치기 안 막음).
   - BC-c 의 ①②③ 닫힘은 지시문 요지와 맞는다.

─────────────────────────────────────────
사람에게 묻는 것
─────────────────────────────────────────
- 뺀 업무 드로어의 상태 칩 「지연」 과 진단 패널 「진행 가능 · 마감에서 N일 경과」 를
  그대로 둘지 (목록에서는 같은 이유로 배지를 뺐다 — 위 7).
- CLAUDE.md 13장의 「이 줄기는 완전히 마무리됐다 · 남은 것은 BC-c 뿐」 에
  화면 점검이 못 돈 것을 함께 적을지 (위 1).
━━━ 검토자 여기까지 ━━━
```

## 7-2. 다시 보인 뒤 (검토 뒤 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 다시 보기(11-3 · 한 번) · 범위: 앞 검토 뒤 문서만 고친 차이 · 파일 4개

읽은 것:
- 변경 파일 넷의 차이: CLAUDE.md · docs/인계.md · docs/봐둘것.md · docs/review/최근.md
- 최근.md 의 장 제목 목록
- scripts/check_named.py 가 경로 이름을 찾는 방식

app/ · templates/ · static/ · tests/ 의 차이는 앞 검토 때와 같다.
줄 수도 같다(+2 · +5 · +3 · +5 · +54). 그래서 재시작하지 않은 것이 11-3 2단계와 맞는다.

─────────────────────────────────────────
막아야 함
─────────────────────────────────────────
없음.

─────────────────────────────────────────
고쳐야 함
─────────────────────────────────────────
1. 보고가 없는 장을 가리킨다 (docs/review/최근.md)
   - 가리키는 자리 둘:
     · 4줄(사람칸 「정할 것」): "(4장 · 8장 · 봐둘것 BC-c)"
     · 51줄(2장 2b): "(검토 뒤 「…뿐」 을 고침 · 8장)"
   - 그런데 보고의 장은 0 부터 7 까지이다(15 · 23 · 48 · 55 · 60 · 83 · 116 · 146줄).
     8장이 없다.
   - 11-3 은 검토 뒤에 고친 것을 「두 번째 검토 뒤 고침」 으로 적으라고 한다.
     8장을 그 자리로 둘 생각이라면 커밋 전에 8장을 채운다.
   - 아니라면 두 곳이 가리키는 장을 실제로 있는 장으로 고친다.
   - 이대로 두면 사람칸이 없는 곳을 가리키게 된다.

─────────────────────────────────────────
봐 둘 것
─────────────────────────────────────────
2. 짚었던 것이 반영됐는지 확인했다. 다섯 모두 맞게 들어갔다.
   - 앞 검토 1:
     · CLAUDE.md 13장이 「남은 것은 둘」 로 바뀌었고, 화면 점검 줄과 인계 맨 위를 함께 가리킨다.
     · 인계 맨 위 첫 줄도 「남은 것은 둘이다 — … 과 바로 아래 줄의 화면 점검」 으로 바뀌어,
       바로 다음 줄과 어긋나지 않는다.
     · 13장의 `phone.js` 는 짧게 적은 이름이다. check_named 는 경로가 「/phone.js」 로
       끝나는 파일이 있으면 통과시키므로 걸리지 않는다(docs/checks/phone.js 가 있다).
   - 앞 검토 2: 보고 0장 첫 줄이 「넷이다」 로 바뀌었다. 선행 저장 자리가 줄을 흔들지 않는다는
     것도 적혀 있다(연결 탭만 다시 그린다 — 코드와 맞다).
   - 앞 검토 5:
     · 인계 맨 위의 문장이 「그 자리에서 만든 앱(임시 DB · 지어낸 로그인) · 돌고 있는 서버의
       화면이 아니다」 로 바뀌었다.
     · 보고 사람칸 「막혀서 못 한 것」 도 같은 뜻이다.
     · 보고 4장과 이제 같은 말을 한다.
   - 앞 검토 7: 봐둘것 BC-c 의 그 줄과 사람칸 「정할 것」 에 목록의 뺀 줄에서 배지를 이미
     뺀 것(4-14)이 함께 적혔다. 둘이 같은 말이다.
   - 2b: 13장 문장이 바뀐 것이 반영됐다.

3. 앞 검토 4 · 6 을 「고칠 것 없음」 으로 둔 것에 이견은 없다.
   - 6(재시작)은 이번 차이에 app/ · templates/ · static/ 이 없어 해당하지 않는다.

4. 이번 다시 보기 뒤에 또 고치는 것이 생길 수 있다(위 1 포함).
   - 그러면 11-3 대로 한 바퀴를 더 돌지 않는다.
   - 대신 보고에 「두 번째 검토 뒤 고침」 으로 파일과 까닭을 적는다.
   - 위 1의 「8장」 이 바로 그 자리가 될 수 있다.

─────────────────────────────────────────
사람에게 묻는 것
─────────────────────────────────────────
앞 검토에서 올린 것과 같다. 새로 묻는 것은 없다.
- 뺀 업무 드로어의 「지연」 칩과 진단 패널을 그대로 둘지
  (지금 사람칸 「정할 것」 에 올라 있다).
━━━ 검토자 여기까지 ━━━
```

# 8. 검토 뒤 고침

## 8-1. 첫 검토가 짚은 것 (전부 문서 — 재시작 안 함)

| 짚은 것 | 한 것 |
|---|---|
| 1 「남은 것은 BC-c 뿐」 이 못 돈 화면 점검을 빠뜨림 | CLAUDE.md 13장과 인계 맨 위를 「남은 것은 둘 — BC-c 에 적어만 둔 것과 한 번도 못 돈 화면 점검」 으로 고침 |
| 2 0단계 훑기가 응답 받는 곳 하나를 빠뜨림 | 보고 0장 첫 줄을 「넷」 으로 고침 |
| 4 `c02` 가 JS 를 글자 그대로 찾음 | 안 고침 — 틀리게 초록이 되는 쪽이 아니라 같은 뜻을 다르게 쓰면 빨개지는 쪽이다 |
| 5 인계의 375px 문장이 「돌고 있는 서버」 로 읽힐 수 있음 | 인계 맨 위와 보고 사람칸을 「그 자리에서 만든 앱이 낸 JSON · 돌고 있는 서버의 화면이 아니다」 로 고침 |
| 6 재시작 순서 | 고칠 것 없음 — 검토 뒤 앱을 안 고쳤다 |
| 7 목록에서는 배지를 뺐는데 드로어 칩은 「지연」 을 냄 | 봐둘것 BC-c 와 사람칸 「정할 것」 에 그 연결을 함께 적음 |

## 8-2. 두 번째 검토 뒤 고침 (다시 보이지 않았다 — 11-3)

| 파일 | 까닭 |
|---|---|
| `docs/review/최근.md` | 두 번째 검토가 사람칸과 2장이 **없는 8장**을 가리킨다고 짚었다 — 이 8장을 채워 그 자리를 세웠다 |

