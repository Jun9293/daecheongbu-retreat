사람이 할 것
노트북에서 직접: 하나 — 개발 DB 를 지우고 seed 를 다시 만드는 일이 앞 판들에서 넘어와 아직 남았다(얼린 판 `docs/review/2026-09-14-계좌게이트.md` 5장). 이 판이 새로 넘기는 것은 없다
관리자 권한: 없음 — app/ 을 안 건드려 재시작하지 않았다(4장)
정할 것: 셋 — ① 운영 미리보기 수(3-2)를 보고 「그대로 해」 로 `scripts/재정짝잇기.py --실행` 을 돌릴지 ② 그 실행이 시트의 옛 구분·항목·세부항목-1 글자 30 줄을 항목 글자로 덮기 전에 옛 글자를 활동 기록에 남기게 할지, 시트·사본을 되돌리는 자리로 둘지(앞 판 정할 것 ② · 5장) ③ 다음 시트(2027)를 처음부터 결산 식으로 이을지 · 그러면 Excel 기본 범위 꼴(탭 이름이 앞에 한 번)도 「아는 꼴」 에 더할지(봐둘것 BB-i)
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 이 판은 화면 코드를 안 바꿨다(4장)

작성 시각: 2026-09-15 13:25 KST
커밋: ef83d57 (스크립트 · 시험) · ce1c0b6 (문서) · 이 보고는 그다음 커밋
작업: 못 이은 30 건 — 시트의 결산 식 연결을 정본으로 다시 잇는다 (이 판은 미리보기까지)

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 글자 비교는 `재정들여오기.고른다` 하나 — 안 건드렸다. 채우는 결과가 같아야 하는 곳은 `routers/expenses.py` 의 `update_expense`(항목이 바뀌면 구분·항목·세부항목-1 을 항목 글자로 덮음). 사본은 `재정들여오기.사본을_뜬다`, 회차는 `비품들여오기.회차머리` 를 부른다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 스크립트만 |
| 그 값을 세어 둔 문서 | CLAUDE.md 7-5 · 14장 · 인계 머리 · 순서표 6 · 봐둘것 BB-h · `docs/재정-잇기-안내.md` 머리 |

# 1. 한 것

- **0-a** 운영을 `mode=ro` 로 셈 — 「예산 항목 미지정」 산 지출 **30** 그대로 · 들여오기 뒤 활동 기록 0. 멈출 까닭 없음
- **1-a** `scripts/재정들여오기.py` 에 `결산식으로(경로)` — 예산 탭 결산금액 식 → 지출 탭 합계금액 칸 → 그 합계가 더하는 지출 줄 범위. **아는 꼴의 식만** 푼다(`=주소` · `=주소:주소` · 대소문자 무시 `SUM(범위, …)` · `+` 로 이은 것 · 결산 식은 주소마다 지출 탭 이름, 합계 식은 탭 이름 없음). 모르는 꼴은 빈 것 → 짝 표. `고른다`(글자 비교)는 그대로
- **1-b** 실제 시트: 글자로 이은 **67 전부 식과 같음** · 못 이은 **30 전부 식이 한 예산 줄씩**. 실제 식 모양(숫자·탭 이름 가려 셈): 결산 한 칸 주소 34 · 합계 `sum(범위)` 14 · 한 칸 9 · `sum(한 칸)` 6 · `SUM(범위)` 4 · 빈 칸 1
- **2** `scripts/재정짝잇기.py` 새로 — 미리보기 기본 · `--실행` · 채울 것이 있을 때만 `VACUUM INTO` 사본(세션 파일과 사본 파일이 다르면 멈춤) · 들여온 기록이 없으면 멈춤 · 시트 줄과 앱 행은 **들여온 순서로** 맞춤(예산 항목은 id 순 글자 · 지출은 id 순 금액·날짜가 시트와 한 곳에서만 맞아야 · 안 맞으면 멈춤) · **들여온 지출 중 예산 항목 없는 것만** · 식이 가리키는 예산 줄이 하나가 아니거나 들여온 줄이 아니거나 그 항목이 취소면 안 채우고 짝 표(`data/재정짝잇기.real.md` · 줄 번호와 까닭만) · 채우면 예산 항목 + 구분·항목·세부항목-1 글자(`update_expense` 와 같은 결과) · 활동 기록 한 줄(`재정_짝잇기` · 수와 지출 id) · commit 전에 DB 에서 미지정 수와 채운 행의 항목·글자를 다시 셈 · 미리보기에 「이미 이은 것 중 결산 식이 가리키는 항목과 다른 것」 도 셈
- **3** 운영 사본(ro `VACUUM INTO`)에서 리허설(3-3)
- **4** 안내문 머리 닫힘(본문 유지) · 인계 머리와 순서표 6 · 봐둘것 BB-h 머리 · 새 BB-i(정할 것) · CLAUDE.md 7-5 · 14장 · 잇기 안내 판 보고 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 새 함수가 30 건 전부의 예산 줄 · 이은 67 은 안 건드림 | O | 실제 시트에서 67 같음 · 30 한 줄씩(1-b) · `test51_a01`(합성 시트에서 줄마다 예산 줄 · 글자로 이은 줄은 식과 같음) · `a02`(`고른다` 에 새 함수가 안 들어감) · `b01`(이미 이은 지출의 항목이 그대로) · 사본 리허설에서 원래 항목 있던 67 중 바뀐 것 0 |
| 나) 운영 30 확인 | O | 1장 0-a |
| 다) 미리보기 수 · 취소 줄은 안 채움 | O | 운영 미리보기 3-2 · `test51_b02`(취소 항목 가리키면 안 채우고 짝 표) · `c04`(식이 둘 · 모르는 꼴) · `c05`(들여온 줄 아님) |
| 라) 실행 뒤 미지정이 비거나 남은 수 = 다) 예외 | O | 사본 리허설 30 → 0(예외 0) · `b01` · `b02`(취소 가리킨 수만큼 남음) |
| 마) 두 번째 「채울 것이 없습니다」 | O | 사본 리허설 · `b01` |
| 바) pytest · 글검사 7/7 | O | 6장 |
| 사) 검토자 원문 | O | 7장 |

# 3. 운영 자료 (값 없이 건수만)

**운영 DB 는 바꾸지 않았다** — 0단계를 `mode=ro` 로 셌고, 미리보기는 DB 를 안 바꾼다(짝 표 파일만 `data/` 에 씀). 리허설은 이 창 임시 폴더의 사본에서.

## 3-1. 이미 확인한 것

운영 미지정 산 지출 30 · 부서 없음 30 · 금액+날짜 겹침 0(앞 판 셈) · 들여오기 뒤 활동 기록 0

## 3-2. 운영 미리보기 (「그대로 해」 에 쓸 수)

| 무엇 | 수 |
|---|---|
| 들여온 뒤에 생긴 미지정 지출(안 건드림) | 0 |
| 예산 항목 미지정 | 30 |
| 이미 예산 항목 있음(안 건드림) | 67 |
| 이미 이은 것 중 결산 식이 가리키는 항목과 다른 것 | 0 |
| 채울 것 | 30 |
| 못 채움(짝 표) | 0 |

마지막 줄: 「바꾸지 않았습니다」. 미리보기 뒤 운영 미지정 여전히 30.

## 3-3. 사본 리허설

- `--실행`: 사본을 떴다 · 「채웠습니다. 예산 항목 미지정: 30 → 0」
- 두 번째: 「이미 예산 항목 있음 97 · 그중 식과 다른 것 0 · 채울 것이 없습니다」
- 따로 셈: 원래 항목 있던 67 중 바뀐 것 0 · 채워진 30 · 항목 글자가 항목과 다른 산 지출 0

## 3-4. 「그대로 해」 뒤 다음 판

운영 미리보기가 3-2 와 같은지 → `--실행`(사본 이름을 보고에) → 미지정 수 · 식과 다른 것 다시 셈 → 두 번째 「채울 것이 없습니다」. 되돌리기는 그 사본 하나를 `data/app.db` 로(7-5 「되돌리기」 와 같은 꼴 · 실행 뒤 앱에서 한 것은 함께 사라짐).

# 4. 재시작

**건너뛰었다** — `app/` · `templates/` · `static/` 이 안 바뀌었다(스크립트 · 시험 · 문서만). 떠 있는 서버는 앞 판 PID 18592 그대로.

# 5. 판단이 들어간 자리

- **시트 줄 ↔ 앱 행 맞추기** — DB 에 시트 줄 번호가 없어 **들여온 순서**로 맞췄다. 예산 항목은 글자 순서, 지출은 금액·날짜 순서가 시트와 한 곳에서만 맞아야 하고, 들인 뒤 이름·금액을 고쳤거나 다른 시트면 멈춘다. **못 가르는 것**: 금액·날짜가 같은 이웃 줄이 시트에서 자리만 바뀐 경우, 들인 뒤 시트의 식·글자만 고쳐진 경우(들여오기 기록에 시트 해시가 없다) — 운영에서는 들인 뒤 활동 0 · 겹침 0 이라 안 걸린다
- **아는 꼴의 식만** — 칸 주소 모양만 모으면 SUMIF · 숫자 붙은 함수 이름 · 따옴표 안 글자가 줄로 잡혀 틀린 한 줄로 조용히 채울 수 있었다(첫 검토). 꼴을 먼저 보게 했더니 처음엔 실제 시트의 소문자 `sum` 을 못 받아 57 건이 어긋났고, 대소문자를 무시하게 고쳐 67 · 30 으로 돌아왔다. **결산 식에서 범위는 두 끝에 탭 이름이 있어야** 받는다 — Excel 기본 꼴(앞에 한 번)은 짝 표로 간다. 이번 시트는 전부 한 칸이라 안 걸린다(정할 것 ③)
- **활동 기록은 한 줄** — 행마다가 아니라 요약(수와 지출 id). 옛 시트 글자는 `update_expense` 처럼 기록에 안 남는다(정할 것 ②)
- **「식과 다른 것」 은 세기만** — 0 이 아니어도 실행을 막지 않는다. 사람이 미리보기를 보고 정하는 순서(11-2)라서다
- **글자 비교를 그대로 둠** — 지시문대로. 67 건은 식과 전부 같았다

# 6. 검사와 시험

- 글검사(커밋 전): 7/7 통과
- 새는지 검사(저장소 밖 도구 · 값 안 찍음): 스크립트 둘 · 시험 · CLAUDE.md · 인계 · 봐둘것 · 안내문에 새로 들어온 시트 값 0(봐둘것 한 낱말과 시험 한 조건이 우연히 겹쳐 바꿨다)
- `tests/test_stage51.py` 13 passed · `test_stage48.py` 함께 통과 — 고장 둘(취소 검사 끄기 · 이미 이은 것 건너뛰기 끄기)을 심어 각각 빨개짐
- 보고를 다 채운 뒤 돌린 것: 글검사 7/7 통과 · 새는지 검사(이 보고) 0 · pytest(옵션 없이) `1589 passed, 3 warnings in 1875.91s (0:31:15)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위 HEAD cbe3f56 .. 스테이지(커밋 전) · 커밋 0개 · 파일 11개

본 것
  git diff --cached 전부 · scripts/재정들여오기.py 의 읽는다·넣는다·돌린다·사본을_뜬다 (diff 밖)
  app/routers/expenses.py update_expense (diff 밖) · tests/test_stage48.py 합성 시트
  _식의줄들 정규식을 저장소 밖 사본에서 여러 식으로 돌려 봄(파일 import 안 함)
  운영 DB mode=ro 건수만: 미지정 산 지출 30 (회차 1 에만) · 재정_짝잇기 활동 기록 0 ·
    재정_들여오기 1 · 산 지출 중 level 글자가 항목과 다른 것 0
  data/backups: 오늘 app-*.db 는 들여오기 직전 사본(00:23)과 새벽 백업(03:00) 뿐 -> 이 판이 사본을 뜨지 않음
  얼린 판 2026-09-15-잇기안내.md = HEAD 의 최근.md (줄끝만 다르고 내용 같음)
  시트(data/*.real.xlsx)는 열지 않음 -> 실제 시트의 식 모양은 확인 못 함

(d) 11-2 순서 · 운영 -- 문제 없음
  운영은 미리보기만 돌렸고 DB 는 안 바뀜(위 건수). 미리보기가 data/재정짝잇기.real.md 를 쓰는 것은
  gitignore(data/*.real.md) 안이라 괜찮음. app/ 는 안 건드렸고 재시작을 건너뛴 것도 맞음.

(c) update_expense 와 같은가 -- 데이터는 같음
  update_expense 도 budget_category_id 가 바뀌면 level1 / level2 / level3a 를 항목 글자로 덮고,
  활동 기록에는 budget_category_id 의 전후만 남김(옛 시트 글자는 거기서도 안 남음).
  둘 다 취소된 항목에는 새로 안 붙임. 다른 점은 활동 기록이 줄마다가 아니라 요약 한 줄(지출 id 목록)이라는 것뿐.

잡은 것

[고쳐야 함] docs/봐둘것.md:3413 (BB-i) -- 「이름이 다른」 줄의 예로 「같은 글자 둘 이상」 을 셈
  왜: 「같은 글자 둘 이상」 은 글자가 같은 예산 줄이 여럿이라 못 이은 경우지, 두 탭의 이름이 달라서 못 이은 경우가 아님.
  그런데 BB-i 는 그것을 「글자로 같지 않은 줄이 흔하다 -- 30 중 26」 에 넣었음. 머리의 주장(글자 비교의 한계 = 이름이
  다름)과 수가 안 맞음. 게다가 같은 문서 BB-h(3376 줄 근처)는 같은 30 건을 다른 갈래
  (4 · 2 · 10 · 5 · 9)로 나눠 두어서, 바로 붙은 두 절이 같은 30 건을 두 가지로 나눔. 사람이 BB-i 를 정하려고 읽는 자리임.
  26 을 「이름이 다름」 과 「같은 글자가 여럿」 으로 가르거나, BB-h 의 갈래와 어떻게 맞는지 한 줄 적어야 함.

[고쳐야 함] scripts/재정짝잇기.py:68 · 107 · 111 · tests/test_stage51.py -- 막는 갈래 셋에 막히는 쪽 시험이 없음
  시험이 있는 갈래: 취소 항목(b02) · 들여온 기록 없음(b03) · 사본 파일 다름(b04) · 지출 순서 어긋남(b05).
  시험이 없는 갈래:
    _창 이 두 곳 이상에서 맞음 -> [] -> 멈춤 (두 곳 맞음 판정 자체를 안 잼)
    예산 쪽 창이 안 맞음 (예: 들인 뒤 update_category 로 이름을 고침 -- 7-3 이 허락하는 동작)
    식이 가리키는 예산 줄이 0 개 또는 2 개 이상 -> 짝 표
    가리키는 줄이 들여온 예산 줄이 아님 -> 짝 표
  왜: CLAUDE.md 11-3 「막는 코드에는 막히는 쪽 시험이 함께 있다」. 운영 미리보기에서 이 갈래들은 0 이었으므로
  처음 밟히는 날은 다음 시트나 사람이 이름을 고친 뒤임.

[고쳐야 함] scripts/재정들여오기.py:248-256 (_식의줄들) -- 정규식이 식의 뜻을 안 보고 칸 주소 모양만 모음
  돌려 본 것(값 없음):
    ='지출 상세내역'!$H$5   -> {5}      맞음 ($ · 따옴표 탭 이름)
    =SUM($G$2:$G$4)         -> {2,3,4}  맞음 (SUM 의 알파벳은 숫자가 안 따라와 주소로 안 잡힘)
    =LOG10(G2)              -> {2,10}   틀림 (숫자가 붙은 함수 이름을 주소로 잡음)
    =SUMIF(C:C,C5,G:G)      -> {5}      틀림 (열 전체 범위는 버리고 조건 칸만 남아 「한 줄」 이 됨)
    ='지출 상세내역'!H5-D3  -> {3,5}    틀림 (같은 탭을 가리키는 주소도 지출 탭 줄로 셈)
    =H5&"A1"                -> {1,5}    틀림 (따옴표 안 글자)
    =SUM(G4:G2)             -> {}       거꾸로 쓴 범위는 비어 버림
  틀린 것 대부분은 여러 줄이 되어 짝 표로 가므로 안전한 쪽임. 하지만 SUMIF 꼴은 틀린 한 줄이 되어 조용히 틀린 항목을
  채움. 이번 시트는 구현자가 「글자로 이은 67 이 전부 식과 같다」 를 쟀으니 지금 운영 30 에는 증거가 있음.
  다만 BB-i 가 이 함수를 다음 시트의 기본으로 쓸지 묻고 있음. 싼 막이: 합계·결산 식이 「= [탭!]주소」 나
  「=SUM(범위)」 · 「+」 로 이은 꼴이 아니면 가리킨 줄을 비워 짝 표로 보냄.

[봐 둘 것] scripts/재정짝잇기.py:89 (_창 지출 열쇠 = 금액 · 일자) -- 잘못 맞출 수 있는 자리 (a)
  - 창 전체 길이로 한 곳에서만 맞아야 하므로, 들인 뒤 새 지출·항목을 만든 것(id 가 창 뒤)이나 금액·날짜를 고친 것(멈춤)은
    안전하게 다뤄짐
  - 새는 곳 1: 들인 뒤 시트에서 금액과 일자가 같은 두 줄의 순서만 바뀌면 창은 그대로 맞고, 시트 줄과 앱 행이 서로 바뀐 채
    다른 줄의 결산 식으로 채움. 열쇠에 글자(세부1)가 없어서 못 가름.
    운영에서 앞 판이 센 「금액+날짜가 겹치는 줄 0」 은 겹침이지 붙어 있는지가 아니라서 이 경우를 다 덮지는 않음(드묾)
  - 새는 곳 2: 들인 뒤 시트의 식이나 글자 칸만 고쳐져도 창은 맞고, 들일 때와 다른 식을 정본으로 씀.
    들여오기 활동 기록에 시트 파일 해시가 없어 「같은 시트인가」 를 잴 수 없음
  - 예산 쪽 열쇠 (level1, level2, level3) 는 넣는다 가 쓴 값과 같음(level3 이 None 인 것까지) -- 맞음

[봐 둘 것] scripts/재정짝잇기.py:100-102 -- 「글자로 이은 67 이 식과 같다」 가 스크립트 출력에 없음
  미리보기는 「이미 예산 항목 있음(안 건드림)」 수만 찍음. 수용 기준 가) 의 「67 전부 식과 같음」 은 따로 잰 값이고
  이 스크립트를 다시 돌려서는 안 나옴. 「이미 이은 것 중 식이 가리키는 항목과 다른 것 N」 한 줄이면 운영에서도
  같은 판에서 다시 잴 수 있고, BB-i 를 정하는 근거도 됨. test51_a01 은 합성 시트에서만 잼.

[봐 둘 것] scripts/재정짝잇기.py:131 -- 시트 글자 30 줄을 한 번에 덮고 옛 글자는 기록에 안 남음
  앞 판 사람칸의 정할 것 ② (「잇기로 덮이는 옛 글자를 활동 기록에 남길지, 시트·짝 표·사본을 그 자리로 둘지」) 가
  아직 열려 있는데, 이번에는 한 줄이 아니라 30 줄을 함께 덮음. 되돌리는 길은 시트를 다시 읽거나 사본으로 되돌리는 것뿐.
  update_expense 와 같은 결과라 막을 일은 아님. 다만 이번 보고의 정할 것에서 빠지면 안 됨.

[봐 둘 것] scripts/재정짝잇기.py:142-150 -- 실행 뒤 다시 세는 것이 budget_category_id 만 봄
  level1 / level2 / level3a 는 다시 안 셈. 리허설에서 「항목 글자가 항목과 다른 산 지출 0」 을 따로 쟀으니
  보고에 그 줄이 있으면 됨.

없는 것
  (e) 10장 수 규칙: 새 글에 「셋 이상 + 곳/군데」 없음. CLAUDE.md 7-5 · 14장 줄은 이름한 함수·파일이 실재함
  넘김 세 줄: 이유가 채워졌고 넘기는 말을 다시 쓰지 않음
  글에 운영 값(이름·계좌·금액·항목 이름)이 새로 들어간 것 없음 -- 수만 있음
  안내문 닫기는 머리에 한 단락을 더했고 본문은 지우지 않음(4 a 대로)

사람에게 묻는 것
  1. BB-i -- 다음 시트를 처음부터 결산 식으로 이을지. 정하기 전에 위 [고쳐야 함] _식의줄들 막이를 넣을지도 함께 정하세요
     (결산 식을 기본으로 쓰면 SUMIF 꼴 같은 조용한 틀림이 기본 경로가 됨)
  2. 앞 판 정할 것 ② 가 그대로 열려 있음 -- --실행 이 시트 글자 30 줄을 덮기 전에 옛 글자를 활동 기록에 남길지
     (after_value 에 전 값을 싣는 정도), 아니면 시트와 사본을 되돌리는 자리로 둘지
  3. 「그대로 해」 전에 미리보기에 「이미 이은 것 중 식과 다른 것 N」 줄을 더해 운영에서 67 을 다시 재고 실행할지
━━━ 검토자 여기까지 ━━━
```

## 7-2. 다시 보기 (검토 뒤에 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 · 범위 cbe3f56 .. 스테이지(커밋 전) · 커밋 0개 · 파일 11개

본 것
  git diff --cached cbe3f56 중 재정들여오기.py · 재정짝잇기.py · test_stage51.py · CLAUDE.md 7-5 · 봐둘것 BB-h/BB-i
  새 _식의줄들 을 저장소 밖 사본에서 여러 식으로 돌림(모듈 import 안 함)
  pytest tests/test_stage51.py (옵션 없이) -&gt; 13 passed
  운영 DB mode=ro 건수만: 미지정 산 지출 30 (회차 1) · 재정_짝잇기 활동 기록 0 · level 글자가 항목과 다른 산 지출 0
  data/backups 에 오늘 뜬 app-*.db 는 들여오기 직전 사본과 새벽 백업 둘뿐 -&gt; 이 판도 운영을 안 바꿈
  시트는 열지 않음 -&gt; 구현자가 센 실제 시트의 식 모양(결산 =T!HN · 합계 sum/SUM/=GN)은 확인 못 함

앞 판에서 짚은 것이 닫혔나

  1. _식의줄들 (앞 판 고쳐야 함) -- 닫힘
     돌려 본 결과(값 없음):
       결산 ='지출 상세내역'!$H$5            -&gt; {5}      맞음
       결산 '탭'!H5 + '탭'!H9                -&gt; {5,9}    맞음 (둘 이상 -&gt; 짝 표)
       결산 '탭'!H5-D3 · 다른 탭              -&gt; {}       짝 표로
       합계 =sum(G2:G4) · =SUM($G$4:$G$2)     -&gt; {2,3,4}  맞음 (소문자 · 거꾸로 쓴 범위)
       합계 =SUM(G2:G4,G7) · = G2             -&gt; 맞음
       =LOG10(G2) · =SUMIF(C:C,C5,G:G) · =SUM(..)*2 · 다른 탭 섞임 · 소문자 주소 -&gt; {} (짝 표로)
     앞 판의 조용히 틀린 한 줄(SUMIF 꼴 · 숫자 붙은 함수 이름 · 따옴표 안 글자)이 이제 전부 빈 집합이 되어
     짝 표로 감. 모르는 꼴이 빈 것이 되는 쪽은 안전함.

  2. 막는 갈래 시험 (앞 판 고쳐야 함) -- 닫힘
     c02 두 곳에서 맞음 · c03 예산 이름 고친 뒤 멈춤 · c04 줄 둘 / 모르는 꼴(0개) · c05 들여온 줄이 아님
     (monkeypatch 가 짝잇기 모듈에 들어온 이름을 바꾸고 고른다 가 그 전역 이름을 부르므로 실제로 그 갈래를 탐) ·
     c06 이미 이은 것이 식과 다름. 앞 판의 넷이 전부 시험됨.

  3. BB-i 수 (앞 판 고쳐야 함) -- 닫힘
     「이름 다름 26 (11·7·4·3·1)」 과 「같은 글자 여럿 4」 로 갈라 합이 30 이고, 머리 주장과 수가 이제 맞음.
     BB-h 의 다섯 갈래가 다른 자르기라는 한 줄도 있음. 수 자체는 시트를 안 열어 확인 못 함.

  4. 「이미 이은 것 중 식과 다른 것」 한 줄 (앞 판 봐 둘 것) -- 닫힘. 운영 미리보기 0 은 구현자가 보고한 값
  5. 실행 뒤 level 글자 다시 세기 (앞 판 봐 둘 것) -- 닫힘 (재정짝잇기.py 돌린다 안의 비교)

새로 잡은 것

[봐 둘 것] scripts/재정들여오기.py _식의줄들 (탭을 줄 때) -- Excel 이 쓰는 기본 꼴 '탭'!H2:H4 를 모르는 꼴로 봄
  범위의 두 끝에 모두 탭 이름을 붙여야 fullmatch 가 됨. Excel 은 보통 탭 이름을 앞에 한 번만 씀.
  그래서 그 꼴의 결산 식은 빈 집합이 되어 짝 표로 감. 틀리게 채우는 쪽이 아니라서 막을 일은 아님.
  다만 docstring 은 「=주소:주소」 를 받는다고 적었는데, 결산 식에서는 그 꼴이 안 받아짐.
  이번 시트의 결산 식은 전부 한 칸(=T!HN)이라 운영 30 에는 안 걸림. 다음 시트(BB-i)를 정할 때 볼 자리.

[봐 둘 것] scripts/재정짝잇기.py:106 -- 줄 이음이 한 줄로 합쳐져 가운데에 공백이 길게 박힘
  `... is not None                 and 예산줄로[...]` 가 한 줄임. 문법은 맞고 시험도 통과함.
  줄바꿈(백슬래시 이음이나 괄호)이 붙여넣기 중에 사라진 모양이라 읽기만 나쁨. 동작은 같음.

[봐 둘 것] scripts/재정짝잇기.py:103-107 -- 「식과 다른 것」 이 0 이 아니어도 실행이 막히지 않음
  세기만 하고 --실행 을 멈추지 않음. 미리보기 수를 사람이 보고 「그대로 해」 하는 11-2 순서라 받아들일 만함.
  대신 이번 보고의 운영 미리보기 줄에 그 수(0)가 꼭 실려야 사람이 볼 수 있음.

없는 것
  CLAUDE.md 7-5 에 더한 「아는 꼴의 식만」 구절은 코드와 맞음(탭 두 번 요구라는 세부는 위 봐 둘 것)
  새 시험이 운영 수를 박지 않음 (합성 시트의 6·7행 · 둘)
  새 글에 운영 값 없음 · 「셋 이상 + 곳/군데」 없음

그대로 열린 것 (구현자가 정할 것으로 넘기겠다고 한 것 -- 앞 판 판단 그대로)
  _창 이 금액·일자가 같은 이웃 줄의 자리 바뀜이나, 들인 뒤 식·글자만 바뀐 시트를 못 가름(들여오기 기록에 시트 해시 없음)
  옛 시트 글자 30 줄이 활동 기록 없이 덮임(앞 판 정할 것 ②)

사람에게 묻는 것
  1. BB-i -- 다음 시트를 처음부터 결산 식으로 이을지. 그렇게 하면 '탭'!H2:H4 같은 Excel 기본 범위 꼴을
     「아는 꼴」 에 더할지도 함께
  2. 앞 판 정할 것 ② -- --실행 이 옛 시트 글자 30 줄을 덮기 전에 그 글자를 활동 기록에 남길지,
     시트와 사본을 되돌리는 자리로 둘지
  3. 「그대로 해」 할 때 볼 운영 미리보기 수: 채울 30 · 못 채움 0 · 이미 이은 67 · 그중 식과 다른 것 0
     (구현자가 보고한 값. 이 검토는 운영 미지정 30 만 다시 셈)
━━━ 검토자 여기까지 ━━━
```

## 7-3. 두 번째 검토 뒤 고침 (다시 보이지 않고 적는다 · 11-3 4단계)

- `scripts/재정짝잇기.py` — 다시 보기 [봐 둘 것] 줄 이음. 한 줄에 합쳐진 조건을 `하나 = …` 로 나눠 두 줄로. 동작은 같다(`test_stage51` 13 passed)
- `scripts/재정들여오기.py` `_식의줄들` 독스트링 — 다시 보기 [봐 둘 것]. 탭을 주면 범위의 두 끝에 탭 이름이 있어야 하고 Excel 기본 꼴은 짝 표로 간다는 것, 2026 시트는 안 걸리고 다음 시트에서 봐둘것 BB-i 로 정한다는 것을 적었다(코드는 그대로)
- 「식과 다른 것」 이 0 이 아니어도 실행을 막지 않는 것은 그대로 두고 3-2 표에 그 줄을 실었다

# 8. 바뀐 파일 (`git diff --name-only cbe3f56..ce1c0b6`)

- CLAUDE.md
- docs/review/2026-09-15-잇기안내.md
- docs/봐둘것.md
- docs/세어둠.txt
- docs/옛말-넘김.txt
- docs/이름-넘김.txt
- docs/인계.md
- docs/재정-잇기-안내.md
- scripts/재정들여오기.py
- scripts/재정짝잇기.py
- tests/test_stage51.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 0-a | 운영 ro 로 미지정 30 확인 · 다르면 멈춤 | **했다** — 30 |
| 0-b | 건너뜀 | **했다** — 건너뛰었다 |
| 1-a | 결산 식 → 합계 칸 → 지출 범위를 새 함수로 · 글자 비교는 그대로 | **했다** — `결산식으로` · 아는 꼴의 식만 푼다(첫 검토) |
| 1-b | 30 건 전부 나와야 · 안 나오면 멈춤 | **했다** — 30 전부 한 줄씩 |
| 2-a | 재정짝잇기.py · 미리보기 · --실행 · 사본 · 파일 다르면 멈춤 | **했다** — 사본은 채울 것이 있을 때만 뜬다 |
| 2-b | 미지정 지출만 | **했다** — 들여온 지출 중 미지정만(들인 뒤 앱에서 만든 미지정은 세기만) |
| 2-c | 채울 수 · 가리키는 줄이 살아 있는지 · 취소면 짝 표 | **했다** — 식이 하나가 아니거나 들여온 줄이 아닌 것도 짝 표 |
| 2-d | 예산 항목 + 구분·항목·세부항목-1 글자 | **했다** |
| 3-a | 채운 뒤 미지정 수 | **다르게 했다** — 운영은 미리보기까지라 사본 리허설에서 셌다(30 → 0) |
| 3-b | 채운 30 이 식이 가리키는 줄과 같은지 | **다르게 했다** — 사본 리허설 뒤 「이미 이은 97 중 식과 다른 것 0」 과 실행 안의 다시 세기로. 운영은 다음 판 |
| 3-c | 두 번째 「채울 것이 없습니다」 | **다르게 했다** — 사본 리허설과 시험에서. 운영은 다음 판 |
| 4-a | 안내문 닫기 · 지우지 않음 | **했다** |
| 4-b | 인계 · BB-h 「시트 결산 식으로 다시 이음」 | **했다** |
| 4-c | 봐둘것 새로 · 다음 시트 방식을 사람이 정함 | **했다** — BB-i |
| 가~사 | 수용 기준 | **다르게 했다** — 라) 마) 는 운영이 아니라 사본 리허설과 시험으로(운영 실행은 다음 판) |
| 끝줄 | 운영은 0단계 읽기만 · 2단계는 미리보기까지 · --실행 은 「그대로 해」 뒤 | **했다** |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
