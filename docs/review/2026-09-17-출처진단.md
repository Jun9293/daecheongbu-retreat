사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: 시드 업무의 처리 갈래(확정된 것 ㄱ·ㄴ·ㄷ · 확정 못 한 것 ㄹ·ㅁ) · 다시 안 새게 하는 제안 ①~③ · 출처 불명 수를 판단 근거로 쓸지 참고값으로만 둘지 (봐둘것 BD-a · 2장)
막혀서 못 한 것: 화면 점검(drawer.js · phone.js)은 로그인된 브라우저가 필요해 여전히 못 돌았다 — 이 판은 화면을 안 고쳤다 (11-2 · 인계 맨 위)

작성 시각: 2026-09-17 12:40 (KST)
커밋: 8577760 다음 — 이 판의 커밋들(4장 파일 목록)
작업: 운영 DB 에 낀 테스트/오류 자료 진단 — 추측 금지, 전부 흔적으로 확인

---

**이 판은 진단만 했다.** 운영 DB 는 `mode=ro` 로만 열었고, 노션은 안 열었다(이미 저장소 밖에 옮겨 둔 `data/노션업무전체.real.tsv` 만 읽었다).
코드는 안 고쳤다. **값(제목)은 이 보고에 안 싣고 id 로만 가리킨다.** 운영 시각은 UTC 로 저장돼 있어(`models._now`) KST 로 옮겨 적는다.
2장의 수는 저장소 밖 `data/출처진단.real.py`(읽기만 · 수만 찍음)가 다시 낸다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 코드를 안 고쳐 해당 없음 — `LIBRARY_ONLY` 를 부르는 자리는 `seed.py` · `seed_library.py` 둘이고(진단용으로 읽음) 건드리지 않았다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면을 안 고쳤다 |
| 그 값을 세어 둔 문서 | 봐둘것 BC-c 의 「적어만 둔다」 두 줄(사람 확정을 덧붙임) · 인계 맨 위. 새로 센 수는 봐둘것 BD-a · 인계 맨 위 · 이 보고에 적었다 |

# 1. 0단계 — run 98 · 99 · 101 의 출처 (확정)

**0a · 0d — 몇 커밋의 무슨 스크립트가 언제 만들었나**

- 커밋 `730541a`(2026-08-29 00:28 KST)가 `seed_library_data.py` 에 `LIBRARY_ONLY` 여섯 줄을 넣었다. 그 뒤로 안 바뀌었다(`git blame` · 주석 한 줄만 `10ac176`).
  파일 머리가 출처를 목업(`docs/mockups/retreat-setup.html`)으로 적고, 목업에 같은 여섯 줄이 예시 행으로 있다(`git log -S` 로 그 제목이 들어온 커밋은 `730541a` · `10ac176` 둘뿐)
- `seed.py` → `seed_library.seed_all` → `build_library` 가 `LIBRARY` 96 다음에 `LIBRARY_ONLY` 를 순서대로 세워 **라이브러리 id 97~102**, `_add_runs` 가 **run id 97~102**(그 회차 미실행)를 만들었다
- 운영의 생성 시각: 회차 1 `2026-08-29 14:00:36.716 UTC`, 라이브러리 98·99·101 `14:00:37.007~.008`, run `14:00:37.026~.027` → **2026-08-29 23:00:36~37 KST**
- 그 1 초 안에 선 행 가운데 **지금 남은 수**가 그때의 시드가 만드는 수와 맞는다. 코드 판은 **`463b73c`(22:52 KST) 이후 · `6365130`(23:01:39 KST) 커밋 전의
  작업 트리**까지만 좁혀진다 — 그 사이 시드 파일(`seed_data.py` · `seed_library.py` · `seed_library_data.py`)은 같고, `seed.py` 는 `6365130` 에서만 바뀌었다.
  아래 오른쪽 칸은 같은 내용인 `ea5c329` 에서 셌다:

| 표 | 운영(그 1 초 · 지금 남은 것) | 그때의 시드 |
|---|---|---|
| retreats | 1 (이름 같음) | `RETREAT_NAME` |
| task_library · task_runs | 102 · 102 | `LIBRARY` 96 + `LIBRARY_ONLY` 6 |
| tasks(옛 할 일) | 175 | `TASKS` 175 |
| checklists | 5 | `CHECKLISTS` 5 |
| meetings | 3 | `MEETINGS` 3 |
| expense_entries | 12 | `MEAL_EXPENSES` + `GENERAL_EXPENSES` 12 |
| discussion_entries | 18 | `DISCUSSIONS`(고침 포함) 18 |
| file_assets · review_requests | 2 · 1 | 데모 파일 2 · 확인 요청 1 |
| users | 1 | **같지 않다** — 시드 19(`USERS` 14 + 부서 리더 5). 나머지는 뒤에 계정 정리로 지워졌다(0장 · 2026-09-09) |

  그때 `seed.py` 는 데모 갈래 하나뿐이었다(`if not demo` 는 `9fd68e9` · 2026-08-31 에 생김). 그 초 이후 지워진 행이 없었는지는 users 말고는 확인하지 않았다(지출은 취소 표시라 남는다).
  **`6365130` 의 커밋 메시지가 「곁들여 seed.py --reset 을 고쳤다. 서버가 켜진 채로는 drop_all 이 파일을 완전히 비우지 못해 옛 데이터가 남는다 (이번 작업 중 실제로 겪었다)」 고 적고**, 회차 id 가 1 이며 첫 활동 기록이 3 분 뒤(23:03:41 KST)다.
  **결론: `730541a` 가 넣은 `LIBRARY_ONLY` 를, `463b73c` 이후 · `6365130` 커밋 전 작업 트리의 `seed.py` 가 2026-08-29 23:00:36 KST 에 넣었다.**
  돌린 명령줄 자체의 기록은 없다 — 확정의 근거는 시각 · 수 · 순서 · 그 커밋 메시지다
- **그때는 운영이 아니었다** — 배포 준비는 `9fd68e9`(2026-08-31 09:06 KST · `serve.bat` · `backup.py` 도 그때 생김). 시드는 그때 하나뿐인 DB(`data/app.db`)에 들어갔고
  **그 DB 가 시드를 안은 채 운영이 됐다.** 샌 길은 「데모 시드가 든 DB 가 그대로 운영으로 올라갔다」 이다
- 활동 기록에서 run 98·99·101 을 가리키는 줄은 0 — 시드는 활동 기록을 안 남겼다

**0b — 번호가 어느 범위인가**

- run_no 98 · 99 · 101 · library_id 98 · 99 · 101 — **원래 범위(1~102)** 안이다. 노션 범위(run_no 103~335 · 라이브러리 107~339) 밖
- 회차 2(2027)의 run 200 · 201 · 203(run_no 102 · 103 · 105)이 같은 라이브러리를 가리킨다 — 마법사가 회차 2 를 연 순간(2026-08-31 00:17:50 KST ·
  활동 기록 `회차_생성` 과 같은 초)에 꺼진 채로 복사됐다

**0c — 운영에 업무를 직접 만든 화면 점검 · 시험이 있었나**

- 운영 라이브러리의 생성 시각은 **셋으로만 갈린다** — 시드 102(2026-08-29 KST) · 마법사 제안 4(2026-08-31 00:17 KST · id 103~106 · `origin=claude_suggestion`) ·
  노션 233(2026-09-16 · `notion_page_id` 있음). 그 밖의 순간에 선 업무가 없다
- 커밋 제목에 「화면 점검」 이 든 판(`1970d10`~`d612539` · 2026-09-15)은 그 커밋 메시지들이 「개발 서버에서 눌러 저장까지 확인」 · 「개발 DB 다시 만들기」 라고 적고,
  그 시각에 운영에 선 업무는 0
- 2026-09-13 의 사고(시험이 운영 표를 비움)는 되돌린 사본으로 돌아갔고 새 업무를 만들지 않았다(봐둘것 AZ-a · 생성 시각이 셋뿐인 것과 같은 근거)

**걸린 것 (3a 의 비용)** — run 98·99·101·200·201·203 에 논의 · 논의 잇기 · 첨부 · 확인 요청 · 알림 기록 · 활동 기록이 **모두 0**.
라이브러리 98·99·101 을 관련업무 · 선행 · 상위로 가리키는 줄 0, 다른 run 의 `blocked_by_run_ids` 참조 0.

# 2. 1 · 2 · 3 · 4단계

**1 — 노션 속성 이름 ↔ 앱 표시 이름 (코드로 확인)**

「메인업무 · 서브 · 일정 업무」 라는 말은 코드(`app/` · 템플릿 · 들이기 스크립트)에 **없다.** 노션의 값은 `Main` · `Sub` · `Sch` 이고, 앱의 저장값과 표시 이름은 이렇다.

| 노션 속성 · 값 | 들이기(`scripts/노션업무들이기.py`) | 앱 저장값 | 앱 표시(`app/models.py` 의 `TASK_KIND_LABELS`) |
|---|---|---|---|
| 업무속성 `Main` | `구분표` | `main` | Main |
| 업무속성 `Sub` | `구분표` | `sub` | 하위 |
| 업무속성 `Sch` | `구분표` | `schedule` | 일정 |
| 업무속성 (빈 값) | `--구분없음`(기본 `main`) · D-주차 표지는 뺌 | `main` | Main |
| 할 일 | 제목 | `TaskLibrary.title` | 제목 |
| 담당팀 | `팀표` → 부서 키 | `default_department_key` · run 의 부서 | 담당팀 |
| 관련 팀 | `팀표` → 부서 키 | `related_department_keys` | 관련팀 |
| 일자(시작 · 마감) | `dweek.relative_position` | D-주차 네 값 · run 의 시작 · 마감 | 기간 |
| 관련업무 | 노션 id → 라이브러리 id(양쪽에) | `related_library_ids` | 연결 › 관련 |
| 설명 | 업무 규칙 | `rules` | 업무 규칙 |
| 진행 상황 · 행사 전후 | 안 들임 | — | — |

tsv 의 업무속성 분포는 Main 87 · Sub 74 · Sch 62 · 빈 값 34(표지 24 포함 · 합 257) — 속성 값의 이름만 센 것이다.

**2 — 운영 전체의 출처 불명 (기준 · 수)**

기준(순서대로): ① `notion_page_id` 있음 → 노션 ② `origin=claude_suggestion` → 마법사 제안(회차 2 를 연 순간에 섬)
③ 나머지(시드)는 제목을 노션 줄(`노션업무들이기.읽는다` 가 표지를 뺀 233 줄)의 제목과 견줌 — **한글 · 영문 · 숫자만 남긴 뒤** 같으면 「있음」,
한쪽이 다른 쪽에 들어 있고 짧은 쪽이 3 글자 이상이면 「부분」, 아니면 「없음」.

| 무리 | 라이브러리 | 회차 1 run | 회차 2 run (산 / 뺀) | 논의 잇기 · 첨부 · 확인 요청 (회차 1 / 2) |
|---|---|---|---|---|
| 노션 | 233 | 233 (산) | — | 0 |
| 마법사 제안 | 4 | — | 4 / 0 | 0 |
| 시드 · 있음 | 50 | 50 (뺌) | 50 / 0 | 22·3·1 / 24·1·0 |
| 시드 · 부분 | 32 | 32 (뺌) | 30 / 2 | 6·0·0 / 6·0·0 |
| **시드 · 없음 = 출처 불명** | **20** | 20 (뺌) | **16** / 4 | 0 / 0 |

- **출처 불명 합계 — 라이브러리 20 · run 40.** 알림 기록은 모든 무리에서 0. 회차 1 의 합(논의 잇기 28 · 첨부 3 · 확인 요청 1)은 BC-c 의 수와 같다
- **이 수는 비교 규칙의 결과다** — 첫 판은 「빈칸 · 가운뎃점 등」 을 빼는 규칙으로 21 · 42 를 냈고, 검토가 다른 규칙으로 20~35 가 나온다고 짚었다(6-1 [D]).
  규칙을 위처럼 못박고 스크립트를 남겨 다시 셌다. 규칙에 안 흔들리는 것은 99~102 가 늘 「없음」, 97 · 98 이 늘 「부분」 이라는 것뿐이다
- 「부분」 은 우연이 섞인다 — 총무님이 없다고 한 98 이 「부분」 으로 갔다. 부분 32 도 출처가 확인된 것이 아니다
- 없음 20 = `LIBRARY_ONLY` 넷(99~102) + `LIBRARY` 16(Main 10 · Sub 6). 그 Main 중 8 은 `reclassification_note`(노션을 재분류해 묶은 까닭)가 있다 — 글 흔적은 있고 어느 노션 줄인지는 없다
- **확정 못 함** — 97 · 100 · 102 는 98 · 99 · 101 과 같은 목록 · 같은 커밋 · 같은 스크립트지만 「노션에 없었다」 는 사람 확인이 없다
- **2027 회차에 산 채로 46**(없음 16 · 부분 30)

**3 — 처리 제안 (실행 안 함)** — 봐둘것 BD-a 에 갈래와 대가를 적었다

- 확정된 것: ㄱ 라이브러리 `archived_at`(행 남음 · 마법사 · 업무 추가 · 분류 · 제안이 이미 거름 · 잃는 것 0 · 찍는 스크립트가 필요) ·
  ㄴ 행 삭제(run 6 · 라이브러리 3 · 함께 사라지는 것 0 · **0장 예외를 먼저 적어야 함**) · ㄷ 그대로(6-2 가 지어낸 「미실행」 을 계속 읽음).
  어느 갈래든 시드에서 `LIBRARY_ONLY` 를 걷는 것은 따로 정한다
- 확정 못 한 것: ㄹ 저장소 밖 사람 확인표(이때 2장의 수는 참고값) · ㅁ 보류(2027 의 산 46 은 섞인 채). 새 격리 칸은 읽는 화면이 없으면 뜻이 없다

**4 — 기록과 재발 막기**

- 봐둘것 **BD-a** 에 전부 적었다. 재발 막기 제안:
  ① 시드 행에 표지 + 자가진단이 표지 · 출처 열쇠 없는 업무 수를 말함 — **이번 길(데모 DB 가 그대로 운영이 됨)을 막는 것은 이것**
  ② `seed.py` 가 회차가 있는 DB · 운영 경로면 멈춤 — 이번 길은 못 막고, 운영이 된 뒤 다시 시드를 도는 길을 막음
  ③ 목업 예시를 시드에 안 넣는다는 규칙을 6-9 로 넓힘 — 이번 여섯이 들어온 입구
- 봐둘것 BC-c 의 두 줄(되살릴 때 날짜 · 상위-하위)에 사람 확정(「그 일을 하는 판이 오면 올린다」)을 덧붙였다

# 3. 검사 · 시험 · 앱

- 글검사 · pytest: 보고를 다 채운 뒤 돌린 결과를 아래 「8. 마지막으로 돌린 것」 에 싣는다
- **앱은 재시작하지 않았다** — 코드를 안 고쳤다(11-3 2단계 건너뜀). **다만 포트를 쥔 PID 가 12316 → 2776 으로 바뀌어 있었다** —
  시스템 기록에 2026-09-17 10:19:42 KST 의 「전원 끄기」 종료 이벤트가 있고, 11:25:54 에 부팅했으며, 앱이 11:26:21 에 스스로 섰다(작업 스케줄러 · 11-2).
  이 창이 한 것이 아니다. `/login` 200
- 운영 DB 는 `sqlite3` 의 `mode=ro` 로만 열었다. 백업 · 노션 · 업로드는 안 건드렸다

# 4. 파일

`git diff --name-only 8577760..HEAD` 는 8장에 싣는다. 손으로 적은 목록:

- `docs/봐둘것.md` — BC-c 두 줄에 사람 확정 · 새 항목 BD-a
- `docs/인계.md` — 맨 위 갱신 줄
- `docs/review/2026-09-16-노션줄기닫기.md` — 앞 판 보고를 얼림
- `docs/review/최근.md` — 이 보고
- 저장소 밖: `data/출처진단.real.py`(수를 다시 내는 스크립트 · gitignore)

# 5. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 출처를 흔적으로 확정하거나, 못 하면 이유 · 후보 · 근거 | O | 1장 — 98·99·101 확정(시각 · 수 · 순서 · 커밋 메시지 · 코드 판은 범위로). 97·100·102 는 출처는 같고 사람 확인만 없다고 갈라 적음 |
| 나) 노션 속성 이름 ↔ 앱 이름을 코드로 확인 | O | 2장 1 — `구분표` · `TASK_KIND_LABELS` · `팀표` |
| 다) 운영 전체 출처 불명 총수 | O | 2장 2 — 라이브러리 20 · run 40 (규칙 · 스크립트 함께 · 규칙의 결과라고 밝힘) |
| 라) 실행 없음 · 진단만 | O | 3장 — `mode=ro` · 코드 안 고침 · 재시작 안 함 |
| 마) pytest 옵션 없이 전부 통과 · 글검사 7/7 | O | 8장 |
| 바) 검토자 원문을 최근.md 에 그대로 | O | 6장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| BC-c 마지막 둘 확정 | 했다 | 봐둘것 BC-c 두 줄 |
| 0a | 했다 | 1장 — created_at · 활동 기록 0 · 커밋 시각 대조 |
| 0b | 했다 | 1장 — 원래 범위(1~102) |
| 0c | 했다 | 1장 — 생성 순간이 셋뿐 · 화면 점검 판의 커밋 메시지 |
| 0d | 했다 | 1장 — 「`730541a` 의 `LIBRARY_ONLY` 를 `463b73c` 이후 · `6365130` 커밋 전 작업 트리의 `seed.py` 가 2026-08-29 23:00:36 KST 에」. 코드 판은 한 커밋으로 못 좁혀 범위로 적었고, 명령줄 기록이 없다고 함께 적음 |
| 1 | 했다 | 2장 1 |
| 2a | 했다 | 2장 2 — 20 · 40 |
| 2b | 했다 | 2장 2 — 기준 세 단계 · 비교 규칙을 글자 단위로 |
| 3a | 했다 | 2장 3 · BD-a — 비용(걸린 것 0) 포함 |
| 3b | 했다 | 2장 3 · BD-a |
| 4 | 했다 | BD-a — 조사 전부와 재발 막기 셋(이번 길을 무엇이 막는지 함께) |
| 추측 금지 · 흔적만 | 했다 | 「~일 것이다」 없음. 흔적이 없는 것은 「기록이 없다」 · 「확인하지 않았다」 · 「확정 못 함」 으로 적음 |
| 값은 보고 · 덩어리에 안 싣는다 | 했다 | id 와 수만 |
| 검토자 원문을 그대로 싣는다 | 했다 | 6장 — 전사 기록에서 뽑아 그대로 |
| 운영 · 노션은 읽기만 | 했다 | 노션은 안 열었고 저장소 밖 tsv 만 읽음 |
| 앱을 안 고치면 재시작 안 함 | 했다 | 3장 — PID 가 바뀐 것은 부팅 때문이라고 근거와 함께 적음 |
| 마무리는 11-3 대로 | 다르게 했다 | 화면 점검은 로그인이 필요해 못 돌렸다(11-2) — 화면을 안 고친 판. 재시작은 규칙대로 건너뜀 |

# 6. 검토자 원문

## 6-1. 커밋 전 검토

```
━━━ 검토자 여기부터 ━━━ 범위 8577760..작업 트리(커밋 전) · 커밋 0개 · 파일 4개

검토자가 한 것
  읽은 것: docs/review/최근.md 전체, docs/봐둘것.md 와 docs/인계.md 의 diff, 얼린 판.
  운영 DB 는 sqlite3 file:...?mode=ro 로만 열었고 git log 와 git show 로 커밋을 대조했다.
  글검사를 한 번 돌렸다(읽기만 함). 저장소 파일은 하나도 고치지 않았다.
  임시 스크립트는 scratchpad 에 검토자_* 이름으로 두었다. 첫 셸 명령을 잘못 적어
  $TEMP/claude/rv1.py 에 빈 파일이 하나 생겼을 수 있다. 저장소 밖이다.

다시 잰 것 (보고와 맞음)
  - 라이브러리 97~102 는 2026-08-29 14:00:37.007 UTC 에 생겼고 origin=history, notion id 없음.
    run 97~102 (회차 1)와 199~204 (회차 2)는 모두 included=0.
    200·201·203 의 run_no 는 102·103·105.
  - 그 1초(14:00:36~38 UTC)에 선 행: 회차 1 · 라이브러리 102 · run 102 · tasks 175 ·
    checklists 5 · meetings 3 · expense 12 · discussion 18 · file_assets 2 ·
    review_requests 1 · users 1.
  - 라이브러리의 생성 순간은 셋뿐이다: 102 · 4 · 233.
  - 회차 2 는 산 100 · 뺀 6 이고, 뺀 6 이 곧 LIBRARY_ONLY 여섯이다.
  - run 98·99·101·200·201·203 에 걸린 것은 논의 · 논의 잇기 · 첨부 · 확인 요청 · 알림 기록 ·
    활동 기록 모두 0 이다. 라이브러리 관련 · 선행 · 상위 참조와 blocked_by 참조도 0.
    가장 이른 활동 기록은 14:03:41 UTC 다.
  - tsv 분포는 Main 87 · Sub 74 · Sch 62 · 빈 값 34 (합 257).
    TASK_KIND_LABELS 는 main→Main · sub→하위 · schedule→일정, 구분표는 Main/Sub/Sch.
    「메인업무 · 서브 · 일정 업무」 는 app/ 과 들이기 스크립트에 없다.
  - PID: 부팅 11:25:54, 이벤트 6005 는 11:26:11, 포트 8000 주인은 2776 (11:26:21 생성).
  - 얼린 판 docs/review/2026-09-16-노션줄기닫기.md 는 HEAD 의 최근.md 와 줄 끝(CRLF)만 다르다.
    autocrlf=true 라 커밋할 때 맞춰진다.

막아야 함

  [A] 지시문 대조 표에 「검토자 원문을 그대로 싣는다」 줄이 없다
      docs/review/최근.md 150~167
      지시문에 있는 항목인데 수용 기준 바)에만 있고 대조 표에는 없다.
      검토-원칙 8-1 에 따라 한 줄 더한다.
      (나머지 항목 0a~0d · 1 · 2a · 2b · 3a · 3b · 4 · BC-c · 읽기만 · 재시작 · 11-3 은 표에 있다.)

고쳐야 함

  [B] 「23:00 직전 마지막 커밋 ea5c329(22:23 KST)」 가 사실이 아니다
      docs/review/최근.md 32 · 46 · 156, docs/봐둘것.md BD-a 「누가 · 언제」 줄
      22:23 과 23:00:36 사이에 560fa73(22:42:55)과 463b73c(22:52:25)가 있다.
      시드 1분 뒤에는 6365130(23:01:39)이 있다.
      시드 파일(seed_data · seed_library · seed_library_data)은 ea5c329..6365130 동안 같고,
      seed.py 는 6365130 에서만 바뀌었다(--reset 고침).
      그래서 수를 견주는 것으로는 ea5c329 를 골라낼 수 없다.
      적을 수 있는 것은 「463b73c 이후, 6365130 커밋 전의 작업 트리」 까지다.
      보강할 흔적이 있는데 보고가 인용하지 않았다.
        6365130 의 커밋 메시지: 「곁들여 seed.py --reset 을 고쳤다 …
        (이번 작업 중 실제로 겪었다)」
      이 메시지와 「회차 id 1 · 첫 활동 기록이 3분 뒤」 가 함께 --reset 실행을 받친다.
      이것을 넣으면 확정의 근거가 수 · 순서만일 때보다 단단해진다.

  [C] 「운영 DB 로 넣었다」 는 그때 기준으로 맞지 않는다
      docs/review/최근.md 46, docs/인계.md 3, docs/봐둘것.md BD-a
      2026-08-29 에는 배포 전이었다(배포 준비는 9fd68e9 · 08-31 09:06,
      serve.bat 과 backup.py 도 그때 생김). data/app.db 가 유일한 DB 였다.
      그 DB 가 시드를 안은 채 운영이 된 것이다.
      이 차이 때문에 재발 막기 제안 1(「운영 경로면 멈춘다」)은 이번 경로를 못 막는다.
      그때는 빈 DB 였고, 경로도 아직 운영이 아니었다.
      실제로 샌 길은 「데모 시드가 든 DB 가 그대로 운영으로 올라갔다」 이다.
      제안 셋 중 무엇이 이 길을 막는지 한 줄 적어 달라.

  [D] 출처 불명 기준이 다시 셀 수 있게 적혀 있지 않고, 수가 규칙에 따라 크게 움직인다
      docs/review/최근.md 91, docs/봐둘것.md BD-a 기준 ③
      「빈칸과 가운뎃점 등을 뺀 뒤」 의 「등」 이 무엇인지 안 적혔고, 센 스크립트도 남지 않았다.
      검토자가 같은 tsv(읽는다 · 233줄)와 시드 102 로 다시 셌다(있음 · 부분 · 없음).
        한글 · 영문 · 숫자만 남기고 비교:  50 · 32 · 20
          없음 중 LIBRARY 는 16(Main 10 · Sub 6), 2027 산은 16
        빈칸과 · 만 빼고 비교:              43 · 24 · 35
        보고:                               49 · 32 · 21 (2027 산 17)
      규칙이 바뀌어도 흔들리지 않는 것은 둘뿐이다.
        99~102 는 늘 「없음」, 97·98 은 늘 「부분」
      「출처 불명 = 라이브러리 21 · run 42 · 2027 산 17」 은 헤드라인이자 인계와 사람칸에
      실린 수다. 그러니 뺀 글자를 정확히 적고, 그 수가 비교 규칙의 결과라고 밝혀 달라.
      scratchpad 에도 규칙이 남지 않았다.
      사람 확인표(ㄹ)로 가면 이 수는 참고값이 된다는 것도 함께.
      세 파일 사이에서는 서로 맞다(21 · 42 · 17 · 47).

  [E] 인계가 「PID 12316 그대로」 라고 적는다
      docs/인계.md 3
      보고 124~125 와 지금 포트 주인(2776)이 다르다.
      같은 판 안에서 옮겨 적은 곳이 어긋났다(검토-원칙 4).

  [F] 보고의 검사 · 시험 줄이 아직 사실이 아니거나 가리키는 곳이 없다
      docs/review/최근.md 122, 123, 145
      - 122 「글검사 7/7 통과」: 지금은 6/7 이다. check_placeholder 가 146줄의 「6장」 에 걸린다
        (예상된 빨강). 6 · 7장을 채운 뒤 다시 돌린 결과로 적는다.
      - 123 「끝 줄은 아래 「pytest」 에」: 보고에 그런 절이 없다.
        수용 기준 마)를 O 로 찍었으니 pytest 마지막 줄을 싣는다.
        11-3 4단계가 요구하는 git diff --name-only 도 없다(4장은 손으로 적은 목록이다).
        이 꼴의 빈 자리는 check_placeholder 가 못 본다(14장).

  [G] 마법사 제안의 날짜가 UTC 로 남았다
      docs/review/최근.md 58
      「마법사 제안 4(08-30 …)」 는 UTC 날짜다. KST 로는 08-31 00:17 이다(같은 보고 53 · 14줄 「KST 로 옮겨 적는다」).

  [H] 사람칸 「정할 것」 에 이유가 섞였다
      docs/review/최근.md 4
      11-3 4단계에 「이유는 여기 적지 않고 어디에 있는지만 가리킨다」 고 되어 있다.
      지금은 갈래 설명과 괄호 속 까닭까지 한 줄에 담겼다. 갈래 이름과 BD-a 가리키기로 줄이면 된다.
      모양 검사(check_handoff)는 통과한다.

봐 둘 것

  [I] 확정이 「짐작」 선을 넘었는가 (물음 1)
      넘지 않았다고 본다. 넷이 한 초 안에서 서로 맞는다.
        - 생성 시각
        - 당시 시드 목록과 같은 수
        - LIBRARY 96 다음 LIBRARY_ONLY 로 이어지는 id 순서
        - 6365130 메시지의 --reset
      다만 표의 users 줄(최근.md 43)은 「같다」 가 아니다.
        ea5c329 의 USERS 는 14 줄이다.
        운영 1 은 지금 남은 수이고(id 1 · admin · 그 초에 생성), 나머지는 뒤에 정리됐다.
        「전부 같다」 에서 이 줄은 빼거나 「시드 14 · 지금 1」 로 적는 편이 정확하다.
      다른 수(지출 12 · 논의 18 등)도 「지금 남은 것 중 그 초에 선 수」 다.
      이후 지운 행이 없었다는 것은 보고가 확인하지 않았다.
      (지출은 취소 표시라 남는다.)

  [J] PID 근거 (물음 5)
      충분하다. 더할 만한 흔적이 하나 있다.
      시스템 로그의 종료 이벤트 1074 가 10:19:42 에 있다.
      건 프로세스는 RuntimeBroker 이고 종류는 전원 끄기다.
      시작 메뉴에서 사람이 끈 꼴로 보이며, 「이 창이 한 것이 아니다」 를 받친다.
      장치 이름은 여기 옮기지 않았다.

  [K] 0c 「화면 점검 판은 개발 DB 에서 돌았다」
      생성 시각만으로는 「운영에 안 만들었다」 까지만 나온다.
      그 판들의 커밋 메시지(「개발 서버에서 눌러 저장까지 확인」, 1970d10 「개발 DB 다시 만들기」)가
      「개발 DB」 를 받친다. 근거로 그 메시지를 짚어 두면 좋다.

  [L] 값 새는 곳 (물음 3)
      새 글에 업무 제목 · 사람 이름 · 연락처 · 계좌 · 바깥 주소는 없다.
      BD-a 의 「Main 을 신설해 묶었다」 는 공개 저장소의 seed_library_data.py 글자 조각이다.
      운영 값이 아니다. check_names 는 통과했다.

  [M] 지시문 대조 표의 「다르게 했다」 (물음 4)
      11-3 줄 하나뿐이다. 0d 는 명령줄 기록이 없어 [B] 의 폭으로만 확정할 수 있다.
      그러니 「했다」 로 두려면 비고를 「463b73c~6365130 사이 작업 트리」 로 고쳐야 한다.

  [N] 셈 · 재발 막기 제안의 범위
      없음 21 에 속한 LIBRARY 17 이 [D] 로 흔들린다.
      「그 17 중 Main 8 은 reclassification_note」 도 같다(검토자 셈에서는 note 8 이 나왔다).

지시문 대비
  [A] 말고 빠진 항목은 없다.
  사람칸 다섯 줄은 순서와 값 모두 있다(check_handoff 통과).

사람에게 묻는 것
  1. 출처 불명의 비교 규칙을 무엇으로 둘지.
     규칙에 따라 20~35 로 움직인다. 이 수를 판단 근거로 쓸지, ㄹ(사람 확인표)의 참고값으로만 둘지.
  2. 「운영에 넣었다」 를 「데모 시드가 든 DB 가 그대로 운영이 됐다」 로 고쳐 적을지.
     그에 맞춰 재발 막기 제안(특히 1번)을 다시 볼지.
  3. 확정 폭을 「ea5c329 판」 이 아니라 「463b73c~6365130 사이 작업 트리」 로 두는 데 동의하는지.
━━━ 검토자 여기까지 ━━━
```

## 6-2. 다시 보인 뒤 (검토 뒤 고친 차이만)

```
━━━ 검토자 여기부터 ━━━
범위: 8577760..작업 트리(커밋 전)의 다시 보기. 첫 검토 뒤에 고친 차이만 봤습니다(최근.md 7-1 표 · 봐둘것.md BD-a · 인계.md 맨 위 · 저장소 밖 data/출처진단.real.py).

**검토자가 한 것**
- 스크립트를 한 번 돌렸습니다: `data/출처진단.real.py` (종료 코드 0 · 운영 DB 는 mode=ro).
- 따로 잰 것
  - 운영 DB 를 mode=ro 로 읽어, 노션·마법사 무리의 run 수와 걸린 수를 셌습니다.
  - 그 1 초에 선 지출·체크리스트·회의의 지금 상태를 봤습니다.
  - 회차 2 를 연 시각을 봤습니다.
- 대조한 커밋
  - `git show`: 463b73c 의 seed_data.USERS · seed_library.EXTRA_USERS
  - `git log`: 463b73c^..6365130 과 6365130 의 메시지
  - `git show`: 9fd68e9 의 stat 과 seed.py
- 글검사를 읽기만으로 돌렸습니다. 파일은 하나도 고치지 않았습니다.

**다시 잰 것 (보고와 맞음)**
- 스크립트 결과: 노션 233 · 마법사 4 · 시드-있음 50 · 부분 32 · 없음 20.
  - 없음 20 중 id ≤96 은 16 개(Main 10 · Sub 6)입니다. note 가 붙은 것은 Main 8 이고 Sub 는 0 이라, 「Main 2 · Sub 6 은 흔적 없음」 이 맞습니다.
  - 99~102 는 없음, 97·98 은 부분에 있습니다.
  - 회차 2 의 산/뺀
    - 없음 16/4 — 뺀 4 는 99~102
    - 부분 30/2 — 뺀 2 는 97·98
    - 그래서 산 채로 있는 것은 16+30 = 46 입니다.
  - 논의 잇기·첨부·요청 수
    - 회차 1 의 합이 28·3·1 로 BC-c 와 같습니다.
    - 회차 2 는 있음 24·1·0, 부분 6·0·0 입니다.
  - 알림 기록은 전부 0 입니다.
- 노션 run 233 은 전부 회차 1 에 있고 전부 산 채입니다. 마법사 run 4 는 전부 회차 2 에 있고 전부 산 채입니다. 이 둘에 걸린 논의 잇기·첨부·확인 요청은 0 입니다. 따라서 표의 「— · 0」 이 맞습니다.
- 마법사 라이브러리 103~106 은 2026-08-30 15:17:50 UTC(= 08-31 00:17 KST)에 생겼습니다. `회차_생성` 도 같은 초입니다.
- 사용자 19 는 USERS 14(463b73c) + EXTRA_USERS 5(`seed_all` 이 부름)로 맞습니다.
- 커밋 대조
  - 463b73c(22:52:25) 다음이 6365130(23:01:39)이고, 그 사이 seed.py 만 바뀌었습니다.
  - 6365130 메시지에 「곁들여 seed.py --reset 을 고쳤다 … (이번 작업 중 실제로 겪었다)」 가 있습니다.
  - 9fd68e9 는 2026-08-31 09:06 입니다. 여기서 scripts/backup.py · serve.bat 가 들어왔고 `if not demo` 도 생겼습니다.
- BD-a 의 곁말도 맞습니다.
  - 그 초의 지출 12 는 12 개 모두 canceled_at 이 2026-09-15 입니다.
  - 체크리스트 5 는 5 개 모두 moved_at 이 찍혀 있습니다.
  - 회의 3 은 3 개 모두 notion_page_id 가 NULL 입니다.
- 세 파일의 수가 서로 맞습니다. 20·40·16 은 세 곳 모두, 46 은 보고·BD-a, 20~35 는 BD-a·보고 7-1 에 있고 서로 어긋나지 않습니다.
- 값 새는 곳: 새 글에 업무 제목·사람 이름·연락처·계좌·바깥 주소가 없습니다. check_names 도 통과했습니다. 스크립트는 gitignore(`data/*.real.*`)에 걸려 저장소 밖에 있고, 출력도 id 와 수뿐입니다.
- 첫 검토의 [A]~[N] 은 7-1 표대로 반영됐습니다.

**막아야 함**
없음

**고쳐야 함**

[O] 인계가 사람이 정할 것을 미리 정해 적었습니다
- 자리: docs/인계.md 3 — 「이 수는 비교 규칙의 결과라 참고값이다」
- 무엇이 문제인가: 사람칸 「정할 것」(최근.md 4)은 이 수를 「판단 근거로 쓸지 참고값으로만 둘지」 사람에게 묻습니다. BD-a(3655~)와 보고 2장 3 은 「사람 확인표(ㄹ)로 가면 참고값」 이라고 조건을 붙여 적었습니다.
- 인계만 조건 없이 「참고값이다」 로 끝나 사람의 결정을 앞질렀습니다.
- 고치는 법: 「비교 규칙의 결과다 — 판단 근거로 쓸지는 사람이 정한다(BD-a)」 쯤으로 적으면 됩니다.

[P] 인계의 「노트북이 11:25 에 스스로 부팅돼」 는 흔적 밖입니다
- 자리: docs/인계.md 3
- 흔적이 받치는 것
  - 10:19:42 에 사람 손 꼴의 「전원 끄기」 이벤트가 있습니다.
  - 11:25:54 에 부팅했습니다.
  - 앱이 11:26:21 에 작업 스케줄러로 스스로 섰습니다(보고 3장).
- 켠 것이 사람인지 기계인지는 흔적이 없습니다.
- 「스스로」 가 붙을 자리는 노트북이 아니라 앱입니다. 추측을 금한 이 판에서 새로 생긴 틀린 말입니다.
- 고치는 법: 보고 3장처럼 「11:25 에 부팅돼(10:19 전원 끄기 이벤트) 앱이 스스로 서며 PID 12316 → 2776」 으로 적으면 됩니다.

**봐 둘 것**

[Q] BD-a 의 「검토 보고 [D]」 는 위치를 적지 않았습니다
- 자리: docs/봐둘것.md 3655
- 무엇이 문제인가: 지금 그 원문은 최근.md 6-1 에 있고, 최근.md 는 다음 판이 덮습니다. 11-3 4단계는 봐둘것이 얼린 판을 가리키게 합니다.
- 왜 급하지 않은가: 20~35 라는 수 자체는 BD-a 에 적혀 있어 잃는 것은 적습니다.
- 고치는 법: 얼릴 이름을 알 때 「docs/review/2026-09-17-….md 6-1 [D]」 로 적어 두면 길이 남습니다.

[R] 6-2 의 `@@검토2@@` 는 check_placeholder 에 안 걸립니다
- 지금 그 검사는 161줄(8장) 하나만 빨갛게 냅니다. 6장은 검토자 원문 덩어리라 검사가 보지 않는 것으로 보입니다.
- 그래서 채우는 것을 잊어도 초록이 납니다. 커밋 전에 눈으로 확인하십시오.

[S] 머리 둘째 줄이 11-3 의 꼴과 다릅니다
- 자리: 최근.md 8 — 「커밋: 8577760 다음 — 이 판의 커밋들(4장 파일 목록)」
- 11-3 은 `커밋: <짧은 해시>` 를 요구합니다. 커밋한 뒤 해시로 채우는 편이 맞습니다. 4장도 파일 목록이지 커밋 목록이 아닙니다.
- 첫 검토 때부터 있던 줄입니다.

[T] 인용에서 생략한 부분
- 자리: 최근.md 50 의 「곁들여 seed.py --reset 을 고쳤다 … 이번 작업 중 실제로 겪었다」
- 메시지 원문에서 「겪었다」 의 대상은 「서버가 켜진 채로는 drop_all 이 파일을 완전히 비우지 못해 옛 데이터가 남는다」 입니다.
- 원문 쪽이 --reset 실행을 오히려 더 직접 받칩니다. 틀린 말은 아니고, 줄임표가 그 부분을 가렸을 뿐입니다.

**지시문 대비**
- 대조 표에 원문 싣기 줄이 들어갔습니다([A] 해소).
- 0d 비고는 범위로 고쳐졌습니다.
- 사람칸 다섯 줄은 제자리에 있습니다(check_handoff 통과).
- 글검사는 지금 6/7 입니다. check_placeholder 가 8장 없음에 걸렸고, 이는 예고된 대로입니다.

**사람에게 묻는 것**
1. 출처 불명 수(20 · 40 · 16)를 판단 근거로 쓸지, ㄹ(사람 확인표)의 참고값으로만 둘지 정해 주십시오. 인계는 이미 「참고값」 으로 적어 두었습니다([O]).
━━━ 검토자 여기까지 ━━━
```

# 7. 검토 뒤 고침 (전부 문서 — 재시작 안 함)

## 7-1. 첫 검토가 짚은 것

| 짚은 것 | 고친 것 |
|---|---|
| [A] 대조 표에 원문 싣기 줄 없음 | 줄을 더함 |
| [B] 「직전 커밋 `ea5c329`」 가 사실 아님 | 코드 판을 「`463b73c` 이후 · `6365130` 커밋 전 작업 트리」 로 · `6365130` 메시지를 근거로 더함(보고 · BD-a) |
| [C] 그때는 운영이 아니었음 | 「그 DB 가 시드를 안은 채 운영이 됐다」 로(보고 · BD-a · 인계) · 재발 막기에 이번 길을 막는 것이 ① 이라고 적고 ② 는 못 막는다고 적음 |
| [D] 비교 규칙이 다시 셀 수 없게 적힘 | 규칙을 「한글 · 영문 · 숫자만 남김 · 표지 뺀 233 줄 · 부분은 짧은 쪽 3 글자 이상」 으로 못박고 `data/출처진단.real.py` 로 다시 셈 → **있음 50 · 부분 32 · 없음 20 · run 40 · 2027 산 16**(첫 판 49 · 32 · 21 · 42 · 17). 수가 규칙의 결과이고 확인표로 가면 참고값이라고 밝힘. 사람칸에 「판단 근거로 쓸지」 를 올림 |
| [E] 인계의 「PID 12316 그대로」 | 부팅으로 2776 이 됐다고 고침 |
| [F] 검사 줄이 사실이 아님 · pytest 절 없음 | 8장에 보고를 다 채운 뒤 돌린 결과와 `git diff --name-only` 를 싣는 것으로 |
| [G] 마법사 제안 날짜가 UTC | 2026-08-31 00:17 KST 로 |
| [H] 사람칸에 이유가 섞임 | 갈래 이름과 BD-a 가리키기로 줄임 |
| [I] users 줄은 같지 않음 · 「지금 남은 수」 | 표에서 users 를 「같지 않다 — 시드 19 · 지금 1」 로 갈라 적고, 그 초 이후 지워진 행을 users 말고는 확인하지 않았다고 적음 |
| [J] 종료 이벤트 1074 | 3장에 10:19:42 「전원 끄기」 를 더함(이 창이 다시 확인 · 장치 이름은 안 적음) |
| [K] 화면 점검 판의 「개발 DB」 근거 | 커밋 메시지를 짚음 |
| [M] 0d 비고 | [B] 의 범위로 고침 |
| [N] 17 · Main 8 이 흔들림 | 다시 세어 `LIBRARY` 16(Main 10 · Sub 6) · note 8 로 |
| 사람에게 묻는 것 1 · 2 · 3 | 1 은 사람칸 「정할 것」 에 올림. 2 · 3 은 사실 고침이라 위 [C] · [B] 대로 적음 |
| [L] 값 새는 곳 없음 | 고칠 것 없음 |

## 7-2. 두 번째 검토 뒤 고침 (다시 보이지 않았다 — 11-3)

| 짚은 것 | 고친 것 |
|---|---|
| [O] 인계가 「참고값이다」 로 사람 결정을 앞지름 | 「판단 근거로 쓸지는 사람이 정한다(BD-a)」 로 |
| [P] 「노트북이 스스로 부팅」 은 흔적 밖 | 「10:19 전원 끄기 이벤트 뒤 11:25 에 부팅됐고 앱이 스스로 서며」 로 — 켠 것이 누구인지는 안 적음 |
| [Q] BD-a 의 「검토 보고 [D]」 가 덮일 자리를 가리킴 | 이 판이 얼릴 이름 `docs/review/2026-09-17-출처진단.md` 6-1 [D] 로 |
| [R] 6-2 자리 표시를 검사가 못 봄 | 채웠고 눈으로 확인함 |
| [S] 머리 「커밋:」 줄의 꼴 | 고치지 않음 — 커밋 전에 쓰는 보고라 해시를 모르고, 앞 판들과 같은 꼴(「앞 해시 다음」)이다. 이 판의 커밋 해시는 덩어리 첫 줄에 있다 |
| [T] 인용의 줄임표 | 보고 · BD-a 에 커밋 메시지의 그 문장을 줄이지 않고 옮김 |
| 사람에게 묻는 것 1 | 사람칸 「정할 것」 에 이미 있음 |

# 8. 마지막으로 돌린 것 (보고를 다 채운 뒤)


`git diff --name-only 8577760`(커밋 직전 작업 트리 · 커밋 뒤 `8577760..HEAD` 와 같은 목록):

```
docs/review/2026-09-16-노션줄기닫기.md
docs/review/최근.md
docs/봐둘것.md
docs/인계.md
```

- 글검사(`scripts/글검사.py`): 7/7 통과
- pytest(옵션 없음 · `.venv/Scripts/python.exe -m pytest`) 마지막 줄: `1677 passed, 3 warnings in 1274.98s (0:21:14)`
