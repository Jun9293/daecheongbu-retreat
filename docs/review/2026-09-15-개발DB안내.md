사람이 할 것
노트북에서 직접: 하나 — 개발 DB 다시 만들기. 지울 파일은 `C:\Users\user\AppData\Local\Temp\dcb-dev\app.db` 하나(운영 `data\app.db` 와 폴더가 다름). 저장소 폴더의 PowerShell 에서 ① 개발 서버가 켜져 있으면 끄고 ② `Remove-Item "$env:TEMP\dcb-dev\app.db"` ③ `.\scripts\devserve.bat`(DB 가 없으면 기존 `seed.py` 로 새로 만들고 뜸) ④ 서버가 뜬 뒤 다른 창에서 `.venv\Scripts\python.exe scripts\check_dev_db.py; $LASTEXITCODE` 가 `0` · `(Invoke-WebRequest http://127.0.0.1:8001/login -UseBasicParsing).StatusCode` 가 `200` ⑤ 로그인하려면 첫 관리자(비밀번호가 찍힘) — `$env:DCB_DATA_DIR = "$env:TEMP\dcb-dev"` 를 먼저 치고 `scripts/계정문열기.py --첫관리자` 뒤 `Remove-Item Env:DCB_DATA_DIR`. 순서와 까닭은 인계 「개발 DB 다시 만들기 — 안내」 · 실행하고 알려 주시면 그 줄을 닫는다
관리자 권한: 없음 — app/ · templates/ · static/ 을 안 건드려 재시작하지 않았다(4장)
정할 것: 하나 — 옛 개발 DB 를 `--demo` 로 만들어 쓰셨는지. 그렇다면 새 DB 에는 demo 화면 자료(할 일 · 회의록 · 지출)가 없다 — 그대로 둘지, 안내에 `--demo` 갈래를 더할지(그 갈래는 게이트를 다시 재야 한다 · 검토 7-1 물음 1)
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 이 판은 화면 코드를 안 바꿨다(4장)

작성 시각: 2026-09-15 19:10 KST
커밋: 5b3daa3 (안내 · 얼림) · 258c67b (검토 고침) · 42569aa (두 번째 검토 뒤 고침) · 이 보고는 그다음 커밋
작업: 개발 DB 재생성 — 안내를 준비한다

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 개발 DB 경로를 정하는 곳: `scripts/devserve.bat`(`DCB_DATA_DIR`) · `app/paths.py`(`DATA_DIR`) · `app/config.py`(`DATABASE_URL` · `DCB_DATABASE_URL` 이 덮어씀) · `scripts/check_dev_db.py`(`main` 이 같은 규칙으로 찾음) |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 문서만 |
| 그 값을 세어 둔 문서 | 인계 재정 절 남은 것 표의 한 줄(계좌 게이트 판부터) |

# 1. 한 것

- **1a 경로** — `devserve.bat` 이 `DCB_DATA_DIR=%TEMP%\dcb-dev` 를 걸고, `app/paths.py` 가 그것을 `DATA_DIR` 로, `app/config.py` 가 `DATA_DIR/app.db` 를 `DATABASE_URL` 기본으로 쓴다 → `C:\Users\user\AppData\Local\Temp\dcb-dev\app.db`
- **1b 안전** — 운영은 `C:\Users\user\Desktop\대청부-수련회-시스템\data\app.db` 로 폴더부터 다르다. 그 경로를 덮어쓸 수 있는 `DCB_DATABASE_URL`(과 다른 `DCB_*`)이 프로세스 · 사용자 · 시스템 환경변수에 **없다**. 개발 서버(포트 8001)도 안 떠 있어 파일이 잠기지 않았다
- **막힌 까닭 확인** — 지금 개발 DB 에 `check_dev_db.py` 를 돌리니 exit 1 · `expense_entries.payer_account: 5칸`(값은 안 찍힘)
- **3a seed 계좌** — 기본 `seed.py`(devserve 가 인자 없이 부름)는 회차 · 부서 · 업무 라이브러리만 만들고 **계정도 지출도 안 만든다**(`seed.py` 156~160행). `--demo` 갈래에서도 `seed_data.py` 식대 계좌 다섯은 999 머리이고 `seed.py` 가 `_account` 로 받아 **안 쓴다**. 네 seed 파일에서 `app/account_shape.py` 계좌 꼴에 걸리는 글 0
- **3b 게이트** — 코드로 볼 뿐 아니라 **이 창 임시 폴더**에 `DCB_DATA_DIR` 를 걸고 `DATABASE_URL` 이 그 폴더를 가리키는지 단언한 뒤 `seed.py` 를 돌리고 `check_dev_db.py` 를 돌리니 exit 0. 그 폴더는 지웠고 운영 `app.db` 수정 시각은 앞 판 짝잇기 실행 때(14:49) 그대로
- **2 · 4a 안내** — 인계 재정 절에 「개발 DB 다시 만들기 — 안내」(막힌 까닭 · 지울 파일 · 운영과 떨어진 근거 · seed 가 안전한 근거 · 명령 다섯 단계) · 남은 것 표 줄을 「안내 준비됨 · 실행하고 알려 주시면 닫힌다」 · 맨 위 갱신 · 앞 판 보고를 `docs/review/2026-09-15-재정마감.md` 로 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 정확한 파일 경로와 명령이 사람칸에 | O | 사람칸 「노트북에서 직접」 · 인계 안내 절 |
| 나) seed 계좌가 안전한 근거 | O | 1장 3a · 3b(임시 폴더 seed → 게이트 exit 0) |
| 다) pytest 옵션 없이 · 글검사 7/7 | O | 6장 |
| 라) 검토자 원문 | O | 7장 |

# 3. 운영 자료

**안 건드렸다** — 운영 DB 와 백업 폴더를 열지 않았다. 개발 DB 는 `check_dev_db.py` 로 **읽기만** 했다(값 안 찍음 · 지우지 않음). 삭제는 총무님이 한다.

# 4. 재시작

**건너뛰었다** — 커밋에 `app/` · `templates/` · `static/` 이 없다(문서만). 운영 서버 PID 18592 그대로.

# 5. 판단이 들어간 자리

- **「코드로만」 인데 임시 폴더에서 실제로 돌림** — 지시문 3b 가 「못 돌려도 됨」 이라 막지 않았고, 대상이 개발 DB · 운영 DB 가 아닌 이 창의 임시 폴더다. 돌리기 전에 환경변수가 없는 것을 보고 `DATABASE_URL` 을 단언했고 뒤에 운영 파일 시각을 봤다(검토 7-1 동의)
- **`seed.py --reset` 이 아니라 파일을 지우는 명령** — `--reset` 은 `DCB_DATA_DIR` 를 빠뜨리면 운영 표를 비우는 명령이라, 경로를 박은 `Remove-Item` 을 골랐다(검토 7-1 동의)
- **첫 관리자 단계를 넣음** — 새 seed 는 계정을 안 만들어 로그인하려면 필요하다. 비밀번호가 찍혀 사람 몫(11-2)이고, 환경변수를 빠뜨리면 운영 폴더를 보므로 첫 줄과 되돌리는 줄을 함께 적었다. 「운영에 관리자가 있으면 멈춘다」 는 조건형으로 — 운영 계정이 빈 날에는 그 막이 없다
- **안 고친 것** — `check_dev_db.py` 가 DB 파일이 없으면 0 을 내는 모양(검토 7-2 · 안내는 「서버가 뜬 뒤」 로 순서를 둬 안 밟힘 · 스크립트 몫이라 범위 밖)

# 6. 검사와 시험

- 글검사(커밋마다): 7/7 · `tests/test_docs_shell.py` 11 passed(안내의 PowerShell 블록)
- 임시 폴더 리허설: seed → 「만들었습니다 (실제 이력만)」 → `check_dev_db.py` exit 0
- **두 번째 검토 뒤 고침**(42569aa · 11-3 4단계대로 다시 보이지 않음): 인계 안내의 한 문장 — 옛 개발 DB 에 demo 자료가 있었다고 단정하던 것을 「`--demo` 로 만들었다면」 조건형으로(검토 7-2 [L])
- 보고를 다 채운 뒤 돌린 것: 아래 줄

- 글검사 7/7 통과(검토자 원문 한 낱말을 실명 검사가 짚어 바꾼 뒤) · pytest(옵션 없이) `1596 passed, 3 warnings in 1533.86s (0:25:33)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토 (44da387..5b3daa3)

```
━━━ 검토자 여기부터 ━━━ 범위 44da387..5b3daa3 · 커밋 1개 · 파일 2개

본 것
  git diff 44da387..HEAD (docs/인계.md · docs/review/2026-09-15-재정마감.md)
  scripts/devserve.bat · app/paths.py · app/config.py · seed.py · seed_data.py
  scripts/계정문열기.py · scripts/check_dev_db.py · app/routers/login.py
  돌린 것 (둘 다 옵션 없음)
    pytest tests/test_docs_shell.py   11 passed
    scripts/글검사.py                  검사 일곱 개 전부 exit 0
  data/ 와 개발 DB 는 열지 않았다.

얼린 판
  docs/review/2026-09-15-재정마감.md 는 44da387 의 최근.md 와 바이트까지 같다 (cmp). 문제 없음.

[M] 고쳐야 함 — 4단계 확인이 새로 만든 DB 에서는 아무것도 증명하지 못한다
  docs/인계.md 265행: 「그 창에 !! 개발 DB 에 실명·연락처·계좌 꼴이 있습니다 가 안 나오고」
  scripts/devserve.bat 는 app.db 가 있을 때만 check_dev_db 를 부른다 (if exist ... 줄).
  이 안내는 2단계에서 app.db 를 지우고 3단계에서 켜므로 처음 켤 때는 [이 창: 실명 검사가 낱말 안의 이름 모양을 짚어 한 낱말만 바꿈]
  seed.py 에서 곧바로 uvicorn 으로 넘어간다. 검사가 돌지 않으니 그 문구는
  반드시 안 나온다. 검사를 거쳤다는 뜻으로 읽히지만 거치지 않은 채로 초록이다
  (11-3 「아무것도 안 보는 검사는 통과가 아니다」).
  기동 순서 자체는 문제가 아니다. seed 는 저장소의 가명 자료이고 다음 기동부터 검사가 돈다.
  고칠 곳은 확인 방법이다. 예시:
      .venv\Scripts\python.exe scripts\check_dev_db.py
      $LASTEXITCODE          # 0 이면 통과
  check_dev_db.main() 은 DCB_DATA_DIR 가 없으면 TEMP\dcb-dev 로 떨어지므로
  환경변수를 따로 걸지 않아도 운영 쪽을 안 본다. 서버를 한 번 껐다 켜 보는 방법도 된다.

[L] 봐 둘 것 — 「새 seed 가 안전한 근거」 가 맞지만 가장 강한 근거가 빠졌다
  docs/인계.md 248~249행: seed.py 가 계좌를 _account 로 받아 버린다는 설명.
  그 줄(seed.py 302행)은 demo 갈래에만 있다. devserve.bat 는 seed.py 를
  인자 없이 부르고, 이때 seed() 는 seed_library.seed_all 뒤에 return 한다 (seed.py 150~159행).
  그러니 사용자·예산·지출 행이 아예 생기지 않는다.
  「기본 seed 는 지출도 계정도 만들지 않는다」 가 먼저 오면 _account 설명은 곁가지가 된다.
  같은 까닭으로 사람이 알아야 할 것도 있다. 옛 개발 DB 가 --demo 로 만든 것이었다면
  할 일·회의록·지출 demo 화면은 다시 만든 뒤 비어 있다.
  안내는 「계정을 안 만든다」 만 적었다.

[L] 봐 둘 것 — 5단계를 어느 창에서 치는지 안 적혔다
  docs/인계.md 5단계 블록: .venv\Scripts\python.exe 는 상대 경로라 저장소 폴더에서만 돈다.
  4단계가 「다른 PowerShell 창」 을 열게 하므로 그 창이 저장소 밖일 수 있다.
  그 경우 첫 줄의 환경변수만 걸리고 둘째 줄은 「인식되지 않음」 으로 끝난다. 해는 없지만 헷갈린다.
  「저장소 폴더에서」 를 2단계처럼 한 번 더 적으면 된다.
  또 seed.py 가 끝나며 찍는 안내(157행)는 DCB_DATA_DIR 없이 계정문열기.py 를 치라고 한다.
  devserve 창에 뜬 그 줄을 그대로 긁어 쓰면 운영 폴더를 본다.
  안내의 「첫 줄부터 친다」 가 막고 있으니 알아만 둔다.

물은 것에 대한 답

  PowerShell 로 맞나 — 맞다
    .\scripts\devserve.bat 는 cmd 가 읽고, bat 안의 cd /d "%~dp0.." 가 폴더를 잡는다.
    Remove-Item · Invoke-WebRequest -UseBasicParsing · (...).StatusCode · Remove-Item Env:...
    모두 PowerShell 문법이고 가려지는 별칭이 없다.
    /login 은 로그인하지 않은 요청에 login.html 을 그리므로 200 이 기대값으로 맞다.
    test_docs_shell 도 통과했다.
    -wal/-shm: app/ 어디에도 journal_mode 설정이 없어 SQLite 기본(롤백 저널)이다.
    지금은 app.db 하나만 지우면 된다.
    WAL 을 켜는 날에는 이 안내가 낡는다. seed.py --reset 은 넷을 다 지우지만,
    DCB_DATA_DIR 가 빠지면 운영 app.db 를 지우는 명령이다.
    그러니 경로를 박은 Remove-Item 을 고른 판단이 더 안전하다.

  첫 관리자 단계에서 환경변수를 빠뜨리면 — 완화 문장은 맞다
    환경변수가 없으면 DATA_DIR = 저장소\data 가 된다.
    config 를 읽을 때 폴더를 만들고 서명키가 없으면 쓰지만, 운영에는 둘 다 있어 바뀌는 것이 없다.
    첫관리자() 는 perm.admins(db, active_only=False) 가 비어 있지 않으면
    쓰기 전에 return 1 로 끝난다 (계정문열기.py 123~133행). init_db 도 안 부른다.
    남는 것 둘:
      (1) 멈추면서 운영 관리자 목록(이름·id)을 그 창에 찍는다. 값이 오가지는 않고 사람 창이라 규칙 안이다.
      (2) 「운영에 관리자가 있다」 는 전제가 있어야 참이다. 09-13 처럼 운영 계정이 비었을 때는
          운영에 관리자를 만든다. 괄호 문장이 「그래도 첫 줄부터」 로 닫고 있어 충분하다.
    환경변수를 건 채로는 dcb-dev 에 secret_key.txt 가 없으면 새로 쓴다.
    devserve 는 DCB_SECRET_KEY 를 쓰므로 해가 없다.

  devserve 가 새 DB 에서 check_dev_db 를 안 거치는 순서 — 순서는 문제 없음. 확인 방법만 위 [M]

  경로를 사용자 폴더 전체로 적은 것 — 걸리지 않는다
    11-2 「바깥 주소」 는 터널 주소처럼 밖에서 여는 주소를 보고·덩어리에 적지 말라는 규칙이다.
    로컬 파일 경로는 대상이 아니고, 인계.md 는 보고나 덩어리도 아니다.
    경로의 사용자 폴더 이름은 사람 이름이 아니다. check_names 도 exit 0 이었다.

  check_dev_db 가 새 seed 로 통과할 근거 — 충분하다
    기본 seed 에는 users · expense_entries 행이 없다 (위 [L]).
    이름이 들 수 있는 칸은 라이브러리·부서뿐이고 그 원본은 저장소 파일이다.
    그 파일은 커밋마다 check_names 를 지난다.
    구현자가 대응표가 있는 이 노트북에서 실제로 seed 를 만들어 exit 0 을 봤다.
    MAP_PATH 가 저장소 기준이라 그 실행은 실명·연락처·장소·계좌 꼴을 모두 봤다.
    다만 check_names 는 docs/ 를 보고 개발 DB 게이트는 DB 를 본다.
    seed_*.py 자체를 실명 대조하는 자리가 있는지는 이 판 범위 밖이라 확인하지 않았다.

  「코드로만」 인데 실제로 돌린 판단 — [L] 봐 둘 것 · 괜찮다고 본다
    지시문 3b 는 「못 돌려도 됨」 이라 돌리는 것을 막지 않았다.
    대상도 운영·개발 DB 가 아닌 이 창 임시 폴더다.
    DCB_DATABASE_URL 이 없는 것과 DATABASE_URL 을 단언하고 돌렸다.
    뒤에 운영 app.db 수정 시각까지 봤으니 11-2 가 요구하는 「무엇이 달라질지 알고 돌린다」 를 채운다.
    지시문 대조 표에는 3b 를 「다르게 했다」 로 까닭과 함께 적어야 한다.
    보고(최근.md)가 아직 앞 판이라 지금은 볼 수 없다.

막아야 함: 없음

사람에게 묻는 것
  1. 옛 개발 DB 를 --demo 로 만들어 두고 쓰셨나.
     그렇다면 devserve 가 새로 만드는 DB 에는 demo 화면 자료가 없다.
     그대로 둘지, 안내에 --demo 갈래를 더할지 정해 주세요.
     (--demo 로 가면 지출 계좌 칸이 다시 들어온다.
      seed.py 는 그 칸을 버리지만, 게이트를 지나는지는 다시 재야 한다)
  2. 5단계 괄호 문장처럼 「운영에 관리자가 있다」 는 전제에 기대는 안전 문장을 문서에 남겨 둘지.
     운영 계정이 빈 날에는 거짓이 된다.

━━━ 검토자 여기까지 ━━━
```

이 창의 처리: [M] 과 [L] 둘은 258c67b · 물음 1 은 사람칸 「정할 것」 · 물음 2 는 조건형으로 고쳐 닫았다.

## 7-2. 다시 보기 (5b3daa3..258c67b)

```
━━━ 검토자 여기부터 ━━━ 범위 5b3daa3..258c67b · 커밋 1개 · 파일 1개 (docs/인계.md) · 검토 뒤 고침 다시 보기

본 것
  git diff 5b3daa3..258c67b 전체
  scripts/check_dev_db.py main() · 실명이있나()
  pytest tests/test_docs_shell.py (옵션 없음) 11 passed · git status 깨끗
  데이터는 열지 않았다.

지난 검토에서 짚은 것이 고쳐졌나
  [M] 4단계 확인이 빈 초록이던 것 — 고쳐짐
    인계.md 268~274행
    새 DB 에서는 devserve 가 검사를 건너뛴다는 사실이 적혔다.
    check_dev_db.py; $LASTEXITCODE → 0 과 /login → 200 을 따로 보게 했다.
    창 위치도 「저장소 폴더에서」 로 적혔다.
    PowerShell 문법(; 로 잇기 · $LASTEXITCODE)이 맞다.
    환경변수가 없어도 TEMP\dcb-dev 로 떨어져 운영을 보지 않는다.
  [L] 근거의 순서 — 고쳐짐 (250~252행)
  [L] 5단계 창 위치 · seed 가 찍는 명령을 긁어 쓰지 않기 · 조건형 괄호 — 고쳐짐 (277~286행)
    괄호가 이제 「운영에 관리자가 있는 동안에는」 이다.
    계정문열기.py 123~133행의 거절 조건과 맞다.
  맨 위 갱신 줄 — 새 줄을 넣고 앞 줄은 「그 전 갱신」 으로 내렸다.
    얼린 판 이름(2026-09-15-재정마감.md)이 실제 파일과 같다.

막아야 함: 없음
고쳐야 함: 없음

[L] 봐 둘 것 — 4단계 검사도 DB 파일이 없으면 0 이 나온다
  check_dev_db.실명이있나() 에 「if not db_path.exists(): return {}」 가 있다.
  그래서 파일이 없어도 exit 0 이다.
  안내 순서대로 「Uvicorn running」 을 본 뒤라면 seed 가 이미 파일을 만들었으니 지금 안내로는 안 밟힌다.
  서버가 뜨기 전에 먼저 치면 0 이 뜨고, 지난 판과 같은 빈 초록이 된다.
  문장에 「Uvicorn running 을 본 뒤에」 가 이미 있으니 문서는 두어도 된다.
  「DB 가 없으면 0」 이라는 모양은 스크립트 쪽 몫이다. 이 판 범위 밖이다.

[L] 봐 둘 것 — 「그래서 옛 개발 DB 에 있던 demo 화면 자료」 는 아직 모르는 사실을 앞에 둔다
  인계.md 252행
  옛 개발 DB 를 --demo 로 만들었는지는 아직 사람에게 묻는 중이다 (물음 1 · 보고 「정할 것」).
  문장은 demo 자료가 있었다고 단정한다.
  또 「그래서」 가 바로 앞 문장(demo 갈래의 계좌)에 붙어 까닭이 한 문장 건너뛴다.
  「옛 개발 DB 를 --demo 로 만들었다면 그 자료는 다시 만든 뒤 비어 있다」 꼴이면 물음과 어긋나지 않는다.

사람에게 묻는 것
  1. (지난 검토에서 넘어온 것 · 보고 사람칸 「정할 것」 으로 간다고 함)
     옛 개발 DB 를 --demo 로 쓰셨나. 안내에 --demo 갈래를 더할까.

━━━ 검토자 여기까지 ━━━
```

이 창의 처리: 둘째 [L] 은 42569aa(두 번째 검토 뒤 고침) · 첫째 [L] 은 순서로 안 밟혀 두었다(5장).

# 8. 바뀐 파일 (44da387..이 보고)

docs/review/2026-09-15-재정마감.md · docs/review/최근.md · docs/인계.md

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1a | 개발 DB 경로를 코드에서 찾음 | **했다** |
| 1b | 지워도 안전한지 · 운영과 분리 · 잘못 지정 위험 | **했다** — 환경변수까지 확인 |
| 2a | 사람칸에 정확한 경로와 명령 | **했다** |
| 2b | seed 다시 만드는 명령(기존 스크립트) | **했다** — devserve 가 기존 seed.py 를 부른다 |
| 2c | 뜨는지 확인 방법 | **했다** — 검사 0 과 /login 200(검토로 고침) |
| 3a | seed_data 계좌가 999 머리인지 코드로 | **했다** — 기본 seed 는 지출을 아예 안 만든다까지 |
| 3b | check_dev_db 통과 예상을 코드로 | **다르게 했다** — 코드로 본 뒤 이 창 임시 폴더에서 실제로 seed 를 만들어 게이트를 돌렸다(exit 0 · 개발·운영 DB 는 안 건드림) |
| 4a | 인계 항목을 「안내 준비됨 · 실행 뒤 닫힘」 | **했다** |
| 가~라 | 수용 기준 | **했다** — 2장 |
| 끝줄 | 운영 안 건드림 · 삭제는 총무님 | **했다** |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
