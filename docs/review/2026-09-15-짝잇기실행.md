사람이 할 것
노트북에서 직접: 하나 — 개발 DB 를 지우고 seed 를 다시 만드는 일이 앞 판들에서 넘어와 아직 남았다(얼린 판 `docs/review/2026-09-14-계좌게이트.md` 5장). 이 판이 새로 넘기는 것은 없다
관리자 권한: 없음 — app/ · templates/ · static/ 을 안 건드려 재시작하지 않았다(4장)
정할 것: 하나 — 「문서가 되돌리는 자리로 가리키는 사본은 `지키는판` 에 넣는다」 를 CLAUDE.md 11-2 백업 절에 규칙으로 적을지, 지금처럼 판마다 까닭 문자열로만 둘지(다시 보기 검토 L1 · 7-2). 봐 둘 것 둘(파서가 열을 안 봄 · 「글자와 식」 이 글자로 하나인 줄만 셈)은 봐둘것 BB-i 머리에 적었고 다음 시트를 들이기 전에 정한다
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 이 판은 화면 코드를 안 바꿨다(4장)

작성 시각: 2026-09-15 15:15 KST
커밋: 24f7d30 (스크립트 · 시험) · 01fd8eb (문서) · f98c600 (검토 고침) · 이 보고는 그다음 커밋
작업: 짝잇기 운영 실행 · 사람이 정한 둘을 적기 · 들여오기를 결산 식 기본으로

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `결산식으로` 는 `scripts/재정짝잇기.py` 가 from-import 하고 `tests/test_stage51.py` c05 가 monkeypatch 한다(이름 유지 · 안쪽을 `_결산식(wf)` 로 가름). `_식의줄들` 은 `_결산식` 과 stage51 c01. `고른다` 의 수 이름(「글자로 이음」 · 「같은 글자의 예산 줄」)을 읽는 데는 stage48 뿐 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 스크립트만 |
| 그 값을 세어 둔 문서 | CLAUDE.md 7-5 · 14장 · 봐둘것 BB-h · BB-i · 인계 머리와 순서표 6 · 두 스크립트 머리 · 옛말 목록 |

# 1. 한 것

- **① 운영 실행** — 운영 미리보기를 다시 재어 앞 판 수와 같은 것을 보고(3-1) `scripts/재정짝잇기.py --회차 1 --실행` → 직전 사본 `data/backups/app-20260915-144929.db` · 「채웠습니다. 예산 항목 미지정: 30 → 0」 → 두 번째 「채울 것이 없습니다」 → `mode=ro` 로 다시 셈(3-2)
- **② 옛 글자를 활동 기록에 안 남김** — 코드는 그 모양이라 안 고침. CLAUDE.md 7-5 · 봐둘것 BB-h 에 「사람이 정함 · 2026-09-15」. 되돌리는 자리인 채우기 직전 사본을 `scripts/backup.py` 의 `지키는판` 에 넣음(첫 검토 M2)
- **③ 들여오기가 결산 식 기본** — `scripts/재정들여오기.py` 의 `고른다` 가 짝 표 json → 결산 식(가리키는 예산 줄이 하나) → 못 이음(짝 표) 순. 글자 비교는 확인용: 글자로 하나인데 식과 다르면 식을 따르고 「글자와 식」 수와 짝 표에. `읽는다` 가 연 통합문서로 `_결산식(wf)` 를 구해 `시트.결산식` 에 둠. `_식의줄들` 이 탭 이름이 범위 앞에 한 번 붙은 꼴을 받음(뒤 끝에만 붙은 꼴은 모름)
- **시험** — stage48 합성 시트에 합계금액 식(H2) · e01(모르는 꼴 → 0개) · e03(글자와 식이 어긋나면 식 · 짝 표) · e04(식이 둘 → 못 이음). stage51 `_옛들여오기`(들인 뒤 두 줄의 항목과 글자를 2026 모양으로 되돌림) · a01/a02 뒤집음 · c01 에 새 꼴
- **문서** — CLAUDE.md 7-5 두 불릿 새로 · 14장 한 줄 · 봐둘것 BB-h 처리함 · BB-i 사람이 정함(원문 유지 · 봐 둘 것 둘) · 인계 머리와 순서표 6 · 옛말 새 항목 · 앞 판 보고를 `docs/review/2026-09-15-결산식잇기.md` 로 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 미리보기 수가 앞 판과 같을 때만 실행 | O | 3-1 표 — 다섯 수 전부 같음 |
| 나) 실행 뒤 미지정 0 · 채운 행 글자 = 항목 | O | 3-2 (ro 다시 셈) · 스크립트 안 다시 세기 |
| 다) 두 번째 「채울 것이 없습니다」 | O | 3-1 |
| 라) 들여오기가 결산 식으로 잇고 글자는 확인용 | O | `test48_e01` · `e03` · `e04` · `test51_a01` · `a02` |
| 마) 파서가 Excel 기본 범위 꼴을 받음 · 틀린 꼴은 안 받음 | O | `test51_c01` |
| 바) 짝잇기가 글자를 덮는지 시험이 잰다 | O | `test51_b01` · `b02` — 덮는 줄을 지우면 둘 다 빨개짐(6장) |
| 사) 글검사 · pytest · 검토자 원문 | O | 6장 · 7장 |

# 3. 운영 자료 (값 없이 건수만)

## 3-1. 미리보기 · 실행 · 두 번째

| 무엇 | 앞 판 | 이 판 미리보기 | 실행 | 두 번째 |
|---|---|---|---|---|
| 들여온 뒤에 생긴 미지정 | 0 | 0 | 0 | 0 |
| 예산 항목 미지정 | 30 | 30 | 30 → 0 | — |
| 이미 예산 항목 있음 | 67 | 67 | 67 | 97 |
| 이미 이은 것 중 식과 다른 것 | 0 | 0 | 0 | 0 |
| 채울 것 | 30 | 30 | 30 | 없음 |
| 못 채움(짝 표) | 0 | 0 | 0 | 0 |

사본: `app-20260915-144929.db` (761,856 바이트) — `지키는판` 에 넣었다.

## 3-2. 실행 뒤 다시 셈 (`mode=ro`)

산 지출 97 · 예산 항목 미지정 0 · 글자(구분 · 항목 · 세부항목-1)가 항목과 다른 산 지출 0 · 취소된 항목에 걸린 산 지출 0 · 활동 기록 `재정_짝잇기` 1

# 4. 재시작

**건너뛰었다** — 이 판의 커밋 셋에 `app/` · `templates/` · `static/` 이 없다(스크립트 · 시험 · 문서만). 떠 있는 서버는 PID 18592 그대로.

# 5. 판단이 들어간 자리

- **글자와 식이 어긋나면 식** — 사람이 정한 대로 식이 기본. 어긋난 줄은 막지 않고 세어 짝 표 「글자와 식」 에 남긴다. 글자로 못 잇는 줄은 수만 센다(검토 L2 · 봐둘것 BB-i ②)
- **짝 표 json 이 여전히 이긴다** — 들이기 전에 사람이 적는 자리라 식보다 앞
- **짝잇기 시험의 옛 모양 흉내** — 들여오기가 바뀌어 합성 시트로는 못 이은 줄이 안 생긴다. 들인 뒤 두 줄의 항목을 비우고 **글자도 시트 글자로** 되돌려 2026 모양을 만든다(첫 검토 M1 — 글자를 안 되돌리면 덮기를 아무것도 안 잼)
- **사본을 사람에게 묻지 않고 지킴** — 002352 와 까닭이 글자 그대로 같고, 넣는 쪽은 아무것도 안 지운다. 규칙으로 적을지는 정할 것
- **파서가 열을 안 봄** — 고치지 않고 봐둘것에(2026 시트는 전부 한 칸이라 안 걸림)

# 6. 검사와 시험

- 글검사(커밋 전): 7/7 통과
- 고장 심기: 재정짝잇기 `넣는다` 의 글자 덮는 줄을 지움 → `test51_b01` · `b02` 빨강 → `git checkout` 으로 되돌림 → stage44 · 48 · 51 `56 passed`
- 이 판 시트 값: 합성 시험 값만 새로 들어왔다(이름 · 계좌 · 금액 모두 지어낸 것 · 검토자 확인)
- 보고를 다 채운 뒤 돌린 것: 아래 줄

- 글검사 7/7 통과(보고의 검토자 원문 한 낱말을 실명 검사가 짚어 바꾼 뒤) · pytest(옵션 없이) `1591 passed, 3 warnings in 1493.33s (0:24:53)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토 (27fa555..01fd8eb)

```
━━━ 검토자 여기부터 ━━━ 범위 27fa555..01fd8eb · 커밋 2개 · 파일 9개

돌려 본 것
  pytest tests/test_stage48.py tests/test_stage51.py (옵션 없이) -> 27 passed
  diff 에 app/ · templates/ · static/ 없음 -> 재시작 건너뛰기 맞음
  운영 DB 는 안 열었음

[H] 없음

[M1] 고쳐야 함 · tests/test_stage51.py:52-61 (_옛들여오기) -> :111 (b01)
  b01 의 「채우면 글자가 항목을 따른다」 단언이 이제 아무것도 안 잽니다.
  예전: 들여오기가 글자로 이었으니 지출 7행(5000)은 시트 글자 "없는세부" 로
    남았습니다. 짝잇기가 그 글자를 항목 글자로 덮는지를 b01 이 실제로 쟀습니다.
  지금: 들여오기가 결산 식으로 7행을 예산 6행에 잇고, 그때 이미 level3a 를
    항목 글자로 복사합니다. _옛들여오기 는 budget_category_id 만 비웁니다.
    그래서 짝잇기가 돌기 전에 이미 글자가 항목과 같습니다.
  실패 시나리오: 재정짝잇기.넣는다 에서 이 줄을 지워도
      x.level1, x.level2, x.level3a = c.level1, c.level2, c.level3
    b01 은 초록입니다. 짝잇기.돌린다 안의 다시 세기(재정짝잇기.py:182)도
    같은 까닭으로 통과합니다.
  이번 판에서 사람이 정한 ② 「덮인 옛 시트 글자」 가 가리키는 바로 그 동작이
    시험에서 빠졌습니다(11-3 되짚기 ③ 「볼 것을 보는가」).
  고칠 방향: _옛들여오기 에서 id 만 말고 글자도 시트 글자로 되돌립니다.
    예: 5000 행의 level3a 를 "없는세부" 로.
    stage48 e02 는 들여오기 쪽 시트 글자를 여전히 잽니다. 그쪽은 괜찮습니다.

[M2] 고쳐야 함(문서) / 사람이 정할 것 · 되돌리는 사본 144929
  CLAUDE.md:2603 은 이렇게 적었습니다.
      되돌리는 자리는 시트와 직전 사본입니다
  docs/인계.md:4 와 :232 는 app-20260915-144929.db 를 이름으로 가리킵니다.
  그런데 scripts/backup.py:95 지키는판 에는 002352 만 있습니다. 거기 적힌
    까닭이 「문서가 되돌리는 자리로 가리키는 판이라」 입니다. 144929 도 지금
    문서가 되돌리는 자리로 가리키므로 같은 까닭에 걸리는데 안 지킵니다.
  실패 시나리오: 성한 판 30 칸이 차면 144929 가 지워집니다. 그러면 기준 문서가
    없는 파일을 되돌리는 자리로 가리킵니다.
    ② 에 따라 옛 글자는 활동 기록에도 없습니다. 7-1 은 들인 뒤로 앱이 정본이고
    시트는 바뀔 수 있다고 적었습니다. 그래서 30 줄의 옛 시트 글자가 앱 쪽에서
    조용히 사라집니다.
  이 판이 「정할 것」 으로 넘긴 판단(봐둘것.md:3372)은 받아들일 수 있습니다.
    다만 두 가지가 필요합니다.
    CLAUDE.md:2603 에도 「그 사본은 지키지 않는다(정할 것)」 를 적거나 봐둘것
      BB-h 를 가리켜야 합니다. 안 그러면 기준 문서만 읽는 사람이 영구한
      자리로 믿습니다.
    이 판 보고의 사람칸 「정할 것」 에 실려야 합니다(아래 [M3]).

[M3] 막아야 함(푸시 전 조건부) · docs/review/최근.md
  범위 안의 최근.md 는 아직 앞 판(결산 식 잇기 판 · ef83d57) 보고 그대로입니다.
    사람칸 「정할 것」 도 이미 이 판이 닫은 ①②③ 입니다.
  보고를 아직 안 쓴 커밋 전 검토라면 순서상 맞습니다.
  이대로 푸시하면 이렇게 됩니다.
    사람칸이 닫힌 물음을 열린 것으로 말합니다.
    이 판의 지시문 대조 표(①②③)가 없습니다(검토-원칙 8-1 · 9).
  보고를 쓴 뒤 check_handoff · check_orders · 글검사 · 전체 pytest 를 다시
    돌려야 합니다.
  보고에 실을 수: 운영 미리보기 · 실행 · 다시 세기 수.
    대조 표 ① 「했다」 의 흔적은 그것뿐입니다.

[L1] 봐 둘 것 · scripts/재정들여오기.py:262-273 (_식의줄들) · :313-318 (_결산식)
  정규식 변경 자체는 짚어 주신 꼴에서 안전합니다(따로 재어 봄).
    다른 탭이 뒤 끝에 붙은 꼴 '지출 상세내역'!G2:'예산(실시간)'!G4
      -> fullmatch 실패 -> 빈 것
    뒤 끝에만 탭이 붙은 꼴 -> 빈 것
    $ 섞임은 칸 · _주소 둘 다 \$? 로 받습니다
    '탭'! 뒤의 공백 -> 빈 것(보수 쪽)
    fullmatch 와 finditer 의 두 무늬도 같은 꼴입니다
  넓어진 자리는 따로 있습니다. 파서는 열(column)을 안 봅니다.
    예전에는 탭 이름이 앞에 한 번만 붙은 범위를 모르는 꼴로 버렸습니다.
    지금은 결산 식 =SUM('지출 상세내역'!G2:G9) 처럼 합계금액(H) 열이 아닌
      범위도 받습니다.
    _결산식 은 그 줄 번호마다 H 칸을 읽고, H 가 식이 아니면 그 줄 자신으로
      셉니다.
  실패 시나리오: 결산 식이 G2:G4 만 가리키는데 H2 가 =SUM(G2:G9) 입니다.
    그러면 5~9 행이 이 예산 줄에 잡힙니다.
    그 줄들을 다른 예산 줄도 잡으면 「2개」 로 짝 표에 가서 드러납니다.
    아무도 안 잡으면 조용히 틀린 줄로 이어집니다.
  열 확인(합계금액 열만 받기)을 더할지 봐 두세요. 2026 시트는 전부 한 칸이라
    안 걸립니다.

[L2] 봐 둘 것 · CLAUDE.md:2595 · scripts/재정들여오기.py:443
  문서는 「「글자와 식」 수가 그 자리(줄이 끼어 식이 틀어지는 위험)를 봅니다」
    라고 적었습니다.
  그 수는 글자로 하나가 맞는 줄에서만 셉니다.
  2026 시트에서는 30/97 이 글자로 못 잇는 줄이었습니다. 그 줄들은 식이
    틀어져도 「글자로는 못 잇는 줄」 에 수만 올라가고 짝 표 md 에는 안 남습니다.
    사람이 볼 줄 목록이 없습니다.
  문장이 확인 범위보다 넓게 말합니다. 「글자로 하나일 때만」 을 붙이거나 그
    줄들도 짝 표에 남길지 정하면 됩니다.

[L3] 봐 둘 것 · tests/test_stage48.py e01~e03
  11-3 되짚기의 셋:
    막는 쪽: e01 (SUMIF -> 7행이 0개로 짝 표) · 있음
    통과 쪽: e01 (같은 글자 둘인 6행이 식으로 이어짐 · 「다른 줄」 0) · 있음
    볼 것:   e03 (DB 의 실제 항목 · md 「글자와 식 — 2건」) · 있음
  빠진 한 갈래가 있습니다. 들여오기 고른다 에서 식이 예산 줄 둘을 가리키는
    경우(「2개」 -> 짝 표)는 stage48 에 없습니다.
    짝잇기 쪽은 stage51 c04 가 잽니다.
    식줄 len>1 을 len>=1 로 잘못 바꾸는 회귀는 stage48 이 못 잡습니다.

짚어 달라신 나머지 — 문제 없음
  고른다 의 e["예산줄"] not in 줄로 멈춤이 결산 식에서 오는가:
    안 옵니다. _결산식 의 예산 줄 판정(머리줄 · 지출끝 · 결산이 = 로 시작하고
      ! 포함)이 읽는다 와 같습니다. 식이 주는 줄은 늘 판.예산 안입니다.
    지출끝이 없으면 _결산식 은 max_row 까지 보지만, 읽는다 가 곧바로
      멈춥니다.
    그 멈춤은 사실상 짝 표 json 에서만 옵니다. 문구를 「짝 표나 결산 식」 으로
      넓힌 것은 해가 없습니다.
  읽는다 가 _결산식(wf) 를 부르는 것:
    짝잇기는 from-import 한 이름 결산식으로 를 쓰고(재정짝잇기.py:81) 판.결산식
      은 안 씁니다. c05 의 monkeypatch 는 여전히 뜻대로 걸립니다.
    대가 둘:
      읽는다 가 이제 지출 탭 「합계금액」 머리글을 요구합니다. 없으면 들여오기가
        멈춥니다(드러나는 쪽).
      짝잇기가 식을 두 번 읽습니다(느릴 뿐).
  _옛들여오기 가 짝잇기의 _창 맞추기를 해치는가:
    안 해칩니다. _창 은 예산을 (구분 · 항목 · 세부), 지출을 (금액 · 일자) 로
      맞춥니다. id 를 비우는 것은 그 열쇠에 안 닿습니다.
    금액 필터(10000 · 5000)는 산 지출 중 합성 시트의 6 · 7행에만 맞습니다.
    남는 문제는 [M1] 의 글자뿐입니다.
  문서끼리 같은 말을 하는가:
    같은 말을 합니다 — CLAUDE.md 7-5 두 불릿 · 14장 두 줄 · 봐둘것 BB-h/BB-i
      머리 · 인계 3행 · 232행 · 옛말 새 항목 · 재정들여오기.py 머리 표 · 재정짝잇기.py
      머리.
    낡은 주장 넷(글자 그대로 같고 그런 · 처음부터 결산 식으로 이을지는 · 두
      끝에 모두 탭 · 그대로 둡니다 — 글자로 이은)은 봐둘것 BB-i 본문에만
      남았습니다. 머리가 「아래는 물을 때의 원문」 으로 밝혀 둡니다.
      check_stale 넘김이 필요할지는 글검사로 확인하세요. [이 창: 실명 검사가 낱말 안의 이름 모양을 짚어 한 낱말만 바꿈]
    scripts · tests 안에 옛 수 이름(글자로 이음 · 같은 글자의 예산 줄)은 0 입니다.
  실명 · 계좌 · 금액: diff 에 새로 들어온 것 없음. 합성 시험 값은 지어낸 것.

사람에게 묻는 것
  1. 되돌리는 사본 144929 를 지키는판 에 넣을까요?
     002352 와 까닭이 같습니다: 문서가 되돌리는 자리로 가리킵니다.
     안 넣는다면 CLAUDE.md 7-5 의 「되돌리는 자리는 … 직전 사본」 문장을
       어떻게 적을까요?
  2. 다음 시트의 결산 식 파서가 합계금액 열이 아닌 범위(예: G 열)를 받아도
     될까요? 열을 확인해 모르는 꼴로 보낼까요?
  3. 들여오기에서 결산 식으로 이었지만 글자로는 못 잇는 줄을 수만 셀까요?
     짝 표 md 에도 줄 목록으로 남길까요?

━━━ 검토자 여기까지 ━━━
```

이 창의 처리: M1 · M2 · L2 · L3 고침(f98c600) · M3 은 이 보고 · L1 과 묻는 것 2 · 3 은 봐둘것 BB-i 머리에 「적어만 둔다」 · 묻는 것 1 은 지키는판에 넣음.

## 7-2. 다시 보기 (01fd8eb..f98c600)

```
━━━ 검토자 여기부터 ━━━ 범위 01fd8eb..f98c600 · 커밋 1개 · 파일 5개

돌려 본 것
  pytest tests/test_stage44.py tests/test_stage48.py tests/test_stage51.py (옵션 없이)
    -> 56 passed
  git status 는 깨끗합니다
  data/backups 에 app-20260915-144929.db 가 실제로 있습니다(rows.json 은 없음)
  diff 에 app/ · templates/ · static/ 은 여전히 없습니다

[H] 없음
[M] 없음

앞 검토에서 짚은 것을 하나씩 봤습니다
  M1 · tests/test_stage51.py:52-64
    닫힘입니다.
    5000 행(지출 7행)의 level3a 를 "없는세부" 로 되돌립니다. 그 행에서 시트 글자와
      항목 글자가 갈리는 칸은 세부 하나였습니다. 구분 · 항목은 병합으로 둘이 같습니다.
    그래서 옛 들여오기의 모양이 그대로 돌아옵니다.
    이제 b01:111 의 글자 단언과 짝잇기.돌린다 의 다시 세기(재정짝잇기.py:182)가
      다시 뭔가를 잽니다.
    넣는다 의 줄을 지우면 빨개지는 것을 확인했다는 보고는 저는 다시 심어 보지는
      않았습니다. 다만 코드상 그렇게 됩니다.

  M2 · scripts/backup.py:100-101 · CLAUDE.md 7-5(:2604) · 봐둘것.md:3372
    닫힘입니다. 사람에게 묻지 않고 넣은 판단도 맞다고 봅니다. 까닭은 셋입니다.
      지키는판 에 적힌 002352 의 까닭이 글자 그대로 「문서가 되돌리는 자리로
        가리키는 판」 입니다. 144929 도 이제 CLAUDE.md 가 그렇게 가리킵니다.
        같은 규칙을 같은 모양에 쓴 것이지 새로 정한 것이 아닙니다.
      넣는 쪽은 아무것도 안 지웁니다. 틀렸어도 한 줄을 빼면 되돌아갑니다.
      「정할 것」 으로 두었다면 이미 적은 기준 문서 문장이 30 칸 정리 뒤에
        거짓이 될 수 있었습니다.

  L2 · CLAUDE.md:2595-2596
    닫힘입니다. 문장이 확인 범위와 맞게 좁혀졌습니다.
    봐둘것 BB-i 머리 ② 와 같은 말입니다.

  L3 · tests/test_stage48.py:314-319 (e04)
    닫힘입니다.
    예산 5행 식이 H6+H7 이고 6행이 H7 이므로 지출 7행이 {5,6} 에 잡힙니다.
    단언 셋이 두 쪽을 함께 잽니다.
      막히는 쪽: 7행이 「2개」 로 짝 표에 가고 예산줄이 None
      통과하는 쪽: 6행은 5 로 이어짐
    식줄 len==1 을 len>=1 로 바꾸는 회귀가 이제 잡힙니다.

  L1 · 봐둘것 BB-i 머리
    「적어만 둔다」 로 옮긴 것은 맞습니다. 사람이 정할 판단이고 2026 시트는
      안 걸립니다.

[L1] 봐 둘 것 · scripts/backup.py:95-102
  지키는판 은 사본을 뜨는 판마다 한 줄씩 늘 수 있습니다. 까닭은 「문서가 가리키면
    지킨다」 입니다.
  같은 까닭이 다음 짝잇기 · 들여오기 판에도 걸립니다. 그런데 그 기준이 글로 선
    자리는 지키는판 안의 문자열뿐입니다.
  그러면 다음 판이 문서에 사본 이름을 적고 지키는판 에 안 넣는 일이 또 날 수
    있습니다. 이번 판에서 실제로 났습니다.
  지금 고칠 것은 아닙니다. 사람에게 묻는 것 1 로 남깁니다.

[L2] 봐 둘 것 · CLAUDE.md:2604
  그 한 줄만 괄호가 들어가 다른 줄보다 한참 깁니다.
  뜻에는 문제가 없습니다. 모양만의 문제입니다.

남은 것
  M3(보고)는 이 뒤에 쓴다고 하셨으니 이번 범위 밖입니다.
  쓴 뒤에 check_handoff · check_orders · 글검사 · 전체 pytest 를 다시 돌리세요.
  이번 고침은 검토 뒤 고친 것이라 보고에 이 다시 보기 원문도 실어야 합니다(11-3 4단계).

사람에게 묻는 것
  1. 「문서가 되돌리는 자리로 가리키는 사본은 지키는판 에 넣는다」 를 11-2 백업
     절에 규칙으로 한 줄 적을까요? 아니면 지금처럼 판마다 까닭 문자열로만 둘까요?

━━━ 검토자 여기까지 ━━━
```

이 창의 처리: L1 은 사람칸 「정할 것」 · L2(줄 길이)는 뜻에 문제가 없어 두었다.

# 8. 바뀐 파일 (27fa555..이 보고)

CLAUDE.md · docs/review/2026-09-15-결산식잇기.md · docs/review/최근.md · docs/봐둘것.md · docs/옛말.md · docs/인계.md · scripts/backup.py · scripts/재정들여오기.py · scripts/재정짝잇기.py · tests/test_stage48.py · tests/test_stage51.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 2 | 미리보기 → 수 비교 → --실행 → 다시 세기 → 두 번째 · 사본 이름을 보고에 | **했다** — 3장 |
| 2 | 스크립트만이면 재시작 건너뛰고 적기 | **했다** — 4장 |
| 3-① | 「그대로 해」 — 수가 같으면 --실행 | **했다** |
| 3-② | 옛 글자를 활동 기록에 안 남김 · 코드는 안 고침 · 문서에 사람이 정함 | **했다** — 되돌리는 사본을 지키는판에 넣은 것은 더 했다(검토 M2) |
| 3-③ | 들여오기 결산 식 기본 · 파서에 Excel 기본 범위 꼴 · BB-i 사람이 정함(원문 유지) | **했다** |
| 4 | 개발 DB 다시 만들기를 사람칸에 계속 | **했다** |
| 5 | drawer.js · phone.js | **안 했다** — 로그인 필요(사람칸) |
| 7 | 커밋 전 검토 · 검토 뒤 차이만 한 번 더 · 원문 둘 | **했다** — 7장 |
| 7 | 마무리는 11-3 대로 | **다르게 했다** — 커밋 뒤 푸시 전에 검토를 불렀다(푸시는 이 보고 뒤) · 점검 스크립트는 못 돌림 |
