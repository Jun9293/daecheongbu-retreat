사람이 할 것
노트북에서 직접: 없음 — 이 판은 운영 자료를 안 건드렸다(4장)
관리자 권한: 없음 — 재시작은 일반 창에서 됐다(6장)
정할 것: 둘 — ① 지출을 고칠 때 지원금액은 금액이나 식사 인원이 바뀔 때만 다시 센다(코드가 이 모양 · 검토 G-1 · 5장) ② 계좌를 못 보는 사람(부서 리더)이 계좌 적힌 지출의 지출자만 바꾸면 400 으로 막는다(코드가 이 모양 · 검토 B-1 · 5장) — 둘 다 뒤집을지 사람이 본다
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 사람이 들어간 창을 열어 주면 붙여 돌린다(6장)

작성 시각: 2026-09-14 19:55 KST
커밋: 6721729 (코드) · 8ae25d0 (문서) · 이 보고는 그다음 커밋
작업: 재정 차례 5 — 고치는 길을 새 모양에 맞춘다

---

# 0. 먼저 읽은 것 (지시문 0 · 11-3 0단계)

## 0-1. 만들기 전에 무엇이 되고 안 됐나 (코드로 읽음 · 5f64508)

| 대상 | 등록 | 수정 | 취소 | 계좌 셋 | 영수증 잇기 | 원본 번호 |
|---|---|---|---|---|---|---|
| 지출 | 됨(`/expenses/create`) | **없음** | 됨 · 되살림 | 등록 때만 받음 · 고칠 길 없음 · 목록에 번호 전체 | 번호로 잇기 · 떼기 됨 | 영수증 붙일 때만 받음 · 고칠 길 없음 |
| 예산 항목 | 됨 | 됨(총무팀) — **취소된 항목도 받음**(화면은 단추를 안 그리지만 요청은 들어감) | 됨 · 되살림 | 해당 없음 | 해당 없음 | 해당 없음 |
| 수입 | 됨 | **없음**(재정 보기 판이 적은 그대로) | 됨 · 되살림 | 해당 없음 | 해당 없음 | 해당 없음 |
| 내 정보 계좌 | 셋 받음 | 됨 | — | 셋 | — | — |

활동 기록: 지출 등록·취소 · 예산 항목 생성·수정·취소 · 수입 생성·취소는 남음. 예산 항목 수정은 이름·예산금액 둘만 늘 남김(단가·명수·횟수가 빠짐). 내 정보 저장은 **안 남음**.

## 0-2. 빈 곳의 갈래 — 이 판에 채운 것과 미룬 것

| 갈래 | 이 판 |
|---|---|
| 지출 고치기(일자·금액·예산 항목·세부 항목명·식사 인원·지출자·계좌 셋·비고) | **채움** |
| 영수증 원본 번호 고치기 | **채움** |
| 취소된 예산 항목 고치기 막음 | **채움** |
| 수입 고치기 | **채움** |
| 예산 항목·수입 기록을 바뀐 칸만 | **채움**(검토 G-2 뒤) |
| 계좌 가르기 규칙을 domain 으로 | **채움** |
| 목록 계좌 끝 숫자 넷 + 복사 · 내 정보 계좌 기록(옛 인계 11 의 미룬 둘 · 사람이 「차례 5 에서」 로 정함) | **채움** |
| 지출의 부서·식대 여부 고치기 | 미룸 — 취소하고 새로(7-4 · 봐둘것 BB-g) |
| 지급 여부·지급일 고치기 | 미룸(BB-g) |
| 예산 항목 이름을 고칠 때 지출에 복사된 시트 칸 따라가기 | 미룸 — 차례 6 에서 정본을 정한다(BB-g) |
| 개발 DB 게이트의 계좌 꼴 | 미룸 — 차례 6 전(BB-c) |

## 0-3. 고치기 전 훑기

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `_my_entry`(붙이기·잇기·떼기 → 고치기 둘이 더함) · `calculate_meal_settlement`(등록 한 곳 → `_settle` 로 모아 고치기와 공유) · `income_amount_of`(수입 등록 → 고치기) · `_category_values`(예산 항목 취소 기록 → 수정 기록도) · `can_see_account`(목록·엑셀·폼 기본값 → 고치기 폼·서버) |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 지출 행은 expenses.html, 예산·수입은 budget.html 한 곳씩. `.editform` 은 두 화면이 같이 쓴다(좁은 화면 16px 규칙은 앞 판에서 섰다) |
| 그 값을 세어 둔 문서 | CLAUDE.md 7-3(수정 기록) · 7-4(계좌 가르기 규칙 「차례 6 스크립트에 둔다」 · 목록 계좌 전체) · 14장 표. 인계 순서표 차례 5 · 6 · 현황 표 「수입을 고치는 길이 없다」 「등록한 지출을 고치는 길이 없다」. 봐둘것 BB-c(미룬 둘) |

# 1. 한 것

- **지출 고치기** `POST /expenses/{id}/update` — 행의 「고치기」. 문은 `_my_entry`(이 회차 · 부서 403 · 취소 409). 등록과 **같은 검사·셈**(`_category_or_none` · `_settle` · `_headcount`)을 쓴다. 바뀐 칸만 활동 기록에 전후 값 · 계좌 셋은 「(바뀜)」. 예산 항목을 바꾸면 복사된 시트 칸(구분·항목·세부항목)도 새 항목을 따라간다. 취소된 예산 항목에 이미 걸린 지출은 머물 수 있고 새로 옮겨 붙이는 것만 400
- **지원금액은 금액이나 식사 인원이 바뀔 때만 다시 센다**(검토 G-1 뒤) · 인원이 빈 식대 줄의 금액을 바꾸면 400 · 식사 인원이 숫자 아님·음수면 400(등록도 · 전에는 500)
- **계좌를 볼 수 없는 사람**(부서 리더): 이미 적힌 계좌 칸을 폼에 안 싣고 서버도 그 칸을 안 받는다 · 그 지출의 지출자만 바꾸면 400(검토 B-1 뒤) · 계좌가 빈 지출에는 적을 수 있다
- **원본 번호 고치기** `POST /expenses/{id}/receipts/{rid}/original` — 그 지출에 지금 걸린 영수증만 · 영수증이 다른 부서 지출에도 걸렸으면 그 부서도 본다 · 전후 기록
- **예산 항목 고치기**: 취소된 항목 409 · 기록을 바뀐 칸만(이름·예산금액·단가·명수·횟수) · 안 바뀌면 기록 없음
- **수입 고치기** `POST /budget/incomes/{id}/update` — 예산 항목과 같은 모양(총무팀 · 취소 409 · 금액 식은 `income_amount_of` · 바뀐 칸만 기록). 화면은 수입 행의 「수정」(취소된 수입엔 안 그림)
- **`budget.split_account`** — 한 칸 계좌를 (은행 · 번호 · 예금주)로. 첫 빈칸으로 가르고 앞뒤 빈칸을 떼고 예금주는 지출자 이름. 화면은 셋을 따로 받아 안 부른다 — **차례 6 이 부른다**(docstring · CLAUDE.md 7-4)
- **`budget.account_tail`** · 필터 `account_tail` — 목록 계좌번호는 끝 숫자 넷(「…1111」) + 「복사」 단추(전체 · `expenses.js` · 복사를 못 하면 단추 글자로 말함). 엑셀은 전체
- **내 정보**: 바뀐 칸 이름만 활동 기록(`내정보_수정`) · 안 바뀌면 없음
- 문서: CLAUDE.md 7-3 · 7-4 · 14장 · 인계(머리 · 순서표 5 끝 · 6 다음 · 현황 표) · 봐둘것 BB-c(미룬 둘 닫음) · BB-g(차례 6 으로 넘긴 것). 차례 4 닫기 판 보고를 `docs/review/2026-09-14-차례4닫기.md` 로 얼림. **8장은 안 고쳤다** — 표·칸이 안 바뀌었다

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 · 시험 |
|---|---|---|
| 가) 지출·예산 항목 등록 뒤 고치기 · 계좌 셋 · 원본 번호 각각 | O | 폼으로 등록한 지출을 고치기 엔드포인트로 고친 뒤 DB 칸 값(금액·지원금액·일자·지출자·비고·계좌 셋·예산 항목과 복사 칸) — `test47_a01` · 원본 번호 고치기·비우기·자동 번호 그대로·다른 지출의 영수증 404 — `test47_a02` · 식대 인원 다시 셈 `test47_a03` · 리더 제 부서만(남의 부서 403)·남의 계좌 안 지움 `test47_a04` · 예산 항목 산 것 고치기 `test47_b02` 뒷부분 · 걸린 지출 있는 항목의 예산금액·이름 `test47_c01` |
| 나) 취소된 지출·항목은 못 고침 | O | 취소한 지출에 고치기·원본 번호 409 · 값 그대로 · 화면에 고치기 폼 없음 — `test47_b01` · 취소한 예산 항목 고치기 409 · 값 그대로 — `test47_b02` · 취소한 수입 409 — `test47_b03` |
| 다) 수입 고치기 | O | 이름·단가·명수·금액·비고 저장, 금액은 `income_amount_of` 결과, 전후 기록, 빈 이름 400, 리더 403 — `test47_d01` · 바뀐 칸만·안 바뀌면 기록 없음 `test47_g02` |
| 라) 계좌 가르기 규칙이 budget.py 에 · 두 곳에 안 적은 것을 코드에서 | O | 가르기 결과 넷 `test47_e01` · app·scripts·seed 를 AST 로 훑어 「계좌 칸에 넣는 함수 안에서 가르면서 `split_account` 를 안 부르는 곳 + 이름에 계좌 든 함수의 가르기」 가 `budget.split_account` 하나뿐인지, 정본이 실제로 잡히는지, 심은 두 모양이 잡히는지, `split_account` 를 부르는 모양은 안 잡히는지 — `test47_e02`(검토 G-3 뒤 다시 씀). **한계** — 함수 밖·도우미 함수 모양은 못 잡는다(BB-g) |
| 마) 활동 기록 | O | 지출: 바뀐 칸만·계좌 값이 요약·전후 어디에도 없음·안 바뀌면 줄 없음 `test47_a01` · 원본 번호 전후 `test47_a02` · 지원금액 다시 셈 전후 `test47_g01` · 예산 항목·수입 바뀐 칸만 `test47_g02` · 내 정보 칸 이름만·계좌 값 없음·안 바뀌면 줄 없음 `test47_f01`. 되돌리기: 계좌 옛 값은 기록에 없어 백업으로만(CLAUDE.md 7-4 · BB-g) |
| 바) 375px 새 입력 16px 이상 | O | 3장 — 측정 · 시험 없음 |
| 사) pytest · 글검사 | O | 7장 |
| 아) 검토자 원문 | O | 8장 |

그 밖에 검토가 짚어 더한 시험: 상한이 바뀐 뒤·인원 빈 손 셈 줄에서 비고만 고치면 지원금액 그대로 · 인원 빈 줄 금액 400 · 숫자 아닌 인원 400 `test47_g01` · 리더의 지출자 400·계좌 빈 지출엔 적음 `test47_g03` · 취소된 항목에 머문 지출 고치기·다른 지출을 취소된 항목으로 옮기면 400·선택지 표시 `test47_g04` · 끝 숫자 넷·복사 속성 `test47_a05`.

# 3. 375px 에서 잰 값

앱을 그 자리에서 만들어(지어낸 값) 그린 화면을 375×812 에서, 폼을 열어 계산된 글자 크기를 잼. 임시물은 스크래치에만.

| 자리 | 칸 | 글자 크기 | 문서 가로 |
|---|---|---|---|
| 지출 「고치기」 | 일자 · 금액 · 예산 항목(선택) · 세부 항목명 · 지출자 · 은행 · 계좌번호 · 예금주 · 비고 | 전부 16px | 375 |
| 지출 「고치기」 원본 번호 | original_no | 16px | 375 |
| 예산 화면 수입 「수정」 | 이름 · 단가 · 명수 · 금액 · 비고 | 전부 16px | 375 |
| 목록 계좌 「복사」 단추 | (입력칸 아님) | 13px | 375 |

식사 인원 칸은 식대 줄에만 그려져 합성 자료(식대 아님)에서는 안 쟀다 — 같은 `.editform input` 규칙이다.

# 4. 운영 자료

안 건드렸다. 읽지도 않았다.

# 5. 판단이 들어간 자리

- **지원금액을 금액·인원이 바뀔 때만 다시 센다** — 사람칸 ①. 늘 세면(처음 코드) 회차 상한을 바꾼 뒤 비고만 고친 식대 줄이 새 상한으로 셈해지고, 인원 없이 손으로 적은 지원금액을 들여온 줄은 0 이 된다(7-1 시트 실측: 식대 줄 절반 넘게 식 없이 손으로). 반대로 두면 상한을 바꿨을 때 옛 줄을 새 상한으로 맞추고 싶어도 금액이나 인원을 건드려야 한다
- **리더의 지출자 400** — 사람칸 ②. 비우면 계좌를 잃고 그대로 두면 계좌가 옛 사람 것으로 남아 리더 화면에 안 보인다. 막는 쪽이 잃는 것이 없다
- **부서·식대 여부를 안 고친다** — 부서는 누가 고칠 수 있는지를 정하고, 식대 여부는 지원금액의 뜻을 바꾼다. 취소하고 새로 등록하면 옛 행이 기록으로 남는다(0장)
- **계좌 값을 활동 기록에 안 남긴다** — 활동 기록은 화면에서 누구나 읽는 자리(4-12). 대가는 옛 계좌 값을 되돌리는 길이 백업뿐이라는 것(BB-g)
- **옛 인계 11 의 미룬 둘을 이 판에서 했다** — 지시문에는 없지만 사람이 앞 판에서 「차례 5(고치는 길)에서 한다」 로 정했다

# 6. 재시작

- 멈추기 전 PID 10780 → 다시 켠 뒤 PID 31936 · 「성공 — PID 가 바뀌었습니다」 · 2026-09-14 19:46:13
- 스타일 주소가 `retreat.0399519f.css` 로 바뀌었고 그 파일에 `.acctcopy{` 규칙이 있다
- 로그인 없이 새 길을 부르면 303(로그인으로) · 없는 길은 404 — `POST /budget/incomes/1/update` 303 · `POST /expenses/1/update` 303 · `POST /budget/incomes/1/nothing` 404. 새 코드의 라우트가 서 있다
- **drawer.js · phone.js 는 못 돌렸다** — 로그인이 필요하다(사람칸)

# 7. 검사와 시험

- 글검사(커밋 전): 7/7 통과
- pytest(커밋 전 · 파일 다섯): `84 passed` — test_stage47 · 45 · 46 · web_flows · 23. 두 번째 검토 뒤 고침 뒤 test_stage47 `17 passed`
- 보고를 다 채운 뒤 돌린 것: 글검사 7/7 통과 · pytest(옵션 없이) `1548 passed, 3 warnings in 1528.44s (0:25:28)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 8. 검토자 원문

## 8-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위 5f64508..작업 트리 · 커밋 0개(작업 트리) · 파일 16개

돌려 본 것: pytest tests/test_stage47.py tests/test_stage45.py → 25 passed (옵션 없이)
글검사는 다시 돌리지 않았다. 7/7 은 구현자 말이다.

[막아야 함]

M-1  보고가 아직 앞 판 그대로다
     docs/review/최근.md:1-5 — 사람칸 내용이 차례 4 닫기 판의 것이고, 지금 파일이
     얼린 판 docs/review/2026-09-14-차례4닫기.md 와 같다. 그래서 이 판의 사람칸 ·
     지시문 대조 표(0~6 · 가~아) · 고치기 전 훑기 · 검토자 원문(아)을 볼 수 없다.
     보고를 채운 뒤 커밋해야 한다. 채운 다음 글검사와 pytest 를 다시 돌린다(11-3 4단계).

[고쳐야 함]

G-1  지출 고치기가 어느 칸을 고치든 지원금액을 늘 다시 센다 — (a)
     app/routers/expenses.py 420-423 (headcount · _settle) 과 426-439 (새값에 subsidy · burden)
     식대 줄이면 비고 하나만 고쳐도 지원금액과 개인부담을 지금 회차의 상한으로 다시 셈한다.
     돈이 틀어지는 모양은 둘이다.
       ㄱ. 회차 상한을 설정에서 바꾼 뒤(routers/settings.py:311) 옛 식대 지출의 비고를 고치면
           그 지출의 지원금액이 새 상한으로 바뀐다. 사람은 비고만 고쳤다고 안다.
       ㄴ. 식사 인원이 빈 식대 줄에서 아무 칸이나 고치면 headcount 가 None → 0 이 되어
           지원금액이 0 이 된다(domain/meal.py 의 min(금액, 0×상한)). 7-1 에 따르면 시트의
           식대 줄은 절반 넘게 식 없이 손으로 적은 지원금액이고, 인원은 비고 글에만 있다.
           차례 6 이 그 줄을 인원 없이 들여오면, 들여온 뒤 처음 고치는 순간 지원금액이 0 이 된다.
     활동 기록에 subsidy_amount 전후가 남기는 하지만 화면에는 summary(칸 이름)만 뜬다.
     재현: 식대 지출을 인원 5 · 상한 8000 으로 등록 → 설정에서 상한을 5000 으로 바꿈 →
           note 만 바꿔 /expenses/{id}/update → subsidy 가 40000 에서 25000 으로 바뀐다.
     생각할 수 있는 모양: 금액이나 인원이 바뀔 때만 다시 센다. 어느 쪽으로 할지는
     물음 1 로 남긴다. 시험이 없다(f).

G-2  문서는 「바뀐 칸만」 이라는데 수입 · 예산 항목 코드는 그렇지 않다 — (e)
     CLAUDE.md 7-4 새 줄 「고친 기록 — 지출·원본 번호·수입·예산 항목은 바뀐 칸만 전후 값으로」
     실제 코드:
       app/routers/budget.py update_income — 다섯 칸을 늘 통째로 남기고, 바뀐 것이 없어도 기록을 남긴다
       app/routers/budget.py update_category (앞 판 코드) — name · planned_amount 둘만 늘 남긴다.
         단가 · 명수 · 횟수가 바뀐 것은 전후 값에 없다
     7-3 새 줄 「활동 기록에 전후 값」 과는 맞지만 7-4 줄과는 어긋난다. 문서를 고치거나
     코드를 맞추는 것 중 하나를 해야 한다. test47_d01 은 before["amount"] 만 보므로
     이 어긋남을 못 잡는다.

G-3  「계좌를 가르는 코드는 한 곳」 시험이 실제로는 아무것도 잡지 못한다 — 라) · (f)
     tests/test_stage47.py test47_e02
       - 잡는 조건이 받는 쪽 식에 영문 "account" 가 들어 있어야 한다는 것이다.
         정본인 budget.split_account 도 text.partition(" ") 이라 안 잡힌다.
         그래서 가르는곳 이 비고, all([]) 가 참이라 통과한다. 센 것이 0 인데 초록이다(11-3 ③).
       - 이 저장소는 한글 식별자를 흔히 쓴다(비품들여오기 등). 차례 6 스크립트가
         계좌.partition(" ") 이나 raw.split(" ", 1) 로 다시 적어도 이 시험은 초록이다.
       - 「심은 줄을 잡는다」 도 실제 파일 훑기가 아니라 따로 적은 한 줄에 같은 조건식을
         다시 돌린 것이다. 검사 경로가 실제 트리에서 무언가를 잡는지는 안 잰다.
     생각할 수 있는 모양: 정본 안의 가르는 호출이 1 이상 잡히는지 단언한다. 또는
     scripts/ 에서 payer_account · 계좌 칸에 값을 넣는 곳이 split_account 를 부르는지 본다.

[봐 둘 것]

B-1  리더 지출에서 지출자 이름만 바뀌고 계좌는 옛 사람 것으로 남는다 — (b)
     app/routers/expenses.py 440-444
     계좌를 못 보는 사람은 이미 적힌 계좌 칸을 못 건드린다. 이것은 맞다. 하지만 같은 폼에서
     payer_name 은 바꿀 수 있다. 지출자를 다른 사람으로 고치면 계좌 셋(예금주 포함)은 옛 사람의
     것으로 남는다. 환급 대상 계좌가 사람과 어긋나고, 리더 화면에는 그 사실이 안 보인다.
     계좌가 비어 있던 지출에 리더가 계좌를 넣을 수 있게 둔 판단은 등록 폼과 같은 결이라 문제 없다.

B-2  _my_entry 의 409 문구가 새로 부르는 자리와 안 맞는다
     app/routers/expenses.py _my_entry 끝 줄 「영수증을 잇거나 떼려면 먼저 되살리세요」
     지출 고치기와 원본 번호 고치기도 이 문구를 받는다. 동작은 맞고 말만 어긋난다.

B-3  split_account docstring 의 「지금 부르는 곳은 시험뿐이다」 (app/domain/budget.py 새 함수)
     「…뿐」 꼴이라(검토-원칙 7) 차례 6 이 부르는 순간 거짓이 된다. CLAUDE.md 7-4 쪽은
     「스크립트가 그 함수를 부른다」 로 적어서 괜찮다.

B-4  식사 인원이 음수거나 숫자가 아니면 500 이다
     app/routers/expenses.py 422 int(meal_headcount) → calculate_meal_settlement 가 ValueError
     등록(create_expense)에도 원래 있던 모양이고, 폼에는 min=0 이 있다. 새로 생긴 것이 아니라
     고치는 길이 하나 더 연 것이다.

(c) 취소된 예산 항목에 이미 걸린 지출: 같은 id 면 그대로 두는 갈래는 코드가 맞다
    (expenses.py 417-420 · 템플릿 170 이 선택지로 싣는다). 다른 취소 항목으로 옮기는 것은
    _category_or_none 이 400 으로 막는다. 둘 다 시험이 없다(f).
(d) 전후 값의 dt.date: _바뀐것 이 isoformat 으로 바꾼다. 나머지 칸은 int · str · None 이다.
    걸리는 것 없음.
(g) test45_f01: 25 passed. _settle 과 update_income 에 합이나 사칙 셈이 없다. 걸리는 것 없음.
(e) BB-g 사실 확인
    - 묶음 머리 · 행 제목이 항목의 지금 이름을 따른다: expenses.html 118 · 142 — 맞다
    - 엑셀 지출 상세가 복사된 칸을 싣는다: budget_xlsx.py 91 — 맞다
    - 등록 폼의 「이미 지급 완료」 가 지급일을 비워 둔다: 폼에 paid_date 입력이 없다(99) — 맞다
    BB-c 머리와 인계 순서표(5 끝남 · 6 다음)는 코드와 맞다.

(f) 시험이 안 보는 것 (모아서)
    G-1 의 두 모양 · 수입이나 예산 항목을 안 바꿨을 때의 기록 · 원본 번호에서 영수증이
    다른 부서 지출에도 걸린 경우 리더 403 · 취소된 항목에 걸린 지출의 같은 id 갈래와
    다른 취소 항목으로 옮기는 400 · 계좌가 빈 지출에 리더가 계좌를 넣는 갈래 ·
    375px 16px(수동으로 잰 것이라 시험 밖이다)

[사람에게 묻는 것]

1. 지출을 고칠 때 지원금액을 언제 다시 셀까 (G-1)
   ㄱ. 늘 다시 센다(지금 코드) — 상한이 바뀌었거나 인원이 비었으면 비고만 고쳐도 돈이 바뀐다
   ㄴ. 금액이나 식사 인원이 바뀔 때만 다시 센다 — 들여온 손 셈 줄이 지켜진다
   차례 6 이 식대 줄의 인원 없이 들여오는 한 이것은 차례 6 전에 정해야 한다.
2. 리더가 지출자 이름을 바꿀 때 옛 계좌를 그대로 둘까, 아니면 막거나 계좌를 비울까 (B-1)

━━━ 검토자 여기까지 ━━━
```

**첫 검토에 대해 한 것** — M-1: 이 보고. G-1: ㄴ 으로 코드를 두고 사람칸 ① · 인원 빈 줄 금액 400 · `test47_g01`. G-2: `_바뀐칸만` 으로 예산 항목·수입 기록을 바뀐 칸만 · `test47_g02`. G-3: `test47_e02` 를 다시 씀. B-1: 400 으로 막고 사람칸 ② · `test47_g03`. B-2: 409 문구. B-3: 「…뿐」 문장 걷음. B-4: `_headcount`. (c)(f): `test47_g04`. **남긴 것** — 원본 번호의 「다른 부서에도 걸린 영수증 리더 403」 시험은 안 더했다(판정은 앞 판 `test46_h03` 이 잰 잇기와 같은 반복문).

## 8-2. 다시 보기 (검토 뒤에 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 · 범위 5f64508..작업 트리(검토 뒤 고친 차이) · 코드·시험 파일 셋 + CLAUDE.md

돌려 본 것: pytest tests/test_stage47.py → 17 passed (옵션 없이)

[막아야 함]
없음. M-1(최근.md)은 커밋 직전에 덮는다고 했으므로 이번 다시 보기의 대상이 아니다.

[고쳐야 함]
없음. G-1 · G-2 · G-3 · B-1~B-4 · (c)(f) 는 짚은 대로 고쳐졌고, 막히는 쪽과 통과하는 쪽 시험이 함께 있다.
  - G-1 app/routers/expenses.py update_expense: 400 은 _바뀐것 보다 앞에서 난다. 그래서 막히면 값이 안 바뀐다(g01 이 amount 가 그대로인지 잰다)
  - G-2 app/routers/budget.py _바뀐칸만: g02 가 바뀐 칸 집합과 「안 바뀌면 기록 없음」 을 잰다
  - G-3 test47_e02: 정본이 1 이상 잡히고, 심은 두 모양이 잡히고, split_account 를 부르는 모양은 안 잡힌다. 한 함수로 세 쪽을 다 본다

[봐 둘 것]

B-5  리더의 지출자 비교가 빈칸에 민감하다
     app/routers/expenses.py update_expense: 「새값["payer_name"] != entry.payer_name」
     폼 값은 strip() or None 을 거치지만 저장된 값은 그대로 비교한다. 그래서 저장된 지출자에
     앞뒤 빈칸이 있거나 None 이 아닌 "" 이 들어 있으면, 계좌가 적힌 그 지출은 리더가 무엇을
     고치든(비고만 고쳐도) 400 이 난다. 총무팀이 같은 지출을 고치면 payer_name 이 「바뀐 칸」 으로
     기록에 남는다. 앱의 등록 길은 strip 을 하므로 지금은 생기지 않는다. 차례 6 들여오기가
     지출자 칸을 strip 하지 않으면 생긴다. 차례 6 에서 들여올 때 strip 하는 것을 볼 자리다.

B-6  _가르는곳들 은 함수 안만 본다
     tests/test_stage47.py 280-304
     다음 모양은 초록으로 지나간다.
       - 스크립트 맨 위(함수 밖)에서 가르고 넣는 코드
       - 이름에 계좌가 없는 도우미 함수가 가르기만 하고, 계좌 칸에 넣는 것은 부르는 쪽 함수가 하는 경우
     심은 두 모양이 둘 다 「같은 함수에서 가르고 넣는」 꼴이라 이 틈은 안 재진다.
     휴리스틱의 한계로 적어 두고, 차례 6 스크립트를 볼 때 눈으로도 본다.

B-7  예산 항목 「바뀐 것이 없습니다」 길은 rollback 없이 돌아간다
     app/routers/budget.py update_category
     _apply_fields 가 이미 값을 넣은 뒤라, commit 없이 돌아가도 get_db 가 세션을 닫으면서 버린다.
     동작은 맞다. update_income 은 같은 자리에서 db.rollback() 을 부르므로 두 형제 코드의 모양만 다르다.

[사람에게 묻는 것]
보고 「정할 것」 에 오른다고 한 둘과 같다 — 새로 더할 물음은 없다.
  1. 지원금액은 금액이나 인원이 바뀔 때만 다시 센다(ㄴ) — 코드는 이 모양이다
  2. 리더가 계좌 적힌 지출의 지출자만 바꾸면 400 — 코드는 이 모양이다

━━━ 검토자 여기까지 ━━━
```

## 8-3. 두 번째 검토 뒤 고침 (다시 보이지 않고 적는다 · 11-3 4단계)

| 파일 | 무엇 | 까닭 |
|---|---|---|
| `app/routers/expenses.py` | 리더의 지출자 비교에서 저장값도 앞뒤 빈칸을 떼고 빈 값은 None 으로 견줌 | B-5 |
| `app/routers/budget.py` | 예산 항목 「바뀐 것이 없습니다」 길에도 `db.rollback()` | B-7 — 수입과 모양을 맞춤 |
| `docs/봐둘것.md` BB-g | `test47_e02` 가 함수 안만 본다는 한계 · 들여올 때 지출자 빈칸을 뗀다는 줄 | B-6 · B-5 |

# 9. 바뀐 파일 (`git diff --name-only 5f64508..8ae25d0`)

```
CLAUDE.md
app/domain/budget.py
app/routers/budget.py
app/routers/expenses.py
app/routers/settings.py
app/static/css/retreat.css
app/static/js/expenses.js
app/templates/budget.html
app/templates/expenses.html
app/templating.py
docs/review/2026-09-14-차례4닫기.md
docs/봐둘것.md
docs/세어둠.txt
docs/옛말-넘김.txt
docs/인계.md
tests/test_stage47.py
```

# 10. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 0-a | 지출 등록·수정·취소가 계좌 셋·잇기·원본 번호를 다루는지 표 | **했다** — 0-1 |
| 0-b | 예산 항목도 같은 표 | **했다** — 0-1 |
| 0-c | 수입도 · 고치는 길이 아직 없는지 | **했다** — 0-1 · 없었다 |
| 0-d | 빈 곳 갈래 · 채울 것과 미룰 것 | **했다** — 0-2 |
| 1-a | 계좌 셋·원본 번호를 나중에 고칠 수 있게 | **했다** — 일자·금액 등까지 넓혀 만들었다 |
| 1-b | 고칠 수 있는 사람 = 등록·취소하는 사람 · 가리킴 | **했다** — `_my_entry` 한 문 · 7-4 가 BA-f 바-ㄱ 을 가리킴 |
| 1-c | 취소된 지출 못 고침 | **했다** — 409 |
| 1-d | 활동 기록 | **했다** — 바뀐 칸만 · 계좌는 「(바뀜)」 |
| 2-a | 예산금액·이름을 고칠 수 있는지 | **했다** — 이미 됐고 시험으로 잼 |
| 2-b | 걸린 지출이 있어도 · 이름 바꾸면 지출 쪽 표시 | **했다** — 화면은 따라가고 복사된 시트 칸은 안 따라감을 재고 BB-g 에 적음 |
| 2-c | 취소 규칙 그대로 | **다르게 했다** — 취소 규칙은 그대로이고, 취소된 항목 **고치기**를 409 로 막는 것을 더했다 |
| 3-a | 수입 고치는 길이 없는지 코드로 | **했다** — 없었다 |
| 3-b | 없으면 예산 항목과 같은 모양으로 | **했다** |
| 3-c | 시트-사와 맞물림 | **했다** — 들여온 수입 넷을 이 길로 고친다 |
| 4-a | 화면에 계좌 가르는 입력이 생기면 규칙이 budget.py 에 | **했다** — 화면은 셋을 따로 받아 가르지 않는다 |
| 4-b | 없으면 만들고 차례 6 이 쓸 것을 주석으로 · 지금 바꾸지 않음 | **했다** — `split_account` · docstring |
| 5-a | 새 폼 375px 16px | **했다** — 3장 · 식사 인원 칸은 안 잼 |
| 5-b | 활동 기록·되돌리기 맞물림 | **했다** — 7-4 「고친 기록」 · BB-g |
| 6-a | CLAUDE.md 7-4 · 8장 | **다르게 했다** — 7-3 · 7-4 · 14장을 고쳤고 8장은 표·칸이 안 바뀌어 안 고쳤다 |
| 6-b | 인계 순서표 차례 5 끝 · 6 다음 | **했다** |
| 6-c | 차례 6 으로 미룬 것 갈래 | **했다** — 봐둘것 BB-g |
| 가~아 | 수용 기준 | **했다** — 2장 · 바)는 측정만 |
| 끝줄 | 운영 자료 읽기만 | **했다** — 안 읽었다 |
| 끝줄 | 재시작은 PID 로 | **했다** — 6장 |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
| 곁 | 옛 인계 11 의 미룬 둘(끝 숫자 넷 + 복사 · 내 정보 기록) | **다르게 했다** — 지시문에 없지만 사람이 앞 판에서 「차례 5 에서」 로 정해 이 판에서 했다 |
