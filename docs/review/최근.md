사람이 할 것
노트북에서 직접: 둘 — ① **운영을 쓰시던 브라우저에서 운영 화면이 로그인된 채인지 봐 주세요.** 운영을 다시 켰고(PID 18592 → 34552) 서명키 지문과 쿠키 이름이 그대로라 유지될 것으로 보이나 이 창은 그 세션을 못 봅니다. 풀려 있으면 다시 로그인하시고 알려 주세요(1d) ② (선택) 개발 화면 지출 고치기의 계좌번호 칸에 999 로 시작하는 지어낸 번호를 넣어 저장해 보시면 「계좌번호 칸」 못 함이 닫힙니다(4장)
관리자 권한: 없음 — 운영 재시작은 관리자 창 없이 됐다(5장)
정할 것: 둘 — ① 이번 점검(탭이 가려진 채 · DOM 에서 누름 · 400px 틀)을 11-3 2단계의 점검으로 칠지, 한계가 붙은 참고로만 둘지 ② 부서 리더 개발 계정을 만들어 건너뜀 12 쪽을 잴지(비밀번호는 사람이 넣는다)
막혀서 못 한 것: 개발 화면 계좌번호 칸 입력 — 이 창이 따르는 바깥 작업 규칙이 지어낸 값이라도 계좌번호를 입력 칸에 넣지 않게 한다(CLAUDE.md 규칙은 아니다 · 4장)

작성 시각: 2026-09-15 21:30 KST
커밋: faa5a92 (쿠키 이름) · 2a8dc03 (점검 결과 · 문서 · 얼림) · 0226d0c (검토 고침 · 글검사 빨간 채 들어감) · be503e1 (그 두 말 고침) · 이 보고는 그다음 커밋
작업: 화면 점검 다시 시도 · 개발 쿠키 이름을 운영과 가른다

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `SESSION_COOKIE` 는 `app/config.py` 한 곳에서 정하고 `app/security.py` 의 `set_session` · `clear_session` · `_user_id_from_request` 가 쓴다. `tests/test_stage34.py` 가 그 이름으로 쿠키를 심는다. 다른 쿠키(달력 범위 · 알림 띠)는 이름이 따로다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 |
| 그 값을 세어 둔 문서 | 인계 「화면 점검 시도」 절 · 남은 것 표 · 재정 절 머리 · 맨 위 갱신 |

**순서** — 쿠키 이름을 먼저 바꾸면 개발 서버의 `--reload` 로 총무님의 개발 세션이 끊길 수 있어 **화면 점검을 먼저** 했다.

# 1. 한 것

- **2a · 2b 점검** — 확장이 붙은 Chrome 새 탭에서 개발 서버 로그인 상태 확인(목록 행 96). 점검 스크립트는 이 창 임시 폴더의 임시 파일 서버(끝나고 멈춤)에서 받아 바이트 수를 파일과 견준 뒤 페이지 안에서 돌렸다. 달력은 기본 달 · 범위로는 점이 0 이라 8월 · 범위 전체로 열었다. `phone.js` 는 같은 페이지 안 400px 틀에서
- **2c 재정** — 폼에 값을 넣고 DOM 에서 단추를 눌렀다: 예산 항목 추가 → 지출 A(메모 영수증 · 은행 · 예금주) · 지출 B → B 에서 「번호로 잇기」 → A 고치기(금액 · 비고) → B 취소 → 되살림. 개발 DB 를 읽기 전용으로 세어 저장 확인. 점검 행은 개발 DB 에 남겼다
- **1a~1c 쿠키** — `SESSION_COOKIE = os.environ.get("DCB_SESSION_COOKIE", "dcb_session")` · `scripts/devserve.bat`(cp949 유지)에 `set DCB_SESSION_COOKIE=dcb_dev_session` · 로직은 그대로 · `tests/test_stage53.py` 셋(bat 이 서버 앞에서 다른 이름을 건다 · 환경변수 덮기와 기본값 · 세션 쿠키 자리의 첫 인자가 `SESSION_COOKIE`)
- **1d 운영 재시작** — 5장
- **4 문서** — 인계 「화면 점검」 절(결과 표 · 한계 · 앞선 시도) · 남은 것 표 · 재정 절 머리 · 맨 위 갱신 · CLAUDE.md 11-2 한 문단(확장이 붙은 Chrome · 쿠키 이름 · 같은 호스트 조건 · 다시 켠 뒤부터) · 앞 판 보고를 `docs/review/2026-09-15-화면점검.md` 로 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 개발과 운영의 쿠키 이름이 다르다 | O | `test53_a01` · `a02` · `a03` · 사용자 · 시스템 환경변수에 `DCB_SESSION_COOKIE` 없음(운영은 기본값) |
| 나) 운영 재시작 뒤 로그인 유지 확인 결과 | △ | 지문 같음 · 쿠키 이름 그대로 → **유지 예상** · 총무님 세션은 이 창이 못 봄 → 사람칸 ① |
| 다) drawer.js · phone.js 가 돌았고 재정 넷이 눌리고 저장 | △ | 두 점검 실패 0 · 재정 넷 저장 경로 확인 — 계좌번호 칸 못 함 · 사람 손으로 눌리는지는 못 봄(3장 · 4장) |
| 라) 문제가 있으면 사람칸 · 없으면 「없음」 | O | 깨진 화면 **없음**(실제로 누른 뒤 · 한계 안에서) |
| 마) pytest 옵션 없이 · 글검사 7/7 | O | 6장 |
| 바) 검토자 원문 | O | 7장 |

# 3. 점검 결과 — 확인 8 · 실패 0 · 못 함 1

| 무엇 | 결과 |
|---|---|
| `drawer.js` 목록 | 93항목 통과 · 건너뜀 5 · 못 쟀음 0 · 완주 |
| `drawer.js` 보드 | 80항목 통과 · 건너뜀 3 · 못 쟀음 0 · 완주 |
| `drawer.js` 달력 | 83항목 통과 · 건너뜀 4 · 못 쟀음 0 · 완주 |
| `phone.js`(400px 틀) | 11항목 통과 · 건너뜀 2 · 못 쟀음 0 · 완주 |
| 지출 등록 · 은행 · 예금주 | 저장 · 목록에 보임 |
| 영수증 번호로 잇기 | 두 행에 「지출 2건」 |
| 고치기 | 금액 · 비고 저장 · 은행 · 예금주 그대로 |
| 취소 · 되살림 | 흐리게 남고 합계에서 빠짐(21,000 → 13,000원) · 잇기 단추 사라짐 · 되살리면 복귀 · 콘솔 오류 0 |
| 계좌번호 칸 | **못 함**(4장) |

개발 DB 를 다시 센 수: 산 예산 항목 1 · 지출 2(은행 있음 1 · 예금주 있음 1 · 번호 있음 0) · 영수증 1 · 끊기지 않은 잇기 2 · 고친 금액 1 · 지출 활동 기록 6.

**한계** — 탭이 가려진(hidden) 채였다. 11-3 2단계는 탭이 보여야 한다고 적는다 — 완주하고 못 쟀음 0 이었지만 시간에 기대는 항목이 제대로 재졌는지는 모른다. 폭은 1707px. DOM `click()` 은 제출 경로를 타지만 좌표 판정을 건너뛰어 덮여 있어도 눌린다. 취소 확인창은 그 클릭 동안 자동 수락이었다. 400px 틀은 폭 · 미디어 쿼리만 휴대폰을 따른다. 건너뜀 12 의 까닭은 줄마다 옮기지 않았다(개발 DB 에 부서 리더 계정이 없는 몫으로 보인다).

# 4. 계좌번호 칸을 못 한 까닭

이 창이 따르는 바깥 작업 규칙이 **지어낸 값이라도 계좌번호를 입력 칸에 넣지 않게** 한다. CLAUDE.md 에 있는 규칙이 아니다(11-2 가 막는 것은 사람만 쥘 수 있는 값이다 · 검토 7-1 [M2]). 그래서 형식 검사(400) · 뒷자리만 보이는 표시 · 「복사」 단추는 화면에서 안 불렸다 — 셋 다 pytest 는 잰다. 사람이 개발 화면에 999 로 시작하는 지어낸 번호를 넣으면 닫힌다(사람칸 ②).

# 5. 운영 재시작 (배포-안내 12-1)

- 멈추기 전 PID 18592 → 다시 켠 뒤 PID 34552 · 「성공 — PID 가 바뀌었습니다」 · 관리자 창 없이 됐다
- `data/server.log` 에 새 시작 줄이 생겼고(시작 줄 3개 · 마지막 쓰기 20:43) 그 서명키 지문이 재시작 전 줄과 같다(값은 안 적음)
- 사용자 · 시스템 환경변수에 `DCB_SESSION_COOKIE` 없음 · `serve.bat` 도 안 건다 → 운영 쿠키 이름 `dcb_session` 그대로
- `/login` 200
- **총무님의 운영 세션은 이 창이 못 본다** — 운영을 확장 탭으로 열지 않았다(실명 화면 · 같은 호스트 쿠키). 사람칸 ①
- 개발 서버(PID 36152)는 환경변수 없이 뜬 옛 프로세스라 아직 운영과 같은 이름이다 — **다시 켠 뒤부터** 갈리고 그때 개발에 다시 로그인한다

# 6. 검사와 시험

- `tests/test_stage53.py` 3 passed · stage34 · secret_key 함께 42 passed(한 번 한글 값이 하위 프로세스 출력 인코딩에 걸려 영문 값으로 바꿨다)
- 글검사 — **0226d0c 가 `check_counts` 빨간 채 커밋됐다**(글검사와 커밋을 한 명령에 이어 쳐서 멈추지 않았다). 걸린 두 말(자리를 세는 모양)을 be503e1 이 고쳤다 · 히스토리는 고치지 않고 기록으로 둔다
- **두 번째 검토 뒤 고침** — 없음(be503e1 은 다시 보기 범위 안이었다)
- 보고를 다 채운 뒤 돌린 것: 아래 줄

- 글검사 7/7 통과 · pytest(옵션 없이) `1599 passed, 3 warnings in 1262.05s (0:21:02)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토 (c139284..2a8dc03)

```
━━━ 검토자 여기부터 ━━━ 범위 c139284..2a8dc03 · 커밋 2개 · 파일 6개

부분 시험: tests/test_stage53.py 3 passed (옵션 없이 돌림)
개발 DB 게이트: scripts/check_dev_db.py 를 개발 DB 에 돌려 exit 0 (점검하며 넣은 행이 있어도 devserve 를 다시 켤 때 막히지 않는다)
코드: 쿠키 이름을 set_session · clear_session · _user_id_from_request 셋이 모두 config.SESSION_COOKIE 에서 받는다(app/security.py:35,44,48).
      serve.bat 은 그 환경변수를 안 건다. uvicorn 을 띄우는 bat 은 둘뿐이다(serve · devserve).
      실행 로직은 막을 것이 없다.

[H] 없음

[M] 1. 운영 로그인이 끊기지 않았는지를 사실처럼 적었다 — 수용 기준 1d
    docs/인계.md:3 (갱신 줄) 「운영 로그인은 그대로이고」
    근거는 지문(서명키)과 쿠키 이름이 같다는 것뿐이다. 이 창은 운영 세션을 한 번도 보지 않았다(요청에 스스로 적음).
    이 추론은 맞을 가능성이 높다. 같은 키에 같은 이름이면 서명 검증이 통과하기 때문이다.
    그래도 1d 는 「전후로 로그인이 유지되는지 확인」 이다. 추론을 확인으로 적으면 안 된다.
    인계와 보고에는 「예상 · 사람이 확인하지 않음」 으로 적는다.
    사람칸 「노트북에서 직접」 이나 「정할 것」 에는 「운영을 열어 로그인이 살아 있는지 봐 달라」 를 올리는 편이 1d 의 「끊기면 크게」 와 맞다.

[M] 2. 2c 「계좌 셋 입력」 은 셋 중 둘만 넣었다. 그런데 요약은 「넷 전부 통과」 다
    docs/인계.md:3 「재정 화면 넷 전부 통과」 · 「화면 점검」 표 머리 「못 함 0」
    계좌번호 칸을 비웠다. 그래서 다음 셋이 화면에서 한 번도 안 불렸다.
      - 폼 400(account_problem)
      - 뒤 네 자리만 보이는 표시(account_tail)
      - 「복사」 단추
    계좌 셋 가운데 가장 깨지기 쉬운 자리가 이 번호 칸이다.
    또 「이 창은 계좌번호를 입력 칸에 넣지 않는다」 는 CLAUDE.md 에 없는 규칙이다.
    11-2 가 막는 것은 사람만 쥘 수 있는 값이다. 지어낸 번호는 거기에 들지 않는다.
    게이트도 999 로 시작하는 번호 · 한 숫자 되풀이 · 이어 오름을 빼고 본다(app/account_shape.py:36-48).
    이 창의 판단을 뒤집자는 것은 아니다. 다만 두 가지는 고칠 일이다.
      - 없는 규칙을 규칙처럼 적은 것
      - 둘만 넣고 「못 함 0 · 전부 통과」 로 센 것
    「계좌번호: 못 함(까닭)」 으로 따로 센다.

[M] 3. 「확인 」 뒤에 수가 비었다
    docs/인계.md 「화면 점검」 절 첫 줄 「확인 · 실패 0 · 못 함 0」
    바로 앞 판은 「확인 0 · 실패 0 · 못 함 2」 였다. 이번 판은 확인 칸이 비어 있다.
    check_placeholder 는 이런 꼴을 못 본다.
    같은 절의 phone.js 줄도 「통과」 에 항목 수가 없다.

[M] 4. 탭이 hidden 인 채로 drawer.js 를 돌렸다 — 11-3 2단계에 어긋난다
    11-3 은 「탭이 보이는 상태여야 합니다 — 배경 탭에서는 타이머가 묶여」 라고 적는다.
    완주했고 못 쟀음도 0 이다. 그래도 400ms 창 · 280ms 전환 · rAF 에 기대는 항목이 배경 탭에서 제대로 재졌는지는 알 수 없다.
    인계의 「한계」 문단은 hidden 이었다는 사실만 적는다. 그것이 11-3 의 전제에서 벗어났다는 말은 없다.
    1280px 전제도 같다. 창 크기를 못 바꿨다면 그때 폭이 몇이었는지 기록이 없다(1280 아래였다면 점검이 스스로 멈췄을 테니 넘었다고 볼 수는 있다).

[M] 5. DOM click() 으로 한 것을 「눌리고 저장된다」 로 적어도 되나 — 요청에서 물은 것
    button.click() 은 폼 검증과 제출 경로를 탄다. 그래서 「저장된다」 는 확인이 맞다.
    그러나 click() 은 좌표 판정을 건너뛴다. 단추가 다른 요소에 덮여 있어도, 화면 밖에 있어도 눌린다.
    그래서 2c 의 「눌리는지」 는 재지 못했다.
    confirm 을 true 로 바꿔 둔 것도 같은 결이다. 취소 확인창이 실제로 뜨는지는 안 봤다.
    「저장 경로 확인 · 사람 손으로 눌리는지는 못 봄」 으로 가른다.
    phone.js 를 400px iframe 에서 잰 것은 폭 · 미디어 쿼리 범위에서는 받아들일 만하다. 인계에도 한계를 적었다.
    다만 갱신 줄의 「휴대폰 폭 … 전부 통과」 에는 그 한계가 빠져 있다.

[L] 6. 건너뜀 12 를 전부 부서 리더 몫으로 돌렸다
    docs/인계.md 남은 것 표 「부서 리더 계정(개발 DB 에 없음 · 건너뜀 12)」
    5+3+4=12 로 산수는 맞다. 하지만 스크립트가 낸 건너뜀 까닭이 모두 「관리자라 못 잼」 인지는 보고에 없다.
    데이터가 없어서 건너뛴 것이 섞였을 수 있다. 줄마다 까닭을 옮기거나 「까닭 확인 안 함」 으로 적는다.

[L] 7. test53 의 막는 쪽이 좁다
    a03 은 app/*.py 안에서 "dcb_session" 이라는 글자만 찾는다.
    다음 둘은 초록으로 지나간다.
      - set_cookie("sess", …) 처럼 다른 글자를 적는 것
      - 템플릿이나 JS 에 이름을 적는 것
    본 파일 수가 0 보다 큰지 단언하지 않아 ③(볼 것을 실제로 보는가)도 없다.
    1c(한 곳 정의)를 지키려면 set_cookie / delete_cookie / cookies.get 의 첫 인자가 SESSION_COOKIE 인지 보는 편이 곧다.
    a01 은 정규식이 안 맞으면 빨갛게 닫힌다. uvicorn 앞에 있는지도 보므로 막는 쪽으로 된다. a02 는 좋다.

[L] 8. 지금 떠 있는 개발 서버는 아직 dcb_session 을 쓴다
    환경변수 없이 뜬 프로세스라서 --reload 로 다시 읽어도 이름이 안 바뀐다.
    그래서 「다시 켜면 개발에 다시 로그인해야 한다」 는 안내는 맞다.
    다만 다시 켜기 전까지는 두 서버가 여전히 같은 이름이다.
    인계와 CLAUDE.md 는 「갈랐다」 는 현재형으로만 적는다. 다음 창이 그사이 127.0.0.1:8000 을 같은 브라우저로 열면 옛 충돌이 그대로 난다.
    「개발 서버를 다시 켠 뒤부터」 한마디가 필요하다.

[L] 9. CLAUDE.md 11-2 에 새로 넣은 문단
    「같은 이름이면 … 한쪽에 로그인하는 순간 다른 쪽 세션이 풀린다」 는 둘 다 같은 호스트(127.0.0.1)로 열었을 때만 참이다.
    운영을 터널 주소나 localhost 로 열면 해당 없다. 앞 판 인계에는 이 조건이 있었는데 이번 문장에서 빠졌다.
    「Claude in Chrome 확장이 붙은 Chrome 이어야 한다」 는 4c(한 줄)가 시킨 것 밖이다. 문단도 한 줄이 아니다.
    내용은 맞다. 지시문 대조 표에는 「다르게 했다」 로 적을 자리다.
    백틱 이름(SESSION_COOKIE · DCB_SESSION_COOKIE · dcb_session)은 모두 코드에 있다.

없는 것: 옛말 잔여(쿠키 충돌 가설 문장은 인계에서 「앞선 시도」 로 바뀌었다) · 실명 · 바깥 주소 · 계좌 값 없음.
devserve.bat 은 cp949 를 유지했고, 새 REM 줄은 ASCII 다.

사람에게 묻는 것
- 운영(8000)을 쓰던 브라우저에서 지금도 로그인이 살아 있나. 1d 를 닫으려면 이 확인이 있어야 한다
- 999 로 시작하는 지어낸 계좌번호를 개발 화면 칸에 넣어 계좌 셋 입력을 끝까지 재도 되나. 안 된다면 2c 는 「둘만」 으로 닫는가
- hidden 탭 · DOM click() · iframe 으로 잰 이번 점검을 11-3 2단계의 점검으로 쳐 줄 것인가, 한계가 붙은 참고로만 둘 것인가
- 부서 리더 개발 계정을 만들어 건너뜀 쪽을 잴 것인가(비밀번호는 사람이 넣어야 한다)
━━━ 검토자 여기까지 ━━━
```

이 창의 처리: [M] 다섯과 [L] 6~9 는 0226d0c · be503e1 · 묻는 넷은 사람칸(①② 노트북에서 직접 · ①② 정할 것 · 계좌번호는 막혀서 못 한 것).

## 7-2. 다시 보기 (2a8dc03..be503e1)

```
━━━ 검토자 여기부터 ━━━ 범위 2a8dc03..be503e1 · 커밋 2개 · 파일 3개 (CLAUDE.md · docs/인계.md · tests/test_stage53.py)

직접 돌린 것
  tests/test_stage53.py: 3 passed (옵션 없이)
  scripts/check_counts.py (HEAD): 걸린 자리 없음
  git status: 깨끗함

[H] 없음

[M] 없음. 앞서 짚은 M1~M5 가 모두 글에 반영됐다.
  M1  인계 갱신 줄이 「유지될 것으로 보이나 사람이 아직 확인하지 않았다」 로 바뀌었다.
      사람칸의 확인 부탁은 보고에 들어갈 몫이라 아직 볼 수 없다. 보고를 쓸 때 빠지지 않는지 본다.
  M2  「계좌번호 칸」 이 따로 한 줄 「못 함」 으로 섰다. 까닭이 CLAUDE.md 규칙이 아니라고 밝혔고, 안 불린 셋과 닫는 길까지 적었다.
  M3  「확인 8 · 실패 0 · 못 함 1」 은 표와 셈이 맞는다.
        확인 8 = drawer 세 화면 3 + phone 1 + 재정 넷 4
        drawer 항목 256 = 93 + 80 + 83
        phone 11항목도 들어갔다
  M4  hidden 탭이라 11-3 2단계 전제에서 벗어났다는 것과 폭 1707px 를 적었다.
  M5  「사람 손으로 눌리는지는 못 봤다」 · 확인창은 자동 수락이었다는 것 · 갱신 줄의 「400px 틀 · 한계가 붙은 점검」 이 다 들어갔다.

[L] 1. 글검사가 빨간 채 들어간 커밋이 있다
    0226d0c 가 check_counts 빨간 채로 커밋됐다. 바로 다음 be503e1 이 두 말을 고쳤다.
      「뒤 네 자리」 → 「뒷자리」
      「세 자리」 → 「그 자리들」
    뜻은 그대로다. 까닭(명령을 이어 쳐서 멈추지 않았다)을 커밋 메시지에 밝혀 두었다.
    푸시 전이라 공개 기록에 해는 없다. 그래도 11-3 1단계는 「커밋하기 전에」 글검사다.
    보고에는 「두 번째 검토 뒤 고침」 아래에 이 두 커밋과 까닭을 한 줄 싣는다.
    히스토리를 합칠지는 사람이 정한다(아래 물음).

[L] 2. test53_a03 이 보는 곳이 security.py 한 파일이다
    지금 app/ 안에서 세션 쿠키를 다루는 곳은 security.py 뿐이다. calendar.py · templating.py 의 set_cookie / cookies.get 은 다른 쿠키(SCOPE_COOKIE · OPEN_COOKIE · FLASH_COOKIE)라 무관하다.
    그래서 지금은 새는 곳이 없다.
    다만 다른 파일에서 세션 쿠키를 글자 이름으로 set_cookie 하면 이 시험은 못 본다.
    파일 끝의 "dcb_session" 글자 검사가 기본값 글자만 막아 줄 뿐이다.
    막는 쪽 확인도 가짜 문자열에 정규식을 댄 것이라, 실제 코드에 고장을 심어 본 것은 아니다.
    지금 고칠 일은 아니다.

[L] 3. CLAUDE.md 11-2 문단의 조건
    「같은 호스트(127.0.0.1)로」 · 「터널 주소로 열면 해당 없음」 · 「개발 서버를 다시 켠 뒤부터」 가 들어갔다. 사실과 맞는다.
    localhost 도 127.0.0.1 과 다른 호스트라 해당 없다. 적지 않아도 틀린 말은 아니다.

없는 것
  새로 들어온 실명 · 바깥 주소 · 계좌 값
  옛말 잔여: 인계 「앞선 시도」 가 「지금 떠 있는 개발 서버는 아직 운영과 같은 이름」 으로 현재 상태를 바로 적는다

사람에게 묻는 것
- 푸시 전에 0226d0c 와 be503e1 을 한 커밋으로 합칠 것인가, 빨간 커밋을 기록으로 둘 것인가
- (앞 판에서 넘어온 넷은 보고 사람칸에 들어가는지 본다)
  - 운영 로그인이 살아 있는지 확인
  - 999 번호를 넣어 계좌번호 칸을 닫을지
  - 이번 점검을 11-3 점검으로 칠지
  - 부서 리더 개발 계정을 만들지
━━━ 검토자 여기까지 ━━━
```

이 창의 처리: [L]1 은 6장에 적고 히스토리는 기록으로 둔다(합치지 않음 — 커밋을 고쳐 쓰지 않는다) · [L]2 · 3 은 두었다.

# 8. 바뀐 파일 (c139284..이 보고)

CLAUDE.md · app/config.py · docs/review/2026-09-15-화면점검.md · docs/review/최근.md · docs/인계.md · scripts/devserve.bat · tests/test_stage53.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1a | 쿠키 이름을 환경변수로 · devserve 에 개발용 · 운영 기본값 | **했다** |
| 1b | 로직은 그대로 | **했다** |
| 1c | 로그인 · 로그아웃 · 만료가 같은 이름(한 곳 정의) | **했다** — `test53_a03` |
| 1d | 운영 재시작 · PID · 전후 운영 로그인 유지 확인 | **다르게 했다** — 재시작 · PID 는 했고 로그인 유지는 지문 · 이름으로 **예상**만 했다(총무님 세션은 이 창이 못 봄 · 사람칸 ①) |
| 2a | drawer.js | **했다** — 세 화면 실패 0 · 탭이 가려진 채라 11-3 전제에서 벗어남 |
| 2b | phone.js | **다르게 했다** — 창 크기를 못 줄여 같은 페이지의 400px 틀에서 |
| 2c | 재정 화면 넷이 눌리고 저장되는지 | **다르게 했다** — 저장 경로는 넷 다 확인 · 계좌번호 칸은 못 함 · DOM 클릭이라 손으로 눌리는지는 못 봄 |
| 2d | 못 한 것은 정확한 이유 | **했다** — 3장 · 4장 |
| 3a | 문제는 고치지 말고 사람칸 | **했다** — 깨진 화면 없음 |
| 3b | 없으면 「없음」 | **했다** — 한계 안에서의 없음 |
| 4a | 인계 「화면 점검 시도」 절 갱신 | **했다** — 「화면 점검」 으로 |
| 4b | 재정 절 머리 고침 | **했다** — 확인된 범위와 아직인 것 |
| 4c | CLAUDE.md 에 쿠키 이름 한 줄 | **다르게 했다** — 한 문단(확장이 붙은 Chrome 이어야 한다는 것 · 같은 호스트 조건 · 다시 켠 뒤부터를 함께) |
| 가~바 | 수용 기준 | **다르게 했다** — 나) 다) 는 △(2장) |
| 끝줄 | 운영 코드를 바꾸고 재시작 | **했다** |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — 커밋 하나가 글검사 빨간 채 들어갔다(6장) |
