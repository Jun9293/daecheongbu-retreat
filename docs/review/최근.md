작성 시각: 2026-09-09 22:40 (KST)
커밋: 0331a35 (그 위에 이 줄을 채운 커밋 하나)
작업: 도막 1 — 관리자 복구 · 계좌 문 · 인계 문서 (계정은 세기만)

---

# 0. 먼저 셌습니다 (아무것도 안 고치고)

읽기만 한 셈입니다. 아래 수는 전부 운영 `data/app.db` 에서 이 판이 직접
잰 값입니다.

## 0-(a) 역할별 계정

| 역할 | 계정 | 그중 활성 |
|---|---:|---:|
| `admin` | 4 | 2 |
| `dept_lead` | 11 | 11 |
| `member` | 5 | 5 |
| `viewer` | 1 | 1 |
| **합계** | **21** | **19** |

`admin` 이 넷이고 둘이 비활성입니다 — 4-12 가 「관리자 계정이 넷이 됐고
그중 셋이 같은 이름」 이라고 적어 둔 그 자리입니다. **이 판은 세기만
하고 아무것도 지우지 않았습니다.**

## 0-(b) 링크를 쓴 계정

| 무엇 | 값 |
|---|---:|
| `invite_tokens` 행 | 24 |
| 링크를 **한 번이라도 쓴** 계정 | 3 |
| **지금 실제로 쓸 수 있는** 링크를 가진 계정 | **1** |
| `users.first_seen_at` 이 찍힌 계정 | 1 |

24행의 내막 — 쓴 행 8 · 취소된 행 13 · 미사용이고 미취소인데 기한이 지난
행 2.

> **처음에는 「`used_at` 이 빈 계정 = 5」 를 「살아 있는 링크」 로
> 적었습니다. 틀렸습니다** — 검토자가 잡았습니다. 링크가 살아 있는지를
> 정하는 곳은 `app/domain/auth.py:102` 의 `problem_with` 하나이고,
> 거기서는 **취소·사용·만료 셋**을 봅니다. 발급할 때마다 앞의 링크가
> 취소되므로(`auth.py:70`) **`used_at` 이 빈 채로 죽어 있는 토큰이
> 쌓입니다.**
>
> **수가 아니라 방향이 갈립니다.** 5 로 읽으면 「넷은 링크를 쥐고
> 있으니 기다리면 된다」 가 되는데, 실제로는 그 넷의 링크가 죽어 있어
> **재발급이 필요합니다.** 12장이 「다음에 볼 곳」 으로 적어 둔
> 「링크를 열었는데 안 들어와진 사람이 있는가」 의 입력이 이 수입니다.

**「쓴 계정 3」 과 「들어온 계정 1」 이 다릅니다.** `first_seen_at` 은
바로 앞 판에 생긴 칸이라 그 뒤에 화면을 연 사람만 찍힙니다 — 그전에
링크를 쓴 둘은 안 찍혔습니다. **없는 것이 아니라 그때부터 센 것**이고,
12장이 「링크를 열었는데 안 들어와진 사람이 있는가」 를 볼 때 이 차이를
먼저 알아야 합니다.

## 0-(c) 링크를 만드는 코드 경로

만드는 곳은 **한 곳**입니다 (4-12).

| 자리 | 파일:줄 |
|---|---|
| 발급 | `app/domain/auth.py:64` (`issue`) |
| 주소 만들기 | `app/domain/auth.py:26` (`invite_url`) |
| 해시 | `app/domain/auth.py:60` (`hash_token`) |
| 되찾기 | `app/domain/auth.py:115` (`redeem`) |
| 유효기간 | `app/domain/auth.py:22` (`INVITE_TTL_DAYS = 7`) |

부르는 쪽 — 화면 `app/routers/admin_users.py:206` · `:340` (주소는 `:156` ·
`:354`), 명령창 `scripts/create_admin.py:110` · `:134` · `:159`,
그리고 이번에 더한 `scripts/관리자링크.py:121` (주소 `:141`).
**부르기만 하고 새로 만들지 않습니다** — `test23_h01` 이 그것을 잽니다.

## 0-(d) 세션을 잃은 관리자에게 화면 길이 있는가 — **없습니다**

| 물음 | 답 | 근거 |
|---|---|---|
| 로그인 화면에 스스로 받는 길이 있는가 | 없다 | `app/templates/login.html` — 「총무팀에 다시 요청해주세요」 뿐 |
| 링크를 발급하는 화면에 들어갈 수 있는가 | 없다 | `/admin/users` 는 `require_admin` (`app/security.py:89`) |
| 익명으로 열리는 경로가 있는가 | 없다 | 로그인과 초대 되찾기 (`app/routers/invite.py`) 뿐 |

**마지막 관리자가 기기를 잃으면 아무도 아무것도 못 합니다.** 로그인
화면이 가리키는 「총무팀」 이 그 사람 자신이기 때문입니다. 아래 1번이
그 길을 서버 컴퓨터에서 엽니다.

## 0-(e) `users.id` 를 가리키는 참조 — 지우기 전에 세어 둡니다

`PRAGMA foreign_key_list` 로 전 표를 훑어 **FK 22개**를 찾았고, 값이 든
행이 **2,006개**입니다.

| 표.칸 | `ondelete` | 값 있는 행 |
|---|---|---:|
| `notifications.user_id` | CASCADE | 1,839 |
| `tasks.assignee_id` | SET NULL | 89 |
| `invite_tokens.user_id` | CASCADE | 24 |
| `checklist_items.checked_by_id` | SET NULL | 19 |
| `discussion_entries.author_id` | SET NULL | 12 |
| `meeting_items.assignee_id` | SET NULL | 5 |
| `task_attachments.uploaded_by_id` | SET NULL | 4 |
| `file_versions.uploaded_by_id` | SET NULL | 3 |
| `meetings.created_by_id` | SET NULL | 3 |
| `user_retreat_states.user_id` | CASCADE | 3 |
| `file_assets.created_by_id` | SET NULL | 2 |
| `invite_tokens.created_by_id` | SET NULL | 2 |
| `review_requests.requester_id` | SET NULL | 1 |
| 나머지 아홉 — `draft_submissions.submitted_by_id` · `expense_entries.created_by_id` · `expense_entries.payer_id` · `notification_logs.user_id` · `program_items.done_by_id` · `push_subscriptions.user_id` · `retreat_drafts.created_by_id` · `review_requests.responder_id` · `task_runs.assignee_id` | SET NULL / CASCADE | 각 0 |

**`ondelete` 가 자리마다 다릅니다.** CASCADE 쪽(`notifications` ·
`invite_tokens.user_id` · `notification_logs` · `push_subscriptions` ·
`user_retreat_states`)은 계정과 함께 사라지는 것이 맞고, SET NULL 쪽은
**기록이 남되 「누가 썼는지 모르는 것」 이 됩니다** — 4-12 가 삭제 대신
비활성화를 둔 그 이유입니다.

**그리고 FK 가 아예 없는 칸이 하나 있습니다.**

| 칸 | FK | 값 있는 행 | 지우면 |
|---|---|---:|---|
| `activity_logs.actor_id` | **없음** | 41 | 아무 일도 안 일어나고 **가리킬 곳 없는 값 41개가 남습니다** |

`PRAGMA foreign_key_list` 에 안 나오므로 **위의 22개만 보고 지우면 이
41행은 보이지 않습니다.** 0장에 그 문장을 적었습니다.

## 0-(f) 계좌가 담긴 자리 — 다시 확인

| 칸 | 값이 든 행 |
|---|---:|
| `expense_entries.payer_account` | 5 |
| `users.bank_account` | 0 |

값 자체는 이 보고에 적지 않습니다. 도막 0 에서 계좌 숫자를 이 파일에
적었다가 걷어낸 자리입니다 — **지어냈다고 믿은 자릿수가 실제 값과
맞아떨어졌으므로**, 세는 것과 옮겨 적는 것을 가릅니다.

---

# 1. `scripts/관리자링크.py` — 관리자가 다시 들어갈 길

**만드는 코드를 새로 쓰지 않았습니다.** `invites.issue` ·
`invites.invite_url` 둘만 부르고, `token_urlsafe` · `hash_token` ·
`InviteToken(` 은 이 파일에 없습니다 (`test23_h01` 이 낱말로 잽니다).

## 서버에서 세 번 돌린 결과

값(이름 · 연락처 · 주소 · 토큰)은 옮겨 적지 않고 **모양만** 적습니다.

| 부른 방법 | exit | 나온 줄 | 무엇이 나왔나 |
|---|---:|---:|---|
| 인자 없음 | 0 | 10 | 관리자 표(id · 이름 · 역할 · 링크를 마지막으로 쓴 날 · 상태) + 다음에 무엇을 하라는 두 줄 |
| `3` (dept_lead) | **1** | 3 | 「관리자가 아닙니다 — 발급하지 않았습니다」 + 어디서 발급하라는 두 줄. **아무것도 만들지 않았습니다** |
| `2` (admin) | 0 | 8 | 링크 한 줄 (`invite/` 가 든 줄이 **정확히 1개**) + 유효기간 · 이전 링크 무효 안내 |

돌린 뒤 DB — `users` 21 그대로, `초대_링크_발급` 로그 3건, 마지막 로그가
`actor_id` 없음 · `actor_name` 없음 · `target_id=2`.

**행위자를 비운 이유** — 서버 앞에 앉은 사람이 누구인지 이 스크립트는
알 수 없습니다. 받는 사람을 행위자로 적으면 「그 사람이 스스로
발급했다」 는 거짓이 기록에 남습니다.

**「마지막 로그인」 칸은 없습니다.** 세션이 90일이라(4-12) 화면을 언제
열었는지는 아무 데도 안 남습니다. 표에 찍는 것은 `invite_tokens.used_at`
이고 **그 이름 그대로** 「링크 쓴 때」 로 적습니다 — 없는 값을
「마지막 로그인」 이라 부르면 그 뒤로 아무도 안 의심합니다.

`docs/배포-안내.md` §11-③ 에 세 줄로 적었습니다. **예시 링크는 적지
않았습니다** — 안내서의 예시 번호가 그대로 계정이 된 적이 있습니다(4-12).

---

# 2. 계좌 문을 닫았습니다

**담을 자리는 있는데 읽는 데 문이 없었습니다** — 로그인만 하면 `viewer`
까지 지출 목록에서 계좌를 보고 엑셀로 통째로 받았습니다.

| 자리 | 전 | 후 |
|---|---|---|
| 지출 목록의 계좌 (`app/templates/expenses.html`) | 로그인한 누구나 | 총무팀일 때만. 아니면 **요소 자체를 안 냅니다** |
| 등록 폼의 직전 계좌 (`app/routers/expenses.py` 의 `_last_meal_defaults`) | 남의 계좌가 미리 채워짐 | 총무팀에게만. 아니면 **이름도 계좌도 자기 것** |
| `/export/expenses.xlsx` (`app/routers/export.py`) | 로그인만 하면 200 | `require_admin` — 아니면 **403** |
| 지출·예산 화면의 「엑셀」 칩 | 누구에게나 보임 | 총무팀에게만 그립니다 |

**빈 칸이 아니라 없는 칸입니다.** 자리만 남기면 「무엇이 가려져 있다」
가 보이고, 그것도 알 필요 없는 사실입니다.

## 2-c. 시험 — 셋이 한 벌 (11-3)

| 시험 | 무엇을 잡나 | 결과 |
|---|---|---|
| `test23_g01_viewer_는_지출_목록에서_계좌를_못_본다` | ① 막히는가 · 빈 칸도 안 남는가 | 통과 |
| `test23_g02_부서_리더도_못_본다` | ① 편집자도 막히는가 | 통과 |
| `test23_g03_admin_은_본다` | ② **통과해야 할 것이 통과하는가** + ③ 그 회차에 계좌 값이 실제로 있는가 | 통과 |
| `test23_g04_엑셀은_admin_만_받는다` | ①② 파일 쪽 — admin 200 (엑셀 content-type 까지) · viewer·dept_lead 403 | 통과 |
| `test23_g05_등록_폼이_남의_계좌를_미리_채우지_않는다` | ①② 폼으로 새는 자리 · **이름과 계좌의 짝** | 통과 |
| `test23_g06_누를_수_없는_엑셀_단추를_그리지_않는다` | ①② 화면 — 비관리자에게는 칩이 없고 총무팀에게는 있다 | 통과 |
| `test23_h04_거절_갈래를_실제로_밟는다` | ①②③ 스크립트를 **실제로 불러** 거절 1 · 발급 0 · 토큰이 안 늚 | 통과 |

`g03` 이 없으면 **「아무에게도 안 보인다」 와 「제대로 막았다」 가
구별되지 않습니다.** 시험이 쓰는 계좌 문자열은 저장소가 이미 싣고 있는
자리표시자입니다 — 새 문자열을 지어내면 그것이 또 검사에 걸리고 넘김
목록만 늡니다.

## 2-d. `docs/checks/drawer.js` 가 지출 화면을 덮는가 — 안 덮고, 덮을 것이 없습니다

점검 스크립트는 화면을 **셋**으로만 가릅니다
(`docs/checks/drawer.js:137`~`139`) — `#sheet` 와 `#board` 가 있으면 보드,
`#tlist` 가 있으면 목록, **나머지는 전부 달력**입니다. `/expenses` 에서
돌리면 「달력」 으로 잘못 알아보고 열 것을 못 찾아 멈춥니다.

그런데 **고칠 것이 아닙니다.** 상세 패널을 include 하는 화면은
`board.html` · `calendar.html` · `tasks.html` (그리고
`partials/caldot.html`) 뿐이고 `expenses.html` 에는 `drawer` 라는 글자가
**0번** 나옵니다. 그 점검은 「패널이 한 벌인가」 를 재는 것이라 패널이
없는 화면은 잴 것이 없습니다. **이번 판이 지출 화면에서 고친 것은 값을
내보낼지 말지이지 패널 동작이 아닙니다** — 그 자리는 위 2-c 의 다섯
시험이 잽니다.

---

# 3 · 4 · 5. 남기는 자리

- **얼린 판** — `docs/review/2026-09-09-도막0.md` (844줄).
  `git show 452a398:docs/review/최근.md` 와 `diff` **차이 0**. 한 글자도
  안 고쳤습니다. `docs/봐둘것.md` 가 그 파일을 한 줄로 가리킵니다.
- **CLAUDE.md 11-3 4단계** — 「덮어쓰기 전에 날짜 이름으로 복사합니다」
  가 들어갔습니다 (`test23_i03`). 그리고 **보고를 다 채운 뒤에 검사와
  시험을 다시 돌린다**는 줄을 함께 넣었습니다 — 아래 「걸린 것」 을
  보세요.
- **CLAUDE.md 0장** — 계정은 예외라는 한 줄. 지울 때 걸리는 참조를
  **지우기 전에 센다**, `ondelete` 가 자리마다 다르다,
  `activity_logs.actor_id` 는 FK 가 아예 없다.
- **CLAUDE.md 5-5** — 박아 둔 항목 수를 뺐습니다. `check_counts` 는
  「셋 이상의 수 + 곳·군데」 만 잡으므로 **단위가 「건」 이면 안
  걸립니다** — 그 한계를 그 자리에 적었습니다.
- **`docs/인계.md`** — 새 채팅이 처음 읽는 파일. 13개 항목마다
  **무엇 · 재료가 어디 · 미정** 셋과, 맨 아래 「사람이 챙길 재료」
  (`test23_i02`).

## 걸린 것 — 도막 0 은 빨간 채로 커밋됐습니다

도막 0(452a398)의 보고 안에서 검토자가 인용한 SQL 한 줄이 별칭으로
시작해 11-2 의 검사(`test_docs_shell.py`)에 걸렸습니다. **전체 시험을
보고보다 먼저 돌려서** 그대로 나갔고, 이번 판이 그 파일을 얼리며
알았습니다.

두 가지를 했습니다.

1. **11-3 에 순서를 적었습니다** — 보고를 다 채운 뒤에 검사와 시험을
   다시 돌립니다. 검토자 원문에 무엇이 실릴지는 쓰기 전에 알 수 없습니다.
2. **얼린 판은 그 검사 밖으로 뺐습니다** (`test_docs_shell.py` 의
   `얼린판`). 한 글자도 못 고치는 파일을 계속 빨갛게 두면 사람이 검사를
   통째로 안 보게 됩니다. **살아 있는 `최근.md` 는 그대로 봅니다** —
   `test23_i04` 가 ① 얼린 판이 빠지는가 ② `최근.md` 는 남는가
   ③ 볼 목록이 비지 않았는가 셋을 함께 잽니다.

---

# 6. 수용 기준

| # | 기준 | 결과 | 근거 |
|---|---|---|---|
| 1 | 0 의 셈이 표로 나온다 | O | 위 0-(a)~(f) |
| 2 | `관리자링크.py` 가 기존 초대 흐름을 그대로 쓴다 | O | `test23_h01` · 0-(c) |
| 3 | 관리자가 아닌 id 는 거절한다 | O | `test23_h02` · 서버에서 exit 1 |
| 4 | 계좌가 총무팀에게만 보이고 엑셀도 막힌다 | O | `test23_g01`~`test23_g05` |
| 5 | 도막 0 보고가 한 글자도 안 바뀐 채 따로 남았다 | O | `diff` 차이 0 · `test23_i01` |
| 6 | 인계 문서가 있고 항목이 다 있다 | O | `test23_i02` |
| 7 | **`users` 행을 지우거나 고친 코드가 0줄** | O | `git diff 452a398 -- app/ scripts/` 에 `db.delete` · `delete(User` · `update(User)` 가 **0줄**. 이 판이 `users` 에 한 것은 **읽기뿐** |
| 8 | 덩어리와 최근.md 에 실명 · 전화번호 · 계좌 · 링크 토큰이 없다 | O | 글검사 통과 · 이 파일에 값 대신 **모양과 수**만 |
| 9 | pytest 옵션 없이 전부 통과 · 글검사 0 | O | 아래 7장 |
| 10 | 마무리가 11-3 대로 | O | 아래 7장 |

---

# 7. 돌린 것

## pytest (옵션 없이)

```
1274 passed in 608.61s (0:10:08)
```

**보고를 다 채운 뒤에 한 번 더 돌린 값입니다** — 이번 판이 11-3 에 적은
그 순서입니다. 그 전에 돌린 판은 `1271 passed · 1 failed` 였는데, 빨간
하나가 **아직 덮기 전의 도막 0 보고**였습니다(검토자가 인용한 SQL 한 줄).
시험을 보고보다 먼저 돌리면 그 실패가 진짜인지 낡은 것인지 알 수 없습니다.

## 글검사 (`scripts/글검사.py`)

```
━━━ 모아서 ━━━
  통과  check_names (exit 0)
  통과  check_stale (exit 0)
  실패  check_named (exit 1)
  통과  check_counts (exit 0)

!! 1개가 빨갛습니다: check_named
```

**한 번 빨갰습니다** — `check_named` 이 「문서만 아는 이름」 으로
`docs/review/2026-09-09-도막1.md` 를 짚었습니다. 0장이 그 파일을
가리키는데 아직 얼리기 전이라 없었던 것입니다. **얼리고 나서 초록이
됐습니다** — 검사가 「가리키는 곳이 실재하는가」 를 실제로 보고 있다는
증거이기도 합니다.

## 서버 (11-3 2단계 — `app/` 이 바뀌었으므로 건너뛰지 않았습니다)

**두 번 재시작했습니다** — 검토자를 받아 `app/` 을 또 고쳤기 때문입니다.
**커밋 기준으로 봅니다**(11-3): 이번에 내는 커밋에 `app/` · `templates/`
가 들어 있으므로, 앞선 판에서 확인했더라도 **마지막 코드로 도는 서버**를
다시 확인합니다.

| 언제 | 전 PID | 후 PID | 판정 |
|---|---:|---:|---|
| 계좌 문을 닫은 뒤 | 13984 | 18500 | 성공 — PID 가 바뀌었습니다 |
| **검토자를 받아 고친 뒤 (마지막)** | 18500 | **15204** | 성공 — PID 가 바뀌었습니다 |

마지막 PID(15204)에서 실제로 받아 본 것:

| 무엇 | 값 |
|---|---|
| `GET /` | 303 (로그인으로) |
| `GET /login` | 200 |
| 화면이 물어 온 CSS | `retreat.bdf527e8.css` (내용 해시가 붙은 주소) |
| `GET /export/expenses.xlsx` (로그인 없이) | 303 — 로그인 문이 먼저입니다. 총무팀만 받는 것은 그 뒤의 `require_admin` 이고, 그쪽은 `test23_g04` 가 잽니다 |

## `docs/checks/drawer.js` — 세 화면 × 두 계정

개발 서버(8001 · 가명 자료)에서 돌렸습니다. 스크립트를 정적 주소로 받아
돌리되 **해시 자리를 매번 바꾸고 `{cache:'reload'}` 로 받았으며, 받은
길이를 지금 파일과 견줬습니다** — 59,095자로 여섯 번 다 같았습니다(11-3).

| 화면 | 관리자 | 부서 리더 |
|---|---|---|
| 보드 | 79/79 · 건너뜀 2 | 62/62 · 건너뜀 15 |
| 목록 | 92/92 · 건너뜀 4 | 78/78 · 건너뜀 16 |
| 달력 | 82/82 · 건너뜀 3 | 65/65 · 건너뜀 16 |

여섯 판 모두 **실패 0 · 못 쟀음 0 · 완주 참**입니다.
관리자 쪽 건너뜀은 「고칠 수 있는 논의가 없음」 · 「연결된 업무가 없음」
같은 자료 사정과, 스크립트가 스스로 「부서 리더로 한 번 더 돌려 보라」
고 적은 권한 두 항목입니다 — **그 두 항목을 부서 리더 판이 잽니다.**

**검토자 뒤에 고친 것이 화면에 닿아 다시 돌리지 않았습니다** — 바뀐
것은 지출·예산 화면의 엑셀 칩 하나와 변경 이력의 라벨 하나이고, 둘 다
상세 패널이 없는 화면입니다(위 2-d 와 같은 이유). 그 자리는
`test23_g06` 이 잽니다.

**자가시험은 안 돌렸습니다.** 이번 판은 `docs/checks/drawer.js` 를 한
줄도 고치지 않았습니다 (11-3 은 「점검 스크립트 자신을 고쳤으면」 이
조건입니다).

> **첫 판이 한 번 빨갰는데 화면 잘못이 아니었습니다.** 미리보기 창이
> 숨어 있어 `offsetParent` 가 전부 비었고(레이아웃이 안 돌아 「열어 볼
> 업무가 하나도 없습니다」), 창을 보이게 한 뒤에도 폭이 정확히 1280px
> 이라 사이드바 기본 상태 판정이 경계에 걸렸습니다. 1440×900 으로
> 고정하고 저장값을 지운 뒤 다시 재서 위 표를 얻었습니다 —
> **검사가 흔들린 것이지 화면이 흔들린 것이 아닙니다.**

## 바뀐 파일 (`git diff --name-only 452a398..HEAD`)

```
CLAUDE.md
app/routers/expenses.py
app/routers/export.py
app/templates/budget.html
app/templates/expenses.html
app/templates/settings_checkup.html
"docs/review/2026-09-09-\353\217\204\353\247\2110.md"
"docs/review/\354\265\234\352\267\274.md"
"docs/\353\260\260\355\217\254-\354\225\210\353\202\264.md"
"docs/\353\264\220\353\221\230\352\262\203.md"
"docs/\354\204\270\354\226\264\353\221\240.txt"
"docs/\354\235\270\352\263\204.md"
"scripts/\352\264\200\353\246\254\354\236\220\353\247\201\355\201\254.py"
tests/test_docs_shell.py
tests/test_stage23.py
tests/test_stage6.py
```

---

# 8. 커밋

`452a398..0331a35` — 일곱 판입니다.

| 해시 | 무엇 때문에 |
|---|---|
| `9b7086e` | 세션을 잃은 관리자가 서버에서 다시 들어갈 길을 연다 |
| `2565cce` | 계좌를 담을 자리는 있는데 읽는 데 문이 없었다 |
| `f34c5b3` | 이름이 없다고 전부 「시스템」 이 아니다 |
| `bf915f0` | 막는 코드마다 막히는 쪽·통과하는 쪽·검사가 보는지를 함께 잰다 |
| `dd3faff` | 기준 문서에 이번 판의 판단을 적고, 부딪히는 자리에 경계를 긋는다 |
| `266a352` | 새 채팅이 먼저 읽을 것을 만들고, 덮이기 전에 떠 둔다 |
| `0331a35` | 검토용 보고를 이번 작업으로 덮어쓰고, 그 셈을 얼려 둔다 |

**이 줄을 채우는 커밋이 하나 더 붙습니다.** 보고가 자기 커밋의 해시를
담을 수는 없기 때문입니다 — 그 커밋은 이 표와 머리의 해시 두 줄만
바꾸고, 바꾼 뒤 글검사와 문서를 읽는 시험을 다시 돌렸습니다.

---

# 9. 검토자 원문

익명화를 지난 그대로입니다. 「채팅에 있다」 로 가리키지 않습니다(11-3) —
채팅 덩어리는 저장소에 남지 않아 다음 사람이 볼 길이 없어집니다.

```
━━━ 검토자 여기부터 ━━━ 범위 452a398..작업트리 · 파일 14개 · 커밋 전

  다시 잰 것 (읽기만)
    pytest (옵션 없이)  1272 passed in 657.50s  → 초록
    scripts/글검사.py    check_names·check_stale·check_named·check_counts 전부 exit 0
    운영 data/app.db 를 읽기전용으로 다시 세어 0-(a)~(f) 를 대조
    payer_account / bank_account 를 app/ · scripts/ 전수로 훑음

  통과한 수용 기준 (내가 직접 확인)
    1  0-(a)(e)(f) 의 수가 전부 내 셈과 일치. 역할 admin 4(활성 2)·dept_lead 11·
       member 5·viewer 1 = 21(활성 19), FK 22개·값 든 행 2,006, 표별 내림차순도
       한 줄씩 일치. activity_logs.actor_id 41 · FK 없음도 일치.
       payer_account 5 · bank_account 0 일치
    2  scripts/관리자링크.py:121·141 이 invites.issue · invites.invite_url 만 부르고
       token_urlsafe · hash_token · InviteToken( 은 파일에 없음. auth.issue 는
       users 를 건드리지 않음 (app/domain/auth.py:64~82)
    3  거절 갈래가 있음 (scripts/관리자링크.py:111~119) — 비활성 계정까지 막음
    5  git show 452a398:docs/review/최근.md 와 docs/review/2026-09-09-도막0.md 의
       diff 차이 0
    7  git diff 452a398 -- app/ scripts/ 에서 users 를 쓰는 줄은 select 한 줄뿐
       (관리자링크.py:83). delete·update·필드 대입 0줄
    8  최근.md · 인계.md · 관리자링크.py · test_stage23.py · CLAUDE.md 에
       전화·계좌 꼴 정규식으로 걸리는 것은 전부 합성값(01088880001 등,
       "국민 000000-00-000000")
    9  위의 pytest · 글검사 그대로

  ── 고쳐야 함 ────────────────────────────────────────────────

  A. 0-(b) 의 「살아 있는 링크」 가 살아 있지 않은 것까지 셌습니다
     docs/review/최근.md:32
       "살아 있는 링크를 가진 계정 (used_at 없음) | 5"
     used_at 이 비었다는 것과 링크가 살아 있다는 것은 다릅니다.
     app/domain/auth.py:102~112 의 problem_with 가 정본이고, 거기서는
     revoked_at · used_at · expires_at 셋을 봅니다. issue() 는 발급할 때마다
     앞의 것을 취소하므로(auth.py:70) used_at 이 빈 취소된 토큰이 쌓입니다.
     다시 세어 보니 —
       실제로 쓸 수 있는 링크를 가진 계정 : 1
       취소된 토큰 행                     : 13
       미사용·미취소인데 기한 지난 행     : 2
     이 수는 12장이 「다음에 볼 곳」 으로 적어 둔 「링크를 열었는데 안
     들어와진 사람이 있는가」 의 입력입니다. 5 로 읽으면 「넷은 링크를
     쥐고 있으니 기다리면 된다」 가 되는데 실제로는 그 넷의 링크가 죽어
     있어 재발급이 필요합니다 — 방향이 반대로 갈립니다.
     같은 절의 「쓴 계정 3 / first_seen_at 1」 은 맞습니다.

  B. 계정 삭제 — CLAUDE.md 가 자기 자신과 반대를 말합니다
     새로 적은 것   CLAUDE.md:35~49  "예외는 계정(users)입니다. 사람이 고른
                                      계정은 지웁니다"  (근거로 4-12 를 듦)
     그대로 남은 것 CLAUDE.md:794     "삭제는 두지 않고 비활성화만 둡니다"
                    CLAUDE.md:874     "지우지 않습니다 — 작성자로 남아 있습니다"
     4-12 는 바로 그 「중복 계정」 을 다루면서 비활성화로 결론냈고, 그
     결론대로 만든 도구가 scripts/merge_users.py 입니다. 0장이 4-12 를
     근거로 들면서 4-12 와 반대 결론을 냈는데, 둘 중 어느 쪽이 정본인지
     문서에 없습니다. 11-3 3단계가 정확히 이 모양(한 곳을 고치고 그것을
     옮겨 적은 다른 곳이 남는 것)을 잡으라는 자리입니다.
     정하는 것은 사람 몫이라 뒤집지 않고 물음으로 남깁니다 — 아래 참조.
     (화면에는 여전히 삭제가 없고 지우는 길도 아직 없으므로 지금 새는
      것은 없습니다. 새는 것은 다음 사람이 어느 쪽을 읽느냐입니다.)

  C. 이번 판이 만든 규칙을 이번 판이 어겼습니다 — 덮일 파일을 가리킴
     docs/인계.md:30   "참조 표는 docs/review/최근.md 의 0-(d)"
     CLAUDE.md:48      "센 값은 2026-09-09 도막 1 의 보고에 있습니다"
     둘 다 최근.md 를 가리키는데, 11-3 은 그 파일을 매번 덮는다고
     정했고 이번 판이 바로 그 이유로 도막 0 을 얼렸습니다. 다음 판이
     덮는 순간 FK 표(2,006행을 어디서 잃는지)가 사라지고, 인계 2번은
     「재료가 어디」 칸이 빈 항목이 됩니다 — 인계.md:8~9 가 스스로
     「셋 중 하나가 비면 그 자리가 막히는 자리」 라고 적어 둔 그것입니다.
     그리고 절 번호도 틀렸습니다 — FK 표는 0-(d) 가 아니라 0-(e) 입니다
     (0-(d) 는 「세션을 잃은 관리자에게 화면 길이 있는가」).
     인계.md:103 의 「도막 0 의 0-(c)」 는 맞습니다(영수증).

  D. 「얼려 둔 판은 그 검사들 밖」 이 실제보다 넓게 적혔습니다
     CLAUDE.md:2958   "얼려 둔 판은 그 검사들 밖입니다"
     바로 앞 문단이 그 「검사들」 을 「1단계의 글검사와 pytest」 로
     가리키는데, 실제로 뺀 자리는 tests/test_docs_shell.py 의 볼파일
     하나뿐입니다. 글검사 안쪽은 그대로 봅니다 —
       check_stale · check_counts : git ls-files 전부
                                    (scripts/check_stale.py:107,
                                     scripts/check_counts.py:113)
       check_names               : docs/ 아래 전부 (scripts/check_names.py:284)
     증거가 이번 판 안에 있습니다 — docs/세어둠.txt 에 얼린 파일을 위해
     한 줄을 손으로 더했습니다. 즉 check_counts 는 여전히 보고 있고,
     다음에 얼릴 판마다 사람이 또 한 줄씩 적어야 합니다. 문서가
     「밖입니다」 라고 단정해 두면 다음 사람은 그 손질을 안 합니다.
     문서를 좁히거나(어느 검사가 빼는지 이름을 적기), 빼는 자리를
     scripts/넘김.py 쪽으로 옮기거나 둘 중 하나입니다.

  E. 누를 수 없는 단추가 생겼습니다 — 엑셀 칩
     app/templates/expenses.html:16 · app/templates/budget.html:24
       &lt;a class="chip xls" href="/export/expenses.xlsx?..."&gt;엑셀&lt;/a&gt;
     조건 없이 그려집니다. 그런데 그 주소는 이제 require_admin 이라
     (app/routers/export.py:32) viewer·dept_lead 가 누르면 403 화면입니다.
     10장이 「pytest 는 화면을 못 봅니다 — 세 번 당했습니다」 에 「누를 수
     없는 단추」 를 이미 올려 둔 그 모양입니다. is_admin(user) 가 템플릿
     전역에 있으므로(app/templating.py:185) 같은 줄에서 감출 수 있고,
     지출 목록의 계좌 줄은 실제로 그렇게 했습니다(expenses.html:156).
     시험 test23_g04 는 403 을 재지만 화면에 단추가 남아 있는지는
     재지 않습니다.

  F. 등록 폼에서 「이름」 과 「계좌」 의 짝이 깨졌습니다 — 돈 가는 자리
     app/routers/expenses.py:159~165
       payer_name    : last.payer_name or user.name      (그대로 남의 이름)
       payer_account : 남의계좌 or user.bank_account     (비관리자는 내 계좌)
     고치기 전에는 (남의 이름, 남의 계좌) 라 짝은 맞았습니다. 지금은
     비관리자에게 (남의 이름, 내 계좌) 가 뜹니다. 환급은 payer_name 으로
     사람을 가르고(app/domain/budget.py:203~213) 돈은 payer_account 로
     갑니다 — 그대로 저장하면 「A 가 낸 것으로 적힌 지출을 B 계좌로
     환급」 이 되고, 두 칸이 서로를 부정하는데 화면에는 아무 표시도
     나지 않습니다. 계좌를 자기 것으로 떨어뜨렸으면 이름도 함께
     떨어뜨리는 것이 짝을 지키는 쪽입니다(한 줄).
     test23_g05 는 계좌만 보고 이름은 안 봅니다.

  ── 봐 둘 것 ─────────────────────────────────────────────────

  G. 활동 기록이 화면에서 「시스템」 으로 읽힙니다
     scripts/관리자링크.py:127~135 는 actor=None 으로 남기는데(그 판단
     자체는 옳습니다 — 서버 앞의 사람이 누구인지 모릅니다),
     log_activity 의 actor_type 기본값이 "user" 이고(app/deps.py:77)
     화면은 actor_name 이 비면 「시스템」 으로 찍습니다
     (app/templates/settings_checkup.html:46). 8장은 system 을 「앱이 뜰 때
     한 번 도는 전환」 으로 못박아 두었으니, 사람이 서버에서 한 발급이
     그 라벨을 쓰면 뜻이 하나 겹칩니다. summary 에 「(관리자 복구)」 가
     있어 구별은 되지만 라벨은 거짓입니다.

  H. 관리자링크.py 가 DB 가 없을 때 트레이스백으로 죽습니다
     init_db() 를 안 부르는 판단은 맞다고 봅니다 — app/db.py 는 import
     만으로는 아무것도 만들지 않고(엔진·세션만), 이 스크립트가 도는
     때는 정의상 DB 가 이미 있는 때입니다. 다만 데이터 폴더를 잘못
     짚었거나 DCB_DATABASE_URL 이 다른 것을 가리키면 sqlite 가 빈 파일을
     만들고 첫 조회에서 「no such table」 로 죽습니다. 이 스크립트는
     자기 머리(:39~40)에 「안내를 못 찍고 죽으면 막힌 사람이 무엇을
     할지 모른다」 고 적어 둔 파일이라, 그 한 갈래만 한국어 한 줄로
     받는 편이 앞뒤가 맞습니다.

  I. test23_h01 · h02 는 낱말만 잽니다 (보고도 그렇게 적었습니다)
     h02 는 "person.role != perm.ADMIN" 이라는 문자열이 파일에 있는지만
     봅니다. perm.ADMIN 이 없어져도(지금은 app/domain/permissions.py:3 에
     있습니다) 문자열은 그대로라 초록이고, 실제로 부르면 AttributeError
     입니다 — 그리고 그 경로를 처음 밟는 것은 「관리자가 다 막힌 날」
     입니다. 11-3 이 대응표 겹침 거절에서 겪은 그 모양입니다.
     사람이 서버에서 세 번 돌린 기록이 보고에 있어 지금은 사실이지만,
     회귀를 잡는 것은 없습니다. 임시 sqlite 에 admin 아닌 계정 하나를
     넣고 링크()를 직접 불러 반환값 1 과 invite_tokens 가 안 는 것만
     보면 열 줄입니다.

  J. test23_i02 의 항목 세기는 부분문자열이라 헐겁습니다
     tests/test_stage23.py:186  assert f"{n})" in 글
     "11)" 이 "1)" 를 만족시키므로 1~3 번 항목이 통째로 빠져도 초록입니다.
     줄 머리로 보면(^\d+\)) 됩니다.

  K. 화면과 파일의 경계가 5-8 과 어긋난 채로 남았습니다
     7-3 은 엑셀을 「화면과 같은 함수」 로, 5-8 은 「따로 만들면 어긋나고
     어긋난 쪽이 파일이면 아무도 모른다」 로 정해 두었습니다. 이번 판
     뒤로 화면은 계좌만 빼고 누구나, 파일은 통째로 admin 입니다 —
     같은 표인데 보는 사람이 갈렸습니다. 다른 갈래(같은 admin 판정을
     budget_xlsx 에 넘겨 계좌 칸만 비우기 · app/domain/budget_xlsx.py:71,133)
     가 있는데 보고가 그 갈래를 견준 자리를 적지 않았습니다.
     어느 쪽이 맞는지는 사람이 정할 일이라 뒤집지 않습니다.

  L. 6장이 9·10 을 O 로 적었는데 7장의 근거 칸이 비어 있습니다
     docs/review/최근.md:249~250 대 :258~266 · :307~309.
     푸시 직전에 채운다고 적혀 있어 순서상 그럴 수 있지만, 지금 파일만
     보면 「O」 가 아직 사실이 아닙니다. 그리고 이번 판이 새로 적은
     규칙(보고를 다 채운 뒤에 검사와 시험을 다시 돌린다)이 바로 그
     자리이므로, 이 검토자 원문을 9장에 실은 뒤 글검사와 pytest 를
     한 번 더 돌려야 합니다 — 원문이 문서 검사에 걸릴 수 있습니다.

  ── 새는 다른 입구를 물어보셔서 (전수) ───────────────────────

  app/ · scripts/ 에서 payer_account · bank_account 가 나오는 자리 전부:
    app/models.py:141,246                 정의
    app/routers/expenses.py:150,159,163   폼 채우기 (위 F)
    app/routers/expenses.py:266,325       POST 로 받아 저장 (쓰기 — 문 아님)
    app/routers/settings.py:446,451       자기 계좌를 자기가 고침
    app/templates/settings.html:25        자기 계좌 (내 정보)
    app/templates/expenses.html:80        입력칸 (placeholder 는 예시값)
    app/templates/expenses.html:156       목록 — is_admin 으로 막음
    app/domain/budget_xlsx.py:71,133      엑셀 두 시트 — 라우터에서 막음
  budget_xlsx.write 를 부르는 라우터는 app/routers/export.py:39 하나이고,
  /export/expenses.xlsx 를 가리키는 자리는 위 E 의 템플릿 둘뿐입니다.
  지출 값을 내보내는 JSON·API 는 없습니다. 계좌를 활동 기록에
  before/after 로 남기는 자리도 없습니다.
  → 값이 새는 입구는 더 없습니다. 남은 것은 E(죽은 단추)와 F(짝 깨짐).

  ── 얼린판 예외가 너무 넓은지 (물어보셔서) ──────────────────

  tests/test_docs_shell.py:160  docs/review/ 아래 날짜로 시작하는 .md 전부.
  지금 그 꼴인 파일은 얼린 판 하나뿐이고, 나머지(제안-1판·2판·성적표·
  이미지-확인·리뷰요청·최근)는 그대로 봅니다. 범위 자체는 좁습니다.
  다만 「날짜로 시작하면 뺀다」 는 이름 규칙이라, 앞으로 docs/review/ 에
  날짜를 붙인 살아 있는 문서를 두면 조용히 빠집니다. 그리고 test23_i04 는
  최근.md 가 남는지·목록이 30개를 넘는지를 재지만 「뺀 것이 얼린 판뿐인가」
  는 안 잽니다(any(얼린판) 로만 봅니다). 지금 문제는 아니고, 위 D 가 같은 자리의 더 큰 구멍입니다.

  ── 없다고 적는 것 ───────────────────────────────────────────

  users 를 지우거나 고친 코드 — 없습니다 (AC7 통과).
  실명·전화·계좌·토큰이 새 파일로 들어간 자리 — 없습니다 (AC8 통과).
  0장의 셈이 틀린 자리 — 0-(b) 한 줄(위 A) 말고는 없습니다.

  ── 사람에게 묻는 것 ─────────────────────────────────────────

  1. 계정을 지웁니까, 비활성화만 합니까. 0장 예외(CLAUDE.md:35)와
     4-12(:794) · merge_users(:874)가 반대를 말합니다. 어느 쪽이
     정본인지 정해 주시면 나머지 한쪽에 한 줄이 필요합니다 —
     「화면에는 삭제가 없고, 사람이 id 를 골랐을 때만 스크립트로」
     같은 경계라면 두 문단이 함께 설 수 있습니다.
  2. FK 표(2,006행)를 어디에 남깁니까. 지금은 덮일 파일에만 있고
     인계 2번과 0장이 그것을 가리킵니다. 이번 판 보고도 함께
     얼릴 것인지, 아니면 인계.md 안에 표를 옮겨 적을 것인지.
  3. 엑셀을 admin 전용으로 둘 것인지, 계좌 칸만 비워 부서 리더도
     받게 할 것인지 (위 K). 전자면 단추를 감춰야 합니다 (위 E).
  4. 「살아 있는 링크」 의 정의를 problem_with 와 맞출 것인지
     (취소·만료를 뺄 것인지). 설정 › 사용자의 「링크 안 받은 사람 N명」
     이 어느 정의를 쓰는지도 함께 보셔야 합니다.

━━━ 검토자 여기까지 ━━━
```

## 검토자를 받아 고친 것

| # | 무엇 | 어떻게 |
|---|---|---|
| A | 「살아 있는 링크 5」 가 **죽은 링크까지 셌음** | `problem_with` 로 다시 세어 **1** 로 고치고, 24행의 내막(쓴 8 · 취소 13 · 만료 2)과 **왜 방향이 갈리는지**를 0-(b) 에 적었습니다 |
| B | 0장 예외와 4-12 · `merge_users` 가 **서로 반대를 말함** | 뒤집지 않고 **경계를 적었습니다** — 화면에는 삭제가 없고(4-12 가 막는 것은 「누르면 사라지는 단추」), 0장이 연 것은 「무엇을 잃는지 세어 보인 뒤 사람이 고른 계정」 한 갈래입니다. 4-12 와 `merge_users` 절 둘 다에 그 줄을 넣었습니다 |
| C | **덮일 파일을 가리키고 있었음** (그리고 절 번호도 틀림) | 이 보고를 `docs/review/2026-09-09-도막1.md` 로 얼리고, 0장과 `docs/인계.md` 가 **그 파일의 0-(e)** 를 가리키게 했습니다. 인계 2번의 「재료가 어디」 칸에는 표의 요점도 함께 적었습니다 |
| D | 「얼린 판은 그 **검사들** 밖」 이 실제보다 넓었음 | 빼는 자리는 `tests/test_docs_shell.py` 의 `얼린판` **하나뿐**이고 글검사 쪽은 그대로 본다고 고쳤습니다 — **얼릴 때마다 넘김 한 줄이 필요할 수 있다**는 것까지 적었습니다(실제로 이번에 `세어둠.txt` 에 한 줄 들어갔습니다) |
| E | **누를 수 없는 엑셀 단추** | 지출·예산 화면 둘 다 `is_admin(user)` 일 때만 그립니다. `test23_g06` 이 ①(비관리자에게 없음) ②(총무팀에게 있음)를 함께 잽니다 |
| F | 등록 폼에서 **이름과 계좌의 짝이 깨짐** | 계좌를 떨어뜨릴 때 **이름도 함께** 떨어뜨립니다. `test23_g05` 가 이름까지 보고, ③ 으로 **관리자 폼에는 직전 이름이 그대로 들어 있는지**도 잽니다 — 그 줄이 없으면 「폼을 못 찾았다」 로도 통과합니다 |
| G | 서버 발급이 화면에서 **「시스템」** 으로 읽힘 | 8장이 그 말을 「앱이 뜰 때 도는 전환」 에 못박아 두었으므로, 변경 이력이 `actor_type` 으로 가릅니다 — 이름이 없고 `system` 이 아니면 **「서버에서」** |
| H | DB 가 없을 때 **트레이스백으로 죽음** | `OperationalError` 한 갈래만 받아 지금 보고 있는 곳과 무엇을 확인해야 하는지를 한국어로 찍습니다 |
| I | 거절 갈래에 **낱말 시험만 있었음** | `test23_h04` 가 스크립트를 **실제로 불러** ① 거절 1 ② 관리자에게는 발급 0 ③ 거절 쪽은 토큰이 안 늚 을 잽니다 |
| J | 항목 세기가 **부분문자열** | 줄 머리 정규식으로 바꾸고 **빠진 번호를 이름으로** 말하게 했습니다 |
| K | 화면과 파일의 경계가 5-8 과 어긋남 | **고치지 않고 `docs/봐둘것.md` 에 열어 뒀습니다** — 계좌 칸만 비워 부서 리더도 받게 하는 갈래가 있고, 어느 쪽인지는 사람이 정할 일입니다. 지금 통째로 막은 이유(한 번 나간 파일은 손을 떠난다)도 함께 적었습니다 |
| L | 6장이 O 인데 7장 근거가 비어 있었음 | 이 원문을 실은 **뒤에** 글검사와 pytest 를 다시 돌려 위 7장을 채웠습니다 |

**사람이 정할 것 넷**은 검토자 원문 끝에 그대로 있습니다. 그중 둘은
이번 판이 답을 냈고(1 — 경계를 적어 둘이 함께 서게 · 2 — 이 보고를
얼려 그것을 가리키게), 둘은 열어 두었습니다(3 — 엑셀의 범위 ·
4 — 「링크 안 받은 사람 N명」 이 어느 정의를 쓰는지).
