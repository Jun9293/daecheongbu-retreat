사람이 할 것
노트북에서 직접: 하나 — 개발 DB 를 지우고 seed 를 다시 만드는 일이 앞 판들에서 넘어와 아직 남았다(인계 「재정 — 지금 어디까지」 머리의 남은 것 표). 이 판이 새로 넘기는 것은 없다
관리자 권한: 없음 — app/ · templates/ · static/ 을 안 건드려 재시작하지 않았다(4장)
정할 것: 하나 — 2026-09-13 사고 직전 판(`app-20260912-151859.db`)을 `지키는판` 에 넣을지. 봐둘것이 한때 되돌리는 자리로 가리켰으나 사고가 되돌림으로 닫혀 지금은 넘김(`docs/사본-넘김.txt`)에 있다. **안 정하면 성한 판 30 칸이 차는 날 지워진다** — 새벽 백업과 수동 사본이 쌓이는 대로라 한 달 안쪽이다(5장 · 검토 7-2)
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 이 판은 화면 코드를 안 바꿨다(4장)

작성 시각: 2026-09-15 16:35 KST
커밋: 9d23abd (규칙 · 시험) · 5dc28f8 (인계 · 얼림) · f2e10e6 (검토 고침) · 이 보고는 그다음 커밋
작업: 되돌리는 사본 규칙을 못박는다 · 재정 줄기 마무리

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `지키는판` 을 읽는 곳은 `scripts/backup.py`(`prune` · `의심말들`) 와 `tests/test_stage44.py`(첫 원소만 씀). 넘김 목록을 읽는 곳은 `scripts/넘김.py` 하나 — 새 시험도 그것을 부른다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 문서 · 시험 · 주석만 |
| 그 값을 세어 둔 문서 | 사본 이름을 적은 살아 있는 글: CLAUDE.md(7-5) · 인계 · 봐둘것 · `docs/재정-잇기-안내.md`. 얼린 보고에도 많지만 그 판의 기록이라 안 본다 |

# 1. 한 것

- **1a · 1c 규칙** — CLAUDE.md 11-2 백업 절에 「문서가 되돌리는 자리로 가리키는 사본은 `지키는판` 에 반드시 · 문서에 적는 것과 넣는 것은 한 판에서 같이」 와 까닭. `scripts/backup.py` 머리 주석 두 줄은 그 절을 가리키기만 한다(다시 적지 않음). 까닭 문자열 규칙은 그대로(1b)
- **2a 판단** — 기존 글검사로는 안 된다. `check_named` 는 문서가 이름한 것이 코드에 **있는지**만 보고, `check_stale` 은 사람이 적은 **말**만 찾는다 — 둘 다 「이름 X 가 사전 `지키는판` 의 열쇠인가」 를 못 묻는다. 그래서 pytest 시험을 따로 둔다
- **2b 시험** — `tests/test_stage52.py`: CLAUDE.md 와 `docs/**/*.md`(`docs/review/` 제외)에서 `app-날짜-시각.db` 이름을 찾아 `지키는판` 에 있거나 `docs/사본-넘김.txt` 에 이유와 함께 있는지. **이름이 나온다고 다 되돌리는 자리는 아니어서**(「그 사본을 뜨고 돌았다」 같은 기록) 넘김이 필요했다 — 기록인지는 기계가 못 가르므로 넘김의 이유 칸이 사람이 보는 자리. 넘김 다섯(4-2)
- **3a · 3b · 3c 인계** — 재정 절 머리에 「차례 1~6 전부 닫힘 · 30건 운영 반영(2026-09-15)」 과 남은 것 표(개발 DB 재생성 · 점검 스크립트 · 16번 구매목록 · 순서표 7 · BB-i 봐 둘 것 둘) · 맨 위 갱신과 「할 것」 머리에 「다음 줄기는 회의록이거나 사람이 다시 정한다」 · 앞 판 보고를 `docs/review/2026-09-15-짝잇기실행.md` 로 얼림(옛말-넘김 한 줄)
- **검토 고침(f2e10e6)** — 넘김 이유 둘을 원문에 맞게 · 인계 「할 것」 머리 · 순서표 7 · 시험을 실제 문서와 뗌 · 11-2 에 못 보는 범위

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 11-2 와 backup.py 주석이 같은 말 | O | backup.py 는 요약 한 줄 뒤 11-2 를 가리킴 · 검토 7-1 「다른 말 없음」 |
| 나) 빠진 사본을 잡는 시험 · 002352 · 144929 를 빼고 심어 빨개짐 | O | `test52_a01`(실제 문서 · 본 수 > 0) · `a02`(지어낸 글에서 빼면 잡음) · `a03`(넘김·지키는판이면 통과 · 얼린 보고 안 봄) · `a04`(이유 없는 넘김은 안 넘김). **backup.py 에서 두 줄을 실제로 지우자 a01 이 두 이름과 그 이름이 나온 파일로 빨갛고** 되돌린 뒤 통과(6장) |
| 다) 인계 재정 순서표 「전부 닫힘」 · 남은 것 한 자리 | O | 인계 「재정 — 지금 어디까지」 머리 |
| 라) pytest 옵션 없이 · 글검사 7/7 | O | 6장 |
| 마) 검토자 원문 | O | 7장 |

# 3. 운영 자료

**안 건드렸다** — 이 판은 운영 DB · 백업 폴더를 열지 않았다(사본 이름은 앞 판 보고와 문서에서 옴).

# 4. 재시작 · 넘김

## 4-1. 재시작

**건너뛰었다** — 커밋 넷에 `app/` · `templates/` · `static/` 이 없다(`scripts/backup.py` 는 주석만). 떠 있는 서버는 PID 18592 그대로.

## 4-2. 넘김 다섯 (`docs/사본-넘김.txt`)

| 사본 | 왜 지키지 않나 |
|---|---|
| 09-12 15:18 | 되돌리는 자리로 적혔으나 사고가 되돌림으로 닫혀 뜻을 잃음 — **지킬지는 정할 것** |
| 09-14 16:42 | 영수증 잇기 직전 사본 — 그 뒤 한 일은 앱에서 뗄 수 있어 DB 로 되돌릴 일이 없음 |
| 09-11 · 09-10 비품 사본 셋 | 그 판이 뜬 사본을 적은 기록 |

# 5. 판단이 들어간 자리

- **pytest 시험으로 둠** — 글검사에 넣지 않았다. 규칙이 사전 하나와 문서의 짝이라 코드를 import 하는 시험이 짧고, pytest 는 커밋마다 돈다
- **넘김 목록을 둠** — 이름 단위라 한 번 넘긴 이름이 뒤에 되돌리는 자리로 가리켜지면 못 잡는다. 11-2 에 그 한계와 다른 못 보는 범위(날짜만 적은 이름 · `.md` 밖 · 살아 있는 `최근.md`)를 적고, 되돌리는 사본 이름은 인계나 봐둘것에도 적는다고 했다
- **09-12 15:18 판을 넣지 않고 사람에게 올림** — 넣는 쪽은 아무것도 안 지우지만, 지시문이 규칙으로 정한 대상은 「되돌리는 자리로 가리키는」 사본이고 이 판은 사고가 닫혀 그 뜻을 잃었다. 증거 성격도 있어(030001 은 증거로 지킴) 기계적으로 가르지 않았다. **시한** — `stamps_in` 이 수동 사본도 성한 판 30 칸에 세므로 한 달보다 이를 수 있다
- **안 고친 것 둘(봐 둘 것)** — 인계 8~10 의 옛 갱신 줄(이름표로 옛 판임을 밝힘) · 같은 이름이 지키는판과 넘김에 함께 있는 모순과 문서에서 사라졌는데 넘김에 남은 줄은 시험이 안 잰다

# 6. 검사와 시험

- 글검사(커밋 전): 7/7 통과 — 처음엔 `check_named`(새 파일을 git 이 몰라서 · 스테이지 뒤 통과) · `check_stale`(얼린 판의 검토자 인용 · 옛말-넘김 한 줄) 이 빨갰다
- 고장 심기: `scripts/backup.py` 의 `지키는판` 에서 002352 · 144929 줄을 지움 → `test52_a01` 이 `app-20260915-002352.db`(CLAUDE.md · 봐둘것 · 인계 · 재정-잇기-안내) · `app-20260915-144929.db`(봐둘것 · 인계) 로 빨강 → 원래 글로 되돌림(`git diff` 에 주석 두 줄만) → stage52 · 44 · 16 `48 passed`
- 검토 고침 뒤: stage52 `4 passed` · 글검사 7/7
- 보고를 다 채운 뒤 돌린 것: 아래 줄

- 글검사 7/7 통과 · pytest(옵션 없이) `1595 passed, 3 warnings in 1550.36s (0:25:50)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토 (2d7048c..5dc28f8)

```
━━━ 검토자 여기부터 ━━━ 범위 2d7048c..5dc28f8 · 커밋 2개 · 파일 7개

무엇을 봤나
  git diff 전체 · backup.py 의 지키는판 · stamps_in · prune · scripts/넘김.py
  문서 전체에서 app-YYYYMMDD-HHMMSS 를 git grep (docs/review 제외, 저장소 전체)
  넘김 다섯 줄이 가리키는 원문(봐둘것 3032·3035·3519 · 인계 299·378·383·413)
  인계 「남은 것」 표의 네 가리킴(계좌게이트 5장 · BB-i 머리 · 할 것 16 · 순서표 8)
  돌린 것: pytest tests/test_stage52.py → 4 passed · scripts/글검사.py → 전부 exit 0
  운영 자료와 data/backups 는 열지 않았다

[H] 이 판의 보고가 범위 안에 없다
  docs/review/최근.md 는 앞 판 보고와 바이트까지 같다(방금 얼린 2026-09-15-짝잇기실행.md 와 cmp 로 같음)
  - 그래서 이 판의 지시문 대조 표(1a~3c) · 고치기 전 훑기 · 사람칸을 볼 수 없다(검토-원칙 8-1 · 8-2 · 9)
  - 지금 최근.md 의 「정할 것」 은 이 판에서 사람이 이미 정한 물음(규칙으로 적을지)을 아직 묻고 있다.
    check_handoff 는 모양만 봐서 통과한다
  - 검토자 원문을 담으려고 검토 뒤에 보고를 쓰는 순서라면 문제 없다. 다만 푸시 전에 반드시 써야 한다.
    안 쓰고 나가면 막아야 한다

[M] docs/사본-넘김.txt:9 — app-20260912-151859.db 는 「기록」 이라고만 보기 어렵다
  원문 봐둘것.md:3035 은 「15:18 판은 한 달쯤 뒤에 밀려난다 — 그 전에 되돌릴 일이 또 생기면 이 줄을 먼저 본다」 이다.
  이 줄은 되돌리는 자리로 가리키는 문장이다. 넘김 이유의 「알고 둔다고 적었다」 는 원문과 다르다.
  - 같은 칸 끝에 「닫았다」 가 붙어 있고, 지금 그 판으로 되돌리면 09-13 이후가 전부 사라진다.
    그래서 실제로는 기록으로 봐도 된다고 판단한다
  - 다만 stamps_in 이 수동 사본(002352 · 144929 · 164215 …)도 성한 판 30 칸에 넣는다.
    그러면 「한 달」 보다 일찍 밀려나고, 봐둘것 3035 이 없는 파일을 가리키게 된다.
    이 판이 막으려던 바로 그 모양이다
  - 사고 직전 판이라 증거 성격도 있다(030001 은 증거로 지키는 중)
  → 지킬지 넘길지는 사람이 정한다. 넘긴다면 이유를 원문대로 고친다
    (예: 「되돌리는 자리였으나 사고가 닫혀 뜻을 잃음」)

[L] docs/사본-넘김.txt:10 — app-20260914-164215.db 는 넘기는 판단이 맞다. 이유만 약하다
  144929 와 모양은 같다. 하지만 144929 는 덮인 시트 글자가 활동 기록에 안 남아서(BB-h) DB 로만 되돌릴 수 있다.
  164215 이후 한 일(영수증 잇기)은 지운 것도 덮은 것도 없다. 잇기 줄에 detachedAt 을 찍어 앱 안에서 되돌릴 수 있다(7-4).
  넘김 이유는 「되돌리는 자리로 가리킨 문장이 없다」(글자 근거)뿐이다.
  위의 까닭(「앱 안에서 뗄 수 있어 DB 로 되돌릴 일이 없다」)을 적어 두면 다음 사람이 144929 와 헷갈리지 않는다.
  사람에게 올릴 일은 아니다. 나머지 셋(155104 · 171608 · 172758)은 원문도 기록이다 — 문제 없음

[M] docs/인계.md:110 — 「할 것」 머리가 낡았다
  「남은 순서는 22, 그다음 재정(차례 5부터)」 가 그대로다.
  같은 파일의 새 머리(3~6줄)와 재정 절(222줄)은 「차례 1~6 전부 닫힘 · 다음은 회의록이거나 사람이 정한다」 이다.
  새 채팅이 먼저 읽는 파일인데 한 파일 안에서 두 말을 한다(검토-원칙 4)

[L] docs/인계.md:8~10 — 「그 전 갱신」 끝의 「다음은 16번(구매목록)이거나 …」
  이름표로 옛 갱신임을 밝혀서 거짓은 아니다. 다만 바로 위 새 머리(3c)와 다음 줄기를 다르게 말한다.
  한 줄 뒤에 옛 판이라는 표시가 더 있으면 덜 헷갈린다

[L] docs/인계.md:222~232 — 「남은 것 — 한 자리에 모음」 에 순서표 7이 없다
  순서표 7(지난 회차 예산 가져오기)이 빠졌다. 머리 인용은 「7 · 8 은 뒤로 미룬 채」 인데,
  표에는 8(=16번)만 있다. 또 순서표 7·8 의 조건(「6 뒤」 · 「4·5 뒤」)은 이미 채워져 이제는 미룬 것이 아니다.
  지시문 3b 목록에 7이 없어서 따른 것이면 괜찮지만, 「한 자리에 모음」 이라고 부르기에는 모자라다.
  나머지 네 가리킴은 실제 자리와 맞다
  (계좌게이트 5장 100줄 · 봐둘것 BB-i 머리의 「남은 봐 둘 것 둘」 · 인계 285 의 16 · 250 의 순서표 8)

[L] tests/test_stage52.py:54-63 — a02 · a03 이 실제 문서에 기댄다
  - a02 · a03 은 실제 문서(볼글())와 실제 이름 두 개에 기댄다.
    어느 판이 7-5 나 인계에서 그 이름을 지우면 a02 가 규칙과 상관없이 빨개진다
    (「문서의 값을 시험에 박지 않는다」 · 11-3).
    막는 쪽·통과 쪽은 지어낸 글 dict 와 지어낸 이름으로 빠진사본() 을 부르면 문서와 떨어진다
  - a03 은 두 번째 단언도 약하다. 볼글() 의 거르는 조건을 그대로 되묻는 것이라,
    「얼린 보고에 이름이 있어도 안 걸린다」 는 재지 않는다. 지어낸 review 경로를 넣어 재야 뜻이 있다
  - 셋 중 ③(a01 의 본수>0)과 넘김 이유(a04)는 충분하다
  - 안 재는 것 둘. 판단에 따라 봐 둘 것이다
    · 같은 이름이 지키는판과 넘김에 함께 있는 모순
    · 문서에서 사라졌는데 넘김에 남은 줄(넘김 목록이 자라기만 한다)

[L] 검사가 못 보는 범위 — 11-2 문단에 한계 하나만 적혀 있다
  tests/test_stage52.py:17 · 21
  - 꼴이 .db 까지 붙은 이름만 잡는다. 「144929 판」 · uploads-/vapid- 이름으로 가리키면 안 걸린다
  - docs/ 아래 .md 만 본다. docs/*.txt 와 scripts 독스트링은 안 본다. 지금은 해당하는 줄이 없다(git grep)
  - docs/review/ 전체를 빼서 살아 있는 최근.md 도 빠진다.
    사본 이름이 처음 적히는 곳은 대개 보고의 사람칸이다.
    지금은 인계가 같은 이름을 옮겨 적어서 잡히지만, 옮겨 적지 않는 판이 오면 샌다
  - CLAUDE.md:3309-3312 는 「한 번 넘긴 이름은 다시 가리켜도 안 걸린다」 만 적었다.
    위 셋도 적어 두면 다음 사람이 믿는 범위가 맞아진다

말이 서로 다른가 · check_counts
  - CLAUDE.md:3302-3312 와 scripts/backup.py:95-96 은 같은 규칙이다.
    backup.py 는 한 줄 요약 뒤 「여기 다시 적지 않는다」 로 11-2 를 가리킨다. 다른 말 없음
  - 까닭 문자열 규칙도 두 곳이 같다
  - check_counts 에 걸릴 말 없음(「두 번」 · 「두 곳」 뿐이다). 글검사 전부 exit 0
  - 옛말-넘김.txt:48 의 얼린 판 한 줄은 앞 줄들과 같은 꼴 — 문제 없음

실명 · 운영 자료: 이 범위에 새로 들어간 실명·계좌·연락처 없음

사람에게 묻는 것
  1. app-20260912-151859.db(09-13 사고 직전 판)를 지키는판에 넣을지, 기록으로 넘길지.
     봐둘것 3035 은 「되돌릴 일이 생기면 이 줄을 본다」 고 가리키고, 수동 사본까지 30 칸에 들어가 한 달보다 일찍 밀려난다
  2. 인계 「남은 것」 표에 순서표 7(지난 회차 예산 가져오기)을 넣을지. 지시문 3b 목록에는 없었다
━━━ 검토자 여기까지 ━━━
```

이 창의 처리: [H] 는 이 보고 · [M] 둘과 [L] 넷(164215 이유 · 순서표 7 · 시험 떼기 · 못 보는 범위)은 f2e10e6 · 인계 8~10 과 시험이 안 재는 둘은 봐 둘 것으로 두었다 · 묻는 것 1 은 사람칸 「정할 것」 · 2 는 표에 넣었다.

## 7-2. 다시 보기 (5dc28f8..f2e10e6)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 범위 5dc28f8..f2e10e6 · 커밋 1개 · 파일 4개

무엇을 봤나
  git diff 5dc28f8..f2e10e6 전체
  pytest tests/test_stage52.py → 4 passed
  scripts/글검사.py → 7 줄 전부 exit 0
  새 표 줄이 가리키는 곳 확인: 봐둘것 BA-c(3191) · 사-ㄴ(3201)

첫 검토에서 짚은 것 — 고친 모양
  [M] 151859 — 닫힘
    docs/사본-넘김.txt:9 의 이유가 원문과 맞는다
    「되돌리는 자리였으나 사고가 닫혀 뜻을 잃음」 과 「지킬지는 사람에게 올림」 이 적혔다
    넘김.인용했나 에 걸리는 말 없음
  [L] 164215 — 닫힘
    :10 에 「앱에서 뗄 수 있어 DB 로 되돌릴 일이 없음」 과 짝잇기 사본과 다른 까닭이 붙었다
  [M] 인계 「할 것」 머리 — 닫힘
    docs/인계.md:110 의 앞머리가 새 머리와 같은 말이다
    옛 머리는 「그 전 머리:」 로 뒤에 붙었다
    옛 「남은 순서는 22, 그다음 재정(차례 5부터)」 는 빠졌다
  [L] 순서표 7 — 닫힘
    :232 에 한 줄이 더해졌고 가리키는 곳(순서표 7 · BA-c)이 실재한다
  [L] a02 · a03 — 닫힘
    tests/test_stage52.py:53-72 가 지어낸 글과 tmp 경로로 잰다
    고장을 머리로 심어 봤다
    · 빠진사본 이 지키는판을 안 보면 → a02 첫 단언이 빨개진다
    · 넘김을 안 보면 → a03 첫 단언이 빨개진다
    · review 거르기를 빼면 → a03 둘째 단언이 000003 을 보고 빨개진다
    · 살아 있는 글을 놓치면 → 000004 가 없어 빨개진다
    실제 문서 이름은 이제 시험에 박혀 있지 않다
  [L] 11-2 못 보는 범위 — 닫힘
    CLAUDE.md:3313-3315 가 셋을 적었다(.db 이름만 · .md 만 · 최근.md 제외)
    대응(인계나 봐둘것에도 적는다)도 함께 적혔다
    backup.py 주석은 11-2 를 가리키기만 해서 두 곳이 다른 말을 하지 않는다

이번에 새로 걸린 것
  [H] 없음 · [M] 없음

  [L] docs/사본-넘김.txt:9 — 「사람이 정할 것으로 올렸다」 가 아직 사실이 아니다
    이 판 보고가 아직 없어서 그렇다
    보고 사람칸 「정할 것」 에 151859 를 반드시 넣어야 이 줄이 참이 된다

  [L] 151859 는 결정을 기다리는 동안 지키는판 밖에 있다
    stamps_in 은 수동 사본도 성한 판 30 칸에 센다
    그래서 「한 달쯤」 보다 일찍 밀려날 수 있다
    정할 것에 올릴 때 「안 정하면 곧 지워진다」 는 시한을 함께 적는 편이 낫다

  안 고치기로 한 둘은 이유가 맞다고 본다 — 봐 둘 것으로 남는다
    · 인계 8~10 의 옛 갱신 줄
    · 지키는판과 넘김의 모순 · 문서에서 사라졌는데 넘김에 남은 줄

사람에게 묻는 것
  1. app-20260912-151859.db(09-13 사고 직전 판)를 지키는판에 넣을지
     안 넣으면 성한 판 30 칸이 차는 날 지워진다
━━━ 검토자 여기까지 ━━━
```

이 창의 처리: 두 [L] 은 이 보고 사람칸 「정할 것」 에 그 사본과 시한을 실어 닫았다.

# 8. 바뀐 파일 (2d7048c..이 보고)

CLAUDE.md · docs/review/2026-09-15-짝잇기실행.md · docs/review/최근.md · docs/사본-넘김.txt · docs/옛말-넘김.txt · docs/인계.md · scripts/backup.py · tests/test_stage52.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1a | 11-2 백업 절에 규칙 · 한 판에서 같이 | **했다** |
| 1b | 까닭 문자열 규칙은 그대로 · 자동 검사를 묻지 않음 | **했다** — 묻지 않았다(2 에서 시험은 따로 만들었다) |
| 1c | backup.py 머리 주석이 같은 규칙을 가리킴 · 다른 말 안 적음 | **했다** — 11-2 를 가리키기만 |
| 2a | 기존 검사로 되는지 코드를 읽고 판단 | **했다** — 안 된다(1장) |
| 2b | 되면 만든다 · 002352 · 144929 를 빼고 심어 빨개짐 | **다르게 했다** — 문서의 이름을 지키는판과만 견주면 기록으로 적힌 이름 다섯이 걸려 넘김 목록을 더했다. 심기는 backup.py 를 실제로 고쳐 봤다 |
| 3a | 순서표에 「차례 1~6 전부 닫힘 · 30건 운영 반영」 | **했다** — 재정 절 머리 |
| 3b | 남은 것 한 자리 | **다르게 했다** — 지시문 넷에 순서표 7 을 더했다(검토) |
| 3c | 다음 줄기는 회의록이거나 사람이 정함을 인계 머리에 | **했다** — 맨 위 갱신과 「할 것」 머리 |
| 가~마 | 수용 기준 | **했다** — 2장 |
| 끝줄 | 운영 자료 안 건드림 · 앱 안 고치면 재시작 안 함 | **했다** |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
