# 검토용 보고

작성 시각: 2026-09-06 23:45 (KST)
커밋: 2ecb77f (브랜치 `main` · 직전은 `3bd5ea4` · 이 줄을 적는 커밋만 그 뒤입니다)
작업: UI 개편 — A(CLAUDE.md 개정) + 단계 1(껍데기 · 설정 · 목업)

---

## A. CLAUDE.md 개정 (커밋 2ecb77f · 코드보다 먼저)

A-1~A-18 전부 반영. 함께 한 것:

- `test_w_14`(test_calendar.py)가 10장의 「보드 N항목 · 달력 N항목」 숫자를
  요구하던 시험이라, A-15(숫자를 문서에 박지 않는다)에 맞춰 **방향을 뒤집었다**
  — 수가 다시 박히지 않는가 + 기준 문장이 남아 있는가. 문서를 재는 시험이라
  문서와 같은 커밋에 넣었다.
- 11-3 의 `Co-Authored-By` 모델명을 실제(Fable 5)에 맞췄다 — 옛말이었다.
- 「준비 단계 보드」 제목·목업 표를 「수련회 준비 — 보드」 로, 15장 「HTML 3개」
  를 4개로 — A-2 의 「"준비 단계" 라는 이름은 쓰지 않는다」 를 따라간 옛말 정리.

**지시문과 CLAUDE.md 가 어긋난 곳 (CLAUDE.md 를 따랐다):**

| 어긋남 | 어떻게 했나 |
|---|---|
| A-15 는 「보드 61항목 · 달력 62항목」 을 지우라는데 실제 문서는 69·70 이었다 | 그 문장을 지웠으므로 결과는 같다 |
| A-4 「하위 = 13.5px」 — 4-0 글자 눈금은 혼자 뜻을 지는 글자 하한 14px, `test_typescale` 이 숫자 크기를 막는다 | 하위를 `--fz-md`(14) 로 두고 위계는 색·굵기·들여쓰기로 갈랐다 |
| A-2 「상단 탭 4개 문장…은 그대로 둔다」 — 새 구조와 나란히 두면 3장이 두 말을 한다 | 탭 4개 문장과 코드 블록을 **인용(역사)** 으로 남기고, 나눈 이유 문단은 그대로 두었다 |

## 단계 1-0. 먼저 센 것 (운영 DB 사본 — 코드 수정 전)

| 센 것 | 값 |
|---|---|
| /files 파일 자산 | **2건** (버전 행 3) |
| 버전 2 이상 | 1건 (「수련회 포스터」 · png 2) |
| 승인 상태 | 승인 1 · 검토요청 1 |
| 업무(task_id)에 안 붙은 것 | 1건 (「1일차 집회 큐시트」 xlsx) |
| /checklists | 목록 5 · 항목 30 |
| ScheduleItem | **136행** (ScheduleDay 4) — 표·행은 남겼다 |
| 옛 /tasks 가 읽는 모델 | **`Task` 표다. `TaskRun` 이 아니다** (tasks 175행 · task_runs 208행) |

> **⚠ /files 에 파일이 2건 있다.** 지시문 1-0 대로 여기 보고한다 — **단계 4 에서
> 그 2건을 TaskAttachment 로 옮길지 사람이 정한다.** 특히 「1일차 집회 큐시트」 는
> 업무에 안 붙어 있어 옮길 곳이 없다. 단계 1 자체는 /files 를 건드리지 않아 진행했다.
>
> **⚠ 옛 /tasks 는 Task 표를 읽는다** — 단계 3 의 301 은 `/tasks/{id}` → `/tasks`
> (id 가 TaskRun.id 가 아니므로) 가 된다. 지시문 1-0 이 예고한 그 갈림이다.

## 단계 1. 한 것

1. **목업** — `확정-사이드바-목업.html` → `docs/mockups/retreat-shell.html`.
   - anonymize.py 가 **6곳**을 바꿨다 (같은 실명 5곳 + 1곳). 아바타 머리글자도 가명에 맞췄다.
   - **대응표에 없는 이름 하나가 남았다** — 가명으로도 실명 표기로도 표에 없어
     실명일 위험이 있어 목업에서 가명 `정하윤` 으로 바꿨다. **실명이 맞으면
     대응표에 넣어야 한다** — 지금은 검사가 그 이름을 영영 못 본다 (11-2 의
     「볼 목록이 불완전하다」 그 자리다). 원문은 저장소 밖 원본 파일에 있다.
   - 그룹 제목 `<div class="g">` → `<a class="g" href>` (지시문 0장 23-1 = CLAUDE.md 3장),
     남은 지출 붉은색은 두고 주석 한 줄, **사이드바의 `작업 파일` 항목은 뺐다**
     — 지시문 0장 20(= CLAUDE.md 3장·4-9) 확정과 어긋난 채 시각 스펙으로 남으면 안 된다 (지시문에 없던
     판단이므로 여기 적는다).
2. **토큰·껍데기** — retreat.css 에 accent(`#4F46E5` · `--link` 가 이를 가리켜
   옛 선택 파랑 흡수) · 카드 · 배지 4종 토큰. retreat_base.html 사이드바를 3장
   구조 · 4-0 B안으로 (그룹=첫 하위 링크 · 선택은 하위에만 · 알림 배지 ·
   사용자 카드는 부서를 **키로** 찾음 — `templating._side_dept_name`).
   ≥1280 펼침 고정 / 그 아래 접힘+호버 (`@media` 로 sidepin 을 1280 위에서만).
   본문 max-width 제거(`.wiz .pane`). 아직 없는 화면은 옛 주소로 — 홈→/board ·
   목록→옛 /tasks · 알림→옛 /notifications. `active_draft` 가 있으면 「회차 준비」
   홑 항목을 남겼다(부서 리더가 /draft 로 갈 길이 지시문 구조에 없어서 — 판단).
   푸시 구독 UI 는 app.js 에서 **push.js 로 그대로 떼어** 옛 알림함과 새 설정이
   한 벌을 쓴다. setup.html 의 자체 사이드바도 같은 구조로 맞췄다.
3. **설정 여섯 탭** — `/settings`(내 정보) · `/settings/retreats`(+상세) ·
   `/settings/departments` · `/admin/users`(주소 그대로, 탭 줄만) ·
   `/settings/library`(GET 이 옮겨 가고 `/library` → 301) · `/settings/checkup`
   (/more 의 위험 점검 + 알림 미리보기 + 자가진단·백업 마지막·변경 이력).
   POST 엔드포인트는 전부 그대로. `app/domain/period.py` 의 `is_over` 를 만들어
   회차 상세 「남은 지출」 이 쓴다 (4-10·5-6 판정을 모으는 것은 단계 2).
4. **옛 화면** — `/schedule` 라우트·템플릿 삭제 → `/live/staff` 301 (표는 남음).
   `/more` 삭제. `/checklists` 를 retreat_base 로 (폼 action·권한 불변).
   옛 껍데기(base.html)의 일정표 링크를 `/live/staff` 로, 더보기를 설정으로.

## 수용 기준

| # | 기준 | 결과 | 근거 |
|---|---|---|---|
| 1 | 토큰이 4-0 표와 같다 (문서에서 읽어 비교) | 통과 | `test_s01_배지_토큰이_문서의_표와_같다` · `test_s02` — 문서 표를 파싱해 CSS var 를 끝까지 풀어 견준다 |
| 2 | 사이드바 B안 · 그룹이 `<a>` · 선택은 하위에만 | 통과 | `test_s03` `test_s04` `test_s06` + drawer.js 「그룹 제목 3개가 링크이고 활성 표시가 없다」 |
| 3 | 본문 max-width 없음 · 1920px 지표 4칸 | 통과 | `test_s07` + 1920×950 실측 스크린샷 (현황 4칸이 창 끝까지) |
| 4 | 새 회차 만들기·라이브러리·작업 파일이 사이드바에 없다 | 통과 | `test_s05` |
| 5 | ≥1280 펼침 고정 · ≤1279 접힘+호버 · localStorage | 통과 | `test_s08` + 브라우저 실측 (1440 펼침·본문 밀림 / 1000 접힘·호버 열림·본문 그대로) |
| 6 | 사용자 카드 이름·역할·부서(키) | 통과 | `test_s09` + 실측 (카드에 이름·역할·부서) |
| 7 | 배지 = 안 읽은 알림 + 대기 요청, 0이면 없음 | 통과 | `test_s09` (`{% if nav_badge %}`) |
| 8 | drawer.js 보드·달력 전부 통과 | 통과 | **보드 73/73 · 달력 73/73** (1440×900, dev 서버). 사이드바 항목을 1280 기준으로 고쳐 썼다 |
| 9 | 설정 여섯 탭, 앞 둘 누구나·넷 admin | 통과 | `test_s10` (dept_lead 로 넷이 403/404) |
| 10 | /schedule 301 · /more 없음 · /library 301 · /checklists retreat_base | 통과 | `test_s11` · `test_옛_일정표_주소는…`(seed) · `test_옛_일정표는_지워졌고…`(web_flows) |
| 11 | retreat-shell.html 실명 없음 | 통과 | check_names(커밋 직전 전체): **실명 37개를 파일 204개에서 봤고 걸림 0 · 아직 안 고친 곳 0곳 · 대응표에 없는 담당자 표기 0개** (읽은 수 0 아님). 참고 — 이 보고 초안에 그 이름을 적었다가 검사에 걸려 지웠다 |
| 12 | 단계 0 숫자가 보고에 있다 | 통과 | 위 표 |

## 시험

- pytest (옵션 없이 · 검토자 반영 뒤 마지막 실행): `957 passed in 472.91s (0:07:52)`
- 고친 시험: `test_w_14`(방향 뒤집음) · `test_ts_02/04/08/09`(사이드바 급 구조) ·
  `test_02b`(사이드바 목록) · `test_01`·`test_t_01/02/03`(달력 쪽 사이드바 —
  navgroup/navlabel 구조를 재던 것을 B안으로) · seed/web_flows/phase2 의
  /schedule·/more 경로. 새 시험: `tests/test_shell.py` 12개.
- 흐림색(`--ink-3`) 새 자리 4곳을 `흐려도_되는곳` 에 이름으로 넣었다
  (`.stabs .adm` `.kpi .v small` `.setting th` `.tickrow.done .ticklabel`) —
  혼자 뜻을 지는 두 곳(`.sidefoot small` · `.fld .hint`)은 보조색으로 올렸다.

## 바뀐 파일

새 파일 — `app/domain/period.py` · `app/static/js/push.js` ·
`app/templates/partials/settings_tabs.html` · `settings_retreats.html` ·
`settings_retreat_detail.html` · `settings_departments.html` ·
`settings_checkup.html` · `docs/mockups/retreat-shell.html` · `tests/test_shell.py`

지운 파일 — `app/templates/schedule.html` · `app/templates/more.html`

고친 파일 — `CLAUDE.md` · `app/routers/{admin_users,checklists,dashboard,library,live,schedule,settings}.py` ·
`app/templating.py` · `app/static/css/retreat.css` · `app/static/js/{app,sidenav}.js` ·
`app/templates/{admin_users,base,checklists,library,retreat_base,settings,setup}.html` ·
`docs/checks/drawer.js` · `docs/review/최근.md` ·
`tests/test_{phase2_flows,seed,typescale,ui_shift,web_flows}.py`

## 검토자 판정과 반영

검토자(서브에이전트)가 커밋 전에 봤고 **막아야 함 없음 · 고쳐야 함 9 · 봐 둘 것 7** 을
냈다. 원문은 채팅 덩어리에 그대로 있다. 고쳐야 함 9건 전부 + 봐 둘 것 중 둘을
커밋 전에 반영했다:

1. CLAUDE.md 4-0 의 「13.5px」 → 눈금의 목록 단(14px)으로 (목업 4곳도 함께)
2. 「0장 N」 참조 8곳 → CLAUDE.md 장 번호로 (지시문은 저장소에 남지 않는다)
3. retreat.css 의 옛 파랑 rgba 7곳 → accent rgb(79,70,229) (+ test_board_web:722)
4. test_calendar 의 오늘 칸 대비 → 박힌 색이 아니라 `_token("--accent")` 로
5. 옛 목업 4개의 #2383E2 → #4F46E5 로 흡수 (test_ui_shift 도 「옛 파랑이 없다」 로 뒤집음)
6. sidenav.js — 좁은 화면에서 토글이 늘 '1' 만 쓰던 버그: 토글은 클래스 원값
   (`pinnedClass`)을 뒤집고, peek 걷기는 ≥1280 에서만
7. period.py 독스트링 — 「부른다」 → 「불러야 한다(단계 2에서 모은다)」 + 현황 명시
8. 회차 상세 스모크 시험(test_s10 에 200/404) 추가
9. CLAUDE.md 6-2·README·현황-브리핑·setup.js 의 옛 `/library`·`/schedule`·
   「준비 단계」, library POST 리다이렉트의 301 이중 홉
10. (봐 둘 것 14) 배지 렌더링 시험 `test_s09b` — 0건이면 없음·1건이면 «1»
11. (봐 둘 것 16) test_s06 의 죽은 줄 제거

**반영하지 않고 사람에게 넘기는 것** — 검토자 질문 A~F 그대로 (덩어리의 검토자
원문 참조): 대응표에 넣을지(A) · 배지 대기색 3.3:1 을 예외로 확정할지(C) ·
/reviews 응답 경로 공백(E) · 구설계 사용자 POST 를 지울 단계(F) 등.

## 판단이 들어간 자리 (검토자가 봐 줬으면 하는 곳)

1. **목업에서 `작업 파일` 항목 제거 + 대응표에 없던 이름을 가명 `정하윤` 으로** —
   지시문의 세 가지 목업 편집에 없던 둘. 이유는 위 단계 1-1. (그 이름 자체는
   여기 적지 않는다 — 11-2. 원본 목업 파일과 diff 로 확인할 수 있다)
2. **`회차 준비`(초안) 홑 항목 유지** — 3장 구조에 없지만 빼면 부서 리더가
   /draft 에 갈 길이 없다 (6-7). `active_draft` 있을 때만 뜬다.
3. **설정 옛 POST(`/users/create` 등 구설계 사용자 관리)** 는 엔드포인트만 남고
   화면에서 안 보인다 — 사용자 탭은 /admin/users(4-12 키 기반)다.
4. **회차 관리 탭을 누구나 보게 열었다** (A-11 「앞 둘은 누구나」) — 비 admin
   에게는 읽기 전용으로 그리고, 만들기·수정 폼은 admin 에게만. POST 는 원래
   admin 게이트다.
5. `/schedule?retreat_id=99999` 로 재던 404 시험을 `/tasks?retreat_id=99999` 로
   옮겼다 — 301 이 쿼리를 버리므로 옛 자리로는 그 오류 화면을 잴 수 없다.
