사람이 할 것
노트북에서 직접: 없음 — 이 판은 읽기만 했다(4장)
관리자 권한: 없음 — `app/` 을 안 고쳐 재시작하지 않았다(4장)
정할 것: 총무님이 `data/업무대조-확인용.real.md` 에 표시(짝 확인 55줄 · 노션만 151줄) · 다음 판이 무엇을 반영할지 갈래 ㄱ·ㄴ·ㄷ(5장) · 봐둘것 BC-b 의 넷(확인용 파일을 건네는 길 · included 6 을 뺄지 · 덤 값의 근거 · 덤이 제목을 이기는 성질)
막혀서 못 한 것: 화면 점검(drawer.js · phone.js) — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다. 화면을 안 고친 판이라 급하지 않다(4장)

작성 시각: 2026-09-16 02:20 KST
커밋: e0f99b0 (앞 판 얼림) · 8f012d4 (규칙 · 시험) · b225688 (보고 · 문서) · 검토 뒤 고침은 그다음 커밋
작업: 업무 대조 1판 — 짝짓기를 확정하고 애매한 것을 사람 앞에 낸다

---

**값은 여기 없다.** 제목 · 날짜 · 담당은 저장소 밖 `data/업무대조.real.md`(등급별 상세)와
`data/업무대조-확인용.real.md`(총무님이 체크할 표)에만 있다. 여기에는 건수 · 규칙 · 문턱만 적는다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 없음 — 새 파일 둘(`scripts/노션업무짝짓기.py` · `tests/test_stage55.py`)이고 앱 코드는 안 건드린다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면은 안 고친다 |
| 그 값을 세어 둔 문서 | 인계 1)(1판의 네 갈래 수 · 업무 ㄱ2 줄) · CLAUDE.md 14장 표(회의록 id 줄 옆) · 1판 얼린 보고(안 고친다) |

# 1. 짝짓기 규칙을 다시 짰다 (1a–1e)

**규칙과 문턱은 저장소 안에 둔다** — `scripts/노션업무짝짓기.py`. 1판은 짝짓기 코드가 `data/` 에만 있어 다음 판이 재현할 수 없었다(1판 검토 [M] 6). 값을 읽어 파일로 내는 실행기만 저장소 밖(`data/업무대조.real.py`)이다.

| 1판(`data/노션대조.real.py`) | 이번 |
|---|---|
| ① 제목이 같은 것부터 순서대로 ② 남은 것 중 닮음이 문턱을 넘는 것을 **가장 닮은 순으로** 1:1 | **모든 짝의 점수를 다 매긴 뒤 높은 것부터 확정** — 앞에서 먹은 짝이 뒤의 더 맞는 짝을 안 밀어낸다(`test55_b01`). **점수가 정확히 같을 때만 줄 번호가 이긴다**(검토 [M] 4 — 「순서가 답을 전혀 안 바꾼다」 고 적지 않는다) |
| 제목 닮음만 | 제목 닮음 + **구분 · 담당 · 시작일이 맞으면 더함**(덤) |
| 구분 없는 34줄을 통째로 뺌 | **D-주차 표지 24 만 빼고 10줄을 후보에** |
| 같은 제목이 노션에 여럿이면 앞줄이 가져감 | **날짜 · 담당이 가까운 줄이 이김**(덤이 가른다 · `test55_c01`) |

**문턱** — 제목 닮음 `0.50` 위만 후보, 등급 A 는 제목 `0.93` **이고 어긋난 칸이 없을 때**, 등급 B 는 총점 `0.70`. **잰 값이 아니라 고른 값이다**(2026 여름 자료 몇 건을 눈으로 보고 정함 · 봐둘것 BC-b) — 값은 그 파일 한 곳에 있다.

**총점을 1 에서 자르지 않는다** — 자르면 제목이 똑같은 줄이 여럿일 때 덤이 지워져 날짜 · 담당이 가른 결과가 사라진다(시험이 먼저 잡았다).

## 1e. 네 갈래가 어떻게 달라졌나 (건수만)

| 갈래 | 1판 방식 | 이번 규칙 |
|---|---|---|
| 노션만 | 143 | 151 |
| 앱만 | 22 | 20 |
| 둘 다 · 제목 같음 | 50 | 50 |
| 둘 다 · 제목 다름 | 30 | 32 |
| 짝 전체 | 80 | 82 |

**노션만이 는 까닭은 후보가 늘었기 때문이다** — 구분 없는 10줄이 들어와 후보가 233 이 됐고(1판은 223), 짝이 80 → 82 로 늘어 233 − 82 = 151 이다. 앱 run 102 = 짝 82 + 앱만 20, 노션 후보 233 = 짝 82 + 노션만 151(표지 24 는 갈래 밖).

# 2. 등급 A · B · C (2a–2d)

| 등급 | 뜻 | 건수 |
|---|---|---|
| A | 제목이 사실상 같고 **구분 · 담당 · 시작일이 하나도 안 어긋남** — 자동으로 믿는다 | 27 |
| B | 짝일 만하나 사람이 봐야 한다(제목이 같아도 칸이 어긋나면 여기) | 54 |
| C | 근거가 약하다 — 노션만/앱만으로 두는 편이 나을 수 있다 | 1 |

- **어긋난 짝 45** — 구분 33 · 담당 13 · 시작일(이레 넘게) 6. 한쪽 값이 비면 어긋남으로 안 센다
- 구분 없는 후보 10 중 짝지은 것 1
- 앱 run 102 를 그대로 셌다 — 이번 회차에 빼 둔 run(included 꺼짐) 6 이 섞여 있고 그중 5 가 「앱만 20」 안, 1 이 짝 안이다. 빼고 세면 앱 96 · 앱만 14(짝 82 · 등급은 그대로) — 어느 쪽으로 할지는 봐둘것 BC-b
- **B · C 상세는 저장소 밖** `data/업무대조.real.md` — 등급 · 점수 · 양쪽 제목과 구분 · 담당 · 시작일을 나란히 놓았다. 같은 파일에 A 목록 · 노션만 · 앱만도 있다

# 3. 총무님 확인용 표 (3a · 3b)

`data/업무대조-확인용.real.md` — 저장소 밖. **표시만 하면 되는 두 표**다.

| 표 | 줄 | 체크 칸 |
|---|---|---|
| 1. 이 둘이 같은 업무인가 (등급 B · C) | 55 | 「맞다 / 아니다 / 모르겠다」 · 비우면 다음 판이 「모르겠다」 로 본다. 어긋난 칸이 함께 적힌다 |
| 2. 노션에만 있는 업무 | 151 | 「넣음 / 안 넣음 / 버림」 과 「(안 넣음이면) 어느 업무에 묶였나」 |

`.xlsx` 가 아니라 `.md` 로 냈다 — 총무님이 열어 글자만 적으면 되고, 다음 판이 다시 읽기도 쉽다(엑셀은 칸마다 옮겨 다녀야 하고 이 표는 셈이 없다).

# 4. 이번 판이 안 한 것

- **앱에 아무것도 반영하지 않았다** — 코드 diff 에 `app/` 이 없다(7장 파일 목록). 운영 DB 는 `mode=ro` 로 읽기만, 노션은 이번 판에서 아예 안 열었다(1판이 옮겨 적은 tsv 를 썼다)
- **재시작 안 함** — `app/` · `templates/` · `static/` 변경 없음. 운영 서버 PID 19156 그대로
- **drawer.js · phone.js 못 돌림** — 로그인이 필요하다(11-2). 화면을 안 고친 판이라 급하지 않다

# 5. 다음 판 갈래

총무님 표시를 받은 뒤 무엇을 반영할지.

| 갈래 | 무엇 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **ㄱ. 표시된 것만 반영** | 「맞다」 는 잇고 「아니다」 는 갈라 두고 「모르겠다」 · 빈 칸은 그대로 둔다 | 사람이 본 것만 굳는다 | 표시가 덜 오면 다음 판이 또 기다린다 |
| **ㄴ. A 는 자동 확정 · B · C 만 기다림** | 등급 A 27 은 바로 잇고 55줄의 표시만 기다린다 | 3분의 1 이 먼저 닫힌다 | A 를 사람이 본 적은 없다. **A 는 제목이 사실상 같고 구분 · 담당 · 시작일도 안 어긋난 것만**이라 앞 판(제목만 보던 51)보다 좁지만, 제목이 같아도 다른 업무일 수 있다(1판이 적어 둔 한계) |
| **ㄷ. 표시가 올 때까지 아무것도 안 함** | 기다린다 | 틀린 반영이 0 | 회차가 지나도 노션만 151 이 그대로 남는다 |

함께 정할 것 — 노션만 줄에 run 을 새로 세울지(ㄱ2 의 그 절반) · 앱 run 에 노션 id 를 담을 칸을 어디에 둘지 · 「원본에 없음」 을 칸으로 둘지 상태로 둘지(인계 1) 의 옛 미정).

# 6. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 전역 방식으로 바뀌고 옛 방식과 건수 비교 | O | 1장 표 · 1e 표 |
| 나) 구분 없는 34줄 중 10줄이 후보 | O | 1e 밑줄 · `test55_a01` |
| 다) 등급 A/B/C 건수 · B/C 상세가 저장소 밖 | O | 2장 |
| 라) 총무님이 체크만 하면 되는 파일 | O | 3장 |
| 마) 앱에 반영 없음(`app/` diff 없음) | O | 4장 · 7장 파일 목록 |
| 바) pytest 옵션 없이 · 글검사 7/7 | 마지막 pytest 뒤 채움 | 7장 |
| 사) 검토자 원문 | O — 첫 검토 · 두 번째 검토 둘 다 | 8장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 1a | 했다 | 1판 코드를 열어 그 규칙을 표로 옮겼다 |
| 1b | 했다 | 점수를 다 매긴 뒤 높은 것부터 — `test55_b01` · `b02` |
| 1c | 했다 | 표지 24 만 뺀다 — `test55_a01` |
| 1d | 했다 | 덤(구분 · 담당 · 날짜)이 가른다 — `test55_c01` · `c02` |
| 1e | 했다 | 1e 표 |
| 2a | 했다 | 2장 — A 27(검토 [H] 1 뒤 어긋남을 넣어 좁힘) |
| 2b | 했다 | 2장 — B 54 |
| 2c | 했다 | 2장 — C 1 |
| 2d | 했다 | 2장 · `data/업무대조.real.md` |
| 3a | 다르게 했다 | `.xlsx` 가 아니라 `.md` 로 냈다(지시문이 「나 `.md`」 로 열어 뒀다) — 까닭은 3장 |
| 3b | 했다 | 3장 표 2 |
| 4a | 했다 | 고장을 심어 재는 시험 둘(`b02` · `c02`)과 검토가 「안 재진다」 고 짚은 넷(`e01`~`e04`) · 이 판은 DB 에 안 쓴다 |
| 4b | 했다 | 규칙 · 문턱을 저장소 안 `scripts/노션업무짝짓기.py` 에 뒀다(1판 검토 지적) — 저장소 밖보다 한 걸음 더 |
| 5a | 했다 | 인계 맨 위와 1) |
| 5b | 했다 | 5장 · 인계 1) |
| 운영 · 노션 읽기만 · 재시작 안 함 | 했다 | 4장 |
| 마무리는 11-3 대로 | 했다 | 7 · 8장 |

# 7. 검사와 시험

- 커밋 전 `git status --short`: 코드 · 시험 · 문서만 — `data/` 안의 것은 안 잡혔다(값 파일 · 실행기는 gitignore)
- 글검사: 첫 커밋 전 7/7(새 파일이 추적 전이라 이름 검사가 한 번 걸려, 추적한 뒤 저장소 밖 실행기 한 줄을 `docs/이름-넘김.txt` 에) · 검토 뒤 고침 커밋 전 7/7 · 두 번째 검토 뒤 고침 커밋 전 7/7
- 새 시험: `tests/test_stage55.py` 15 통과(첫 판 8 → 검토가 짚은 자리 넷과 A 규칙 둘 → 두 번째 검토 뒤 어긋난수 · 손댄줄 둘)
- 바뀐 파일(`git diff --name-only 63be095..HEAD`): `CLAUDE.md` · `docs/review/2026-09-16-회의록id.md` · `docs/review/최근.md` · `docs/봐둘것.md` · `docs/이름-넘김.txt` · `docs/인계.md` · `scripts/노션업무짝짓기.py` · `tests/test_stage55.py`
- 보고를 다 채운 뒤 돌린 pytest: <!-- 최종 -->

# 8. 검토자 원문

## 8-1. 첫 검토 (범위 63be095..b225688)

```
━━━ 검토자 여기부터 ━━━ 범위 63be095..b225688 · 커밋 2개 · 파일 7개

먼저 재어 본 것 (값은 안 옮기고 건수만)
  규칙 모듈로 직접 다시 셌다 — 1e 표의 수가 전부 그대로 나온다.
    노션 줄 257(표지 24 · 후보 233) · 앱 run 102
    짝 82 · 노션만 151 · 앱만 20 · 제목 같음 50 · 제목 다름 32
    A 51 · B 30 · C 1 · 구분 없는 후보 10 중 짝 1 · 확인용 B·C 31줄
  1판 수(143 · 22 · 50 · 30 · 짝 80)도 얼린 1판 보고와 맞다.
  입력(노션·앱)을 통째로 뒤집어 다시 돌려도 짝 82 가 하나도 안 달라졌다.
  글검사 7/7 초록 · pytest tests/test_stage55.py 8 passed (내가 돌린 것).
  앱/템플릿/정적 변경 0 · 보고에 값(제목·날짜·사람) 샌 곳 없음 · 사람칸 다섯 줄 제 순서.
  얼린 판(2026-09-16-회의록id.md)은 앞 판 최근.md 와 한 글자도 안 다르다.


[H] 1. 등급 A 의 뜻이 코드와 글이 서로 다르다 — 51 중 24 가 실제로 어긋난다

  scripts/노션업무짝짓기.py:35
      등급A = 0.93  # 제목이 사실상 같고 나머지도 어긋나지 않는다
  scripts/노션업무짝짓기.py:99
      if 제목 >= 등급A and 총점 >= 등급A:
  총점 = 바탕 + 덤 이고 덤 >= 0 이라 (:86) 제목 >= 0.93 이면 총점 >= 0.93 이
  늘 참이다. 즉 뒤 조건은 아무것도 안 막는다 — A 는 오직 제목 닮음으로만 난다.
  구분·담당·날짜가 얼마나 어긋나든 A 가 된다.

  규칙 모듈로 A 51 을 재 봤다:
      어긋난 짝 24 (구분 15 · 담당 8 · 시작일 7일 초과 4)
      B 30 중 20 · C 1 중 1
  A 51 은 사실상 「제목 같음 50」 그 집합인데, 얼린 1판 보고
  docs/review/2026-09-15-노션대조1판.md:112 가 이미
  「제목 같음 50 도 틀릴 수 있다 — 노션에 같은 제목이 여러 줄이면…」 이라고
  적어 두었다. 1판이 의심하라고 남긴 그 집합이 이번 판에서 「자동으로 믿는다」 로
  올라갔다.

  글이 세 자리에서 갈린다:
      docs/review/최근.md:55  A = 「제목이 사실상 같고 나머지도 안 어긋남」
      scripts/노션업무짝짓기.py:35  같은 말
      data/업무대조.real.py:57  「## 등급 A — 자동으로 믿는 짝 (제목만)」
  저장소 밖 실행기만 사실대로(제목만) 적었고, 사람이 읽는 보고가 반대로 적었다.

  이것이 왜 막아야 할 무게인가 — 보고 5장의 갈래 ㄴ(「A 51 은 바로 잇는다」)을
  총무님이 그 문장을 근거로 고른다. 잃는 것 칸에 「A 를 사람이 본 적은 없다」 는
  적혀 있지만, 「24/51 이 구분·담당·날짜에서 어긋난다」 는 수가 없다.
  그 수가 보고에 없으면 ㄴ 은 고를 수 있는 선택지가 아니다.

  tests/test_stage55.py:88 도 같은 자리에 걸려 있다.
      assert 짝.등급매김(0.95, 0.80) == "B"   # 제목이 덜 닮으면 A 가 아니다
  총점 0.95 · 제목 0.80 은 점수() 가 낼 수 없는 값이다(총점 >= 제목).
  실제로 일어날 수 없는 입력으로 「제목이 A 를 가른다」 를 재고 있다.


[H] 2. 총무님이 채운 확인용 파일을 덮어쓰는 것을 아무것도 안 막는다

  data/업무대조.real.py:81
      확인.write_text("\n".join(ㅎ) + "\n", encoding="utf-8")
  가드가 없다. 다음 판이 수를 다시 세려고 실행기를 한 번 더 돌리면 총무님이 적은
  31 + 151 줄의 표시가 사본 없이 사라진다.

  선례가 저장소 안에 그대로 있다 — CLAUDE.md 14장
      scripts/suggest_sample.py 의 막는다 · 채운곳 — 나갈 파일이 있으면 --replace 를
      줘야 쓰고, 사람이 채운 판정이 있으면 --replace 로도 막는다.
      사람이 채운 판정은 되돌릴 수 없는 유일본이다.
  이 파일이 정확히 「사람이 채운 판정」 이다.

  되읽을 열쇠도 없다. 확인용 표의 열쇠가 번호뿐인데
      짝 표의 번호 = 짝들을 (-총점, 노션, 앱) 으로 정렬한 자리 (:129)
      노션만 표의 번호 = 표지를 뺀 tsv 줄 순서
  라서, 문턱이나 덤을 한 자리라도 손대면 총점이 바뀌고 번호가 통째로 밀린다.
  표에 run id 도 노션 줄 번호도 안 실린다 — 실행기는 r.id 를 이미 읽고도(:28) 안 찍는다.
  다음 판이 표시를 되읽을 때 「번호 17」 이 지난주의 17 인지 알 길이 없다.
  data/재정짝표.real.json 이 시트 줄 번호로 열쇠를 잡은 것과 반대 방향이다.


[M] 3. 시험이 안 보는 자리가 넷 — 고장을 심어 재 봤다

  모듈 사본에 고장을 하나씩 심고 tests/test_stage55.py 를 돌렸다.

  잡히는 것
      전역 정렬 제거(후보.sort 를 뺌)        -> b01 · c01 빨감
      덤을 통째로 0 으로                    -> c01 빨감
      총점을 1 에서 자름                    -> c01 빨감  (:83 의 그 주장이 실제로 재진다)
      등급A 를 총점만으로                   -> d01 빨감
      동점 갈림(-i, -j)을 뺌                -> c02 빨감
      1판처럼 구분 없는 줄을 뺌             -> a01 빨감  (수용 기준 나 는 실제로 재진다)
      네갈래의 표지 제외를 뺌               -> a01 빨감

  안 잡히는 것 — 전부 8 passed
      (가) 짝짓는다:113 의 표지 거르기를 없앰
           a01 은 표지 술어(표지인가)와 네갈래의 셈만 재고, 짝짓는다 안의 그 줄은
           안 잰다. a01 의 표지 제목이 앱 제목과 안 닮아서, 걸러도 안 걸러도 답이 같다.
      (나) 문턱 검사를 없앰 (바탕 >= 문턱 -> 바탕 > 0)
           d02 의 반례 두 제목이 닮음 0.0 이라 문턱을 하나도 안 재고 지나간다.
           0 과 0.50 사이의 짝이 시험에 없어서 닮음문턱 0.50 은 아무 시험도 안 붙들고 있다.
      (다) 제목점수:63-64 의 담음(포함) 보정을 없앰
           짧은 제목이 긴 제목 안에 들면 0.60 + 0.30*짧/긴 로 올려 주는 줄이다.
           b01 의 예는 raw ratio 도 같은 값이 나와 이 줄을 안 밟는다.
           실제로는 이 보정이 문턱 아래 짝을 후보로 끌어올리는 줄이다 —
           줄기 길이 3 대 12 면 raw 0.40(탈락)이 0.675 로 올라가고, 덤까지 붙으면 B 가 된다.
           B 30 중 몇이 이 줄로 올라온 것인지 보고에 없다.
      (라) 날짜 덤을 0 으로 · 담당 덤을 0 으로 (각각)
           c01 은 남은 덤만으로도 같은 답이 나와 안 빨개진다.
           덤 네 값(0.05 · 0.05 · 0.06 · 0.03)은 어느 하나도 시험이 안 붙든다.
           7일 이내 0.03 갈래는 시험이 한 번도 안 밟는다.

  덤 값에 근거가 보고에도 코드에도 없다. 왜 날짜가 구분·담당보다 비싼지(0.06 대 0.05),
  왜 7일이 경계인지가 적혀 있지 않다. 그리고 덤은 동점을 가르는 데만 쓰이지 않는다 —
  최대 0.16 이라 제목 0.16 낮은 짝이 제목이 똑같은 짝을 이길 수 있다.
  이번 자료에서는 「제목 같음」 이 1판과 같은 50 이라 실제로 밀린 자리는 없어 보이지만,
  그 성질도 시험도 글도 없다.


[M] 4. 「입력 순서가 답을 안 바꾼다」 는 동점에서 거짓이다 — 그 반례가 c02 자신이다

  scripts/노션업무짝짓기.py:107  docstring — 「입력 순서가 답을 안 바꾼다」
  docs/review/최근.md:30 · CLAUDE.md 14장 새 줄 — 같은 말
  갈림 열쇠가 (총, 바탕, -i, -j) 라(:118) 총점이 정확히 같으면 줄 번호가 이기고,
  줄 번호는 입력 순서다. test55_c02 는 덤을 지워 동점을 만든 뒤 앞줄이 이기는 것을
  단언한다 — 그 상황에서 입력을 뒤집으면 답이 뒤집힌다.
  :109 의 「같은 자료면 늘 같은 답이 나온다」 는 맞다(되풀이 가능). 두 문장이 한 독스트링
  안에서 서로 다른 세기를 말한다.

  실제 자료로는 안전하다 — 노션·앱을 뒤집어 다시 돌려도 짝 82 가 전부 같았다.
  주장을 「되풀이 가능하다」 로 좁히면 코드는 그대로 두어도 된다.
  b01 이 재는 것도 「순서 뒤집어도 같다」 가 아니라 「동점이 아닌 두 짝이 안 밀린다」 다.


[M] 5. 보고 7장이 통째로 빈 채로 커밋됐다

  docs/review/최근.md:129-133 — 글검사 · 새 시험 · 바뀐 파일 · 마지막 pytest 가 전부
  「아래 줄」 이고 아래에 아무것도 없다. 11-3 3단계는 글검사 결과를
  「깨끗해도 적습니다 — 안 한 것과 구별되지 않습니다」 라고 못박아 두었다.
  check_placeholder 는 이 꼴을 못 본다(CLAUDE.md 가 그렇게 적어 둔 그대로다) —
  그래서 이 자리가 검토자 몫이다.
  내가 돌린 값: 글검사 7/7 초록 · tests/test_stage55.py 8 passed.

  머리 줄도 같은 자리다.
  docs/review/최근.md:8  커밋: 63be095 다음 — 이 판의 커밋들(7장 파일 목록)
  11-3 은 「커밋: <짧은 해시>」 를 요구하고, 가리킨 7장이 비어 있어 이 보고가 어느
  커밋의 것인지 보고만 봐서는 알 수 없다. 바로 앞 얼린 판(2026-09-16-회의록id.md:9)은
  해시 둘을 적어 두었다.


[M] 6. docs/봐둘것.md 를 안 열었다 (11-3 3단계)

  이 범위에 봐둘것 변경이 없고 보고에도 그 말이 없다. 닫은 항목이 없으면 「없음」 이라도
  적혀야 하고(사람칸과 같은 이유), 무엇보다 이 판이 새로 미룬 것이 어디에도 안 남았다 —
  위 1(A 24/51 어긋남) · 2(확인용 덮어쓰기와 번호 열쇠) · 3(안 재는 자리 넷)이
  다음 판에 전해지는 길이 이 검토 보고뿐이다. 그 보고는 다음 판이 덮는다.


[L] 7. included=false 6 이 섞인 채로 앱 102 를 셌고 보고가 그 말을 안 한다

  data/업무대조.real.py:28-32 가 r.included 를 읽고도 안 거른다.
  재 보니 앱 102 중 included=0 이 6 이고, 그중 1 은 짝 안에, 5 는 「앱만 20」 안에 있다.
  「앱만 20」 을 「노션에 없는 앱 업무」 로 읽으면 그 5 는 이번 회차에 안 하기로 한 것이다.
  1판은 앱만 목록에 「(빼 둠)」 을 붙이고 「앱 run included」 를 따로 셌다
  (data/노션대조.real.py:75 · :92) — 이번 판에서 그 표시가 사라졌다.
  included 를 빼고 세면 앱 96 · 앱만 14 로 바뀐다(짝 82 · A/B/C 는 그대로).
  어느 쪽이 맞는지는 사람이 정할 일이지만, 보고가 그 갈림을 안 보여 준다.


[L] 8. 1e 밑줄의 셈이 글로는 안 맞는다

  docs/review/최근.md:49
      「구분 없는 10줄이 들어와(143 + 10) 그중 1 이 짝지었고 새 규칙이 앱만에서
       2 를 더 이어(−2) 151 이다」
  글대로 빼면 143 + 10 − 1 − 2 = 150 이다. 「1」 은 「2」 안에 든 부분집합인데 두 번 뺀
  꼴로 읽힌다. 최종 수 151 은 맞다(노션 후보 233 − 짝 82). 같은 줄의 뒷문장이
  그 셈을 제대로 적어 두었으니 앞문장만 고치면 된다.


[L] 9. 확인용 표에 제목을 그대로 넣는다 — 세로줄 이스케이프 없음

  data/업무대조.real.py:73 · :80 이 제목을 마크다운 표 칸에 날것으로 넣는다.
  지금 자료에는 세로줄·줄바꿈·탭이 든 제목이 0 이라 안 터졌다(재 봤다. 표 데이터 182줄
  전부 칸 수가 같다 = 31 + 151). 다만 선례가 있다 — CLAUDE.md 14장 칸글:
  「한 줄이 쪼개지면 그 제안은 화면에 안 뜨고 가드도 못 지킨다(3판에서 22개 중 5개가
  그렇게 사라졌다)」. 노션 제목은 사람이 자유로 적는 칸이고, 다음 판은 이 표를
  세로줄로 갈라 되읽을 참이다.


[L] 10. 실행기의 회차 고르기가 말없이 첫 줄을 집는다

  data/업무대조.real.py:27  ... where name like '%2026 여름%' order by id ... fetchone()
  지금은 맞는 회차가 1 개(전체 회차 2)라 안전하다. 4-18 에 같은 모양의 사고가 적혀
  있다 — 「전에는 시작일이 늦은 것을 말없이 골랐는데 … 코람데오 28줄이 그리로
  들어갔습니다」. 비품들여오기의 회차머리 처럼 둘 이상이면 멈추고 이름을 찍는 쪽이
  이 저장소의 규약이다.


[L] 11. 「전부 지어낸 것」 인데 한 줄이 실제 자료에 그대로 있다

  tests/test_stage55.py:3  「제목 · 날짜 · 담당은 전부 지어낸 것이다」
  시험이 쓴 제목 9 중 1 이 노션 tsv 에 글자 그대로 있다 — D-주차 표지 줄이라
  CLAUDE.md 전체에 이미 나오는 말이고 샌 것이 아니다. 다른 하나는 세 글자짜리
  일반명사가 실제 제목 안에 들어 있는 정도다.
  적어 두는 까닭은 봐둘것 BC-a 가 바로 앞 판에서 「시험 값인 줄 알았는데 실물이었다」
  였기 때문이다. 표지 줄은 구분해 적으면 된다.


[L] 12. 인계의 「앞 판 글」 줄이 아직 옛 짝짓기 코드를 가리킨다

  docs/인계.md 의 새 항목 아래 「앞 판 글:」 줄에
  「짝짓기 코드 data/노션대조.real.py」 가 그대로 남아 있다. 라벨이 붙어 있어 기록으로는
  읽히지만, 이 판이 규칙을 저장소 안으로 옮긴 것이 그 바로 위 줄이라 나란히 읽으면
  어느 쪽이 지금 것인지 흐리다. docs/옛말.md 에는 아무것도 안 더했다.


지시문 대조 (검토-원칙 8-1)

  받은 요지의 항목이 표에 다 있다 — 1a~1e · 2 · 3 · 4a · 4b · 5 · 운영/노션 읽기만 ·
  재시작 · 마무리. 「값은 보고·덩어리에 안 싣는다」 만 줄이 없는데 실제로는 지켜졌다
  (보고 :13). 2a~2d · 3a~3b · 5a~5b 라는 잔가지 번호는 내가 받은 요지에 없다 —
  실제 지시문이 그렇게 번호를 매겼는지는 사람이 본다.
  3a 를 「다르게 했다」 로 적었는데 그 까닭 칸이 스스로 「지시문이 .md 로 열어 뒀다」 고
  말한다 — 허락된 쪽을 고른 것이라 「했다」 가 맞아 보인다. 무겁게 잡지 않는다.
  표의 판정 중 본문과 어긋나는 것은 「마무리는 11-3 대로 · 했다 · 7 · 8장」 하나다 —
  7장이 비어 있다([M] 5).

  고치기 전 훑기(8-2)는 있고, 셋 다 값이 적혀 있고, 짚은 두 자리(인계 · CLAUDE.md 14장)를
  실제로 고쳤다.


사람에게 묻는 것

  1. 등급 A 51 중 24 가 구분·담당·시작일에서 어긋난다. 이 수를 보고도 갈래 ㄴ
     (A 자동 확정)을 고르시겠습니까 — 아니면 A 도 「담당·날짜가 어긋난 것」 만
     따로 떼어 확인용 표에 올릴까요.
  2. 확인용 파일은 서버 노트북의 data/ 안에 있고 gitignore 됩니다. 총무님이 그 파일을
     어떻게 받아 보고 어떻게 돌려주나요 — 그 길이 정해져야 번호 열쇠 문제도 같이 풀립니다.
  3. included=false 6 을 대조에서 뺄까요 두고 볼까요. 빼면 앱만 20 -> 14 입니다.
  4. 덤 네 값(0.05 · 0.05 · 0.06 · 0.03)과 닮음문턱 0.50 은 어디서 나온 값인가요 —
     눈으로 몇 건 맞춰 보고 정한 것이면 그 사실을 파일에 한 줄 적어 두면 다음 판이
     같은 값을 다시 고르지 않습니다.

━━━ 검토자 여기까지 ━━━
```

## 8-2. 첫 검토를 받아 고친 것

| 번호 | 받았나 | 고친 곳 |
|---|---|---|
| [H] 1 | 받음 | `등급매김` 이 **어긋난 칸**을 받는다 — 제목이 사실상 같아도 구분 · 담당 · 시작일이 어긋나면 B. A 51 → **27**, B 30 → 54. `어긋남` · `어긋난수` 를 더하고 보고 2장 · 5장 ㄴ · 확인용 표에 어긋난 칸을 실었다. `d01` 을 실제로 날 수 있는 입력으로 다시 쓰고 `d01b`(한쪽이 비면 어긋남 아님)를 더했다 |
| [H] 2 | 받음 | 실행기가 **사람이 채운 표시를 덮지 않는다** — 채운 줄이 있으면 `--replace` 로도 멈추고, 없어도 `--replace` 없이는 안 덮는다(두 표 모두 심어서 확인). 확인용 · 상세 표에 **노션 줄 번호와 run id** 를 실어 번호가 밀려도 되읽을 열쇠가 남는다 |
| [M] 3 | 받음 | 안 재던 넷에 시험을 더했다 — `e01`(짝짓는다 안의 표지 거르기) · `e02`(문턱 위아래) · `e03`(담음 보정이 없으면 문턱 아래) · `e04`(날짜 · 담당 · 구분 덤이 따로). 덤 값이 **고른 값**이라는 것과 「덤이 제목을 이길 수 있다」 는 성질은 봐둘것 BC-b 로 넘겼다 |
| [M] 4 | 받음 | 독스트링 · 보고 · CLAUDE.md 를 「앞 짝이 뒤 짝을 안 밀어낸다 · 되풀이 가능 · 동점에서는 줄 번호가 이긴다」 로 |
| [M] 5 | 받음 | 7장을 채웠다(이 장) · 머리 커밋 줄을 짧은 해시로 |
| [M] 6 | 받음 | `docs/봐둘것.md` 에 **BC-b** — 확인용 파일을 건네는 길 · `included` 6 · 덤 값의 근거 · 덤이 제목을 이기는 성질 |
| [L] 7 | 받음 | 실행기가 빼 둔 run 수를 찍고 상세에 「(이번 회차에 빼 둠)」 을 붙인다. 보고 2장에 「빼면 앱 96 · 앱만 14」 를 적고 갈림은 BC-b 로 |
| [L] 8 | 받음 | 1e 밑줄을 「후보 233 − 짝 82 = 151」 로 다시 썼다 |
| [L] 9 | 받음 | `칸글` — `|` · 줄바꿈 · 탭을 표 칸에 안 넣는다 |
| [L] 10 | 받음 | 이름이 맞는 회차가 하나가 아니면 멈추고 id 를 찍는다 |
| [L] 11 | 받음 | 시험 머리글을 「표지처럼 기준 문서에 이미 있는 꼴만 그대로 쓴다」 로 |
| [L] 12 | 안 받음(그대로 둠) | 인계의 「앞 판 글」 은 그 판의 기록이라 글자를 안 고친다 — 지금 규칙이 어디 있는지는 바로 위 줄과 CLAUDE.md 14장이 말한다 |

사람에게 묻는 것 넷 중 1 · 3 · 4 는 사람칸 「정할 것」 과 BC-b 로, 2 도 BC-b 로 올렸다.

## 8-3. 두 번째 검토 (범위 b225688..12e2603)

```
━━━ 검토자 여기부터 ━━━ 범위 b225688..12e2603 · 커밋 1개 · 파일 6개 (+ 저장소 밖 실행기 1)

다시 재 본 것 (값은 안 옮기고 건수만)

  등급을 규칙 모듈로 직접 다시 셌다 — 보고 · 인계의 수가 그대로 나온다.
      짝 82 · A 27 · B 54 · C 1
      어긋난 짝 45 (구분 33 · 담당 13 · 시작일 6)
      A 안에 어긋난 칸이 있는 것 0  <- 새 규칙이 실제로 선다
      B·C 55줄 · 노션만 151줄
  네갈래는 하나도 안 움직였다(151 · 20 · 50 · 32 · 표지 24) — 등급만 좁아지고 짝은 그대로다.
  확인용 파일 실물: 데이터 206줄(55 + 151) · 칸 수 6 과 8 · 사람이 채운 줄 0.

  안 재던 넷이 이제 잡힌다(모듈 사본에 고장을 하나씩 심어 재 봤다).
      짝짓는다 안의 표지 거르기 제거   -> e01 빨감   (앞 판: 통과했음)
      문턱 검사 제거                  -> e02 빨감   (앞 판: 통과했음)
      담음 보정 제거                  -> e03 빨감   (앞 판: 통과했음)
      날짜 덤 0                       -> e04 빨감   (앞 판: 통과했음)
      구분 덤 0                       -> e04 빨감
  어긋남 갈래도 잡힌다.
      A 에서 어긋남 검사 제거          -> d01 빨감
      구분 · 담당 · 시작일 줄 하나씩 제거 -> 각각 d01 빨감
      「한쪽이 비면」 검사 제거          -> d01b 빨감
      가까운날 7 -> 90                 -> d01 · e04 빨감
  앞 판에 있던 것도 그대로 산다(정렬 제거 b01·c01 · 덤 통째 c01 · 총점 자르기 c01 ·
  동점 갈림 c02 · 1판처럼 구분없는 줄 빼기 a01·d01b · 네갈래 표지 제외 a01).

  덮어쓰기 가드를 다섯 경우로 재 봤다(파일은 안 돌리고 채운줄 만 꺼내 씀).
      갓 만든 빈 파일 -> 0 · 표 1 의 답 한 칸 -> 1 · 표 2 앞 칸 -> 1 ·
      표 2 뒤 칸 -> 1 · 머리와 가름줄만 -> 0.   막으면 안 되는 것을 안 막는다.

  글검사 7/7 초록 · pytest tests/test_docs_shell.py tests/test_stage55.py 24 passed
  (내 첫 검토 원문이 보고에 실린 뒤에도 초록이다 — 11-3 이 적어 둔 그 사고 자리를 봤다).
  값 누출 없음 — 보고 · 인계 · 봐둘것 · 실행기 출력 · 가드 메시지 · 회차 멈춤 메시지가 전부 건수뿐.
  1e 밑줄 셈 고쳐졌고, 머리 커밋 줄이 해시 셋이 됐고, 7장 파일 목록이 실제와 같다(8개).
  [L] 12 를 안 받은 까닭이 적혀 있다.


[H] 1. 8-2 표가 「CLAUDE.md 를 고쳤다」 고 적었는데 안 고쳐졌다 — 모듈 머리도 안 고쳐졌다

  docs/review/최근.md:386
      | [M] 4 | 받음 | 독스트링 · 보고 · CLAUDE.md 를 「앞 짝이 뒤 짝을 안 밀어낸다 ·
        되풀이 가능 · 동점에서는 줄 번호가 이긴다」 로 |

  실제로 고쳐진 곳은 둘뿐이다.
      scripts/노션업무짝짓기.py:129-133  (짝짓는다 독스트링)  고쳐짐
      docs/review/최근.md:30                                고쳐짐
      CLAUDE.md:4419                                        안 고쳐짐
      scripts/노션업무짝짓기.py:8                            안 고쳐짐

  CLAUDE.md:4419 는 그대로다.
      … **점수를 다 매긴 뒤 높은 것부터 확정한다**(입력 순서가 답을 안 바꾼다 ·
        1판의 탐욕 1:1 이 뒤 짝을 밀어냈다). …
  이 판이 CLAUDE.md 를 실제로 고친 것은 등급 A 문장과 문턱 문장 둘뿐이다(diff 로 확인).

  그리고 같은 파일이 자기 자신과 어긋난다.
      scripts/노션업무짝짓기.py:8    입력 순서가 결과를 바꾸지 않습니다(전역에 가깝게)
      scripts/노션업무짝짓기.py:131  다만 점수가 정확히 같으면 줄 번호가 이기므로
                                     그 자리에서는 입력 순서가 답을 가른다
  여덟째 줄과 백서른한째 줄이 반대를 말한다. 앞 검토 [M] 4 가 잡은 것이 정확히 이 모양이고,
  고치면서 문장 표기가 달라(「답을 안 바꾼다」 / 「결과를 바꾸지 않습니다」) 한쪽만 걸렸다.

  무겁게 잡는 까닭 — 남은 두 자리가 **기준 문서와 파일 머리**다. 다음 사람이 여는 순서가
  CLAUDE.md 14장 -> 파일 머리 -> 함수 독스트링이라, 고쳐진 곳이 가장 안쪽 하나다.
  그리고 8-2 표에 「받음」 이라고 적혀 있으면 사람은 그 자리를 다시 안 본다 —
  검토-원칙 8-1 이 「표의 판정이 본문과 어긋나지 않나」 를 검토자 몫으로 둔 그 자리다.

  고칠 것은 글 둘이거나 표 한 줄이다(무엇을 고칠지는 사람이 정한다).


[M] 2. e04 가 담당 덤은 안 잰다 — 8-2 는 「날짜 · 담당 · 구분 덤이 따로」 라고 적었다

  tests/test_stage55.py 의 e04 안
      담당만 = 짝.덤(ㄴ("x", team="1 총무팀", start=None), ㅇ("x", team="chongmu", start=None))
      assert 담당만 > 0 and 구분만 > 0
  ㄴ 의 기본 kind 가 "Main", ㅇ 의 기본 kind 가 "main" 이라 **구분 덤 0.05 가 그대로 붙는다.**
  담당 덤을 0 으로 만들어도 담당만 = 0.05 > 0 이라 통과한다.

  심어서 재 봤다.
      담당 덤 0  -> 13 passed  (안 잡힘)
      날짜 덤 0  -> e04 빨감
      구분 덤 0  -> e04 빨감
  바로 위 줄의 구분만 은 양쪽에 team="" 을 줘 제대로 떼어 놨다. 담당 쪽만 그 처리가 빠졌다.
  e04 의 독스트링이 스스로 「하나를 지워도 다른 하나가 덮어 주면 그 갈래는 안 재진다」 고
  적어 두었는데, 그 문장이 담당 줄에서 그대로 일어난다.

  한 줄이면 닫힌다 — 담당만 의 양쪽에 kind="" 를 준다.
  8-2 [M] 3 의 「e04(날짜 · 담당 · 구분 덤이 따로)」 도 함께 봐야 한다.


[M] 3. 새로 만든 어긋난수() 에 시험이 하나도 없다 — 사람이 읽을 머리 수를 내는 함수다

  scripts/노션업무짝짓기.py:177-182
  이 함수가 내는 수가 보고 2장(어긋난 짝 45 · 구분 33 · 담당 13 · 시작일 6) ·
  인계 첫 줄 · 상세 파일 머리에 그대로 실린다.

  심어서 재 봤다.
      어긋난수 를 늘 0 으로  -> 13 passed  (아무도 안 잡는다)
  검토-원칙 2 ③ 「센 것이 0인데 통과하지 않나」 그 자리다. 짝 몇 개로 칸별 수를 세는
  단언 한 줄이면 된다(d03 이 등급수 의 합을 재는 것과 같은 꼴).


[M] 4. 덮어쓰기 가드가 상세 파일을 먼저 쓴 뒤에 선다 — 그리고 가드 자체에 시험이 없다

  data/업무대조.real.py:79   상세.write_text(...)
  data/업무대조.real.py:102  if 확인.exists(): ... raise SystemExit
  가드가 서면 **상세는 이미 새 판으로 덮였고 확인용만 옛 판**이다. 그 둘을 나란히 보는
  사람에게는 등급 · 번호 · B·C 집합이 서로 안 맞는 두 파일이 남는다.
  게다가 :133 이후의 건수 출력이 아예 안 뜬다 — 무엇이 달라졌는지도 못 본다.
  가드를 파일 쓰기 **앞**으로 올리면 「거절할 때는 아무것도 안 쓴다」 가 된다.

  그리고 이 판이 새로 넣은 **막는 코드**인데 막히는 쪽 시험이 없다(11-3).
  data/ 안이라 시험을 붙일 자리가 없다고 볼 수 있지만, 채운줄(:86-99)은 **글을 받아
  줄을 돌려주는 함수**라 값이 필요 없다 — scripts/노션업무짝짓기.py 로 옮기면 규칙 모듈이
  「값은 안 읽고 안 쓴다」 를 그대로 지키면서 그 자리에 시험이 선다.
  이 판이 규칙과 문턱을 저장소 안으로 옮긴 것과 같은 까닭이다.


[M] 5. 채운줄 에 구멍 하나 — 끝 빈칸이 잘린 줄의 답을 못 본다

  data/업무대조.real.py:94
      if len(칸) < 6 or not 칸[0].isdigit(): continue
  표 2 는 칸이 정확히 6 이라, 마지막 빈 칸이 하나 떨어져 나간 줄(칸 5)은 통째로 건너뛴다.
  재 봤다.
      "| 1 | 5 | … | x | 넣음 |"   (칸 5, 답이 들어 있음)  ->  채운 줄 0
  즉 그 줄의 답을 못 보고 덮는다. 표를 편집기나 다른 도구로 열었다 저장하면 끝의 빈 칸이
  떨어지는 일이 있다. 앞의 다섯 경우는 전부 바르게 돌았으니 구멍은 이 하나다.

  :87 의 독스트링이 「뒤에서 센다 — 앞에서 세면 칸이 하나 늘거나 줄 때 엉뚱한 칸을 본다」 고
  적었는데, 정작 표를 **가르는 기준**(len(칸) >= 8)이 칸 수라 그 위험이 그대로 남아 있다.
  머리글 글자로 어느 표인지 기억하며 훑거나, 답 칸을 「빈 칸이 아니면」 이 아니라
  「번호 · 노션 줄 · run id 말고 다른 칸에 글자가 있으면」 으로 보면 칸 수에 안 매인다.


[L] 6. --replace 를 첫 인자로 주면 그것이 ROOT 가 된다

  data/업무대조.real.py:13   ROOT = pathlib.Path(sys.argv[1] if len(sys.argv) > 1 else ".")
  독스트링(:7)에는 --replace 를 쓰는 꼴이 없어서, 가드 메시지(:108)를 보고
  `... 업무대조.real.py --replace` 라고 치면 ROOT 가 "--replace" 가 되어 엉뚱한 곳에서
  tsv 를 찾는다. 독스트링에 `. --replace` 꼴을 한 줄 적어 두면 된다.


[L] 7. 5장 ㄴ 의 「4분의 1 이 먼저 닫힌다」 — 실제로는 3분의 1 이다

  docs/review/최근.md:88 · A 27 / 짝 82 = 0.329.
  앞 판은 51/82 를 「절반 넘게」 로 적었고 수가 좁아지며 이 문장만 한 단 더 내려갔다.
  사람이 갈래를 고르는 표 안의 수라 짚는다.


[L] 8. 회차가 둘 이상일 때 id 만 찍고 이름을 안 찍는다

  data/업무대조.real.py:27-30 이 name 을 읽고도 메시지에는 id 만 넣는다.
  4-18 의 회차머리 는 「id 와 이름을 늘어놓습니다」 다 — 「사람이 골라야 합니다」 라고
  적어 두고 고를 근거를 안 주면 사람이 DB 를 따로 열어야 한다.
  회차 이름은 CLAUDE.md 에 이미 나오는 꼴이라 값 문제는 아니다.


[L] 9. 칸글 이 제목에만 걸린다

  data/업무대조.real.py:68-69 · :122-123 에서 줄값(n) 과 「앱 구분/담당/시작」 문자열은
  칸글 을 안 지난다. 지금 자료에서는 그 칸이 고정 어휘와 날짜라 위험이 낮다.
  자유 글이 들어오는 칸만 감싸는 것이 일부러 고른 것이라면 그대로 둬도 된다.


[L] 10. 등급매김 의 어긋난칸 기본값이 () 라 옛 동작이 조용히 돌아온다

  scripts/노션업무짝짓기.py:115
      def 등급매김(총점, 제목, 어긋난칸=()) -> str
  인자를 빼고 부르면 제목만으로 A 가 난다 — 이 판이 고친 바로 그 동작이다. 지금 부르는
  곳은 짝짓는다 하나라 안 밟히지만, 기본값이 「고치기 전」 인 것은 다음 호출자가 잊었을 때
  아무것도 안 빨개진다. 필수 인자로 두면 그 자리가 구조로 막힌다.


[L] 11. origin/main 이 아직 63be095 다 — 두 커밋이 안 올라갔다

  b225688 도 12e2603 도 원격에 없다. 덩어리에 raw · compare 주소를 달면 **고치기 전 것**을
  가리킨다(11-3 4단계 「푸시하지 않았으면 주소를 지어내지 않습니다」).
  amend 로 걷어낸 fed8749 는 로컬에만 남아 있고 원격에는 애초에 간 적이 없다 — 그 쪽은 깨끗하다.


코디가 이미 아는 것 (확인만)

  7장 끝 줄이 아직 채울 자리 표시이고 수용 기준 바) · 사) 가 비어 있다.
  check_placeholder 는 그 꼴을 못 봐서 글검사는 지금도 7/7 초록으로 나온다 —
  전체 pytest 가 끝난 뒤 그 두 줄을 채우는 것은 사람 · 구현자 몫으로 남는다.


8-2 표를 읽고 (검토-원칙 8-1 과 같은 자리)

  열두 줄 중 열한 줄이 「받음」 이고, 내가 코드와 시험으로 되짚어 실제로 선 것은 열이다.
  [M] 4 하나가 표와 실물이 갈린다([H] 1). [M] 3 은 넷 중 셋만 섰다([M] 2).
  [L] 12 는 「안 받음」 과 까닭이 함께 적혀 있어 그대로 둘 만하다.


사람에게 묻는 것

  1. CLAUDE.md:4419 와 모듈 머리 8줄을 고칠까요, 아니면 8-2 표의 [M] 4 줄을
     「독스트링과 보고만 고쳤다」 로 고칠까요 — 둘 중 하나는 지금 사실이 아닙니다.
  2. 채운줄 을 저장소 안 규칙 모듈로 옮겨 시험을 붙일까요. 그 함수는 글만 받고
     값을 안 봐서 「값은 안 읽고 안 쓴다」 를 안 깹니다.
  3. 등급매김 의 어긋난칸 을 필수 인자로 둘까요 — 기본값이 고치기 전 동작입니다.

━━━ 검토자 여기까지 ━━━
```

## 8-4. 두 번째 검토 뒤 고침 (다시 보이지 않았다 — 11-3)

| 번호 | 파일 | 고친 것 |
|---|---|---|
| [H] 1 | `CLAUDE.md` · `scripts/노션업무짝짓기.py` | 기준 문서 14장 줄과 모듈 머리 8줄을 「앞 짝이 뒤 짝을 안 밀어낸다 · 같은 자료면 늘 같은 답 · **점수가 정확히 같을 때만** 줄 번호가 이긴다」 로. 8-2 표의 [M] 4 줄이 이제 사실이다 |
| [M] 2 | `tests/test_stage55.py` | `e04` 의 「담당만」 양쪽에 `kind=""` — 구분 덤이 덮어 주던 것을 떼어 담당 덤이 실제로 재진다 |
| [M] 3 | `tests/test_stage55.py` | `f01` — `어긋난수` 를 칸마다 센다(어긋난 짝 3 · 구분 1 · 담당 1 · 시작일 1) |
| [M] 4 | `data/업무대조.real.py` · `scripts/노션업무짝짓기.py` · `tests/test_stage55.py` | 가드를 **파일 쓰기 앞**으로 올려 거절할 때는 아무것도 안 쓴다(상세가 안 바뀌는 것까지 확인). 판정을 규칙 모듈의 `표줄` · `손댄줄` 로 옮기고 `f02` 로 잰다 |
| [M] 5 | `scripts/노션업무짝짓기.py` | 칸 수로 답 칸을 세지 않는다 — **새로 낸 판과 글자로 견줘** 손댄 줄을 찾는다(끝 빈 칸이 떨어진 줄도 잡힌다 · `f02`) |
| [L] 6 | `data/업무대조.real.py` | 독스트링에 `. --replace` 꼴 한 줄 |
| [L] 7 | `docs/review/최근.md` | 5장 ㄴ 을 「3분의 1」 로 |
| [L] 8 | `data/업무대조.real.py` | 회차가 둘 이상이면 id 와 이름을 함께 찍는다 |
| [L] 9 | — (그대로 둠) | `칸글` 은 사람이 자유로 적는 칸(제목)에만 건다. 구분 · 담당 · 날짜는 고정 어휘라 감싸면 값이 더 읽기 어려워진다 |
| [L] 10 | `scripts/노션업무짝짓기.py` · `tests/test_stage55.py` | `등급매김` 의 `어긋난칸` 을 **필수 인자**로 — 기본값이 곧 고치기 전 동작이었다 |
| [L] 11 | — | 푸시는 마지막 pytest 뒤에 한다. 덩어리 주소는 그 뒤에 만든다 |

**커밋 메시지를 한 번 반대 뜻으로 적었다** — 「…사람이 채운 표시를 지키지 않는다」. 푸시 전이라 `amend` 로 고쳤다(`fed8749` → `12e2603`). 메시지는 새 커밋으로 못 고치는 자리라 이번만 고쳐 썼다.
