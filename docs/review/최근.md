사람이 할 것
노트북에서 직접: 없음 — 운영 실행은 이 창이 돌렸다(1장)
관리자 권한: 없음 — `app/` 을 안 고쳐 재시작하지 않았다(2장)
정할 것: 회의록 id 채우기를 활동 기록에 남길지(지금은 안 남음 · 재정 들여오기는 남김) · 사람 이름이 아닌 원본 값(페이지 제목)도 커밋 전 검사가 보게 넓힐지(봐둘것 BC-a) · 수용 기준 가) 를 4장 그 줄처럼 읽은 것이 맞는지
막혀서 못 한 것: 화면 점검(drawer.js · phone.js) — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다. 화면을 안 고친 판이라 급하지 않다(2장)

작성 시각: 2026-09-16 01:00 KST
커밋: cfcb107 (2판 얼림) · 594d631 (보고 · 문서) · 검토 뒤 고침은 그다음 커밋
작업: 회의록 id 채우기 — 운영 실행

---

**값은 여기 없다** — 페이지 제목 · 페이지 id 는 싣지 않는다. 운영 DB 를 바꾼 판이다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 없음 — 코드는 안 고친다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 |
| 그 값을 세어 둔 문서 | CLAUDE.md 13장(「짝 확인 · id 채우기 전」) · 인계 맨 위와 1)(「운영 채우기는 다음 판」) · 앞 판 보고(얼림) |

# 1. 운영 실행 (1a–1d)

11-2 의 순서 — 미리보기 → 실행 → 다시 세기.

**1a 실행 전 미리보기** — 앞 판(a946d87 보고 4장)과 줄마다 같다.

| 줄 | 앞 판 | 이번 실행 전 | 실행 뒤(1d) |
|---|---|---|---|
| 노션 페이지 | 10 | 10 | 10 |
| 앱 출처 파일 · 짝지은 파일 | 10 · 10 | 10 · 10 | 10 · 10 |
| 짝 없는 페이지 | 0 | 0 | 0 |
| 노션 회의 | 50 | 50 | 50 |
| 노션이 아닌 회의(안 건드림) | 3 | 3 | 3 |
| 이미 id 있음 | 0 | 0 | 50 |
| 채울 회의 | 50 | 50 | 0 |

실행 전 SQL(`mode=ro`) — id 달린 행 0 · 회의 전체 53 · 노션이 아닌 회의 3 과 그 행 전체 칸의 지문을 떠 뒀다.

**1b 실행** — `scripts/회의록id달기.py --실행`. 직전 사본 `data/backups/app-20260916-004600.db` 를 떴다. 사본을 `mode=ro` 로 열어 보니 회의 53 · id 달린 행 0 — 채우기 전 판이 맞다. 스크립트 끝 줄 「id 가 달린 회의: 0 → 50」.

이 사본은 문서가 되돌리는 자리로 가리키지 않는다 — 되돌리기는 origin 이 노션인 회의의 그 칸을 NULL 로 되돌리면 된다(채우기 전 그 칸은 53행 전부 비어 있었다). 그래서 `지키는판` 에 넣지 않았다(11-2 · 인계에도 사본 이름을 안 적었다).

**채운 일은 활동 기록에 안 남았다** — 스크립트가 남기지 않는다(재정 들여오기는 남긴다 · 사람칸 「정할 것」).

**1c 실행 뒤 SQL 로 다시 셈** (`mode=ro` · 세션 캐시가 아닌 새 연결):

| 셈 | 계획 | 실제 |
|---|---|---|
| id 달린 행 | 50 | 50 |
| 그중 노션 회의 | 50 | 50 |
| 노션이 아닌 회의 | 3 · 지문 그대로 | 3 · 지문 실행 전과 같음 |
| 서로 다른 페이지 id | 10 | 10 |
| 출처 하나에 id 둘 이상 | 0 | 0 |
| 목록에 없는 id · 안 쓰인 목록 id | 0 · 0 | 0 · 0 |
| 회의 전체 | 53 | 53 |

**1d 두 번째** — 미리보기가 「이미 id 있음 50 · 채울 회의 0 · 채울 것이 없습니다」. `--실행` 을 한 번 더 붙여도 「채울 것이 없습니다」 로 끝나고 사본을 새로 뜨지 않았다(백업 폴더의 마지막 사본이 그대로).

# 2. 재시작 · 화면 점검 (2a · 2b)

- **재시작 안 함** — `git diff --name-only a946d87 -- app templates static` 이 비어 있다. 운영 서버 PID 는 앞 판 재시작 뒤 그대로 19156(확인만 · 2026-09-16)
- **drawer.js · phone.js 못 돌림** — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다(11-2). 이번 판과 앞 판은 화면(템플릿 · 정적 파일)을 안 고쳤고 앞 판의 `app/` 변경은 NULL 칸 하나라 **급하지 않다.** 사람칸 「막혀서 못 한 것」

# 3. 문서 (3a–3c)

- **3a** CLAUDE.md 13장 — 「대조가 끝났고(2026-09-15) id 가 채워졌습니다(운영 실행 2026-09-16)」. 지시문의 날짜는 대조가 끝난 날에 붙이고, 실제 실행이 자정을 넘겨 채운 날을 따로 적었다(검토 [L] 뒤 날짜 자리를 옮김)
- **3b** 인계 1) — 「회의록 — 닫힘」 · 「업무(ㄱ2) — 남음 · 다음 판」 줄로 갈랐다. 옛 줄(2판의 「채우기 전」)은 그 판 글자를 두고 「→ 다음 판에 채워짐」 꼬리를 붙였다. 「무엇」 줄의 옛 계획(외부 id 칸을 만들고 다시 들여오기)에는 「사람이 다시 정함」 꼬리를 붙이고, 2판 줄의 「다음은 업무 ㄱ2」 는 새 줄로 넘겼다(검토 [M]). 되돌리기 문장에 범위(origin 노션)를 적었다. 맨 위 갱신 줄 새로
- **3c** 봐둘것 `BC-a` — 시험 파일 이력에 실제 노션 페이지 제목 둘이 남았다(팀 이름 · 사람 이름 없음 · 이력 안 고침)
- 앞 판 보고를 `docs/review/2026-09-15-노션대조2판.md` 로 얼렸다

# 4. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 실행 전후 미리보기 수가 같다 | O — 오른쪽처럼 읽음 | 1장 1a 표 — 실행 뒤는 정의상 채울 0 이라 「채울 + 이미」 의 합과 나머지 줄이 같은지로 봄 |
| 나) SQL 로 id 50 · 안 건드림 3 | O | 1장 1c 표 |
| 다) 두 번째 「채울 것이 없습니다」 | O | 1장 1d |
| 라) pytest 옵션 없이 · 글검사 7/7 | 마지막 pytest 뒤 채움 | 5장 |
| 마) 검토자 원문 | 검토 뒤 채움 | 6장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 1a | 했다 | 1장 — 줄마다 같음 |
| 1b | 했다 | 1장 — 사본을 열어 채우기 전 판인지까지 봄 |
| 1c | 했다 | 1장 — 새 연결 `mode=ro` SQL |
| 1d | 했다 | 1장 — 미리보기와 `--실행` 둘 다 |
| 2a | 했다 | 2장 |
| 2b | 했다 | 사람칸 · 2장 |
| 3a | 했다 | 3장 — 날짜를 둘 적음 |
| 3b | 했다 | 3장 |
| 3c | 했다 | `BC-a` |
| 운영 DB 를 실제로 바꾼다 · 마무리는 11-3 대로 | 했다 | 1 · 5 · 6장 |

# 5. 검사와 시험

- 커밋 전 `git status --short`: 문서만 — `data/` 안의 것은 안 잡혔다
- 글검사: 첫 커밋 전 7/7 · 검토 뒤 고침 커밋 전 7/7
- 바뀐 파일(`git diff --name-only a946d87..HEAD`): `CLAUDE.md` · `docs/review/2026-09-15-노션대조2판.md` · `docs/review/최근.md` · `docs/봐둘것.md` · `docs/인계.md`
- 보고를 다 채운 뒤 돌린 pytest: <!-- 최종 -->

# 6. 검토자 원문

## 6-1. 첫 검토 (범위 a946d87..594d631)

```
━━━ 검토자 여기부터 ━━━ 범위 a946d87..594d631 · 커밋 2개 · 파일 5개

막아야 함: 없음

운영 수를 직접 세어 봄 (sqlite mode=ro, 새 연결)
  운영 data/app.db — 회의 53 · id 달린 행 50 · 빈 문자열 0 · origin 노션 50 중 id 50 ·
    노션 아닌 회의 3 중 id 0 · 서로 다른 id 10 · 출처 하나에 id 둘 이상 0 ·
    id 하나에 출처 둘 이상 0 · 노션인데 id 없음 0
  사본 app-20260916-004600.db — 회의 53 · id 달린 행 0 (채우기 전 판이 맞음)
  사본과 운영의 meetings 표에서 notion_page_id 말고 다른 칸은 53행 전부 같음.
    이 스크립트는 그 칸 하나만 바꿨음
  보고 1c 표와 모두 맞음. 수용 기준 나)·다) 는 근거가 있음

[M] 고쳐야 함 — 인계 1) 정리가 덜 됐음 (3b)
  docs/인계.md 1) 「무엇」 줄(대략 108~112행)이 아직 옛 계획을 말함:
    「다음은 외부 id 칸(노션 페이지 id · …)을 만들고 그 열쇠로 들여오는 도막」
    「대조가 아니라 다시 들여오기」
  노션 페이지 id 칸은 이미 만들어져 채워졌고, 사람은 업무를 「다시 들여오기」(ㄱ1)가
  아니라 ㄱ2(짝은 살리고 노션에만 있는 것만 새로)로 정했음. 이번 diff 가 건드린 줄은
  아니지만, 3b 가 1) 을 정리하라는 지시였음. 새 줄 「업무(ㄱ2) — 남음」 바로 위에
  이것과 반대되는 말이 남아 있음.
  같은 1) 안에서 120행 「다음은 업무 ㄱ2 — …」 와 새 줄 「업무(ㄱ2) — 남음 · 다음 판」 이
  같은 사실을 두 번 적고 있음 (검토-원칙 3·4). 옛 줄에 꼬리를 단 방식은 괜찮지만,
  이 줄은 새 줄이 맡으면 됨.

[L] 봐 둘 것 — 사본을 지키는판에 안 넣은 판단
  11-2 백업 절과 맞음. 그 규칙은 「문서가 되돌리는 자리로 가리키는 사본」 에만
  걸리는데, 인계는 되돌리기를 「칸을 NULL 로」 라고 적었고 사본 이름은 적지 않았음.
  test_stage52 는 docs/review/ 를 안 보므로, 최근.md 41행에 이름이 있어도 안 걸림.
  얼린 뒤에도 같음. 사본은 성한 판 30개가 차면 지워지는데, 되돌리는 길이 사본에
  기대지 않으므로 잃는 것이 없음.

[L] 봐 둘 것 — 「칸을 NULL 로」 되돌리기 (인계 124행 · 보고 43행)
  사실은 맞음. 사본에서 그 칸은 53행 전부 NULL 이었고, 다른 칸은 바뀐 것이 없음.
  다만 두 가지:
  - 대상이 적혀 있지 않음. 「그 칸을 NULL 로」 는 표 전체로 읽힘. 지금은 결과가 같지만,
    ㄱ2 판이나 앞으로 다른 길로 id 가 붙으면 달라짐. 「origin 노션인 회의의 그 칸」 처럼
    범위를 적는 편이 안전함
  - 채운 일이 활동 기록에 안 남았음 (activity_logs 는 사본 65 · 운영 65 로 같음).
    같은 꼴의 재정들여오기는 「재정_들여오기」 를 남김. 이 스크립트는 앞 판에서 검토를
    거쳤으니 뒤집자는 말이 아님. 다만 되돌려도 흔적이 없고, 보고도 이 사실을 적지 않았음

[L] 봐 둘 것 — 수용 기준 가) 를 읽은 방식 (보고 77행)
  실행 뒤 미리보기는 정의상 「채울 0」 이라, 문자 그대로 「같다」 가 될 수 없음.
  보고는 가) 를 셋으로 읽었음: 실행 전 미리보기 = 앞 판 · 실행 때 찍힌 계획 = 실행 전 ·
  실행 뒤 이미 50 + 채울 0 = 합이 같음. 합리적이고 드러내 적었음.
  다만 실행 뒤 수는 1d 에 「이미 50 · 채울 0」 두 줄만 있음. 페이지 10 · 짝 10 ·
  짝 없는 페이지 0 · 노션 회의 50 · 안 건드림 3 이 실행 뒤에도 같았는지는 표에 없음.
  「실행 전후」 를 줄마다 보이려면 1a 표에 「실행 뒤」 칸 하나면 됨.
  이 해석이 맞는지는 사람에게 묻는 것에 넣음.

[L] 봐 둘 것 — CLAUDE.md 13장 날짜 표기 (CLAUDE.md 4396행)
  「id 가 채워졌습니다(2026-09-15 · 운영 실행은 자정을 넘긴 2026-09-16)」
  사본 이름(20260916-004600)과 인계(운영 실행 2026-09-16)와는 맞음.
  다만 괄호 첫 날짜가 「채워졌습니다」 바로 뒤에 붙어 있어, 채운 날이 09-15 로 읽혔다가
  곧바로 뒤집힘. 09-15 는 「대조가 끝났다」 의 날짜이므로 그쪽에 붙이면 한 번에 읽힘.
  지시문이 준 날짜를 살리면서 실제 실행 시각도 적은 판단 자체는 괜찮음.

[L] 봐 둘 것 — BC-a 내용 (docs/봐둘것.md 끝)
  건수로만 확인함 (값은 옮기지 않음). 저장소 밖 페이지 목록의 제목과 id 를 git 이력 전체와
  견줌. 제목 둘이 0f5dad4 에서 들어오고 6790d0d 에서 빠짐. 그사이 746bf5d 에도 둘이 있음.
  HEAD 에는 제목도 id 도 0. 페이지 id 는 이력 어디에도 없음.
  「사람 이름 · 연락처 · 페이지 id 없음」 도 맞음.
  작은 것 하나: 「초판(0f5dad4 · 746bf5d)」 에서 746bf5d 는 초판이 아니라 그 값을 그대로
  갖고 있던 다음 판임.
  입구 분석(check_names 는 페이지 제목을 안 봄)은 맞음. 넓힐지는 사람이 정할 일이라 적어만
  둔 것이 맞음.

[L] 봐 둘 것 — 11-3 절차
  - 보고 머리 「커밋: a946d87 다음 — 이 판의 커밋들」 은 짧은 해시가 아님 (11-3 4단계 머리 세 줄).
    커밋은 이미 594d631 로 섰음
  - 5장의 「아래 줄」 셋과 6장 「검토 뒤 여기에 싣는다」 는 아직 안 채움. 라)·마) 가
    「채우기 전」 인 것과 짝이 맞고, 보고를 다 채운 뒤 글검사와 pytest 를 다시 돌리면 됨
    (11-3 4단계). 지금 글검사를 돌려 보니 7/7 통과였고, check_placeholder 도 이 꼴은
    잡지 않았음. 그러니 다 채웠는지는 기계가 아니라 사람이 봐야 함
  - 지시문 대조 표: 1a~3c 와 마무리 줄이 모두 있음. 판정과 본문도 어긋나지 않음 (8-1 충족)
  - 사람칸: 다섯 줄 · 순서 · 값 모두 있음 (9 충족)
  - 고치기 전 훑기: 있음 (8-2). 다만 「그 값을 세어 둔 문서」 에 인계 1) 을 적어 놓고,
    같은 절의 「무엇」 줄을 못 봤음 (위 [M])
  - 얼린 판 docs/review/2026-09-15-노션대조2판.md 는 a946d87 의 최근.md 와 blob 해시가 같음.
    한 글자도 안 고침
  - 재시작 생략: app/·templates/·static/ 이 이 범위에서 안 바뀌었음 (git diff 로 확인).
    생략한 까닭도 적었음. 맞음

사람에게 묻는 것
  1. 수용 기준 가) 「실행 전후 미리보기 수가 같다」 를 「실행 전 = 앞 판, 실행 뒤는 이미 + 채울 의
     합이 같다」 로 읽은 것이 뜻한 바인가
  2. 회의록 id 채우기를 활동 기록에 안 남긴 채 둘 것인가 (재정 들여오기는 남김)
  3. 페이지 제목처럼 사람 이름이 아닌 원본 값도 커밋 전 검사가 보게 넓힐 것인가 (BC-a)

━━━ 검토자 여기까지 ━━━
```

## 6-2. 첫 검토를 받아 고친 것

| 번호 | 받았나 | 고친 곳 |
|---|---|---|
| [M] 인계 1) | 받음 | 「무엇」 줄에 「사람이 다시 정함」 꼬리 · 2판 줄의 「다음은 업무 ㄱ2」 를 새 줄로 넘김 |
| [L] 지키는판 | 받음(바꿀 것 없음) | — |
| [L] NULL 범위 · 활동 기록 | 받음 | 인계와 보고 1장에 범위(origin 노션)와 「활동 기록에 안 남았다」 · 사람칸 「정할 것」 |
| [L] 가) 읽기 | 받음 | 1장 1a 표에 「실행 뒤」 칸 · 4장 가) 줄 · 사람칸 「정할 것」 |
| [L] 13장 날짜 | 받음 | 「대조가 끝났고(2026-09-15) id 가 채워졌습니다(운영 실행 2026-09-16)」 |
| [L] BC-a | 받음 | 「초판(`0f5dad4`)과 그 값을 그대로 가진 다음 커밋(`746bf5d`)」 |
| [L] 11-3 | 받음 | 머리 커밋 줄을 짧은 해시로 · 5 · 6장을 채움 |

사람에게 묻는 것 셋은 사람칸 「정할 것」 에 올렸다.

## 6-3. 두 번째 검토 (검토 뒤 고친 차이)

두 번째 검토의 답을 여기에 싣는다.
