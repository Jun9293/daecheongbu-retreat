# 검토용 보고

작성 시각: 2026-09-05 07:26 (KST)
커밋: f37674a 기준 (브랜치 `main` · 커밋 뒤 이 줄을 그 커밋으로 맞춥니다)
작업: 미뤄 둔 목록으로 가는 길을 안 덮이는 자리에 두고, 재시작 판단을 커밋 기준으로

---

## 요약

```
909 passed
```

문서만 고쳤습니다 — `CLAUDE.md` · `docs/봐둘것.md` · `docs/review/최근.md`.

---

## 왜

`docs/봐둘것.md` 에 열한 개를 넘겨 두었는데, **그 파일을 가리키는 곳이
`docs/review/최근.md` 하나뿐**이었습니다. 그 파일은 11-3 이
**매번 이번 작업으로 덮어쓴다**고 정한 것이라, 다음 판이 덮는 순간
목록으로 가는 길이 사라지고 거기 넘겨 둔 것을 통째로 잃습니다.

4-12 의 「보이지 않으면 없는 것과 같습니다」 자리입니다.

**`docs/이름-남은곳.txt` 와 뜻은 같지만 살아남는 방식이 다릅니다.**
그쪽이 잊히지 않는 이유는 목록이어서가 아니라 `check_names` 가 돌
때마다 끝에 「아직 안 고친 곳 N곳」 을 찍기 때문입니다. `봐둘것.md`
에는 그런 자리가 없었습니다.

---

## 1. 11-3 되짚는 목록이 가리킵니다

**검사 출력에 얹지 않았습니다** — 실명 검사에 성격이 다른 것을 붙이는
일입니다. 되짚는 목록은 **안 덮이고 마칠 때마다 읽는 자리**입니다.

```
**고치지 않기로 한 것은 `docs/봐둘것.md` 에 있습니다.** 무게가
「봐 둘 것」 이거나, 지금 새는 곳이 없어 미룬 것들입니다.
**그 언저리를 손댈 때 먼저 엽니다** — 미룬 것을 다시 만나고도
모르면 같은 것을 두 번 고칩니다.
```

왜 그 자리여야 하는지도 인용으로 함께 적었습니다 — 다음에 누가
「검사 출력에 붙이면 되지 않나」 로 되돌리지 않도록.

`봐둘것.md` 머리의 **「`이름-남은곳.txt` 와 같은 성격입니다」 를
고쳤습니다.** 뜻은 같지만 **살아남는 방식이 다르다**는 것을 적고,
처음에 그 차이를 못 보고 「같은 성격」 이라고만 적었던 것도 남겼습니다.

---

## 2. 11-3 2단계를 커밋 기준으로

지금 문장이 **판 기준으로 읽혔습니다.** 지난 판이 실제로 그렇게
갈렸습니다 — 판에서는 `app/` 이 안 바뀌었는데 커밋 `d615ae2` 에는
`app/domain/suggest.py` 와 `app/templates/expenses.html` 이 들어
있습니다. **커밋을 여는 사람은 판이 아니라 커밋을 봅니다.**

```
**커밋 기준으로 봅니다.** 판에서 안 바뀌었어도 이번에 내는 커밋에
`app/` · `templates/` · `static/` 이 들어 있으면 건너뛰지 않습니다 —
앞선 판에서 확인했더라도, **그 커밋을 여는 사람은 판을 모릅니다.**
앞서 확인한 것이면 **그 PID 와 시각을 다시 적습니다.**
```

---

## 3. B5 자리를 채웠습니다

번호만 비어 있으면 **빠뜨린 것과 구별되지 않습니다.**
「커밋 해시였고 그 판에서 처리했다」 를 그 자리에 적었습니다.

**G-b 에도 한 줄 붙였습니다.** 이번 변경으로 해소된 항목이라
그대로 두면 「가리키는 곳이 최근.md 하나뿐」 이 **옛말**이 됩니다
(11-3 3단계). 아래 본문은 무엇이 문제였는지의 기록으로 남깁니다.

---

## 수용 기준

| # | 기준 | 결과 |
|---|---|---|
| 1 | 11-3 되짚는 목록이 `봐둘것.md` 를 가리킨다 | 통과 |
| 2 | `봐둘것.md` 머리가 `이름-남은곳.txt` 와 어떻게 다른지 적는다 | 통과 |
| 3 | 11-3 2단계가 커밋 기준이라고 말한다 | 통과 |
| 4 | `봐둘것.md` 에 B5 자리가 채워졌다 | 통과 |
| 5 | 바뀐 파일이 셋뿐이다 | 통과 |
| 6 | pytest 전체 통과 (옵션 없이) | 통과 (`909 passed`) |
| 7 | 덩어리에 경계 표시 | 통과 |

---

## 재시작 (11-3 2단계)

**건너뛰었습니다.** 문서 셋만 고쳤고 서버가 읽는 `app/` · `templates/`
· `static/` 은 하나도 안 바뀌었습니다 — **이번에 내는 커밋에도**
들어 있지 않습니다(바로 위에서 고친 그 기준으로 봤습니다).

검토자도 부르지 않았습니다 — 세 줄짜리 문서 수정입니다.

---

## 바뀐 파일

```
CLAUDE.md
docs/봐둘것.md
docs/review/최근.md
```

---

## 이번에 걸린 것

1. **미룬 것을 목록에 넘겨 놓고, 그 목록으로 가는 길을 매번 덮이는
   파일에 두었습니다.** 목록을 만든 판에서 「`이름-남은곳.txt` 와 같은
   성격」 이라고 적었는데, **무엇이 그것을 살려 두는지는 안 봤습니다** —
   목록이라서가 아니라 검사가 매번 찍기 때문이었습니다.
   **넘겨 두는 것만으로는 안 되고 다시 만나는 자리가 있어야 합니다**
2. **규칙이 판 기준으로 읽혔습니다.** 「서버가 읽는 것이 안 바뀌었으면
   건너뛴다」 가 맞는 말인데, **누구 기준으로 안 바뀐 것인지**가 없었습니다.
   판에서는 안 바뀌었어도 커밋에는 들어 있을 수 있고, 그 커밋을 여는
   사람은 판을 모릅니다
