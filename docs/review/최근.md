# 검토용 보고 — 검사 둘의 범위 맞추기 (+ 남은 후속)

작성 시각: 2026-09-07 (KST)
커밋: 127334d (작업 커밋 셋의 끝 — e7282c7 · 323d146 · 127334d)
작업: Y-a(두 검사의 찾는 형태를 한 곳에서) · 제외칸에 근거(이유+쓰는 곳) ·
11-2 원칙 한 줄 · 잔손질(3-b·3-c 확인·주석)

---

## 0. 먼저 센 것 (지시문 0번)

| 잰 것 | 값 |
|---|---|
| 두 검사가 찾는 형태 | **check_names(문서)**: 대응표 표기 원문의 느슨한 substring — 이름·장소·번호(숫자 그대로)만. 변형(3-4-4·공백)은 못 봤다. **check_dev_db(게이트)**: 이름·장소는 경계 정규식+M-가지, 번호는 숫자 그대로 + 3-4-4(**11자리 숫자로 적혔을 때만** — 하이픈으로 적으면 숫자형이 빠짐). **차이 = 「한쪽만 보는 것」**: 3-4-4·공백형은 게이트만, 그것도 대응표 표기법에 따라 흔들렸다. 개편 후: 변형을 `anonymize.번호변형들` **한 곳**에서 만들어 둘 다 같은 것을 본다(숫자·3-4-4·공백, 표기법 무관) |
| 저장소의 3-4-4 꼴 숫자 | `0\d\d-\d{3,4}-\d{4}` 로 재서 **17곳 · 4파일** — 전부 코드·시험의 **가짜 예시**(입력 정규화 독스트링 1 · 시험 픽스처 16. 일반 3-4-4 로 넓히면 seed 의 가짜 계좌도 잡힌다). 하중을 지는 사실은 이것이다: **실번호의 3-4-4 는 저장소에 없다.** (이 보고 자신도 커밋되면 가짜 예시 한 곳을 docs/ 에 더한다) |

## 1. 두 검사가 같은 것을 찾게 (Y-a)

- **`anonymize.번호변형들(표기)` 하나가 변형을 만든다** — 숫자 그대로 ·
  3-4-4 · 공백 낀 것. 하이픈·공백을 섞어 적어도 숫자를 먼저 모으므로
  같은 변형이 나온다(검토자 5-a 닫힘). 문서 검사는 `표기들()`(변형 포함)로
  받고, 게이트는 직접 부른다 — 3-4-4 를 제 손으로 조립하는 곳이 검사들에
  없다(test10_v01 이 grep 수준으로 지킨다)
- 시험: `test10_v02`(하이픈으로 적어도 두 형이 다 나온다 — 합성 번호로
  규칙 자체를 잰다 · 11자리가 아니면 변형을 지어내지 않는다) ·
  `test10_v03`(문서에 3-4-4 실번호를 심으면 걸리고 — 막는 쪽 — 가명
  번호는 통과) · `test10_v04`(**두 검사가 같은 문자열 여섯에 같은 판정** —
  실번호 세 변형 둘 다 걸리고, 가명 세 변형 둘 다 조용)
- check_names 의 표기 수가 39 → **41** (번호 변형 둘 추가). 걸림 0 그대로

## 2. 제외 목록에 근거를

- **제외칸 52개 전부에 「이유 — 근거: 파일:줄」** — 그 값을 만드는(또는
  값의 목록을 정하는) 자리를 실제로 열어 보고 적었다. import_batch 가
  잘못 제외됐던 이유가 정확히 그 자리를 안 열어 본 것이었다
- `test10_e01` — 근거 자리(파일:줄 꼴)가 빈 제외칸이 있으면 그 칸 이름으로
  실패한다. 자리만 채우고 거짓을 적는 것은 못 막지만, 빈 채로 늘어나는
  것은 막는다
- **import_batch 는 지난 판 검토 반영에서 이미 제외에서 빠졌다** — 이번
  판은 확인만. 볼칸 재실측: **모델 글자 칸 127 = 볼칸 75 + 제외 52**
  (test9_g01·g02 가 실측으로 지킨다)

## 3. 문구

- **3-a** — CLAUDE.md 11-2 에 원칙 한 줄: 「막는 것은 기본이 전부이고,
  사람이 적는 것은 안 보는 이유다. 볼 목록은 세 번 불완전했다 — 목록을
  넓히는 것으로는 네 번째가 온다」
- **3-b** — 최근.md 의 3-b 「확인만」 표기는 **지난 판 검토 반영에서 이미
  고쳐졌다**(이번 판은 이 보고 자체가 새로 쓰였으므로 해당 절이 없다)
- **3-c** — stage9 의 안 쓰는 임포트는 지난 판에 정리됐고(확인만),
  「&u=」 낱말 시험에는 **가짜 빨강 쪽이라 안전하다**는 주석 한 줄을
  더했다 — 걸리면 사람이 그 자리로 가서 주석을 다르게 적으면 된다
- **A-18 옛말 grep** — 「11자리 숫자일 때만」·「게이트에만 있다」 류의 옛
  서술이 규칙 자리에 없다(봐둘것 Y-a 원문은 취소선 밑 역사 기록)

## 4. 봐둘것

- Y-a — **처리함** (위 1)

## 검증

- **drawer.js 세 화면** (dev 서버 재기동 — 통일된 변형으로 게이트를
  지나 뜬 것 확인 겸 · 1600×900): 보드 **79/79** · 달력(?scope=all)
  **82/82** · 목록 **86/86** — 전부 통과
- **pytest** (옵션 없이): `1088 passed` — 검토 반영 전후 두 번의 전체
  실행이 같은 수로 초록
- **check_names**: 실명 41개(장소 1 · 번호 변형 포함) · 걸린 것 없음 ·
  아직 안 고친 곳 0곳 · 대응표에 없는 표기 0개 · 이미지 14개 전부
  경로·해시 일치 · exit 0
- **check_dev_db**(가명 dev DB): exit 0 — 변형 통일 후 재실행, 오탐 0
- **anonymize 미리보기**: 0개 파일 · 0곳

## 운영 반영 (12-1)

**건너뛴다** — 이번 커밋에 `app/`·`templates/`·`static/` 이 없다
(scripts·tests·docs·CLAUDE.md 뿐). 서버가 읽는 것이 안 바뀌었으므로
11-3 대로 재시작하지 않고, 그 사실과 이유를 여기 적는다.

## 바뀐 파일 (git diff --name-only + 신규)

```
CLAUDE.md
docs/review/최근.md
docs/봐둘것.md
scripts/anonymize.py
scripts/check_dev_db.py
tests/test_stage9.py
tests/test_stage10.py                 (신규)
```

## 판단이 들어간 자리 (발췌)

- **표기들() 에 변형을 합친 것** — 문서 검사에 별도 패턴 경로를 새로
  두는 대신, 이미 있는 느슨한 substring 길에 변형 문자열을 실었다.
  검사 코드가 안 바뀌고 목록만 길어지므로 두 검사의 구조 차이(느슨/경계)는
  그대로 두되 **무엇을 찾는가**만 한 곳이 됐다
- **공백형(010 1234 5678)도 만든 것** — 지시문이 「공백 낀 것」 을 셋에
  넣었다. 카카오톡에서 복사한 번호가 그 모양으로 온다
- **제외 근거의 줄 번호** — 밀리면 목록이 낡지만, k05 와 같은 판단이다:
  낡아서 사람이 다시 보는 비용이, 근거 없이 쌓이는 비용보다 싸다.
  e01 은 자리의 유무만 재고 줄의 정확성은 사람 몫으로 남긴다
- **가짜 예시 번호(010-1234-5678)를 시험에 쓴 것** — 4-12 가 뺀 것은
  문서·도움말의 예시 번호다(그대로 입력해 계정이 생겼다). 시험 코드는
  입력 화면이 아니고, 대응표에 없는 수라 검사에도 안 걸린다

## 검토자 (커밋 전 · 원문 그대로 — 익명화를 지나며, 치환이 있으면 그 자리에 한 줄 적는다)

(이 원문은 anonymize 를 지났고, 치환된 곳은 없다 — 검토자가 실명·실장소·
실번호를 적지 않았다.)

```
━━━ 검토자 여기부터 ━━━ 범위 e13e83d..작업트리(미커밋) · 파일 7개 (신규 tests/test_stage10.py 포함)

무엇을 봤나 — git diff HEAD 전체, tests/test_stage10.py 전문,
scripts/anonymize.py·check_names.py·check_dev_db.py 전문, 제외칸 52개의
근거 인용줄 전수 대조(파일 열어 해당 줄 출력), FileVersion·tasks JSON
컬럼·ScheduleItem 의 쓰는 곳 grep, 저장소의 3-4-4 꼴 숫자 grep,
부분 스위트 재실행(stage9+10+check_names+anonymize_map: 39 passed),
check_names 실행(표기 41 · 걸림 0 · 표밖 0 · 이미지 14 확인됨 · exit 0),
check_dev_db 실행(exit 0). 전체 pytest 는 구현자 쪽에서 돌고 있어 여기서는
안 돌렸다.

────────────────────────────────────────────────
고쳐야 함

1. 제외칸 주석의 「쓰는 곳 없음」 이 두 칸에서 거짓이다
   - scripts/check_dev_db.py:68 file_versions.stored_name —
     「옛 표 — 쓰는 곳 없음, 행만 남음」 이라 적었으나 seed.py:372·410 이
     지금도 FileVersion 행을 쓴다(stored_name="demo-poster-v{n}.png" 등).
     개발 DB 는 seed 로 만들어지므로 게이트가 보는 바로 그 DB 에 이 칸이
     채워진다. 「임의 난수 파일명」 도 이 행들에는 안 맞는다 — 코드가 박은
     데모 이름이다(실명이 들어올 수 없다는 결론 자체는 맞다). 근거
     app/models.py:520 은 class FileVersion 정의 줄이지 쓰는 곳이 아니다.
     이 목록 머리 주석이 「쓰는 곳을 실제로 열어 보고 적는다」 를 이번 판의
     규칙으로 세웠는데, 그 규칙을 세운 커밋 안에서 어긋난다
   - scripts/check_dev_db.py:107-108 tasks.blocked_by_task_ids ·
     related_department_ids — 「쓰는 곳 없음」 이나 seed.py:229·241-242 와
     app/routers/meetings.py:225-226(회의 항목→할 일 전환, 살아 있는 라우트)
     이 쓴다. 값은 [] 또는 숫자 id 라 제외 자체는 안전하다 — 틀린 것은
     주장이다. 근거 「app/models.py:26 언저리」 도 어긋난다 — 26줄은
     TASK_STATUSES 이고 이 두 컬럼은 models.py:367-368 이다

2. scripts/check_dev_db.py:96-97 schedule_items start/end_time 의 근거
   「seed.py:260 언저리」 가 다른 자리를 가리킨다 — 260줄은 ChecklistItem
   의 label 이고, ScheduleItem 을 쓰는 곳은 seed.py:205-211
   (start_time=start 가 210줄). e01 은 자리의 유무만 재므로 이런 어긋남은
   기계가 못 잡는다 — 사람이 대조해 달라던 바로 그 지점이다.
   (그 밖의 근거 약 45곳은 표본 전수로 대조했고 맞았다 — deps 77·85·86,
   seed 143·199, settings 390, setup 333, drafts 44·51·57·135, expenses 76,
   reviews 177·185·239, auth 76, models 36·416·937·942·1008·1010,
   import_meetings 296, meetings 403·413·419·471, notify 306,
   notifications 137·141·143·272, live 52·95, routers/live 306,
   push 102·104·112, attachments 223, library 345·588언저리·590·594·705,
   board 816·950, permissions 8)

3. scripts/anonymize.py:137-144 번호변형들 — 11자리가 아니면 원표기를
   버린다. 옛 코드는 두 검사 다 대응표의 원문 표기를 그대로 찾았다
   (check_names 는 raw a, 게이트는 re.escape(num)). 새 코드는 숫자를 모아
   11자리일 때만 세 형을 내므로, 나중에 누가 지역번호를 하이픈 붙여
   대응표에 적으면(02-XXX-XXXX 꼴, 숫자 9-10자리) 하이픈 원문형을 아무
   검사도 안 찾는다 — 이전보다 좁아진다. Y-a 가 막으려던 「대응표 표기법에
   따라 흔들린다」 와 같은 모양이 한 칸 옆에 남았다. 지금 대응표는 11자리
   번호 하나뿐이라(39→41 로 역산) 오늘 새는 곳은 없다. 숫자와 다른
   원표기를 변형 목록에 보태면 닫힌다(v02 의 기대값도 그에 맞춰 조정)

4. docs/review/최근.md:15 「docs/ 에는 0곳」 이 보고 자신과 어긋난다 —
   같은 파일 89줄이 가짜 예시 번호를 3-4-4 로 적어서, 이 보고가 커밋되는
   순간 docs/ 의 3-4-4 는 0곳이 아니다(가짜라 새는 것은 아니고, 측정
   문장이 거짓이 되는 문제). 「17곳 · 4파일」 도 010 한정 정규식이면
   맞지만 일반 3-4-4 로 재면 seed_data.py:591 의 가짜 계좌가 더 잡힌다 —
   무엇으로 쟀는지 한 줄 붙이거나, 하중을 지는 문장(「실번호의 3-4-4 는
   저장소에 없다」)만 남기는 쪽이 안전하다

5. CLAUDE.md:2307-2311 — 새로 넣은 원칙 문단이 「볼 목록은 세 번
   불완전했습니다(사람 이름만 · 텍스트만 · 칸 일부만)」 를 적고, 바로 다음
   문단이 같은 문장을 같은 세 예로 다시 적는다. 같은 사실이 이웃한 두
   문단에 두 번 — 이 문서가 스스로 금하는 모양이다. 한 문단으로 합치거나
   새 줄에서는 반복 절을 빼면 된다

────────────────────────────────────────────────
봐 둘 것

6. scripts/anonymize.py swap()(263줄~) 은 변형을 안 바꾼다 — phones 의
   원표기만 치환하므로, 3-4-4 실번호가 check_names 에 걸려도 anonymize
   미리보기·--apply 에는 안 나오고 사람이 손으로 고친다. 이름의 엄격/느슨
   분리와 같은 설계라 틀린 것은 아니지만, 번호는 이름과 달리 치환해도
   낱말이 깨질 위험이 없다 — 다음에 실제로 걸렸을 때 「미리보기에 왜 안
   나오나」 를 겪고 나서 고치게 될 자리다

7. docs/review/최근.md:70-72 운영 반영 자리표시자가 재시작을 전제로 적혀
   있는데, 이번 diff 에는 app/·templates/·static/ 이 없다(scripts·tests·
   docs·CLAUDE.md 뿐). 11-3 대로면 건너뛰고 「건너뛴 사실과 이유 한 줄」
   이 맞는 채움새다 — 채울 때 방향만 맞추면 된다

8. scripts/anonymize.py:148 표기들() 독스트링 「긴 것부터」 — 번호 변형
   꼬리(11자→13자 순)는 길이순이 아니다. 찾기에는 순서가 무관해 실해는
   없으나, 이 주석을 믿고 순서에 기대는 코드가 생기면 그때 어긋난다

────────────────────────────────────────────────
괜찮음 (지시문이 물은 곳)

- v04 의 일반화 — 게이트는 볼칸마다 같은 패턴을 적용하므로 칸 선택이
  판정을 바꾸지 않는다. meetings.body 는 제외칸에 없어 볼칸이고, 만약
  제외칸에 든 칸을 골랐다면 실번호 단언이 빨개져서(조용한 초록이 아니라)
  드러난다. 일반화 성립
- 공백형 — v04 가 실형·가형 각 세 변형을 문서 검사와 게이트 양쪽에 다
  넣어 잰다(v03 은 3-4-4·숫자형만이지만 v04 가 덮는다). 문서 검사의
  줄 단위 find 로 공백형이 실제로 잡히는 것을 시험이 실행으로 확인한다
- 숫자 없는 표기 — [표기] 그대로 반환, 표기들() 의 phones 에는 숫자가
  들어오므로 닿지 않는 경계다. 10자리는 변형을 안 지어낸다(v02) —
  다만 위 3번의 원표기 소실이 그 옆에 있다
- 표기들() 확장의 파급 — 표밖담당자() 의 있는표기 는 1~5글자 조각만
  견주므로 11자 이상인 번호 변형이 닿지 않고(실행 결과 표밖 0 · 담당자
  칸 482 그대로), 가명(변형)은 None 이라 test_anonymize_map 의 길이
  검사에서 걸러진다. swap 은 표기들() 을 안 쓴다. 기존 시험에 표기 수를
  박은 곳 없음. 부분 스위트 39 passed 재현
- test10 넷의 짜임 — v03(막히는 쪽+뚫리는 쪽), v04(두 검사 동판정,
  실·가 양방향), e01(빈 근거 자리로 늘어나는 것을 막고 거짓 근거는 사람
  몫이라고 스스로 적음), 대응표 없는 컴퓨터는 skip. 11-3 의 세 벌
  (①막힘 ②통과 ③실제로 봄)이 갖춰졌다
- 실명·토큰 — 이번 diff 와 신규 파일에 실명·실번호·실장소 없음.
  시험의 010-1234-5678 은 대응표 밖 가짜이고 4-12 가 뺀 것은 문서·도움말
  쪽이라는 판단 발췌도 보고에 있다. check_names exit 0 재현

────────────────────────────────────────────────
사람에게 묻는 것

- 대응표에 앞으로 하이픈 표기·지역번호가 들어올 수 있는가? 있다면 3번
  (원표기 보존)을 이번에 닫을지, 봐둘것으로 미룰지
- 「쓰는 곳 없음」 두 칸(1번)의 문구를 「seed 와 옛 라우트만 쓴다 —
  값은 숫자 id」 류로 고칠지, 아예 meetings.py 의 옛 Task 생성 경로가
  아직 살아 있는 것이 맞는지(0장 원칙상 표는 남기되, 쓰는 라우트가
  살아 있는 것과 「옛 표」 표기가 어울리는지)는 구현자·사람이 정할 일

━━━ 검토자 여기까지 ━━━
```

### 반영

- **고쳐야 함 1 — 반영.** 두 칸의 거짓 문구를 사실로: file_versions 는
  「코드가 박는 데모 파일명 — seed.py:372」, tasks 둘은 「seed 와 할 일
  전환만 쓴다 — seed.py:241 · meetings.py:225」. 제외 결론은 그대로
  (숫자 id·코드 고정 값 — 실명이 들어올 입력 경로 없음)
- **고쳐야 함 2 — 반영.** schedule_items 근거를 seed.py:210 으로
- **고쳐야 함 3 — 반영.** 번호변형들이 **원표기를 버리지 않는다** —
  숫자와 다르면 변형 목록에 보탠다. v02 에 하이픈 지역번호 사례를 더했다
- **고쳐야 함 4 — 반영.** 0-b 문장에 잰 정규식을 명시하고, 하중을 지는
  문장(실번호의 3-4-4 는 저장소에 없다)을 축으로 다시 적었다 — 이 보고
  자신이 더하는 가짜 예시 한 곳도 밝혔다
- **고쳐야 함 5 — 반영.** 11-2 의 이웃 문단 중복을 걷었다 — 원칙 문단이
  세 예를 지고, 다음 문단은 「그 세 번은 이랬습니다」 로 잇는다
- **봐 둘 것 7 — 반영.** 운영 반영 절을 「건너뛴다 + 이유」 로 —
  이번 커밋에 서버가 읽는 것이 없다. **8 — 반영**(표기들 독스트링).
  **6 — docs/봐둘것.md 에 Z-a 로**(바꾸기는 변형을 안 바꿈 — 다음에
  실제로 걸리기 전에 붙일 자리)
- **사람에게 묻는 것 1** — 원표기 보존(3번 반영)으로 표기법이 무엇이든
  최소한 그 모양 그대로는 찾는다. 지역번호가 실제로 들어오면 그때 변형
  규칙(3-3-4 등)을 넓힌다. **2** — 문구를 사실로 고치는 쪽을 택했고,
  옛 Task 생성 경로의 존폐는 이 판 범위 밖이라 사람의 자리로 남긴다
