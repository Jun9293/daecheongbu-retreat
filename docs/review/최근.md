작성 시각: 2026-09-10 14:40 (KST)
커밋: ec6bf70 (그 위에 이 보고를 담는 커밋 하나)
작업: 봐둘것 AN-a · AN-b 닫기 (문서만)

---

# 1. 한 것

사람이 정한 둘을 문서에 옮겼습니다. **코드·시험은 안 건드렸습니다** — diff 에 `app/` · `tests/` 없음.

| 지시 | 한 것 | 자리 |
|---|---|---|
| 1 AN-a 「만들기 폼은 예외」 | CLAUDE.md 4-9 「회의록 본문은 켭니다」 문단에 한 줄 — 「회의록 만들기 폼의 본문은 예외 — 접힌 상자 안이라 처음 잴 때 0 으로 재므로 켜지 않습니다」. 봐둘것 AN-a 를 「정함 · 2026-09-10 · 만들기 폼 예외」 로 닫고 본문 끝에 정한 것을 적음 | `CLAUDE.md` 4-9 · `docs/봐둘것.md` |
| 2 AN-b 「둔다」 | 봐둘것 AN-b 를 「정함 · 2026-09-10 · 그대로 둔다(말만 하는 수)」 로 닫고 본문 끝에 정한 것을 적음 | `docs/봐둘것.md` |
| 3 인계.md | 갱신 줄에 「AN-a·AN-b 닫음 — 다음 채팅은 7번(회차별 비품)부터 시작한다」. **미결 목록에는 AN-a·AN-b 가 애초에 없었습니다**(판 2 가 봐둘것에만 적었음) — 뺄 것이 없어 갱신 줄로만 말합니다. 「할 것」 머리의 「다음은 7번이다」 는 그대로 | `docs/인계.md` |
| 3 AM-f | 「작업 스케줄러 이력 켬(2026-09-10) — 다음에 혼자 다시 뜨면 그 이력을 본다」 한 줄 (이력 로그 이름을 함께) | `docs/봐둘것.md` AM-f 끝 |

# 2. 수용 기준

| # | 기준 | 결과 |
|---|---|---|
| 1 | 4-9 한 줄 | **통과** — 4-9 의 autogrow 문단 |
| 2 | AN-a · AN-b 닫힘 | **통과** — 제목 취소선 + 「정함 · 2026-09-10 · …」, 본문 끝에 정한 것 |
| 3 | 인계.md 갱신 | **통과** — 갱신 줄 (미결 목록에 둘이 없었음은 위에) |
| 4 | AM-f 한 줄 | **통과** |
| 5 | 글검사 4/4 | **통과** — 3장 |
| 6 | diff 에 app/ tests/ 없음 | **통과** — 3장의 파일 목록 |

# 3. 잰 것

## 글검사

`scripts/글검사.py` — **4/4 통과** (보고를 쓴 뒤 · 검토자 원문을 넣은 뒤 한 번 더 — 같음).
문서를 읽는 시험 묶음(`test_docs_shell` · `test_check_names` · `test_stage13` · `test_typescale` ·
`test_stage18` · `test_stage23`)도 원문을 넣은 뒤 돌려 **89 passed** — 전체 pytest 는 지시 4 대로
안 돌렸습니다.

| 검사 | 결과 |
|---|---|
| check_names | 실명 42개로 파일 280개 · 낱말 11개 넘김 · 걸린 것 없음 · **아직 안 고친 곳 0곳** · 대응표에 없는 담당자 표기 0개(담당자 칸 482개) · 이미지 18개 전부 사람이 확인함 |
| check_stale | 낡은 표기 108개로 파일 239개 · 넘김 17개 · 옛말 없음 |
| check_named | 문서가 이름한 586개 · 넘김 16개 · 문서에만 있는 이름 없음 |
| check_counts | 파일 235개 · 넘김 13개 · 세어 둔 자리 없음 |

## pytest · 서버 재시작 (11-3 2단계) — 건너뜀

**서버가 읽는 것이 하나도 안 바뀌었습니다** — `app/` · `templates/` · `static/` 이 이번 커밋에
없습니다(문서 셋만). 그래서 재시작을 건너뛰고, pytest 도 돌리지 않았습니다(지시 4).
앞선 확인은 UI 정리 판 2 — PID 21724 → 15976 · 2026-09-10 14:22 · 그 커밋(b053b3c)이 지금
운영에서 도는 코드입니다.

## `docs/checks/drawer.js` — 건너뜀

같은 이유(화면·코드가 안 바뀜). 판 2 의 결과(보드·달력·목록 × 두 계정 · 실패 0)는
`docs/review/2026-09-10-UI정리판2.md` 7장에.

## 바뀐 파일 (`git diff --name-only de4c260..HEAD` — 이 보고를 담는 커밋 앞까지)

```
CLAUDE.md
docs/봐둘것.md
docs/인계.md
```

# 4. 판단이 들어간 자리

1. **인계.md 의 「미결 목록에서 AN-a·AN-b 를 뺀다」** — 목록에 그 둘이 없었습니다. 판 2 는
   봐둘것에만 적고 인계.md 에는 안 올렸습니다. 없는 것을 뺐다고 적지 않고, 갱신 줄에
   「닫음」 과 다음 시작점을 적는 것으로 지시의 뜻(다음 채팅이 열린 줄 알고 다시 보지
   않게)을 채웠습니다.
2. **AM-f 의 한 줄에 이력 로그 이름을 붙였습니다** — 「그 이력」 만 적으면 다음 사람이
   어느 로그인지 다시 찾습니다. 켠 것은 사람이 했고(시스템 설정) 이쪽은 적기만 했습니다.
3. **이 판은 얼리지 않습니다** — 다음 판이 입력으로 쓸 셈·실측이 없습니다(11-3 「모든 판을
   남기는 것이 아닙니다」). 판 2 의 얼린 판이 그대로 남아 있습니다.
4. **(검토 뒤) 봐둘것의 닫는 모양은 파일 관례를 따랐습니다** — 제목 아래 `>` 인용 블록에
   정한 것을 두고, 지시가 말한 본문 끝의 문장도 그대로 두었습니다. 둘이 같은 결정을
   말하므로 라벨과 본문이 갈리지 않습니다.
5. **(검토 뒤) AN-a 예외의 이유 문장은 사람이 지시문에 적은 그대로**(「접힌 상자 안이라
   처음 잴 때 0 으로 재므로」)입니다 — 다른 이유로 바꾸지 않았습니다. 재기를 고친 뒤
   예외를 걷을지는 그때 사람이 정할 일이고, 봐둘것 AN-a 본문에 「켜려면 펼칠 때 다시
   재야 한다」 가 남아 있어 길은 적혀 있습니다.
6. **(검토 뒤) 이력 로그가 실제로 켜졌는지 쟀습니다** — `wevtutil gl` 로 enabled true.
   봐둘것 줄에 「사람이 켬 · 확인」 을 붙였습니다.
7. **(검토 뒤) 14장 listinput 줄을 고쳤습니다** — 요약이 본문(4-9)과 갈리는 그 모양이라
   「회의록 본문(상세 화면)은 켜고, 만들기 폼의 본문과 논의 입력칸은 안 켠다」 로.
8. **예외를 잡는 시험은 안 넣었습니다** — 범위가 문서만입니다(지시 4). 넣는다면
   `test13_w02` 옆에 「meetings.html 에는 `data-autogrow` 가 없다」 한 줄입니다.

# 5. 검토자 원문 (그대로)

```
━━━ 검토자 여기부터 ━━━ 범위 de4c260..작업 트리(커밋 없음) · 파일 4개(보고 포함)

막아야 함
  없음.

고쳐야 함
  1. docs/봐둘것.md:2136 · :2146 — 닫는 모양이 앞의 닫힌 항목 전부와 다르다
     AE-c(1278)부터 AM-e(2096)까지 ~~제목~~ 뒤에 > 인용 블록으로 「무엇으로 정했고
     어디에 적었나」 를 제목 바로 아래에 둔다. AN-a·AN-b 는 인용 블록이 없고
     정한 것이 본문 맨 끝 한 문장(「사람이 정했다 — …」)이다. 파일을 훑는 사람은
     제목 다음 줄을 보고 결과를 읽어 왔는데 여기서만 본문 끝까지 내려가야 한다.
     라벨과 본문이 다른 것을 가리키지는 않는다(둘 다 같은 결정).
     단, 지시가 「본문 끝에 정한 것」 이라고 적었으니 지시 쪽 모양이 파일 관례와
     어긋난 것이다 — 어느 쪽을 따를지는 아래 「사람에게 묻는 것」 1.

  2. CLAUDE.md:3443 (14장 listinput 줄) — 「회의록 본문은 켜고 논의 입력칸은 안 켠다」
     4-9 가 「회의록 만들기 폼의 본문은 예외」 를 얻었으므로 이 요약은 이제
     상세 화면 본문에만 맞다. (4-9) 를 가리키고 있어 옛말 검사에는 안 걸리지만,
     11-3 3단계가 경고한 그 모양(요약이 본문과 갈린다)이다. 14장만 읽고
     meetings.html 에 data-autogrow 를 붙이면 4-9 를 어기는데 아무 검사도 안 잡는다.
     「회의록 본문(상세 화면)은 켜고」 정도면 된다. 코드 쪽 주석(listinput.js:91 ·
     meeting_detail.html:48)은 「논의 입력칸은 안 켠다」 만 말해 어긋나지 않는다.

봐 둘 것
  3. CLAUDE.md:435-437 — 예외의 이유가 「못 켠다」 로 읽힌다
     「접힌 상자 안이라 처음 잴 때 0 으로 재므로 켜지 않습니다」 는 기술적 제약이고,
     봐둘것 AN-a 본문 자신이 「켜려면 펼칠 때 다시 재야 한다」 고 풀 길을 적어 둔다.
     다음 사람이 그 재기를 고치면 「이유가 없어졌다」 며 예외를 걷을 수 있다.
     그리고 그 이유는 판 2 검토자의 문장(2026-09-10-UI정리판2.md:483)에서 온 것이고
     사람이 정한 것은 「예외」 라는 결정이다 — 「사람이 정함」 라벨이 결정에는 맞고
     이유까지 덮는지는 모른다. 0 으로 재는 것 자체는 맞다(listinput.js:97-101 —
     display:none 안에서 scrollHeight 0 → height 0px 가 인라인으로 남는다).

  4. 예외를 잡는 시험이 없다 — tests/test_stage13.py:50-64
     test13_w02 는 meeting_detail 에 있고 drawer 에 없는 것만 본다. meetings.html 에
     없어야 한다는 단언이 없어 4-9 의 새 줄은 글로만 산다(4-0 「규칙만 적어 두면
     새어 들어옵니다」). 범위가 문서만이라 이번에 안 넣은 것은 맞고, 넣는다면 한 줄이다.

  5. docs/인계.md:3 · :27 — 「다음은 7번」 이 두 줄에 있다
     갱신 줄(3)과 「할 것」 머리(27)가 같은 사실을 말한다. 7번이 끝나면 둘 다 고쳐야
     한다. 지시의 뜻(다음 채팅이 열린 줄 알고 다시 보지 않게)은 채웠고, 「미결
     목록에서 뺀다」 를 갱신 줄로 대신한 판단은 맞다 — HEAD 의 인계.md 에도 AN-a·AN-b
     가 없는 것을 확인했다(git show HEAD:docs/인계.md 에 AN- 없음).

  6. docs/봐둘것.md:2133 — 「작업 스케줄러 이력 켬(2026-09-10)」 에 주어가 없다
     바로 위 인용 블록(2116)이 「시스템 설정이라 사람이 켠다」 고 해 이어 읽으면
     사람이 켠 것으로 읽히지만, 굵은 글자 한 줄만 보면 이쪽이 켠 것으로도 읽힌다.
     「(사람이 켬)」 세 글자면 갈린다. 그리고 켜졌다는 것은 사람의 말을 적은 것이지
     잰 것이 아니다(검토-원칙 5) — 아래 묻는 것 3.

  7. docs/review/최근.md:42-47 — pytest 를 안 돌린 것
     재시작 건너뜀 이유는 적혀 있고 맞다(app/·templates/·static/ 없음). 그런데
     11-3 은 「보고를 다 채운 뒤에 검사와 시험을 다시 돌립니다 — 글검사와 pytest 는
     이 파일도 봅니다」 라 문서만 바뀌어도 pytest 는 대상이다. 보고는 「지시 4」 를
     근거로 건너뛰었다고 적었으니 못 한 것을 적은 것은 맞다.
     내가 문서를 읽는 시험만 골라 돌렸다 — test_docs_shell · test_check_names ·
     test_stage13 · test_typescale · test_stage18 · test_stage23: 89 passed.
     REVIEW_PLACEHOLDER 에 이 원문을 넣은 뒤 글검사와 위 시험을 한 번 더 돌려야
     한다(11-3 「보고를 다 채운 뒤에」).

확인한 것
  - 실명·연락처·토큰·바깥 주소: diff 에 없음. 있는 숫자는 PID 와 커밋 짧은 해시뿐이고
    `Microsoft-Windows-TaskScheduler/Operational` 은 윈도우 로그 채널 이름이다.
  - 글검사: 내가 다시 돌려 check_names · check_stale · check_named · check_counts
    전부 exit 0 (보고의 4/4 와 같음). 단, 위 7 의 이유로 원문을 넣은 뒤 재실행 필요.
  - diff 에 app/ tests/ 없음: git diff --stat 으로 확인(CLAUDE.md · 봐둘것 · 인계 · 최근.md).
  - 4-9 새 줄과 앞뒤 문장: 「회의록 본문은 켭니다」 → 「만들기 폼은 예외」 →
    「논의 입력칸은 켜지 않습니다」 순서라 서로 부딪히지 않는다. 다만 「회의록
    본문은 켭니다」 가 이제 「상세 화면의」 를 속으로 품게 됐다(위 2 와 같은 자리).
  - AN-b: app/notifications.py:196 주석이 「봐둘것 AN-b」 를 가리키는데, 닫힌 뒤에도
    그 항목이 「그대로 둔다」 와 이유를 담고 있어 가리킴이 살아 있다.
  - 최근.md:54 의 `de4c260..HEAD` 는 지금은 빈 범위다(커밋 전) — 커밋 뒤에는 맞다.

사람에게 묻는 것
  1. 봐둘것의 닫는 모양 — 지시대로 본문 끝에 둘 것인가, 파일 관례대로 제목 아래
     > 인용 블록으로 옮길 것인가. 앞으로 닫는 항목의 기준이 된다.
  2. AN-a 예외의 이유 — 「0 으로 재므로」(기술 제약)가 정한 이유인가, 아니면
     「짧게 적는 4줄 칸이라 필요 없다」 같은 다른 이유인가. 전자면 재기를 고친 뒤
     예외를 걷어도 되는 것이고, 후자면 4-9 의 이유 문장을 바꿔야 한다.
  3. 작업 스케줄러 이력 — 실제로 켜졌는지(IsEnabled true) 한 번 확인했는가.
     안 했으면 봐둘것 줄을 「켰다고 함」 으로 두거나 확인한 뒤 적는다.
━━━ 검토자 여기까지 ━━━
```

## 검토자를 받아 고친 것

| 검토 | 한 것 |
|---|---|
| 1 닫는 모양이 관례와 다름 | 제목 아래 `>` 인용 블록을 더했습니다(본문 끝 문장도 둠) — 4장 4 |
| 2 14장 listinput 줄이 옛말이 됨 | 고쳤습니다 — 4장 7 |
| 3 예외의 이유가 「못 켠다」 로 읽힘 | 사람이 적은 문장 그대로 두었습니다 — 4장 5 |
| 4 예외를 잡는 시험 없음 | 문서만인 판이라 안 넣음 — 4장 8 |
| 5 「다음은 7번」 이 두 줄 | 그대로 — 갱신 줄은 「언제·왜」, 「할 것」 머리는 목록의 시작점이라 자리가 다릅니다 |
| 6 AM-f 줄에 주어가 없음 · 잰 것이 아님 | 「사람이 켬」 을 붙이고 `wevtutil gl` 로 enabled true 를 확인해 적었습니다 — 4장 6 |
| 7 원문을 넣은 뒤 검사·시험 재실행 | 글검사와 문서를 읽는 시험 묶음(89개)을 원문을 넣은 뒤 다시 돌렸습니다 — 3장 |
