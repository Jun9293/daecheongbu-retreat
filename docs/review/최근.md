# 검토용 보고

작성 시각: 2026-09-07 (KST)
커밋: a7378bb (브랜치 `main` · 이 단계의 마지막 코드·문서 커밋 · 보고 커밋은 그 뒤)
작업: UI 개편 단계 5 — 재정(7장) 새 기능 · 껍데기 하나로 · 단계 4 후속 (마지막 단계)

---

## 0번 — 먼저 센 것 (코드 고치기 전 실측)

### 0-a·b. 운영 DB (data/app.db)

| 잰 것 | 값 |
|---|---|
| BudgetCategory | **9행** (회차 1) · plannedAmount 합 **11,900,000원** · 깊이는 구분›항목›세부항목 각 1행씩 9조합 · **표의 세부항목 컬럼은 level3 하나** (8장의 level3a/b/c 는 지출 쪽 이야기 — 문서를 실측에 맞춤) · 단가·명수·횟수 자리 **없음** (_ADDED_COLUMNS 대상) |
| ExpenseEntry | **12행** (회차 1) · receipt_number 있는 것 **12** · receipt_file_url 있는 것 **0** · 지출자=수련회계좌 **5** · paid=False **7** · subsidy>0 **12** · 식대 **5** · 예산 미연결 **0** |
| income_items · expense_receipts 표 | **없음** — create_all 이 만든다 |
| 지출자 표기 | 수련회계좌(5건·전부 지급) + 개인 4명(7건·전부 미지급) — 「수련회계좌」 는 리터럴 표기라 환급 판정 기준으로 삼음 |

### 0-c. 옛 재정 코드 — 그대로 쓰는 것 / 지운 것

| 그대로 쓰는 것 | 지운 것 |
|---|---|
| `budget.build_budget_summary` (확장 — 소계·비율·수입·남은 지출을 안에 넣음) | `_next_receipt_number` (ExpenseEntry.receipt_number 기반 → `budget.next_receipt_number` 가 ExpenseReceipt 로) |
| `meal.calculate_meal_settlement` · `parse_attendees` · `_last_meal_defaults` · `_next_meal_label` (7-2 그대로) | `/refunds` 화면·`refunds.html` (지출 필터가 그 자리 · 301) |
| 지출·예산 CRUD 엔드포인트 골격 (필드 추가) | `export.py` 의 시트 조립 (→ `domain/budget_xlsx.write` — summary 를 놓기만) |
| `_save_receipt` (이름만 반환하게 개조 — ExpenseReceipt 에 붙임) | 옛 `budget.html`·`expenses.html` (새 껍데기로 다시 씀) |

### 0-d. base.html · app.css 참조 (지우기 전)

- `extends "base.html"` : **6곳** — budget · expenses · refunds · login · error · no_retreat
- `app.css` : **2곳** — base.html 의 link 1 + flash.js 주석 1 (+ CLAUDE.md 14장 표의 계획 행)
- (덤) `app.js` 참조는 base.html 하나뿐 — 서비스워커 등록·식대 미리보기가 그 안에 있어
  각각 push.js · expenses.js 로 옮기고 함께 지움

### 0-e. 스케줄러 세 작업 현재값 (읽기만 — 사람이 이미 바꿨음)

| 작업 | DisallowStartIfOnBatteries | StopIfGoingOnBatteries | ExecutionTimeLimit | WorkingDirectory |
|---|---|---|---|---|
| 대청부 앱 | **False** | **False** | **PT0S** (무제한) | (없음 — serve.bat 절대경로라 무관) |
| 대청부 백업 | **False** | **False** | PT72H (그대로 — 목표대로) | **저장소 폴더** |
| 대청부 알림 | True | True | PT72H | (없음) — 사용 안 함 상태라 대상 아님 |

**전부 목표값과 일치.** 백업은 사람이 바꾼 직후 손 실행이 LastResult=**0**,
`data/backups/` 에 당일(09-07 15:20) 산출물 확인 — 봐둘것 T-f 를 「닫힘」 으로.

---

## 1번 — 단계 4 후속

| 항목 | 근거 |
|---|---|
| 1-a 취소 = 상태 | REVIEW_STATUSES 에 '취소', cancel 이 삭제 대신 status 변경 + 로그. 「내가 보낸 요청」 에 흐리게(.cancelled), pending(배지·답 버튼)에서 빠짐. 시험 `test5_c01`(행 수 불변 · pending 0) |
| 1-b 배지 = 칩 수 | 배지 = 안 읽은 **시스템** 알림(system_unread_count — 사람 발신 제외) + pending_for_user. 「답을 기다리는 것」 칩이 **같은 함수**의 행을 그대로 그림 — 부서 없는 admin 도 배지와 화면이 같음. 시험 `test5_c02`(요청 하나 = 배지 1 = 칩 행 1 · admin 칩 행 수 = pending 수) |
| 1-c 폐회일 당일 경계 | 코드 그대로 두고 4-10 에 「폐회일 당일 마감 미완료는 늦음이 아니다」 를 적어 결정으로. 시험 `test5_c03` |
| 1-d 체크리스트 흐림 키 | `is_other_dept` 가 Department **행**을 받아 키로 비교(키 없는 부서만 행으로 — None==None 방지). 시험 `test5_c04`(다른 회차 같은 키 소속이 선명하게 봄) |

## 2·3·4번 — 재정

- **모델**: BudgetCategory + unit_price·headcount·times(_ADDED_COLUMNS) ·
  IncomeItem · ExpenseReceipt 신설 · ExpenseEntry 의 receipt_number ·
  receipt_file_url 은 `_legacy_*` 속성으로 봉인(컬럼은 남음), 부팅 시
  `db._move_receipts()` 가 한 번 옮김
- **숫자는 전부 `domain/budget.py`** — summary(소계 GroupSummary · 비율 ·
  차액 · 수입 · 잔액 · unspent_*) + refund_entries · no_receipt_entries ·
  next_receipt_number. 홈 결산·회차 상세·예산·지출 그룹 헤더·엑셀이 같은 것을 씀
- **엑셀**: `domain/budget_xlsx.write(summary, entries)` — 세 시트, 소계·총계
  전부 summary 값. `/export/expenses.xlsx` 는 흘려보내기만
- 시험 (수용 기준 3~7):
  `test5_b01`(계산/직접 둘 다) · `test5_b02`(화면 계산은 미리보기 — fetch 0) ·
  `test5_b03`(초과 붉음 · 180.0%) · `test5_b04`(수입 변경에 집행률 불변) ·
  `test5_b05`(홈·회차 상세·예산 같은 수) · `test5_b06`(라우터·템플릿에 세는 코드 없음) ·
  `test5_x01`(엑셀 소계 = summary.groups) · `test5_e01`(지출 그룹 헤더 = 예산 값) ·
  `test5_r01`(영수증 옮김 한 번 · 두 번 안 옮김) · `test5_r02`(옛 컬럼 읽기 AttributeError + 읽는 곳 grep 0) ·
  `test5_r03`(다음 번호 · 메모 영수증) · `test5_f01`(환급 필터 · /refunds 301 · 옛 템플릿 없음) ·
  `test5_f02`(홈 결산 둘 = 필터 행 수) · `test5_u01`(남은 지출 셋 — 진행 중/끝났고 N건/끝났고 0건)

## 5번 — 껍데기 하나로

- `git rm`: base.html · app.css · refunds.html · **app.js** (참조가 base.html
  뿐 — 서비스워커 등록은 push.js 로, 식대 미리보기는 expenses.js 로 옮김.
  push.js 는 retreat_base 가 전역으로 실어 어느 화면을 먼저 열든 SW 가 깔림)
- 로그인·오류·회차 없음 화면을 retreat_base 로 — 로그인 전에는 사이드바
  블록을 안 그림(`{% if user %}`). 껍데기는 retreat_base.html **하나**
- 지운 뒤 grep: `extends "base.html"` **0곳** · `app.css` 참조 **0곳**
  (flash.js 주석은 옛 이름을 지우고 다시 썼음) — 시험 `test5_s01` ·
  `test5_s02`(로그인 화면) · `test5_s03`(회차 없음 화면)
- 목업 중 base.html 모양을 그린 것은 **없음** — 다섯 파일 전부 무채색 새 기조
  (drawer-attachment-link.html 은 드로어 하나짜리 목업). 옮긴 것 없음

## drawer.js 점검 (세 화면)

| 화면 | 통과/전체 |
|---|---|
| 보드 | **82 / 82** |
| 달력 | **82 / 82** (기본 범위가 「내 것」 이라 `?scope=all` 로 열고 돌림 — 개발 계정은 담당 업무가 없음) |
| 목록 | **80 / 80** |

(개발 서버 8001 · 개발 데이터 · CDP 주입 + MessageChannel 타이머 — 단계 4 방식 그대로)

## 스크린샷 (사람이 눈으로 본다 — seed 데모 데이터(가명) · 개발확인용 계정)

- [단계5-예산.png](단계5-예산.png) — 지표 4칸(총액·결산·수입·잔액 붉음) ·
  구분 소계 · 비율 · 차액 · 집행률 · 행 수정
- [단계5-지출.png](단계5-지출.png) — 칩 5 + 엑셀 · 다음 영수증 번호 ·
  예산 라인별 그룹(헤더에 예산/집행/잔액) · 식대 인원 칩 · 영수증 칩 ·
  영수증 붙이기
- [단계5-결산홈.png](단계5-결산홈.png) — 미지급 환급 7건(붉음) · 영수증 없는
  지출 0건 · 미완료 업무 · 집행률 70%

> **첫 촬영본은 커밋 전 검토자가 막았습니다** — 개발 DB(%TEMP%)가 익명화
> 전 자료를 들고 있어 지출자·강사 이름 **실명 7개**가 찍혀 있었습니다.
> 개발 DB 를 지우고 seed(가명)로 다시 만들어 재촬영했고, 지금 세 장의
> 이름(다은·하람·서윤·준서·오세영·신태호·임채원)은 전부 seed 의 가명입니다.
> 커밋된 적은 없습니다. 구조적 구멍(이미지가 실명 검사 밖)은 봐둘것 U-e.

## 검증

```
1031 passed in 262.58s (0:04:22)
```
(검토자 반영 뒤 마지막 전체 실행. 반영 전 실행도 1031 passed — 시험 수는
같고 죽은 단언 둘이 실제 단언으로 바뀌었습니다.)

check_names: 실명 38 · 걸린 것 없음 · 안 고친 곳 0곳 · 대응표 밖 표기 0.
anonymize 미리보기: 0개 파일 · 0곳.

## 검토자 (커밋 전) — 원문은 채팅 덩어리에 그대로

| 지적 | 처치 |
|---|---|
| **막아야 함 — 스크린샷 2장에 실명 7개** (개발 DB 가 익명화 전 자료) | 촬영본 폐기 · 개발 DB 삭제 후 seed(가명) 재생성 · 재촬영. 커밋 전에 잡혀 유출 없음. 구조적 구멍은 봐둘것 U-e |
| `or True` 죽은 단언 2곳 (test5_f01 · test5_c02) | 지우고 실제 단언으로 (admin pending == 1 확인 추가) |
| 재정 편집 권한이 부서 id 비교인데 기록이 없음 | **미루되 기록** — 봐둘것 U-a (permissions 층과 한 몸이라 반쪽 수정 금지) |
| _move_receipts 가 번호 있는 행만 봄 — url 만 있는 행의 파일이 영영 봉인 | OR 로 넓히고 번호 없는 행은 회차의 다음 번호를 부여. 시험 test5_r01 에 url-only 경우 추가 |
| 최근.md 스크린샷 절이 「개발 데이터」 라고 말함 (실자료였음) | 절을 다시 씀 — 사고 경위 포함 |
| budget_xlsx 의 지원금액 인라인 재계산 · 환급 시트 열 이름 | `e.settlement_amount` 로 · 열 이름 「환급액」 으로 |
| test5_b06 의 첫 단언이 헛돎 | 예산 라우터 `sum(` 0곳 + 두 라우터가 summary 를 쓰는지로 다시 씀 |
| 환급 시험 이름이 없는 기능(지출자별 합계)을 주장 | 이름·독스트링을 지금 검증하는 것으로 고침 |
| cancel 이 responded_at 을 찍음 | 안 찍음 — 취소 시각은 ActivityLog |
| 환급 리터럴 변형 표기 · /refunds 쿼리 유실 · 로그인 상태 404 외관 | 봐둘것 U-b · U-c · U-d |

## 운영 반영 (11-3의 2단계)

- 12-1 재시작: **멈추기 전 PID 21320 → 다시 켠 뒤 PID 30148 — 「성공 —
  PID 가 바뀌었습니다」** (스케줄러 관리 그대로 — 배터리 게이트는 사람이
  꺼 두어 이번엔 바로 떴다)
- 부팅 마이그레이션 로그: **「지출 12건의 영수증 번호를 ExpenseReceipt 로
  옮겼습니다 — 한 번만」** · budget_categories 에 단가·명수·횟수 컬럼 추가
- 돌고 있는 서버(30148) 실측: `/refunds` **301 →
  /expenses?filter=refund** · 새 해시 `retreat.4fc3f9e0.css` **200** ·
  `/login` **200** (새 껍데기 — 사이드바 없음)

## 판단이 들어간 자리

- **환급 판정 기준** — 지출자 표기 「수련회계좌」 리터럴 비교(budget.RETREAT_ACCOUNT).
  운영 12행 실측에서 이 표기 5건이 정확히 시트의 「환급 아님」 과 일치
- **?filter=refund = 환급 대상 & 미지급** — 홈 「미지급 환급 N건」 을 눌러 간
  화면의 행 수와 같아야 해서(4-15 규칙). 지급 끝난 환급까지 보려면 엑셀의
  환급 시트(전체 + 지급여부)가 있음
- **행 번호 표시** — 지출 행의 「번호」 는 그 지출에 붙은 영수증 번호들(콤마).
  영수증이 없으면 — 로 표시
- **`test_v_07`·`test_15`·`test_ts_*` 갱신** — base.html·app.js 삭제와 새 CSS
  (표 머리 라벨·404 코드는 4-0 의 장식 자리로 등록, 「미지정」 안내는 보조로 승격)
