사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: 삭제 대상 수(지시문 20 · 40 ↔ 미리보기 13 · 26 — 갈래 ㄱ·ㄴ·ㄷ) · 걸려서 뺀 상위 여덟을 테스트 자료로 볼지 · 시드 파일의 `LIBRARY_ONLY` 를 걷을지 · 그 뒤 `--실행` (봐둘것 BD-b · 1장)
막혀서 못 한 것: 화면 점검(drawer.js · phone.js)은 로그인된 브라우저가 필요해 여전히 못 돌았다 — 이 판은 화면을 안 고쳤다 (11-2 · 인계 맨 위)

작성 시각: 2026-09-17 13:10 (KST)
커밋: d18a0c1 다음 — 이 판의 커밋들(8장 파일 목록)
작업: 테스트 시드 삭제 — 0장 예외 (미리보기까지)

---

**이 판은 미리보기까지다.** 운영 DB 에서는 `scripts/테스트시드삭제.py` 를 `--실행` 없이 한 번 돌렸고(읽기만 · 사본도 안 뜸),
출력은 id 와 수뿐이다. 지울 id 와 제목은 저장소 밖 `data/테스트시드삭제.real.md` 에만 있다. `--실행` 은 운영 사본(이 창의 임시 폴더)에서만 돌려 봤다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `seed.py` 의 `seed()` 를 부르는 곳은 `__main__` · `scripts/devserve.bat`(개발 폴더에서만) · 시험 `tests/test_seed.py` · `tests/test_login.py`(실제로 부름 · conftest 가 임시 폴더를 가리켜 막는 줄에 안 걸림 — 통과가 「막히면 안 되는 쪽」 을 잰다) — 첫 판은 이 시험 둘을 빠뜨렸다(6-1 [F]). `test_live` · `test_stage46` · `test_stage47` 은 글로만 읽는다. 새 스크립트는 `재정들여오기.사본을_뜬다` · `비품들여오기.멈춤` 을 그대로 부른다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면을 안 고쳤다. 「뺀 업무」 목록(`tasks.html`)은 비면 안 그리므로 2027 쪽이 비어도 고칠 것 없음(2장 4b) |
| 그 값을 세어 둔 문서 | CLAUDE.md 0장(첫 예외만 있던 자리에 둘째 예외) · 6-9 · 14장 표 · 봐둘것 BD-a 머리 · 인계 맨 위. 삭제 수는 봐둘것 BD-b · 인계 · 이 보고에만 |

# 1. 미리보기 — 지시문의 수와 다르다

운영 DB 에서 돌린 미리보기 출력(그대로):

```
대상 라이브러리 22 (확정 6 · 출처 불명 20 · 겹침 4)
지울 것: 라이브러리 13 · run 26
  회차 1 · 뺀 run 13
  회차 2 · 뺀 run 6
  회차 2 · 산 run 7
  라이브러리 id: [31, 44, 64, 92, 94, 95, 96, 97, 98, 99, 100, 101, 102]
걸린 것이 있어 뺀 것: 라이브러리 9 · run 18
  4: 남는 라이브러리의 상위
  5: 남는 라이브러리의 상위
  13: 남는 라이브러리의 상위
  17: 남는 라이브러리의 상위
  18: 남는 라이브러리의 상위
  19: 남는 라이브러리의 상위
  26: 남는 라이브러리의 상위
  37: 남는 라이브러리의 상위
  65: 남는 라이브러리의 관련업무
안 건드릴 것: 노션 라이브러리 233 · 마법사 제안 4 · 남길 라이브러리 326 · 남길 run 415
바꾸지 않았습니다. 목록: data/테스트시드삭제.real.md
실제로 지우려면 이 목록을 확인한 뒤: --실행 --기대 31,44,64,92,94,95,96,97,98,99,100,101,102
```

(검토 뒤 고친 스크립트로 다시 돌린 출력이다 — 수는 첫 판과 같고 끝 두 줄만 달라졌다.)

**지시문은 「라이브러리 20 · run 40」 인데 미리보기는 13 · 26 이다.** 까닭이 둘이다.

- **97 · 98 은 ③(출처 불명 20) 밖이다** — d18a0c1 의 비교 규칙으로 「부분」 에 들어갔다. 지시문 ② 가 둘을 넣으라고 했으므로 후보는 20 + 2 = **22**(run 44)가 된다
- **지시문 0c · 1c 대로 걸린 것이 있는 아홉을 뺐다** — d18a0c1 은 run 에 걸린 것(논의 · 첨부 · 요청 · 활동 기록)과 **대상 여섯**의 관련업무 참조만 셌고,
  **대상 스물이 남는 줄의 상위이거나 관련업무로 걸리는지**는 안 셌다. 이 판에서 FK 목록(`PRAGMA foreign_key_list`)과 JSON 칸을 전부 훑어 새로 잡혔다:
  - 4 · 5 · 13 · 17 · 18 · 19 · 26 · 37 — 대상이 아닌 라이브러리 15 가 이들을 상위로 가리키고, 그 15 는 전부 2027 에 산 채다. `task_library.parent_library_id` 가 `SET NULL` 이라
    지우면 그 하위들이 말없이 최상위가 된다. 여덟 가운데 일곱에 「노션을 재분류해 Main 을 신설해 묶었다」 는 `reclassification_note` 가 있다
  - 65 — 남는 라이브러리 76 의 관련업무가 가리킨다. 18 은 65 가 남으면 그 하위가 남는 것으로도 걸린다(스크립트가 되풀이해 뺀다)
- run 에 걸린 것은 22 전부에서 0 이다 — 논의 · 논의 잇기 · 첨부 · 확인 요청 · 알림 기록 · 회의 항목 전환(`meeting_items.converted_run_id` · FK NO ACTION) · 따라온 논의 ·
  활동 기록(run · 라이브러리) · 업무 규칙 · 남는 run 의 `blocked_by_run_ids` · 초안 제출의 `library_ids`

그래서 가) 「정확히 20 · 40」 은 **이 판의 미리보기로는 맞출 수 없다** — 사람이 갈래를 고른다(봐둘것 BD-b):
ㄱ 미리보기대로 13 · 26 · ㄴ 걸려서 뺀 9 까지(하위 15 가 최상위가 되고 76 의 관련업무를 걷는다 · 2027 의 산 줄이 바뀐다) · ㄷ 97 · 98 을 빼고 ③ 만(11 · 22).

**운영 사본에서 `--실행` 을 해 봤다** (이 창의 임시 폴더 · 운영 DB 는 안 건드림): 라이브러리 339 → 326 · run 441 → 415 · 노션 233 · 마법사 4 그대로 ·
회차별 run 회차 1 322(산 233) · 2027 93(산 93) · 두 번째 `--실행` 은 「지울 것이 없습니다」 이고 사본을 안 떴다.

# 2. 단계별

**0 — 안전장치**

- a. CLAUDE.md 0장에 **둘째 예외**를 적었다(무엇을 · 왜 · d18a0c1 의 근거 · 걸린 것은 뺀다 · 이 목록에만 걸린다). 봐둘것 BD-b 에 같은 내용과 미리보기를 적고, BD-a 머리를 「사람이 정함: 지운다」 로 바꿨다
- b. 대상은 스크립트의 `대상` 한 곳에 **id 로만** 있다(확정 6 ∪ 출처 불명 20 = 22). 2027 의 run 은 라이브러리에서 따라 나온다 — 2027 의 산 16 가운데 7 을 지우고 9 는 걸려서 남는다
- c. 걸린 것은 `고른다` 가 미리보기와 `--실행` 둘 다에서 세고, `--실행` 은 지우기 직전에 **한 번 더** 골라 미리보기와 다르면 멈춘다. 달라서 멈춘 것은 없고, 걸려서 뺀 아홉은 사람칸에 올렸다

**1 — 스크립트 (`scripts/테스트시드삭제.py`)**

- a. 미리보기 기본 · `--실행`(사람이 본 id 를 `--기대` 로 함께 · 다르면 멈춤) · 지울 것이 있을 때만 `VACUUM INTO` 사본 · 세션 파일과 사본 파일이 다르면 멈춤(`재정들여오기.사본을_뜬다` 를 그대로 부름)
- b. **실제 삭제** — run 을 먼저 이름 불러 지우고(두 회차) 라이브러리를 지운다. 활동 기록에 `테스트시드_삭제` 한 줄(system · 지운 id)
- c. 지운 뒤 commit 전에 `센다` 로 다시 센다 — 라이브러리 · run 수, 대상이 남았는지, 노션 · 마법사 수, **남길 라이브러리 · run 의 지문**(수 · id 합 · 산 수 · library_id 합). 하나라도 다르면 되돌린다
- d. 지운(미리보기면 지울) id 와 제목, 걸려서 뺀 것의 까닭을 `data/테스트시드삭제.real.md` 에 쓴다(gitignore)
- 각 id 가 시드였다는 흔적(노션 id 없음 · `origin=history` · 2026-08-29 14:00:36~38 UTC 에 생김)과 안 맞으면 아무것도 안 한다 — id 가 다른 줄을 가리키게 된 DB 에서 도는 것을 막는다
- 빈 목록을 `NULL` 로 두면 `not in (NULL)` 이 아무 줄도 안 돌려줘 남길 줄을 0 으로 세던 것을 운영 사본에서 봤고 `-1` 로 고쳤다

**2 — 재발 방지**

- a. `seed.py` 에 `운영경로인가`(데이터 폴더나 DB 주소가 `data/` 를 가리키는가) · `_운영이면_멈춘다` — `__main__` 에서 `--reset` 이 파일을 지우기 **전**과 `seed()` 머리에서 본다.
  지시문 표현 「회차가 있는 DB(운영 경로)」 는 **운영 경로로** 갈랐다 — 회차가 있는 개발 DB 는 이미 `seed()` 가 「이미 데이터가 있습니다」 로 멈추고, 개발 DB 를 다시 만드는 `--reset` 은 막지 않는다. 운영 경로는 비어 있어도 막는다(이번 길이 빈 DB 에서 시작했다)
- b. CLAUDE.md 6-9 에 「목업의 예시 문구를 시드 데이터에 넣지 않는다」 · 「시드는 운영 데이터 폴더에서 돌지 않는다」 를 적었다. **지금 시드 파일이 그 규칙보다 먼저 들어가 있다는 것**도 함께 적고 걷는 것은 사람칸에 올렸다

**3 — 검증**

- a. 1장의 미리보기 — 라이브러리 13 · run 26 · 회차별 산/뺀 · 뺀 것 9 · 18, 값 없이
- b. 운영 사본에서 전후를 다시 셌다(1장) · 시험 `test59_a03` 이 정확히 그만큼 줄고 남길 run id 가 그대로인지 잰다
- c. 노션 233 · 마법사 4 — 미리보기 「안 건드릴 것」 · 사본 실행 뒤 그대로 · `test59_a03` · 대상에 노션 · 마법사 줄을 잘못 적으면 멈추는 것은 `test59_a04`
- d. 2027 의 정상 run — 남길 run 지문(수 · id 합 · 산 수 · library_id 합)이 전후 같아야 commit 한다. 사본에서 2027 은 산 100 → 93(지운 산 7)이고 나머지는 그대로

**4 — 문서**

- a. 봐둘것 BD-b · 인계 맨 위 · CLAUDE.md 0장 · 6-9 · 14장
- b. 「뺀 업무」 화면은 **계속 필요하다** — 지운 뒤에도 회차 1 에 뺀 run 89(노션에 있음 50 · 부분 30 · 걸려서 남은 9)와 거기 걸린 논의 잇기 28 · 첨부 3 · 확인 요청 1 이 남는다.
  2027 의 뺀 run 6 은 전부 지울 것이라 그 목록은 비고, 비면 화면이 안 그린다(`tasks.html` 의 `excluded_rows` 조건) — 고칠 코드가 없다

# 3. 검사 · 시험 · 앱

- **고장 열넷을 심어 전부 잡혔다** (`tests/test_stage59.py` · 파일마다 try/finally 로 되돌림 · 되돌린 뒤 `git status` 가 이 판의 파일만):
  빈 목록 NULL · 되풀이 없앰 · 다시 세기 없앰 · 직전 재확인 없앰 · 시드 흔적 검사를 노션 id 하나로 줄임 · 상위 걸림 없앰 · 사본 없앰 · `__main__` 의 막기 없앰 · 데이터 폴더 비교 없앰 · `--기대` 없어도 지나감 · `--기대` 비교 없앰 · 라이브러리 활동 기록의 글자 값 틀림 · 업무 규칙 걸림 없앰 · run 활동 기록의 글자 값 틀림
- 글검사 · pytest: 8장(보고를 다 채운 뒤)
- **앱은 재시작하지 않았다** — `app/` · 템플릿 · 정적 파일을 안 고쳤다(11-3 2단계 건너뜀). 포트 8000 PID 2776(앞 판과 같음)

# 4. 파일

8장의 `git diff --name-only` 가 정본이다. 저장소 밖: `data/테스트시드삭제.real.md`(미리보기 목록) · `data/출처진단.real.py`(앞 판).

# 5. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 삭제 전후 정확히 라이브러리 20 · run 40 감소 | X | 1장 — 이 판은 미리보기만(지시문대로). 지시문의 규칙대로 센 수가 13 · 26 이고 20 · 40 과 다르다. 사람이 갈래를 고른다 |
| 나) 걸린 것이 0 임을 삭제 직전에 재확인 | O | 2장 0c · `test59_a06` · `test59_a10` — `--실행` 이 직전에 다시 고르고, 사람이 본 id 와 다르면 멈춘다. 미리보기에서 걸린 아홉은 뺐다 |
| 다) 노션 233 · 마법사 4 · 2027 정상 run 무손상 | O | 1장 · 2장 3c · 3d — 미리보기와 운영 사본 실행 · `test59_a03` · `test59_a05` |
| 라) 0장 예외 근거가 문서에 명확히 | O | CLAUDE.md 0장 · 봐둘것 BD-b · `test59_c01` |
| 마) pytest 옵션 없이 전부 통과 · 글검사 7/7 | O | 8장 |
| 바) 검토자 원문이 최근.md 에 그대로 | O | 6장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| ① 시드 20 · 40 을 실제 삭제 | 다르게 했다 | 삭제는 다음 판(지시문 끝줄). 미리보기 수가 13 · 26 이라 사람칸에 올렸다 |
| ② 97 · 100 · 102 함께 | 했다 | 대상에 넣었다. 97 · 98 은 ③ 밖이라 후보가 22 가 됐다 |
| ③ 출처 불명 20 함께 | 다르게 했다 | 대상에 넣었지만 걸린 것이 있는 아홉(20 가운데 여덟 · 그리고 65)은 0c · 1c 대로 뺐다 |
| 0a | 했다 | CLAUDE.md 0장 · 봐둘것 BD-b |
| 0b | 했다 | `대상` 22 · 2027 run 은 라이브러리에서 따라 나옴(산 16 중 7 지움) |
| 0c | 다르게 했다 | 「멈춘다」 가 아니라 1c 대로 **그 항목을 빼고** 나머지를 계획했고, 뺀 아홉을 사람칸에 올렸다. 사람이 본 목록과 실행 때 목록이 다르면 **멈추는 것**은 `--기대` 로 넣었다 |
| 1a | 했다 | 2장 1 |
| 1b | 했다 | hard delete — 운영 사본에서만 실행 |
| 1c | 했다 | 2장 1 · `test59_a01` |
| 1d | 했다 | `data/테스트시드삭제.real.md` |
| 2a | 다르게 했다 | 「회차가 있는 DB」 가 아니라 「운영 경로」 로 막았다 — 2장 2a 의 까닭 |
| 2b | 했다 | CLAUDE.md 6-9 |
| 3a | 했다 | 1장 |
| 3b | 다르게 했다 | 운영 실행이 다음 판이라 운영 사본에서 전후를 셌다 |
| 3c | 했다 | 1장 · 2장 3c |
| 3d | 했다 | 2장 3d — 남길 run 지문 |
| 4a | 했다 | 봐둘것 BD-b · 인계 |
| 4b | 했다 | 2장 4b — 화면은 계속 필요 · 고칠 코드 없음 |
| 공통 규칙은 CLAUDE.md 그대로 | 했다 | 미리보기 → (다음 판) 실행 → 다시 세기 순서 · 값은 저장소 밖 |
| 검토자 원문을 그대로 싣는다 | 했다 | 6장 |
| 미리보기까지만 | 했다 | 운영에는 `--실행` 을 안 돌렸다 |
| 마무리는 11-3 대로 | 다르게 했다 | 화면 점검은 로그인이 필요해 못 돌렸다(11-2) — 화면을 안 고친 판. 재시작은 규칙대로 건너뜀 |

# 6. 검토자 원문

## 6-1. 커밋 전 검토

```
━━━ 검토자 여기부터 ━━━ 범위 d18a0c1..작업 트리 · 커밋 0개(커밋 전) · 파일 8개

검토자가 한 일 (파일은 안 고쳤습니다)
  - 운영 data/app.db 는 sqlite3 file:...?mode=ro 로만 열었습니다. PRAGMA foreign_key_list 로 FK 를 보고,
    대상 22 의 칸(rules · always_required · dates_changed_at · created_at · origin · 상위)을 봤고,
    notifications 의 link 와 target 도 확인했습니다
  - 운영 DB 를 sqlite backup API 로 제 임시 폴더에 복사했습니다. DCB_DATA_DIR 를 그 폴더로 두고
    스크립트를 미리보기 → --실행 → --실행 순서로 돌렸습니다
      미리보기 출력은 보고 1장과 같았습니다 (22 · 13/26 · 9/18 · 233 · 4 · 326 · 415)
      실행 뒤 라이브러리 326 · run 415 · 회차별 ((1,322,233),(2,93,93))
      상위 id 가 없는 줄 0 · foreign_key_check 빈 결과
      두 번째 실행은 「지울 것이 없습니다」 이고 사본을 안 떴습니다
  - 운영 data/backups 에 이 판이 만든 사본은 없습니다. 미리보기가 사본을 안 뜬다는 보고와 맞습니다
  - pytest tests/test_stage59.py tests/test_seed.py tests/test_login.py → 43 passed (-p 없음)
  - scripts/글검사.py → check_placeholder 만 exit 1 (아래 [G])
  - 바뀐 파일의 + 줄과 최근.md 전체에서 LIBRARY · LIBRARY_ONLY 제목(4글자 이상)을 찾았습니다 → 0건
  - data/테스트시드삭제.real.md 는 gitignore(data/*.real.md) 안에 있습니다

[A] 고쳐야 함 — 「직전 재확인」 이 사람이 본 미리보기와 견주지 않습니다
    scripts/테스트시드삭제.py 253-256
    판2 는 같은 --실행 프로세스 안에서 판을 뽑은 뒤 몇 밀리초 만에 다시 고릅니다. 사실상 늘 같습니다.
    test59_a06 은 monkeypatch 로만 빨개지고, 실제로 이것이 달라지는 경우는 없습니다.
    사람이 승인하는 것은 「앞 판 미리보기의 13 · 26」 입니다. 그 사이 DB 가 바뀌면 어떻게 되나:
      - 걸린 것이 새로 생기면: 그 항목은 조용히 빠지고 12 로 진행합니다 (규칙상 안전)
      - 걸린 것이 풀리면 (예: 76 의 관련업무에서 65 를 떼면): 승인 안 된 14 번째를 멈추지 않고 지웁니다
    지시문 0c 「다르면 삭제하지 않고 멈춘다」 가 겨누는 것은 뒤쪽입니다.
    제안: --실행 이 기대 id 목록(또는 미리보기가 쓴 목록 파일)을 받아, 다르면 멈춥니다.
    다음 판에 실행하기 전에 넣는 것이 맞습니다.

[B] 고쳐야 함 — 라이브러리에 걸린 활동 기록을 안 셉니다
    scripts/테스트시드삭제.py 134-136
    활동 기록은 target_type='task_run' 만 봅니다. 그런데 앱은 라이브러리 편집을 target_type="task_library" 로 남깁니다:
      제목 변경 · 규칙 · 선행 — app/routers/board.py 933 · 1146 · 1211
      app/routers/library.py 133 · 168 · 203 · 276 · 328
    지금 운영에는 task_library 기록이 0 이고, 대상 22 의 rules 도 전부 빈 값이라 현재 수는 맞습니다.
    하지만 미리보기와 실행 사이에 누가 대상 업무에 「업무 규칙」 을 적거나 제목을 고치면,
    그 기록(0장 둘째 원칙의 「지식」)은 걸림으로 안 잡히고 행째 지워집니다.
    지시문 0c 가 활동 기록을 명시했습니다.
    제안:
      - target_type='task_library' and target_id in (후보) 를 걸림에 더합니다
      - 가능하면 rules 가 비지 않은 것도 걸림에 더합니다
    그리고 run걸림표 중 시험에서 한 번도 심지 않은 것이 있습니다:
      첨부 · 확인 요청 · 알림 기록 · 회의 항목 전환 · 따라온 논의 · 논의 잇기 · 활동 기록 · 초안 제출 · 남는 라이브러리의 선행
    칸 이름이 틀리면 SQL 이 터지므로 조용히 새지는 않습니다.
    다만 activity_logs 의 'task_run' 처럼 글자 값을 견주는 줄은 틀려도 0 이 나옵니다
    (11-3 ③ 「볼 것을 실제로 보나」). 최소한 활동 기록 한 줄은 심어 잴 자리입니다.

[C] 봐 둘 것 — 참조를 빠짐없이 셌나 (질문 2)
    FK 는 다 덮었습니다. task_runs · task_library 를 가리키는 FK 는 아래뿐이고, 전부 걸림표나 되풀이 루프에 있습니다:
      task_library.parent (SET NULL)
      task_runs.library_id (CASCADE — run 을 먼저 지우므로 안 탐)
      meeting_items.converted_run_id (NO ACTION)
      review_requests.run_id (NO ACTION)
      discussion_entries.run_id · notification_logs.run_id · task_attachments.run_id · discussion_entry_runs.run_id (CASCADE)
    JSON 칸도 다 덮었습니다 — related · prerequisite · blocked_by · draft library_ids.
    FK 없는 칸:
      - notifications 의 target 은 전부 'task'(옛 표)이고, link 에 task= 가 든 줄은 0 입니다
      - review_requests.task_id · checklists.task_id · file_assets.task_id 는 옛 Task 표를 가리킵니다
    빠진 참조는 [B] 하나입니다.
    되풀이 루프(144-163)는 제가 읽기로 맞습니다. 뺀 라이브러리의 상위나 선행이 대상을 가리키면 다음 바퀴에 잡힙니다.

[D] 봐 둘 것 — 미리보기 13 · 26 판단 (질문 1 · 6)
    수를 운영 사본에서 다시 냈고 보고와 같습니다.
    - 97 · 98 이 ③ 밖이라 후보가 22 가 된 것은 d18a0c1 BD-a 표와 맞습니다
    - 걸린 것 목록에 「상위」 를 넣은 것: 지시문 0c 목록에는 없지만, 넣지 않으면 2027 의 산 하위 15 가
      말없이 최상위가 됩니다. 보수적인 쪽이라 타당하다고 봅니다
    - 그 여덟 중 일곱에 reclassification_note 가 있습니다 (운영에서 길이 40~98 확인).
      「노션 줄을 묶는 상위」 라는 보고 설명과 맞고, 테스트 자료인지는 사람이 정할 일입니다
    - 수용 기준 가) 를 X 로 적고 사람칸 「정할 것」 에 올린 것은 맞는 처리입니다
    - 대조 표 0c 는 「했다」 로 적혔습니다. 그런데 지시문은 「멈춘다」 이고 실제로는 「그 항목을 빼고 진행」 입니다.
      1c 와 합쳐 읽으면 맞지만, 판정은 「다르게 했다」 가 더 정확합니다
    - 회차 1 의 대상 run 가운데 status 가 대기가 아닌 것이 12 있습니다.
      started_at 과 활동 기록이 0 이라 시드가 넣은 값으로 보이고, 걸림으로 안 친 것이 맞습니다.
      「실제로 진행된 적 없다」 는 0장 문장과 겉보기가 달라, 다음 사람이 물을 수 있습니다

[E] 봐 둘 것 — seed.py 막기 (질문 4)
    「운영 경로」 로 가른 것은 타당합니다. seed() 는 이미 「회차가 있으면」 돌아서고
    (seed.py 152-154), 위험한 것은 --reset 이 DATA_DIR/app.db 를 unlink 하는 길입니다.
    막는 줄이 unlink 전(475)에 있고, DATA_DIR 와 DATABASE_URL 을 둘 다 봅니다.
    남는 틈:
      운영 사본을 다른 DCB_DATA_DIR 에 두고 --reset 하면 안 막힙니다.
      다만 지우는 것이 사본이라 손실은 없습니다. 11-2 의 실명 게이트(check_dev_db)가 그 자리를 따로 봅니다.
    시험이 운영을 건드릴 위험:
      conftest 가 DCB_DATA_DIR · DCB_DATABASE_URL 을 임시 폴더로 두므로 없습니다.
      test_seed.py · test_login.py 가 seed() 를 실제로 부르는데 통과했습니다.
      이것이 「막히면 안 되는 쪽」 을 실제 호출로 재고 있는 셈입니다.

[F] 고쳐야 함 — 보고의 고치기 전 훑기가 사실과 다릅니다
    docs/review/최근.md 20
    「시험은 seed.py 를 안 돌리고 글로만 읽는다(test_live · test_stage46 · test_stage47)」 라고 적혔습니다.
    실제로는 아래가 seed() 를 부르고, 이번에 넣은 _운영이면_멈춘다() 를 지납니다:
      tests/test_seed.py 24-26
      tests/test_login.py 154-156 · 165-167
    새 test_stage59 도 import seed 를 합니다.
    훑기가 짚어야 할 「같은 이름을 쓰는 데」 가 빠진 것입니다. 결과는 무해합니다(43 passed).

[G] 고쳐야 함 — 보고가 아직 미완성입니다 (알고 있는 것이지만 기계가 절반만 잡습니다)
    check_placeholder 는 docs/review/최근.md 123 의 「8장」 참조만 빨갛게 잡습니다.
    6-1 의 @@검토1@@ 자리 표시는 그 검사의 꼴이 아니라 안 잡힙니다.
    8장과 함께 6-1 을 손으로 채웠는지 확인한 뒤에 글검사와 pytest 를 다시 돌려야 합니다 (11-3 4단계).

[H] 봐 둘 것 — 다음 판(--실행)이 할 일이 BD-b 에 안 적혀 있습니다
    docs/봐둘것.md BD-b
    - --실행 이 뜨는 사본(data/backups/app-날짜.db)이 유일한 되돌리기입니다.
      11-2(2026-09-15 사람이 정함)대로, 문서에 그 이름을 적는 판에서 scripts/backup.py 의 지키는판 에도 넣어야 합니다
    - 되돌리기 절차가 7-5 처럼 DB 하나뿐이고, 그 사이 앱에서 한 일이 함께 사라진다는 것도 BD-b 에 없습니다
    - 스크립트 245 줄의 「목록: data/{이름}」 은 DCB_DATA_DIR 가 다른 곳이어도 data/ 로 찍힙니다 (표시뿐)

[I] 없음 — 값 유출 (질문 5)
    바뀐 파일과 출력에서 제목과 사람 이름을 찾지 못했습니다. 스크립트는 id 와 수만 찍습니다.
    test59_a09 는 제목 50개 넘게를 실제로 견줍니다 (0개면 실패).

[J] 없음 — 다시 세기 · 시드 흔적 (질문 3)
    센다 의 지문(수 · id 합 · 산 수 · library_id 합)과 노션 · 마법사 수는 a05 에서 고장을 심었을 때 되돌립니다.
    시드 흔적 검사는 a04 에서 세 경우를 잽니다: 노션 · 마법사 · 그 초를 벗어난 줄.
    빈 목록을 -1 로 두는 처리는 a08 이 잽니다.
    다만 「직전 재확인」 은 [A] 대로 실제 상황에서는 늘 같아, 잡을 대상이 없습니다.

사람에게 묻는 것
  1. 대상 수를 어떻게 할까요?
     ㄱ 미리보기대로 13 · 26
     ㄴ 걸린 9 까지 (2027 의 산 하위 15 가 최상위가 되고, 76 의 관련업무를 걷음)
     ㄷ ③ 만 (11 · 22)
  2. reclassification_note 가 있는 상위 여덟을 「테스트 자료」 로 볼까요, 「노션을 묶는 상위」 로 볼까요?
  3. --실행 판에서 「사람이 본 미리보기의 id 목록과 다르면 멈춘다」 를 조건으로 둘까요? ([A])
  4. 업무 규칙이 적혔거나 라이브러리 편집 기록이 있는 대상도 「걸린 것」 으로 칠까요? ([B] — 지금은 0 이라 수는 안 바뀝니다)
  5. seed_library_data.LIBRARY_ONLY 를 시드 파일에서 걷을까요? (BD-b 에 이미 올라 있음)
━━━ 검토자 여기까지 ━━━
```

## 6-2. 다시 보인 뒤 (검토 뒤 고친 차이만)

```
━━━ 검토자 여기부터 ━━━
범위: d18a0c1..작업 트리 · 다시 보기(첫 검토 뒤 고친 차이만) · 커밋 0개(커밋 전)

검토자가 한 일 (파일은 고치지 않았습니다)
  - 운영 data/app.db 는 mode=ro 로만 열었습니다.
    activity_logs 의 target_type 분포를 봤고, task_library 기록은 0 건이었습니다.
  - 운영 DB 를 sqlite backup API 로 제 임시 폴더에 복사했습니다.
    DCB_DATA_DIR 를 그 폴더로 두고 아래 순서로 스크립트를 돌렸습니다.
      1) 미리보기
         보고 1장과 같은 수가 나왔습니다(13 · 26 / 9 · 18 / 233 · 4 · 326 · 415).
         끝 두 줄도 같은 꼴입니다. 경로는 「<그 폴더 이름>/테스트시드삭제.real.md」 로 찍혔습니다.
      2) --실행 을 --기대 없이 줌
         「--기대 로 … 줍니다 — 아무것도 안 했습니다」 로 멈췄고, 사본은 뜨지 않았습니다.
      3) --실행 --기대 31,44
         「사람이 본 미리보기와 다릅니다(지금 [...])」 로 멈췄고, 사본은 뜨지 않았습니다.
      4) --실행 --기대 <미리보기가 찍은 13개>
         지웠습니다. 결과는 라이브러리 326 · run 415 · 노션 233 · 마법사 4,
         회차별 ((1,322,233),(2,93,93)) 입니다.
         사본은 그 임시 폴더의 backups 에만 떴습니다.
  - 운영 data/backups 에 새 파일은 없습니다. git status 는 이 판의 파일 여덟뿐입니다.
  - PowerShell 5.1 에서 쉼표 목록(31,44,64)이 exe 에 한 인자로 넘어가는 것을 확인했습니다.
    그래서 안내 줄을 그대로 붙여 넣어도 됩니다.
  - pytest tests/test_stage59.py → 15 passed (-p 없음)
  - 앱 코드가 쓰는 target_type 값을 모았습니다.
    업무 쪽은 'task_run' · 'task_library' 둘뿐입니다.
    app/db.py 의 부팅 전환도 'task_run' 이고, 첨부와 회의 전환도 'task_run' 입니다.
    [B] 의 고침이 빠진 글자 값 없이 덮습니다.

[A] 고친 것 확인 — 맞습니다
    scripts/테스트시드삭제.py 243-248
    --기대 를 견주는 자리가 고르기 다음, 사본 앞에 있습니다.
    그래서 목록이 달라 멈출 때는 사본도 안 뜹니다(임시 폴더에서 확인).
    test59_a10 이 두 경우를 잽니다.
      - --기대 없음
      - 걸림이 풀려 목록이 늘어난 경우(503 · 505 가 새로 낌)
    둘째 경우는 첫 검토가 겨눈 자리 그대로입니다.

[B] 고친 것 확인 — 맞습니다
    scripts/테스트시드삭제.py 137-142
    라이브러리 활동 기록과 비지 않은 rules 가 걸림에 들어갔습니다.
    test59_a11 이 세 줄을 실제로 심어 잽니다: task_library 기록 · task_run 기록 · rules.
    글자 값이 틀리면 0 이 나오는 줄을 막는 모양입니다(11-3 ③).
    「대상끼리의 상위는 걸림이 아니다」(502 는 지워짐)도 함께 잽니다.

[K] 봐 둘 것 — --기대 는 라이브러리 id 만 견주고 run 은 안 견줍니다
    scripts/테스트시드삭제.py 246
    미리보기와 실행 사이에 대상 라이브러리의 run 이 새로 생기는 경우가 있습니다.
    예: 2027 에서 복제한 새 회차가 대상 일곱을 산 채로 가져가는 경우입니다.
    그 run 이 새로 생긴 줄이라 걸린 것이 없으면, 라이브러리 목록은 같으므로 멈추지 않고 함께 지웁니다.
    판2 의 재확인은 같은 프로세스 안이라 이것을 못 봅니다.
    BD-b 「다음 판이 할 것」 은 「같은 id 가 나오는지」 만 봅니다.
    run 26 과 회차별 수도 미리보기에 찍히므로, 그 수까지 견주라고 한 줄 더하면 닫힙니다.
    지금 회차는 둘뿐이라 당장 새는 곳은 없습니다.

[L] 봐 둘 것 — 미리보기 안내 줄의 「(없음)」
    scripts/테스트시드삭제.py 236
    지울 것이 없을 때 안내 줄이 「--기대 (없음)」 을 찍습니다.
    그 줄을 그대로 붙여 넣으면 main 의 int() 에서 ValueError 트레이스백이 납니다(재현함).
    「지울 것이 없습니다」 보다 먼저 터집니다.
    지울 것이 없으면 실행할 일도 없어 해는 없고, 모양만 어긋납니다.

[M] 봐 둘 것 — CLAUDE.md 0장이 걸림을 괄호로 일부만 나열합니다
    CLAUDE.md 0장 둘째 예외(diff +61 줄 언저리)
    「(논의 · 첨부 · 확인 요청 · 활동 기록 · 남는 줄의 관련업무 · 선행 · 상위)」 로 적혀 있습니다.
    이번에 더한 업무 규칙이 빠졌고, 알림 기록 · 회의 항목 전환 · 초안 제출 · 따라온 논의도 없습니다.
    스크립트 머리 주석과 BD-b 에는 전부 있습니다.
    같은 목록이 기준 문서에 일부만 옮겨진 모양이라, 다음에 하나 더 늘면 또 갈립니다.
    둘 중 하나를 권합니다.
      - 「… 등 · 목록은 스크립트 머리」 로 가리키기만 한다
      - 업무 규칙을 더한다

[N] 봐 둘 것 — 문구가 정확하지 않은 곳 둘
    docs/review/최근.md 7-1 표 [H] 줄
      「출력은 data/… 꼴로」 라고 적혔습니다.
      실제 코드는 데이터 폴더의 마지막 이름을 찍습니다(임시 폴더에서는 그 폴더 이름).
      운영에서는 data/ 라 틀린 값은 아니고, 첫 검토 [H] 의 지적(다른 곳이어도 data/ 로 찍힘)은 고쳐졌습니다.
    docs/봐둘것.md BD-b 「도구」 문단
      지문을 「수 · id 합 · 산 수」 로 적었습니다.
      코드와 보고 2장은 library_id 합까지 넷입니다. 빠진 것일 뿐 틀린 말은 아닙니다.

새는 값
  없음 — 바뀐 차이(스크립트 출력 두 줄 · 시험 두 개 · 문서 줄)에 제목과 사람 이름이 없습니다.
  id 와 수뿐입니다. 멈춤 문구도 id 목록만 찍습니다.

새로 생긴 틀린 말
  없음 — [M] · [N] 은 틀린 말이 아니라 모자란 말입니다.
  보고 1장의 출력은 제가 사본에서 낸 것과 글자가 같습니다.
  3장 「고장 열넷」 은 커밋된 시험이 아니라 심었다 되돌린 기록이라, 저는 다시 심어 보지 않았습니다.
  코드를 읽기로는 --기대 두 가드와 걸림 두 줄이 각각 a10 · a11 에서 빨개지는 모양입니다.

예상된 빈 자리
  docs/review/최근.md 6-2 의 @@검토2@@ 와 8장은 이 답 뒤에 채울 자리로 알고 넘깁니다.
  채운 뒤 글검사와 pytest 를 다시 돌리는 것(11-3 4단계)만 남습니다.

사람에게 묻는 것
  1. --실행 판에서 라이브러리 id 뿐 아니라 run 수(26)와 회차별 수도 미리보기와 같은지 볼까요? ([K])
  2. CLAUDE.md 0장의 걸림 괄호를 「목록은 스크립트 머리」 로 가리키기만 할까요, 빠진 것을 더할까요? ([M])
  (첫 검토의 물음 1 · 2 · 5 — 대상 수 갈래 · 상위 여덟 · LIBRARY_ONLY — 는 사람칸에 그대로 있습니다)
━━━ 검토자 여기까지 ━━━
```

# 7. 검토 뒤 고침 (앱 코드는 안 고침 — 재시작 안 함)

## 7-1. 첫 검토가 짚은 것

| 짚은 것 | 고친 것 |
|---|---|
| [A] 직전 재확인이 사람이 본 미리보기와 안 견줌 | `--실행` 에 `--기대`(사람이 본 라이브러리 id)를 받고, 없거나 지금 목록과 다르면 멈춤. 미리보기가 그 명령 줄을 찍음 · `test59_a10`(걸림이 풀려 목록이 늘어난 경우) |
| [B] 라이브러리에 걸린 활동 기록 · 업무 규칙을 안 셈 | `target_type='task_library'` 기록과 비지 않은 `rules` 를 걸림에 더함 · 글자 값을 견주는 줄을 심어 재는 `test59_a11`(run 활동 기록 포함). 운영 수는 그대로(둘 다 0) |
| [D] 대조 표 0c 판정 | 「다르게 했다」 로 · 대기 아닌 상태값이 시드 값이라는 것을 BD-b 에 적음 |
| [F] 훑기에서 `seed()` 를 실제로 부르는 시험 둘을 빠뜨림 | 0장 표에 `tests/test_seed.py` · `tests/test_login.py` 를 더함 |
| [G] 6-1 · 8장 미완성 | 채움 — 글검사 · pytest 는 8장 |
| [H] 다음 판이 할 일 · 출력 경로 | BD-b 에 「다음 판이 할 것」(같은 id 확인 · 사본을 `지키는판` 에 · 되돌리기 절차와 대가) · 출력은 데이터 폴더의 마지막 이름과 파일 이름으로(운영에서는 `data/…`) |
| 고장 심기 | 새 가드 다섯을 더해 **열넷** 전부 잡힘(3장) |
| [C] [E] [I] [J] | 고칠 것 없음 |
| 사람에게 묻는 것 1 · 2 · 5 | 사람칸 「정할 것」 에 이미 있음. 3 · 4 는 [A] · [B] 로 넣어 두었다(사람이 빼라 하면 뺀다) |

## 7-2. 두 번째 검토 뒤 고침 (다시 보이지 않았다 — 11-3)

| 짚은 것 | 고친 것 |
|---|---|
| [K] `--기대` 가 run 은 안 견줌 | 코드는 그대로 두고 BD-b 「다음 판이 할 것」 에 run 수(26) · 회차별 수까지 미리보기와 같은지 보라고 적음. 지금 회차는 둘뿐이라 당장 새는 곳 없음(검토 판단) |
| [L] 지울 것이 없을 때 안내 줄 「(없음)」 을 붙여 넣으면 트레이스백 | 그때는 안내 줄 대신 「지울 것이 없습니다 — 실행할 것도 없습니다」 를 찍음 |
| [M] CLAUDE.md 0장이 걸림을 일부만 나열 | 「… 등 — 목록은 그 스크립트 머리」 로 가리키고 업무 규칙을 넣음 |
| [N] 7-1 의 출력 경로 · BD-b 의 지문 | 7-1 을 「데이터 폴더의 마지막 이름」 으로 · BD-b 지문에 library_id 합을 더함 |
| 사람에게 묻는 것 1 · 2 | 1 은 [K] 대로 문서에 넣음(코드로 넣을지는 다음 판이 사람에게 물어도 됨) · 2 는 [M] 대로 가리키기로 |

# 8. 마지막으로 돌린 것 (보고를 다 채운 뒤)

`git diff --name-only --cached d18a0c1`(커밋 직전 · 커밋 뒤 `d18a0c1..HEAD` 와 같은 목록):

```
CLAUDE.md
docs/review/2026-09-17-출처진단.md
docs/review/최근.md
docs/봐둘것.md
docs/인계.md
scripts/테스트시드삭제.py
seed.py
tests/test_stage59.py
```

- 글검사(`scripts/글검사.py`): 7/7 통과
- pytest(옵션 없음 · `.venv/Scripts/python.exe -m pytest`) 마지막 줄: `1692 passed, 3 warnings in 1770.83s (0:29:30)`
