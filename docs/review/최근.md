사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: 하나 — 자가시험 표의 **「안 심음 / 심음」 두 칸**을 그대로 둘지. 지시문 7 이 시킨 모양이라 이 판은 그대로 뒀고, 커밋 전 검토가 「안 심음 칸은 갈래마다 다시 재는 것이 아니라 맨 위 깨끗한 판을 옮겨 적은 것이라 늘 같은 값」 이라고 짚었습니다 — 13장
막혀서 못 한 것: **넷.** 앞의 셋은 까닭이 하나입니다 — 이 창이 **브라우저 창 크기를 못 바꿉니다**(`resize_window` 가 「성공」 이라 답하고 실제로는 안 바뀝니다 — 넓게·좁게 세 번 재어 944px 그대로). 그래서 ① 부서 리더 화면 점검(1280px 위가 전제) ② `phone.js`(375·400px) ③ 자가시험을 **브라우저에서 못 돌렸습니다.** **사람이 창을 그 폭으로 맞춰 주시면** 바로 돌립니다 — 11장. 넷째는 **개발 서버를 다시 안 켠 것**입니다(이 판이 `board.py` 를 고친 뒤라 그 서버는 옛 코드를 듭니다 — 다음 판이 그 서버로 화면을 보기 전에 켭니다. 8장)

작성 시각: 2026-09-24 11:20 (KST)
커밋: 3bcfa0c 다음 — 이 판의 커밋
작업: 1단계 남은 것 닫는 판 — 사람이 정한 넷 · 자가시험 빠르게 · 화면 점검

---

# 0. 고치기 전에 훑은 것 (11-3 0단계)

| 무엇을 찾았나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 로그 한 줄을 만드는 곳은 라우터 하나다. **합(`걸러진수`)으로 적고 있어서** 팀별로 가르려면 그 변수를 걷고 조각을 모아야 한다 — 두 자리에 나눠 두면 한쪽만 고쳐진다 |
| 같은 부분 템플릿을 쓰는 화면 | 없다(이 판은 화면을 안 고쳤다). 다만 **점검 스크립트는 세 화면이 함께 쓰므로** `점검()` 의 시그니처를 바꾸면 세 화면이 같이 바뀐다 |
| 그 값을 세어 둔 문서·시험 | `docs/옛말-넘김.txt` 가 **`docs/인계.md` 를 통째로 넘기고** 있었다 — 옛 갱신 블록 때문이었으므로, 그것을 옮기면 **그 넘김을 빼야** 한다(안 빼면 살아 있는 인계가 영영 검사 밖이다). `test66_b02` 가 그 둘을 함께 잰다 |
| 이 판이 낡게 만들 글 | 「열람전용으로_거른_사람=」 이라는 옛 로그 모양(`test65_c03` 이 그 낱말을 잰다 — 함께 고쳤다) |

**이 훑기가 셋을 놓쳤고 전체 pytest 가 여섯으로 잡았습니다** (커밋 전 검토가 짚었습니다) — ㄱ. `점검()` 의 시그니처를 바꾼 것을 시험이 글자로 박고 있었다(`test19_s05`·`s06`) ㄴ. `잰다` 밖에 **✗ 를 찍는 길이 하나 더** 생겼다(`test20_p01`·`p03`) ㄷ. 「할 것」 번호를 얼린 파일로 옮긴 것을 시험이 몰랐다(`test23_i02`). **셋 다 위 표의 둘째·셋째 칸이 잡았어야 하는 종류**입니다 — 둘째 칸에 시그니처 이야기를 적어 놓고도 **그것을 글자로 박은 시험을 찾지 않았습니다.** 다음 판이 같은 자리에서 또 놓칠 수 있어 적어 둡니다.

---

# 1. ① 화면은 한 말, 로그만 팀별로 가른다

**화면 문구는 두 경우가 같습니다** — 「받을 사람이 없습니다」. 가려 적으면
그 팀의 소속 구성이 화면에 드러납니다.

**서버 로그가 팀마다 한 조각씩** 적습니다:

```
[관련팀 알림] run=1 hebron:못감(소속없음,거른사람=0) sketch:보냄(거른사람=2)
[관련팀 알림] run=1 hebron:못감(전원열람전용,거른사람=1)
```

- 사정은 셋 — `소속없음` · `전원열람전용` · `받을사람없음`(나머지, 누른 사람이
  유일한 받을 사람일 때)
- **이름과 아이디는 안 적습니다**(`test66_a03`) — 로그도 새는 자리입니다(11-2)
- **합으로 안 적습니다**(`test66_a04`) — 팀이 둘 이상이면 어느 팀에서 몇을
  걸렀는지 되짚을 수 없습니다. 옛 `열람전용으로_거른_사람=N` 은 걷었습니다

---

# 2. ② 인계는 두 파일

`docs/인계.md` 에는 **H1 · 맨 위 갱신 하나 · 남은 단계 줄 · 「남은 것」 표**만
남기고, 옛 갱신 블록과 지난 판의 안내·기록은 `docs/인계-지난.md` 로 옮겨
**얼렸습니다.**

**처음에 잰 방식이 틀렸고 커밋 전 검토가 잡았습니다.** 「옮긴 것」 을
**옛 파일에서 남긴 것을 뺀 나머지**로 세고 그 합이 맞는지를 봤는데, 그렇게 세면
**옮기다 잃어버려도 합이 맞습니다** — 뺄셈의 나머지는 실제로 저쪽에 갔는지를
묻지 않기 때문입니다. 그 틈으로 앞 판 갱신 블록이 **두 파일 어디에도 없이**
사라졌고, 저는 「차이 1 글자」 라고 적어 두었습니다.

**다시 잰 것은 줄 단위입니다** — 옛 파일의 빈 줄 아닌 줄마다 **지금 두 파일
가운데 어디에 있는지**를 봅니다.

```
옛 docs/인계.md       52,953자 · 661줄   (git show 3bcfa0c:docs/인계.md)
지금 docs/인계.md      2,952자 ·  53줄
지금 docs/인계-지난.md 53,414자 · 678줄
어디에도 없는 줄            0개 ·      0자
```

**고친 것** — 사라졌던 스무 줄을 `인계-지난.md` 에 한 글자도 안 고치고
얹었습니다. 열세 줄은 2026-09-24 「뒤처리 판」 의 맨 위 갱신이고(2026-09-23
셋째 판 **위**에 놓았습니다 — 새 것이 위입니다), 열 줄은 ④ 로 뺀 표의 옛
행입니다(뺀 까닭을 한 줄 달아 함께 얼렸습니다).

**그리고 그 틈을 막는 시험을 뒀습니다** (`test66_b05`). 그때 초록이던 시험
둘은 **각자 한쪽만** 보고 있었습니다 — `b01` 은 「인계.md 에 옛 갱신이
없는가」, `test23_i02` 는 「지난.md 에 할 것이 있는가」. **「저쪽으로 갔는가」
를 보는 자리가 없었습니다.** 새 시험은 지난 쪽에 갱신이 쌓이는지와 지금
갱신이 두 벌이 아닌지를 봅니다. 날짜는 안 박습니다 — 판마다 낡습니다.

- `인계-지난.md` 머리에 규약을 적었습니다 — 얼린 기록이라 고치지 않는다 ·
  새 판은 **자기 갱신을 여기에 쓰지 않고** 다음 판이 `인계.md` 의 맨 위 갱신을
  여기 맨 위로 **옮긴다**. **「옮겨 오는 것은 쓰는 것이 아니다」 를 함께
  적었습니다** — 검토자가 두 문장이 겹쳐 읽힌다고 물었고, 실제로 이 판이 그
  겹침에서 덮어썼습니다
- **옛말 검사는 `인계-지난.md` 를 빼고, `인계.md` 는 다시 봅니다** — 그 넘김이
  원래 옛 갱신 블록 때문에 있던 것이라 함께 걷었습니다(`test66_b02`)
- 규약의 정본은 **CLAUDE.md 11-3** 한 곳입니다(`test66_b04`)

**앞 판 검토의 [고3] 이 이것으로 닫힙니다** — 한 파일 안에서 맨 위는 「다시
저장하면 다시 간다」 인데 옛 블록은 「두 번 안 간다」 라고 말하던 자리입니다.

---

# 3. ③ 새 판 확인은 내용 비교로만

CLAUDE.md **11-3 2단계** 에 박았습니다 — 자산이 새 판인지는 **받아서
SHA-256 을 견주고**, 「그 해시 주소가 200 이었다」 는 증거로 쓰지 않습니다.
**아무 8자리나 붙여도 200 이 오기 때문**입니다(해시를 아예 안 붙이면 404 로
막히는 것이 그 자리의 일부러 그런 동작입니다).

`docs/봐둘것.md` **BI-c 는 처리함 · 정할 것 없음**.

---

# 4. ④ 「남은 것」 표에는 이어받을 것만

기준을 **표 바로 위 한 줄**에 적고, 그 기준으로 표를 한 번 훑어
**다섯 줄을 뺐습니다. 지우지 않고** 갈 곳이 이미 있는 것들입니다.

| 뺀 줄 | 까닭 | 어디로 |
|---|---|---|
| ~~`drawer.js`·`phone.js` 정식 점검~~(2026-09-17 에 닫힘) | **이미 닫힌 것**이라 이어받을 것이 아님 | `docs/인계-지난.md` 의 그때 갱신 |
| ~~부서 리더 개발 계정 만들기~~(2026-09-17 에 닫힘) | 〃 | 〃 |
| 지출 화면의 첫 「되살리기」 누름 | **봐 둘 것 후보** — 다음 판이 이어받을 일이 아님 | 봐둘것 BE-b(그대로 있음) |
| `main` 이 없는 화면 | 〃 | 봐둘것 BE-c(그대로 있음) |
| 로그인 실패 사유 · 「200 을 증거로」 | 〃(그리고 뒤엣것은 이 판이 닫음) | 봐둘것 BI-b · BI-c |

---

# 5. 7 — 자가시험이 판 안에 끝나게

**고장마다 점검을 통째로 다시 돌던 것**이 원인이었습니다(앞 판: 목록 40분 ·
달력 24분을 돌고도 안 끝남). 고장 하나는 **한 항목**을 걸리게 하려고 심는
것이라, 그 항목이 ✗ 가 된 뒤의 나머지는 그 고장에 대해 아무 말도 안 합니다.

| 무엇 | 어떻게 |
|---|---|
| 그 항목까지만 돈다 | `점검({멈춤})` — 찾던 ✗ 가 나오면 거기서 멈춥니다 |
| **짝은 한 표에만** | `고장들` 의 `나와야` 하나가 이미 그 짝입니다 — 표를 새로 만들지 않았습니다 |
| 일부러 멈춘 것과 죽은 것 | **갈랐습니다** — 안 가르면 자가시험이 자기 신호를 「끝까지 못 돌았다」 로 읽습니다 |
| 끝까지 봐야 하는 갈래 | `건너뜀나와야`·`완주못함` 에는 멈춤을 **안 겁니다** — 그쪽은 「✗ 가 안 나는 것」 이 정답이라 끝까지 봐야 판정이 섭니다 |
| **두 칸** | 「안 심음 통과」 와 「심음 실패」 가 **둘 다 맞을 때만** 잡은 것으로 셉니다(`잡았나`). 「안 심음」 은 맨 위의 깨끗한 판이 잰 값입니다 — 그 판은 실패 0 이므로 그 항목도 그때 통과했습니다 |
| 못 심은 갈래 | 두 칸을 `—` 로 두고 **따로 셉니다** — 잡았다도 놓쳤다도 아닙니다 |
| 걸린 시간 | 갈래마다 · 전체 둘 다 냅니다 |

**앞 판 검토가 짚은 불투명 판정에도 고장을 심었습니다**(㉑ — 보드 화면).
왼쪽 라벨 열을 **진짜로 반투명하게** 칠해 그 판정이 ✗ 를 내는지 봅니다.
막는 코드를 느슨하게 고친 판에 그 막는 쪽 시험이 없던 자리입니다(11-3).

**브라우저에서 실제로 재 보지는 못했습니다** — 11장.

---

# 6. 검사 결과

- **글검사: 일곱 전부 통과.**
- **`check_named` 가 새 파일 둘을 「문서에만 있다」 로 잡았습니다** — `git add`
  로 풀렸습니다(그 검사는 `git ls-files` 를 봅니다). 실제로 파일이 없는 것과
  **아직 추적 안 되는 것**을 그 검사는 못 가릅니다
- **`test66_b02` 가 넘김 목록의 낡은 줄을 잡았습니다** — `docs/인계.md` 가
  옛 갱신 블록 때문에 통째로 넘겨져 있었는데, 그것을 옮겼으므로 **넘김도
  함께 걷어야** 했습니다. 안 걷었으면 살아 있는 인계가 영영 검사 밖입니다
**전체 pytest 가 여섯을 잡았고 여섯 다 이 판이 낸 것입니다.** 종류가 셋입니다 —
ㄱ. `점검()` 이 인자를 받게 된 것을 시험이 글자로 박고 있었다(`test19_s05`·`s06`)
ㄴ. `잰다` 밖에 **✗ 를 찍는 길이 하나 더** 생겨 「찍는 길은 하나」 가 깨졌다
(`test20_p01`·`p03` — 멈춤 줄의 문구에서 ✗ 를 뺐습니다)
ㄷ. 「할 것」 번호를 얼린 파일로 옮긴 것을 시험이 몰랐다(`test23_i02`).

**셋 다 0단계 훑기가 잡았어야 하는 종류입니다** — 0장에 그렇게 적었습니다.

- pytest 전체: **`1805 passed, 3 warnings in 884.33s (0:14:44)`** (옵션 없이)

  **이 수는 커밋되는 트리에서 잰 것입니다.** 앞서 돌던 판은 **검토 뒤의 고침
  열 몇 개보다 먼저 시작해** 그 트리를 말하지 않으므로 51%(전부 통과) 에서
  멈추고 다시 돌렸습니다 — 「돌고 있는 것에서 확인한 것만 확인」(11-3 2단계)이
  시험에도 걸리는 자리입니다.
- `git diff --name-only`:

```
CLAUDE.md
app/routers/board.py
docs/checks/drawer.js
docs/review/2026-09-24-목업1단계뒤처리.md
docs/review/최근.md
docs/봐둘것.md
docs/세어둠.txt
docs/옛말-넘김.txt
docs/인계-지난.md
docs/인계.md
tests/test_stage19.py
tests/test_stage23.py
tests/test_stage65.py
tests/test_stage66.py
```

---

# 7. 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 0 — 개발 서버가 떠 있고 HEAD 코드다 | **반만** | 8장 · 10장 — **정적 자산은** 같았지만 그 여섯은 이 판이 하나도 안 고친 파일입니다. 서버는 `board.py` 를 고치기 **전**에 떴고 다시 안 켰으므로 **지금 도는 파이썬은 옛 코드**입니다 |
| ① 두 사정이 로그에 다르게 · 화면 문구는 같다 | O | `test66_a01` · `a02`(덤으로 `a03`·`a04`) |
| ② 인계에 서로 반대인 말이 없다 · 잃은 줄이 없다 | O | 2장 · `test66_b01`~`b05`. **합이 아니라 줄 단위로 쟀습니다** — 합으로 재던 첫 판이 잃은 블록을 못 잡았습니다 |
| ③ 11-3 에 한 곳 · BI-c 처리함 | O | 3장 · `test66_c01` · `c02` |
| ④ 사람칸과 남은 것 표가 같은 것을 말한다 | O | 4장 · 11장 · `docs/인계.md` |
| ⑤ 부서 리더 세 화면 표 | **못 함** | 11장 — 창 폭을 못 바꿔 점검이 스스로 멈춥니다 |
| ⑥ `phone.js` 폭×화면 표 | **못 함** | 11장 — 같은 까닭 |
| ⑦ 고장마다 두 칸 표 · 앞 판 넷과 새 고장이 잡힘 | **반만** | 코드는 고쳤고 낱말로 쟀습니다(`test66_d01`~`d04`). **브라우저에서 실제로 재 보지는 못했습니다** |
| ⑧ pytest 전체 · 글검사 일곱 | O | 6장 |
| ⑧ 검토자 원문 | O | 13장 |
| ⑧ 재시작과 PID · 내용 비교 | O | 12장 |

---

# 8. 개발 서버 (0단계)

떠 있었습니다 — **PID 26044**(다시 안 띄웠습니다). 자산은 **내용 비교(SHA-256)**
로 **6/6 이 HEAD 와 같았습니다**(3장이 박은 그 규칙대로 — 상태 코드로 안 셌습니다).
개발 DB 는 안 건드렸습니다.

**그런데 그 6/6 은 이 판에 대해서는 아무것도 말하지 않습니다** (커밋 전 검토가
짚었습니다). 잰 여섯은 **이 판이 한 글자도 안 고친 파일**이라, 서버가 언제 떴든
같게 나옵니다 — **늘 통과하는 검사에 O 를 찍은 자리**입니다.

**실제로는 옛 코드가 돌고 있습니다.** 이 판이 고친 것은 `app/routers/board.py`
이고 그 서버는 그 고침 **전**에 떴습니다. 파이썬은 켤 때 읽은 코드를 들고
있으므로(11-2), **다시 켜기 전에는 관련팀 알림의 새 로그가 그 서버에서 안
나옵니다.** 이 판은 개발 서버에서 화면 점검을 못 했으니 실제로 걸린 일은
없지만, 다음 판이 그 서버로 ①(팀별 로그)을 확인하려면 **먼저 다시 켜야**
합니다 — 「남은 것」 표에 그 줄을 뒀습니다.

---

# 9. 이 판이 운영에 한 것

**읽기도 안 했습니다.** 이 판은 운영 DB 를 열지 않았습니다.

| | 판 시작 | 판 끝 |
|---|---|---|
| `data/app.db` | 856,064 bytes · 2026-09-24 00:33:10.887 | **856,064 bytes · 2026-09-24 00:33:10.887** (그대로) |

---

# 10. 자산 내용 비교 (③ 의 규칙을 실제로 쓴 자리)

8001 에서 받아 저장소 파일과 SHA-256 을 견줘 **6/6** 이 같았습니다 —
`retreat.css` · `drawer.js` · `datepick.js` · `tasks.js` · `board.js` ·
`calendar.js`. **상태 코드는 안 봤습니다.**

---

# 11. 화면 점검 — 막혔습니다

**로그인된 계정은 `testlead` 가 맞습니다**(테스트리더 · 6 코람데오 리더 ·
화면의 사용자 카드에서 읽었습니다). 주소도 `127.0.0.1:8001` 입니다.
**이 창은 로그인 칸에 값을 안 넣었고 로그아웃도 안 했습니다**(11-2).

**막은 것은 창 폭 하나입니다.**

| 시킨 것 | 필요한 폭 | 실제 | 결과 |
|---|---|---|---|
| `drawer.js` 세 화면 (5) | 1280px **위** | 944px | 점검이 스스로 멈춤 |
| `phone.js` 375 · 400 (6) | 820px **아래** | 944px | 〃 |
| 자가시험 (7) | 위와 같음(점검을 부릅니다) | 944px | 〃 |

`resize_window` 를 **네 번** 불렀고 매번 「성공」 이라 답했는데 `innerWidth` 는
**944 그대로**였습니다 — 1440 · 1500 · 1300(넓게) · 700(좁게). 넓은 쪽도 좁은
쪽도 안 먹으므로 **창이 최대화라서가 아닙니다**(사람이 최대화는 풀어 두셨고,
1707 → 944 로 실제로 줄어 있었습니다).

> **점검을 억지로 돌리지 않았습니다.** 그 폭 전제는 그 점검이 스스로 적어 둔
> 것이고(「넓은 화면에서 돌리면 전부 초록이 나오고 아무것도 안 잰 것이
> 됩니다」), 가드를 꺼서 통과시키면 **잰 적 없는 것을 쟀다고 적는 것**이
> 됩니다 — 11-3 이 가장 여러 번 적어 둔 그 자리입니다.

**사람이 창을 그 폭으로 맞춰 주시면** 이 창이 바로 돌립니다. 셋 다
`docs/인계.md` 의 「남은 것」 표에 있습니다.

---

# 12. 재시작과 PID

**세 번 켰습니다** — `docs/배포-안내.md` 12-1 그대로.

**정본은 마지막 것입니다.** 11-3 2단계가 적어 둔 그대로입니다 — 보고를 쓴 뒤에
`app/` 을 또 고쳤으면(여기서는 커밋 전 검토가 짚어 셋째 로그 사정의 이름을
`누른사람뿐` 으로 바꿨습니다) 다시 켜고 이 줄을 고칩니다. **안 고치면 얼린
판의 PID 가 그 커밋이 아닌 코드에서 잰 값이 됩니다.**

```
(첫 번째 — 검토 전)
멈추기 전 PID: 21580 → 다시 켠 뒤 PID: 23348 · 성공

(두 번째 — 검토 뒤 board.py 의 갈래 이름을 고치고)
멈추기 전 PID: 23348 → 다시 켠 뒤 PID: 32092 · 성공

(세 번째 — 그 자리의 주석을 한 번 더 손보고 · **이 커밋의 코드**)
멈추기 전 PID: 32092
포트가 비었습니다
다시 켠 뒤 PID: 16480
성공 — PID 가 바뀌었습니다
```

**주석만 고친 판에도 다시 켰습니다.** 도는 코드와 커밋되는 글자가 같아야
「이 커밋에서 잰 PID」 라고 말할 수 있습니다 — 주석은 동작을 안 바꾸지만
파일은 바꿉니다.

올라온 뒤 `http://127.0.0.1:8000/login` 이 **HTTP 200** 이었습니다.

**이 판이 고친 것은 파이썬 한 파일(`app/routers/board.py`)입니다** — 정적
자산은 하나도 안 바뀌었습니다. 그래서 내용 비교(③ 의 규칙)는 「서버가 내주는
것이 저장소와 같다」 를 그대로 보여 주지만, **그것이 새 코드가 실렸다는
증거는 아닙니다** — 안 바뀐 파일은 재시작 전에도 같았을 것입니다.

**두 번째 재시작 뒤에 다시 쟀습니다** — 로그인 화면이 부르는 넷 전부:

| 받은 것 | 서버 | 저장소 | |
|---|---|---|---|
| `/static/css/retreat.3f3683f6.css` | `6ff09df17d1e7440…` | `6ff09df17d1e7440…` | 같음 |
| `/static/js/sidenav.a877df3e.js` | `a5b9ff26b8f8ff13…` | `a5b9ff26b8f8ff13…` | 같음 |
| `/static/js/flash.64365f65.js` | `9886a2ca5eaadeec…` | `9886a2ca5eaadeec…` | 같음 |
| `/static/js/push.4a9e585a.js` | `7792a1e10fc9a2d3…` | `7792a1e10fc9a2d3…` | 같음 |

**파이썬 쪽이 새 코드라는 증거는 PID 가 바뀐 것**입니다 — 11-2 가 적어 둔
그대로입니다(「파이썬은 켤 때 읽은 코드를 그대로 들고 있습니다」). 그 파일이
하는 일(관련팀 알림 로그)은 로그인해야 밟히는 자리라 이 창이 눌러 볼 수
없고, **`tests/test_stage66.py` 가 그것을 잽니다.**

---

# 13. 검토자 원문 (커밋 전 검토)

**두 번 봤습니다.** 첫 검토 뒤에 고친 것이 `docs/review/` 밖이라 11-3 대로 그
차이를 한 번 더 보였습니다. 아래는 **둘 다 원문 그대로**입니다.

## 13-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위 스테이지된 변경(3bcfa0c 다음) · 파일 10개 · 보고 docs/review/최근.md

읽은 것 — CLAUDE.md(0·2·4-9·9·10·11-2·11-3·14) · docs/검토-원칙.md ·
         git diff --cached 전부 · docs/review/최근.md 전부 ·
         docs/인계.md · docs/인계-지난.md · HEAD 의 docs/인계.md ·
         app/routers/board.py · app/notifications.py · app/domain/permissions.py ·
         docs/checks/drawer.js · tests/test_stage65.py · tests/test_stage66.py ·
         app/templates/board.html · app/static/css/retreat.css
돌린 것 — scripts/글검사.py (일곱 전부 0) ·
         pytest tests/test_stage65.py tests/test_stage66.py (1 failed, 28 passed) ·
         전체 pytest 는 20분 넘게 안 끝나 이 검토 안에서 못 받음(아래 [막1] 은
         그와 무관하게 확정된 실패다)


━━━ 막아야 함 ━━━

[막1] tests/test_stage65.py:197 — pytest 가 빨갛고 보고는 O 로 적었다

  app/routers/board.py 에서 옛 로그 낱말 `열람전용으로_거른_사람` 을 걷었는데
  tests/test_stage65.py 가 그 낱말을 아직 찾고 있다.

      tests/test_stage65.py:197
      assert "열람전용으로_거른_사람" in 자리, "몇 명을 걸렀는지 로그에 안 남는다"

  실제로 돌렸다 —
      FAILED tests/test_stage65.py::test65_c03_판정을_새로_짓지_않는다
      1 failed, 28 passed in 8.40s

  무게가 여기인 까닭은 「시험 하나가 빨갛다」 가 아니다. 세 가지가 겹친다.

  ㄱ. 보고 0장(11-3 0단계 훑기)이 **이 자리를 정확히 짚었다** —
     최근.md:20 「「열람전용으로_거른_사람=」 이라는 옛 로그 모양
     (`test65_c03` 이 그 낱말을 잰다 — **함께 고쳤다**)」.
     그런데 `git status --short` 에 tests/test_stage65.py 가 없다. **안 고쳤다.**
     검토-원칙 8-2 의 「훑기가 짚은 자리를 실제로 고쳤나」 가 여기서 깨진다 —
     적어 놓고 안 고치면 훑은 뜻이 없다.

  ㄴ. 최근.md:164 가 수용 기준에 「⑧ pytest 전체 · 글검사 일곱 | O」 를 찍었다.
     같은 보고 133줄의 pytest 값은 아직 `PYTEST_LINE` 이다. 지시문 8 이
     「수용 기준의 O 는 값을 채운 뒤에만」 이라고 한 그 자리이고,
     지시문 대조 표(최근.md:279)도 「8 — pytest 전체 … O 는 값 채운 뒤 | 했다」
     로 적었다. **값이 없는데 O 이고, 그 값은 빨갛다.**

  ㄷ. `scripts/check_placeholder.py` 는 `PYTEST_LINE` 꼴을 못 본다(글검사에서
     「안 채운 자리 표시가 없고」 로 초록이 났다). CLAUDE.md 14장이 그 한계를
     스스로 적어 둔 그대로다 — 그러니 이 자리는 기계가 아니라 사람이 막는 자리다.

  고칠 것 — test65_c03 의 셋째 단언을 새 로그 모양으로 바꾸거나(예: 팀 조각에
  `거른사람=` 이 들어가는지) test66 쪽으로 옮기고, pytest 전체를 돌려 값을 채운
  뒤에 O 를 찍는다.


[막2] docs/인계.md · docs/인계-지난.md — 앞 판 갱신 블록이 **두 파일 어디에도 없다**

  옛 docs/인계.md 의 맨 위 갱신(2026-09-24 「목업 확정분 1단계 뒤처리 판」)
  **열 줄이 통째로 사라졌다.** 인계.md 에도 없고 인계-지난.md 에도 없다.
  인계-지난.md 의 첫 갱신은 2026-09-23 셋째 판이다.

  줄 단위로 재 봤다(옛 인계.md 대 지금 두 파일, 빈 줄 제외) —
      어디에도 없는 줄 20개 · 2,436자
        그중 갱신 블록 10줄 · 1,181자
        나머지 10줄은 「남은 것」 표의 옛 행(다섯은 보고 4장이 뺐다고 적은 것,
        다섯은 새 행으로 고쳐 쓴 것 — 이쪽은 문제 없다)

  없어진 갱신 블록이 들고 있던 것(확인한 문자열) —
      · 「4-0 눈금으로 확정」(①)
      · 「화면 점검은 되돌릴 수 없는 자국을 남기는 누름을 안 한다」(②)
      · 「열람 전용을 거른다 … 봐둘것 BI-a 는 닫혔다」(③)
      · 「dedupe_key 가 (업무, 팀, 저장 식별값) 셋이다 … 400」(④)
      · 「앱 결함 하나를 잡아 고쳤다 — … originOf 에 datepick 을 더했다」
      · 「점검 스크립트도 하나 고쳤다 — color-mix … 거짓 실패」
      · 「봐 둘 것 둘을 적었다 — BI-b · BI-c」
      · 「앞 판 보고는 docs/review/2026-09-24-목업1단계구현.md 로 얼렸다」

  이 판이 CLAUDE.md:4656 에 스스로 적은 규약과 정면으로 어긋난다 —
  「판마다 `인계.md` 의 맨 위 갱신을 새로 쓰고 **다음 판이 그 줄을 지난 쪽으로
  옮깁니다**」. 이 판이 바로 그 「다음 판」 인데 옮기지 않고 지웠다.
  인계.md:5 도 같은 말을 한다(「다음 판이 이 갱신 줄을 그리로 옮긴다」).

  **곧바로 끊긴 참조가 하나 생겼다** —
      docs/인계-지난.md 의 2026-09-23 셋째 판 갱신:
      「… 같은 구성으로 다시 저장해도 두 번 안 간다 **(2026-09-24 에 바뀜 —
        맨 위 갱신을 보라)**」
  그 「맨 위 갱신」 이 dedupe_key 를 말하던 줄이었는데 사라졌다. 지금 인계.md 의
  맨 위 갱신에는 dedupe_key 얘기가 없다. 얼린 글이 없는 곳을 가리킨다.

  ── 그리고 보고 2장의 「글자 수 증거」 가 이것을 못 잡은 까닭 (질문 2 의 답)

  보고 52~60줄의 표를 실제로 재 봤다.

      옮긴 것(지난.md 의 머리와 `---` 뒤 본문) = 50,236자   (보고: 50,234)
      옛 인계.md                              = 52,953자   (보고: 52,953)
      52,953 − 50,236                         = 2,717자    (보고: 남긴 것 2,718)
      지금 인계.md 의 실제 글자 수            = **2,131자**

  즉 보고의 「남긴 것 2,718」 은 **지금 파일을 잰 값이 아니라 옛 파일에서 옮긴
  것을 뺀 나머지**다. 그러니 둘을 더하면 옛 파일이 나오는 것이 당연하고,
  **차이 1 글자는 「아무것도 안 잃었다」 의 증거가 아니라 뺄셈의 나머지**다.
  구조상 어떤 손실도 못 잡는다 — 「센 것이 0인데 통과하지 않나」(검토-원칙 2-③)
  와 같은 모양이고, 이 저장소가 실명 검사에서 겪은 그 자리다.

  실제로 남긴 쪽에서 586자(2,717 − 2,131)가 안 남았고, 그중 1,181자짜리 갱신
  블록이 통째로 없어졌다(표 행을 새로 쓰며 늘어난 글자와 상쇄돼 586 으로 보인다).

  질문 2 의 나머지 — 「할 것」 과 「화면 점검」 절은 지난.md 에 그대로 있다
  (각각 25회 · 26회 등장). **「순서표」 는 지난.md 에 0회**인데, 옛 인계.md 에도
  표의 두 행에만 있었고 절 자체가 없었다 — 이 판이 잃은 것이 아니라 **원래부터
  가리킬 곳이 없는 참조**다([봐7]).

  고칠 것 — 옛 맨 위 갱신 열 줄을 인계-지난.md 의 2026-09-23 셋째 판 **위**에
  한 글자도 안 고치고 얹는다. 그리고 「글자 수가 같다」 를 다시 잰다면 **지금
  두 파일을 각각 재어** 옛 파일과 견준다(뺄셈이 아니라).


━━━ 고쳐야 함 ━━━

[고1] 인계 규약이 네 곳에 거의 같은 글자로 있다 — 지시문 2 는 「한 곳에만」 이었다

  · CLAUDE.md:4656~4670 (정본이라고 보고가 말하는 자리)
  · docs/인계.md:5      (갱신 ②)
  · docs/인계.md:34~37  (파일 꼬리)
  · docs/인계-지난.md 머리 (「새 판은 여기 맨 위에 쌓지 않는다」 등)

  꼬리 쪽은 거의 그대로 옮겨 적은 것이다.
      CLAUDE.md  「한 파일에 옛 말과 새 말이 함께 있으면 「새 채팅이 먼저
                  읽는다」 고 스스로 적어 둔 파일이 서로 반대를 말합니다」
      인계.md:36 「한 파일 안에 옛 말과 새 말이 함께 있으면 「새 채팅이
                  먼저 읽는다」 고 적어 둔 파일이 서로 반대를 말하게 된다」

  그리고 인계.md:17~19 의 「표 기준」 도 CLAUDE.md:4670 문단과 거의 같은 글자다
  (이쪽은 지시문 4 가 「표 바로 위에 한 줄」 이라고 시켰으므로 그 자체는 시킨
  것이다 — 다만 CLAUDE.md 쪽이 한 문단을 또 들고 있는 것이 겹침이다).

  보고 67줄은 「규약의 정본은 CLAUDE.md 11-3 한 곳입니다(test66_b04)」 라고
  적었지만, **test66_b04 는 CLAUDE.md 에 그 말이 있는지만 본다.** 다른 곳에
  없는지는 아무도 안 본다 — 「한 곳에만」 이 시험으로 지켜지지 않는다.
  인계.md 의 꼬리에는 「정본은 CLAUDE.md」 라는 표시도 없어서, 다음 사람이
  그 꼬리를 고치면 두 글이 갈린다(검토-원칙 3).

[고2] 로그의 셋째 사정에 시험이 없다 (질문 1 의 답)

  app/routers/board.py:1162~1164

      사정 = ("소속없음" if not 소속
            else "전원열람전용" if 걸러진 == len(소속)
            else "받을사람없음")

  갈래 자체는 **맞다.** 확인한 것 —
      · `소속 = perm.members_of(db, key)` 와
        `people = department_members(db, dept.id)` 는 같은 모집단이다.
        `notifications.department_members` 가 안에서 `perm.members_of(dept.key)`
        를 부르고 `is_readonly` 만 뺀다(app/notifications.py:86~90). 둘 다
        `active_only=True` 기본. 그래서 `걸러진` 은 정확히 열람 전용 수다.
      · `dept_by_key` 는 키 있는 부서만 담으므로 `department_members` 가
        키 없음으로 [] 를 돌려주는 길은 안 밟힌다.
      · 셋째 갈래는 「소속이 있고 · 열람 전용이 전부는 아닌데 · 누른 사람을
        뺐더니 비었다」 = 누른 사람이 유일한 받을 사람. 맞게 선다.

  **이름과 아이디가 샐 자리도 없다.** 로그에 나가는 값은 `key`(부서 키) ·
  사정 낱말 · `걸러진`(정수) 뿐이고, `except` 갈래는 `{key}:막힘` 만 적어
  예외 메시지조차 안 싣는다. 응답의 `failed[*].why` 에만 `str(exc)` 가 간다.

  다만 **셋째 갈래를 밟는 시험이 하나도 없다.** test66_a01 은 첫째·둘째만
  재고, a03·a04 는 보냄 쪽이다. 11-3 의 「막는 코드에 막히는 쪽 시험이 함께」 ·
  「막는 코드에 시험이 없으면 그 경로를 처음 밟는 것은 실제로 막아야 할
  순간」 이 그대로 걸린다. 누른 사람을 그 부서 소속으로 붙이고 혼자 두면
  바로 재진다.

[고3] 수용 기준 「0 — 개발 서버가 떠 있고 HEAD 코드다 | O」 가 ③ 이 막으려던 바로 그 모양이다

  최근.md:156 · 8장(170~174줄) — 근거가 「자산 6/6 이 내용 비교로 같았다」 다.
  그런데 **이 판은 정적 자산을 하나도 안 고쳤다**(바뀐 코드는 app/routers/board.py
  하나). 안 바뀐 파일은 재시작 전에도 같았을 것이므로 그 6/6 은 **늘 통과**한다 —
  이 판이 11-3 에 박은 ③ 이 「상태 코드로 세면 늘 통과한다」 고 말한 것과 같은
  자리다. 보고 12장은 8000 쪽에 대해 이 사실을 스스로 적었는데(236~239줄)
  8장과 수용 기준 표에는 그 단서가 없고 O 만 있다.

  그리고 개발 서버(PID 26044)는 **재시작하지 않았다**(보고 172줄). 즉 지금 8001
  에서 도는 파이썬은 board.py 를 고치기 **전** 코드다. 이 판의 화면 점검이
  notify 경로를 안 밟으므로(docs/checks/drawer.js 에 `관련팀`·`notify` 문자열
  0건 — 확인함) 지금은 해롭지 않지만, 사람이 창 폭을 맞춰 준 다음 판이 그대로
  점검을 돌리면 11-2 가 말하는 「새 화면 + 옛 코드」 다. 보고나 인계에 한 줄이
  필요하다.


━━━ 봐 둘 것 ━━━

[봐1] 자가시험 멈춤 — 중간에서 throw 하면 **그 뒤의 되돌림을 건너뛴다** (질문 4 의 곁가지)

  docs/checks/drawer.js:123~125 의 `throw 멈춤표` 는 점검 한복판에서 튄다.
  점검 안에는 자기가 바꾼 것을 **나중에** 되돌리는 자리가 있다 —
      992~994 · 1049 : nav/body 의 transition 을 껐다가 끝에서 되돌린다
      1010~1013 · 1045~1048 : 사이드바를 접었다가 wasPinned 면 되돌린다
      414~422 : 제목 편집창을 열고 Esc 로 되돌린다
  예를 들어 갈래 ② 는 414줄에서, ⑯ 은 475줄 근처에서 멈춘다 — 사이드바 블록
  (990~1049)에는 아예 못 가므로 그쪽은 안전하지만, **제목 편집창이 열린 채**
  다음 갈래의 점검이 시작될 수 있다. 전에는 모든 판이 끝까지 돌아 되돌림이
  반드시 실행됐다.
  더러운 판에서 시작하면 다음 갈래의 목표 항목이 건너뛰거나 엉뚱하게 ✗ 가 나서
  「놓침」 으로 잡히는데, **그것이 거짓 놓침인지 진짜인지 표가 안 갈라 준다.**
  갈래마다 `location.reload()` 나 드로어 닫기 같은 시작 정리를 두는 것이 안전하다.
  — **브라우저에서 못 재 본 판이라 이것도 실측이 없다.**

[봐2] 「두 칸」 의 둘째 항은 **늘 참**이다 — 질문 4 에 대한 답

  docs/checks/drawer.js:1693
      잡았나: 잡혔나 && 깨끗.실패.length === 0

  그런데 바로 위 1644~1645 가 이미 조기 return 한다.
      if (깨끗.실패.length) return {자가시험: false, 실패: 깨끗.실패,
        까닭: '심기 전부터 ✗ 가 있습니다 — 그것을 먼저 고치세요'};

  그러니 루프 안에서 `깨끗.실패.length === 0` 은 **거짓이 될 수 없다.**
  1689 의 `안심음: … : '(깨끗한 판이 이미 빨감)'` 도 못 찍히는 갈래다.
  `놓친갈래++` 도 `잡혔나` 만 보므로 결론값(`자가시험`)은 전과 같다.

  **지시문의 「두 번 다 재어」 를 만족하나** — 뜻은 만족한다. 깨끗한 판의 실패가
  0 이면 그 안에 든 모든 항목이 그때 통과했고, 그러니 심은 뒤 나온 ✗ 는 새 것이다
  (「짝이 틀려 거짓 통과」 를 막는 데 필요한 것이 정확히 이것이다). 보고 112줄의
  설명도 그 논증을 제대로 적었다.
  **다만 「두 번 쟀다」 가 아니라 「한 번 잰 값을 갈래마다 다시 적은 것」** 이고,
  이 조기 return 은 **이 판이 만든 것이 아니라 전부터 있던 것**이다. 즉 이 판이
  새로 얻은 보호는 없고 표의 칸만 늘었다.
  그리고 tests/test_stage66.py:249 가 **죽은 조건을 글자로 박는다** —
      assert "잡았나: 잡혈나 && 깨끗.실패.length === 0" in 자리
  (실제 문자열은 `잡혔나`). 나중에 조기 return 을 걷어도 이 시험은 아무 말을
  안 하고, 반대로 이 식을 정리하면 시험이 빨개진다.
  **정할 것** — 사람이 「한 번 잰 값을 근거로 쓰는 것」 을 받을지, 아니면 갈래마다
  목표 항목만 다시 재는(멈춤을 쓰면 싸다) 진짜 두 번째 측정을 요구할지.

[봐3] 로그의 셋째 사정 이름이 화면 문구와 같은 말이다

  `받을사람없음` 은 화면의 「받을 사람이 없습니다」 와 사실상 같은 말이라,
  로그만 보고는 「누른 사람이 유일한 받을 사람이었다」 를 못 읽는다.
  ① 의 목적이 **총무팀이 할 일을 가르는 것**(사람을 붙일지 · 권한을 바꿀지 ·
  아무것도 안 해도 되는지)이므로 셋째도 이름이 그 일을 말하는 편이 낫다
  (예: `누른사람뿐`). 사람 이름이 아니라 사정 이름이라 새는 자리도 아니다.

[봐4] 멈춘 판에도 `완주 = true` 를 찍는다

  docs/checks/drawer.js:1289 — 「일부러 멈춘 것과 죽은 것은 다르다」 는 판단은
  맞다(안 가르면 자기 신호를 고장으로 읽는다). 다만 `완주` 는 「끝까지 돌았나」
  를 뜻하는 값이고, 자가시험 표의 `완주` 칸(1696)에 그대로 실린다 —
  끝까지 돈 판과 중간에 멈춘 판이 같은 `true` 로 보인다.
  `results` 에 `· 찾던 ✗ 에서 멈춤` 줄이 남으니 되짚을 수는 있다. 값을 갈라 둘지는
  사람이 볼 것.

[봐5] 「평소 점검은 멈춤 없이 돈다」 에 막히면 안 되는 쪽 시험이 없다

  tests/test_stage66.py 의 d01~d04 는 멈춤이 **있는지**만 본다.
  `return window.__자가시험 ? 자가시험() : 점검();`(1714줄)이 옵션 없이 부르는지는
  아무도 안 잰다. 여기가 새면 평소 점검이 첫 ✗ 에서 멈춰 나머지를 안 재는데,
  그때도 「N/N 통과」 처럼 보인다.

[봐6] CLAUDE.md:4656 의 자리 — 「하는 일이 아니라 목록」 절 안에 하는 일을 넣었다

  새 인계 규약은 `### 마치기 전에 되짚는 것 (하는 일이 아닙니다)`(4588줄) 안에
  들어갔다. 그 절의 머리말(11-3 4132줄)은 「**하는 일이 아니라 목록**입니다 —
  다 마친 뒤 읽습니다」 라고 못박는다. 그런데 이 규약은 **판마다 해야 하는 일**
  (「다음 판이 그 줄을 지난 쪽으로 옮깁니다」)을 담고 있다 — [막2] 가 바로 그 일을
  안 한 것이다. 3단계(문서 되짚기)나 4단계(보고 얼리기) 쪽이 자리로 맞아 보인다.

[봐7] 「순서표」 참조는 가리킬 곳이 없다 (질문 2 의 일부)

  docs/인계.md:29 「`docs/인계-지난.md` 의 「할 것」 16 · 순서표 8」
  docs/인계.md:30 「순서표 7 · 봐둘것 BA-c」
  지난.md 에 「순서표」 는 0회다. **옛 인계.md 에도 이 두 행에만 있었고 절은
  없었다** — 이 판이 잃은 것이 아니라 원래부터 끊긴 참조다. 30번 행은 어느
  파일의 순서표인지도 안 적혀 있다.

[봐8] ㉑ 는 잴 자리가 없으면 「통과로 지나감 — 검사가 새는 입구다」 로 나온다 (질문 5 의 답)

  **심는 CSS 는 맞다.** 확인한 것 —
    · 점검이 보는 요소는 `sheet.querySelectorAll('.row.main .lc, .row.sub .lc')`
      (drawer.js:1110)이고 `sheet = $('sheet')`(193줄).
    · app/templates/board.html:68 이 `<div class="sheet" id="sheet" …>` 라
      **class 와 id 가 둘 다 있다** — 심는 `.sheet .row.main .lc` 가 맞는다.
    · `!important` 라 retreat.css 를 이긴다. 계산값이 `rgba(…, 0.5)` 로 나오고
      `불투명()`(1075~1092)의 마지막 갈래가 `parts[3] >= 1` 로 false → ✗.
    · ✗ 의 까닭은 '왼쪽 라벨 열 배경이 반투명 — 뒤의 바가 비친다'(1130) 이고
      `나와야`('왼쪽 라벨 열 배경이 반투명')를 포함한다 → 멈춤도 맞게 걸린다.
    · 불투명 판정 전체가 `if (isBoard)`(1055) 안이므로 **보드 화면에서만 심는
      것이 맞다.**

  남는 것 하나 — 그 판정은 `if (main && team)`(1112) 안에서만 돈다. 부서 줄이나
  Main/하위 줄이 안 보이는 판(필터·좁은 회차)에서는 항목 자체가 안 서고, 그때
  ㉑ 는 `못심음` 이 없어서 **「통과로 지나감 — 검사가 새는 입구다」** 로 찍힌다.
  「이 화면에 그 자리가 없다」 와 「판정이 샌다」 가 같은 칸에 들어간다 —
  다른 갈래들이 `못심음` 으로 가려 둔 것과 어긋난다.


━━━ 없는 것 ━━━

· 실명·연락처·계좌가 새 파일로 간 자리 — 없음. 새 로그는 부서 키 · 사정 낱말 ·
  정수만 싣고, tests/test_stage66.py 의 이름은 CLAUDE.md 가 쓰는 가명이다.
  scripts/글검사.py 의 check_names 통과(읽은 실명 0 아님 · 이미지 18개 확인됨).
· 사람칸 — 보고 1~5줄에 다섯 줄이 그 순서로 있고 네 값이 다 차 있다
  (「없음」 도 값으로 적혔다). check_handoff 통과.
· 지시문 대조 표 — 22줄로 지시문 0~9 를 다 덮는다. 「안 했다」 · 「다르게 했다」
  에 까닭이 붙어 있다. 빠진 항목 없음.
· 고치기 전 훑기(0단계) — 보고 13~20줄에 있고 네 칸이 다 채워졌다.
  다만 그 훑기가 짚은 한 줄을 안 고친 것이 [막1] 이다.
· 앞 판 보고 얼리기 — docs/review/2026-09-24-목업1단계뒤처리.md 가 HEAD 의
  최근.md 와 **바이트까지 같다**(25,033자). 앞 판의 …구현.md 와 이름이 다르다.
· 글검사 일곱 — 전부 exit 0 으로 직접 확인했다.


━━━ 사람에게 묻는 것 ━━━

1. [막2] 의 앞 판 갱신 열 줄을 **되살릴지**. 되살린다면 인계-지난.md 의
   2026-09-23 셋째 판 위에 한 글자도 안 고치고 얹는 것이 맞는지
   (얼린 글에 나중 것을 위에 쌓는 셈이라, 그 파일 머리의 「새 판은 여기 맨 위에
   쌓지 않는다」 와 어떻게 읽힐지를 사람이 정해야 한다 — 그 문장은 *인계.md 에서
   옮겨 오는 것*을 막는 뜻이 아닌 듯하지만 글자로는 겹친다).

2. [봐2] — 「안 심음」 칸을 깨끗한 판 하나로 갈음하는 것을 받을지.
   뜻은 만족하지만 **새로 얻은 보호는 없고**, 시험이 늘 참인 조건을 박는다.
   갈래마다 목표 항목만 다시 재는 진짜 두 번째 측정을 요구할지.

3. [고1] — 인계 규약을 CLAUDE.md 한 곳으로 줄일지(인계.md 꼬리와 지난.md 머리는
   「정본은 CLAUDE.md 11-3」 한 줄로), 아니면 지금처럼 각 파일이 자기 머리말로
   말하게 둘지. 후자라면 「한 곳에만」 이라는 지시문 문장과 보고 67줄의 문장을
   고쳐야 한다.

4. [봐3] — 셋째 사정 이름을 `누른사람뿐` 처럼 할 일이 보이는 말로 바꿀지.

5. [고3] — 다음 판이 화면 점검을 돌리기 전에 **개발 서버를 먼저 재시작**하도록
   인계에 한 줄을 남길지.

━━━ 검토자 여기까지 ━━━
```

## 13-2. 다시 보기 (첫 검토 뒤에 고친 것)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 · 범위: 첫 검토 뒤에 고친 것 (CLAUDE.md 11-3 「검토 뒤에 고친 것은 검토를 안 거친 것입니다」)

첫 검토를 낸 뒤 작업 트리가 움직였습니다. 바뀐 파일은 넷입니다 —
  docs/checks/drawer.js · tests/test_stage19.py · tests/test_stage23.py ·
  tests/test_stage65.py
(전부 `docs/review/` 밖이라 다시 봅니다.)


━━━ 첫 검토에서 올린 것의 지금 상태 ━━━

[막1] pytest 빨강 — **닫혔습니다.**

  세 파일이 고쳐졌고 실제로 돌려 확인했습니다.
      pytest tests/test_stage19.py tests/test_stage20.py tests/test_stage23.py
             tests/test_stage65.py tests/test_stage66.py
      → 68 passed in 54.45s

  **그런데 제 첫 보고가 실패를 하나로 적은 것은 틀렸습니다.** 그때 돌린
  전체 pytest 가 뒤늦게 끝났고 결과가 이랬습니다 —

      6 failed, 1795 passed, 3 warnings in 1553.80s (0:25:53)
      FAILED tests/test_stage19.py::test19_s05_평소_점검과_따로_돈다
      FAILED tests/test_stage19.py::test19_s06_심은_것을_반드시_걷어낸다
      FAILED tests/test_stage20.py::test20_p01_찍는_길과_세는_길이_각각_하나다
      FAILED tests/test_stage20.py::test20_p03_안_세는_줄을_심으면_걸린다
      FAILED tests/test_stage23.py::test23_i02_인계_문서가_있고_열세_항목이_다_있다
      FAILED tests/test_stage65.py::test65_c03_판정을_새로_짓지_않는다

  **하나가 아니라 여섯이었고, 여섯 다 이 판이 낸 것입니다** — 우연히 겹친
  옛 실패가 아닙니다. 종류가 셋입니다.

    ㄱ. `점검()` → `점검(옵션)` 으로 인자를 받게 바꾼 것을 시험이 몰랐다
        (test19_s05 · s06). s06 의 정규식이 `점검\(\)` 을 글자로 박고 있었다.
    ㄴ. **`잰다` 밖에 ✗ 를 찍는 길이 하나 더 생겼다** (test20_p01 · p03).
        새로 넣은 줄이 이랬습니다 —
            results.push('· 찾던 ✗ 에서 멈춤 (자가시험)');
        `찍는자리` 가 그것을 두 번째 「✗ 찍는 자리」 로 세어, **점검의 ③
        (찍는 길과 세는 길이 각각 하나) 이 깨졌습니다.**
    ㄷ. `인계.md` 의 「할 것」 1~13 을 지난.md 로 옮긴 것을 시험이 몰랐다
        (test23_i02).

  **세 종류 다 11-3 0단계 훑기가 잡았어야 하는 것**입니다 —
  「같은 이름을 쓰는 데」(점검의 시그니처) · 「그 값을 세어 둔 문서·시험」
  (인계의 할 것 번호 · ✗ 를 세는 시험). 보고 0장은 네 칸 중 셋을 「없다」 나
  다른 것으로 채웠고 이 셋은 안 나왔습니다. 고친 것은 좋지만, **다음 판이
  같은 자리에서 또 놓칠 수 있는 종류**라 적어 둡니다.

  그리고 이번 판의 보고 133줄은 아직 `PYTEST_LINE` 입니다. 지금 값을 채울 때
  **전체를 다시 돌린 값**이어야 합니다 — 위의 68 passed 는 다섯 파일만 돌린
  것이라 전체의 값이 아닙니다.


[막2] 앞 판 갱신 블록 상실 — **아직 그대로입니다. 여전히 막아야 함입니다.**

  다시 쟀습니다(옛 docs/인계.md 대 지금 두 파일, 빈 줄 제외) —
      어디에도 없는 줄 20개 (갱신 블록 10줄 1,181자 + 표 옛 행 10줄)
      `'뒤처리 판' in docs/인계-지난.md` → False

  2026-09-24 「목업 확정분 1단계 뒤처리 판」 의 갱신 열 줄이 인계.md 에도
  인계-지난.md 에도 없습니다. 이 판이 CLAUDE.md:4656 에 스스로 적은
  「다음 판이 그 줄을 지난 쪽으로 옮깁니다」 를 지키지 않은 자리이고,
  인계-지난.md 의 2026-09-23 셋째 판 갱신이 가리키는
  「(2026-09-24 에 바뀜 — 맨 위 갱신을 보라)」 가 없는 곳을 가리킵니다.

  보고 2장의 「글자 수 차이 1」 은 여전히 **뺄셈의 나머지**라 이것을 못 잡습니다
  (옮긴 것 50,236 · 옛 파일 52,953 · 둘의 차 2,717 인데 **지금 인계.md 의 실제
  글자 수는 2,131** 입니다).

[고1] 인계 규약이 네 곳 — 그대로입니다.
[고2] 셋째 사정 `받을사람없음` 시험 없음 — 그대로입니다.
[고3] 수용 기준 「0 … | O」 의 근거가 늘 통과하는 검사 — 그대로입니다.
[봐1~봐8] — 아래 [다시1] 말고는 그대로입니다.


━━━ 이번에 바뀐 넷을 본 결과 ━━━

[다시1] docs/checks/drawer.js — `✗` 를 문구에서 뺀 것: **맞는 고침입니다**

      - results.push('· 찾던 ✗ 에서 멈춤 (자가시험)');
      + results.push('· 찾던 자리에서 멈춤 (고장 심기)');

  test20_p01 이 요구하는 「✗ 를 찍는 길은 하나」 를 되돌립니다. 그리고 그 줄은
  `·` 로 시작해 **건너뜀 줄**로 세어지므로(1296줄의 `건너뜀`) errors 를 안
  늘립니다 — 뜻도 맞습니다.

  다만 첫 검토의 [봐4] 가 여기서 더 무거워집니다. 이제 멈춘 판의 표시는
  `완주: true` + `· 찾던 자리에서 멈춤` 한 줄뿐이고, **그 줄은 건너뜀 수에
  섞여 들어갑니다.** 자가시험 표의 `완주` 칸만 보면 끝까지 돈 판과 안 갈립니다.

[다시2] tests/test_stage19.py:122 — 정규식 넓히기: **맞습니다만 한 칸 헐거워집니다**

      - r"try \{[^}]*점검\(\);[^}]*\} finally \{ 걷어낸다\(\); \}"
      + r"try \{[^}]*점검\([^)]*\);[^}]*\} finally \{ 걷어낸다\(\); \}"

  `점검({멈춤})` 을 받게 한 것은 맞습니다. 그 시험이 지키려는 것은 「던지면 심은
  고장이 남는다」 이므로 인자는 본질이 아닙니다.

  **봐 둘 것** — 같은 파일 test19_s05 는 통과했는데, 그 시험이 보는 것은
  「평소 점검 안에 `자가시험` 이라는 글자가 없는가」 입니다. 이 판이 만든
  **진짜 위험은 반대쪽**입니다: 평소 점검이 `멈춤` 없이 도는가. 지금 그것을
  재는 시험은 어디에도 없습니다(첫 검토 [봐5]). s05 가 초록이라 「다 봤다」 로
  읽히기 쉬운 자리입니다.

[다시3] tests/test_stage23.py:265 — 「할 것」 을 지난.md 에서 찾게: **맞습니다**

  주석에 까닭(11-3 · 2026-09-24)을 함께 적었습니다. `인계-지난.md` 에
  1~13 번이 다 있고 「무엇 · 재료 · 미정」 과 도막0 보고 참조도 남아 있는 것을
  확인했습니다(제가 따로 세어 「할 것」 25회 · 「화면 점검」 26회).

  **봐 둘 것** — 이 시험은 이제 **얼린 파일만** 봅니다. `docs/인계.md` 가
  비어도, 심지어 없어도 이 시험은 초록입니다. 「새 채팅이 처음 읽는 파일이다」
  라는 그 시험의 독스트링이 가리키는 파일은 여전히 `인계.md` 인데, 재는 것은
  지난 쪽이 됐습니다 — **[막2] 가 정확히 그 틈으로 지나갔습니다.**
  살아 있는 인계.md 의 모양은 test66_b01 이 보지만, 그쪽은 「옛 갱신이 없는가」
  만 보고 **「옮겨야 할 것이 실제로 저쪽에 갔는가」 는 아무도 안 봅니다.**
  [막2] 를 고칠 때 그 짝도 함께 두는 것이 맞아 보입니다
  (예: 인계.md 에서 사라진 `갱신:` 줄이 지난.md 에 있는가).

[다시4] tests/test_stage65.py:197~199 — 로그 모양 바꾸기: **좋습니다. 짝이 생겼습니다**

      + assert "거른사람=" in 자리, "몇 명을 걸렀는지 로그에 안 남는다"
      + assert "열람전용으로_거른_사람" not in 자리, "팀을 안 가르고 합으로 적는다"

  막는 쪽(새 모양이 있다)과 옛 모양이 안 남았다는 쪽이 한 줄씩 있고, 주석에
  날짜와 까닭이 붙었습니다. test66_a04 가 **출력**에서 같은 것을 재고 이쪽은
  **코드**에서 재므로 두 겹입니다 — 겹침이 아니라 다른 자리입니다.


━━━ 이번 판에서 새로 잡은 것은 없습니다 ━━━

넷 다 제가 짚은 것을 좁게 고친 것이고, 새로 샌 자리는 안 보입니다.
scripts/글검사.py 일곱도 다시 돌려 전부 0 이었습니다.


━━━ 남은 막아야 함 (하나) ━━━

[막2] 가 닫히지 않았습니다. 이것이 닫히기 전에는 커밋하면 안 됩니다 —
**커밋 뒤에는 그 열 줄이 어느 파일에도 없어, 되살리려면 git 히스토리를
뒤져야 합니다.** 지금은 `git show HEAD:docs/인계.md` 한 줄로 그대로 꺼낼
수 있습니다.

  꺼내는 자리 — 옛 docs/인계.md 의 3~13줄
  놓을 자리 — docs/인계-지난.md 의 `---` 바로 아래, 2026-09-23 셋째 판 **위**
  고칠 것 없음 — 한 글자도 안 고칩니다(얼린 기록)


━━━ 사람에게 묻는 것 ━━━

1. [막2] — 열 줄을 지난.md 맨 위(2026-09-23 셋째 판 위)에 얹는 것이 맞는지.
   지난.md 머리의 「새 판은 여기 맨 위에 쌓지 않는다」 는 *새로 쓰는 판*을
   막는 말로 읽히지만, 글자로는 이 복원과 겹칩니다. 사람이 한 번 갈라 주세요.

2. [다시3] — 「인계.md 에서 사라진 갱신 줄이 지난.md 에 있는가」 를 재는 짝을
   지금 둘지, 다음 판으로 미룰지.

3. 첫 검토의 물음 1~5 는 아직 열려 있습니다(인계 규약 네 곳 · 「두 칸」 을
   받을지 · 셋째 사정 이름 · 개발 서버 재시작 한 줄).

4. 보고 133줄의 pytest 값은 **전체를 다시 돌린 값**으로 채워 주세요 —
   지금 확인된 것은 다섯 파일 68 passed 뿐이고, 직전 전체 판은 6 failed 였습니다.

━━━ 검토자 여기까지 ━━━
```

## 13-3. 검토 뒤에 이 판이 한 것

**다시 보기가 낸 [막2] 를 닫았습니다** — 사라진 스무 줄을 얼린 파일에 되살리고
(2장), 재는 방식을 뺄셈에서 **줄 단위 대조**로 바꾸고, 그 틈을 막는 시험
(`test66_b05`)을 뒀습니다. [막1] 은 다시 보기가 닫혔다고 확인했습니다.

그 밖에 검토가 남긴 것 가운데 이 판에서 닫은 것 —

| 검토 | 한 것 |
|---|---|
| [고1] 인계 규약이 네 곳 | 정본은 CLAUDE.md 11-3 하나. `인계.md` 꼬리는 가리키기만 (`test66_b04`) |
| [고2] 셋째 사정에 시험이 없다 | `test66_a05` — 소속이 있고 열람 전용도 아닌데 받을 사람이 누른 사람뿐인 갈래 |
| [고3] 「0 — 개발 서버 HEAD」 의 O | **반만**으로 내렸습니다. 잰 여섯은 이 판이 안 고친 파일이라 늘 통과합니다 (8장) |
| [봐3] `받을사람없음` 이라는 이름 | `누른사람뿐` 으로 — 앞 이름은 셋 다에 맞는 말이라 가른 뜻이 없었습니다 |
| [봐4] 멈춘 판과 끝까지 돈 판이 같아 보인다 | `멈춤함` 을 따로 돌려주고 표에 `멈췄나` 칸 (`test66_d06`) |
| [봐5] 평소 점검이 `멈춤` 없이 도는가 | `test66_d05` — 자가시험 밖에서 인자를 주면 빨개집니다 |
| [다시3] 옮김의 짝을 지금 둘지 | 뒀습니다 — 그것이 [막2] 가 지나간 틈입니다 |
| [봐6] 인계 규약이 CLAUDE.md 의 「하는 일이 아닙니다」 절에 있다 | 11-3 **3단계**로 옮겼습니다 — 그 절은 다 마친 뒤 읽는 목록이고 이것은 **하는 일**입니다. 글자는 한 자도 안 고쳤습니다 |

**못 닫은 것은 브라우저가 필요한 것들입니다** — [봐1](심은 고장이 다 걷혔나) ·
[봐8](㉑ 이 목록·달력에서 「못 심음」 으로 나오나). `docs/봐둘것.md` BI-f · BI-g
에 적었습니다.

**검토자가 사람에게 물은 것 넷 가운데 셋은 이 판이 정했습니다** — 얹는 자리
(지난.md 머리가 「다음 판이 옮긴다」 고 이미 정해 둔 자리라 그대로) · 짝을 지금
둘지(둠) · 셋째 사정 이름(`누른사람뿐`). **「두 칸」 을 받을지는 사람 몫으로
남깁니다** — 지시문 7 이 시킨 모양이라 이 판은 그대로 뒀습니다.

---

# 14. 지시문 대조

| 항목 | 했나 | 비고 |
|---|---|---|
| 0 — 개발 서버가 떠 있나 · HEAD 인가 | 했다 | 8장. **다만 그 확인은 약합니다** — 떠 있어서 다시 안 띄웠는데, **그래서 이 판이 고친 `board.py` 는 그 서버에 안 실렸습니다.** 잰 자산 6/6 은 이 판이 안 고친 파일이라 늘 통과합니다 |
| 1 — 화면 문구는 한 말 · 로그만 팀별로 | 했다 | 1장 |
| 1 — 로그에 팀 키·사정·거른 수 · 이름과 아이디는 안 남김 | 했다 | `test66_a03` |
| 1 — 4-9 에 한 줄 · [봐7] 닫기 | 했다 | 봐둘것 **BI-d** 로 적어 닫았습니다(번호가 없던 항목이라 새로 붙였습니다) |
| 1 수용 — 시험 둘 | 했다 | `test66_a01` · `a02` |
| 2 — 인계-지난.md 로 옮겨 얼리기 | 했다 | 2장 |
| 2 — 인계.md 에 넷만 · 지난 머리에 한 줄 | 했다 | `test66_b01` · `b02` |
| 2 — 글자 수 합 | **다르게 했다** | 2장. 뺄셈의 나머지로 세는 방식이라 **잃어버려도 합이 맞았고**, 실제로 한 블록을 잃었습니다(검토가 잡음). **줄 단위 대조**로 바꿨습니다 — 어디에도 없는 줄 0 |
| 2 — check_stale 이 지난은 빼고 인계는 본다 | 했다 | `test66_b02` |
| 2 — CLAUDE.md 한 곳에 규약 | 했다 | `test66_b04` |
| 3 — 11-3 에 박기 · BI-c 닫기 | 했다 | 3장 · `test66_c01`·`c02` |
| 4 — 표 기준 한 줄 · 한 번 훑어 보고에 | 했다 | 4장 |
| 5 — 부서 리더 세 화면 | **안 했다** | 11장 — 창 폭을 못 바꿉니다 |
| 6 — `phone.js` 375·400 · 창 되돌리기 | **안 했다** | 11장 — 같은 까닭. **창을 안 건드렸으니 되돌릴 것도 없습니다**(944 그대로) |
| 7 — 자가시험이 판 안에 끝나게 | **다르게 했다** | 코드는 고쳤고(5장) **브라우저에서 못 재 봤습니다** — 「10분 안에」 도 실측이 없습니다 |
| 7 — 불투명 판정에 고장 하나 | 했다 | ㉑ · `test66_d04` |
| 8 — pytest 전체 · 글검사 일곱 · O 는 값 채운 뒤 | 했다 | 6장 |
| 8 — 검토자 원문 · 검토 뒤 차이 | 했다 | 13장 |
| 8 — 재시작 · 내용 비교로 확인 | 했다 | 12장 |
| 8 — 앞 판 보고를 덮지 않는 이름으로 얼리기 | 했다 | `2026-09-24-목업1단계구현.md` 가 이미 있어 **`…뒤처리.md`** 로 |
| 8 — 인계 맨 위 갱신 · 남은 단계 줄 | 했다 | `docs/인계.md` |
| 9 — 결과 덩어리 | 했다 | 마지막 응답 |
