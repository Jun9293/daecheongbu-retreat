사람이 할 것
노트북에서 직접: 없음 — 운영 실행은 사람이 「그대로 해」 라고 하면 다음 판에서 이 창이 돌린다(3장 · 11-2)
관리자 권한: 없음 — app/ 이 안 바뀌어 재시작하지 않았다(4장)
정할 것: 넷 — ① 「그대로 해」 로 운영에 들일지(3장) ② 식대로 본 줄 중 항목·세부 글에 「식」 이 없는 2 줄을 지금대로 식대로 둘지, 「식」 이 든 줄로 좁힐지(5장 · 봐둘것 BB-h) ③ 계좌 꼴 게이트(봐둘것 BB-c)를 --실행 전에 넣을지 뒤로 미룰지(5장) ④ 지출 비고 88 줄을 안 들이는 것을 그대로 둘지(5장)
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 이 판은 화면이 안 바뀌었다(4장)

작성 시각: 2026-09-14 21:55 KST
커밋: 80d1949 (스크립트 · 시험) · 5b69cd4 (문서) · 이 보고는 그다음 커밋
작업: 재정 차례 6 — 시트를 들여온다 (이 판은 미리보기까지)

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `split_account`(domain/budget · 차례 5 시험 `test47_e02` 가 가르는 곳을 AST 로 센다 — 새 스크립트도 그 검사에 넣었다) · `회차머리`(scripts/비품들여오기) · `attach_receipt`(모델 · 붙이는 곳은 하나) · `next_receipt_number`(첫 검토 B-3 이 짚어 스크립트가 그것을 부르게 했다) |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 템플릿·app/ 안 바뀜 |
| 그 값을 세어 둔 문서 | 인계 머리 · 순서표 차례 6 · 16번 줄 · 봐둘것 BA-f · BB-a · BB-b · BB-c(「차례 6 전에 넣는다」 — 첫 검토 G-4 가 짚었다) · BB-g · CLAUDE.md 7-1(시트 모양) · 14장 표 |

# 1. 한 것

- **`scripts/재정들여오기.py`** — `읽는다`(시트) → `고른다`(계획 · 아무것도 안 바꿈) → `넣는다`(넣기만) → `돌린다`(사본 → 넣기 → DB 를 다시 세어 계획과 견주고 맞을 때만 commit). 규칙은 CLAUDE.md 7-5
- 기본은 미리보기 · `--실행` · 직전 `VACUUM INTO` 사본 · 세션 DB 와 사본 파일이 다르면 멈춤 · `--회차` 없고 열린 회차가 둘이면 멈춤 · 두 번째는 「이미 들여왔습니다」(활동 기록 `재정_들여오기`)
- 출력은 건수만. 값이 든 짝 표는 `DATA_DIR/재정짝표.real.md`, 사람이 짝을 적는 자리는 `DATA_DIR/재정짝표.real.json`
- **`tests/test_stage48.py`** — 합성 워크북(가명·지어낸 값)으로 가~아 를 잰다. 12 passed
- 문서: CLAUDE.md 7-5 열기 · 14장 한 줄 · 인계 머리와 순서표 차례 6 · 16번 · 봐둘것 BA-f · BB-a · BB-b · BB-c · BB-g 머리(원문 그대로) · 새 BB-h(짝 표 건수와 이유)
- 앞 판 보고를 `docs/review/2026-09-14-차례5닫기.md` 로 얼렸다(넘김 두 줄 · 이름-넘김에 저장소 밖 짝 표 둘)

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) seed 취소 · 새 항목 = 계획 | O | `test48_a01` · 운영 사본 리허설 산 예산 항목 9 → 34 (9 취소 + 34 새) |
| 나) 지출 97 · 식대 인원 식/비고 · 못 뽑으면 빈 값 | O | `test48_b01`(식 · 비고 · 못 뽑음 세 갈래) · 운영 사본 지출 97 · 식 12 · 비고 14 · 인원 빈 식대 0 |
| 다) split_account 실제 호출 | O | `test48_c01`(가로채 부른 수 = 계좌 줄 수) · `test48_c02`(스크립트에 가르는 코드가 없다 · AST) |
| 라) 영수증 · 다중 잇기 · 번호 둘 → 두 장 | O | `test48_d01` · 운영 사본 영수증 97 · 공유 1 · 번호 둘 적힌 줄 2 |
| 마) 짝 표 · 시험에 수 박지 않기 | O | `test48_e01`(md 가 서고 json 이 이긴다) · `test48_e02`(글자 규칙) · 운영 수는 시험에 없음(첫 검토 (d)) |
| 바) 두 번째는 멈추고 안 바꿈 | O | `test48_f01` · 운영 사본 두 번째 실행 「이미 들여왔습니다」 |
| 아) 미리보기 수 = 실행 뒤 수 | O | `test48_g01` · 운영 사본 리허설(3장) · 어긋나면 되돌리는지 `test48_g04` |
| 자) pytest 옵션 없이 · 글검사 7/7 | O | 6장 |
| 차) 검토자 원문 | O | 7장 |
| 사) 새는지 검사를 커밋마다 | O | 6장 |

# 3. 운영 자료 — 미리보기와 리허설 (값 없이 건수만)

**운영 DB 는 읽기만 했다.** `mode=ro` 로 열어 `VACUUM INTO` 로 이 창의 임시 폴더에 사본을 떴고, 미리보기와 리허설은 그 사본에서 돌렸다. 운영 `data/` 에는 아무것도 안 썼다.

## 3-1. 미리보기 (회차 1)

| 무엇 | 수 |
|---|---|
| seed 예산 항목 취소 · seed 지출 취소 | 9 · 12 |
| 예산 항목 | 34 (예산금액 0 이 7 · 셈이 안 맞아 셈 칸을 비운 것 1) |
| 수입 | 4 (금액만 2 · 단가·명수로 2) |
| 지출 | 97 |
| 지출 · 예산 항목 글자로 이음 / 없음 / 둘 이상 | 67 / 26 / 4 |
| 지출 · 부서 붙음 / 비움(둘 이상) / 없음 | 39 / 1 / 57 |
| 지출 · 계좌 셋으로 가름 · 끝 빈칸 뗌 · 빈칸 없어 은행 비움 | 64 · 22 · 0 |
| 지출 · 식대 인원 식 / 비고 / 못 뽑음 | 12 / 14 / 0 |
| 지출 · 식대 다시 센 값이 시트와 다름 | 1 |
| 지출 · 식대 아닌데 시트 지원금액이 금액과 다름 | 0 |
| 지출 · 지급됨 · 비고 안 들임 · 번호 둘 적힌 줄 | 41 · 88 · 2 |
| 영수증 · 원본 번호로 / 따로 첨부(메모) · 한 장에 지출 여럿 · 이름표에 없는 번호 | 97 = 89 / 8 · 1 · 0 |
| 잇기 | 99 |
| 짝 표 | 예산 30 · 부서 1 · 영수증 0 · 식대 1 |

## 3-2. 리허설 (같은 사본에서 `--실행`)

- 사본을 떴다(사본 폴더 안 `backups/`) · 넣은 뒤 DB 를 다시 센 수가 계획과 같아 commit
- 다시 센 수: 산 예산 항목 9 → 34 · 산 지출 12 → 97 · 산 수입 0 → 4 · 영수증 12 → 109 · 산 잇기 12 → 111 (seed 12 + 99)
- 식대 줄 26 중 인원 빈 줄 0 · 번호로 들인 영수증 중 메모 있는 것 0(초록 없음)
- 두 번째 미리보기: 「이미 들여왔습니다」 로 멈춤

## 3-3. 「그대로 해」 뒤 다음 판이 할 것

1. 운영에서 미리보기(`--회차 1`) → 수가 3-1 과 같은지 본다. 다르면 `--실행` 안 하고 멈춘다(11-2)
2. `--실행` → 다시 센 수를 보고에 싣는다
3. 짝 표 값은 그때 `data/재정짝표.real.md` 에 선다 — 사람이 보고 `data/재정짝표.real.json` 에 짝을 적으면, 들인 뒤라 이 스크립트로는 다시 안 들어간다. **짝을 먼저 적고 들이려면 「그대로 해」 전에 그렇게 말해 달라** (그러면 다음 판은 미리보기로 짝 표만 세우고 멈춘다)

## 3-4. 되돌리기

들이기 직전 사본 `data/backups/app-날짜.db` 가 그 회차의 seed 가 산 채이고 들여온 행이 없는 판이다. CLAUDE.md 11-2 「되돌리기」 대로 서버를 끄고 그 사본을 `data/app.db` 로 되돌린다(업로드·VAPID 는 이 스크립트가 안 건드려 그대로 둔다). 절차는 `docs/배포-안내.md` 12장.

# 4. 재시작

**건너뛰었다** — 이번 커밋들에 `app/` · `templates/` · `static/` 이 없다(스크립트 · 시험 · 문서만). drawer.js · phone.js 도 같은 까닭에 더해 로그인이 필요해 못 돌렸다(사람칸).

# 5. 판단이 들어간 자리

- **식대 가르기** — 「지원금액이 곱셈 식이거나 비고에 숫자+명」 이면 식대. 운영 사본에서 비고로 식대가 된 14 줄 중 2 줄은 항목·세부 글에 「식」 이 없고 시트 지원금액 = 금액이다(첫 검토 G-3). 식대로 두면 「식대만」 필터에 서고 고칠 때 식대 규칙을 탄다. 사람이 정할 일이라 코드는 안 바꿨다(정할 것 ②)
- **식대 아닌 줄은 지원금액 = 금액** — 시트와 다른 줄이 0 이라 잃는 값이 없다(G-2)
- **계좌 꼴 게이트** — 봐둘것 BB-c 원문이 「차례 6 전에 넣는다」 인데 이 판도 안 넣었다. 실행이 다음 판이라 순서는 아직 안 뒤집혔다(정할 것 ③)
- **비고를 안 들인다** — 88 줄. 식대 줄 비고가 명단이라 BB-a 시트-다가 명단 칸을 고르지 않았고, 다른 줄의 비고도 같은 칸이라 함께 뺐다. 들이면 명단(사람 이름)이 DB 에 들어간다(정할 것 ④)
- **67 과 73** — 앞 판 셈 73 과 이 판 규칙 67 이 다르다. 까닭은 BB-h
- **번호로 들인 영수증에 메모를 안 단다** — 원본 번호가 이미 말하고, 달면 지출 목록 칩마다 글이 붙는다(첫 검토 B-1)
- **넣은 수가 아니라 DB 에서 다시 센 수로 견준다** — 계획에서 되짚은 수는 절대 안 어긋나 되돌림이 헛돈다(첫 검토 G-1)

# 6. 검사와 시험

- 글검사(커밋 전): 7/7 통과
- 새는지 검사(저장소 밖 도구 · 시트의 네 자리 이상 수 · 세 글자 이상 글과 그 조각을 모아 찾는다 · 값은 안 찍는다): 자가시험 심은 둘 다 잡음 · CLAUDE.md · 봐둘것 · 인계 · 넘김 셋 · 스크립트 0.
  **`tests/test_stage48.py` 에 14 · 얼린 판에 1 이 걸렸다** — 줄을 보니 시험 쪽은 합성 워크북에 지어 넣은 둥근 금액(만 단위 · 천 단위)과 CLAUDE.md 7-2 가 이미 적은 예시 금액이 시트의 수와 우연히 같은 것이고, 얼린 판 쪽은 그 판 보고에 적힌 프로세스 번호가 시트의 어떤 수와 같은 것이다. 계좌·이름꼴은 없다. 도구가 지어낸 값과 시트 값을 못 가르는 자리라 그대로 두었다
- `tests/test_stage48.py` 12 passed · stage45 · 47 과 함께 40 passed(검토 전)
- 보고를 다 채운 뒤 돌린 것: 글검사 7/7 통과 · 새는지 검사(이 보고) 0 · pytest(옵션 없이) `1560 passed, 3 warnings in 1462.82s (0:24:22)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위: HEAD 2764138 위 작업 트리 · 커밋 0개 · 파일 9개 (scripts/재정들여오기.py · tests/test_stage48.py · CLAUDE.md · docs/봐둘것.md · docs/인계.md · 세어둠 · 옛말-넘김 · 이름-넘김 · 새로 얼린 판 차례5닫기.md)

실제 시트와 운영 DB 는 열지 않았습니다. 아래에는 값(이름·계좌·금액)을 적지 않았습니다.

[막아야 함]
없음

[고쳐야 함]
G-1  scripts/재정들여오기.py:527-529 · 582-584 · CLAUDE.md 7-5 「계획의 수와 넣은 수가 다르면 되돌림」
     넣는다 가 돌려주는 「넣은 수」 는 DB 를 세지 않고 계획에서 다시 계산합니다.
       예산 항목  len(판.예산)
       수입       len(판.수입)
       지출       판.지출 을 줄 번호로 담은 dict
       잇기       sum(len(e["번호들"])) + len(글영수증)
     고른다 가 수["..."] 를 만든 식과 같습니다. 영수증도 계획의 번호에서만 만들어져 어긋날 수 없습니다.
     그래서 582 의 멈춤은 절대 안 걸립니다. 기준 문서는 이것을 막는 장치로 적었습니다.
     실행 뒤 센다(532-539)에는 잇기가 없습니다.
     그래서 운영에서 수용 기준 아)의 「잇기 99」 가 DB 에서 재진 적이 없을 수 있습니다.
     시험에서는 test48_d01 이 잇기 표를 직접 세서 봅니다.
     보강안: commit 전에 flush 한 뒤 새 행을 DB 에서 세서 견줍니다(지출 · 영수증 · 끊기지 않은 잇기 줄 · 수입).
     센다 에도 잇기를 넣습니다.
     되돌림 갈래 시험도 없습니다. 11-3 이 「막는 코드에는 막히는 쪽 시험」 을 요구합니다.
     예를 들어 넣는다 중간에 예외를 심고, DB 가 그대로인지 재는 시험이 필요합니다.

G-2  scripts/재정들여오기.py:421-423
     식대가 아니라고 본 줄은 지원금액을 금액으로 덮어씁니다.
     시트의 지원금액이 금액과 달라도 그렇습니다.
     미리보기가 「식대 아님인데 시트 지원금액이 금액과 다름」 으로 세기는 합니다.
     그런데 보고에 실린 미리보기 요약에는 이 수가 없습니다.
     7-1 에 따르면 식대 줄의 절반 넘게는 식 없이 손으로 적은 줄입니다.
     그런 줄의 비고에 「숫자+명」 이 없으면 식대 아님으로 떨어집니다. 그 줄의 손 셈은 들일 때 사라지고 결산 합이 달라집니다.
     그 수가 0 인지 보고에 실어야 합니다. 0 이 아니면 사람에게 올릴 일입니다(물음 2).

G-3  scripts/재정들여오기.py:291-292
     반대 방향도 틀릴 수 있습니다. 식이 아닌 줄이라도 비고에 「숫자+명」 이 있으면 식대로 봅니다.
     예산 항목(모임 식사비)은 보지 않습니다.
     그러면 식대가 아닌 줄의 비고에 인원이 적혀 있어도 is_meal_expense 가 켜집니다.
       - 「식대만」 필터에 섭니다
       - 지원금액이 min(금액, 인원×상한) 으로 다시 셈해집니다
       - 7-4 에 따라 이후 고칠 때도 식대 규칙을 탑니다
     기준 문서 7-5 의 「식이 아니면 비고의 숫자+명」 은 식대 줄 안에서의 인원 뽑기로도 읽힙니다.
     식대인지 가르는 기준으로 쓴 것은 해석입니다(물음 3).
     「인원 비고 14」 가 전부 모임 식사비 줄인지 확인할 수 있으면 확인하세요.

G-4  docs/봐둘것.md:3330-3337 (BB-c)
     원문에 「남은 것은 게이트의 계좌 꼴 하나 — 차례 6(시트 들여오기) 전에 넣는다」 가 있습니다.
     그런데 이번 판은 scripts/check_dev_db.py · check_names.py 에 계좌 꼴을 더하지 않았습니다(grep 결과 0).
     BB-c 머리도 안 고쳤습니다.
     다음 판의 --실행 이 운영 DB 에 실제 계좌를 넣습니다(미리보기의 계좌 가름 수만큼).
     기준 문서가 「그 전에」 라고 적은 순서가 조용히 뒤집힙니다.
     실행 전에 할지, 순서를 바꾸는지 적어야 합니다(물음 1).

[봐 둘 것]
B-1  scripts/재정들여오기.py:88 · 516
     번호로 들인 영수증 전부에 memo 「시트에서 들여옴 · 그림은 시트에」 가 붙습니다.
     templates/expenses.html:157 은 파일 없는 영수증 칩에 「라벨 · memo」 를 그립니다.
     그래서 지출 목록의 칩마다 이 글이 붙어 길어집니다.
     기준 문서 7-5 와 BB-a 에는 이 메모가 없습니다. 판단이 들어간 자리라 7-5 에 한 줄 적거나 사람에게 보여 둘 만합니다.

B-2  CLAUDE.md 7-5 영수증 줄 「번호 칸이 초록이거나 번호 대신 글이 적힌 줄은 … 메모 영수증 한 장」
     코드(430)는 「번호가 없고 초록」 이거나 「번호 대신 글」 일 때만 메모 영수증으로 잇습니다.
     초록인데 번호가 있으면 원본 번호 영수증이고, memo 만 「따로 첨부」 가 됩니다(516).
     글과 코드가 조금 다릅니다.

B-3  scripts/재정들여오기.py:448-450
     다음 자동 번호를 스크립트가 직접 셉니다(coalesce max).
     domain/budget.py 에 next_receipt_number 가 이미 있습니다. 조건은 같지만 두 벌입니다(14장 「재정 숫자를 세는 곳은 budget.py 하나」).

B-4  scripts/재정들여오기.py:442-445
     한 칸에 같은 번호가 두 번 적히면(예: 번호 뒤에 같은 번호) 같은 지출에 잇기 줄이 둘 섭니다.
     잇기 표에 유니크가 없어 막히지 않습니다.
     「공유 1 · 잇기 99」 는 한 번호가 세 줄에 걸린 경우로도 산수가 맞습니다. 그래서 실제로 겹친 칸이 있는지는 시트를 봐야 압니다.

B-5  scripts/재정들여오기.py:416
     「앱이 다시 센 지원금액이 시트와 다름」 은 수만 세고, 짝 표 md 에 그 줄 번호가 안 남습니다.
     BB-a 시트-다는 「시트와 달라지는 줄은 미리보기가 말한다」 입니다. 사람이 그 한 줄을 찾을 길이 없습니다.

B-6  scripts/재정들여오기.py:473-476
     들이기 전에 산 예산 항목과 산 지출을 전부 내리고, 활동 기록은 요약 한 줄만 남깁니다.
     이름은 「seed」 지만 앱에서 손으로 넣은 행도 같이 내려갑니다(가-ㄷ 의 뜻대로이고 7-5 에도 그렇게 적혔습니다).
     7-3 · 7-4 의 취소는 행마다 그때의 값을 기록에 남기지만, 여기서는 안 남깁니다. 값은 행에 남아 되살릴 수는 있습니다.

B-7  scripts/재정들여오기.py:234
     지급여부를 bool(값) 으로 읽습니다. 체크박스가 글자 "FALSE" 로 오면 참이 됩니다.
     구글 시트 체크박스는 보통 불리언으로 오니 그대로 둬도 됩니다.

B-8  docs/봐둘것.md:3203 (BA-f 머리)
     「운영 실행은 사람이 미리보기를 보고 확인한 뒤다(CLAUDE.md 7-5)」 라고 적었습니다.
     그런데 7-5 에는 그 말이 없습니다(인계 머리에만 있습니다).
     그리고 BB-h 가 BB-g 앞에 들어가 머리 순서가 a·b·c·h·g·f·e·d 가 됐습니다.

B-9  docs/봐둘것.md:3358 BB-g 머리 「적어만 둔다」
     새로 붙인 머리 인용은 넷을 다뤘다고 말하는데, 머리 라벨은 그대로입니다.
     남은 줄(부서·식대 여부 못 고침 · 지급 고치기 없음 · 백업뿐)이 있어 「적어만 둔다」 가 틀린 것은 아닙니다.
     다만 3374 줄의 「두 말이 갈린다 · 차례 6 지시문이 정한다」 는 닫혔는데, 원문 그대로라 본문만 보면 열린 줄로 읽힙니다(11-3 3단계 「라벨과 본문」).

B-10 docs/review/최근.md 가 아직 차례 5 닫기 판 그대로입니다.
     새로 얼린 차례5닫기.md 는 HEAD 의 최근.md 와 줄바꿈(CRLF)만 다르고 내용은 같습니다.
     커밋 전에 이번 판 보고로 덮고, 지시문 대조 표와 사람칸을 채워야 합니다(check_handoff · check_orders).

짚어 달라고 한 것
(a) 한 트랜잭션인가
    예. 취소 표시 → 예산 · 수입 → 지출 · 영수증 · 잇기 → 활동 기록이 한 세션 안에서 flush 만 하고, commit 은 586 한 번입니다.
    예외가 나면 rollback 뒤 다시 던집니다. 사본은 try 밖에서 먼저 뜨므로, 거기서 실패하면 아무것도 안 바뀝니다.
    다만 되돌림을 부르는 대조가 헛돕니다(G-1).
(b) 사본 경로 검사
    config.DATA_DIR/app.db 와 make_url(config.DATABASE_URL).database 를 resolve 해서 견줍니다.
    환경변수가 없으면 운영의 data/app.db 를 가리킵니다. 영수증잇기옮기기.py 와 같은 꼴입니다.
    시험 DB(test.db)는 다른 파일이라 test48_g02 가 멈춤을 잽니다.
    엔진은 같은 config.DATABASE_URL 로 만들어지므로 세션이 여는 파일과 같습니다.
(c) 식대
    인원을 뽑은 줄은 calculate_meal_settlement 로 다시 셉니다(BB-a 시트-다 ㄴ).
    못 뽑은 줄은 인원을 비우고 시트 값을 둡니다. 이것은 7-4 의 「인원 빈 줄은 다시 안 셈」 과 맞습니다.
    어긋나는 곳은 G-2 · G-3 의 식대 가르기 쪽입니다.
(d) 시험
    합성 워크북만 엽니다. tmp_path 에 쓰고, DATA_DIR 은 conftest 의 임시 폴더라 data/ 에 쓰지 않습니다.
    박힌 수는 합성 파일에서 정해지는 것뿐이고, 운영 수(97 · 67 등)는 없습니다.
(e) 문서
    7-5 는 대부분 코드와 맞습니다. 다른 곳은 G-1(되돌림) · B-2(초록) · B-1(메모)입니다.
    BB-h 에는 「곳 · 군데」 가 붙은 수가 없고, 머리에 날짜가 있어 check_counts 에 걸리지 않습니다.
(f) app/ · templates/ · static/
    작업 트리와 스테이지에서 바뀐 것이 없습니다. 재시작을 건너뛴 판단은 11-3 에 맞습니다.

새로 걸린 실명 · 계좌 · 바깥 주소: 없음(합성 값은 「가명」 · 「지어낸」 꼴)

[사람에게 묻는 것]
1. BB-c 는 계좌 꼴 게이트를 「차례 6 전에 넣는다」 고 했습니다. --실행(운영에 실제 계좌 64줄) 전에 게이트를 먼저 넣을까요,
   아니면 실행 뒤로 미룰까요?
2. 식대가 아니라고 본 줄 중 시트 지원금액이 금액과 다른 줄이 있으면, 들일 때 지원금액을 금액으로 덮습니다(G-2).
   그 수가 0 이 아니면 어떻게 할까요?
   ㄱ. 시트의 지원금액을 그대로 둔다
   ㄴ. 식대로 보고 인원을 비운다
   ㄷ. 지금처럼 금액으로 덮는다
3. 식대인지를 「지원금액이 곱셈 식이거나 비고에 숫자+명이 있는가」 로 가릅니다. 예산 항목(모임 식사비)은 안 봅니다.
   이 기준으로 괜찮을까요, 아니면 모임 식사비 줄로 좁힐까요?
4. 번호로 들인 영수증마다 붙는 메모 「시트에서 들여옴 · 그림은 시트에」 를 지출 목록 칩에 그대로 보여도 될까요(B-1)?
━━━ 검토자 여기까지 ━━━
```

## 7-2. 다시 보기 (검토 뒤에 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 (한 번) · 대상: scripts/재정들여오기.py · tests/test_stage48.py 의 스테이지→작업 트리 차이 · CLAUDE.md 7-5 · 봐둘것 BA-f · BB-a · BB-c · BB-h · BB-g 머리

시트와 운영 DB 는 열지 않았고, 값은 적지 않았습니다.

[막아야 함]
없음

[고쳐야 함]
없음. 짚은 대로 고쳐졌습니다.
- G-1  넣는다 는 수를 돌려주지 않고 commit 도 하지 않습니다.
       돌린다 가 넣은 뒤 commit 전에 센다 를 다시 부릅니다.
       그 수를 「전 − 취소 + 계획」 과 견주고, 다르면 멈추고 rollback 합니다.
       기대 수를 계획에서 되짚지 않고 DB 에서 재므로 이제 실제로 걸립니다.
       센다 의 「산 잇기」 는 이 회차 지출에 걸린 잇기 줄 가운데 끊기지 않은 것만 셉니다.
       취소된 seed 지출의 잇기 줄도 그대로 세집니다.
       기대 수가 「전 + 계획」 이라 그 줄은 양쪽에 똑같이 들어가 어긋나지 않습니다(재리허설의 seed 12 + 99 와 맞습니다).
       test48_g04 는 두 갈래를 잽니다. 넣는 중 예외와 계획에 없는 줄 하나입니다.
       두 갈래 모두 DB 수와 활동 기록 수가 그대로인지 봅니다.
       monkeypatch 가 모듈 전역 넣는다 를 바꾸고, 돌린다 가 그 전역을 부르므로 실제 경로를 탑니다.
- G-2  보고에 0 을 싣는다고 했고, 7-5 에 「식대 아닌 줄 = 금액 · 다른 줄은 미리보기가 센다」 가 들어갔습니다.
- G-3 · G-4  둘 다 「정할 것」 으로 올린다고 했습니다. BB-h 와 BB-c 머리에 그 사실이 적혔습니다.
- B-1  번호로 들인 영수증의 memo 는 초록일 때 「따로 첨부」 이고, 아니면 비어 있습니다. 상수가 지워졌습니다.
       d01 의 필터가 coalesce 로 바뀌어 NULL 메모가 빠지지 않습니다. 그대로였다면 새 영수증이 전부 세기에서 빠졌을 자리입니다.
- B-2 · B-8 · B-9  글이 코드와 맞습니다.
       7-5 에 「운영 실행은 그대로 해 뒤」 가 있고, BA-f 머리가 인계 머리를 가리키며, BB-g 머리가 갈림이 닫혔다고 말합니다.
- B-3  budget.next_receipt_number − 1 에서 시작해 1 씩 올리므로 첫 번호가 같습니다.
- B-4  dict.fromkeys 로 순서를 지키며 겹친 번호를 한 번만 봅니다.
- B-5  짝 표에 「식대」 묶음이 생겼고, 요약 줄과 BB-h 표에도 한 줄씩 들어갔습니다.

[봐 둘 것]
B-1  CLAUDE.md 14장 표의 「재정 시트 들여오기」 줄
     아직 `넣는다`(한 트랜잭션) 이라고 적혀 있습니다.
     이제 트랜잭션을 여닫고 DB 를 다시 세어 되돌리는 것은 돌린다 입니다(7-5 본문은 맞게 고쳐졌습니다).
     14장만 읽으면 되돌림이 어디서 일어나는지 헷갈립니다.
B-2  tests/test_stage48.py
     새로 생긴 갈래 둘은 합성 시트가 밟지 않습니다.
     - 짝 표 「식대」 묶음: 합성 시트에서 「다시 센 값 다름」 이 0 이라 그 줄이 한 번도 안 섭니다
     - 번호들() 의 겹친 번호 한 번만 보기: 같은 번호를 두 번 적은 칸이 합성 시트에 없습니다
     둘 다 작은 자리라 막을 일은 아닙니다. 합성 시트에 칸 하나씩 더하면 잽니다.
B-3  CLAUDE.md 7-5 「계획에서 되짚은 수로 견주면 절대 안 어긋나 막는 뜻이 없습니다」
     지금 코드가 그렇게 하지 않으니, 이 문장은 옛 구현을 왜 버렸는지 적은 것입니다.
     그대로 두어도 틀린 말은 아닙니다.

새로 걸린 실명 · 계좌 · 바깥 주소: 없음

[사람에게 묻는 것]
없음. G-3(식대를 무엇으로 가르나)과 G-4(계좌 꼴 게이트를 실행 전에 넣나 뒤에 넣나)는 구현자가 보고 「정할 것」 으로 올린다고 했습니다.
보고를 쓸 때 사람칸에 두 줄이 다 들어갔는지만 보면 됩니다.
━━━ 검토자 여기까지 ━━━
```

## 7-3. 두 번째 검토 뒤 고침 (다시 보이지 않고 적는다 · 11-3 4단계)

- `CLAUDE.md` 14장 「재정 시트 들여오기」 줄 — 다시 보기 B-1. `넣는다` 는 넣기만, commit 과 되돌림은 `돌린다` 라고 고쳤다
- 다시 보기 B-2(합성 시트가 안 밟는 갈래 둘)는 안 고쳤다 — 운영 사본 리허설이 「식대」 짝 1 을 밟았고, 겹친 번호는 한 줄짜리 `dict.fromkeys` 다. 다음 판에 시험을 더할 수 있다
- 첫 검토의 물음 4(메모)는 B-1 로 메모를 걷어 물을 것이 없어졌다. 물음 2 는 수가 0 이라 물을 것이 없다

# 8. 바뀐 파일 (`git diff --name-only 2764138..5b69cd4`)

- CLAUDE.md
- docs/review/2026-09-14-차례5닫기.md
- docs/봐둘것.md
- docs/세어둠.txt
- docs/옛말-넘김.txt
- docs/이름-넘김.txt
- docs/인계.md
- scripts/재정들여오기.py
- tests/test_stage48.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1 | 스크립트 · 미리보기 기본 · --실행 · 사본 · 경로 불일치 멈춤 · --회차 · 건수만 | **했다** — 1장 |
| 2 | 예산 줄 · seed 9 / 12 취소 | **했다** — 3-1 |
| 3 | 지출 97 글자로 잇기 · 못 이은 것은 짝 표 | **했다** — 67 이음 · 짝 표 30 |
| 4 | 계좌는 split_account · 예금주 = 지출자 | **했다** — test48_c01 · c02 |
| 5 | 식대 인원 식 / 비고 · 못 뽑으면 빈 값 | **했다** — 식대로 가르는 기준은 정할 것 ② |
| 6 | 지급여부·지급일 그대로 · 취소 지출 없음 | **했다** |
| 7 | 영수증 원본 번호 · 번호 둘 → 두 장 · 다중 잇기 · 그림 안 들임 · 초록 = 따로 첨부 | **했다** — 3-1 |
| 8 | 부서 이름→키 · 수입 4 · 구매목록 안 들임 | **했다** |
| 9 | 두 번째는 멈춤 · 되돌리기 절차 | **했다** — 3-2 · 3-4 · 7-5 |
| 10 | 미리보기 건수 | **했다** — 3-1 |
| 11-a | CLAUDE.md 7-5 | **했다** |
| 11-b | 인계 차례 6 끝 · 16번 다음 | **다르게 했다** — 운영 실행 전이라 「스크립트 끝 · 운영 실행 대기」 로 적고 16번은 「운영 실행 뒤 다음」 으로 적었다 |
| 11-c | 봐둘것 BA-f · BB-a · BB-g 닫기 · 원문 유지 | **다르게 했다** — 「이 판이 실행한 사실」 이 아직 없어 「코드에 옮겼다 · 실행은 확인 뒤」 로 적었다. 원문은 그대로 |
| 11-d | 짝 표를 건수와 이유만 문서로 | **했다** — BB-h |
| 12 | 반품 어긋남 세기 | **다르게 했다** — 영수증 금액이 그림에만 있어 셀 수 없다고 BB-b 에 적었다 |
| 13 | BB-g 글자 규칙 정하고 시험 | **했다** — 이은 줄은 예산 항목 글자 · 못 이은 줄은 시트 글자 · test48_e02 |
| 14 | 새는지 검사 도구를 다시 세우고 커밋마다 | **했다** — 6장 |
| 15 | 이 판은 미리보기까지 · 실행은 「그대로 해」 뒤 | **했다** — 운영 사본에서만 리허설했다 |
| 가~차 | 수용 기준 | **했다** — 2장 |
| 끝줄 | 운영 자료 읽기만 · 값을 보고·덩어리에 안 싣기 | **했다** |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) · 재시작은 건너뜀(4장) |
