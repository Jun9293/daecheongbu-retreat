사람이 할 것
노트북에서 직접: 없음 — 사역자 열 분 계정(인계 18번)과 개기자 주소(인계 8번)는 그대로 남아 있습니다
관리자 권한: 없음 — 재시작은 이 창에서 됐습니다(7장)
정할 것: 둘 — ① 검토 뒤 고친 것을 다시 보이는 것을 한 번에서 멈추게 적었는데 그 선이 맞는지(6장 · CLAUDE.md 11-3 4단계) ② 재정의 지우는 길 둘(예산 항목 · 수입)이 0장과 부딪히는지를 다음 재정 판 첫머리에서 볼지(5-2 · 인계 「재정 — 지금 어디까지」)
막혀서 못 한 것: phone.js 의 안 심은 자가시험 둘 — 이 창 혼자서는 못 돌리고 사람이 개발 서버에 들어가 준 창에서 돌릴 수 있습니다. 그때까지 총무팀이 휴대폰으로 동작만 봅니다(3장 · 인계 22번)

작성 시각: 2026-09-13 20:25 KST — 검토 두 번을 받고 고친 판
커밋: (이 보고와 함께 나갑니다 — 11장)
작업: 재려다 흔적을 남기지 않게 · 비밀번호는 칸에 안 넣는다

---

# 0. 한 줄 요약

**`backup.py --살핀다` 가 `app.config`(읽히는 순간 폴더를 만들고 서명키를 쓴다) 대신 새 `app/paths.py`
에서 경로만 받습니다.** `data/` 에 백업만 남은 자리에서 불러도 전후가 같고, 성한 자리와 글자까지 같은
말을 합니다. **이 창은 도는 서버의 로그인 칸에 비밀번호를 넣지 않는다**는 것이 CLAUDE.md 11-2 의
규칙이 됐고, 그래서 못 하는 일(phone.js 자가시험 둘)과 사람이 대신 볼 것을 함께 적었습니다.
재정은 만들지 않고 지금 서 있는 것만 인계에 표로 적었습니다.

---

# 1. 고치기 전 훑기 (11-3 0단계)

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| `app.config` 의 `BASE_DIR` · `DATA_DIR` · `UPLOAD_DIR` 을 쓰는 데 | `app/main.py` · `app/templating.py` · `app/routers/settings.py` · `app/push.py` · `app/routers/expenses.py` · `app/domain/llm.py` · `seed.py` · 스크립트들. config 가 `app.paths` 에서 받아 **같은 이름으로 다시 내보내므로** 부르는 쪽은 안 고쳤습니다. `importlib.reload(config)` 를 쓰는 곳은 없습니다(검토 [A] 가 따로 셌습니다) |
| config 의 폴더 만들기 · 서명키 | **config 에 그대로 둡니다** — 앱이 뜨는 자리에는 맞습니다. 달라진 것은 새벽 백업(`run`)이 그것을 부르는 때 하나입니다: 전에는 import 순간, 지금은 끝 무렵 `알린다` → `app.notifications` 를 import 할 때(검토 [A] · 무해) |
| `되살아나나.ps1` · 그 출력을 적어 둔 문서 | ps1 은 안 고쳤습니다(부르는 명령이 같습니다). CLAUDE.md 11-2 의 「다만 `app.config` 를 읽으므로」 문장은 고치고 옛말에 적었습니다 |
| 「사람이 대신 본다」 · 「닫음」 을 적어 둔 곳 | CLAUDE.md 11-2 · 봐둘것 AY-a(제목 · 소제목 · 본문) · 인계 머리 · 인계 맨 위 표 · 인계 22번 — 다섯 자리를 같은 뜻으로 맞췄습니다(검토 [D][G] 두 번) |
| `test_docs_shell.py` 의 원문 경계 글자 | `scripts/check_placeholder.py` 가 같은 두 글자를 줄머리로 봅니다. 그 규칙으로 맞췄습니다(다시 보기 [E]) |

---

# 2. 앱 밖 자리가 읽기만 하게 (지시 1)

## 2-1. 경로를 어디서 (1-a · 1-b)

**「app.config 말고 다른 길」 은 원래 없었습니다** — 경로 셈이 config 한 파일 안에 폴더 만들기·서명키와
붙어 있었습니다. 이 갈래에서만 건너뛰게 하면(예: `--살핀다` 일 때 경로를 따로 셈) 경로 셈이 두 곳이 되어
갈립니다. 그래서 **셈만 `app/paths.py` 로 떼고 config 도 그것을 받아 쓰게** 했습니다 — 길을 만든 쪽입니다.
`backup.py` 는 맨 위에서 `app.paths` 만 읽습니다. `알린다` 의 함수 안 import(앱 안 갈래)는 그대로입니다.

## 2-2. 비운 자리에서 잰 것 (1-c · 1-d · 수용 가 · 나)

`test44_h01` — `tmp_path` 에 **사고 직후 모양의 data/**(백업 폴더만 · app.db · 서명키 · uploads 없음)를 두고,
`backup.py --살핀다` 를 **자식 프로세스로** 부릅니다. 환경에서 `DCB_SECRET_KEY` 와 `DCB_DATABASE_URL` 을 **뺍니다** —
넣은 채면 흔적이 안 나도 「안 읽어서」 인지 「환경변수가 있어서」 인지 못 가립니다.

1. 부르기 전과 뒤에 그 뿌리 아래 **모든 경로 · 폴더 여부 · 크기 · 수정 시각**을 통째로 견줍니다 → 같음
2. 서명키 · uploads 가 없음을 따로 봅니다
3. 빈 판의 제목을 말합니다
4. 같은 시험에서 성한 data/(DB · 서명키 · uploads 가 있음)에 같은 백업을 두고 부른 출력과 **글자까지 같습니다**

`test44_h02` — data/ 가 **아예 없는** 자리: 「빈 판 없음」 을 말하고 data/ 도 그 부모도 안 생깁니다.

검토자가 따로 저장소 밖에서 견줬습니다 — 지금 코드는 흔적 없음, **HEAD 의 backup.py 는 secret_key.txt 와 uploads 를
만듦**. 시험이 옛 코드에서 실제로 빨개지는 모양입니다.

**못 보는 것**(검토 [B]) — ① 뿌리 밖(`BASE_DIR/data` · `%TEMP%`)에 쓰는 것 ② 만들었다 곧 지운 파일 ③ WAL 모드 백업을
ro 로 열 때 생길 수 있는 곁 파일(지금 백업 판은 롤백 모드라 이 갈래가 안 재집니다) ④ ps1 이 실제로 부르는 모양
(DCB_DATA_DIR 없음). h01 의 「성한」 app.db 는 표가 없는 빈 파일입니다 — 살핀다가 DB 를 안 열어 결과는 맞습니다.

운영에서 `되살아나나.ps1` 을 돌린 전후 `data/` 아래 항목 수: **76 → 76** · 출력 「빈 판 없음」(증거 판은 `지키는판`).

## 2-3. 판정 한 곳 (1-e · 수용 다)

`test44_h03` — `backup.py` 를 **구문 나무로** 읽어 ① 맨 위 import 중 `app.` 으로 시작하는 것이 `app.paths` 뿐인지
② `app.config` 와 `app.paths` 의 `DATA_DIR` · `UPLOAD_DIR` 이 같은 값인지 ③ `알린다` 와 `살핀다` 둘 다 `의심말들` 을
부르는지 봅니다. 앞 판의 `e04`(알림 행 · 앱 밖 줄이 `의심말들` 결과와 글자 단위로 같음) · `e05`(문장이 한 곳)가 그대로 돕니다.
h03 은 맨 위의 try/if 안 import 를 못 보고, 그 자리는 h01 이 행동으로 막습니다(검토 [B]).

## 2-4. 고장을 심어 본 것 (`-p` 없이 · 작업 트리에 심고 되돌린 뒤 `cmp` 로 확인)

```
① backup.py 맨 위 import 를 app.config 로 되돌림   -> h01 · h02 · h03 빨강
② 살핀다가 옆 파일을 씀(기록=True)                 -> h01 빨강
③ 원문 경계를 「파일 끝까지 뺌」 으로                -> 05c 빨강
④ 원문 경계를 줄머리가 아니라 「들어 있으면」 으로    -> 05c 빨강
```

---

# 3. 비밀번호를 칸에 안 넣는 규칙 (지시 2)

- **2-a** CLAUDE.md 11-2 「운영 자료를 바꾸는 명령은 누가 돌리나」 표 아래 — 까닭 둘(값이 이 창의 기록에 남음 · 「버리는 계정이니까」 는 그때그때 가르는 예외). **경계는 도구가 아니라 조건으로 적었습니다**(다시 보기 [C]): 대상은 도는 서버(운영·개발 · 이 창이 띄운 개발 서버 포함)의 로그인 칸 · `/login` 요청 · 세션 쿠키. 앱을 그 자리에서 만들고 값이 저장소에 적힌 지어낸 것이면 대상이 아니고, pytest 의 `login_as` 가 그 예입니다
- **2-b** 못 하는 일 — phone.js 에 아직 안 심은 자가시험 둘(사이드바가 처음에 접혀 있는가 · 그 안의 링크가 눌리는가)을 **이 창 혼자서는** 못 돌립니다. 고장을 적어 넣는 데는 로그인이 필요 없고 돌리는 데만 필요해서, **사람이 들어가 준 창에서 이 창이 붙여넣어 돌리는 길은 남습니다**(검토 [D])
- **2-c** 봐둘것 AY-a — 원문을 지우지 않고 「정리」 문단을 붙였습니다. 라벨은 처음에 「닫음」 으로 달았다가 **본문과 어긋난다**는 다시 보기 [D] 를 받고 「이 창 혼자서는 못 돌림 · 사람이 들어가 준 창에서는 열려 있음」 으로 바꿨습니다 — 지시문의 「닫는다」 와 다르게 한 자리입니다(12장)
- **2-d** 인계 22번 「사람이 챙길 것(총무팀)」 — 휴대폰으로 사이드바가 처음에 접혀 있는지, 메뉴를 열어 링크를 누르면 화면이 옮겨지는지. **동작 확인이고 자가시험을 대신하지 않는다**고 함께 적었습니다

---

# 4. 검토자가 못 본 것 (지시 3)

## 4-1. 원문 경계 · 대가 (3-a · 3-b)

앞 판이 검토 뒤에 고친 `%VAR%` 예외를 다시 봤고, 검토자도 두 번 봤습니다. 고친 것 둘:
- **끝 표시가 나와야** 시작~끝을 원문으로 칩니다 — 끝이 없으면 그 뒤 파일 끝까지 규칙이 꺼졌습니다(검토 [E])
- **줄머리로만** 경계를 봅니다 — 산문이 두 글자를 인용한 줄 사이가 통째로 빠졌습니다(다시 보기 [E]). `check_placeholder` 와 같은 규칙입니다

**대가**(주석에 적음) — 검토자가 원문 안에 사람이 붙여 넣을 cmd 명령을 적어도 이 규칙은 안 잡습니다. 원문은 고칠 수
없는 글이고, 그 명령을 문서로 옮기는 순간 원문 밖이 되어 다시 걸립니다.

## 4-2. 되풀이인가 (3-c)

**처음이 아닙니다.** 얼린 판을 찾아보니 「검토 뒤에 더한 줄」(2026-09-11 로그인판) · 「검토 뒤에 더한 넷」(비품도막4) ·
「검토 뒤에 한 번 더」(비밀번호바꾸기 · 안내글자 · 회차넘나듦) · 앞 판의 test_docs_shell 이 같은 모양입니다.
CLAUDE.md 11-3 4단계에 적었습니다 — **검토 뒤에 고친 것은 그 차이만 한 번 더 보인다 · 가르는 것은 파일(`docs/review/`
밖은 전부) · 다시 보이는 것은 한 번이고, 그 뒤에 또 고친 것은 보고에 「두 번째 검토 뒤 고침」 으로 적는다.**
처음에는 「판단이 안 들어간 고침은 넘긴다」 로 적었다가 검토 [H] 를 받고 파일로 가르게 바꿨고, 다시 보기 [H] 가 짚은
「끝이 없다」 를 받고 한 번으로 멈추게 했습니다 — **그 선이 맞는지는 사람칸 「정할 것」 ①**.

이 판이 그 규칙을 처음 따랐습니다: 첫 검토 → 고침 → **다시 보기**(13장 둘째 원문) → 고침(6장).

---

# 5. 남은 것 (지시 4)

## 5-1. 인계 (4-a)

머리를 넷째 판으로 바꾸고 「남은 것: 사역자 열 분 계정(18) · 개기자 주소(8) · 재정」 과 28번(끝난 것)을 적었습니다.
셋째 판 보고는 `docs/review/2026-09-13-앱밖자리.md` 로 얼렸습니다(HEAD 의 최근.md 와 한 글자도 안 다름 — 검토자가 diff 로 확인).

## 5-2. 재정 — 지금 어디까지 (4-b · 읽기만)

인계에 표로 적었습니다. 요지:
- **숫자** — `app/domain/budget.py` 의 `build_budget_summary` 가 홈 · 회차 상세 · 예산 · 지출 · 엑셀에서 같이 쓰입니다(취소 뺌 · 식대는 지원금액 · 구분 소계 · 수입과 잔액 · `unspent_*`). 환급 · 영수증 없음 · 다음 영수증 번호도 여기
- **예산 화면** — 목록 · 항목 만들기/고치기 · 수입 넣기. **수입을 고치는 길이 없고, 지우는 길이 둘**(예산 항목은 지출이 안 붙었을 때만 · 수입) — 0장과 부딪히는지가 사람칸 「정할 것」 ②
- **지출 화면** — 등록 · 영수증 붙이기(1:N) · 지급 · 취소/되살림 · 필터. **등록한 지출을 고치는 길이 없습니다** · 영수증 1장에 지출 여럿(12번) · 계좌가 한 칸(11번)
- **엑셀** — `/expenses.xlsx` 하나에 시트 셋. 예산만 따로 내려받는 길은 없음
- **운영(읽기 전용으로 셈)** — 2026 회차에 예산 항목 9 · 지출 12 · 영수증 12(전부 파일·메모 없음) · 수입 0 · 취소 0. 2027 회차에는 재정 행이 없음(검토자가 다시 셈)

## 5-3. 15·16 (4-c)

**16 은 붙습니다**(구매 탭 → 지출 후보). **15 는 안 붙습니다** — 옛 회차 탭을 비품 라이브러리에 들여오는 일이고 기다리는
것은 봐둘것 AO-b 입니다. 인계에 그렇게 적었습니다.

---

# 6. 두 번째 검토 뒤 고침 (11-3 4단계 · 다시 보이지 않음)

다시 보기의 답을 받고 고친 것 — 새 규칙대로 한 바퀴 더 돌리지 않고 여기 적습니다. 다음 판의 검토자가 이 커밋을 봅니다.

| 파일 | 무엇 · 까닭 |
|---|---|
| `CLAUDE.md` 11-2 | 비밀번호 경계를 도구 이름에서 조건으로(다시 보기 [C]) |
| `CLAUDE.md` 11-3 4단계 | 「docs 의 목록들」 → 「docs/review/ 밖 전부」 · 다시 보이는 것은 한 번(다시 보기 [H]) |
| `docs/봐둘것.md` AY-a | 제목·소제목의 「닫음」 을 열린 쪽 말로(다시 보기 [D]) |
| `docs/인계.md` 머리 | 옛 뜻(「사람이 본다」)이 남은 갱신 줄(다시 보기 [D]) |
| `tests/test_docs_shell.py` | 경계를 줄머리로 · 05c 에 산문 인용 쌍(다시 보기 [E]) · 고장 ④ 로 빨강 확인 |

곁에서: 앞 판 고장 심기의 흔적인 `tests/__pycache__/test_zz심기_*.pyc`(추적 안 됨)를 지웠습니다.

---

# 7. 돌고 있는 서버 (11-3 2단계)

`app/config.py` · `app/paths.py` 가 바뀌어 배포-안내 12-1 대로 재시작했습니다. 그 뒤로 `app/` · `templates/` · `static/` 은 안 고쳤습니다.

```
멈추기 전 PID: 23752 · 19:56:52
포트가 비었습니다
다시 켠 뒤 PID: 28860 · 19:57:01
성공 — PID 가 바뀌었습니다
GET http://127.0.0.1:8000/login -> 200
```

화면에 새 요소는 없습니다 — 앱이 새 경로 모듈로 떠서 답하는지가 확인할 것이었고, `/login` 200 과 data/ 아래가 그대로(서명키가 새로 안 섬 · 76 → 76)인 것으로 봤습니다.

---

# 8. 수용 기준 — 각 줄이 무엇을 보나

| 기준 | 어디서 잰다 | 통과 |
|---|---|---|
| 가) data/ 가 빈 자리에서 불러도 폴더·파일이 안 생김 · 전후를 실제로 견줌 | 2-2 — `test44_h01`(뿌리 아래 경로·크기·시각 전후 · 서명키 환경변수 뺌) · `h02`(data/ 없음) · 고장 ① ② | O |
| 나) 그 자리에서도 의심 판을 말함 · 성한 자리와 같은 말 · 같은 판 | 2-2 — `test44_h01` 의 3·4 | O |
| 다) 앱 안·밖 판정 한 곳을 코드에서 끌어내 견줌 | 2-3 — `test44_h03`(구문 나무) · `e04` · `e05` · 고장 ① | O |
| 라) pytest 옵션 없이 전부 · 글검사 7/7 · 얼린 판 git add 뒤 | 9장 · 10장. 얼린 판 `2026-09-13-앱밖자리.md` 는 스테이징한 뒤 돌렸습니다 | O |
| 마) 검토자 원문 | 13장(두 번 다) | O — **원문을 실은 뒤에 검사와 시험을 돌렸습니다** |

---

# 9. 시험

```
1503 passed, 3 warnings in 1014.12s (0:16:54)
```

**이 보고를 다 쓴 뒤 옵션 없이 처음부터 끝까지 돌린 판입니다.** 검토자도 두 번 옵션 없이 돌렸습니다(1503 passed · 둘 다).

---

# 10. 글검사 결과

```
━━━ 모아서 ━━━
  통과  check_names · check_stale · check_named · check_counts
  통과  check_handoff · check_placeholder · check_orders

글 검사가 전부 통과했습니다.
```

**이 보고를 다 쓴 뒤에 다시 돌린 결과입니다.** `app/paths.py` 는 처음에 추적 전이라 `check_named` 가 「문서에만 있는 이름」 으로 잡았고, 스테이징한 뒤 통과했습니다.

---

# 11. 커밋

나눕니다 — 경로 떼기(app) / 흔적 없음 시험 / 원문 경계 시험 / 문서(규칙 · 인계 · 봐둘것 · 옛말) / 얼린 판과 이 보고.
해시는 푸시한 뒤 덩어리 첫 줄에 적습니다.

---

# 12. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| ① | 앱 밖 자리가 폴더·서명키를 안 만든다 | **했다** — 2장 |
| ② | 이 창은 비밀번호를 칸에 안 넣는다 | **했다** — 3장 |
| 1-a | --살핀다 가 app.config 를 읽어 생기는 일을 끊는다 | **했다** — 2-1 |
| 1-b | 경로를 어디서 · 다른 길인지 건너뛰기인지와 까닭 | **다르게 했다** — 다른 길이 없어서 셈을 `app/paths.py` 로 떼어 길을 만들었다. 건너뛰기는 경로 셈이 두 곳이 되어 안 했다(2-1) |
| 1-c | data/ 를 비운 자리에서 전후 견줌 · 운영 아닌 곳 | **했다** — 2-2 · tmp_path |
| 1-d | 그 상태에서도 말은 제대로 · 같은 판 | **했다** — 2-2 |
| 1-e | 앱 안은 지금대로 · 판정 한 곳 확인 | **했다** — 2-3 |
| 2-a | 11-2 에 규칙과 까닭 | **했다** — 3장 |
| 2-b | 그래서 못 하는 일 | **했다** — 3장 |
| 2-c | 봐둘것 AY-a 를 닫되 원문 유지 | **다르게 했다** — 원문은 두고 정리 문단을 붙였지만, 라벨은 「닫음」 이 아니라 「혼자서는 못 돌림 · 들어가 준 창에서는 열림」 으로 둠. 자가시험 자체는 남은 길이 있어서다(3장 · 검토 [D]) |
| 2-d | 사람이 대신 볼 한 줄을 인계에 | **했다** — 인계 22번 |
| 3-a · 3-b | 원문 경계 다시 보기 · 대가 | **했다** — 4-1. 새는 곳 둘을 더 막음 |
| 3-c | 되풀이인지 · 되풀이면 11-3 에 한 줄 | **했다** — 4-2 |
| 4-a | 인계를 지금 사실로 | **했다** — 5-1 |
| 4-b | 재정이 어디까지 서 있나 · 만들지 않음 | **했다** — 5-2 |
| 4-c | 15·16 이 재정에 붙는지 | **했다** — 16 은 붙고 15 는 안 붙음(5-3) |
| 가)~마) | 수용 기준 | **했다** — 8장 |
| (끝줄) | 덩어리와 보고에 실명·전화·계좌·토큰·비밀번호·바깥 주소 안 적음 · 검토자 원문 그대로 | **했다** |
| (끝줄) | 운영 자료는 읽기만 | **했다** — 운영 DB 는 mode=ro 로 셈 · ps1 전후 항목 수 같음 |
| (끝줄) | 앱을 고쳤으면 PID | **했다** — 7장 |
| (끝줄) | 마무리는 11-3 | **했다** — pytest 앞에 아무것도 안 끼움 · 검토 뒤 고친 것을 한 번 더 보임 |

---

# 13. 검토자 원문

**두 번 받았고 둘 다 통째로 싣습니다.** 한 글자도 안 가렸습니다.

## 13-1. 첫 검토

| 라벨 | 무게 | 어떻게 |
|---|---|---|
| [A] | 막을 것 없음 | 보고 1장에 적음 |
| [B] | 봐 둘 것 | 보고 2-2 · 2-3 에 못 보는 것으로 적음 |
| [C] | 고쳐야 함 | 고침 — 11-2 경계(다시 보기 뒤 조건으로 한 번 더) |
| [D] | 고쳐야 함 | 고침 — 「혼자서는 못 돌림」 · 동작 확인은 대신하지 않음 |
| [E] | 봐 둘 것 | 고침 — 끝 표시가 있어야 원문 |
| [F] | 막을 것 없음 | 「수입을 고치는 길이 없다」 더함 |
| [G] | 고쳐야 함 | 고침 — 인계 맨 위 표 |
| [H] | 봐 둘 것 | 고침 — 파일로 가름 |
| [I] | — | 확인만 |

```
━━━ 검토자 여기부터 ━━━ 범위 e5464cf..작업 트리(커밋 안 함, app/paths.py · 얼린 판 스테이징) · 커밋 0개 · 파일 10개

직접 돌려 본 것
  pytest (옵션 없이, python -m pytest): 1503 passed, 3 warnings in 928.69s. 운영 data/ 에는 안 썼습니다.
    (처음 한 번은 내가 바깥에 timeout 을 걸어 78% 에서 끊겼습니다. 거기까지 실패는 없었고, 두 번째는 끝까지 돌렸습니다)
  scripts/글검사.py: 7/7 통과 (얼린 판이 스테이징된 상태)
  얼린 판: git show HEAD:docs/review/최근.md 와 docs/review/2026-09-13-앱밖자리.md 를 diff 로 견줌. 한 글자도 안 다릅니다
  흔적 견주기 (저장소 밖 scratchpad · DCB_SECRET_KEY/DCB_DATABASE_URL 뺌 · 데이터 폴더 없음):
    지금 backup.py --살핀다  -> 「빈 판 없음」, 폴더 안 생김
    HEAD 의 backup.py        -> 「빈 판 없음」, data/ 아래에 secret_key.txt · uploads 가 생김
    그러니 h02 가 옛 코드에서 실제로 빨개지는 모양이 맞습니다
  운영 DB 는 mode=ro 로 읽기만 해서 재정 표의 수를 셌습니다(아래 [F])

[A] app/paths.py 로 떼어낸 뒤 앱 쪽 동작 — 막을 것 없음
  app/paths.py:13  BASE_DIR 는 여전히 Path(__file__).parent.parent 이고, 파일이 같은 app/ 폴더라 값이 같습니다.
    DATA_DIR · UPLOAD_DIR 도 환경변수 셈이 같습니다
  app/config.py:8  BASE_DIR 를 다시 내보냅니다. app/main.py:16 · app/templating.py:13 · app/routers/settings.py:276 ·
    app/push.py:20 · app/routers/expenses.py:20 · app/domain/llm.py:50 · seed.py:21 과 스크립트들의
    config.DATA_DIR 가 모두 같은 객체를 받습니다. importlib.reload(config) 를 쓰는 곳도 없습니다
  mkdir 과 서명키는 config 에 그대로 남았습니다(config.py:21-24, 108)
  달라진 곳은 하나이고 무해합니다. 봐 둘 것입니다.
    예전에는 새벽 백업(run)이 import 되는 순간 폴더와 서명키를 만들었습니다.
    지금은 run 이 끝 무렵 알린다 → app.notifications 를 import 할 때 만듭니다(backup.py:498 → config).
    app.db 가 없으면 backup.py:535 에서 먼저 돌아가므로, 그날은 아무것도 안 만듭니다.
    오히려 새 규칙 쪽에 가깝지만, 보고에 한 줄 적어 두면 좋습니다

[B] h01 이 「흔적 없음」을 정말 재나 — 잰다. 못 보는 것은 적어 둘 것 · 봐 둘 것
  tests/test_stage44.py:490-526  _판 이 rows.json 을 안 만듭니다. 그래서 count_rows 의 읽기 전용 경로
    (backup.py:298 mode=ro)까지 실제로 지납니다. 좋습니다
  못 보는 것
    ① 뿌리(tmp_path/빈) 밖에 쓰는 것. 누군가 DCB_DATA_DIR 를 무시하고 BASE_DIR/data 나 %TEMP% 에 쓰면 안 잡힙니다
    ② 만들었다 곧 지운 파일(-journal 같은 것)
    ③ WAL 모드 백업을 ro 로 열 때 생길 수 있는 -shm/-wal.
       지금 백업 판은 헤더가 롤백 모드(1,1)라 해당이 없습니다. _판 도 롤백 모드라 이 갈래는 한 번도 안 재집니다
    ④ 되살아나나.ps1 이 실제로 부르는 모양(DCB_DATA_DIR 없음 · 저장소 data/). 코드 경로는 같습니다
  h01 의 「성한 data/」에 있는 app.db 는 표가 없는 빈 파일입니다. 살핀다가 DB 를 안 여니 결과는 맞지만, 이름만큼 성하지는 않습니다
  h03 은 맨 위 import 만 구문 나무로 봅니다. 맨 위의 try/if 안 import 나 간접 import 는 못 보고, 그 자리는 h01 이 행동으로 막습니다.
    둘이 짝이라 괜찮습니다

[C] 새 11-2 비밀번호 규칙이 좁거나 넓은가 · 고쳐야 함
  CLAUDE.md 11-2 (추가된 문단 첫 줄 「이 창은 로그인 칸에 비밀번호를 넣지 않습니다 — 운영이든 개발이든」)
  pytest 와 부딪힙니다
    tests/conftest.py:184-221 login_as 는 시험 비밀번호로 POST /login 을 보냅니다.
    이 창이 매번 pytest 를 돌리므로, 글자 그대로 읽으면 「로그인 칸에 비밀번호」와 「세션을 만들어 넣는 길」 둘 다에 걸립니다.
    규칙이 「판단이 필요한 예외를 안 둔다」에서 나왔는데, 시험이 거기에 드는지를 매번 사람이 가르게 됩니다
  고칠 방향(정하는 것은 사람)
    도는 서버(운영·개발)의 로그인 칸 · 세션 쿠키 · curl POST /login 이 대상이고,
    TestClient 안에서 저장소에 적힌 지어낸 값으로 도는 시험은 대상이 아니라고 경계를 적습니다
  넓이 쪽: 「사람이 들어가 준 창에 붙여넣는 것은 막지 않는다」는 값이 안 오가서 규칙의 까닭과 맞습니다. 막을 것 없음

[D] phone.js 둘을 「사람이 본다」로 닫은 방식 · 고쳐야 함 (사람에게 물음과 함께)
  docs/봐둘것.md:2868 제목 「규칙으로 닫음」 · :2885 「닫음: 이 창은 못 하고 사람이 본다」 · :2905-2908
  CLAUDE.md 11-2 추가 문단 「사람이 대신 봅니다」 · docs/인계.md:67-69
  안 심은 둘은 자가시험(phone.js 가 그 고장을 잡는지)입니다. 사람이 휴대폰으로 보는 것은 지금 동작이 맞는지이지,
    점검이 새는지가 아닙니다. 같은 이름으로 닫으면 「검사가 볼 것을 보나」(검토-원칙 2-③) 자리가 닫힌 것으로 읽힙니다
  「못 하는 일」도 조금 넓게 적혔습니다
    고장을 phone.js 고장들에 적어 넣는 데는 로그인이 필요 없습니다. 돌리는 데만 필요합니다.
    새 규칙 스스로 「사람이 들어가 준 창에서 붙여넣기」를 열어 두었으니 길은 남아 있습니다(원문 :2903-2904 에도 있음)
  권함: 「이 창 혼자서는 못 돌림 — 사람이 개발 서버에 들어가 준 창에서 돌리면 됨」으로 남기고,
    사람이 휴대폰으로 보는 한 줄은 그것과 별개의 대체 확인이라고 적습니다

[E] test_docs_shell.py 의 원문 경계(3-a/b) — 맞음. 대가 한 줄 더 · 봐 둘 것
  tests/test_docs_shell.py:167-184  줄번호 셈이 블록줄 과 같이 1부터라 어긋나지 않습니다.
    예외는 %VAR% 하나에만 걸리고, 원문 안의 줄 잇기는 여전히 걸립니다(test 342-346 이 양쪽을 잽니다)
  지금 docs 아래 여기부터/여기까지 는 모두 짝이 맞습니다(최근.md · 얼린 판 · 검토-원칙.md)
  적힌 대가에 없는 것
    끝 표시가 빠진 원문(잘렸거나 검토자가 끝 줄을 안 낸 경우)은 그 파일 끝까지 %VAR% 가 통째로 안 걸립니다.
    원문시작 글자를 산문으로 인용한 문서도 끝 표시가 나올 때까지 같습니다.
    짝이 안 맞으면 멈추는 시험이 없습니다. 주석 :115-118 에 한 줄 더하거나 짝 검사를 두면 됩니다

[F] 재정 표의 사실 여부 — 맞음. 막을 것 없음
  라우트: budget.py 에는 목록 · 항목 만들기/고치기/지우기(:166, 지출이 붙으면 거절 :182) · 수입 넣기/지우기(:252, db.delete)가 있고,
    수입을 고치는 길은 없습니다. expenses.py 에는 create/receipts/paid/cancel 만 있고 지출을 고치는 길이 없습니다.
    /refunds 가 있고(:461), 엑셀은 routers/export.py:26 /expenses.xlsx 하나에 시트 셋(budget_xlsx.py:63,103,139)입니다.
    계좌는 can_see_account 로 가립니다. 모두 표와 같습니다
  숫자(운영 DB · 읽기 전용): 회차 1 에 예산 항목 9 · 지출 12 · 취소 0 · 수입 0 · 영수증 12(파일·메모 없는 것 12). 표와 같습니다.
    2027 회차에는 재정 행이 없습니다
  사소한 것: 표의 「수입 넣기」 옆에 「수입을 고치는 길도 없다」를 적으면 「없는 것」 칸이 온전해집니다

[G] 인계 안에서 한 곳이 자기와 어긋남 · 고쳐야 함 (작음)
  docs/인계.md:32  맨 위 표의 화면 시험자 줄이 아직 「phone.js 의 안 심은 고장 둘(AY-a 아래)」을 남은 것으로 적습니다.
    같은 판에서 :67-69 와 봐둘것은 「사람이 본다/닫음」으로 바꿨습니다([D] 를 어느 쪽으로 정하든 둘을 맞춰야 합니다)
  :26 제목 「(2026-09-13 셋째 판)」도 넷째 판 기준으로 볼지 사람이 봅니다

[H] 11-3 에 더한 「검토 뒤에 고친 것」 규칙 · 봐 둘 것
  CLAUDE.md 11-3 4단계 추가 문단 「문서의 오탈자처럼 판단이 안 들어간 것만 … 넘깁니다」
  「판단이 안 들어갔나」를 고친 사람이 스스로 가릅니다. 검토-원칙 7(…만 꼴)과 이번 판 ②(판단이 필요한 예외를 안 둔다)가
    경계한 모양입니다. 「코드·시험·규칙 파일이면 무조건 다시 보인다」처럼 파일 종류로 가르면 가를 사람이 필요 없어집니다
  이 판에 걸리는 것: 이 검토 뒤에 [C][D][G] 를 고치면 그 차이를 한 번 더 보여야 합니다(새 규칙대로)

[I] 옛말·이름·중복
  docs/옛말.md 에 새 줄이 있고 check_stale 이 통과합니다
  CLAUDE.md 14장 새 줄의 app/paths.py · test44_h01/h02/h03 은 실재합니다
  같은 사실이 두 곳에 생긴 것: 경로 셈은 paths.py 하나로 모였고 h03 이 config 와 값이 같은지 잽니다. 판정은 의심말들 하나이고,
    h03 이 알린다·살핀다 둘 다 그것을 부르는지 구문으로 잽니다. 없음
  실명·전화·계좌·토큰·바깥 주소가 새로 들어간 것: 이번 diff 에서 못 찾았습니다(check_names 통과).
    config.py 의 BASE_URL 줄은 이번에 안 바뀌었습니다

못 본 것
  docs/review/최근.md 가 아직 앞 판이라 이 판의 사람칸 · 지시문 대조 표 · 고치기 전 훑기(검토-원칙 8-1·8-2·9)는 못 봤습니다.
    보고를 쓴 뒤 check_handoff/check_orders 가 모양을, 사람이 번호를 봐야 합니다
  3-c 가 「되풀이」라는 판단의 근거(얼린 판 grep)는 다시 세지 않았습니다
  재시작 · 돌고 있는 서버 확인은 아직 안 한 판이라 볼 것이 없습니다(app/ 가 바뀌었으니 커밋 전에 해야 합니다)
  덩어리는 검토자가 볼 수 없습니다

막아야 함: 없음

사람에게 묻는 것
  1. [C] 비밀번호 규칙의 경계에 pytest(TestClient · 지어낸 시험 비밀번호)가 드는지. 안 든다면 그 말을 11-2 에 적을지
  2. [D] phone.js 자가시험 둘을 「닫음」으로 둘지, 「이 창 혼자 못 함 · 사람이 들어가 준 창에서 돌릴 수 있음」으로 열어 둘지.
     그리고 사람의 휴대폰 확인이 자가시험을 대신한다고 볼지
  3. [H] 「판단이 안 들어간 고침만 넘긴다」의 가름을 고친 사람에게 맡길지, 파일 종류로 못박을지
  4. [F] 재정의 「지우는 길 둘」(예산 항목 · 수입)이 0장과 부딪히는지는 인계에 다음 판 몫으로 적혀 있습니다. 순서를 사람이 정할지
━━━ 검토자 여기까지 ━━━
```

**물은 넷에 대해** — 1·2·3 은 이 창이 기본값으로 정해 고쳤고(되돌리기 쉬운 문장), 다시 보기에서 한 번 더 좁혔습니다. 4 는 사람칸 「정할 것」 ② 입니다.

## 13-2. 다시 보기 (검토 뒤에 고친 차이만)

| 라벨 | 무게 | 어떻게 |
|---|---|---|
| [C] | 고쳐야 함(작음) | 고침 — 조건으로(6장) |
| [D] | 고쳐야 함 | 고침 — 봐둘것 라벨 · 인계 머리(6장) |
| [E] | 봐 둘 것 | 고침 — 줄머리 · 산문 인용 쌍(6장) |
| [F] · [G] | 막을 것 없음 | — |
| [H] | 봐 둘 것 | 고침 — 한 번에서 멈춤 · 「docs/review/ 밖 전부」(6장) · 선은 「정할 것」 ① |
| 곁 | — | 심기 흔적 .pyc 지움 |

```
━━━ 검토자 여기부터 ━━━ 다시 보기 · 범위 e5464cf..작업 트리 · 커밋 0개 · 파일 10개 · 앞선 검토 뒤에 고친 차이만 봄

직접 돌려 본 것
  pytest (옵션 없이 · python -m pytest): 1503 passed, 3 warnings in 1276.52s. 앞선 검토 때와 같은 수입니다
    (05c 는 기존 함수에 단언을 더한 것이라 수가 안 늘어난 것이 맞습니다)
  scripts/글검사.py: 7/7 통과
  [E] 의 짝 셈: 저장소 밖(scratchpad)에서 걸린줄 을 모양 셋으로 불러 봄. 결과는 [E] 에
  운영 data/ 에는 안 썼습니다. 파일도 안 고쳤습니다

[A][B] 코드는 안 고치고 보고에 싣는다고 함
  보고(최근.md)가 아직 앞 판이라 확인할 수 없습니다. 보고를 쓴 뒤 사람이 봐야 합니다

[C] 비밀번호 경계 문장 · 고쳐야 함 (작음)
  CLAUDE.md 11-2 추가 문단 「대상은 도는 서버(운영·개발)의 로그인입니다 … pytest 는 대상이 아닙니다」
  pytest 와 부딪히던 자리는 풀렸습니다. 막을 것 없음
  경계를 도구 이름(pytest)으로 적어서 새는 칸이 하나 남습니다
    pytest 도 아니고 도는 서버도 아닌 것 — 예를 들어 scratchpad 의 스크립트가 TestClient 로 앱을 만들어
    개발 DB 사본에 들어가는 경우 — 는 어느 쪽인지 문장이 말하지 않습니다.
    그러면 그때 사람이 가르게 되고, 이 규칙이 피하려던 모양입니다
  문장 안에 이미 조건이 있습니다. 「시험 안에서 만든 앱 · 저장소에 적힌 지어낸 값」.
    이 둘을 경계로 올리고 pytest 는 그 예로 내리면 닫힙니다
    (예: 「도구가 무엇이든, 앱을 그 자리에서 만들고 값이 저장소에 적힌 지어낸 것이면 대상이 아니다」)
  반대쪽 한 줄: 이 창이 스스로 띄운 uvicorn(개발 서버) 에 지어낸 값으로 /login 을 보내는 것은 「도는 서버」라 막힙니다.
    의도대로 읽힙니다

[D] 「이 창 혼자서는 못 함」으로 좁힌 것 · 고쳐야 함
  CLAUDE.md 11-2 · 인계 22번(docs/인계.md:67-70) · 인계 맨 위 표(:33) 는 뜻이 맞습니다. 막을 것 없음
  라벨과 본문이 어긋납니다(11-3 3단계 「라벨과 본문이 다른 것을 가리키게 두지 않습니다」)
    docs/봐둘것.md:2885  소제목에 취소선과 「닫음」이 붙었습니다
    docs/봐둘것.md:2905-2908  같은 자리 본문은 「자가시험 자체는 열린 채다」라고 합니다
    docs/봐둘것.md:2868  AY-a 제목도 「… 으로 닫음」입니다
    닫힌 줄 알고 넘어가는 모양이라, 앞서 AC-c 에서 한 번 겪은 것과 같습니다.
    라벨을 「이 창 혼자서는 못 돌림 · 사람이 들어가 준 창에서 열려 있음」처럼 열린 쪽으로 두는 편이 맞습니다
  옛 뜻이 남은 줄
    docs/인계.md:4  맨 위 갱신 줄이 아직 「그래서 phone.js 의 안 심은 둘은 총무팀이 본다(22번)」입니다.
    [D] 가 바로잡은 옛 뜻(사람이 보면 대신된다)입니다

[E] 짝 셈 · 봐 둘 것 (지금 새는 파일은 없음)
  tests/test_docs_shell.py:167-176  잰 것
    시작이 두 번 나오고 끝이 하나 -> 뒤 시작부터만 뺍니다. 앞 구간의 %VAR% 는 걸립니다(3번 줄이 걸림).
      빨개지는 쪽이라 안전합니다
    원문 안 산문에 끝 글자를 인용 -> 거기서 닫혀 그 아래 원문 줄이 걸립니다(4번 줄이 걸림).
      역시 빨개지는 쪽입니다
    원문 밖 산문 한 줄에 시작 글자, 한참 아래 다른 산문 한 줄에 끝 글자 -> 그 사이 블록의 %VAR% 가 통째로 안 걸립니다(걸린 것 없음).
      이것이 남은 새는 곳입니다
  원인은 「in」 으로 찾는 것입니다(:170, :172)
    scripts/check_placeholder.py:107-112, 160-162 는 같은 두 글자를 줄머리(startswith)로만 경계로 봅니다.
    까닭(본문이 경계 글자를 인용하는 순간 아래가 검사 밖이 된다)도 거기 적혀 있습니다.
    test_docs_shell.py:115-116 주석은 「그 글자」라고만 하는데, 실제로는 같은 경계가 두 규칙으로 갈렸습니다
  지금 저장소: git grep 으로 두 글자를 찾았고, 모든 파일에서 줄머리에 짝이 맞습니다.
    tests/test_stage38.py:105 가 산문 인용이지만 뒤에 진짜 시작이 있어 초기화됩니다
  권함: 줄머리로 맞추고, 05c 에 「산문 인용 시작 + 산문 인용 끝」 한 쌍을 더합니다
  「파일 끝까지 뺌」 고장이면 05c 가 빨개진다는 말은 코드로 읽어 맞습니다.
    직접 심어 보지는 않았습니다(파일을 안 고침)

[F] 재정 표 · 막을 것 없음
  docs/인계.md 재정 표 예산 줄의 「수입을 고치는 길이 없다」는 app/routers/budget.py 의 라우트(수입은 넣기 :206 · 지우기 :252 뿐)와 같습니다

[G] 인계 맨 위 표 · 막을 것 없음
  docs/인계.md:26, :33 은 [D] 와 같은 뜻입니다. 남은 옛 줄은 :4 이고, [D] 에 적었습니다

[H] 「검토 뒤 고친 것」을 파일로 가름 · 봐 둘 것 (사람에게 물음)
  CLAUDE.md 11-3 4단계 추가 문단
  가름이 사람 판단에서 파일로 옮겨져, 앞선 [H] 의 걱정은 풀렸습니다
  이번 판의 다른 문장과 부딪히는 자리 둘
    ① 끝이 없습니다
       다시 보인 뒤에 또 고치면(지금처럼 [D] 의 :4 를 고치면) 그 차이도 또 보여야 하는지 문장이 말하지 않습니다.
       글자대로면 매번 한 바퀴 더 돕니다
    ② 같은 4단계의 「보고를 다 채운 뒤에 검사와 시험을 다시 돌립니다」와 맞물립니다
       검토자 원문을 보고에 싣다 시험이 걸려 시험을 고치면(3판 test_docs_shell 이 그 모양), 그 고침은 보고 밖 파일이라 다시 보여야 합니다.
       그 답을 또 원문으로 보고에 실으면 다시 걸릴 수 있습니다. 순서가 닫히지 않습니다
  문장 안의 「docs 의 목록들」도 경계가 흐립니다.
    docs/review/ 밖이 전부라면 docs/인계.md · 봐둘것.md 도 드는데, 「목록」이라는 말이 그중 일부만 가리키는 것처럼 읽힙니다.
    「docs/review/ 밖 전부」로 한 번만 적으면 됩니다

곁에서 본 것
  tests/__pycache__ 에 test_zz심기_* 의 .pyc 가 남아 있습니다(원본 .py 는 없음 · 추적 안 됨).
    앞 판에서 고장을 심은 흔적이고, 해는 없습니다

막아야 함: 없음

사람에게 묻는 것
  1. [C] 비밀번호 규칙의 경계를 도구 이름(pytest)이 아니라 조건(그 자리에서 만든 앱 · 저장소의 지어낸 값)으로 적을지
  2. [D] 봐둘것 AY-a 의 두 라벨을 「닫음」에서 열린 쪽 말로 바꿀지
  3. [H] 다시 보인 뒤의 고침을 또 보일지. 보인다면 몇 바퀴에서 멈출지, 멈추면 무엇을 남길지
  4. [E] 원문 경계를 check_placeholder 와 같은 줄머리 규칙 하나로 맞출지
━━━ 검토자 여기까지 ━━━
```

**물은 넷에 대해** — 네 가지 모두 권한 쪽으로 고쳤습니다(6장). 3 의 「한 번」 이라는 선만 사람칸 「정할 것」 ① 로 올렸습니다.
