# 검토용 보고

작성 시각: 2026-09-04 23:15 (KST)
커밋: b807459 기준 (브랜치 `main`)
작업: 검토자 결과를 통째로 복사할 수 있게 한다 (문서만)

---

## 요약

```
881 passed in 425.19s (0:07:05)
```

```
> .venv\Scripts\python.exe scripts/check_names.py
실명 33개로 파일 192개를 봤습니다.
걸린 것이 없습니다.
아직 안 고친 곳 5곳 (docs/이름-남은곳.txt)
                                                    exit=0
```

---

## 무엇이 문제였나

**첫 시험에서 검토자 결과가 중간에 갈렸습니다.** 코드블록으로 시작했는데
안쪽에 또 코드블록을 열었고(`python` 인용 여러 개), **안쪽 울타리가
바깥 블록을 끊었습니다.**

사람이 이 결과를 **채팅에 붙여 넣습니다.** 복사 버튼 한 번으로 전체가
잡혀야 하는데, 조각조각 골라 긁어야 했습니다.

> Claude Code 덩어리(11-3 4단계)는 처음부터 끝까지 한 블록이라 한 번에
> 잡힙니다. **검토자 결과도 같은 모양이어야 합니다.**

---

## 고친 것

### `docs/검토-원칙.md` — 「어떻게 낼 것인가 — 코드블록 하나」 (84~99줄)

| | |
|---|---|
| 1 | **결과 전체를 하나의 코드블록 안에.** 중간에 풀지 않는다 |
| 2 | **그 안에서 마크다운 서식을 쓰지 않는다** — 어차피 안 그려지고, 붙여 넣은 쪽에서 기호가 그대로 보인다. 강조는 **줄바꿈과 들여쓰기**로 |
| 3 | **블록 안에 또 블록을 열지 않는다.** 인용할 코드는 들여쓰기로 — **이번에 그래서 갈렸다** |
| 4 | **경계 표시는 블록 안에** (서식 예시를 함께 적음) |
| 5 | 구현자는 **손대지 않고 그대로** 낸다. 앞뒤에 자기 말을 붙이지 않는다 — 붙이면 사람이 어디까지 복사할지 다시 골라야 한다 |

### `.claude/agents/검토자.md` — 「내는 것」 앞 (40~52줄)

같은 내용을 짧게 넣었습니다. **검토자가 원칙 문서를 읽기 전에도 이것만은
알도록** 에이전트 정의 쪽에도 뒀습니다.

> 규칙을 두 곳에 적은 셈이지만, 여기는 **같은 사실이 갈릴 자리가
> 아닙니다** — 에이전트 정의는 "이렇게 내라" 한 문단이고 원칙 문서가
> 정본입니다. 검토자.md 는 마지막 줄에서 원칙 문서를 가리킵니다.

---

## 수용 기준

| # | 기준 | 결과 |
|---|---|---|
| 1 | 검토-원칙.md 에 다섯이 적혀 있다 | 통과 (84 · 85 · 88 · 91 · 97줄) |
| 2 | 검토자.md 에도 "코드블록 하나로 낸다" | 통과 (40 · 47줄) |
| 3 | 다음 검토부터 복사 버튼 하나로 전체가 잡힌다 | **다음 검토에서 확인됩니다** |

**3번은 이번에 확인할 수 없습니다.** 검토자를 다시 돌리지 않았기
때문입니다 — 지시문에 없었습니다. 확인하려면 한 번 더 돌리면 됩니다.

---

## 바뀐 파일

```
.claude/agents/검토자.md
docs/검토-원칙.md
docs/review/최근.md
```

**코드는 하나도 안 바뀌었습니다.**

---

## 재시작 (11-3 2단계) — 건너뛰었습니다

`app/` · `templates/` · `static/` 이 하나도 바뀌지 않았습니다.
서버가 읽는 것이 하나도 달라지지 않았습니다.
