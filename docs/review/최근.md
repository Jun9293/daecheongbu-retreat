사람이 할 것
노트북에서 직접: 없음 — 운영 채우기(`--실행`)는 지시문대로 다음 판에 이 창이 돌린다(4장)
관리자 권한: 없음 — 재시작은 이 창에서 됐다(PID 34552 → 19156 · 5장)
정할 것: 회의록 id 채우기 — 미리보기 수(채울 회의 50 · 안 건드릴 회의 3)를 보고 「그대로 해」 할지(4장)
막혀서 못 한 것: 화면 점검(drawer.js · phone.js) — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다(5장)

작성 시각: 2026-09-15 23:58 KST
커밋: 4a3ab40 (1판 얼림) · 0f5dad4 (칸 · 전환 · 시험) · ecde39c (보고 · 문서) · 검토 뒤 고침은 그다음 커밋
작업: 노션 대조 2판 — 회의록에 외부 id 를 단다

---

**값은 여기 없다.** 페이지 제목 · 페이지 id 는 저장소 밖 `data/노션회의록.real.json` 에만 있다(`data/*.real.*` · gitignore).
여기에는 건수 · 칸 이름 · 구조만 적는다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `meetings` 표를 읽고 쓰는 곳 — 새 칸은 모델 · 칸 목록(`app/db.py`) · 새 스크립트만 쓴다. `app/` 의 다른 파일은 그 칸을 모른다(`test54_a01` 이 잰다) |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면은 안 고쳤다 |
| 그 값을 세어 둔 문서 | 인계 「진행 중 1)」(미정 줄 · 1판의 「관련 팀 없음」 줄) · CLAUDE.md 13장(노션 줄) · 14장 표(회의록 옮기기 줄 옆) |

# 1. 확인 — 제목을 글자 그대로 견줌 (0a · 0b)

노션 페이지 목록은 노션에서 **조회만** 해 `data/노션회의록.real.json` 에 옮겨 적었다(제목에 회의록이 든 페이지 10). 운영 DB 는 `mode=ro` 로 셌다.

| 견준 것 | 같음 | 다름 |
|---|---|---|
| 노션 페이지 제목 ↔ 앱 출처(`source_ref` = 내보낸 파일 이름) | 0 | 10 |
| 노션 페이지 제목 ↔ 앱 회의 제목(그 출처로 이어진 노션 회의) | 0 | 50 |

**다른 까닭 (짐작 · 고치지 않았다)**

- 파일 이름은 내보낼 때 사람이 **순서 번호를 붙이고 줄여 지은 이름**이다 — 번호 · 빈칸 · 끝의 「회의록」 을 떼면 **8 이 페이지 제목과 글자가 같고**, 남은 2 는 파일 이름 글자가 제목 안에 **순서대로 다 들어 있다**(줄인 이름)
- 회의 제목은 페이지 제목이 아니라 **페이지 안의 날짜 머리**다 — `scripts/import_meetings.py` 가 한 페이지를 날짜마다 잘라 그 머리를 제목으로 넣었다(`title=m.heading`). 50 중 48 이 숫자로 시작한다
- 그래서 「제목이 같은가」 는 이 자료에서 짝의 근거가 못 되고, 짝은 2장의 규칙(파일 이름 줄기 ↔ 페이지 제목 줄기)으로 정했다

**1판을 바로잡을 것 하나** — 1판 5장이 「노션 업무 DB 속성 목록에 `관련 팀` 이 없다」 고 적었는데 틀렸다. 페이지를 열어 보면 속성에 있다(1판이 속성 목록을 뽑다 놓쳤다). CLAUDE.md 13장은 맞았고, 인계의 그 줄에 「2판에서 틀린 것으로 확인」 을 덧붙였다.

# 2. 외부 id 칸 (1a · 1b)

- **`meetings.notion_page_id`** · `VARCHAR(36)` · NULL — 모델(`app/models.py` 의 `Meeting`)과 `app/db.py` 의 `_ADDED_COLUMNS` 한 줄
- **부팅은 칸만 붙이고 값을 안 채운다** — `_ADDED_COLUMNS` 는 NULL 칸이라 11-2 의 「칸 붙이기」 쪽이다. 채우는 곳은 스크립트 하나(`test54_a01` 이 `app/` 에 그 칸을 쓰는 곳이 없음을 잰다)
- 유니크 아님 — 한 페이지가 회의 여럿으로 잘려 여러 행이 같은 값을 갖는다
- 규칙을 적은 곳 — 짝 규칙은 스크립트의 `짝짓는다` 하나, CLAUDE.md 는 14장 표 한 줄이 그리로 가리킨다(두 곳에 규칙을 적지 않았다)

# 3. 전환 `scripts/회의록id달기.py` (2a–2d)

- 다른 전환과 같은 꼴 — 기본 미리보기 · `--실행` · 채울 것이 있을 때만 직전 `VACUUM INTO` 사본(`재정들여오기.사본을_뜬다` — 세션이 여는 파일과 사본 뜰 파일이 다르면 멈춤) · 채운 뒤 commit 전에 DB 를 다시 셈(id 달린 수 · 채운 행마다 id · 노션이 아닌 회의의 칸 — 셋 다 칸만 고른 select 로 DB 에서 읽는다 · 검토 [M] 1 뒤 고침)
- **짝** — 파일 이름에서 앞 번호를 떼고 페이지 제목에서 빈칸과 끝의 「회의록」 을 뗀 것이 ① 글자 같으면 짝 ② 남은 것끼리는 파일 이름 글자가 제목에 순서대로 다 든 페이지가 **전체 페이지 중 하나일 때만** 짝. 둘 이상이거나 없으면, 번호를 뗀 출처 이름이 겹치거나 한 페이지가 출처 둘과 짝지어지면 멈춘다(검토 [M] 2 뒤 좁힘 · 운영 미리보기 수는 좁힌 뒤에도 같다)
- **대상** — origin 이 노션이고 출처가 그 파일인 회의 전부(한 파일이 여럿으로 잘렸으면 그 여럿 전부에 같은 id)
- **안 건드림** — 노션이 아닌 회의(앱에서 만든 3). 같은 id 가 있으면 건너뛰고, **다른 id 가 있으면 덮지 않고 멈춘다**
- 두 번째 실행은 「채울 것이 없습니다」
- 값은 안 찍는다 — 건수만

# 4. 검증 (3a · 3b)

**운영 미리보기** (재시작 뒤 · `--실행` 없음 · 운영 DB 에 쓴 것 없음):

| 줄 | 수 |
|---|---|
| 노션 페이지 | 10 |
| 앱 출처 파일 | 10 |
| 짝지은 파일 | 10 |
| 짝 없는 페이지 | 0 |
| 파일 이름 = 페이지 제목(글자 그대로) | 0 |
| 노션 회의 | 50 |
| 회의 제목 = 페이지 제목(글자 그대로) | 0 |
| 출처 없는 노션 회의 | 0 |
| 노션이 아닌 회의(안 건드림) | 3 |
| 이미 id 있음 | 0 |
| 채울 회의 | 50 |

재시작 뒤 운영 DB(`mode=ro`)에 칸이 있고 값이 있는 행은 0 이다 — 부팅이 칸만 붙였다.

**`--실행` 은 돌리지 않았다** — 지시문이 「미리보기까지만 내고 총무님이 수를 보고 「그대로 해」 한 뒤 다음 판에서」 로 정했다. 11-2 의 「미리보기·사본·되돌릴 수 있는 운영 명령은 작업하는 쪽이 돈다」 와 부딪히지 않는다 — 돌리는 쪽은 여전히 이 창이고, 지시문이 **때**를 다음 판으로 정했다.

**실행 뒤 수가 계획과 같은가 · 두 번째가 「채울 것이 없습니다」 인가**는 운영이 아니라 시험에서 쟀다(`test54_c01`) — 운영에서는 다음 판에 잰다.

| 시험 | 무엇 |
|---|---|
| `test54_a01` | 칸이 NULL 로 붙고 `app/` 에 그 칸을 쓰는 곳이 모델 · 칸 목록 말고 없다 |
| `test54_b01` · `b02` · `b03` | 짝 규칙 — 글자 같음 먼저 · 순서대로 든 것이 전체에서 하나 / 없거나 둘 · 출처 이름 겹침이면 멈춤 |
| `test54_c01` | 미리보기는 안 바꿈 · 실행은 잘린 회의 둘에 같은 id · 사람 회의 안 건드림 · 두 번째 「채울 것이 없습니다」 |
| `test54_c02` | 다른 id 가 이미 있으면 덮지 않고 멈추고 아무것도 안 바뀜 |
| `test54_c03` | 페이지 목록 파일이 없거나 비면 멈춤 |
| `test54_c04` | 채운 뒤 DB 에서 다시 센 수가 어긋나면 되돌리고 아무것도 안 남음(고장을 심어 잼) |
| `test54_c05` | 사본을 뜰 수 없는 DB(시험 DB)에서 `--실행` 은 쓰기 전에 멈춤 |

# 5. 재시작 (11-3 2단계)

`app/models.py` · `app/db.py` 를 고쳐 12-1 대로 재시작했다 — **멈추기 전 PID 34552 → 다시 켠 뒤 PID 19156 · 성공**(2026-09-15 23:50). 올라온 뒤 `/login` 이 200 이고, 운영 DB 에 `notion_page_id` 칸이 붙었고 값 있는 행은 0 이다. **`drawer.js` · `phone.js` 는 못 돌렸다** — 11-3 2단계는 `app/` 이 바뀐 판에 조건 없이 돌리라고 하는데, 로그인이 필요하고 이 창은 비밀번호를 안 넣는다(11-2). 화면(템플릿 · 정적 파일)은 안 고쳤다. 사람이 들어가 준 창에서 돌리는 길이 남는다 — 사람칸 「막혀서 못 한 것」(검토 [M] 3)

# 6. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 제목을 실제로 견준 건수 | O | 1장 표 |
| 나) 외부 id 칸 · 부팅은 안 채움 | O | 2장 · `test54_a01` · 5장(운영 값 0) |
| 다) 미리보기와 실행 뒤 수가 같음 · 두 번째 「채울 것이 없습니다」 | 시험에서만 — 운영은 다음 판 | `test54_c01` · `c04` · 4장 |
| 라) 앱에서 만든 회의 3 안 건드림 | O — 시험과 운영 미리보기 | `test54_c01` · 4장 표 |
| 마) pytest 옵션 없이 · 글검사 7/7 | 마지막 pytest 뒤 채움 | 9장 |
| 바) 검토자 원문 | 검토 뒤 채움 | 10장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 0a | 했다 | 1장 — 페이지 제목 ↔ 출처 파일 · 회의 제목 |
| 0b | 했다 | 1장 — 짐작만 |
| 1a | 했다 | 2장 |
| 1b | 했다 | 2장 — 규칙은 스크립트 한 곳 |
| 2a | 했다 | 3장 |
| 2b | 했다 | 3장 — 잘린 회의 전부에 같은 id(`test54_c01`) |
| 2c | 했다 | 3장 · 4장 표 |
| 2d | 했다 | `test54_c01` |
| 3a | 했다 | 4장 표 |
| 3b | 다르게 했다 | 운영 `--실행` 을 안 돌려 실행 뒤 다시 세기는 시험에서만 쟀다 — 지시문 끝줄이 실행을 다음 판으로 정했다 |
| 4a | 했다 | CLAUDE.md 13장 · 14장 표 · 인계 맨 위와 1) |
| 4b | 했다 | 인계 1) |
| 운영 자료는 0·1·3단계에서 읽기만 · 2단계는 미리보기까지 | 했다 | 운영 DB 는 `mode=ro` 와 미리보기만 · 부팅이 NULL 칸을 붙인 것은 11-2 의 칸 붙이기 |
| 값을 보고와 덩어리에 싣지 않는다 | 했다 | 제목 · id 는 `data/노션회의록.real.json` 에만 |
| 마무리는 11-3 대로 | 했다 | 5 · 9 · 10장 |

# 7. 판단이 들어간 자리

- **짝의 근거를 제목이 아니라 줄기로** — 제목 그대로는 0/10 이라 못 쓴다. 번호 · 빈칸 · 「회의록」 만 떼는 좁은 규칙으로 8 이 글자 같고, 남은 2 는 「순서대로 든 것이 하나일 때만」 으로 묶었다. 기계가 한 쪽으로 못 정하면 멈춘다
- **페이지 목록을 저장소 밖 json 으로** — 저장소에 노션 API 코드가 없고(1판 0b), 이 판도 API 코드를 더하지 않았다. 목록은 이 창이 노션 조회로 옮겨 적었다
- **다른 id 가 이미 있으면 멈춤** — 덮으면 사람이 손으로 바로잡은 값을 조용히 지운다

# 8. 봐둘것 · 옛말

- `docs/봐둘것.md` 를 열었다 — 이 판이 닫거나 새로 미룬 항목은 없다(다음 할 일은 인계 1) 에)
- 1판 보고를 `docs/review/2026-09-15-노션대조1판.md` 로 얼렸다

# 9. 검사와 시험

- 커밋 전 `git status --short`: 코드 · 시험 · 문서만 — `data/` 안의 것은 안 잡혔다(페이지 목록 json 은 gitignore)
- 글검사: 첫 커밋 전 7/7(새 파일이 아직 추적 전이라 이름 검사가 한 번 걸려, 추적한 뒤 저장소 밖 json 한 줄을 `docs/이름-넘김.txt` 에) · 검토 뒤 고침 커밋 전 7/7
- 새 시험: `tests/test_stage54.py` 9 통과
- 바뀐 파일(`git diff --name-only 7c3e9a4..HEAD`): `CLAUDE.md` · `app/db.py` · `app/models.py` · `docs/review/2026-09-15-노션대조1판.md` · `docs/review/최근.md` · `docs/이름-넘김.txt` · `docs/인계.md` · `scripts/회의록id달기.py` · `tests/test_stage54.py`
- 보고를 다 채운 뒤 돌린 pytest: <!-- 최종 -->

# 10. 검토자 원문

## 10-1. 첫 검토 (범위 7c3e9a4..ecde39c)

```
━━━ 검토자 여기부터 ━━━ 범위 7c3e9a4..ecde39c · 커밋 3개 · 파일 9개

막아야 함: 없음

[M] 1. 실행 뒤 다시 세기 셋 중 둘은 틀릴 수가 없는 검사다. 실제로 막는 검사는 수 하나뿐이고, 되돌리는 길을 밟는 시험이 없다
    scripts/회의록id달기.py 돌린다() · 164–171행 근처
        if any(db.get(Meeting, mid).notion_page_id != pid for mid, pid in 판.채울)
        if any(db.get(Meeting, mid).notion_page_id != v for mid, v in 건드리지않을.items())
    - db.get 은 세션이 이미 들고 있는 객체를 돌려준다. 첫째는 바로 앞 줄에서 넣은 값을
      그대로 다시 읽는다. 둘째도 세션이 들고 있는 객체의 값끼리 견준다
      (건드리지않을 을 채울 때 불러온 객체와 같은 객체다). 7-5 가 경계한
      "계획에서 되짚은 수로 견주면 절대 안 어긋난다" 와 같은 꼴이다
    - SQL 로 다시 세는 것은 단수(db) 하나다. 그런데 채울 목록에는 이미 NULL 인 행만 들어가고
      id 가 겹치지 않아서, 사실상 이것도 어긋날 길이 거의 없다
    - 되돌림(rollback)이 도는지 재는 시험이 없다. test54_c02 는 고른다() 단계에서 멈추는 경우라
      쓰기 전이다. 막는 코드의 ①·③(11-3 되짚는 것)이 비어 있다
    - 고칠 곳: 둘째·셋째 검사를 SQL 로 바꾼다 (select id, notion_page_id 를 새로 읽거나
      populate_existing 을 쓴다. 노션이 아닌 회의는 count(notion_page_id) 로 센다).
      고장을 하나 심어 멈추고 되돌리는지 재는 시험을 하나 둔다

[M] 2. 짝 규칙이 문서가 말하는 것보다 느슨하다. 틀린 짝이 조용히 생길 자리가 둘 있다
    scripts/회의록id달기.py 짝짓는다() · 모듈 독스트링 · CLAUDE.md 14장 새 줄
    ㄱ. 출처 둘의 줄기가 같으면 멈추지 않는다. 1차에서 첫째가 페이지를 가져가고 둘째는 2차로
        넘어간다. 거기서 그 줄기를 순서대로 담은 다른 페이지가 하나 남아 있으면 그 페이지와
        짝이 된다. len(같은) > 1 은 페이지 쪽이 겹칠 때만 본다
    ㄴ. "하나일 때만" 을 모든 페이지가 아니라 남은 페이지 가운데서 센다. 1차에서 빠진 페이지를
        빼고 세므로, 여럿에 걸리는 짧은 줄기도 하나로 보인다
    ㄷ. json 에 빠진 페이지가 있으면 ㄱ·ㄴ 과 겹쳐 틀린 페이지가 "하나" 로 잡힌다
        (목록은 "제목에 회의록이 든 페이지" 로 거른 것이다 — 보고 1장)
    - 지금 운영 자료로는 틀린 짝이 없다. 이 검토에서 읽기 전용으로 쟀다
        출처 줄기끼리 겹침 0
        글자가 같은 짝 8
        순서대로 든 짝 2 — 둘 다 전체 10 페이지 가운데 후보가 1
        페이지 id 는 전부 36자이고 겹침 0
        남은 페이지에서 세든 전체에서 세든 결과가 같다
    - 그래도 규칙은 이 파일 하나가 정본이다. 다시 들여오기(import_meetings --undo 뒤)나
      다음 판(업무 ㄱ2)이 이 규칙을 가져다 쓰면 그대로 샌다. 고치는 데 몇 줄이면 된다
      (출처 줄기가 겹치면 멈추고, 후보는 전체 페이지에서 센다). test54_b02 에 ㄱ 한 줄을 더한다

[M] 3. drawer.js · phone.js 를 건너뛰었는데 사람칸이 "막혀서 못 한 것: 없음" 이다
    docs/review/최근.md 5장 · 사람칸
    - 11-3 2단계는 app/ 이 바뀐 판에서 두 점검을 조건 없이 돌리라고 한다
      ("상세 패널을 건드렸으면" 같은 조건을 두지 않는다). 보고의 까닭
      "화면은 안 고쳐" 는 바로 그 규칙이 막은 조건이다
    - 실제로 막은 것은 로그인이다(11-2: 이 창은 비밀번호를 넣지 않는다). 그렇다면 막혀서 못 한
      것이므로 "막혀서 못 한 것" 이나 "노트북에서 직접"(사람이 들어가 준 창) 줄에 올라가야 한다
    - 화면 영향이 사실상 없는 변경이라 무게는 낮다. 다만 빈 줄로 두면 11-3 을 어긴 것이
      기록에서 보이지 않는다

[L] 4. test54_a01 이 무엇을 재는지 — 대체로 제대로 보지만 글자만 본다
    tests/test_stage54.py a01
    - app/*.py 에서 models.py 와 db.py 는 통째로 빼고 이름을 찾는다. db.py 는 그 이름이
      한 번만 나오는지 따로 센다. 부팅이 UPDATE 로 채우거나 다른 파일이 그 칸을 쓰면 잡힌다
    - 못 보는 것
        models.py 안에서 채우는 경우
        훑은 파일 수가 0 이어도 통과한다 (rglob 결과가 비는지 단언하지 않는다)
        글자로 찾으므로 실제 동작을 재지 않는다
    - "부팅이 안 채운다" 의 실제 증거는 보고 4·5장에 있다. 재시작 뒤 운영에서 값이 든 행이 0 이다.
      이 검토에서도 운영 DB 를 읽기 전용으로 봤다: 53행 중 값 있는 행 0, origin NULL 3 · 노션 50
    - 원하면 init_db 를 새 DB 에 돌린 뒤 NULL 인지 재는 시험 하나로 대신할 수 있다

[L] 5. 사본을_뜬다 를 가져다 쓴 것은 맞다. 다만 사본=True 로 도는 길은 이 스크립트 시험에 없다
    - 세션이 여는 파일과 사본 뜰 파일을 견주는 지킴은 같은 함수라 그대로 온다.
      stage48 · stage51 이 그 함수를 잰다
    - 이 스크립트가 쓰기 전에 그 함수를 부르는지는 재지 않았다. 시험 DB 는 data/app.db 가 아니므로
      사본=True 로 부르면 멈춰야 한다. 그 한 줄이면 ① 이 된다
    - 채운 값은 NULL 로 되돌리면 되므로, 사본 이름을 지키는판 에 넣을 일은 없어 보인다.
      다음 판 인계가 그 사본을 되돌리는 자리로 가리키면 11-2 대로 함께 넣는다

[L] 6. 3b 를 "다르게 했다" 로 둔 판단은 맞다. 그런데 수용 기준 다) 의 O 는 근거보다 앞선다
    docs/review/최근.md 6장
    - 지시문 끝줄이 실행을 다음 판으로 정했으므로 3b 를 운영에서 못 한 것은 까닭이 맞다
    - 다) 는 "미리보기와 실행 뒤 수가 같음" 이다. 운영에서 한 번도 안 쟀고, 시험 쪽 다시 세기는
      1번처럼 거의 틀릴 수가 없다. "O" 보다 "시험에서만 · 운영은 다음 판" 을 판정 칸에 두는 편이
      읽는 사람을 덜 속인다

[L] 7. 문장이 근거보다 앞서거나 흐린 자리
    - CLAUDE.md 13장 "2026 여름 회의록은 노션과 대조가 끝났습니다" — 운영 값은 아직 0 이고
      채우기는 사람 결정 뒤다. 바로 뒤 문장이 그것을 밝히므로 틀린 말은 아니다.
      다만 "짝 확인 · 채우기 전" 쪽이 사실에 가깝다
    - docs/인계.md 맨 위 "…날짜 머리라서로 짐작" — 오타 (라서로 → 라서)
    - docs/인계.md 1) 미정 줄 "(정함 … 회의록 id 칸은 …) 업무의 외부 id 를 어느 표의 어느 칸에 둘지"
      — 정한 것과 안 정한 것이 한 문장에 섞여 무엇이 남았는지 읽기 어렵다
    - 1판의 "관련 팀 없음" 을 바로잡은 줄은 지시문 밖이다. 보고 1장에 적혀 있어 괜찮다

없는 것
- 사람칸 다섯 줄 · 순서 · 값: 이상 없음
- 지시문 대조 표: 0a~4b 와 끝줄 항목이 다 있다. "다르게 했다" 에 까닭이 있다
- 고치기 전 훑기: 있고, 짚은 자리(인계 1) · 13장 · 14장)를 실제로 고쳤다
- 얼린 1판(docs/review/2026-09-15-노션대조1판.md): 7c3e9a4 의 최근.md 와 글자까지 같다
- 실명 · 페이지 제목 · id 는 커밋된 글에 없다. 시험 값은 지어낸 것이다.
  data/노션회의록.real.json 은 이름-넘김에만 이름이 적혔다
- 같은 사실이 두 곳에 생긴 자리: 짝 규칙의 정본을 스크립트로 두고 14장이 그리로 가리킨다.
  다만 독스트링과 14장 설명이 2번의 느슨함을 같이 틀리게 적는다
- 칸이 NULL 로 붙는 것(11-2 칸 붙이기 쪽): 맞다

사람에게 묻는 것
1. 운영 채우기 전에 짝 규칙(2번 ㄱ·ㄴ)을 먼저 좁힐지, 운영에서 쟀으니 이번 한 번은 그대로 돌릴지
2. 이 판이 건너뛴 drawer.js · phone.js 를 사람이 들어가 준 창에서 돌릴지, 화면 영향이 없는 판이라 넘길지
3. 수용 기준 다) 를 O 로 둘지, "운영은 다음 판" 판정으로 내릴지
━━━ 검토자 여기까지 ━━━
```

## 10-2. 첫 검토를 받아 고친 것

| 번호 | 받았나 | 고친 곳 |
|---|---|---|
| [M] 1 | 받음 | 다시 세기 셋을 칸만 고른 select 로 DB 에서 읽게 · `test54_c04`(고장을 심어 되돌림을 잼) |
| [M] 2 | 받음 | `짝짓는다` — 번호를 뗀 출처 이름이 겹치면 멈춤 · 순서대로 든 후보를 전체 페이지에서 셈 · 한 페이지가 출처 둘과 짝이면 멈춤 · 독스트링 · `test54_b02` 두 줄 · `b03`. 좁힌 뒤 운영 미리보기 수는 같다(4장 표 그대로) |
| [M] 3 | 받음 | 사람칸 「막혀서 못 한 것」 과 5장 |
| [L] 4 | 일부 받음 | 훑은 파일 수가 비지 않는지 · `models.py` 에 그 이름이 선언 한 번뿐인지. `init_db` 를 새 DB 에 돌리는 시험은 안 더했다 — 그 함수는 앱의 전역 엔진에 붙어 시험 DB 가드(11-3)와 부딪힌다. 운영 값 0 이 실제 증거로 남는다 |
| [L] 5 | 받음 | `test54_c05` |
| [L] 6 | 받음 | 수용 기준 다) 판정을 「시험에서만 — 운영은 다음 판」 으로 |
| [L] 7 | 받음 | CLAUDE.md 13장 「짝 확인까지 끝났고 id 는 채우기 전」 · 인계 맨 위 같은 뜻과 오타 · 1) 을 정함 줄과 미정 줄로 가름 |

사람에게 묻는 것 — 1 은 좁혀서 닫았다. 2 는 사람칸 「막혀서 못 한 것」 에, 3 은 판정을 내려 닫았다.

## 10-3. 두 번째 검토 (검토 뒤 고친 차이)

두 번째 검토의 답을 여기에 싣는다.
