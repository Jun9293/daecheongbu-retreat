작성 시각: 2026-09-10 10:15 (KST)
커밋: 818eca3 (그 위에 이 보고를 담는 커밋 하나)
작업: 도막 4 — 여러 부서·역할 구현 · 바깥 주소는 설정으로

---

# 0. 먼저 셌습니다

## 0-(a) 도막 3 의 212 자리 점검표

도막 3 보고(`docs/review/2026-09-10-도막3.md` 0-(b)~(e))의 표를 줄 단위로
따라가며 이번 판에서 어떻게 됐는지 적었습니다. 줄 번호는 **도막 3 커밋 기준**
(5190b52)이고 이번 판에서는 대부분 밀렸습니다 — 자리는 이름으로 찾습니다.
「안 고쳐도 된다」 는 그 줄이 부르는 함수 쪽이 바뀌어 **부르는 쪽은 그대로여도
새 규칙으로 도는** 자리입니다.

### (b) 역할 읽기·쓰기 — 72

| 자리 (도막 3 줄) | 수 | 결과 |
|---|---:|---|
| `permissions.py` 판정 함수 여섯 | 6 | **고쳤다** — 파일을 다시 썼다. `can_manage_retreat`·`can_see_account`·`can_manage_users` 는 `User` 도 역할 문자열도 받고, `is_readonly` 는 소속 유무로(②), `can_edit_department_content`(id 비교)는 없앴고 `can_edit_department_by_key` 는 `can_edit_department_key` 로 |
| `security.py` 90 · 97 | 2 | **고쳤다** — `perm.is_admin(user)` · `perm.is_readonly(user)` |
| `security.py` 117 · 123 | 2 | **고쳤다** — `perm.can_edit_department_key(user, target.key)` 하나 |
| `templating.py` 149 · 154 | 2 | **고쳤다** — `can_edit_dept` 가 `can_edit_department_key` |
| `templating.py` 168 | 1 | **고쳤다** — `is_other_dept` 가 키 **집합**으로 |
| `templating.py` 187 · 190 · 191 | 3 | **고쳤다** — 전역 셋이 `user` 를 받는다 |
| `departments.py` 67 | 1 | **고쳤다** — `users_in_department(dept_role=)` 이 `perm.members_of` 를 부른다 |
| `home.py` 148 | 1 | **고쳤다** — `my_dept_keys` 집합으로 흐림 |
| `notify.py` 113 · 249 | 2 | **고쳤다** — `perm.admins(db)` (재촉의 총무팀은 비활성 포함 `active_only=False` 그대로) |
| `notify.py` 108 | 1 | **고쳤다** — `perm.leads_of` 로 **리더 전부** (⑤) · `recipient_for` → `recipients_for` |
| `notifications.py` 54 · 96 | 2 | **고쳤다** — `perm.is_readonly(user)` |
| `notifications.py` 67 · 102 | 2 | **고쳤다** — `perm.admins(db)` |
| `admin_users.py` 106 · 107 (표시) | 2 | **고쳤다** — 파일을 다시 썼다. 역할은 `ALL_ROLES` 셋, 소속은 `perm.describe` |
| `admin_users.py` 289 · 299 (쓰기) | 2 | **고쳤다** — `role` 은 `ALL_ROLES` 만 받고 소속은 `perm.set_memberships` |
| `attachments.py` 67 | 1 | **고쳤다** — `can_edit_department_key` |
| `board.py` 37 · 59 · 699 · 794 | 4 | **고쳤다** — 같은 함수 |
| `board.py` 149 | 1 | **고쳤다** — `perm.is_admin(user)` (문자열 비교 없앰) |
| `board.py` 349 · 362 | 2 | **고쳤다** — 후보는 `perm.my_dept_keys(user) & keys` · 라벨은 `perm.ROLE_LABELS` |
| `board.py` 396 | 1 | **고쳤다** — `perm.is_admin(run.assignee)` |
| `board.py` 602 · 688 · 785 | 3 | **고쳤다** — `perm.is_readonly(user)` |
| `board.py` 664 | 1 | **고쳤다** — `perm.is_admin(user)` |
| `drafts.py` 57 · 156 · 158 | 3 | **고쳤다** — `is_admin` · `is_readonly` · `key not in my_dept_keys` |
| `expenses.py` 167 | 1 | **고쳤다** — `can_see_account(user)` |
| `export.py` 44 | 1 | **고쳤다** — 같은 것 |
| `live.py` 55 · 103 | 2 | **안 고쳐도 된다** — `can_manage_retreat(user.role)` 은 전체 역할 물음이고 함수가 문자열을 그대로 받는다 |
| `meetings.py` 91 · 114 · 702 | 3 | **고쳤다** — `is_readonly(user)` (사람을 넘겨야 소속을 본다) |
| `reviews.py` 43 · 73 · 228 | 3 | **고쳤다** — 셋 다 `perm.is_admin(user)` (문자열 `"admin"` 비교 없앰) |
| `settings.py` 231 | 1 | **고쳤다** — `perm.admins(db)` |
| `tasks.py` 63 · 68 | 2 | **고쳤다** — `perm.is_admin` · `can_edit_department_key` |
| `scripts/create_admin.py` 59 · 68 · 96 · 122 · 123 · 150 | 6 | **고쳤다** — `perm.ADMIN` · `perm.admins(db, active_only=False)` · `perm.is_admin` (68 은 라벨 표시라 그대로) |
| `scripts/healthcheck.py` 205 | 1 | **고쳤다** — `perm.ADMIN` |
| `scripts/merge_users.py` 85 · 101 · 168 | 3 | **고쳤다** — 85 는 `perm.ADMIN`, 표시 둘은 `perm.describe` 로 소속 줄을 낸다 |
| `scripts/계정정리.py` 180 | 1 | **안 고쳐도 된다** — 이미 `perm.ADMIN` 이었다 |
| `scripts/관리자링크.py` 84 · 96 · 112 | 3 | **안 고쳐도 된다** — 이미 `perm.ADMIN` 이었다 |
| **합** | **72** | 고쳤다 66 · 안 고쳐도 된다 6 |

### (c) 부서 읽기 — 54

| 자리 (도막 3 줄) | 수 | 결과 |
|---|---:|---|
| `departments.py` 44 · 46 (`department_key_of`) | 2 | **고쳤다** — 함수를 없애고 `department_keys_of(user) -> set` 이 `perm.my_dept_keys` 를 부른다 |
| `departments.py` 65 · 67 (`users_in_department`) | 2 | **고쳤다** — `perm.members_of(db, key, dept_role=)` |
| `home.py` 125 · 131 | 2 | **고쳤다** — `mine_unassigned` 가 **부서마다 한 줄**(`{key, name, count}`) |
| `home.py` 148 · 153 | 2 | **고쳤다** — 집합 |
| `notify.py` 108 | 1 | **고쳤다** — (b) 와 같은 줄 |
| `notifications.py` 87 · 91 | 2 | **고쳤다** — `department_members` 가 `perm.members_of(dept.key)` |
| `admin_users.py` 67 · 68 · 97 | 3 | **고쳤다** — 표의 소속은 `perm.roles_by_key` · `perm.membership_names` · `perm.describe` |
| `admin_users.py` 289 · 300 (쓰기) | 2 | **고쳤다** — `set_memberships(…, among=이번 회차 부서)` · `unassign_keys`(지난 부서 떼기) |
| `attachments.py` 68 | 1 | **고쳤다** |
| `board.py` 32 · 38 · 59 · 700 · 795 | 5 | **고쳤다** — `_dept_keys_of` 집합 · `can_edit_department_key` |
| `board.py` 352 · 354 | 2 | **고쳤다** — 후보 = 소속 키 집합과 업무 부서 키의 교집합 |
| `board.py` 400 | 1 | **고쳤다** — 옮긴 부서 키가 담당자의 소속 집합에 없으면 비운다 |
| `board.py` 663 | 1 | **고쳤다** — 기본값 `depts`(내 부서 전부 · ④), 소속 없으면 `all` |
| `calendar.py` 49 | 1 | **고쳤다** — 기본값 `depts`, 소속 없으면 `mine` |
| `drafts.py` 32 · 36 · 158 | 3 | **고쳤다** — `_default_key` 가 리더인 부서 첫째(③), 둘 이상이면 `draft_pick.html` |
| `expenses.py` 122 · 131 | 2 | **고쳤다** — 리더인 부서 우선, 없으면 소속 첫째 |
| `live.py` 81 · 125 · 214 | 3 | **고쳤다** — `_staff_default`(소속 중 하나가 헤브론·코람데오, admin 은 아님 ①) · `_first_key` |
| `notifications.py`(라우터) 57 | 1 | **고쳤다** — `perm.first_department(sender)` (리더인 부서 우선) |
| `reviews.py` 42 · 49 · 60 | 3 | **고쳤다** — `pending_for_user` 를 키 집합으로 다시 썼다 |
| `reviews.py` 79 · 80 | 2 | **고쳤다** — `can_respond_to` = admin 또는 `target.key in my_dept_keys` |
| `settings.py` 165 · 167 | 2 | **고쳤다** — 인원수는 `users_in_department(db, key)` |
| `tasks.py` 54 · 69 | 2 | **고쳤다** — 기본 `depts` · `can_edit_department_key` |
| `security.py` 118 · 124 | 2 | **고쳤다** — (b) 와 같은 줄 |
| `templating.py` 146 · 155 · 170 | 3 | **고쳤다** |
| `templating.py` 243 · 262 (`_side_dept_name`) | 2 | **고쳤다** — `perm.describe(user, dept_names=이번 회차 부서 이름)` → 「4 스케치 리더 · 5 헤브론 리더」 |
| `main.py` 236 | 1 | **고쳤다** — `perm.eager_load()` (표 이름을 밖에서 안 적는다) |
| `scripts/merge_users.py` 102 | 1 | **고쳤다** — `perm.describe(p)` |
| `anonymize.py` · `check_dev_db.py` (없음) | 0 | **고쳤다** — `check_dev_db.제외칸` 에 `user_departments.dept_role` · `site_settings.key` 를 근거와 함께. `anonymize.py` 는 아래 0-(c) |
| **합** | **54** | 고쳤다 54 · 안 고쳐도 된다 0 |

### (d) 템플릿 — 42

| 자리 | 수 | 결과 |
|---|---:|---|
| `board.html` 7 · 13 | 2 | **고쳤다** — `deptpick(…, depts=my_department_keys)` · `is_readonly(user)` |
| `budget.html` 25 · 34 … 157 (`엑셀을_받는다` · `is_admin` 11) | 12 | **안 고쳐도 된다** — 전역이 `user` 를 받게 바뀌었고 이 화면은 이미 `is_admin(user)` 꼴로 부르고 있었다. 파일이 diff 에 없다 |
| `checklists.html` 11 | 1 | **안 고쳐도 된다** — 같은 이유 |
| `checklists.html` 23 (`user.department.key`) | 1 | **고쳤다** — 내 부서 **중** 첫째를 미리 고른다 (처음엔 목록의 첫째일 때만 잡혔다 — 검토자가 잡았다) |
| `checklists.html` 48 · 58 · 72 · 83 (`can_edit_dept`) | 4 | **안 고쳐도 된다** — 함수 쪽이 바뀌었다 |
| `draft_none.html` 8 | 1 | **안 고쳐도 된다** |
| `expenses.html` 19 · 29 | 2 | **고쳤다** — `is_readonly(user)` |
| `expenses.html` 138 · 163 · 175 | 3 | **안 고쳐도 된다** — `can_edit_dept` |
| `expenses.html` 159 | 1 | **안 고쳐도 된다** — `계좌를_본다(user)` |
| `home.html` 95 | 1 | **고쳤다** — `home.mine_unassigned` 를 돌며 부서마다 한 줄 |
| `no_retreat.html` 8 | 1 | **고쳤다** — `user.role == 'admin'` 문자열 → `is_admin(user)` |
| `partials/settings_tabs.html` 8 · 13 | 2 | **안 고쳐도 된다** — `is_admin(user)` · 라벨은 전체 역할 |
| `retreat_base.html` 104 · 105 | 2 | **고쳤다** — 부서 줄은 `side_dept_name`(= `perm.describe`) 이 여럿을 잇는다. 그 밑에 바깥 링크 항목(값이 있을 때만) |
| `settings.html` 23 | 1 | **안 고쳐도 된다** — 라벨은 전체 역할. (같은 파일에 바깥 링크 폼을 더했다 — 아래 3장) |
| `settings_retreats.html` 13 · 20 | 2 | **안 고쳐도 된다** |
| `settings_retreat_detail.html` 17 | 1 | **안 고쳐도 된다** |
| `setup.html` 33 | 1 | **안 고쳐도 된다** — 라벨 |
| `admin_users.html` 62 · 66 · 113 · 125 | 4 | **고쳤다** — `<select name="department_key">` 는 없고 부서마다 `<select name="dept_<키>">`(— / 팀원 / 리더), 역할은 셋 |
| **합** | **42** | 고쳤다 13 · 안 고쳐도 된다 29 |

### (e) 시험 — 44

| 자리 | 수 | 결과 |
|---|---:|---|
| `conftest.make_user` 자체 | 1 | **고쳤다** — `make_user(name, phone, role, dept=None, departments=None)`. 옛 `role="dept_lead", dept=N` 도 그대로 통과(`test25_d01`) |
| `make_user(…, department_id=)` 호출 | 25 | **고쳤다** — 인자 이름을 `dept=` 로 (AST 로 바꿨다) |
| `models.User(…, department_id=)` 직접 | 17 | **고쳤다** — `conftest.legacy_user(db, …, dept=)` (세션 안에서 옛 모양으로 만든다) |
| `test_invite.py:542` 직접 `User` | 1 | **고쳤다** — `perm.assign(db, legacy, gone, perm.MEMBER)` |
| **합** | **44** | 고쳤다 44 |

### 새로 찾은 자리 — 도막 3 이 안 센 것

| 자리 | 수 | 무엇 |
|---|---:|---|
| `app/routers/settings.py` `/departments/create` | 1 | **키 없이 부서를 만들고 있었다.** 소속·알림이 키로만 돌게 되자 그 부서 사람은 아무것도 못 받는다 — 마법사의 발급 코드를 `departments.next_team_key` 로 빼서 둘이 같이 쓴다 |
| `app/domain/tasklist.py` · `templates/tasks.html` 「부서 전체」 | 2 | 기본이 내 부서가 되자 **빈 값(전체)이 기본으로 되돌아가 전체를 고를 수 없었다** — 값 `all` 을 뒀다. 시험 셋(`test14_b04`·`test15_m04`·`test22_d03`)이 잡았다 |
| `seed.py` · `seed_library.py` 의 `User(department_id=)` | 2 | 데모·추가 계정 만들기 — `perm.assign` 으로 |
| `scripts/부서옮기기.py` (새 파일) | 1 | 옮기는 스크립트 |
| `app/templates/draft_pick.html` (새 파일) | 1 | ③ 의 고르는 화면 |
| 시험의 옛 폼 값 `"role": "dept_lead", "department_key": …` (`test_admin_users` 8 · `test_invite` 17) | 25 | 도막 3 은 `department_id=` 인자만 셌다 — 설정 › 사용자 폼이 바뀌어 새 폼 값(`"role": "general", "dept_<키>": "lead"`)으로 |
| 시험의 `department_key_of(db, …)` 단언 (`test_admin_users` 4 · `test_invite` 6 · `test_calendar` 1) | 11 | `conftest.dept_key_of` (키 **하나**, 둘 이상이면 실패) |
| `calendar.build(my_dept_key=)` (`test_calendar`) · `tasklist.build(my_key=)` (`test_stage3·4·13`) | 18 | `my_dept_keys={…}` · `my_keys=…` |
| 남의 부서 행을 보는 시험 (`test_stage14·15·22`) | 4 | 기본이 내 부서라 `?dept=all` 을 붙였다 |
| 옛 역할 값을 단언하던 자리 (`test_admin_users` 5 · `test_notify` 3 · `test_stage5` 1 · `test_stage13` 1 · `test_stage22` 1 · `test_shell` 1) | 12 | `perm.is_lead_of` · `dept_key_of` · 「내 부서」 값 등으로 |
| `tests/conftest.py` `sample_retreat` 의 부서 셋 | 1 | 키가 없었다 — `chongmu`·`hongbo`·`chanyang` |
| **합** | **78** | |

**다시 센 총수 — 212 + 78 = 290.** 그중 고쳤다 255 (66 + 54 + 13 + 44 + 78) ·
안 고쳐도 된다 35 (b 6 + d 29). 빈 칸은 없습니다. (처음 적은 291 · 257 · 34 는
줄을 더한 값과 안 맞았습니다 — 검토자가 잡았습니다.)

## 0-(b) 운영 `users` 의 행 종류

읽기 전용으로 열었습니다 (`sqlite 3.50.4`). 옮기기 **전**:

| id | role | department_id | 가리키는 부서 | 활성 |
|---|---|---|---|---|
| 1 | `admin` | 1 | `chongmuM` · 회차 1 | 1 |

한 종류뿐입니다 — `role=admin · 부서 있음 · 활성`. 옮긴 뒤는 7장.

## 0-(c) `anonymize.py` · 개발 DB 만들기가 표를 읽는 자리

- `scripts/anonymize.py` — **표를 읽지 않습니다.** 파일(JSON·MD·PNG 목록)만
  대응표로 바꿉니다. `users` 도 `user_departments` 도 모릅니다 → 손댈 것 없음
- 개발 DB — `scripts/devserve.bat` 가 `%TEMP%\dcb-dev\app.db` 가 없으면
  `seed.py` 를 돌리고, 있으면 `scripts/check_dev_db.py` 로 실명 게이트를 지납니다.
  `seed.py`(데모 계정)와 `seed_library.py`(추가 계정)가 `User(department_id=)`
  로 만들고 있었습니다 → `perm.assign` 으로. `check_dev_db.제외칸` 은 글자 칸
  전부를 훑으므로 새 표의 글자 칸 둘(`user_departments.dept_role` ·
  `site_settings.key`)을 근거(파일:줄)와 함께 뺐습니다 — `test10_e01` 이 근거
  자리를 잽니다
- 이번 판은 개발 DB 를 지우지 않고 **`scripts/부서옮기기.py --실행` 으로
  옮겼습니다**(운영과 같은 길을 먼저 밟아 보려고) — 소속 줄 18개, 칸 지움.
  드로어 점검용으로 두 부서 계정 하나를 그 DB 에만 더했습니다

# 1. 표

## 1-1. `user_departments`

`app/models.py` — `UserDepartment(user_id · department_id · dept_role)`,
`UniqueConstraint(user_id, department_id)`, 둘 다 `ON DELETE CASCADE`.
`User.department_id` · `User.department` 는 **없습니다**(`test25_g01`).
`User.departments` 관계 하나. 부서는 `Department.key` 로 견줍니다.

`SiteSetting(key · value)` — 바깥 링크 이름·주소 (3장).

## 1-2. `users.role` 셋 · 파생 함수는 `permissions.py` 에만

| 축 | 값 |
|---|---|
| `users.role` | `admin` · `general` · `viewer` (`ALL_ROLES`) |
| `user_departments.dept_role` | `lead` · `member` (`DEPT_ROLES`) |

`app/domain/permissions.py` 를 다시 썼습니다. **`user_departments` 를 읽고
쓰는 곳은 이 파일뿐입니다** — `test25_f01` 이 `app/`·`scripts/`·템플릿을
훑어 밖에서 읽는 줄이 0 인지 재고(주석·문자열은 걷어내고 봅니다 — 10장),
`test25_f02` 가 네 모양을 심어 잡히는지 잽니다.

| 함수 | 무엇 |
|---|---|
| `my_dept_keys(user)` | 내 부서 키 **집합** |
| `lead_keys(user)` · `is_lead_of(user, key)` · `dept_role_of` · `is_member_of` | 부서 역할 |
| `is_readonly(user)` | admin 은 아님 · viewer 는 맞음 · 나머지는 **소속이 없으면** 맞음 (②) |
| `can_edit_department_key(user, key)` | admin 전부 · 아니면 `key in my_dept_keys` |
| `members_of(db, key, dept_role=)` · `leads_of(db, key)` · `admins(db)` | 부서 쪽에서 사람 찾기 — 모든 회차의 같은 키 부서 행 |
| `assign` · `unassign` · `set_memberships(db, user, {키: 역할}, among=이번 회차 부서)` · `unassign_keys` | 쓰기 — **같은 키의 다른 회차 부서 행은 같은 소속**이라 쓰기도 키로 맞춘다(있는 줄의 역할을 바꾸고, 뗄 때는 같은 키 줄 전부). 처음에는 행 id 로 찾아 지난 회차 줄을 못 떼면서 뗐다고 적었다 — 검토자가 잡았고 `test25_h01` 이 잰다 |
| `describe(user)` · `roles_by_key` · `membership_names` · `first_department` · `eager_load` | 화면이 받아 쓰는 것 |
| `LEGACY_ROLE_MAP` | 옛 `dept_lead`/`member` → (`general`, `lead`/`member`) — 옮기기와 시험 도우미만 쓴다 |

## 1-3. `users.department_id` 를 지우는 스크립트 — `scripts/부서옮기기.py`

- 미리보기가 기본, `--실행` 으로만 옮깁니다
- `계정정리.py` 와 같은 길 — **세션이 실제로 여는 파일**(`engine.url.database`)의
  `VACUUM INTO` 사본을 `data/backups/` 에 먼저 뜨고, 사본이 안 떠지면 아무것도
  안 합니다
- SQLite 는 FK 가 걸린 칸을 `DROP COLUMN` 으로 못 지우므로 **표를 다시 만듭니다**
  (문서의 12단계). **트랜잭션 밖의 날 연결**에서 `PRAGMA foreign_keys=OFF` 를
  세운 뒤 BEGIN 합니다 — 세션 안에서 하면 PRAGMA 가 아무 일도 안 하고, 켜진
  채로 `DROP TABLE users` 를 하면 `ON DELETE CASCADE` 가 돌아 **알림·토큰·소속
  줄이 통째로 사라집니다**(아무 오류도 없이). 시험(`test25_e01`)이 옮긴 뒤
  알림 수가 그대로인지 잽니다
- `ix_users_phone_held` 를 다시 만들고 `foreign_key_check` 가 비어야 COMMIT
- 활동 기록 `부서_옮김` 한 줄

## 1-4. 문자열 `"admin"` 비교

`permissions.py` 밖의 `"admin"` 문자열 비교는 **0** 입니다 — `board.py` ·
`reviews.py` 셋 · `no_retreat.html` · `create_admin.py` 다섯 · `healthcheck.py` ·
`merge_users.py` 가 `perm.is_admin` / `perm.ADMIN` / `perm.admins` 로.
(`관리자링크.py` · `계정정리.py` 는 이미 상수였습니다.)

# 2. 자리별 — 사람이 정한 다섯

| | 정한 것 | 어디 |
|---|---|---|
| ① | admin 도 부서 줄을 가질 수 있다 | `set_memberships` 는 역할을 안 본다. 봉사팀 보기 기본(`live._staff_default`)은 admin 이면 아니다 — 총무팀은 총무팀 일정이 기본 |
| ② | 소속 없는 일반은 열람 전용 | `perm.is_readonly` · `test25_c01` (편집 화면 403, admin 은 부서 줄이 있어도 없어도 된다) |
| ③ | `/draft` 기본 칸은 리더인 부서 첫째, 둘 이상이면 고른다 | `drafts._default_key` · `draft_pick.html` |
| ④ | 드롭다운 기본은 내 부서 전부 | 보드·목록·달력이 값 `depts` (`deptpick.html` 의 「내 부서」 · `data-mykeys`) · `board.js` 가 그 값을 안다 · `test25_a02`. 옛 `?scope=mine` · 달력의 옛 `dept` 도 `depts` 로 |
| ⑤ | 리더가 둘이면 둘 다 | `notify.recipients_for(db, run) -> list` — 담당자 → 리더 전부 → 총무팀 · `test25_b01` |

그 밖에 — 확인 요청의 「받은 것」 은 내 부서 **중 하나**에 온 것, 답하기는
그 부서 소속이면 됨(`reviews.py`) · 담당자 후보는 그 부서 소속 전원 + 총무팀
· 담당팀을 옮기면 담당자의 소속에 새 부서가 없을 때만 비움 · 엑셀·폼의 계좌
경계는 그대로(`can_see_account`) · 초대 발급은 손대지 않음(계정에 붙는다).

# 3. 개기자 관리자 주소 — 설정 「바깥 링크」

- `app/routers/settings.py` — `external_link(db)` 가 `site_settings` 의
  `external_link_name` · `external_link_url` 을 읽고, `POST /settings/external-link`
  (admin 만 · http/https 만)가 씁니다. 활동 기록에는 「바깥_링크_변경」 만 남기고
  **주소는 안 적습니다**
- `templating.render` 가 `external_link` 를 넘기고 `retreat_base.html` 이 값이
  있을 때만 사이드바 끝(설정 밑)에 `target="_blank" rel="noopener noreferrer"`
  항목을 그립니다. 운영 DB 의 `site_settings` 는 0행 — **사람이 넣습니다**
- `docs/인계.md` 8번에서 주소를 뺐습니다 → 「설정 › 바깥 링크에 사람이 넣는다」.
  git log 의 옛 판에는 남아 있다고 적어 뒀습니다
- **검사는 저장소 밖에서 잡습니다** — 처음에는 `docs/옛말.md` 에 그 주소를
  낡은 표기로 적었는데, 검토자가 짚은 대로 **그 목록이 공개 저장소로 나가므로
  주소가 저장소에 남습니다**(검사가 금지하는 것을 검사가 담는 10장의 모양).
  그래서 옛말 항목을 걷고 대응표(`data/anonymize-map.json` · gitignore)의
  장소 칸에 넣어 `check_names` 가 저장소 전체를 보게 했습니다 — 지시문의
  「check_names 목록」 이 바로 그 표입니다. 봐둘것 AM-d 에 한계(대응표가
  없는 컴퓨터)를 적었습니다
- **심어서 잡히는지** — CLAUDE.md 끝에 한 줄을 심어 `check_names` 를
  돌렸습니다 (주소는 이 보고에 안 적습니다):

```
(심은 뒤)   exit=1   !! 2곳에서 실명이 보입니다 (이름은 가려 찍습니다).  CLAUDE.md · docs/review/최근.md
(걷은 뒤)   exit=0   걸린 것 없음 · 아직 안 고친 곳 0곳 · 대응표에 없는 담당자 표기 0개
```

  둘째 자리는 이 보고 자신이었습니다 — 처음 판이 심어 본 출력을 주소째
  인용하고 있었고, 검토자가 그것도 잡았습니다. 지금은 `git grep` 으로
  저장소 전체 **0** 입니다(인계.md 포함).

# 4. 시험 — `tests/test_stage25.py` (9) · `tests/test_permissions.py` (11)

| 기준 | 시험 |
|---|---|
| a. 두 부서 사람이 둘 다 고치고 셋째는 403 | `test25_a01` · `test_두_부서_사람은_두_부서를_고치고_셋째는_못_고친다` |
| b. 리더 둘 다 알림 | `test25_b01` |
| c. 소속 없는 일반 403 · admin 은 부서 줄 유무와 무관 | `test25_c01` · `test_소속_없는_일반은_읽기_전용이다` · `test_admin은_소속이_있어도_없어도_읽기_전용이_아니다` |
| d. `make_user` 가 부서 여럿, 옛 부르기도 통과 | `test25_d01` |
| e. 옮기기 — 세 계정 → 소속 줄 3 · 칸 없음 · 사본 있음 · 알림 그대로 | `test25_e01` |
| 수용 2 (표 읽기 0줄 · 심으면 잡힘) | `test25_f01` · `test25_f02` |
| 수용 3 (`department_id` 0번) | `test25_g01` |
| 검토자가 짚은 셋 — 같은 키 두 회차의 떼기·역할 바꾸기 · 업무 추가의 담당 부서 기본값 · 부서 줄 있는 admin 의 받은 요청 | `test25_h01` · `h02` · `h03` |

# 5. 문서

- CLAUDE.md — 3장 사이드바에 바깥 링크 · 4-11 「리더 전부」 · **4-12 끝에
  「부서 여럿 · 역할 둘」 절**(두 축 표 · 세 파생 함수 · 다섯 결정 · 지난 부서
  떼기 · 키 발급) · 4-13 범위 기본 「내 부서」 · 4-14 기본값과 `all` ·
  4-17 표에 바깥 링크 · 8장 `User`/`UserDepartment`/`SiteSetting` · 14장 네 줄
- `docs/옛말.md` — 「소속은 user_departments 의 줄 여럿」 · 「바깥 주소는
  설정에」. 넘김 셋(얼린 도막 3 · 옮기기 스크립트 · `test_stage25`)
- `docs/인계.md` — 갱신 도막 4 · 3번을 끝난 것으로 · 8번 갱신 · 「사람이 챙길
  재료」 에 주소 넣기
- `docs/봐둘것.md` — AM-a(지난 회차 소속은 켜서만 뗀다) · AM-b(이름 검사의 구멍)

# 6. 수용 기준

| # | 기준 | 결과 |
|---|---|---|
| 1 | 점검표 합이 총수와 같고 빈 칸 없음 | **통과** — 212 + 새로 찾은 78 = 290 · 고쳤다 255 · 안 고쳐도 된다 35 |
| 2 | `user_departments` 읽는 줄 0 (permissions 밖) · 심으면 잡힘 | **통과** — `test25_f01` · `f02` |
| 3 | `users.department_id` 0번 (모델·DB·템플릿·시험) | **통과** — `test25_g01` |
| 4 | 운영 DB 에 실제로 돌림 · 사본 · 소속 줄 수 · users 1 | **통과** — 7장 |
| 5 | 4-a~e 양쪽 통과 | **통과** — 4장 |
| 6 | 저장소 전체 그 주소 0 · 심은 검사 결과 | **통과** — 3장. `git grep` 0 (검사 목록은 저장소 밖 대응표에) |
| 7 | 덩어리·최근.md 에 실명·전화·계좌·토큰·바깥 주소 없음 | **통과** — 사본 파일명은 경로만, 링크는 안 실었다. 첫 판의 이 보고가 주소를 인용하고 있던 것을 검토자가 잡아 걷었다 |
| 8 | pytest 전부 · 글검사 4/4 · drawer.js 실패 0 | **통과** — 7장 |
| 9 | 검토자 원문 그대로 | 8장 |

# 7. 잰 것

## 운영 DB 옮김 (서버를 끄고 → 옮기고 → 켰다)

| 무엇 | 값 |
|---|---|
| 사본 | `data/backups/app-20260910-092200.db` · 610,304 바이트 · 2026-09-10 09:22:00 |
| 옮긴 계정 | 1 (`admin` · 부서 있음) → `admin` · `chongmuM` `member` |
| `user_departments` | **1행** |
| `users` | **1** (id 1 · admin · 활성) |
| `users` 칸 | `department_id` 없음 (`id · phone_number · retired_phone · name · role · bank_account · is_active · created_at · first_seen_at`) |
| `ix_users_phone_held` | 있음 |
| `foreign_key_check` | 빈 목록 |
| `notifications` · `invite_tokens` | 380 · 18 — 옮기기 전과 같다 (CASCADE 가 안 돌았다) |
| `site_settings` | 0행 |
| 활동 기록 | `부서_옮김` 1행 |

## 서버 (11-3 2단계)

| 무엇 | 값 |
|---|---|
| 멈추기 전 PID | 26680 |
| 다시 켠 뒤 PID | **24416** (성공 — PID 가 바뀌었습니다 · 09:22:03) |
| `GET /` | 303 → `/login` |
| `GET /login` | 200 |
| `GET /board` (로그인 없이) | 303 → `/login` |
| 화면이 물어 온 CSS | `retreat.bdf527e8.css` (CSS 는 안 고쳐 같은 값) |
| `data/server.log` | 오류 없음 (startup complete · 위 요청 넷) |

새 코드로 떠서 옮긴 DB 를 읽습니다 — 옛 코드였으면 `users.department_id` 가
없어 첫 요청에서 500 이 났을 자리입니다.

**검토 뒤에 한 번 더 켰습니다** — 검토자 지적으로 `app/` 이 다시 바뀌었으므로
(permissions·board·reviews·admin_users·checklists) 커밋한 코드로 다시 떠야
「돌고 있는 서버에서 확인」 입니다. 24416 → **25864** (성공 — PID 가 바뀌었습니다 ·
10:13:33) · `GET /login` 200. 얼린 판(`2026-09-10-도막4.md`)은 이 줄이 없는
채로 얼렸습니다 — 얼린 뒤에 일어난 일이라 그 파일은 안 고칩니다.

## `docs/checks/drawer.js` — 세 화면 × 세 계정

개발 서버(8001 · 가명 자료 · 위에서 옮긴 DB)에서, 해시 자리를 매번 바꾸고
`{cache:'reload'}` 로 받아 길이(59,095자)를 매번 견줬습니다. 창 1440×900.

| 화면 | 관리자 (소속 없음) | 부서 리더 (헤브론) | **두 부서 리더** (스케치 · 헤브론) |
|---|---|---|---|
| 보드 | 79/79 · 건너뜀 2 | 62/62 · 건너뜀 15 | 62/62 · 건너뜀 15 (`data-mykeys="hebron,sketch"` · 기본 「내 부서」) |
| 목록 (기본 = 내 부서) | 92/92 · 건너뜀 4 | 92/92 · 건너뜀 4 | 92/92 · 건너뜀 4 (두 부서 칩이 같이 뜬다) |
| 목록 `?dept=all` | — | 95/95 · 건너뜀 3 | 95/95 · 건너뜀 3 |
| 달력 | 82/82 · 건너뜀 3 (`?scope=all&month=2026-08`) | 65/65 · 건너뜀 16 (`?month=2026-08`) | 82/82 · 건너뜀 3 (`?scope=depts&month=2026-08`) |

열 판 모두 **실패 0 · 못 쟀음 0 · 완주 참.** 소속 없는 관리자의 달력 기본은
「내 것」 이라 담당 업무가 없어 점이 없었고(「열어 볼 업무가 없습니다」 로
스크립트가 멈춤), 범위를 전체로 두고 업무가 있는 달로 가서 돌렸습니다.
목록은 **기본이 내 부서라 남의 행이 안 보여** 「못 바꾸는 칸」 이 안 재지므로
리더 둘에게는 `?dept=all` 로 한 번 더 돌렸습니다(그 판에서 재집니다).
자가시험은 안 돌렸습니다 — 점검 스크립트를 안 고쳤습니다.

## pytest (옵션 없이)

```
1292 passed, 3 warnings in 738.67s (0:12:18)
```

**검토자 원문을 실은 뒤에 돌린 값입니다** (11-3). 검토 전 판은 1289 였고, 검토가 짚은 자리에 시험 셋(`test25_h01`~`h03`)을 더해 1292 입니다.

## 글검사 (`scripts/글검사.py`)

```
━━━ 모아서 ━━━
  통과  check_names (exit 0)
  통과  check_stale (exit 0)
  통과  check_named (exit 0)
  통과  check_counts (exit 0)

글 검사가 전부 통과했습니다.
```

**네 번 빨갰습니다** — ① 새 파일 셋(`draft_pick.html` · `부서옮기기.py` ·
`test_stage25.py`)이 `git ls-files` 에 없어 `check_named` 가 「문서에만 있는
이름」 으로 잡았다(도막 3 과 같은 자리 — `git add` 로 풀림). ② 옛말 표기를
넓게 적었더니(`role="dept_lead"` · `department_key": `) 시험의 옛 부르기와
업무의 `department_key` 까지 잡아 표기 둘을 뺐다. ③ 스크립트 셋의 `"admin"`
비교를 바꾸는 패치가 import 줄 둘을 망가뜨려 `healthcheck.py` 가 파싱이 안
됐다 — `test25_f01` 의 주석 걷어내기(`tokenize`)가 `IndentationError` 로
그것을 먼저 잡았다. ④ 검토 뒤 — 이 보고와 `docs/옛말.md` 가 바깥 주소를
글자로 담고 있어 `check_names`(대응표로 옮긴 뒤)가 둘을 잡았다. 넷 다 고친
뒤의 값이 위 것입니다.

# 8. 판단이 들어간 자리

1. **목록 「부서 전체」 값 `all`** — 기본이 내 부서가 되면서 빈 값이 「전체」
   와 「기본」 두 뜻이 됐고 시험 셋이 그것을 잡았습니다. 보드·달력이 이미
   `all` 을 쓰고 있어 같은 값으로.
2. **지난 회차 소속은 「켜면 뗀다」 로만** — 폼에 없는 소속을 빈 값으로 지우면
   권한만 고쳤는데 소속이 조용히 사라집니다(배포 5b). 옛 시험 05d 「빈 값으로
   뗀다」 는 그 규칙과 부딪혀, 켜서 떼는 것으로 다시 썼습니다(봐둘것 AM-a).
3. **없는 부서를 붙이려 하면 400 이 아니라 말로** — 옛 시험이 「이번 회차에
   없는 부서라 배정할 수 없습니다」 문구를 잰다. 화면의 선택지에는 없는 키라
   손으로 만든 요청뿐인데, 그래도 아무 설명 없는 400 보다 낫다고 봤습니다.
   회차가 없을 때는 「아직 회차가 없어 …」.
4. **`f01` 스캐너의 범위** — `.departments` 는 회차·초안·모듈 이름에도 쓰여
   처음 정규식이 21 줄을 잡았습니다. 주어가 사람을 뜻하는 이름일 때만 잡고,
   `.py` 는 주석·문자열을 `tokenize` 로 걷어내고 봅니다(10장). 새 이름으로
   사람을 부르면 목록에 더해야 합니다 — 그 한계는 시험 주석에.
5. **k05 허용 셋** — `permissions.py` 안의 `Department.id == UserDepartment.department_id`
   (조인) · `m.department_id == department.id` (assign·unassign 의 같은 행
   찾기, 두 번) 은 소속 표 안의 행 맞춤이라 (파일, 식) 으로 허용에 넣었고,
   같은 식이 두 번인 것은 `같은식_둘` 로 따로 뒀습니다. 사람 소속을 id 로
   되돌리던 다섯 줄(permissions·notifications·reviews 둘·settings)은 사라져
   목록에서 뺐습니다.
6. **소속 없는 관리자의 달력 기본은 「내 것」** — ④ 는 「내 부서」 인데 부서가
   없으면 갈 곳이 없어 옛 기본으로. 보드·목록은 `all`.
7. **`check_named` 가 없는 이름을 못 잡았습니다** — CLAUDE.md 14장이
   `department_key_of` 를 가리킨 채로 초록이었습니다(봐둘것 AM-b). 옛말 목록에
   그 이름을 적어 `check_stale` 이 잡게 뒀습니다 — 목록에 안 기대는 축이
   목록에 기대게 된 자리라 미룬 것으로 적었습니다.
8-a. **같은 키 = 같은 소속, 쓰기도** — 검토자의 probe 로 `set_memberships`
   가 지난 회차 행의 소속을 못 떼면서 「뗌」 을 돌려주던 것을 확인했습니다.
   모델 독스트링과 `members_of` 가 이미 「같은 키의 다른 회차 행도 같은
   소속」 으로 보고 있었으므로 쓰기(`assign` · `unassign` · `set_memberships`)
   도 그쪽으로 맞췄습니다 — 반대로(이번 회차 행에 다시 붙여야 소속)
   가면 회차를 열 때마다 사람 소속을 다시 붙여야 합니다. `dept_role_of` 는
   줄이 둘이면 리더가 이겨 `lead_keys` 와 같은 답을 냅니다.
8-b. **admin 은 부서 줄이 있어도 받은 요청을 전부** — ① 「권한은 admin 이
   이긴다」 에 맞췄습니다(`can_respond_to` 는 이미 전부였고 세는 쪽만
   부서로 좁혀 있었습니다). 「총무팀도 자기 부서 것부터 보고 싶다」 는
   요구가 있으면 그때 칩으로 가릅니다.
8-c. **담당자도 리더도 없는 업무의 알림은 활성 총무팀 전원** — 전에는
   `.first()` 한 사람이었습니다. 4-12 「에스컬레이션은 admin 전원」 과
   맞춘 것이지만 ⑤ 밖의 바뀐 동작이라 적어 둡니다.
8. **옛 시험 `test_notify.보완02b`** 가 「board 의 도우미가 `department_key_of`
   를 부른다」 를 재고 있었습니다 — 이제 셋(`board._dept_keys_of` ·
   `departments.department_keys_of` · `users_in_department`) 이 전부
   `permissions` 의 그 함수를 부르는지로.

# 9. 바뀐 파일 (`git diff --name-only 5190b52..HEAD` — 이 보고를 담는 커밋 앞까지)

```
CLAUDE.md
app/domain/calendar.py
app/domain/departments.py
app/domain/home.py
app/domain/notify.py
app/domain/permissions.py
app/domain/tasklist.py
app/main.py
app/models.py
app/notifications.py
app/routers/admin_users.py
app/routers/attachments.py
app/routers/board.py
app/routers/calendar.py
app/routers/drafts.py
app/routers/expenses.py
app/routers/export.py
app/routers/live.py
app/routers/meetings.py
app/routers/notifications.py
app/routers/reviews.py
app/routers/settings.py
app/routers/setup.py
app/routers/tasks.py
app/security.py
app/static/js/board.js
app/templates/admin_users.html
app/templates/board.html
app/templates/calendar.html
app/templates/checklists.html
app/templates/draft_pick.html
app/templates/expenses.html
app/templates/home.html
app/templates/no_retreat.html
app/templates/partials/deptpick.html
app/templates/retreat_base.html
app/templates/settings.html
app/templates/tasks.html
app/templating.py
docs/봐둘것.md
docs/옛말-넘김.txt
docs/옛말.md
docs/인계.md
scripts/check_dev_db.py
scripts/create_admin.py
scripts/healthcheck.py
scripts/merge_users.py
scripts/부서옮기기.py
seed.py
seed_library.py
tests/conftest.py
tests/test_admin_users.py
tests/test_attachments.py
tests/test_board_web.py
tests/test_calendar.py
tests/test_draft_flow.py
tests/test_home.py
tests/test_invite.py
tests/test_live.py
tests/test_notifications.py
tests/test_notify.py
tests/test_permissions.py
tests/test_prerequisites.py
tests/test_shell.py
tests/test_staff_sheet.py
tests/test_stage13.py
tests/test_stage14.py
tests/test_stage15.py
tests/test_stage22.py
tests/test_stage23.py
tests/test_stage25.py
tests/test_stage3.py
tests/test_stage4.py
tests/test_stage5.py
tests/test_stage6.py
tests/test_web_flows.py
```

# 10. 검토자 원문 (그대로)

```
━━━ 검토자 여기부터 ━━━ 범위 d8a4bcb..워킹 트리(미커밋 전부) · 커밋 0개 · 파일 77개 (수정 74 · 새 파일 3)

잰 것
  pytest (옵션 없이)      1289 passed, 3 warnings in 679.49s
  scripts/글검사.py       check_names · check_stale · check_named · check_counts 넷 다 exit 0
  스크래치 probe          아래 2번 — 메모리 SQLite 에 회차 둘·같은 키 부서 둘·사람 하나로 돌렸다

────────────────────────────────────────────
막아야 함 (커밋 전에 고쳐야 함)
────────────────────────────────────────────

1. 바깥 주소가 저장소에 그대로 남는다 — 이번 판의 목적이 그것을 빼는 것이었다
   docs/옛말.md:283          「낡은 표기」 칸에 그 호스트가 글자 그대로 적혀 있다
   docs/review/최근.md:258   check_stale 을 심어 본 출력을 인용하면서 「…」 안에 호스트가 그대로 들어 있다
   - 보고 3장 「저장소 전체에서 그 주소는 옛말.md 한 줄뿐」 은 틀렸다 — 최근.md 258줄에도 있다.
     수용 기준 6(저장소 전체 0)·7(최근.md 에 바깥 주소 없음) 둘 다 「통과」 로 적혔는데 둘 다 아니다
   - 옛말.md 는 git ls-files 안이고 공개 저장소로 나간다. 검사의 needle 로 주소를 적어 두면
     「이 도구가 금지하는 것을 설명하려고 그것을 담는」 10장의 그 모양이다 — CLAUDE.md 4-17 이
     「DB 에만 있고 저장소에는 없다」 고 이번 판에서 적은 것과 같은 파일이 어긋난다(검토-원칙 4)
   - 최근.md 는 얼려서 docs/review/2026-09-10-도막4.md 로 남을 파일이라 한 번 들어가면 못 고친다

2. 같은 키의 지난 회차 소속 줄은 「떼기」 도 「역할 바꾸기」 도 안 되고, 활동 기록은 됐다고 적는다
   app/domain/permissions.py:281 · 292      assign · unassign 이 m.department_id == department.id 로 행을 찾는다
   app/domain/permissions.py:321-333         set_memberships 의 now 는 키로 모으고(지난 회차 줄 포함),
                                             unassign 에는 이번 회차 행을 넘긴다 → 못 찾고 False, 그래도 뗌.append(key)
   app/domain/permissions.py:126-137         dept_role_of 는 첫 줄, lead_keys 는 아무 줄 — 같은 사람에 답이 갈린다
   - probe (회차 old·new, 둘 다 key=hebron, 사람은 old 의 hebron 리더):
       set_memberships(db, u, {}, among=[new_hebron])          → ([], ['hebron'])   뗐다고 돌려준다
       그 뒤 my_dept_keys(u)                                    → {'hebron'}         그대로 붙어 있다
       set_memberships(db, u, {'hebron': 'member'}, among=[new]) → (['hebron:member'], [])  줄이 둘이 된다
       dept_role_of → 'lead' · is_lead_of → True · lead_keys → {'hebron'} · roles_by_key → {'hebron': 'member'}
   - 결과: 설정 › 사용자 화면(roles_by_key)은 「팀원」 을 보여 주고, 알림(leads_of 는 두 줄 다 봄)·
     /draft(lead_keys)는 리더로 대한다. 활동 기록에 「소속 -hebron」 이 남는데 사람은 그대로 hebron 편집 가능.
     0장의 「누가 놓쳐도 시스템이 알아차리는가」 와 반대로, 틀린 채로 조용하다
   - 지금 운영은 회차 하나·계정 하나라 안 밟히지만, 인계.md 9번(2027 회차 생성)이 다음 일이고
     회차를 만들면 같은 키 부서 행이 하나 더 생겨 곧바로 밟힌다. models.py 의 UserDepartment 독스트링이
     「같은 키의 다른 회차 부서도 같은 소속으로 본다」 고 적었으므로 쓰기 쪽도 키로 맞춰야 한다
     (now 의 have 줄을 그대로 unassign 하거나 have.dept_role 을 고치는 쪽). 시험은 test_invite 배포05d 가
     이번 회차에 없는 키(bongsa)만 재고, 같은 키가 두 회차에 있는 경우는 없다 — 막는 코드에 그 모양의 시험이 없다(11-3)

────────────────────────────────────────────
고쳐야 함 (커밋은 되지만 고쳐야 함)
────────────────────────────────────────────

3. 업무 추가 화면의 「담당 부서」 기본 선택이 조용히 사라졌다
   app/routers/board.py:658      "my_department_key": _pick_default(user)   → 이제 값이 'depts' 또는 'all'
   app/templates/board_add.html:54   {% if d.key == my_department_key %}selected  → 어느 부서 키와도 안 맞는다
   - 전에는 부서 리더의 부서가 골라져 있었다. 같은 렌더에 my_department_keys 도 넘기지만 이 템플릿은 안 쓴다.
     드로어와 달리 시험이 이 자리를 안 잰다(tests 에서 board/add 의 selected 를 재는 곳 0)

4. 체크리스트 만들기의 부서 기본값이 「내 부서가 목록 첫째일 때」 만 잡힌다
   app/templates/checklists.html:23   {% if d.key and d.key in 내_부서키(user) and loop.first %}selected
   - loop.first 는 「부서 목록의 첫째 항목」 이지 「내 부서 중 첫째」 가 아니다. 헤브론만 소속인 사람은
     헤브론이 목록 첫째가 아니므로 아무것도 안 골라진다. 보고 0-(d) 는 이 줄을 「고쳤다」 로 적었다

5. admin 이 부서 줄을 가지면 확인 요청 「받은 것」 이 자기 부서 것만 된다 — ①(권한은 admin 이 이긴다)과 어긋난다
   app/routers/reviews.py:55-63   keys 가 비지 않으면 admin 여부와 무관하게 Department.key.in_(keys) 로 거른다
   app/routers/reviews.py:75-79   can_respond_to 는 admin 이면 전부 True
   - 배지(templating.py:219)·「답을 기다리는 것」 칩(notifications.py:88)은 pending_for_user, 답 단추는 can_respond_to →
     admin 이 부서 줄을 가지면 셀 때와 답할 때가 갈린다. 보고 7장대로 운영의 유일한 admin 이 지금
     정확히 그 상태(chongmuM member)다. 옛 코드도 같은 모양이었지만 그때는 admin 에 부서를 붙일 이유가 없었다

6. 보고 0-(a) 점검표의 셈이 안 맞는다 (수용 기준 1 이 「합이 총수와 같다」 를 요구한다)
   docs/review/최근.md:113   (d) 「고쳤다 14 · 안 고쳐도 된다 28」 → 줄을 더하면 고쳤다 13 · 안 고쳐도 된다 29
                             (12+1+4+1+3+1+2+1+2+1+1 = 29)
   docs/review/최근.md:140   「새로 찾은 자리 합 79」 → 줄을 더하면 78 (1+2+2+1+1+25+11+18+4+12+1)
   docs/review/최근.md:142 · 293   따라서 총수 291 → 290, 고쳤다 257 → 255, 안 고쳐도 된다 34 → 35
   - 「안 고쳐도 된다」 의 이유는 성립한다 — 확인함: budget.html 의 is_admin(user) 는 전역이 user 를 받게
     바뀌었고, live.py 의 can_manage_retreat(user.role) 은 _role_of 가 문자열을 받는다, checklists·expenses 의
     can_edit_dept 는 서명이 그대로다, draft_none.html:8 은 is_admin(user) 꼴이다

7. CLAUDE.md 14장의 새 줄이 코드와 다르다
   CLAUDE.md:3421   「부서를 키로 찾기 | departments.py (department_keys_of · users_in_department · next_team_key) — 셋 다 permissions 를 부른다」
   app/domain/departments.py:86-106   next_team_key 는 Department 표를 select 할 뿐 permissions 를 부르지 않는다 → 「둘」 이 맞다

8. test25_f01 스캐너가 놓치는 모양 (물음 1)
   tests/test_stage25.py:187-205   _코드만 이 STRING 토큰을 전부 빈칸으로 만든다
     → db.execute(text("select … from user_departments")) 같은 날 SQL 은 안 잡힌다. 이 저장소는 날 SQL 을 실제로 쓴다
       (app/db.py:173 의 users, scripts/부서옮기기.py:63). getattr(user, "departments") 도 같은 이유로 안 잡힌다
     → test25_f02 는 select(user_departments) 를 코드로 심으므로 이 모양은 재 본 적이 없다(검토-원칙 2-③)
   tests/test_stage25.py:220-222   _볼파일 이 app/·scripts/·app/templates 만 본다
     → 루트의 seed.py · seed_library.py 는 밖이다(지금은 perm.assign 을 쓰지만 검사가 지키지 않는다)
   tests/test_stage25.py:181-184   주어 목록(user·u·person·…·owner) 밖 이름은 안 잡힌다
     → sender · requester · boss · author · self · row · usr 등. 보고 8-4 가 한계로 적었으니 새 종류는 아니지만,
       .departments 뒤에 (?!\s*=) 만 두고 앞을 목록으로 좁힌 것이라 「이름을 새로 지으면 통과」 가 남는다

────────────────────────────────────────────
봐 둘 것 (판단이 갈릴 수 있어 사람이 볼 것)
────────────────────────────────────────────

9. scripts/부서옮기기.py — 순서(사본 → create_all → 소속 줄 → 닫고 → 날 연결 재생성)에 구멍은 못 찾았다.
   FK OFF 를 트랜잭션 밖에서 세우고 BEGIN 하는 것, DROP 뒤 RENAME, foreign_key_check 뒤 COMMIT 은 SQLite 문서
   절차 그대로이고 test25_e01 이 CASCADE 가 안 돈 것을 잰다. 다만 셋은 적어 둔다
   - 69-80줄 미리보기 vs 141-149줄 실행: department_id 가 있는데 그 부서 행이 없으면(dangling) 미리보기는
     「general · lead」 로 찍고 실행은 소속 없이 general 로 두어 열람 전용이 된다. n 에도 안 세고 말도 없다
   - 150-154줄: 소속 줄을 commit 한 뒤 표 재생성이 실패하면 「칸도 있고 줄도 있는」 절반 상태로 남는다.
     재실행이 멱등(assign 이 있는 줄을 갱신)이라 복구되지만 보고 1-3 에는 그 말이 없다
   - 109줄 assert 로 PRAGMA 를 확인한다 — python -O 에서는 사라진다. 운영은 -O 를 안 쓰니 지금은 문제 아님

10. app/routers/admin_users.py:111   배정불가(…, no_retreat=not choices)
    회차는 있는데 키 있는 부서가 0 이면 「아직 회차가 없어 …」 로 말한다 — 틀린 안내. 지금은 키 없는 부서가
    안 생기므로(settings.py:377) 밟힐 일이 거의 없다

11. CLAUDE.md:920-923   「다른 파일은 이 셋(과 members_of · admins · assign · set_memberships)을 부릅니다」
    실제로 밖에서 부르는 이름이 더 있다 — lead_keys(drafts·expenses) · roles_by_key · membership_names · describe ·
    first_department(notifications) · unassign_keys · dept_role_of · eager_load(main) · is_readonly · can_edit_department_key.
    10장 「자리를 세어 두지 않습니다」 의 그 모양 — 이름을 세지 말고 「그 파일의 함수」 라고만 적는 편이 안 낡는다

12. app/routers/drafts.py:31-39   _default_key — 리더인 데가 없고 팀원으로만 두 부서면 고르는 화면 없이 가나다 첫째.
    문서 ③은 「리더인 부서가 둘 이상이면 고른다」 만 말한다. 그리고 소속 없는 admin 은 /draft 가 400
    (「소속 부서가 없어 고를 수 없습니다. 총무팀에 문의해주세요」 — 총무팀 본인에게). 둘 다 옛 동작과 같아 새로
    생긴 것은 아니지만 draft_pick.html 이 생긴 김에 볼 자리

13. tests/test_stage6.py:186   같은식_둘 — 이름은 「둘」 인데 n > 1 을 전부 넘긴다(셋이 돼도 초록). 지금 코드는 정확히 둘

14. tests/test_stage25.py:114   assert "/expenses/create" not in page or "+ 지출 등록" not in page
    or 라서 한쪽만 없어도 통과한다 — 등록 단추가 남아도 다른 문자열이 없으면 초록

15. docs/인계.md:111   docs/review/2026-09-10-도막4.md 를 가리키는데 그 파일은 아직 없다(11-3 4단계에서 얼릴 때 생긴다).
    순서 문제일 뿐이지만 커밋 시점에 없는 파일을 가리킨다

16. app/domain/notify.py:116   담당자도 리더도 없을 때 perm.admins(db) — 전에는 .first() 한 사람, 지금은 활성 총무팀 전원.
    4-11·4-12 문구(「총무팀으로 올린다」 · 「admin 전원에게 간다」)와는 맞으나 보고 2장 ⑤ 밖의 바뀐 동작이라 적어 둔다.
    같은 파일 250줄의 재촉 쪽은 active_only=False 라 비활성 admin 도 받는다 — 옛 동작 그대로이고 보고 0-(b) 가 밝혔다

17. 문서 대조 — 4-11 · 4-13 · 4-14 · 8장 · 4-17 의 새 문장은 코드와 맞는다(기본값 depts/all, 소속 없으면 mine, 옛 dept·
    scope=mine 옮김, User/UserDepartment/SiteSetting). 옛말 잔재는 permissions.py:10 · CLAUDE.md:907 의 「전에는 …」
    서술뿐이다. 실명·전화·계좌·토큰은 새로 들어간 것을 못 찾았다 — 시험 번호는 0106666… 꼴 자리표시자, 사본 파일명은
    경로만. 바깥 주소만 1번이다

18. 바깥 링크 (물음 7) — href 는 Jinja 자동 이스케이프, 저장 시 http/https 검사, target=_blank rel=noopener noreferrer,
    활동 기록은 이름만(settings.py:490), 보고에는 안 실렸다(1번의 옛 호스트는 별개). templating._external_link 가
    렌더마다 SessionLocal 을 하나 더 연다(_badge_counts 와 같은 모양) — 문제라기보다 알아 둘 것

────────────────────────────────────────────
사람에게 묻는 것
────────────────────────────────────────────

- (1번) 옛말.md 의 needle 로 그 주소를 둘 것인가. 두면 저장소에 남는다. 대신 「인계.md 8번의 재료가 https 로 시작」 같은
  다른 표기로 잡을지, 아니면 needle 없이 사람이 한 번 본 것으로 끝낼지
- (2번) 같은 키의 지난 회차 소속 줄을 「같은 소속」 으로 볼 것인가(모델 독스트링·members_of 는 그렇게 본다). 그렇다면
  set_memberships 가 키로 맞춰야 하고, 아니라면 「이번 회차 행에 다시 붙여야 소속」 이 되어 회차를 열 때마다 사람 소속을
  다시 붙여야 한다 — 어느 쪽인지 사람이 정할 것
- (5번) admin 이 부서 줄을 가질 때 확인 요청 「받은 것」 을 전부로 볼지 자기 부서만 볼지. ①대로면 전부인데,
  「총무팀도 자기 부서 것부터 보고 싶다」 는 요구가 있었는지
- (12번) 팀원으로만 여러 부서인 사람의 /draft 기본을 고르게 할지 첫째로 둘지

━━━ 검토자 여기까지 ━━━
```

## 검토자를 받아 고친 것

| # | 무엇 | 어떻게 |
|---|---|---|
| 1 | 바깥 주소가 `docs/옛말.md` 의 표기와 이 보고의 인용에 남았음 (막음) | 옛말 항목을 걷고 저장소 밖 대응표의 장소 칸으로 옮겼다. 보고의 인용은 주소 없이 다시 적었다. `git grep` 0 — 3장 |
| 2 | 같은 키의 지난 회차 소속 줄을 못 떼면서 뗐다고 적고, 역할을 바꾸면 줄이 둘이 됨 (막음) | `assign`·`unassign`·`set_memberships` 를 키로 맞췄다. `dept_role_of` 는 리더 우선. `test25_h01` 이 검토자의 probe 를 그대로 잰다 — 8-a |
| 3 | 업무 추가 화면의 담당 부서 기본 선택이 사라짐 | `perm.primary_key(user)`(리더인 부서 먼저)를 넘긴다. `test25_h02` |
| 4 | 체크리스트 부서 기본값이 「목록 첫째」 일 때만 잡힘 | 내 부서 **중** 첫째를 고른다 (`checklists.html`) |
| 5 | admin 이 부서 줄을 가지면 받은 요청이 자기 부서 것만 | 전부 본다 — `pending_for_user` · `test25_h03` — 8-b |
| 6 | 0-(a) 의 셈이 줄 합과 안 맞음 (d 13/29 · 새로 찾은 78 · 총 290) | 고쳤다 — 수용 기준 1 도 그 값으로 |
| 7 | 14장 「셋 다 permissions 를 부른다」 — `next_team_key` 는 아님 | 「앞의 둘」 로 |
| 8 | 스캐너가 문자열 안의 날 SQL 과 루트의 seed 파일을 안 봄 | 원문 줄에 `from|join|into|update|table user_departments` 를 따로 보고, 루트 `*.py` 도 본다. `f02` 가 날 SQL 을 심어 잰다. 주어 목록의 한계는 그대로(8-4) |
| 9 | 옮기기 — 끊긴 `department_id` 를 미리보기·실행이 다르게 말함 · 재실행 멱등을 안 적음 | 둘 다 「부서 행 없음 → 소속 없이」 로 말하고 수를 찍는다. 독스트링에 「다시 돌려도 된다」 |
| 10 | 회차는 있는데 부서가 0 이면 「회차가 없어」 로 말함 | `has_retreat` 를 넘겨 회차 유무로 가른다 |
| 11 | 4-12 가 밖에서 부르는 함수 이름을 세어 둠 | 「그 파일의 함수를 부른다」 로 — 이름을 안 센다 |
| 12 | `/draft` 의 팀원만 여럿 · 소속 없는 admin | 그대로 두고 봐둘것 AM-c 에 — 사람이 정할 것 |
| 13 | `같은식_둘` 이 둘 초과도 넘김 | 정확히 그 수만 넘긴다 |
| 14 | `test25_c01` 의 `or` 단언 | 등록 단추 하나로 |
| 15 | 인계.md 가 아직 없는 얼린 파일을 가리킴 | 얼릴 때 생긴다 — 도막 3 과 같은 순서 |
| 16 | 알림의 총무팀이 한 사람 → 전원 | 8-c 에 적어 뒀다(동작은 그대로) |

**사람에게 묻는 것 넷** — ①은 저장소 밖 대응표로 옮겼고(1), ②는 「같은
소속」 으로 정했으며(2), ③은 「전부」 로(5), ④는 봐둘것 AM-c 에 그대로
있습니다.
