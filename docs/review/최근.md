사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: --실행 과 화면 고치기의 순서 · 표시 갈래 · 부서가 다른 자식을 「+ 업무 추가」 · 마법사에서 어떻게 할지 (6장)
막혀서 못 한 것: 없음

작성 시각: 2026-09-17 21:10 (KST)
커밋: e338005 다음 — 이 판의 커밋들(9장 파일 목록)
작업: 계층 관계 채우기 — 미리보기까지

---

**앱은 안 고쳤다** — 더한 것은 전환 스크립트 `scripts/업무계층채우기.py` 와 시험 `tests/test_stage61.py` 와 문서다. `app/` · `templates/` · `static/` 이 없어 **재시작은 건너뛰었다**(운영 PID 19996 그대로).
**운영 DB 는 읽기만 했다** — 미리보기만 두 번 돌렸고(사본도 안 떴다) DB 파일의 수정 시각이 15:20:10 그대로다. **노션은 안 불렀다.**
**값은 싣지 않는다** — 건수만. 제목이 든 짝 표는 저장소 밖 `data/업무계층채우기.real.md`.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | `parent_library_id` 를 읽는 자리 — `board.py`(부서 블록의 트리 · 드로어의 상위 제목) · `library.py`(마법사 목록 · 회차 만들기 · 라이브러리 화면) · `diagnosis.py`(상위 · 하위 근거) · `suggest.py`(목록 글) · `tasks.py`(새 업무) · 라우터의 `routers/board.py`(「+ 업무 추가」 후보 · 넣기) · `routers/library.py`(필수 일괄 지정) — 라우터 둘은 커밋 전 검토가 짚어 더했다. 이 판은 값을 안 채우지만 **채우면 이 자리들이 다 움직인다** — 3장 |
| 같은 부분 템플릿을 쓰는 화면 | 드로어(`partials/drawer.html` · `drawer.js`)의 「상위」 줄이 이미 있다 — 보드 · 달력 · 목록이 같이 쓴다 |
| 그 값을 세어 둔 문서 | 봐둘것 BF-a(갈래 · 수) · 인계 맨 위 · CLAUDE.md 14장 — 셋 다 이번 판 사실로 맞췄다 |
| 얼린 판이 걸리는 검사 | 앞 판 보고를 얼리자 그 안의 검토자 원문 한 줄이 `check_counts` 에 걸렸다 — 원문은 못 고치므로 `docs/세어둠.txt` 에 까닭과 함께 한 줄 더했다(11-3 4단계) |

# 1. 0 · 1단계 — 확인과 계산

- **0a** — 노션을 안 불렀다. 읽은 것은 저장소 밖 tsv(업무속성 · 담당팀)와 운영 DB 의 `related_library_ids` · `default_department_key` 뿐이다. tsv 는 `노션업무들이기.읽는다` 로 읽는다(표지 빼기 · 칸 검사가 한 곳)
- **0b** — `TaskLibrary.parent_library_id` 는 이미 있다(`ondelete=SET NULL` · NULL 허용). 노션에서 들인 233 가운데 상위가 있는 것은 **0** 이다 — 스크립트가 하나라도 있으면 멈추고, 미리보기가 멈추지 않았다
- **1a** — Main 은 노션 업무속성으로 가른다 → **87**. 앱 `kind=main` 은 97 이다(속성 빈 줄 10 이 섞임)
- **1b · 1c · 1d** — 미리보기(운영 · 21:10 앞뒤로 두 번 · 같은 수)

| 항목 | 수 |
|---|---|
| 노션 줄(표지 뺌) | 233 |
| 노션 Main(업무속성) | 87 |
| Main 이 아닌 줄 | 146 |
| **채울 것** | **100** |
| · Main 하나만 가리킴 | 84 |
| · 같은 담당팀 Main 이 하나 | 16 |
| · 채울 것 중 담당 부서가 다른 부모 | 10 |
| **비울 것** | **46** |
| · Main 여럿 · 가를 수 없음 | 11 |
| · Main 을 안 가리킴 | 35 |
| 부모가 Main 이 아닌 것 | 0 |
| 고리 | 0 |

- 앞 판(노션 SQL)의 수와 한 자리도 안 다르다
- **담당 부서가 다른 10** 은 앱 부서 키로 견준 수다. 운영 회차 1 의 run 으로 보면 그 10 중 **9 는 자식에게 부서가 있고 1 은 없다**
- **같은 담당팀**은 노션 담당팀 글자로 견준다(부서 키가 아니다) — 「2 봉사팀 공통」 과 「0」 이 앱에서는 둘 다 빈 키라, 키로 견주면 둘이 같은 팀으로 잡힐 수 있다(운영에서는 어느 쪽으로 견주어도 수가 같았다 · 앞 판의 앱 대조)
- 채우는 것은 이 판이 아니다 — **`--실행` 은 총무님이 수를 보신 뒤 다음 판**

# 2. 3 · 4단계 — 스크립트와 검증

**`scripts/업무계층채우기.py`** — 다른 전환 스크립트와 같은 꼴이다.
- 기본은 미리보기 · `--실행` 일 때만 바꾸고, 바꿀 것이 있을 때만 직전에 사본(`재정들여오기.사본을_뜬다` — 세션이 여는 파일과 사본 뜰 파일이 다르면 멈춤)
- **3b** — 상위가 이미 있는 노션 라이브러리가 하나라도 있으면 멈춘다. 노션 줄과 앱 라이브러리가 노션 페이지 id 로 1:1 이 아니어도 멈춘다
- **3c** — `related_library_ids` 는 안 건드린다. 실행 때 **모든 라이브러리의 관련 목록 지문을 넣기 전후로 견주어** 다르면 되돌린다
- **3d** — 활동 기록 한 줄(`업무_상위_채움` · 건수와 채운 짝)
- 넣은 뒤 commit 전에 DB 를 다시 센다 — 상위가 있는 노션 라이브러리 · 상위가 있는 라이브러리 전체 · 라이브러리 수 · 채움 기록이 계획과 맞을 때만 commit
- **4b 고리** — 채운 뒤의 모양(지금 DB 의 다른 상위까지 함께)에서 상위를 따라 올라가다 같은 줄을 두 번 만나면 멈춘다. 운영에서 **0**. 부모는 모두 노션 Main 이고 Main 은 상위를 안 받으므로(그리고 이미 상위가 있으면 멈추므로) 지금 모양에서는 고리가 못 생긴다 — 그래도 시험이 심어서 감시가 도는지 잰다

**시험 (`tests/test_stage61.py` · 10)**

| 시험 | 무엇 |
|---|---|
| `test61_a01` | Main 하나면 그것 · 여럿이면 같은 담당팀 하나 · 같은 담당팀이 둘이면 비움 · Main 을 안 가리키면 비움 · Main 끼리는 상위가 아님 |
| `test61_a02` | 속성 빈 줄(앱 kind=main)도 자식이 된다 — kind 로 안 가른다 |
| `test61_a03` | 담당 부서가 다른 부모도 채우고 따로 센다 |
| `test61_b01` | 이미 상위가 있으면 멈춘다 |
| `test61_b02` | 노션 줄과 앱이 1:1 이 아니면 멈춘다 |
| `test61_b03` | 고리가 생기면 멈춘다(DB 에만 고리를 심어 잼) |
| `test61_c01` | 미리보기는 DB 도 관련 목록도 안 바꾸고, 출력에 제목이 없고, 짝 표에는 있다 |
| `test61_c02` | 실행은 상위만 채우고 관련은 그대로 · 기록 한 줄 · 두 번째는 멈춘다 |
| `test61_c03` | 관련 목록이 바뀌면 되돌린다(상위 0 · 기록 0) |
| `test61_c04` | 사본은 채우기 전에 뜬다 |

**고장을 심어 쟀다 — 열 다 잡았다**(pytest 는 옵션 없이 그 파일 하나): Main 을 kind 로 가름 · 같은 담당팀 규칙 끔 · 「이미 상위」 멈춤 끔 · 고리 멈춤 끔 · 관련 지문 견주기 끔 · 사본 끔 · 되돌리기 뺌 · 부서 다름 안 셈 · Main 건너뛰기 끔 · 미리보기 멈춤 끔.

# 3. 채우면 바뀌는 자리 — 읽기만으로 잰 것

**보드에서 9 개가 사라진다.** `board.py` 의 부서 블록은 「상위가 없는 줄」 을 머리로 두고 **같은 부서의** 상위 아래에만 자식을 그린다. 운영 회차 1 을 그 규칙으로 흉내 내니(읽기만) **부서 블록의 줄이 208 → 199** 가 된다 — 사라지는 9 는 모두 **담당 부서가 다른 부모를 받는 자식**이다(나머지 91 은 같은 부서 82 · 자식에게 부서 없음 9). 부서가 없는 run 은 「담당 없음」 블록에 상위와 상관없이 서므로(25 → 25) **보드 전체로는 233 → 224** 다. 2027 회차는 노션 업무가 없어 **그대로**(93 → 93).

→ 사람이 정한 ③(자식을 자기 부서에 두고 부모 표시만)을 지키려면 **보드를 먼저 고치고 채워야 한다.** 그대로 `--실행` 하면 그 9 가 보드에서 안 보인다(목록 · 달력에는 남는다).

**그 밖에 움직이는 것**
- **마법사(`library.py` 의 회차 만들기)** — 하위는 상위를 따라 넣는다. 다음 회차부터 자식 100 이 부모와 함께 들고 난다
- **마법사 3단계 · 설정 › 라이브러리** — 자식을 부모 아래로 접는다(6-6)
- **드로어의 「상위」 줄 · 진단 패널의 상위 · 하위 근거** — 지금 「—」 인 자리에 값이 선다
- **보드 「+ 업무 추가」**(`routers/board.py` 의 후보 · 넣기) — 후보는 상위가 없는 라이브러리뿐이라 **자식 100 이 후보에서 빠진다.** 부모를 넣으면 그 자식을 모두 함께 넣는데 **권한은 부모의 부서 키로만** 본다 — 담당 부서가 다른 자식은 부모 부서 사람이 넣는 순간 **남의 부서 run 으로 딸려 들어간다**(자식 run 의 부서는 자식 것). 2027 회차는 노션 run 이 0 이라 이 길을 곧바로 밟는다
- **마법사 · 팀별 초안**(`library.py` 의 회차 만들기) — 자식은 부모가 골렸을 때만 들어간다. 담당 부서가 다른 자식은 **자기 팀이 초안에서 골라도 안 들어온다** — ③(자식은 자기 부서에 둔다)과 부딪힌다
- **라이브러리의 필수 일괄 지정**(`routers/library.py`) — 자식을 건너뛴다. 자식 100 의 필수 표시는 그 화면에서 못 바꾼다
- **처음 적은 「부서 없는 25 는 보드에 안 선다」 는 틀렸다** — 「담당 없음」 블록에 선다(커밋 전 검토 [1])

# 4. 2단계 — 화면 표시 갈래 (코드는 안 고침)

**2a · 부모 이름을 함께 보이기**

| 화면 | 갈래 |
|---|---|
| 보드 | 같은 부서의 자식은 **지금 트리 그대로**(부모 아래 들여쓰기 · depth 1). 따로 할 것이 없다 |
| 목록 | 행 메타(부서 칩 옆)에 **「↑ 부모 제목」**(보조 13px · 4-0 의 보조 단 · 새 자리는 `보조급자리` 에 올린다). 누르면 그 부모의 드로어를 연다(행은 안 열림 — 4-14 의 「안 여는 자리」 에 더함) |
| 드로어 | 머리 표의 「상위」 줄이 이미 있다 — 값을 **누를 수 있게**(열기 / 이동 · 연결 카드와 같은 메뉴) |
| 달력 | 점 툴팁(`tooltip_of` 한 곳)에 「상위: 부모 제목」 한 줄 |

**2b · 부모 부서가 다를 때 (③ · ④)**

- **갈래 가 (권함)** — 보드 부서 블록의 머리를 「상위가 없는 줄 **또는 상위가 이 부서에 없는 줄**」 로 넓혀, 그 자식을 **자기 부서의 머리 줄**로 그린다. 제목 옆에 **「↑ 부모 제목 · 부모 부서 색 점 + 부서 이름」** 을 붙인다 — 부모 제목은 그 줄이 없으면 알 수 없는 사실이라 **흐림이 아니라 보조 급**이고, 새 자리는 `test_typescale` 의 `보조급자리` 에 이름으로 올린다(4-0). 목록 · 드로어의 「↑ 부모」 표시에도 부서가 다를 때만 색 점을 붙인다. **부모에게 부서가 없는 2**(부모 run 이 「담당 없음」 블록)는 색 점 대신 「담당 없음」 글자를 붙인다
- **갈래 나** — 그 자식을 자기 부서 블록에서 **들여쓴 채**(depth 1) 맨 아래 「다른 부서 상위」 묶음으로 모은다. 트리 모양은 지키지만 부모가 그 블록에 없어 들여쓰기의 뜻이 흐려진다
- **어느 쪽이든** — 부모 쪽 부서 블록에는 그 자식을 **고스트 바로 보이지 않는다**(관련팀 고스트와 섞인다 · 4-4). 연결 카드(4-9)의 「관련」 에는 **그대로 남긴다**(④ · 상위와 관련을 둘 다 보인다). 부모 드로어에는 진단 패널의 「하위 N건」 이 이미 부서를 가리지 않고 센다
- **고칠 곳** — `board.py` 부서 블록(머리 판정 · 행에 부모 제목 · 부서 싣기) · `partials/taskrow.html` 메타 · `tasks.js` 의 안 여는 자리 · `drawer.js` 상위 줄 · `board.tooltip_of`. 시험은 「채운 뒤에도 보드 줄 수가 산 run 수와 같다」 를 잰다
- **화면 밖에 함께 볼 것(3장)** — 「+ 업무 추가」 가 부모를 넣을 때 부서가 다른 자식을 딸려 넣는 것(자식 부서 권한도 보거나, 부서가 다른 자식은 빼고 넣기) · 마법사와 초안에서 부서가 다른 자식을 자기 팀이 고르게 할지 · 필수 일괄 지정이 자식도 다룰지
- **kind 가 main 인 자식 1**(속성 빈 줄) — 보드에서 depth 1 에 두꺼운 바로 그려진다(4-2 는 하위를 얇게). ②의 결과라 막지 않는다

**2c** — 이 판은 설계만 냈다.

# 5. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) tsv 업무속성으로 정확히 100건(kind 아님) | O | 1장 표 · `test61_a02` |
| 나) 순환 참조가 없음이 확인됨 | O | 2장 · 운영 고리 0 · `test61_b03` |
| 다) 화면 표시 갈래(코드 안 고침) | O | 4장 |
| 라) 미리보기가 100 · 담당부서 다른 10 을 값 없이 보임 | O | 1장 표 · `test61_c01`(출력에 제목 없음) |
| 마) pytest 옵션 없이 전부 통과 · 글검사 7/7 | O | 9장 |
| 바) 검토자 원문이 최근.md 에 그대로 | O | 7장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 0a | 했다 | tsv 와 운영 DB 만 |
| 0b | 했다 | 칸 있음 · 233 모두 비어 있음 |
| 1a | 했다 | Main 87 |
| 1b | 했다 | 같은 담당팀은 노션 담당팀 글자로 견줌(1장) |
| 1c | 했다 | 100 · 부서 다른 10 도 채울 것에 듦 |
| 1d | 했다 | 46 비움 |
| 2a | 했다 | 4장 |
| 2b | 했다 | 4장 갈래 가 · 나 |
| 2c | 했다 | 코드 안 고침 |
| 3a | 했다 | 미리보기 기본 · `--실행` · 사본 · 파일이 다르면 멈춤 |
| 3b | 했다 | 이미 있으면 멈춤 |
| 3c | 했다 | 관련 지문으로 견줌 |
| 3d | 했다 | 활동 기록 한 줄 |
| 4a | 했다 | 1장 표 |
| 4b | 했다 | 고리 0 · 시험으로 심어 잼 |
| 미리보기까지만 · `--실행` 은 다음 판 | 했다 | 3장 — 그 전에 보드를 고쳐야 함 |
| 노션은 안 부른다 · 운영 DB 는 읽기만 | 했다 | 머리 |
| 검토자 원문을 그대로 싣는다 | 했다 | 7장 |
| 마무리는 11-3 대로 | 했다 | 앞 판 보고를 `docs/review/2026-09-17-계층재조사.md` 로 얼림 · 재시작 건너뜀 |

# 6. 사람이 정할 것 (사람칸)

1. **순서** — 보드(와 「+ 업무 추가」)를 먼저 고치고 `--실행` 할지(권함 · 3장), `--실행` 을 먼저 하고 그 9 가 잠깐 보드에서 빠지는 것을 받을지
2. **2b 갈래** — 가(자기 부서의 머리 줄 + 부모 표시 · 권함) · 나(맨 아래 「다른 부서 상위」 묶음)
3. **목록의 「↑ 부모」 를 누르면** 부모 드로어를 열지, 그 행으로 옮길지
4. **부서가 다른 자식 10 을 「+ 업무 추가」 · 마법사 · 초안에서** — 부모를 따라서만 들어오게 둘지, 자기 팀이 따로 고르게 할지(3장)

# 7. 검토자 원문

## 7-1. 커밋 전 검토

```
━━━ 검토자 여기부터 ━━━ 계층 채우기 판 · 범위 e338005..스테이징(git diff --cached) · 커밋 0개 · 파일 8개

직접 재어 본 것
  - 얼린 사본: docs/review/2026-09-17-계층재조사.md 는 git show e338005:docs/review/최근.md 와 바이트까지 같다(cmp)
  - 운영 DB 는 sqlite mode=ro 로만 열었다. 스크립트는 돌리지 않았다
      노션 줄 233 · 앱 233 · 1:1 맞음 · 노션 라이브러리 중 상위가 있는 것 0
      (상위가 있는 라이브러리는 전체 47 인데, 모두 노션 밖의 것이다)
      Main(업무속성) 87 · 앱 kind=main 97 · 속성 빈 줄 10
      채울 것 100 = Main 하나 84 + 같은 담당팀 16
      비울 것 46 = 가를 수 없음 11 + Main 을 안 가리킴 35
      담당 부서가 다른 부모 10 · 부모가 Main 이 아닌 것 0
      같은 담당팀을 노션 글자로 견주든 앱 부서 키로 견주든 고른 결과가 한 줄도 안 다르다
  - DB 를 안 바꿨다는 주장은 맞다
      journal_mode=delete 이고 app.db 수정 시각은 15:20 이다
      backups 에 15:20:10 뒤의 app-*.db 가 없다
      짝 표 data/업무계층채우기.real.md 는 .gitignore(data/*.real.md)에 걸린다
  - pytest tests/test_stage61.py(옵션 없음): 10 passed
    check_counts 는 통과했다
    check_placeholder 는 최근.md 의 ⟪…⟫ 때문에 실패한다 — 커밋 전에 채운다고 했으니 예상한 결과다
  - 스크립트
      순서: 전 수 세기 → 사본 → 넣기 → 다시 세기 → 관련 지문 견주기 → commit(어긋나면 rollback)
      사본을_뜬다 는 세션 파일과 사본 파일이 다르면 넣기 전에 멈춘다
      출력에 값이 없다(건수와 파일 이름뿐). 앞 판에 적힌 수와 다른 곳이 없다
  - 보드 흉내(board.py 469-528 규칙 그대로 · 읽기만)
      회차 1: 부서 블록 줄 208 → 199 · 「담당 없음」 블록 25 → 25 · 산 run 233
      회차 2: 93 → 93

막아야 함
  없음

고쳐야 함
  1. 「부서 없는 25 는 보드의 어느 부서 블록에도 안 선다」 는 틀린 말이다 (이미 있던 빈틈이 아니다)
     어디: docs/봐둘것.md BF-a 마지막 줄 · docs/review/최근.md 3장 마지막 줄
     근거: app/domain/board.py 512-528 이 department_id 가 없는 산 run 을 전부 「담당 없음」 블록에 depth 0 으로 그린다
       - 이 블록은 상위를 안 본다
       - board.html 70 이 board.departments 를 모두 돈다
       - 흉내 내어 세니 208 + 25 = 233 으로 산 run 전부가 보드에 선다
     같이 틀린 줄: 봐둘것 BF-a 의 「나머지 1 은 자식 run 에 부서가 없어 원래 보드에 안 선다」
       - 그 1 은 「담당 없음」 블록에 서 있고, 채운 뒤에도 남는다
     줄 수 「208 → 199」 는 부서 블록만 센 수다. 보드 전체로는 233 → 224 다
     「사라지는 9」 는 맞다

  2. 훑기(최근.md 0장)와 3장 「채우면 바뀌는 자리」 에 보드 「+ 업무 추가」 가 빠졌다
     app/routers/board.py 726
       - 후보를 상위가 없는 라이브러리로만 낸다
       - 채우면 자식 100 이 후보에서 사라진다
     app/routers/board.py 811-817
       - 부모를 넣으면 그 자식을 모두 함께 넣는다
       - 권한은 부모의 부서 키 하나로만 본다
       - 그래서 담당 부서가 다른 자식 10 은, 부모 부서 사람이 그 Main 을 넣을 때 남의 부서 run 으로 딸려 들어간다
     2027 회차(회차 2)는 노션 run 이 0 이라, 운영에서 이 길을 곧바로 밟는다
     같은 모양이 둘 더 있다
       - app/domain/library.py 607-613(마법사 · 6-7 초안): 자식은 부모가 골렸을 때만 들어간다
         담당 부서가 다른 자식은 자기 팀이 초안에서 골라도 안 들어온다
         사람이 정한 ③(자식은 자기 부서에 둔다)과 부딪힌다
       - app/routers/library.py 190: 필수 일괄 지정이 자식을 건너뛴다
     셋 다 --실행 전에 사람이 볼 자리다. BF-a 에 「보드를 먼저 고친다」 와 함께 적어 둘 것

봐 둘 것
  3. 최근.md 4장 2b 갈래 가의 표시가 설 자리가 없는 줄이 있다
     담당 부서가 다른 10 가운데 2 는 부모 라이브러리에 부서 키가 없다
     부모 run 도 부서가 없어 「담당 없음」 블록에 선다
     그래서 「부모 부서 색 점 + 부서 이름」 을 붙일 부서가 없다
     그때 무엇을 보일지 설계에 한 줄이 필요하다

  4. 최근.md 4장의 글자 급이 CLAUDE.md 4-0 과 맞는지
     보드의 「↑ 부모 제목 · 부서」 를 흐림으로 둔다고 적었다
       - 4-0 은 흐림을 옆의 값이 뜻을 다 지는 자리에만 쓰고, 그 자리를 흐려도_되는곳 에 이름으로 적는다
       - 부모 제목은 그 줄이 없으면 알 수 없는 사실이라 보조 이상으로 읽힌다
     목록의 보조 13px 는 4-0 에 따라 test_typescale 의 보조급자리 에 이름으로 올려야 한다
     다음 판 설계에서 짚을 것

  5. 채울 것 100 가운데 kind=main 인 자식이 1 이다(속성 빈 줄)
     보드에서 depth 1 에 두꺼운 바로 그려진다(4-2 는 하위를 얇은 바로 그린다)
     사람이 정한 ②(셈은 업무속성)의 결과라 막을 일은 아니다
     나머지 99 는 sub 64 · schedule 35 다

  6. test61_b03 의 심기 방식
     세션 객체의 상위만 비우고 DB 에는 고리를 둔다. 그래서 「이미 상위」 검사(ORM 객체를 읽음)를 지나 고리 감시(DB 를 읽음)에 닿는다
     고리 감시가 판.채울 만 보도록 바꾸면 이 시험이 빨개진다 — 볼 것을 본다
     두 검사가 읽는 곳이 다르다는 점은 새 세션으로 도는 실제 실행에서는 해가 없다

  확인만 하고 문제가 없던 것
    CLAUDE.md 14장 새 줄에 적힌 이름은 모두 코드에 있다(check_named 는 따로 돌리지 않았다)
    인계 맨 위 줄 · 세어둠 한 줄(check_counts 통과 · 그 줄을 뺐을 때 걸리는지는 안 재 봤다)
    지시문 대조 표에 빠진 항목이 없다 · 사람칸 다섯 줄이 그 순서로 있다
    출력 · 보고 · 문서에 제목과 사람 이름 값이 없다

사람에게 묻는 것
  - 보드 「+ 업무 추가」 가 부모를 넣을 때 담당 부서가 다른 자식까지 딸려 들어가는 것
      받을지, --실행 전에 보드와 함께 고칠지 (고쳐야 함 2)
  - 마법사와 팀별 초안에서 담당 부서가 다른 자식 10 을 어떻게 할지
      부모를 따라서만 들어오게 둘지, 자기 팀이 고를 수 있게 할지 (③과 관련)
  - 부모에게 부서가 없는 2 줄에서 「↑ 부모」 옆에 무엇을 보일지 (봐 둘 것 3)
━━━ 검토자 여기까지 ━━━
```

## 7-2. 검토 뒤 고침을 다시 본 것

```
━━━ 검토자 여기부터 ━━━ 계층 채우기 판 · 다시 보기 · 범위 e338005..스테이징(git diff --cached) · 커밋 0개 · 파일 8개

본 것
  최근.md 사람칸 · 0장 · 3장 · 4장 · 6장 · 7-1 · 8장 · 9장
  봐둘것 BF-a · 인계 맨 위

  코드는 안 바뀌었다
    app · templates · static 은 스테이징에 없다
    scripts · tests 는 스크립트 260줄 · 시험 188줄로 앞 검토 때와 같다

  고친 문장은 앞 검토에서 잰 값과 맞는다
    부서 블록 208 → 199
    담당 없음 블록 25 → 25
    보드 전체 233 → 224
    나머지 91 = 같은 부서 82 + 자식에게 부서 없음 9
    부서 없는 부모 2
    kind main 자식 1

  「+ 업무 추가」 · 마법사 · 필수 일괄 지정도 코드와 맞게 적혔다
    routers/board.py 726 · 811-817
    domain/library.py 607-613
    routers/library.py 190

  사람칸: 다섯 줄이 그 순서로 있고, 정할 것이 6장 1-4 를 가리킨다
  8장 표: 앞 검토의 [1]~[6] 이 모두 있다

막아야 함
  없음

고쳐야 함
  1. docs/review/최근.md 7-1 의 코드 울타리가 홀수다
     울타리 줄: 165 · 168 · 264(열기만 있고 닫는 줄이 없다)
     까닭: 앞 검토 답에서 제가 경계 줄을 블록 밖에 한 번 더 적었고, 그것이 그대로 실렸다(166-168)
     그래서 168 에서 블록이 닫히고, 264 에서 새 블록이 열린 채 끝난다
     결과: 7-2 · 8장 · 9장이 통째로 코드블록 안에 들어가 제목과 표로 안 그려진다
     고치는 길 둘 중 하나
       - 블록 밖에 있던 경계 줄과 울타리(166-168)만 걷는다
         블록 안의 원문은 한 글자도 안 바뀐다
       - 그 줄을 남기되 울타리 짝을 맞춘다
     이번 답은 경계 줄을 블록 안에만 두었다

봐 둘 것
  2. check_placeholder 는 ⟪…⟫ 꼴을 찾지 않는다
     찾는 꼴: scripts/check_placeholder.py 83 의 PLACEHOLDER 뿐이다
     지금 이 검사는 통과(0)하지만, 7-2 · 9장의 ⟪검토2⟫ · ⟪파일⟫ · ⟪글검사⟫ · ⟪pytest⟫ 가 남아 있어도 통과한다
     게다가 위 1 이 고쳐지기 전에는 그 자리가 원문 경계(166 여기부터 ~ 263 여기까지) 밖이다
     채웠는지는 커밋 전에 사람 눈으로 본다
     (첫 검토 때 이 검사가 실패한 까닭은 따로 캐지 않았다)

  3. 최근.md 0장 훑기 표의 「같은 이름을 쓰는 데」 줄이 그대로다
     여전히 domain 파일만 적혀 있다
     검토가 짚어 3장에 더한 routers/board.py · routers/library.py 가 이 줄에는 없다
     8장이 그 사실을 말하므로 막을 일은 아니다
     다만 훑기 표만 읽는 사람에게는 빠진 채로 보인다(검토-원칙 8-2)

  4. 4장 2a 표의 목록 행
     「↑ 부모 제목(보조 13px)」 에는 보조급자리 에 올린다는 말이 없다
     2b 에만 그 말이 있다
     같은 판단이니 다음 판 설계에서 두 곳을 함께 볼 것

사람에게 묻는 것
  없음(새로 생긴 물음이 없다 · 앞 검토의 물음은 6장 4 와 4장에 들어갔다)
━━━ 검토자 여기까지 ━━━
```

# 8. 검토 뒤 고침

| 검토 | 한 것 | 파일 |
|---|---|---|
| [1] | 「부서 없는 25 는 보드에 안 선다」 를 걷고(「담당 없음」 블록에 선다 · `board.py` 확인) 줄 수를 「부서 블록 208 → 199 · 보드 전체 233 → 224」 로 고쳤다. 봐둘것은 옛 줄에 줄을 긋고 틀렸다고 적었다 | 최근.md · 봐둘것.md |
| [2] | 「+ 업무 추가」(후보에서 자식이 빠짐 · 부모 부서 권한으로 부서 다른 자식이 딸려 듦) · 마법사와 초안(부서 다른 자식을 자기 팀이 못 고름) · 필수 일괄 지정(자식 건너뜀)을 3장 · 4장 · BF-a · 인계에 적고, 사람칸 정할 것에 더했다(6장 4). 코드 위치는 직접 읽어 확인했다 | 최근.md · 봐둘것.md · 인계.md |
| [3] | 부모에게 부서가 없는 2 는 「담당 없음」 글자를 붙이는 설계를 더했다 | 최근.md |
| [4] | 부모 표시를 흐림이 아니라 보조 급으로 두고 `보조급자리` 에 올린다고 설계를 고쳤다 | 최근.md |
| [5] | kind 가 main 인 자식 1 이 두꺼운 바로 그려진다는 것을 4장에 적었다 | 최근.md |
| [6] | 고칠 것 없음 | — |

검토의 「사람에게 묻는 것」 셋은 사람칸 정할 것(6장 1 · 4)과 4장 [3] 의 설계에 넣었다.


## 8-1. 두 번째 검토 뒤 고침

세 번째 검토는 없다(11-3).

| 두 번째 검토 | 고친 것 | 파일 |
|---|---|---|
| 새 1 | 7-1 에 실은 원문 앞에 검토자가 블록 밖에 한 번 더 적은 경계 줄과 울타리가 함께 실려 울타리가 홀수였다 — 그 밖의 줄만 걷었다. 원문 블록은 한 글자도 안 바뀌었다(대화 기록에서 뽑은 것과 같음). 7-2 는 처음부터 블록 안만 실었다 | 최근.md |
| 새 3 | 0장 훑기의 「같은 이름을 쓰는 데」 에 라우터 둘을 더했다 | 최근.md |
| 새 4 | 4장 2a 목록 줄에도 「`보조급자리` 에 올린다」 를 적었다 | 최근.md |
| 새 2 | 안 고침 — 채울 자리는 9장에서 사람 눈(grep)으로 본다 | — |

# 9. 검사 · 시험

**바뀐 파일** (`git diff --name-only --cached e338005`)

```
CLAUDE.md
docs/review/2026-09-17-계층재조사.md
docs/review/최근.md
docs/봐둘것.md
docs/세어둠.txt
docs/인계.md
scripts/업무계층채우기.py
tests/test_stage61.py
```

`app/` · `templates/` · `static/` 이 없어 재시작은 건너뛰었다.

**글검사** — 7개 전부 통과(check_names · check_stale · check_named · check_counts · check_handoff · check_placeholder · check_orders). 채울 자리 표시는 검토자 원문 밖에서 0 이다(9장 pytest 줄을 채운 뒤 다시 봄)

**pytest** (옵션 없이 · 보고를 다 채운 뒤) — `1708 passed, 3 warnings in 863.98s (0:14:23)`
