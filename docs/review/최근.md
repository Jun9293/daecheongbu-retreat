# 검토용 보고

작성 시각: 2026-09-07 (KST)
커밋: 6f6bc43 (브랜치 `main` · 이 판의 문서 커밋 · 보고 커밋은 그 뒤)
작업: 운영 정리 — 스케줄러 (단계 4 0-a 후속). 코드 변경 없음 · 문서만 커밋

---

## 한 것 / 못 한 것

지시 일곱 중 **1·4·6·7 완료, 2·3·5 는 계정 암호가 필요해 사람 몫**으로
넘겼습니다 — 절차를 배포-안내 **12-2-2** 로 적었습니다.

| # | 지시 | 결과 |
|---|---|---|
| 1 | 충전기 확인 | ✅ PowerOnline=**True** · BatteryStatus=2(충전 중) · 90% |
| 2 | 앱 작업 배터리 둘 끄기 + 시간 제한 무제한 | ⛔ 미적용 — 아래 「왜 못 했나」 |
| 3 | 백업 작업 시작 위치 | ⛔ 미적용 — 같은 이유 |
| 4 | 손 서버 → 스케줄러로 재시작 | ✅ PID **11300 → 21320** · 「성공 — PID 가 바뀌었습니다」 · 작업 상태 Running |
| 5 | 백업 손 실행 → LastResult=0 확인 | ⛔ 시작 위치가 아직 비어 있어 돌려도 exit 2 — 승격 시도 중 한 번 돌았고 역시 **LastTaskResult=2**. 12-2-2 적용 뒤에 확인 |
| 6 | 문서 | ✅ 12-1 배터리 규칙 · 12-2 백업 시작 위치 규칙 · 12-2-1 옛 「끄지 마세요」 를 새 정책으로 고침 · **12-2-2 신설**(GUI/붙여넣기 절차, 암호 필요 명시) · 봐둘것 **T-f**(백업 8/31~9/6 산출물 없음 — 그 이레는 되돌릴 지점 없음) |
| 7 | reset 사고 확인 | ✅ 아래 네 줄 |

## 왜 2·3·5 를 못 했나

- `Set-ScheduledTask -InputObject` 는 **오류 없이 소리 없이 안 바뀌었습니다**
  (승격 셸에서 실측 — [전]과 [후]가 같음). 두 작업 다 LogonType=**Password**
  (로그온 안 해도 실행, 암호 저장)라 정의를 다시 등록해야 하고, 그때
  **계정 암호를 다시 물어봅니다.**
- 암호는 제가 다룰 수 없는 영역이라(입력 금지) 여기서 멈췄습니다.
  XML 은 미리 고쳐 두었고, 12-2-2 의 붙여넣기 블록이 같은 것을 처음부터
  만들어 `/RP *` 로 **본인이 암호를 치게** 합니다.
- 승격 자체도 한 번 헛돌았습니다 — BOM 없는 UTF-8 `.ps1` 을 PS 5.1 이
  cp949 로 읽어 한글 식별자에서 파싱이 깨짐(11-2 의 그 함정, `.ps1` 판).
  BOM 을 붙여 해결.

## 4번 실측 (돌고 있는 서버)

```
멈추기 전 PID: 11300   (12-2-1 임시 수동 기동분)
포트가 비었습니다
다시 켠 뒤 PID: 21320   (작업 「대청부 앱」 State=Running)
성공 — PID 가 바뀌었습니다
/ = 303 · /reviews = 301 → /notifications?tab=sent · /files = 404 · drawer.a489b428.js = 200
```

## 7번 — origin/main 계보 (reset 사고 확인)

`git log --oneline origin/main` 에서 넷이 이 순서(최신→과거 1·10·17·19번째)로
있습니다 — 단계 2·3·4 커밋이 전부 제자리이고 잃은 것이 없습니다.

```
e185162 보고에 두 번째 재시작의 배터리 게이트를 적는다 — 자가사망 원인 확정
2e60a9f 검토용 보고에 재시작 결과를 적는다 — PID 33940 · 옮기기 2행·58건 확인
2eb5112 검토용 보고를 이번 작업으로 덮어쓴다
9886504 검토용 보고를 이번 작업으로 덮어쓴다
```

## 설정 전·후 표 (2번 — 적용은 사람 몫이라 「후」 는 목표값)

| 작업 | 항목 | 전 (실측) | 후 (12-2-2 적용 시) |
|---|---|---|---|
| 대청부 앱 | DisallowStartIfOnBatteries | True | False |
| 대청부 앱 | StopIfGoingOnBatteries | True | False |
| 대청부 앱 | ExecutionTimeLimit | PT72H | PT0S (무제한) |
| 대청부 백업 | DisallowStartIfOnBatteries | True | False |
| 대청부 백업 | StopIfGoingOnBatteries | True | False |
| 대청부 백업 | ExecutionTimeLimit | PT72H | PT72H (그대로) |
| 대청부 백업 | WorkingDirectory | (비어 있음) | C:\Users\user\Desktop\대청부-수련회-시스템 |

## 검증

- pytest 옵션 없이 전부 — 마지막 줄: `1010 passed in 236.99s (0:03:56)`
  (문서만 바뀐 판 — 문서를 읽는 다섯 파일 105건은 먼저 따로 통과 확인)
- check_names: 실명 38 · 걸린 것 없음 · 안 고친 곳 0곳 · 대응표 밖 표기 0
- 서버가 읽는 파일(app/·templates/·static/)은 안 바뀌어 재시작 없음 —
  4번의 재시작은 지시된 「스케줄러 관리 복귀」 이지 코드 반영이 아님
- 검토자 없음 (지시 — 코드 변경 없음)
