사람이 할 것
노트북에서 직접: 없음 — 이 판은 읽기만 했다(4장)
관리자 권한: 없음 — `app/` 을 안 고쳐 재시작하지 않았다(4장)
정할 것: 총무님이 `data/업무대조-확인용.real.md` 에 표시(짝 확인 31줄 · 노션만 151줄) · 다음 판이 무엇을 반영할지 갈래 ㄱ·ㄴ·ㄷ(5장)
막혀서 못 한 것: 화면 점검(drawer.js · phone.js) — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다. 화면을 안 고친 판이라 급하지 않다(4장)

작성 시각: 2026-09-16 02:20 KST
커밋: 63be095 다음 — 이 판의 커밋들(7장 파일 목록)
작업: 업무 대조 1판 — 짝짓기를 확정하고 애매한 것을 사람 앞에 낸다

---

**값은 여기 없다.** 제목 · 날짜 · 담당은 저장소 밖 `data/업무대조.real.md`(등급별 상세)와
`data/업무대조-확인용.real.md`(총무님이 체크할 표)에만 있다. 여기에는 건수 · 규칙 · 문턱만 적는다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 없음 — 새 파일 둘(`scripts/노션업무짝짓기.py` · `tests/test_stage55.py`)이고 앱 코드는 안 건드린다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면은 안 고친다 |
| 그 값을 세어 둔 문서 | 인계 1)(1판의 네 갈래 수 · 업무 ㄱ2 줄) · CLAUDE.md 14장 표(회의록 id 줄 옆) · 1판 얼린 보고(안 고친다) |

# 1. 짝짓기 규칙을 다시 짰다 (1a–1e)

**규칙과 문턱은 저장소 안에 둔다** — `scripts/노션업무짝짓기.py`. 1판은 짝짓기 코드가 `data/` 에만 있어 다음 판이 재현할 수 없었다(1판 검토 [M] 6). 값을 읽어 파일로 내는 실행기만 저장소 밖(`data/업무대조.real.py`)이다.

| 1판(`data/노션대조.real.py`) | 이번 |
|---|---|
| ① 제목이 같은 것부터 순서대로 ② 남은 것 중 닮음이 문턱을 넘는 것을 **가장 닮은 순으로** 1:1 | **모든 짝의 점수를 다 매긴 뒤 높은 것부터 확정** — 입력 순서가 답을 안 바꾼다(`test55_b01`) |
| 제목 닮음만 | 제목 닮음 + **구분 · 담당 · 시작일이 맞으면 더함**(덤) |
| 구분 없는 34줄을 통째로 뺌 | **D-주차 표지 24 만 빼고 10줄을 후보에** |
| 같은 제목이 노션에 여럿이면 앞줄이 가져감 | **날짜 · 담당이 가까운 줄이 이김**(덤이 가른다 · `test55_c01`) |

**문턱** — 제목 닮음 `0.50` 위만 후보, 등급 A `0.93`(제목과 총점 둘 다), 등급 B `0.70`(총점). 값은 그 파일 한 곳에 있다.

**총점을 1 에서 자르지 않는다** — 자르면 제목이 똑같은 줄이 여럿일 때 덤이 지워져 날짜 · 담당이 가른 결과가 사라진다(시험이 먼저 잡았다).

## 1e. 네 갈래가 어떻게 달라졌나 (건수만)

| 갈래 | 1판 방식 | 이번 규칙 |
|---|---|---|
| 노션만 | 143 | 151 |
| 앱만 | 22 | 20 |
| 둘 다 · 제목 같음 | 50 | 50 |
| 둘 다 · 제목 다름 | 30 | 32 |
| 짝 전체 | 80 | 82 |

**노션만이 는 까닭은 후보가 늘었기 때문이다** — 구분 없는 10줄이 들어와(143 + 10) 그중 1 이 짝지었고 새 규칙이 앱만에서 2 를 더 이어(−2) 151 이다. 앱 run 102 = 짝 82 + 앱만 20, 노션 후보 233 = 짝 82 + 노션만 151(표지 24 는 갈래 밖).

# 2. 등급 A · B · C (2a–2d)

| 등급 | 뜻 | 건수 |
|---|---|---|
| A | 제목이 사실상 같고 나머지도 안 어긋남 — 자동으로 믿는다 | 51 |
| B | 짝일 만하나 사람이 봐야 한다 | 30 |
| C | 근거가 약하다 — 노션만/앱만으로 두는 편이 나을 수 있다 | 1 |

- 구분 없는 후보 10 중 짝지은 것 1
- **B · C 상세는 저장소 밖** `data/업무대조.real.md` — 등급 · 점수 · 양쪽 제목과 구분 · 담당 · 시작일을 나란히 놓았다. 같은 파일에 A 목록 · 노션만 · 앱만도 있다

# 3. 총무님 확인용 표 (3a · 3b)

`data/업무대조-확인용.real.md` — 저장소 밖. **표시만 하면 되는 두 표**다.

| 표 | 줄 | 체크 칸 |
|---|---|---|
| 1. 이 둘이 같은 업무인가 (등급 B · C) | 31 | 「맞다 / 아니다 / 모르겠다」 · 비우면 다음 판이 「모르겠다」 로 본다 |
| 2. 노션에만 있는 업무 | 151 | 「넣음 / 안 넣음 / 버림」 과 「(안 넣음이면) 어느 업무에 묶였나」 |

`.xlsx` 가 아니라 `.md` 로 냈다 — 총무님이 열어 글자만 적으면 되고, 다음 판이 다시 읽기도 쉽다(엑셀은 칸마다 옮겨 다녀야 하고 이 표는 셈이 없다).

# 4. 이번 판이 안 한 것

- **앱에 아무것도 반영하지 않았다** — 코드 diff 에 `app/` 이 없다(7장 파일 목록). 운영 DB 는 `mode=ro` 로 읽기만, 노션은 이번 판에서 아예 안 열었다(1판이 옮겨 적은 tsv 를 썼다)
- **재시작 안 함** — `app/` · `templates/` · `static/` 변경 없음. 운영 서버 PID 19156 그대로
- **drawer.js · phone.js 못 돌림** — 로그인이 필요하다(11-2). 화면을 안 고친 판이라 급하지 않다

# 5. 다음 판 갈래

총무님 표시를 받은 뒤 무엇을 반영할지.

| 갈래 | 무엇 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **ㄱ. 표시된 것만 반영** | 「맞다」 는 잇고 「아니다」 는 갈라 두고 「모르겠다」 · 빈 칸은 그대로 둔다 | 사람이 본 것만 굳는다 | 표시가 덜 오면 다음 판이 또 기다린다 |
| **ㄴ. A 는 자동 확정 · B · C 만 기다림** | 등급 A 51 은 바로 잇고 31줄의 표시만 기다린다 | 절반 넘게 먼저 닫힌다 | A 의 판정이 틀린 자리가 있으면 조용히 굳는다 — A 를 사람이 본 적은 없다 |
| **ㄷ. 표시가 올 때까지 아무것도 안 함** | 기다린다 | 틀린 반영이 0 | 회차가 지나도 노션만 151 이 그대로 남는다 |

함께 정할 것 — 노션만 줄에 run 을 새로 세울지(ㄱ2 의 그 절반) · 앱 run 에 노션 id 를 담을 칸을 어디에 둘지 · 「원본에 없음」 을 칸으로 둘지 상태로 둘지(인계 1) 의 옛 미정).

# 6. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 전역 방식으로 바뀌고 옛 방식과 건수 비교 | O | 1장 표 · 1e 표 |
| 나) 구분 없는 34줄 중 10줄이 후보 | O | 1e 밑줄 · `test55_a01` |
| 다) 등급 A/B/C 건수 · B/C 상세가 저장소 밖 | O | 2장 |
| 라) 총무님이 체크만 하면 되는 파일 | O | 3장 |
| 마) 앱에 반영 없음(`app/` diff 없음) | O | 4장 · 7장 파일 목록 |
| 바) pytest 옵션 없이 · 글검사 7/7 | 마지막 pytest 뒤 채움 | 7장 |
| 사) 검토자 원문 | 검토 뒤 채움 | 8장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 1a | 했다 | 1판 코드를 열어 그 규칙을 표로 옮겼다 |
| 1b | 했다 | 점수를 다 매긴 뒤 높은 것부터 — `test55_b01` · `b02` |
| 1c | 했다 | 표지 24 만 뺀다 — `test55_a01` |
| 1d | 했다 | 덤(구분 · 담당 · 날짜)이 가른다 — `test55_c01` · `c02` |
| 1e | 했다 | 1e 표 |
| 2a | 했다 | 2장 — A 51 |
| 2b | 했다 | 2장 — B 30 |
| 2c | 했다 | 2장 — C 1 |
| 2d | 했다 | 2장 · `data/업무대조.real.md` |
| 3a | 다르게 했다 | `.xlsx` 가 아니라 `.md` 로 냈다(지시문이 「나 `.md`」 로 열어 뒀다) — 까닭은 3장 |
| 3b | 했다 | 3장 표 2 |
| 4a | 했다 | 고장을 심어 재는 시험 둘(`b02` · `c02`) · 이 판은 DB 에 안 쓴다 |
| 4b | 했다 | 규칙 · 문턱을 저장소 안 `scripts/노션업무짝짓기.py` 에 뒀다(1판 검토 지적) — 저장소 밖보다 한 걸음 더 |
| 5a | 했다 | 인계 맨 위와 1) |
| 5b | 했다 | 5장 · 인계 1) |
| 운영 · 노션 읽기만 · 재시작 안 함 | 했다 | 4장 |
| 마무리는 11-3 대로 | 했다 | 7 · 8장 |

# 7. 검사와 시험

- 커밋 전 `git status --short`: 코드 · 시험 · 문서만 — `data/` 안의 것은 안 잡혔다
- 글검사: 아래 줄
- 새 시험: `tests/test_stage55.py` — 아래 줄
- 바뀐 파일: 아래 줄
- 보고를 다 채운 뒤 돌린 pytest: 아래 줄

# 8. 검토자 원문

검토 뒤 여기에 싣는다.
