사람이 할 것
노트북에서 직접: 없음 — 운영 자료는 **읽기만** 했고(글검사의 `check_names` 가 `mode=ro` 로 엽니다) 운영 명령은 안 돌렸습니다. 코드를 한 줄도 안 고쳐 개발 서버 재시작도 없습니다(3장)
관리자 권한: 없음
정할 것: **둘** — ① **부서 리더 프로필에도 좁은 창을 하나 만들어 주시겠습니까?** 지금 그 프로필의 창은 다른 일에 쓰고 계신 창과 같은 창이라 이 창이 줄이지 않았습니다(2장 ㄱ). 안 만들어도 4단계는 닫힙니다 ② **계좌 팝업을 손가락으로 눌러 여는 것** — 7-4 는 「휴대폰은 눌러서 연다」 고 적었는데 코드에는 마우스 올림뿐입니다(1장의 그 줄). 실기로 한 번 보고 넘어갈지, 누름 갈래를 둘지
막혀서 못 한 것: **부서 리더 계정의 좁은 폭 점검** — 창이 하나뿐이라 막혔습니다(2장 ㄱ). 관리자 계정의 좁은 폭은 다섯 화면 전부 돌았습니다

작성 시각: 2026-09-27 (KST)
커밋: 5장에 이 판의 커밋이 있다 (앞 판은 2e0c3be)
작업: 4단계 뒷정리 — 좁은 폭(<820px) 점검

---

# 0. 고치기 전에 훑은 것 (11-3 0단계)

**코드를 안 고치는 판이라 훑을 것이 좁았다.** 그래도 적는다 — 비어 있으면
훑었는지 안 훑었는지 알 수 없다.

| 무엇을 찾았나 | 나온 것 |
|---|---|
| 이 판이 손댈 파일 | `docs/봐둘것.md`(BJ-i) · `docs/인계.md`(회전) · `docs/review/최근.md`. **`app/` · `templates/` · `static/` 은 하나도 안 건드린다** |
| 좁은 폭 결과를 적어 둔 자리 | 앞 판 보고의 표 하나뿐이다. **CLAUDE.md 에는 그런 표가 없다** — 지시문이 가리킨 「4장 표」 를 찾았으나 없어서 보고에 적었다(4장 대조 표의 그 줄) |
| 「좁은 폭을 못 돌렸다」 고 적힌 자리 | 봐둘것 BJ-i 의 한 문단 · 인계의 갱신 줄과 「남은 것」 표 한 줄. **셋 다 이 판이 고쳤다** — 하나만 고치면 문서가 서로 반대를 말한다 |
| 점검 파일이 폭을 전제하는 자리 | `phone.js` 는 820px **아래**에서만 돌고 넓으면 스스로 멈춘다. `fin.js` 는 폭 전제가 없다 — 그래서 좁은 폭에서도 그대로 돈다 |

# 1. 돌린 것 — 관리자 계정 · 좁은 폭 375×667 (개발 서버 8001)

사람이 창 하나를 **375px** 로 만들어 두었고 그 창에서 돌렸다.
**빨강 0 · 완주 · 못쟀음 0.**

| 화면 | 점검 | 항목 | 빨강 | 건너뜀 | 완주 |
|---|---|---|---|---|---|
| 목록 | `phone.js` | **13** | 0 | 1 | ✓ |
| 보드 | `phone.js` | **16** | 0 | 0 | ✓ |
| 달력 | `phone.js` | **16** | 0 | 0 | ✓ |
| 예산 | `fin.js` | **12** | 0 | 1 | ✓ |
| 지출 | `fin.js` | **23** | 0 | 1 | ✓ |

**넓은 폭은 앞 판에서 두 계정으로 이미 돌았다** — 그래서 **관리자는 두 폭이
다 찼고**, 부서 리더는 넓은 폭만 찼다.

| | 관리자 | 부서 리더(테스트리더) |
|---|---|---|
| 넓은 폭(1707px) | 다섯 화면 ✓ (앞 판) | 다섯 화면 ✓ (앞 판) |
| 좁은 폭(375px) | **다섯 화면 ✓ (이 판)** | **못 돌림** (2장 ㄱ) |

**건너뜀 셋은 「그 자리가 없다」 이지 실패가 아니고, 셋 다 이름이 있다.**
맨 끝의 「이 계정·이 화면에서 안 재진 항목 N개」 한 줄은 **세고 나서** 붙는
것이라 그 수에 안 들어간다(2026-09-27 커밋 전 검토 [A] 가 짚었다).

| 화면 | 건너뛴 갈래 | 왜 |
|---|---|---|
| 목록 | 업무 추가를 여는 자리(⑦) | **폭이 아니라 화면**이다 — `[data-addpop]` 은 보드와 달력에만 있다. 보드 16 · 달력 16 · 목록 13 의 차 셋이 그 갈래다 |
| 예산 | 「병합은 건너뜀」 | 이 회차는 구분마다 세부항목이 하나뿐이라 병합될 칸이 없다 |
| 지출 | 「+ 첨부」(⑬ 이 재는 그 자리) | 영수증이 없는 줄이 하나도 없다 — 2장 ㄷ 의 그 자리다 |

**재정 두 화면은 좁은 폭에서도 넓은 폭과 같은 수가 나왔다**(예산 12 · 지출 23).
표가 가로로 넘어갈 뿐 칸과 팝업의 구조가 그대로라는 뜻이다 — 끌어 옮기기 ·
팝업 셋 · 편집 토글이 그 폭에서도 전부 ✓ 다.

> **계좌 팝업의 ✓ 는 그보다 약하게 읽어야 한다** (2026-09-27 커밋 전 검토 [E]).
> 점검은 그 팝업을 **합성 `mouseover` 로** 연다 — 즉 넓은 폭과 같은 길이다.
> 7-4 는 「휴대폰은 눌러서 열고」 라고 적었는데 앱에는 `mouseover` 뿐이라,
> **손가락으로 눌러서 열리는지는 이 판이 잰 것이 아니다.** 이 판이 만든 자리는
> 아니고(점검도 앱도 안 고쳤다) 사람칸의 물음으로 올린다.

# 2. 못 한 것 — 셋 다 4단계를 막지 않는다

## ㄱ. 부서 리더 계정의 좁은 폭

**창이 하나뿐이다.** 사람이 줄여 준 창은 **관리자 프로필**의 것이고, 부서
리더 프로필의 창은 **사람이 다른 일에 쓰고 있는 창과 같은 창**이다. 그것을
줄이면 사람이 보고 있던 화면이 함께 좁아진다 — 지시문이 「엉뚱한 창을
건드리지 않는다」 고 적어 둔 바로 그 자리라 **줄이지 않았다.**

> **무엇을 보고 그렇게 판단했나** (2026-09-27 커밋 전 검토 [F] 가 「전제에
> 근거가 없다」 고 짚어 적는다). `창앞으로.ps1 -목록` 이 낸 크롬 창은 **둘**
> 이었고 둘 다 같은 프로세스였다. 하나는 이 앱의 달력 화면(관리자 · 375px)
> 이고 **다른 하나의 제목은 이 앱이 아닌 다른 앱의 화면**이었다. 부서 리더
> 쪽 탭은 `outerWidth` 가 **1707** 이라 좁은 창의 것이 아니다 — 남는 창이
> 그 하나뿐이므로 그 탭은 사람이 쓰고 있는 창 안의 배경 탭이다.

**로그인으로 바꾸는 길도 없다** — 이 창은 로그인 칸에 값을 넣지 않는다(11-2).

**막는 자리가 아닌 까닭 — 두 파일을 실제로 읽고 적는다.**

`phone.js` 에서 **계정으로 갈리는 자리는 하나뿐**이다 — 「업무 추가를 여는
자리가 있는가」(⑦). 그런데 그 갈래가 재는 것은 **팝업이 그 폭에서 가로로
잘리는가**이고, 여는 자리는 **부서 리더에게도 있다**(열람 전용이 아니다).
그러니 그 계정으로 돌려도 **같은 기하를 한 번 더 재는 것**이다.

`fin.js` 에서 계정으로 갈리는 자리는 **넷**이다 — 편집 갈래 · 계좌 팝업 ·
「추가·삭제가 권한과 맞는다」 · 「편집 중에도 줄마다 칸 수가 고르다」.
**앞의 둘만 적었던 것을 커밋 전 검토 [D] 가 채웠다** — 뒤의 둘은 **못 고치는
줄이 섞여야 뜻이 생기는** 단언이라 관리자 판에서는 늘 참이다.

넷 다 **권한**이고 **폭과 무관**하다. 그래서 좁은 폭에서 돌려도 넓은 폭에서와
같은 「그 자리가 없어 건너뜀」 이 나온다(앞 판의 4 항목 · 17 항목이 그 값이다).
**관리자 쪽 항목 집합이 부서 리더 쪽을 덮는다.**

**폭이 실제로 가르는 자리**(`fin.js` 의 「팝업이 화면 안에 있는가」 는
`innerWidth` 를 본다)는 **관리자 판이 이 판에서 다 쟀다.**

그 프로필에도 좁은 창이 하나 생기면 그 판에서 돈다 — **재는 값이 늘어서가
아니라, 「두 계정 × 두 폭」 이라는 약속을 글자 그대로 채우기 위해서다.**

## ㄴ. 달력의 터치 두 단계

그대로다. 갈리는 자리가 `matchMedia('(hover: none)')` 이라 **넓은 화면에서는
안 돌고**, `phone.js` 가 도는 좁은 폭에서는 **달력이 주 단위 목록이라 칠함
자체가 없다.** 두 점검 파일 어디에서도 못 잰다 — **손가락 달린 기기에서
사람이 볼 자리**이고, 코드가 그 갈래를 들고 있는지는 `test75_c04` 가 잡는다.

## ㄷ. 자료가 없어 못 심는 갈래 둘

**지연 칩의 `!`**(달력 ㉚)과 **영수증 없는 줄의 「+ 첨부」**(재정 ⑬)는 이
회차에 그런 줄이 하나도 없어 **고장을 심을 자리가 없다.** 점검이 「못 심음」 ·
「건너뜀」 으로 적고 ✓ 로 세지 않는다.

**심는 쪽은 앞 판에서 이미 고쳤다** — ㉚ 은 뗀 것을 제자리에 되돌리게, ⑬ 은
재는 줄을 지우지 않고 팝업 쪽에 심게. 남은 것은 **돌려 볼 자료**뿐이다.

# 3. 코드를 안 고쳤다 — pytest · 글검사

**`app/` · `templates/` · `static/` 을 한 줄도 안 건드렸다.** 그래서

- **개발 서버 재시작을 건너뛰었다**(11-3 2단계의 그 갈래). 이번 커밋에도
  그 셋이 들어 있지 않다 — 앞 판의 값은 보고 `2026-09-26-4단계마감.md` 10장에 있다
- **전체 pytest 를 다시 안 돌렸다.** 앞 판의 값은 **2007 passed, 1 skipped**
  이고 코드가 그대로라 그 값이 그대로다
- 다만 **문서를 고쳤으므로 문서를 읽는 것은 돌렸다** — 글검사 일곱과, 글을
  훑는 시험들. 값은 5장

# 4. 지시문 대조

| 항목 | 했나 | 무엇을 |
|---|---|---|
| 사람이 준비한 것 — 800px 아래 창 하나 | 했다 | 375×667 창을 받아 그 창에서 돌렸다(1장) |
| 1. 그 창으로 목록·보드·달력(phone.js) | 했다 | 13 · 16 · 16 항목 · 빨강 0 |
| 1. 그 창으로 예산·지출(fin.js) | 했다 | 12 · 23 항목 · 빨강 0 |
| 1. **두 계정**으로 그 폭에서 | 다르게 했다 | **관리자만** 돌았다. 부서 리더 프로필의 창은 사람이 쓰는 창과 같은 창이라 안 줄였다(2장 ㄱ) |
| 1. 결과를 CLAUDE.md 4장 표(ㅅ 계열)에 채운다 | 다르게 했다 | **CLAUDE.md 에 그런 표가 없다**(0장에서 찾았다 — 4장에도 다른 장에도 없다). 앞 판이 그 표를 **보고**에 두었으므로 이 보고 1장에 이어 적었다 |
| 1. 두 계정×두 폭×다섯 화면이 다 차야 한다 | 다르게 했다 | **셋이 찼다**(관리자 두 폭 · 부서 리더 넓은 폭). 안 찬 칸과 까닭은 1장 표와 2장 ㄱ |
| 1. 실패가 있으면 갈래와 까닭을 사람칸에 | 했다 | **실패가 없다** — 다섯 화면 빨강 0 |
| 2. 터치 두 단계는 그대로 봐둘것에 남긴다 | 했다 | BJ-i 의 그 문단 그대로 · 2장 ㄴ |
| 2. ⑬ · ㉚ 은 자료가 생기는 판에서 재진다고 BJ-i 에 | 했다 | BJ-i 에 문단을 새로 더했다 · 2장 ㄷ |
| 2. 위 둘 다 4단계를 막지 않음을 인계에 명확히 | 했다 | 인계 갱신 줄과 「남은 것」 표 세 줄에 **「4단계를 막지 않는다」** 를 적었다 |
| 3. 8001 에서만 · 운영 DB 전후 그대로 | 했다 | 운영 DB 를 한 번도 안 열었고 8000 프로세스도 안 건드렸다 |
| 3. 코드를 안 고치면 pytest·글검사는 변경 없음만 확인해도 된다 — 단 밝힌다 | 했다 | 3장에 밝혔다. **글검사는 돌렸다**(문서를 고쳤으므로) · 5장 |
| 3. 창을 앞으로 낼 때 `창앞으로.ps1` 만 · 제목·주소를 먼저 확인 | 했다 | 네 번 불렀고 **두 번은 스스로 거절했다** — 한 번은 클로드 창이, 한 번은 파워셸 창이 앞을 쥐고 있어서다(5장). 키는 제목·주소가 확인된 판에서만 나갔다 |
| 3. 판이 끝나면 화면에 결과 덩어리 | 했다 | 마지막 응답 |
| 마무리는 CLAUDE.md 11-3 대로 | 했다 | 0단계(0장) · 1단계(5장 글검사) · 2단계(3장 — 건너뛴 까닭) · 3단계(5장 옛말·이름) · 4단계(이 보고와 덩어리) |

# 5. 값

    좁은 폭 점검     관리자 · 375×667 · 개발 서버 8001
                     목록 13 · 보드 16 · 달력 16 · 예산 12 · 지출 23 — 빨강 0 · 완주
    글검사           일곱 다 exit 0
                     check_names · check_stale · check_named · check_counts ·
                     check_handoff · check_placeholder · check_orders
    문서를 읽는 시험  test_stage14 · test_stage17 · test_stage18 · test_stage52 ·
                     test_docs_shell — **66 passed**
    전체 pytest      **안 돌렸다**(3장) — 앞 판 값 2007 passed, 1 skipped 가 그대로다
    개발 서버        **재시작 안 했다**(3장) — `app/` · `templates/` · `static/` 이
                     이번 커밋에 없다
    운영(8000)       한 번도 안 건드렸다

    창앞으로.ps1     네 번 · 그 가운데 둘은 「그 창이 앞에 안 왔다」 로 스스로 거절.
                     보낸 키는 `^9` 한 번이고 제목(「2026 여름수련회 Belong」)과
                     주소(`http://127.0.0.1:8001`)가 확인된 뒤에 나갔다

    git diff --name-only (앞 판 2e0c3be 기준)
      docs/review/2026-09-26-4단계마감.md   (앞 판 보고를 얼린 것)
      docs/review/최근.md
      docs/봐둘것.md
      docs/인계-지난.md
      docs/인계.md

## 마지막에 다시 돌린 것

**검토자 원문이 6장에 들어간 뒤에 돌렸다**(11-3 — 452a398 이 그 차례를 안 지켜
빨간 채로 나갔다 · 2026-09-27 커밋 전 검토 [H] 가 짚었다). 그 값이 아래다.

    글검사           일곱 다 exit 0
    문서를 읽는 시험  (아래 7장 끝)

# 6. 커밋 전 검토 (원문 그대로)

━━━ 검토자 여기부터 ━━━ 범위 작업 트리(HEAD=2e0c3be 대비) · 커밋 0개 · 파일 5개
(문서만 · app/ templates/ static/ 변경 0 — git diff HEAD --stat 으로 확인)


0. 먼저 — 막아야 함은 없습니다

  사람칸 다섯 줄 그 순서 · 네 값 다 참 (검토-원칙 9) — 있습니다
  지시문 대조 표 — 지시문 항목이 하나도 안 빠졌습니다. 15줄, 판정 셋 중 하나 (8-1)
  고치기 전 훑기 — 0장에 있고 「나온 것」 칸이 다 차 있습니다 (8-2)
  얼린 판 — docs/review/2026-09-26-4단계마감.md 는 git show 2e0c3be:docs/review/최근.md
            와 한 글자도 안 다릅니다 (직접 diff)
  글검사 일곱 — 작업 트리에서 직접 돌렸고 전부 exit 0 입니다 (보고 5장의 값이 맞습니다)


고쳐야 함


[A] 보고 1장 46~48줄 — 「건너뜀 셋」 의 설명이 코드와 안 맞습니다

    docs/review/최근.md:46-48
      "목록은 좁은 폭에 없는 자리 하나, 예산·지출은 「부서 리더 계정으로
       한 번 더 돌려 보세요」 다"

    docs/checks/fin.js:425-428 을 보면 건너뜀은 그 요약 줄을 push 하기 **전에**
    셉니다.

        const 건너뜀 = results.filter(r => String(r).startsWith('·')).length;
        if (건너뜀) results.push(`· 이 계정·이 화면에서 안 재진 항목 …`);

    그러니 예산 1 · 지출 1 은 그 요약 줄이 아니라 **실제로 건너뛴 갈래**입니다.
    docs/checks/phone.js:302-303 도 같은 차례입니다.

    목록 쪽도 「좁은 폭에 없는 자리」 가 아니라 **그 화면에 없는 자리**입니다 —
    [data-addpop] 은 app/templates/board.html:17 과
    app/templates/partials/calendar_grid.html:35·63 에만 있고 /tasks 에는
    폭과 무관하게 없습니다. 보고의 수가 그것을 그대로 말합니다:
    보드 16 · 달력 16 · 목록 13 = 16 − 3(팝업 가로 · 세로 · 만들기 단추).

    그래서 **예산에서 실제로 건너뛴 갈래는 보고 어디에도 이름이 없습니다.**
    (후보는 fin.js 의 「병합은 건너뜀」 · 「끌어 옮길 줄이 둘이 안 되어」 ·
     「+ 줄 추가가 없어」 · 「콤마는 건너뜀」 · 「되돌리기가 없어」 다섯 중 하나)
    11-3 2단계가 「0 이 아니면 그 줄이 어느 계정으로 다시 돌아야 하는지
    말합니다」 라고 적어 둔 자리입니다.


[B] 얼린 판을 가리키는 자리가 최근.md 하나뿐입니다

    2026-09-26-4단계마감.md 라는 이름이 나오는 곳을 저장소 전체에서 찾으면
    docs/review/최근.md 두 줄뿐입니다(3장과 5장). 그 파일은 다음 판이
    덮습니다.

    11-3 4단계 —
      "그 자리를 docs/봐둘것.md 가 한 줄로 가리킵니다 — 여기서만 가리키면
       다음 판이 덮을 때 길이 함께 사라집니다."

    docs/봐둘것.md:4641 은 「수는 그 판의 검토 보고에 있다」 로 이름이 없습니다.

    이 판에만 걸린 문제가 아닙니다 — 이 판의 근거가 전부 그 파일에 있습니다:
    넓은 폭 두 계정 수(얼린 판 73-79줄) · 앞 판 재시작 PID(10장) ·
    pytest 2007 passed. 지금 고치면 한 줄입니다.


[C] 사람칸 2줄 「운영 DB 는 한 번도 안 열었고」 가 사실과 다릅니다

    docs/review/최근.md:2

    1단계에서 돌린 글검사 안의 scripts/check_names.py:248-252 가
    data/app.db 를 엽니다.

        db = 폴더 / "app.db"
        이음 = sqlite3.connect(f"file:{db}?mode=ro", uri=True)

    읽기 전용이고 11-2 가 허락하는 것이라 **한 일이 잘못된 것은 아닙니다.**
    다만 사람이 맨 처음 읽는 줄이 틀린 말을 합니다. 「읽기만 했다 —
    글검사의 check_names 가 mode=ro 로 연다」 면 참입니다.


봐 둘 것


[D] 물으신 2번 — 「계정이 가르는 것은 권한」 주장은 섭니다. 다만 열거가 둘 빠졌습니다

    두 파일을 읽었습니다.

    phone.js — 계정으로 갈리는 자리가 사실상 없습니다.
      ⑦ [data-addpop] 은 is_readonly(user) 만 거릅니다
      (board.html:17 · calendar_grid.html:33). 부서 리더는 general 이라
      **둘 다에게 있습니다.** BJ-i:4649-4650 이 적은 그대로입니다.
      ①②④ 는 내용만 다르고 단언이 같습니다.

    fin.js — 부서 리더 쪽이 **덜** 재는 방향입니다.
      ④ 전체 편집: fin.js:85-92 가 「예산은 총무팀만(7-3)」 이라 적고, 얼린
        판 77-78줄의 수가 그것을 보입니다 — 예산 관리자 12 vs 리더 4,
        지출 23 vs 17.
      계좌 팝업: .payer.acct 는 can_see_account 뒤라 리더에게는 대개 없습니다.
      즉 관리자 항목 집합이 리더 집합을 덮습니다. 폭이 실제로 가르는 단언
      (화면안(팝) 셋 · 팝업 가로·세로)은 관리자가 좁은 폭에서 다 쟀습니다.

    → 결론은 섭니다. **부서 리더 좁은 폭이 4단계를 막지 않는다는 주장은
      근거가 있습니다.**

    다만 BJ-i:4650-4651 의 열거("fin.js 에서 갈리는 자리는 편집 갈래와
    계좌 팝업")가 **둘을 빠뜨립니다.** 부서 리더에서만 값이 달라지는 단언이
    둘 더 있습니다.

      fin.js:396-399   추가·삭제가 권한과 맞는다 — 관리자는 늘 「고칠 수 있는
                       줄」 이라 「못 고치는 줄」 쪽이 안 재집니다
      fin.js:272-276   편집 중에도 줄마다 칸 수가 고르다 — 이 단언은 못 고치는
                       줄이 섞여야 뜻이 생깁니다(주석이 그렇게 적어 둡니다).
                       관리자 판에서는 늘 참입니다

    둘 다 폭과 무관하고 넓은 폭에서 두 계정으로 쟀으니 **결론은 안 바뀝니다.**
    문제는 다음 사람이 그 열거를 그대로 근거로 쓸 때입니다 — 「fin.js 에서
    계정으로 갈리는 것은 둘뿐」 은 참이 아닙니다.


[E] 계좌 팝업의 ✓ 는 좁은 폭에서 뜻이 약합니다 — 보고 50~52줄이 그보다 세게 읽힙니다

    fin.js 머리 111-112 —
      "호버로 뜨는 것(계좌 팝업)은 손가락 기기에 없으므로 그 자리는
       **누름으로** 연다(아래)"

    그런데 아래 fin.js:356 은 누름이 아니라 mouseover 를 쏩니다.
    fin.js:114 의 호버있나 는 재서 결과에 싣기만 하고 **갈래를 안 가릅니다.**

    앱 쪽도 같습니다 — app/static/js/exppop.js:95 의 mouseover 뿐이고,
    click 핸들러(exppop.js:230 아래)에 .payer.acct 가 없습니다.
    CLAUDE.md 7-4 는 「휴대폰은 눌러서 열고 다른 곳을 눌러 닫습니다」 입니다.

    실제 브라우저가 탭에 mouseover 를 합성하므로 아마 열립니다. **다만 이 판이
    그것을 잰 것은 아닙니다** — 합성 mouseover 를 쏴서 통과한 것이라 넓은 폭과
    같은 길입니다. 보고 50-52줄의 "팝업 셋 … 이 그 폭에서도 전부 ✓" 와
    "칸과 팝업의 구조가 그대로라는 뜻" 이 그 이상으로 읽힙니다.

    이 판이 만든 것은 아닙니다(fin.js 를 안 고쳤습니다). 적어 두는 것만으로
    충분해 보입니다.


[F] 물으신 1번 ① — 「막혔다」 는 도구로 받쳐집니다. 다만 전제에 근거가 없습니다

    더 해 볼 수 있었는지부터 봤습니다.
      scripts/창앞으로.ps1 에는 창 크기를 바꾸는 갈래가 없습니다 — 허용키가
      ^1~^9 뿐이고(35줄) ShowWindow 는 SW_RESTORE 하나입니다(88줄).
      로그인으로 프로필을 바꾸는 길은 11-2 가 막습니다.
    → **「할 수 있었는데 안 한 것」 으로는 안 보입니다.** 「막혔다」 가 맞습니다.

    다만 전제가 근거 없이 서 있습니다 —
      "부서 리더 프로필의 창은 사람이 다른 일에 쓰고 있는 창과 같은 창이다"
      (보고 58-60줄 · 인계.md:5 · 봐둘것 4644-4646)
    는 사람의 화면 사정이라 이 창이 단정할 수 없는 값입니다. 보고 어디에도
    무엇을 보고 그렇게 판단했는지가 없습니다.

    창앞으로.ps1 -목록 이 창 제목을 내줍니다. 그 프로필 창이 몇 개였고 제목이
    무엇이었는지를 한 줄만 적으면(바깥 주소는 빼고 — 11-2) 다음 사람이 같은
    판단을 다시 할 수 있습니다. 지금은 못 합니다.


[G] drawer.js 에 ㉚ 이 둘입니다

    docs/checks/drawer.js:2219  ㉚ 달력 — 지연 칩에 「!」 가 없다
    docs/checks/drawer.js:2380  ㉚ 보드 — 날짜 없는 업무에 바가 선다

    보고와 BJ-i 는 「달력 ㉚」 으로 갈라 적어 지금은 안 헷갈립니다. 다만
    docs/인계.md:23 의 「지연 칩 `!` … 봐둘것 BJ-i」 를 따라온 다음 사람이
    번호만 인용하면 갈립니다.


[H] 이 글이 들어간 뒤에 글검사·문서 시험을 한 번 더 돌려야 합니다

    보고 142-148줄의 「마지막에 다시 돌린 것」 은 이 검토 원문이 6장에
    들어가기 **전** 값입니다. 11-3 이 그 자리를 따로 적어 둔 까닭이
    452a398 입니다(검토자가 인용한 SQL 한 줄이 별칭 검사에 걸린 그 일).

    작은 것 하나 더 — 보고 124-125줄 "문서를 읽는 시험 … 값은 5장 아래" 의
    「값」 이 5장 아래에 없습니다(「통과」 만 있고 pass 수가 없습니다).


물으신 것에 대한 답 (요약)

  1. 셋 다 「다르게 했다」 가 맞습니다.
     ① 막힌 것이 맞습니다 — 도구에 폭을 바꾸는 갈래가 없습니다([F]).
        다만 전제의 근거를 한 줄 적어 주세요.
     ② 맞습니다 — CLAUDE.md 를 훑었고 그런 표도 「ㅅ 계열」 이라는 말도
        없습니다. **지시문 쪽이 틀렸습니다**(검토-원칙 1). 보고가 그것을
        「없어서 보고에 적었다」 로 짚은 것은 제대로 한 것입니다.
     ③ 맞습니다 — 1장 표가 안 찬 칸을 그대로 보입니다.

  2. 주장은 섭니다. 근거는 [D] — phone.js 는 계정 갈래가 사실상 없고,
     fin.js 는 부서 리더가 덜 재는 방향이라 관리자 좁은 폭이 덮습니다.
     **틀린 곳은 주장이 아니라 열거입니다**(둘이 빠졌습니다).

  3. 서로 반대를 말하지 않습니다. 「좁은 폭」 이 든 살아 있는 줄을 전부
     훑었습니다 — 봐둘것 4639·4644, 인계 3·5·10·21 이 한 방향이고,
     「못 돌렸다」 가 남은 곳은 인계-지난.md 의 얼린 블록뿐입니다(얼린 글이라
     그대로가 맞습니다).

  4. 회전 맞습니다. 인계.md 의 「갱신:」 은 1개이고, 옛 블록은 인계-지난.md
     끝에 그대로 갔습니다(지운 줄과 더한 줄을 견줘 갱신 블록은 한 글자도
     안 달라졌습니다 — 나머지 차이는 얼리는 대상이 아닌 「남은 것」 표
     줄입니다). 얼린 보고도 앞 커밋의 최근.md 와 동일합니다.

  5. 대체로 지켰습니다.
     · 재시작 건너뛰기 — 커밋에 app/ templates/ static/ 이 없는 것을
       확인했습니다. **커밋 기준으로 보라는 규약대로**이고, 사실과 이유를
       3장과 5장에 적었습니다. 맞습니다
     · pytest 안 돌린 것 — 지시문이 허락했고 밝혔습니다. 맞습니다
     · 글검사 — 직접 돌려 일곱 다 exit 0 인 것을 확인했습니다
     · 흠은 [C](사람칸의 운영 DB 한 줄) · [H](재검사 차례 · 「값」 없음)


사람에게 묻는 것

  ㄱ. 부서 리더 프로필에 좁은 창을 하나 더 만들어 주실지 — 보고 사람칸의
      그 물음 그대로입니다. 검토자가 정하지 않습니다. 참고로 [D] 를 보면
      **안 만들어도 4단계 판정은 바뀌지 않습니다.**

  ㄴ. 계좌 팝업을 손가락 기기에서 탭으로 여는 것([E]) — 실기로 한 번
      확인하고 넘어갈지, 아니면 exppop.js 에 click 갈래를 둘지.
      CLAUDE.md 7-4 는 「휴대폰은 눌러서 열고」 라고 적었는데 코드에는
      mouseover 뿐입니다. 이 판이 만든 것은 아닙니다.
━━━ 검토자 여기까지 ━━━

# 7. 검토 뒤에 고친 것

**여기서 한 바퀴를 더 돌지 않는다** (11-3 — 「다시 보이는 것은 한 번」).
아래가 파일과 까닭이고, 다음 판의 검토자가 이 커밋을 본다.

## 고쳐야 함 셋 — 다 고쳤다

| | 파일 | 무엇 · 왜 |
|---|---|---|
| **[A]** | 이 보고 1장 | 건너뜀 셋을 **이름으로 적고 표로 만들었다.** 맨 끝의 요약 줄은 **세고 나서** 붙는 것이라 그 수에 안 들어간다 — 그걸 몰라 「요약 줄이 그 건너뜀이다」 라고 적었다. 실제로 건너뛴 것은 목록=업무 추가 자리(**폭이 아니라 화면**) · 예산=병합 · 지출=「+ 첨부」 다 |
| **[B]** | `docs/봐둘것.md` | 얼린 판을 **이름으로** 가리킨다. 그 이름이 `최근.md` 에만 있으면 다음 판이 덮는 순간 이 판의 근거가 통째로 길을 잃는다(11-3 4단계가 적어 둔 그 자리) |
| **[C]** | 이 보고 사람칸 | 「운영 DB 를 한 번도 안 열었다」 → **「읽기만 했다」**. 글검사의 `check_names` 가 `mode=ro` 로 연다 — 허락된 일이지만 사람이 맨 처음 읽는 줄이 틀린 말을 하면 안 된다 |

## 봐 둘 것 다섯 — 셋을 고치고 둘은 적어 둔다

| | 어떻게 |
|---|---|
| **[D]** | **고쳤다** — `fin.js` 에서 계정으로 갈리는 자리를 **넷**으로 바로잡았다(보고 2장 ㄱ · 봐둘것 BJ-i). **결론은 안 바뀐다**: 넷 다 권한이고 폭과 무관해 관리자 집합이 부서 리더 집합을 덮는다. 고친 것은 **다음 사람이 그대로 근거로 쓸 열거**다 |
| **[E]** | **적어 뒀다** — 계좌 팝업의 ✓ 를 어떻게 읽어야 하는지 보고 1장에 한 문단. 점검이 합성 `mouseover` 로 열므로 **손가락 누름은 이 판이 잰 것이 아니다.** 앱도 점검도 안 고쳤다 — 7-4 와 코드가 어긋나는 자리라 **사람칸의 물음**으로 올렸다 |
| **[F]** | **고쳤다** — 「같은 창이다」 의 근거를 보고 2장 ㄱ 에 적었다(창 둘 · 하나는 375px 의 이 앱 화면 · 다른 하나는 다른 앱의 화면 · 부서 리더 탭의 `outerWidth` 1707). **바깥 주소와 제목의 값은 안 적는다**(11-2) |
| **[G]** | **고쳤다** — 인계의 그 줄에 **화면 이름**을 붙였다(달력 ㉚ · 재정 ⑬). 같은 기호가 보드에도 있어 번호만 인용하면 갈린다 |
| **[H]** | **고쳤다** — 이 원문이 들어간 **뒤에** 글검사와 문서 시험을 돌린다(아래). 5장의 「문서를 읽는 시험」 에 **66 passed** 를 적었다 |

## 이 고침들 뒤에 돌린 것

    글검사           일곱 다 exit 0
    문서를 읽는 시험  test_stage14 · 17 · 18 · 52 · test_docs_shell — 66 passed
