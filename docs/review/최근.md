사람이 할 것
노트북에서 직접: 총무님이 `data/업무대조-확인용.real.xlsx` 를 더블클릭으로 열고 노란 칸에서 골라 저장(3장 · 인계 맨 위)
관리자 권한: 없음 — `app/` 을 안 고쳐 재시작하지 않았다(5장)
정할 것: 봐둘것 BC-b 넷째(덤이 제목을 이길 수 있다는 성질에 시험이 없다)를 「적어만 둔다」 로 둘지 — 앞 판에서는 「정할 것」 이었다(검토 [L] 11). 나머지 셋(A 자동 확정 · included 6 뺌 · 덤과 문턱 그대로)은 이 판이 반영했다
막혀서 못 한 것: 화면 점검(drawer.js · phone.js) — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다. 화면을 안 고친 판이라 급하지 않다(5장)

작성 시각: 2026-09-16 04:10 KST
커밋: 5758227 (앞 판 얼림) · 4d20dd9 (확인표 모듈 · 시험) · dd2a7a5 (보고 · 문서) · 검토 뒤 고침은 그다음 커밋
작업: 업무 대조 확인용 파일을 보기 편하게 · 정할 것 셋 확정

---

**값은 여기 없다.** 제목 · 날짜 · 담당은 저장소 밖 `data/업무대조.real.md` · `data/업무대조-확인용.real.md` ·
`data/업무대조-확인용.real.xlsx` 에만 있다(`data/*.real.*` · gitignore). 여기에는 건수 · 구조만 적는다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 확인용 표를 만들던 자리가 실행기(`data/업무대조.real.py`) 안에 있었다 — 새 모듈로 옮기면 그 한 곳만 바뀐다. 짝짓기 규칙(`scripts/노션업무짝짓기.py`)은 안 건드린다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면은 안 고친다 |
| 그 값을 세어 둔 문서 | 인계 맨 위와 1)(앱만 20 · A 51 이 아니라 27) · 봐둘것 BC-b(정할 것 넷 중 셋) · CLAUDE.md 14장 표 · 앞 판 보고(얼린다) |

# 1. 등급 A 를 자동 확정 쪽으로 (1a · 1b)

- **1a 이 판에서는 반영하지 않는다** — 표시만 해 뒀다. 상세 파일의 A 목록 제목이 「자동으로 믿는 짝 (사람이 정함 ① · 다음 판이 확인 없이 잇는다)」 이고, 인계 1) 과 6장 갈래가 다음 판의 몫으로 적는다
- **1b 확인용 표에서 A 를 뺐다** — 총무님이 보는 장은 「1. 짝 확인(B · C 55줄)」 과 「2. 노션에만(151줄)」 둘이고, A 27 은 **「3. 자동 확정 — 참고용, 표시하지 않으셔도 됩니다」** 로 따로 간다. 그 장에는 표시 칸이 아예 없다(`test56_a01`)

# 2. included 꺼진 run 을 뺐다 (2a–2c)

| 셈 | 앞 판 | 이번 |
|---|---|---|
| 앱 run | 102 (빼 둔 6 포함) | **96** |
| 앱만 | 20 | **14** |
| 짝 전체 | 82 | 82 |
| 등급 A · B · C | 27 · 54 · 1 | 27 · 54 · 1 |
| 노션만 | 151 | 151 |
| 어긋난 짝 | 45 (구분 33 · 담당 13 · 시작일 6) | 45 (구분 33 · 담당 13 · **시작일 7**) |

**짝 수와 등급은 그대로인데 짝 하나가 갈렸다** — 빼 둔 run 과 이어져 있던 노션 줄 하나가 다른 run 과 이어졌다(그 자리 하나 때문에 시작일 어긋남이 6 → 7). 빼기 전후를 같은 규칙으로 돌려 **바뀐 자리가 1 임을 세어 확인**했다(값은 안 적는다).

**노션만 151 은 그대로다**(2c) — 노션 쪽 후보(233)와 짝(82)이 안 변했다.

상세 파일에 **「대조에서 뺀 run」** 장을 따로 뒀다 — 빼 둔 6 이 어디로 갔는지 안 보이면 「앱만이 왜 줄었지」 가 된다.

# 3. 확인용 파일을 보기 편하게 (3a–3e)

- **3a 표의 정본은 `.md`** 그대로다 — 어떤 줄이 어떤 열쇠로 서는가는 이 글이 말한다. **다만 표시가 적히는 자리는 엑셀이다**(총무님이 여는 것이 그 파일) — 표시에 한해서는 엑셀이 유일한 출처이고, 다음 판은 엑셀을 읽는다(검토 [M] 6)
- **3b 같은 내용을 엑셀로 함께** 낸다(`data/업무대조-확인용.real.xlsx`). **결정 칸만** 고르기(드롭다운)이고 노란 바탕이다 — 끝 칸으로 짚으면 노션만 표의 메모 칸에 걸린다(검토 [H] 1) — 셀을 누르면 「맞다 / 아니다 / 모르겠다」(짝 확인) · 「넣음 / 안 넣음 / 버림」(노션만)에서 고르기만 하면 된다
- **3c 열쇠 칸(노션 줄 · run id)은 숨겼다** — 총무님이 건드릴 자리가 아니다. 숨긴 것이지 뺀 것이 아니라 다음 판이 그대로 되읽는다(`test56_b01`)
- **3d 저장소 밖** — `.gitignore` 의 `data/*.real.*` 가 `.xlsx` 도 덮는다(확장자마다 적지 않기로 한 그 줄). `git status` 에 안 잡히는 것을 7장에서 확인했다
- **3e 엑셀에 적은 표시를 읽어 `.md` 와 같은 답인지** 시험이 잰다(`test56_b02` · 수용 기준 라). 실행기도 낼 때마다 `견준다` 로 두 파일이 같은 말을 하는지 세고 그 수를 찍는다 — 이번 실행에서 **다른 자리 0**

읽고 쓰는 곳은 `scripts/업무대조확인표.py` 하나다 — `md로` · `md읽기` · `엑셀로` · `엑셀읽기` · `견준다`. 값은 스스로 안 읽고 실행기가 준 줄만 다룬다.

# 4. 총무님이 여시는 방법 (4a · 4b)

- **4a 인계 맨 위**에 적었다 — 더블클릭으로 열고 · 노란 칸에서 고르고 · 저장. 빈 칸은 다음 판이 「모르겠다」 로 본다. 「자동 확정(참고)」 장은 표시할 것이 없다
- **4b 경로는 사람칸**에 `data/업무대조-확인용.real.xlsx` 로 적었다

# 5. 이번 판이 안 한 것

- **앱에 아무것도 반영하지 않았다** — 코드 diff 에 `app/` 이 없다(7장). 운영 DB 는 `mode=ro`, 노션은 안 열었다
- **재시작 안 함** — `app/` · `templates/` · `static/` 변경 없음. 운영 서버 PID 19156 그대로
- **drawer.js · phone.js 못 돌림** — 로그인이 필요하다(11-2). 화면을 안 고친 판이라 급하지 않다

# 6. 다음 판

총무님 표시를 받아 **실제로 잇는 판**이다.

- 등급 A 27 은 확인 없이 확정(사람이 정함 ①)
- 짝 확인 55줄은 「맞다」 만 잇고, 「아니다」 는 노션만/앱만으로 갈라 두고, 「모르겠다」 · 빈 칸은 그대로 둔다
- 노션만 151줄은 「넣음」 만 새로 세우고(ㄱ2), 「안 넣음」 은 묶인 곳을 적은 채 기록으로, 「버림」 은 손대지 않는다
- 그때 정할 것 — 앱 run 에 노션 줄(또는 페이지 id)을 담을 칸을 어디에 둘지 · 「원본에 없음」 을 칸으로 둘지 상태로 둘지(인계 1) 의 옛 미정)

# 7. 검사와 시험

- 커밋 전 `git status --short`: 코드 · 시험 · 문서만 — `data/` 안의 것(값 파일 · 실행기 · 새 `.xlsx`)은 안 잡혔다
- 글검사: 첫 커밋 전 7/7(얼린 판의 한 줄이 세는 말 검사에 걸려 `docs/세어둠.txt` 에 한 줄) · 검토 뒤 고침 커밋 전 7/7 · 두 번째 검토 뒤 고침 커밋 전 7/7
- 새 시험: `tests/test_stage56.py` 11 통과(첫 커밋 10 → 검토 [H] 2 뒤 가드 갈래 하나) · 앞 판 `tests/test_stage55.py` 15 그대로
- 바뀐 파일(`git diff --name-only 559c4b7..HEAD`): `CLAUDE.md` · `docs/review/2026-09-16-업무대조1판.md` · `docs/review/최근.md` · `docs/봐둘것.md` · `docs/세어둠.txt` · `docs/옛말.md` · `docs/인계.md` · `scripts/업무대조확인표.py` · `tests/test_stage56.py`
- 보고를 다 채운 뒤 돌린 pytest: <!-- 최종 -->

# 8. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) A 는 별도 장 · 총무님 표에는 B · C 55 만 | O | 1장 · `test56_a01` |
| 나) 앱 96 · 앱만 14 · 짝과 등급 그대로 | O | 2장 표 |
| 다) 엑셀에 고르기 칸 · `.md` 와 내용 일치 | O | 3장(`견준다` 다른 자리 0) · `test56_b01` |
| 라) 엑셀 표시를 읽어 `.md` 와 같은 결과 | O | `test56_b02` · `b04`(고장을 심어 잼) |
| 마) pytest 옵션 없이 · 글검사 7/7 | 마지막 pytest 뒤 채움 | 7장 |
| 바) 검토자 원문 | O — 첫 검토 · 두 번째 검토 둘 다 | 9장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 1a | 했다 | 1장 — 이 판은 반영 안 함 |
| 1b | 했다 | 1장 · `test56_a01` |
| 2a | 했다 | 2장 표 |
| 2b | 했다 | 2장 — 짝 82 · 등급 그대로. 짝 하나가 갈린 것과 시작일 6 → 7 을 함께 적었다 |
| 2c | 했다 | 2장 — 노션만 151 그대로 |
| 3a | 했다 | 3장 |
| 3b | 했다 | 3장 · `test56_b01` |
| 3c | 했다 | 3장 — 숨김(뺀 것이 아니다) |
| 3d | 했다 | 3장 · 7장 `git status` |
| 3e | 했다 | `test56_b02` · 실행기의 `견준다` |
| 4a | 했다 | 인계 맨 위 |
| 4b | 했다 | 사람칸 |
| ③ 덤 값 · 문턱은 지금 값 그대로 · 기록만 | 했다 | 값을 안 건드렸다 · 봐둘것 BC-b 셋째 줄에 「지금 값 그대로 쓰기로 정함(2026-09-16)」 |
| 값은 보고 · 덩어리에 안 싣는다 | 했다 | 보고 머리 · 검토자가 값 누출 0 을 따로 봤다(9장) |
| 앱에 반영 없음 · 운영 · 노션 읽기만 | 했다 | 5장 |
| 마무리는 11-3 대로 | 했다 | 7 · 9장 |

# 9. 검토자 원문

## 9-1. 첫 검토 (범위 559c4b7..dd2a7a5)

```
━━━ 검토자 여기부터 ━━━ 범위 559c4b7..dd2a7a5 · 커밋 3개 · 파일 7개

먼저 재어 본 것 (전부 읽기만 · 운영 DB 는 mode=ro)

  셈 독립 재현 — 짝짓기를 제가 따로 돌려 보고의 수와 견줬습니다
    앱전부 102 · included 96 · 뺀 것 6 · 짝 82 · A 27 · B 54 · C 1
    노션만 151 · 앱만 14 · 어긋난 짝 45 (구분 33 · 담당 13 · 시작일 7)
    빼기 전으로 돌리면 앱만 20 · 시작일 6 — 보고 2장 표와 한 자리도 안 다릅니다
  「짝 하나가 갈렸다」 는 사실입니다 — 양쪽 다 짝지어졌는데 상대가 달라진 노션 줄이
    정확히 1 이고, 그 줄이 빼 둔 run 과 이어져 있던 바로 그 줄입니다(빼기 전에만 짝 0 ·
    뺀 뒤에만 짝 0). 시작일 6 → 7 의 출처가 그 한 자리인 것도 맞습니다
  md ↔ 엑셀 내용 — 세 장 전부 줄 수(55 / 151 / 27)가 같고 칸 값이 한 줄도 안 다릅니다
  숨김 실제로 걸림 — 짝 장 D·F · 노션만 장 B · 자동확정 장 B·D. 자동확정 장에는
    고르기가 없고 표시 칸 자체가 없습니다
  엑셀 읽는 길 — 고장을 여섯 심어 봤습니다(저장소 파일은 안 고치고 메모리에서만).
    원본은 열 갈래 전부 초록, 아래 넷은 제대로 빨개집니다
      엑셀읽기 열쇠를 번호 칸으로     -> b02 · b04 빨감
      md읽기 표시 칸을 한 칸 왼쪽으로 -> a02 · b02 · b04 빨감
      숨길칸을 비움                   -> b01 빨감
      참고장에도 고르기               -> b01 빨감
      견준다가 표시 차이를 안 봄      -> b03 · b04 빨감
    -> 다음 판이 엑셀을 읽는 길 자체는 시험이 실제로 재고 있습니다. 한 갈래만 빼고 —
       아래 [H] 1
  시험 값이 운영 자료에서 온 것이 아님 — 지어낸 여섯 제목이 노션 tsv 에도 앱 title 에도
    없습니다. BC-a 가 겪은 그 입구(시험 값을 실제 목록 옆에 두고 베낌)는 이번엔 막혔습니다
  글검사 7/7 통과(제가 돌림) · git status 깨끗 · .xlsx 는 .gitignore:29 의 data/*.real.*
    로 덮입니다(git check-ignore 로 확인) · 얼린 판은 앞 판 최근.md 와 한 글자도 안 다릅니다
  재시작 건너뛴 근거 맞습니다 — 이번 커밋에 app/ · templates/ · static/ 이 없습니다
  값 누출 없음 — 새 코드 · 시험 · 보고 · 문서 어디에도 제목 · 이름 · 연락처가 없습니다
  pytest 전체는 안 돌렸습니다 — 그 창에서 돌고 있다고 하셔서 같은 시험 DB 를 두 창이
    함께 건드리지 않게 뒀습니다. test_stage56 의 열 갈래는 직접 불러 전부 통과했습니다


[H] 막아야 함

1. 노션만 장의 드롭다운과 노란 칸이 **결정 칸이 아니라 메모 칸**에 걸렸습니다
   scripts/업무대조확인표.py:115-118  (끝 = get_column_letter(len(머리)))

   노션만머리는 [번호 · 노션 줄 · 노션 제목 · 구분·담당·시작일 · 넣음/안 넣음/버림 ·
   (안 넣음이면) 어느 업무에 묶였나] 라서 결정 칸은 다섯째(E)이고 끝 칸(F)은 자유
   메모입니다. 그런데 「끝 칸」 에 고르기와 노란 채움을 답니다.
   실제 파일에서 잰 것:
       1. 짝 확인    dv H2:H56  · 노랑 H   -> 맞습니다(H 가 표시 칸)
       2. 노션에만   dv F2:F152 · 노랑 F   -> F 는 「어느 업무에 묶였나」 입니다
   E 에는 드롭다운도 노란 칸도 없습니다.

   결과 — 인계 4줄과 보고 사람칸이 「노란 칸을 눌러 목록에서 고르고 저장」 이라고
   적어 두었으므로, 총무님은 151줄의 「넣음/안 넣음/버림」 을 **메모 칸에** 넣게 됩니다.
   엑셀읽기는 칸을 이름으로 집으므로 그 줄은 (표시="", 묶인곳="넣음") 으로 읽히고,
   보고 4장의 「빈 칸은 다음 판이 모르겠다로 본다」 에 따라 **151줄이 통째로
   「모르겠다」** 가 됩니다. 총무님이 한 일이 조용히 사라지는 자리입니다.

   시험이 이 자리를 안 봅니다 — tests/test_stage56.py:51 은 짝 장만 "H2:H3" 로 범위를
   재고, :53 은 노션만 장의 formula1 에 낱말이 들었는지만 봅니다(범위는 안 봅니다).
   확인 삼아 노션만 장의 고르기를 **올바른 칸(E)으로 옮겨** 봤는데도 열 갈래가
   전부 초록이었습니다 — 즉 지금 시험은 맞는 쪽과 틀린 쪽을 구별하지 못합니다
   (검토-원칙 2-③ · 「검사가 볼 것을 실제로 보나」).

   수용 기준 다)「엑셀에 드롭다운」 O 는 짝 장에만 참입니다.

2. 「사람이 손댄 표시는 덮지 않는다」 가드가 새 구조에서 반만 섭니다
   data/업무대조.real.py:102-113  (손댐 = 짝.손댄줄(확인.read_text(...), 새확인))

   가드는 `.md` 만 읽습니다. 그런데 이 판이 총무님이 손대는 자리를 **xlsx 로 옮겼습니다**
   (인계 4줄 · 보고 사람칸). 총무님이 xlsx 에만 표시하면 `.md` 는 새 판과 글자가 같아
   손댐 0 이 되고, `--replace` 를 붙인 재실행이 113줄에서 `확인표.엑셀로(엑셀, 그표)` 로
   **표시가 든 xlsx 를 통째로 덮습니다.** 앞 판이 [M] 5 로 굳혀 둔 가드가 파일이 바뀌면서
   주어를 잃은 자리입니다(CLAUDE.md:4420 도 「실행기는 손댄 줄이 있으면 아무것도 안 쓴다」
   고 적고 있어 지금은 그 문장이 반만 참입니다).

   새 시험도 없습니다 — test_stage56 에 가드를 재는 갈래가 하나도 없습니다.


[M] 고쳐야 함

3. CLAUDE.md:4420 에 **없는 경로**가 들어갔고, 그 줄은 이름 검사의 사각지대입니다
   그 줄 안: "(<TAB>ests/test_stage56.py 가 고장을 심어 잰다)"
   `\t` 가 탭으로 풀려 `tests` 의 t 가 먹혔습니다(data/업무대조.real.py:7 의 `` `n `` 과
   같은 입구 — 파워셸 이스케이프가 안 풀린 채 파일로 들어갔습니다).
   그리고 그 줄에는 **백틱이 하나도 없습니다** — scripts/업무대조확인표.py · md읽기 ·
   엑셀읽기 · 견준다 · data/업무대조.real.py 전부 맨글자입니다. check_named 는 백틱으로
   적은 이름만 코드에서 찾으므로 이 줄을 통째로 안 봅니다. 그래서 글검사 7/7 인데도
   깨진 경로가 남았습니다. 이웃 줄(노션업무짝짓기 · 회의록id달기)은 전부 백틱입니다.

4. docs/인계.md:130 이 옛 파일과 옛 갈래를 그대로 가리킵니다
   129줄은 xlsx 로 고쳤는데 바로 다음 130줄이
   「총무님이 data/업무대조-확인용.real.md 에 표시하면 다음 판이 반영한다 — 갈래:
   ㄱ) … ㄴ) A 는 자동 확정하고 … ㄷ) 표시가 올 때까지 아무것도 안 한다」
   입니다. 파일도 옛것이고, 사람이 이미 ㄴ 쪽으로 정한 갈래가 아직 미정으로 적혀
   있습니다. 인계는 **새 채팅이 먼저 읽는 글**이라 이 한 줄로 다음 판이 엑셀을
   안 열 수 있습니다.
   입구는 11-3 3단계입니다 — 이 판이 「총무님이 표시하는 파일은 .md」 라는 말을
   바꿔 놓고 docs/옛말.md 에 등록하지 않았습니다(변경 파일 목록에 옛말.md 가 없습니다).
   등록했으면 check_stale 이 이 줄을 잡았을 자리입니다.

5. 지시문 ③(덤 값과 문턱은 지금 값 그대로 · 기록만)이 지시문 대조 표에 줄이 없습니다
   docs/review/최근.md:99-114 — 1a~4b · 앱반영없음 · 마무리 14줄인데 ③ 이 없습니다.
   일 자체는 했습니다(봐둘것 BC-b 셋째 줄에 「지금 값 그대로 쓰기로 정했다(2026-09-16)」).
   빠진 것은 줄 하나인데, 검토-원칙 8-1 이 이것을 「막아야 함」 으로 둔 까닭은 빠진
   줄이 「그 일이 없다」 가 아니라 「안 본 것」 으로 읽히기 때문입니다.
   (「값은 보고·덩어리에 안 싣는다」 도 표에 줄이 없습니다 — 다만 실제로는 지켜졌습니다)

6. 표시의 정본이 어디인지가 어디에도 없습니다 — 그리고 견준다는 표시가 붙는 순간
   반드시 「다르다」 를 냅니다
   CLAUDE.md:4420 과 인계 4줄은 「정본은 .md」 라고 합니다. 그런데 총무님은 xlsx 에만
   표시하므로 정본 쪽은 영원히 빈 칸입니다. 그 상태로 견준다를 부르면 표시한 줄마다
   「표시가 다른 줄 N」 이 납니다 — 보고 3장의 「이번 실행에서 다른 자리 0」 은 아직
   아무도 표시를 안 했기 때문에 참입니다.
   다음 판이 「정본은 .md」 를 그대로 믿고 .md 를 읽으면 표시가 하나도 없다고 볼 수
   있습니다. 표시에 한해서는 xlsx 가 유일한 출처라는 것을 글로 못박아야 합니다.

7. 보고 7장이 통째로 미기재이고 커밋 줄에 해시가 없습니다
   docs/review/최근.md:76-82 이 네 줄 다 「아래 줄」 이고, 8줄은 「커밋: 559c4b7 다음 —
   이 판의 커밋들(7장 파일 목록)」 입니다 — 7장이 비어 있으니 이 판은 **자기 커밋을
   아무 데도 안 적은 채로 얼립니다**(앞 판은 해시 셋을 적었습니다).
   마)·바) 가 미완인 것은 미리 알려 주신 대로입니다. 다만 지시문 대조 표는
   「마무리는 11-3 대로 | 했다 | 7 · 9장」 이라고 적었는데 그 7·9장이 비어 있어,
   검토-원칙 8-1 의 「했다 인데 본문에 흔적이 없는 줄」 입니다.
   check_placeholder 는 「아래 줄」 꼴을 못 봅니다(CLAUDE.md 14장이 적어 둔 그 천장) —
   그래서 이 자리는 사람과 검토자만 봅니다.


[L] 봐 둘 것

8. data/업무대조.real.py:7 독스트링에 `` `n `` 이 안 풀린 채 남아 두 명령이 한 줄로
   붙어 있습니다. 저장소 밖 파일이라 test_docs_shell 이 안 보지만, 사람이 긁어 가는
   사용법입니다. [M] 3 과 같은 입구입니다.

9. test56_b04 가 심는 고장은 엑셀읽기 함수를 통째로 갈아끼운 것이라 docstring 의
   「엑셀에서 읽는 칸을 한 칸 밀면」 보다 약합니다 — 재는 것은 견준다의 반응이지
   읽는 칸이 아닙니다. 다만 진짜 칸 고장은 b02 가 잡는 것을 확인했으니 천장은
   낮지 않습니다. docstring 을 실제로 재는 것에 맞추면 됩니다.

10. docs/봐둘것.md BC-b 셋째 줄에 「…정했다(2026-09-16 · 다시 고르지 않는다). —
    2026 여름 자료 몇 건을…」 로 줄표가 겹칩니다(앞 문장을 지우고 덧댄 자국).

11. BC-b 넷째(「덤이 동점만 가르지 않는다 · 그 성질을 막는 시험이 없다」)가 앞 판의
    「정할 것」 에서 이번에 「적어만 둔다」 로 내려갔고(인계 129줄도 「남은 적어 둘 것」
    으로 바뀜), 보고 사람칸은 「정할 것: 없음」 입니다. 지시문 ③ 이 덤 값에 대한
    것이라 여기까지 덮는지는 갈립니다 — 사람이 한 번 보는 것이 좋겠습니다.


사람에게 묻는 것

  ㄱ. 표시의 정본은 엑셀입니까, `.md` 입니까. 총무님이 엑셀에만 표시하는 이상
     「정본은 .md」 와 「다음 판이 둘을 견줍니다」 는 같이 설 수 없습니다
  ㄴ. `--replace` 재실행이 총무님 표시를 덮어도 됩니까. 아니라면 손댐 가드를
     xlsx 까지 넓히는 것을 다음 판의 몫으로 둘지 정해 주세요
  ㄷ. BC-b 넷째를 「적어만 둔다」 로 내린 것이 맞습니까 (앞 판은 「정할 것」 이었습니다)

━━━ 검토자 여기까지 ━━━
```

## 9-2. 첫 검토를 받아 고친 것

| 번호 | 받았나 | 고친 곳 |
|---|---|---|
| [H] 1 | 받음 | 고를 칸을 **이름으로** 짚는다(`고를칸`) — 노션만 표의 고르기 · 노란 칸이 메모 칸(F)에서 **결정 칸(E)** 으로 옮겨졌다. 실제 파일에서 `E2:E152` · `H2:H56` 을 다시 쟀다. 시험도 범위를 칸 이름으로 재고 메모 칸이 아닌지까지 본다(`test56_b01`) |
| [H] 2 | 받음 | 가드가 **엑셀도 본다** — `표시있나` 를 모듈에 두고, 실행기가 엑셀에 고른 표시가 하나라도 있으면 `.md` 가 그대로여도 아무것도 안 쓴다. 심어서 확인했고 `test56_b05` 가 잰다 |
| [M] 3 | 받음 | CLAUDE.md 그 줄을 다시 썼다 — 깨진 경로(`ests/…`)를 고치고 이름을 전부 백틱으로(그래야 `check_named` 가 본다) |
| [M] 4 | 받음 | 인계 그 줄을 엑셀과 사람이 정한 갈래로 고치고, **`docs/옛말.md` 에 등록**했다(「표시하는 파일은 엑셀이다」) — `check_stale` 이 다음부터 잡는다 |
| [M] 5 | 받음 | 지시문 대조 표에 ③ 과 「값은 보고 · 덩어리에 안 싣는다」 두 줄을 더했다 |
| [M] 6 | 받음 | **표의 정본은 `.md` · 표시의 정본은 엑셀** 로 글을 갈랐다(모듈 머리 · CLAUDE.md · 보고 3a · 인계) |
| [M] 7 | 받음 | 7장을 채우고 머리 커밋 줄에 해시를 적었다 |
| [L] 8 | 받음 | 실행기 독스트링의 `` `n `` 을 풀어 두 줄로 |
| [L] 9 | 받음 | `b04` 독스트링을 실제로 재는 것(견준다의 반응)에 맞췄다 |
| [L] 10 | 받음 | BC-b 셋째 줄의 겹친 줄표 |
| [L] 11 | 받음 | 사람칸 「정할 것」 에 올렸다 — BC-b 넷째를 「적어만 둔다」 로 둘지는 사람이 본다 |

사람에게 묻는 것 ㄱ · ㄴ 은 이 판에서 닫았다(표시의 정본은 엑셀 · 가드가 엑셀을 본다). ㄷ 은 사람칸으로 올렸다.

## 9-3. 두 번째 검토 (범위 dd2a7a5..ab054ba)

```
━━━ 검토자 여기부터 ━━━ 범위 dd2a7a5..ab054ba · 커밋 1개 · 파일 7개(+ 저장소 밖 실행기 1)

첫 지적이 실제로 풀렸나 — 전부 다시 쟀습니다

  [H] 1 풀렸습니다. 운영 파일을 다시 재니
      1. 짝 확인    dv H2:H56  · 노랑 H   (결정 칸)
      2. 노션에만   dv E2:E152 · 노랑 E   (결정 칸) · 메모 칸 F 는 깨끗
      md ↔ 엑셀 내용도 그대로입니다 — 55 / 151 / 27 줄, 칸 값이 한 줄도 안 다릅니다.
      **시험이 이제 맞는 쪽과 틀린 쪽을 구별합니다.** 고장 셋을 심어 봤고 전부 b01 이 빨개집니다
        고를칸을 다시 끝 칸으로(첫 지적의 그 고장)   -> b01 빨감
        짝 표의 고를칸만 엉뚱한 칸으로               -> b01 빨감
        노션만 고를칸만 메모 칸으로                  -> b01 빨감

  [H] 2 풀렸습니다. 실행기가 `.md` 를 보기 **전에** 엑셀을 봅니다(표시수 → SystemExit).
      막으면 안 되는 쪽도 섭니다 — 운영 파일의 표시가 0 이라 지금 흐름이 안 막힙니다
      (심어 본 자국이 파일에 안 남았습니다. 제가 따로 세어 엑셀 0 · md 0 · 견준다 0).
        표시있나가 늘 0     -> b05 빨감 (막아야 할 것을 안 막는 쪽)
        표시있나가 늘 셈    -> b05 빨감 (막으면 안 되는 것을 막는 쪽)

  [M] 3 풀렸습니다 — 탭으로 깨졌던 경로가 사라지고 그 줄의 이름이 전부 백틱입니다.
      `check_named` 가 797개를 훑는 사정권 안에 들어왔습니다
  [M] 5 풀렸습니다 — ③ 과 「값은 보고·덩어리에 안 싣는다」 두 줄이 대조 표에 있습니다(항목 32)
  [M] 6 풀렸습니다 — 「표의 정본은 .md · 표시의 정본은 엑셀」 이 모듈 머리 · CLAUDE.md ·
      보고 3a · 인계 네 곳에서 **같은 말**입니다
  [L] 8 · 9 · 10 · 11 풀렸습니다(독스트링 두 줄 · b04 문구 · 줄표 · 사람칸 「정할 것」)

  값 누출 없음 — 새 코드 · 시험 · 문서 · 보고 어디에도 값이 없습니다. 시험에 더한
    「준비물에 묶임」 도 지어낸 말이고 노션 tsv · 앱 title 어디에도 없습니다
  글검사 7/7 통과(제가 돌림) · git status 깨끗 · 재시작 건너뛴 근거 여전히 맞음
  pytest 전체는 이번에도 안 돌렸습니다(그 창에서 도는 중) — test_stage56 의 열한 갈래를
    직접 불러 전부 통과했습니다


고치며 새로 틀린 곳

[H] A. 7장이 **비었는데** 9-2 표는 「채웠다」 고 적었습니다
   docs/review/최근.md:76-80 · 9-2 표 [M] 7 줄

   고치기 전에 있던 세 줄이 **`x` 한 글자로 덮였습니다.**
       -- 글검사: 아래 줄
       -- 새 시험: `tests/test_stage56.py` — 아래 줄
       -- 바뀐 파일: 아래 줄
       ++ x
   지금 7장에 남은 것은 git status 한 줄과 pytest 자리표시자뿐입니다.
   9-2 표는 「[M] 7 받음 | 7장을 채우고 머리 커밋 줄에 해시를 적었다」 인데,
   **해시는 실제로 적었고(8줄 · 세 개) 7장은 반대로 줄었습니다** — 절반만 참인 판정입니다.
   그리고 11-3 3단계가 「글검사 결과를 보고에 적는다 · 깨끗해도 적는다 — 안 한 것과
   구별되지 않는다」 고 못박은 그 줄이 보고에서 통째로 사라졌습니다.
   check_placeholder 는 `x` 도 `<!-- 최종 -->` 도 안 봅니다(7/7 통과) — 이 자리는
   사람과 검토자만 봅니다. 첫 검토의 [M] 7 이 「7장이 비었다」 였으므로 같은 자리가
   두 판 연속입니다.

[M] B. 옛말 등록이 **자기가 쓰려던 줄을 못 잡습니다** — 백틱 자리가 어긋났습니다
   docs/옛말.md:498

   등록한 표기 셋이 전부 백틱 없는 꼴인데, 실제 문장은 경로가 백틱 안에 있어
   `.md` 와 ` 에` 사이에 백틱이 하나 낍니다. 고치기 전 원문에 대고 재 봤습니다
       고치기 전 docs/인계.md:130   「…`…확인용.real.md` 에 표시하면 …」  -> 표기 셋 다 False
       얼린 판 …업무대조1판.md:4    「…`…확인용.real.md` 에 표시(짝 확인 …」 -> 표기 셋 다 False
   즉 **이 등록이 있었어도 [M] 4 의 그 줄은 못 잡았습니다.**
   지금 저장소에서 걸리는 자리는 docs/review/최근.md:207(제 검토를 인용한 코드블록이라
   백틱이 없습니다) 하나뿐인데 그 파일은 옛말-넘김에 있어 결과가 0 입니다 —
   **등록은 됐고 잡는 것은 하나도 없습니다.** 옛 말이 보통 꼴로 돌아와도 초록입니다.
   (파싱 자체는 됩니다 — `읽는다()` 를 직접 불러 174쌍 안에 셋이 든 것을 봤습니다.
    `--목록` 출력만 보고 「파싱이 안 된다」 고 적을 뻔했는데 그건 제 착오였습니다)

[M] C. **노란 칸 검사가 절대 안 빨개집니다**
   tests/test_stage56.py 의 고르는칸 안
       노랑 = [c for c in ws[f"{글자}2"].fill.fgColor.rgb or ""]
       assert 노랑, "고르는 칸에 색이 없다"
   openpyxl 은 채움이 없는 칸도 fgColor.rgb 가 '00000000' 이라 이 리스트가 **늘 참**입니다
   (운영 파일의 빈 칸 A2 로 확인: rgb '00000000' → 참).
   고르기는 제자리에 두고 **노란 칸만 끝 칸으로** 옮기는 고장을 심었더니 열한 갈래가
   전부 초록이었습니다. 인계와 사람칸이 총무님께 「노란 칸을 누르면」 으로 안내하므로
   이것은 재야 하는 값입니다 — 채움 여부(fill_type)나 색값 자체를 보면 닫힙니다.
   첫 검토가 [H] 1 로 짚은 그 자리에 **재지 않는 검사**가 하나 새로 생긴 셈입니다.

[M] D. b05 의 「메모만 적은 것도 손댄 것이다」 는 그 시험이 재지 않는 말입니다
   tests/test_stage56.py:139-142
   E2 에 「넣음」 을 남겨 둔 채 F2 를 적고 다시 ==1 을 보므로, 표시있나가 메모 칸을
   무시해도 결과가 1 입니다. `any(표시)` 를 `표시[0]` 로 좁히는 고장을 심었더니
   열한 갈래가 전부 초록이었습니다.
   **지금 코드는 메모도 셉니다 — 주석이 틀린 게 아니라 시험이 안 볼 뿐입니다.**
   메모만 적힌 줄을 따로 만들어(다른 줄에서) 재면 닫힙니다.

[L] E. `고를칸` 에 "자동확정" 키가 없습니다
   scripts/업무대조확인표.py:36 · 120
   지금은 그 장에 고르기를 안 넘기니 안 밟습니다. 다만 나중에 누가 넘기면 조용한
   실패가 아니라 KeyError 로 터집니다(참고장에 고르기를 붙이는 고장을 심었을 때
   여섯 갈래가 AssertionError 가 아니라 KeyError 로 죽었습니다). 봐 둘 자리입니다.


사람에게 묻는 것

  ㄱ. 옛말 표기를 백틱 낀 꼴(「…real.md` 에 표시하면」)로도 적어 둘까요 — 지금 등록은
     보통 문장을 못 잡습니다([M] B)
  ㄴ. 첫 검토의 ㄷ(BC-b 넷째를 「적어만 둔다」 로 둘지)은 사람칸으로 올라왔습니다.
     그 자리에서 정해 주시면 됩니다

━━━ 검토자 여기까지 ━━━
```

## 9-4. 두 번째 검토 뒤 고침 (다시 보이지 않았다 — 11-3)

| 번호 | 파일 | 고친 것 |
|---|---|---|
| [H] A | `docs/review/최근.md` | 7장의 세 줄을 되살렸다 — 글검사 · 새 시험 수 · 바뀐 파일. 고치다 `x` 한 글자로 덮은 자리다(같은 자리가 두 판 연속이라 9-2 표의 판정도 이 표가 바로잡는다) |
| [M] B | `docs/옛말.md` · `docs/옛말-넘김.txt` | 표기를 **백틱이 안 끼는 조각**으로 바꿨다(「에 표시하면 다음 판이」 · 「에 표시(짝 확인」 · 「확인용.real.md 에서」) — 고치기 전 문장에 실제로 걸리는 것을 확인했고, 얼린 판이 걸려 넘김에 한 줄 |
| [M] C | `tests/test_stage56.py` | 노란 칸을 `fill_type` 과 **색 없는 칸과의 차이**로 잰다 — openpyxl 은 색 없는 칸도 rgb 를 돌려줘 옛 단언은 늘 참이었다 |
| [M] D | `tests/test_stage56.py` | 메모만 적힌 줄을 **다른 줄에** 만들어 2 를 센다 — 앞은 같은 줄이라 메모를 무시해도 통과했다 |
| [L] E | `scripts/업무대조확인표.py` | `고를칸` 에 `자동확정: None` 을 두고 `고를칸.get(갈래)` 로 — 참고장에 고르기를 넘겨도 `KeyError` 로 안 터진다 |

사람에게 묻는 것 ㄱ 은 이 판에서 닫았다(표기를 바꿔 실제로 걸린다). ㄴ 은 사람칸 「정할 것」 그대로다.
