사람이 할 것
노트북에서 직접: 하나 — 화면 점검을 다시 돌리려면 Claude in Chrome 확장이 붙은 그 Chrome 에서 `http://127.0.0.1:8001` 에 로그인하고, 운영(`127.0.0.1:8000`)은 같은 브라우저에서 열지 않은 채로 알려 주세요. 그 로그인은 같은 브라우저의 `127.0.0.1:8000` 운영 세션을 풉니다(인계 「화면 점검 시도」)
관리자 권한: 없음 — app/ · templates/ · static/ 을 안 건드려 재시작하지 않았다(4장)
정할 것: 하나 — 운영과 개발이 같은 쿠키 이름(`dcb_session`)을 같은 호스트에서 다른 서명키로 써서 한쪽에 로그인하면 다른 쪽 세션이 풀린다. 그대로 두고 로그인할 때마다 갈아 쓸지, 개발 쪽 쿠키 이름을 가를지(코드를 고치는 일 · 검토 7-1 물음 3)
막혀서 못 한 것: `docs/checks/drawer.js` · `phone.js` 와 재정 화면 넷(계좌 셋 입력 · 영수증 잇기 · 고치기 · 취소) — 확장이 붙은 Chrome 에서 `127.0.0.1:8001` · `localhost:8001` 둘 다 로그인 화면으로 돌아갔다. 까닭을 가르려면 물음 둘에 답이 필요하다 — 개발에 로그인하신 브라우저가 확장이 붙은 그 Chrome(같은 프로필)인가 · 같은 브라우저에서 운영을 `127.0.0.1:8000` 으로 여셨나(3장)

작성 시각: 2026-09-15 20:40 KST
커밋: 1970d10 (인계 닫기 · 시도 기록 · 얼림) · 03113bf (검토 고침) · 이 보고는 그다음 커밋
작업: 개발 DB 재생성 완료를 닫고 화면 점검을 돌린다(못 돌림)

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 세션을 정하는 곳: `app/security.py` 의 `set_session`(domain · path 없음 · Secure 없음) · `app/config.py` 의 `SESSION_COOKIE`(운영 · 개발 같은 이름) · `scripts/devserve.bat` 의 `DCB_SECRET_KEY` |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 문서만 |
| 그 값을 세어 둔 문서 | 인계 재정 절 남은 것 표 두 줄(개발 DB · 점검) · 맨 위 갱신 · 재정 절 머리 |

# 1. 한 것

- **1a · 1b** 인계 「개발 DB 다시 만들기 — 안내」 를 「닫힘 · 실행 완료」 로(원문은 다시 할 일에 쓰려고 두고 「그때의 사정」 이라고 밝힘) · 남은 것 표에서 그 줄을 뗌 · 재정 절 머리의 「재정과 무관한 일(개발 DB …)」 고침
- **2a · 2b** Claude in Chrome 확장이 붙은 로컬 Chrome 하나에 새 탭을 열어 `127.0.0.1:8001/tasks` · `localhost:8001/tasks` 로 감 → 둘 다 `/login`(비밀번호 칸 있음 · 목록 행 0). 앱 안 Browser 창은 안 열려 있었음. **로그인 칸에 아무것도 넣지 않았고** 탭을 닫았다
- **까닭 좁히기**(비밀번호 없이 할 수 있는 만큼) — 개발 서버 8001 은 떠 있음 · 개발 DB 를 읽기 전용으로 세니 회차 1 · 계정 1(관리자 · 처음 들어온 적 있음 · 비밀번호 있음) · 세션 쿠키에 Secure 가 없어 http 문제 아님 · 쿠키 이름이 운영과 같음 → 후보 둘(3장)
- **2c** 재정 화면 넷도 못 봤다 — 로그인이 필요하다
- **3** 깨진 화면: **모름**(하나도 안 봤다 — 「없음」 이 아니다)
- **4** 인계에 「화면 점검 시도」 절: 확인 0 · 실패 0 · 못 함 2(점검 스크립트 둘을 센 것 · 재정 화면 넷도 못 봄) · 까닭 후보 둘 · 다시 돌리는 법 · 총평 「재정 줄기는 코드와 시험으로는 끝났고 실제 화면으로는 아직 확인되지 않았다」(맨 위 · 재정 절 머리 · 점검 절)
- 앞 판 보고를 `docs/review/2026-09-15-개발DB안내.md` 로 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) drawer.js · phone.js 가 돌았거나 못 돈 이유가 정확히 | O | 못 돌았다 — 이유는 1장 · 3장 · 인계 「화면 점검 시도」 |
| 나) 점검에서 나온 문제가 사람칸에 | O | 점검이 안 돌아 문제는 **모름** · 막힌 것과 물음 둘을 사람칸에 |
| 다) pytest 옵션 없이 · 글검사 7/7 | O | 6장 |
| 라) 검토자 원문 | O | 7장 |

# 3. 못 돌린 까닭 — 둘 중 하나로 보인다

1. **다른 브라우저 · 프로필** — 총무님이 로그인한 브라우저가 확장이 붙은 Chrome(같은 프로필)이 아니다
2. **쿠키 덮어쓰기** — 쿠키는 포트가 아니라 호스트에 붙는다(`set_session` 에 domain 이 없어 host-only · path `/`). 운영(8000)과 개발(8001)이 `dcb_session` 이라는 같은 이름을 다른 서명키(개발은 `devserve.bat` 의 값)로 쓰므로, 같은 브라우저에서 운영을 **`127.0.0.1:8000` 주소로** 열어 로그인하면 개발 쿠키가 덮이고 서명이 안 맞아 로그인 화면으로 간다. 터널 주소나 `localhost` 로 열었다면 해당 없다

이 창 혼자서는 못 가른다 — 확장 탭으로 운영(8000)을 열어 보는 것은 실명 화면을 여는 일이라 안 했고, httponly 쿠키는 확장에서 안 보인다. 사람칸의 물음 둘이면 갈린다.

# 4. 재시작

**건너뛰었다** — 커밋에 `app/` · `templates/` · `static/` 이 없다(문서만). 운영 서버 PID 18592 그대로. 개발 서버(PID 36152)는 총무님이 띄운 것이라 손대지 않았다.

# 5. 판단이 들어간 자리

- **확장이 붙은 Chrome 에 새 탭** — 지시문이 「로그인된 세션에 붙어」 라고 해서, 같은 프로필이면 쿠키를 나눠 갖는 새 탭을 썼다. 세션 쿠키를 만들거나 넣지 않았고 로그인 우회도 두지 않았다(11-2 · 검토 7-1 「부딪히는 것 없음」)
- **깨진 화면을 「없음」 이 아니라 「모름」 으로** — 안 본 것은 없음이 아니다(검토 7-1 부탁)
- **닫은 안내 원문을 지우지 않음** — 다시 만들 일이 생기면 그 순서를 쓴다. 「무엇이 막혀 있나」 는 그때의 사정이라고 한 줄 붙였다
- **안 고친 것** — 재정 절을 맺는 문단(「다음 판이 이 위에서 시작한다」)이 지금 「화면 점검 시도」 절 아래에 이어져 구조로는 그 절의 글로 읽힌다(검토 7-2 [L] · 이 판이 만든 것이 아니고 뜻이 틀어지지 않음)

# 6. 검사와 시험

- 글검사(커밋마다): 7/7 · `tests/test_docs_shell.py` 11 passed
- 보고를 다 채운 뒤 돌린 것: 아래 줄

- 글검사 7/7 통과 · pytest(옵션 없이) `1596 passed, 3 warnings in 1181.47s (0:19:41)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토 (4646395..1970d10)

```
━━━ 검토자 여기부터 ━━━ 범위 4646395..1970d10 · 커밋 1개 · 파일 2개

돌린 것
  scripts/글검사.py — 검사 일곱 모두 exit 0
  pytest tests/test_docs_shell.py — 11 passed
  얼린 판 docs/review/2026-09-15-개발DB안내.md 는 4646395 의 최근.md 와 글자까지 같다 (diff 없음)

[M] 고쳐야 함 — 닫은 안내 절의 본문이 새 절 아래로 들어갔다
  docs/인계.md:240-252
    240 「개발 DB 다시 만들기 — 안내 … 닫힘」 제목
    242 「아래는 그때의 안내 원문이다」
    244 새 제목 「화면 점검 시도」 (246-250 이 그 절 내용)
    252- 옛 안내 원문 (「무엇이 막혀 있나」 … 1~5단계 · 296 까지)
  그래서 242 의 「아래」 바로 밑은 다른 절이고, 옛 안내 원문은 마크다운 구조상
  「화면 점검 시도」 절에 딸린 글이 됐다. 목차로 보면 점검 절 안에 개발 DB 를 지우는
  명령(Remove-Item)이 들어 있다. 「화면 점검 시도」 절을 옛 원문 끝(296 뒤, 298 앞)으로
  옮기거나, 옛 원문 바로 앞에 붙이면 된다.
  원문을 지우지 않고 닫힘 표시만 단 판단 자체는 괜찮다 (다시 할 일에 쓸 순서라서).
  다만 「다시 할 일이 생기면 이 순서를 쓴다」 라면서 252-253 「무엇이 막혀 있나」 가
  현재형(검사가 서버를 안 띄운다)으로 남아 있다. 옛날 얘기라는 한 줄이 있으면
  다음 사람이 지금도 막힌 줄로 안 읽는다. 봐 둘 것 수준.

[L] 봐 둘 것 — 쿠키 충돌 추정(②)은 코드로 맞다. 다만 조건이 하나 빠졌다
  app/security.py:34-40 set_cookie 에 domain·path 인자가 없다 → Starlette 기본값 path="/",
  host-only 쿠키. 쿠키는 포트를 안 가른다(RFC 6265).
  app/config.py:122 SESSION_COOKIE = "dcb_session" 을 두 서버가 같이 쓴다.
  scripts/devserve.bat 에 set DCB_SECRET_KEY=dev-only-secret 이 있다. 운영은 앱이 뜰 때
  환경변수가 없으면 secret_key.txt 를 쓴다(config.py:90-104). 운영 환경변수는 data/ 를
  안 열어서 안 봤다.
  security.py:50-53 은 서명이 틀리면 BadSignature 를 삼키고 None 을 돌려준다
  → /login 으로 간다. 관찰한 것과 맞다.
  빠진 조건: 호스트 이름이 같아야 한다. 총무님이 운영을 터널 주소나 localhost:8000 으로
  열었다면 127.0.0.1:8001 의 쿠키는 안 덮인다. 인계 248 은 「같은 브라우저에서 운영에
  로그인하면」 이라고만 적었다. 「127.0.0.1:8000 으로」 까지 적어야 맞다.
  반대 방향 부작용도 있다: 250 대로 127.0.0.1:8001 에 로그인하면 같은 브라우저의
  127.0.0.1:8000 운영 세션이 풀린다. 알려 둘 만하다.

[L] 봐 둘 것 — 「못 가른다」 는 정직하다. 다만 가르는 길이 하나 있었다
  이 창이 비밀번호 없이 가를 수 있었던 길:
    확장 탭에서 127.0.0.1:8000 을 열어 세션이 있는지 보기 — 운영 화면(실명)을 여는
      것이라 안 한 것이 맞다
    DevTools 에서 httponly 쿠키가 있는지 보기 — 확장에서는 보통 못 한다
  그러니 이 창 혼자서는 못 가른다는 말이 맞다. 대신 사람에게 물음 하나(「어느 브라우저로,
  어느 주소로 로그인하셨나 · 127.0.0.1:8000 을 같은 브라우저에서 여셨나」)면 갈린다.
  이 물음을 인계에만 두지 말고 보고의 「정할 것」 이나 「막혀서 못 한 것」 에서도 가리키는 게 좋다.
  후보가 둘뿐이라고 단정한 것도 조금 좁다. 예를 들어 총무님이 로그인한 뒤 개발 DB 를
  한 번 더 다시 만들었다면 uid 가 달라진다. 가능성은 낮으니 「보인다」 정도의 말투면 충분하다.
  지금 248 이 「둘 중 하나로 보이고」 라고 적어서 이미 그 말투다.

[없음] 11-2 와 부딪히는 것
  비밀번호를 넣지 않았다. 세션 쿠키를 만들거나 넣지 않았다. 로그인 우회를 두지 않았다.
  세션 없는 탭으로 GET 해서 /login 을 받은 것은 로그인 시도가 아니다.
  사람이 들어가 준 창에 붙는 것은 11-2 가 남겨 둔 길이다.
  개발 DB 를 mode=ro 로 센 것은 %TEMP%\dcb-dev 쪽이라 data/ 밖이다.

[없음] 과장 — 인계는 2c 를 못 한 것을 과장하지 않았다
  인계 3 · 235 · 246 이 모두 「시도했으나 못 돌렸다」 「실제 화면에서 아직 확인되지 않았다」
  라고 적었다. 246 의 「브라우저로 눌러 본 판은 없다」 는 앞 보고와 맞다.
  차례4 보고(docs/review/2026-09-14-차례4.md:46,61)는 그린 HTML 을 375px 로 열어
  크기만 쟀고 누르거나 저장하지 않았다.
  옆 문장과 맞지 않는 곳: 226-227 의 재정 절 머리는 여전히 「재정 줄기 완전히 끝남」 한 줄만 있다.
  갱신 줄(3)에는 조건이 붙었지만 이 머리에는 없다. 코드 줄기가 끝났다는 뜻이라 틀린
  말은 아니지만, 「화면으로는 미확인」 을 한 마디 붙이면 머리만 읽는 사람이 안 헷갈린다.
  봐 둘 것.

아직 안 쓴 보고(최근.md)에 부탁할 것 — 지금 커밋에는 해당 없음
  지시문 3 「깨진 화면 없으면 없다고」 의 판정을 「없음」 으로 적지 말 것.
    화면을 하나도 안 봤으니 「모름 · 안 봄」 이다. 없음은 값이고, 안 본 것은 없음이 아니다.
  지시문 대조 표: 2a · 2b(drawer.js · phone.js) · 2c(재정 화면)를 따로 「안 했다」 로
    두고 까닭을 적을 것. 2c 를 2 에 묶어 버리면 재정 화면을 따로 봤는지가 안 보인다.
  사람칸 「막혀서 못 한 것」 에 인계 248-250 을 가리키고, 위 물음 하나를 함께 둘 것.
  지시문 4 의 「건수로」 는 인계 246 「확인 0 · 실패 0 · 못 함 2」 가 채웠다.
    다만 2c 의 재정 화면 넷(계좌 셋 · 영수증 잇기 · 고치기 · 취소)은 건수에 없다.
    「못 함 2」 가 점검 스크립트 둘만 센 것인지 적어 두면 좋다.

사람에게 묻는 것
  1. 개발 서버에 로그인하신 브라우저가 Claude in Chrome 확장이 붙은 그 Chrome(같은 프로필)인가요?
  2. 같은 브라우저에서 운영을 127.0.0.1:8000 주소로 여신 적이 있나요? (터널 주소나 localhost 로
     여셨으면 쿠키 충돌 추정은 해당이 없습니다)
  3. 다시 돌릴 때 127.0.0.1:8001 에 로그인하면 같은 브라우저의 127.0.0.1:8000 운영 세션이 풀립니다.
     괜찮으신가요? 아니면 개발 쪽 쿠키 이름을 운영과 갈라 둘까요? (그건 코드를 고치는 일이라 따로 정할 일입니다)

━━━ 검토자 여기까지 ━━━
```

이 창의 처리: [M] 과 [L] 셋은 03113bf · 보고에 부탁한 셋은 이 보고(1장 「모름」 · 9장 2a/2b/2c 따로 · 사람칸 물음 둘) · 물음 1 · 2 는 사람칸 「막혀서 못 한 것」 · 3 은 「정할 것」.

## 7-2. 다시 보기 (1970d10..03113bf)

```
━━━ 검토자 여기부터 ━━━ 범위 1970d10..HEAD(03113bf) · 커밋 1개 · 파일 1개 (docs/인계.md)

돌린 것
  scripts/글검사.py — exit 0
  pytest tests/test_docs_shell.py — 11 passed

짚은 것이 고쳐졌나
  [M] 점검 절 자리 — 고쳐짐
    인계.md 290 「화면 점검 시도」 가 옛 안내 원문 끝(288) 뒤, 「다음 판이 이 위에서」 앞에 있다.
    242 에 「무엇이 막혀 있나 는 그때의 사정이고 지금은 풀렸다」 가 붙었다.
    243 의 「아래는 원문」 바로 밑이 이제 그 원문이다.
  [L] 쿠키 조건 — 고쳐짐
    294: 「127.0.0.1:8000 주소로」, 「터널 주소나 localhost 면 해당 없음」, 「사람에게 물으면 갈린다」
    296: 개발에 로그인하면 반대로 운영 세션이 풀린다
    코드와 맞다 (security.py:34-40 host-only 쿠키 · config.py:122 같은 이름 · devserve.bat 의 다른 서명키)
  [L] 건수의 뜻 — 고쳐짐
    292: 「못 함 2」 는 점검 스크립트 둘을 센 것이다.
    재정 화면 넷도 못 봤고, 깨진 화면이 있는지는 「모름」 이다.
    과장이 없다.
  [L] 재정 절 머리 — 고쳐짐
    227: 「코드와 시험으로는 끝났고 실제 화면으로는 아직 확인되지 않았다」

새로 생긴 것
  [H] 없음
  [M] 없음
  [L] 봐 둘 것 하나 — 이번 커밋이 만든 것은 아니다
    298 「다음 판이 이 위에서 시작한다. 규칙은 CLAUDE.md 7장 …」 은 재정 절 전체를 맺는 문단이다.
    4646395 에서도 개발 DB 안내 절 제목 밑에 있었다 (그때 288).
    지금은 제목 없이 「화면 점검 시도」(### ) 아래에 이어진다.
    그래서 구조로만 보면 점검 절의 글로 읽힌다.
    뜻이 틀어지지는 않는다.
    한 번 정리한다면 이 문단을 두 ### 절보다 앞, 남은 것 표 뒤로 옮기면 된다.
  같은 사실이 두 곳에 있는가
    「실제 화면으로 미확인」 이 인계.md 3 · 227 · 235 · 292 에 나온다.
    넷 다 292 절을 가리키거나 같은 말이라 서로 어긋나지 않는다.

보고(최근.md) — 아직 안 써서 이번 범위 밖이다
  부탁한 셋은 그대로다: 깨진 화면은 「모름」 · 대조 표에 2a/2b/2c 를 따로 · 사람칸에 물음

사람에게 묻는 것 — 앞 검토와 같다
  1. 개발 서버에 로그인한 브라우저가 확장이 붙은 그 Chrome(같은 프로필)인가
  2. 같은 브라우저에서 운영을 127.0.0.1:8000 주소로 연 적이 있나
  3. 개발에 로그인하면 같은 브라우저의 127.0.0.1:8000 운영 세션이 풀린다.
     괜찮은가, 아니면 쿠키 이름을 가를지 따로 정할 것인가

━━━ 검토자 여기까지 ━━━
```

이 창의 처리: 새 [L] 은 이 판이 만든 것이 아니라 두었다(5장).

# 8. 바뀐 파일 (4646395..이 보고)

docs/review/2026-09-15-개발DB안내.md · docs/review/최근.md · docs/인계.md

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1a | 개발 DB 안내 항목을 실행 완료로 닫음 | **했다** — 원문은 두고 닫힘 표시 |
| 1b | 남은 것 표에서 그 줄을 뗌 | **했다** |
| 2a | drawer.js 를 로그인된 세션에 붙여 돌림 | **안 했다** — 확장이 붙은 Chrome 에 개발 서버 세션이 없었다(3장) · 비밀번호는 넣지 않음 |
| 2b | phone.js 같은 방식 | **안 했다** — 같은 까닭 |
| 2c | 새 재정 화면(계좌 셋 · 영수증 잇기 · 고치기 · 취소)이 눌리고 저장되는지 | **안 했다** — 같은 까닭 · 로그인 없이는 그 화면이 안 열린다 |
| 2d | 못 돌리면 정확한 이유를 사람칸에 | **했다** — 사람칸 「막혀서 못 한 것」 과 3장 |
| 3a | 깨진 화면은 고치지 말고 사람칸에 | **했다** — 본 화면이 없어 올릴 것 없음 · 판정은 「모름」 |
| 3b | 없으면 없다고 | **다르게 했다** — 안 봤으므로 「없음」 이 아니라 「모름」 으로 적었다 |
| 4a | 인계에 결과를 건수로 | **했다** — 확인 0 · 실패 0 · 못 함 2(점검 스크립트 둘 · 재정 화면 넷도 못 봄) |
| 4b | 재정 줄기가 실제 화면에서 확인됐는지 총평 한 줄 | **했다** — 「코드와 시험으로는 끝났고 실제 화면으로는 아직 확인되지 않았다」 |
| 가~라 | 수용 기준 | **했다** — 2장 |
| 끝줄 | 운영 안 건드림 · 개발 서버 · DB 만 | **했다** — 개발 DB 는 읽기 전용으로 셌다 |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
