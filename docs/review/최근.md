사람이 할 것
노트북에서 직접: 없음 — 백업 쪽 바뀐 것은 오늘 새벽 3시 작업이 저절로 씁니다(3-4). 사역자 열 분 계정(인계 18번)과 개기자 주소(인계 8번)는 그대로 남아 있습니다
관리자 권한: 없음 — 이 판은 앱을 안 고쳐 재시작하지 않았습니다(7장)
정할 것: 검토자가 물은 셋 — ① 운영 DB 가 계정까지 비는 사고 모양에서 빈 판 알림이 못 서는데 앱 밖 자리(점검 화면이 백업 폴더를 읽는 등)도 둘지(3-3 · 봐둘것 AZ-c) ② 새벽 백업 알림이 웹 푸시로도 나가도 되는지(3-3) ③ 11-3 「pytest 앞에 아무것도 끼우지 않는다」 에 앱을 안 읽히는 `-p no:…` 를 예외로 둘지(12장)
막혀서 못 한 것: phone.js 의 「다음 판에 남습니다」 둘 — 개발 서버에 로그인해야 자가시험을 돌리는데 이 창은 비밀번호를 칠 수 없습니다(4-2)
작성 시각: 2026-09-13 KST — 검토를 받고 고친 판
커밋: (이 보고와 함께 나갑니다 — 10장)
작업: 멈출 때 범위 좁히기 · 빈 판 경고를 앱 알림에 · 의심 판 상한 · 글검사가 추적 전 파일도 보기

---

# 0. 한 줄 요약

**멈출 프로세스는 작업 폴더가 이 저장소 안인 것만 고르게 했고, 빈 백업은 총무팀
앱 알림에 한 번 서고, 의심 판에는 상한을 두되 성한 판보다 먼저 지웁니다.**

- `scripts/내프로세스.py` — 무늬로 고르고 **작업 폴더로 가릅니다.** 도구 자신과 그것을
  부른 셸·런처는 뺍니다(검토가 잡은 막아야 할 것). 저장소 밖과 안에 **실제로 프로세스를
  띄워** 안의 것만 고르고 멈추는지 잽니다. 이 판에서 고치기 전 코드로 돌던 제 시험을
  이 도구로 멈췄습니다 — 고른 것 다섯이 전부 이 창의 것이었고 밖은 0개였습니다.
- `scripts/backup.py` — 의심 판마다 **이미 있는 알림 자리**(`notify` · `dedupe_key`)에
  한 번 섭니다. 의심 판이 없으면 아무 말도 안 합니다. **다만 운영 DB 가 계정까지 비면
  알릴 사람이 DB 에 없어 안 섭니다**(검토 [B] · 사람칸).
- 의심 판은 따로 `SUSPECT_KEEP`(7)까지 남기고 크기가 넘치면 먼저 지웁니다. 사고의
  증거 판은 `지키는판` 에 까닭과 함께 있어 어느 셈에도, 알림에도 안 듭니다.
- 글검사의 옛말·세는 말·실명 검사가 **추적 전 새 파일도 봅니다** — 앞 판의 「7/7 이
  틀렸다」 자리를 검사로 닫았습니다.

---

# 1. 고치기 전 훑기 (11-3 0단계)

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| **멈추는 일이 도는 자리** (지시 1-d) | 저장소 전체에서 `Stop-Process` · `taskkill` · `kill` · `terminate` 를 훑었습니다. **스크립트에는 없습니다.** 문서의 재시작 절차(배포-안내 12-1 · 12-2)는 **8000 포트를 쥔 PID** 를 멈춥니다 — 그 포트가 이 앱의 것이라 이미 내 것입니다. **그 사고는 이 창이 그때그때 친 명령에서 났습니다** — 그래서 막는 것은 **도구와 규칙**이고(CLAUDE.md 11-3), **규칙이라 안 따르면 안 막힙니다** |
| `prune` 을 부르는 데 | `backup.run` 과 시험(`test_backup` · `test_attachments` · `test_stage43`). 크기 루프가 의심 판을 합에 넣게 바뀌어 기존 시험을 다시 돌렸고 그대로 통과합니다 |
| 알림 자리 | `app/notifications.notify` · `badge_unread_count` · `recent_notifications` · `templates/notifications.html` — 시스템 알림의 제목·본문·바로가기를 이미 14px(`--fz-md`)로 그립니다. **앱 코드는 안 고쳤습니다** |
| `git ls-files` 로 파일을 모으는 검사 | `check_stale` · `check_counts` · `check_names` 를 넓혔습니다. `check_named` 는 안 넓혔습니다(아래 4-1) |
| 「빈 판은 세지도 지우지도 않는다」 를 적어 둔 곳 | CLAUDE.md 11-2 · 14장 · 인계 25번 · 봐둘것 AZ-a 표 · `backup.py` 머리 · `test_stage43` 머리 — 전부 고치고 옛말에 표기로 |

---

# 2. 멈출 때 범위를 좁힌다 (지시 1)

## 2-1. 도구 (지시 1-c)

`scripts/내프로세스.py`

| 무엇 | 어떻게 |
|---|---|
| 고르기 | 명령줄에 무늬가 든 프로세스. **빈 무늬는 거절**(기계의 모든 프로세스를 고릅니다) |
| 가르기 | **작업 폴더(cwd)가 이 저장소 안이면 내 것.** 실행 파일 경로로 안 가릅니다 — 이 저장소의 .venv 로 다른 폴더(worktree)에서 돈 시험이 실제로 있었습니다 |
| 못 읽음 | 작업 폴더를 못 읽으면(권한) **내 것이 아닌 쪽** — 모르는 것을 멈추는 쪽으로 틀리면 되돌릴 수 없습니다 |
| 자기와 조상 | **도구 자신과 부른 셸·런처는 뺍니다** — 무늬가 그 명령줄에도 있습니다(검토 [A]) |
| 멈추기 | `--멈춤` 일 때만. 훑을 때의 `Process` 를 넘겨 그사이 PID 가 다시 쓰여도 엉뚱한 것을 안 멈춥니다(검토 [G]) |
| 못 가르는 것 | **이 저장소 안에서 도는 다른 창** — 작업 폴더가 같습니다. 그래서 늘 고른 목록을 찍습니다 |

psutil 이 필요해 `requirements.txt` 의 개발용 칸에 `psutil==7.1.0` 을 넣고 .venv 에
설치했습니다.

## 2-2. 잰 것 (지시 1-e · 수용 기준 가)

`tests/test_stage44.py` — **실제로 프로세스를 만들어** 잽니다. 같은 무늬를 단 잠자는
파이썬을 `tmp_path`(밖)와 저장소 뿌리(안)에 띄웁니다. 치우는 것은 **띄운 PID 의 나무**로만.

| 시험 | 무엇을 보나 |
|---|---|
| `test44_a01_저장소_밖은_안_고르고_안은_고른다` | 무늬만으로는 둘 다 걸리고(③) · 고른 것 = 안의 나무 · 밖의 나무는 남의 것 |
| `test44_a02_멈추면_안의_것만_멈추고_밖은_산다` | 멈춘 PID = 안의 나무 · 안은 사라지고 밖은 산다 |
| `test44_a05_명령줄로_부르면_부른_쪽은_안_고른다` | **CLI 를 부모 프로세스로 감싸** 부모 명령줄에 무늬를 넣고 작업 폴더를 이 저장소로 · 고른 것 = 안의 잠든 나무만 |
| `test44_a03_빈_무늬는_거절한다` · `test44_a04_작업_폴더를_못_읽으면_내_것이_아니다` | |

**재다가 알게 된 것** — 윈도우의 .venv 의 python 은 **런처**라서 같은 명령줄의 자식을
하나 더 띄웁니다. 처음에 「고른 PID = 띄운 PID」 로 견줬다가 빨개졌고, 둘 다 안의 것이라
**나무 단위로** 견주게 고쳤습니다.

**고장을 심었습니다**

```
① 가르기를 끔(_안인가 → 늘 참)          -> a01 · a02 빨강
⑦ 조상 빼기를 되돌림(자기 PID 만)        -> a05 방식으로 감싸 돌림: 고른 수 5 (안의 나무 2) 빨강
   안 심음                                -> 고른 수 2 = 안의 나무 2 통과
```

⑦ 은 CLI 가 디스크의 파일을 부르므로 시험 파일로는 못 심어, 도구 사본을 scratch 에 두고
a05 와 같은 감싸기로 돌렸습니다.

## 2-3. 실제로 써 봤습니다

이 판 중간에 **고치기 전 코드로 시작한 제 전체 시험**을 멈춰야 했습니다. 이 도구로 먼저
고르기만 해 보니 **안 5개(이 창의 셸 셋 · 파이썬 둘) · 밖 0개**였고, 그 다섯만 멈췄습니다.

## 2-4. 규칙 (지시 1-f)

CLAUDE.md 11-3 「마치기 전에 되짚는 것」 — **무엇을 멈출 때 그것이 이 저장소의 것인지
먼저 본다** · 까닭(다른 프로젝트 창까지 멈춘 사고 · 운영 DB 비움과 같은 모양 · 이 노트북은
여러 창이 같은 도구를 동시에 돌린다) · 도구 · 띄운 PID 와 포트를 쥔 PID 는 이미 내 것 ·
도구도 같은 저장소 안의 다른 창은 못 가른다. 14장에 도구 줄.

---

# 3. 빈 판 경고와 상한 (지시 2 · 3)

## 3-1. 어디에 띄우나 (지시 2-a · 2-b)

**이미 있는 알림 자리**입니다 — `app.notifications.notify`. 총무팀 전원(`permissions.admins`)
에게 `kind` 「백업」, `retreat_id` 없음(어느 회차를 보든 뜸), 바로가기는 설정 › 점검.
관리자가 평소 지나는 **사이드바 배지**와 **알림 화면**에 섭니다. 앱 코드는 안 고쳤습니다.

| 지시 | 어떻게 |
|---|---|
| 2-c 되풀이 안 함 | `dedupe_key = backup-suspect:<판의 때>` — 같은 판으로는 사람마다 한 번. 읽었든 안 읽었든 다시 안 만듭니다 |
| 2-d 없으면 말 안 함 | 의심 판이 없으면 아무것도 안 만듭니다 |
| 2-e 무엇을 하라는지 | 「먼저 운영 DB 가 비지 않았는지 보세요 — 홈에 회차와 업무가 보이면 성한 것입니다. 비었으면 이 판이 아니라 그 앞 날짜의 성한 판으로 되돌립니다(배포-안내 「되돌리려면」). 이 판은 성한 판의 칸에 안 세고, 지울지는 사람이 정합니다」 |
| 2-f 4-0 | 알림 화면의 `.nitem .ttl` · `.nbody` 가 이미 `--fz-md`(14px) · 본문은 `--ink-2` |
| 증거 판 | **알림을 안 세웁니다** — 사람이 이미 알고 남기기로 정한 판입니다 |
| 못 세울 때 | 까닭을 돌려주고 새벽 출력에 찍습니다(`notify_error`) — 조용히 삼키지 않습니다 |

## 3-2. 상한 (지시 3)

| 무엇 | 값 · 까닭 |
|---|---|
| `SUSPECT_KEEP` | **7** — 빈 판이 뜨면 그날 아침 앱 알림에 섭니다. 한 주 동안 아무도 그 알림을 안 봤다면 더 쌓아 둔다고 달라질 것이 없고, 증거로 볼 **처음 빈 날의 판**은 `지키는판` 이 따로 붙듭니다 |
| 성한 30과의 관계 | **겹치지 않는 별도 칸**입니다. 의심 판이 몇이든 성한 판은 30 이 그대로 남습니다 |
| 크기가 넘칠 때 | **의심 판을 먼저**, 그다음 성한 판(마지막 하나는 남김) — 빈 판으로는 되돌릴 수 없어 잃는 것이 가장 적은 쪽부터 |
| `지키는판` | `20260913-030001` 과 까닭(사고의 증거 · 사람이 정함) — 개수·크기·의심 상한·알림 어디에도 안 듭니다 |

## 3-3. 검토가 짚은 한계 — 사고의 모양에서는 알림이 안 섭니다

**운영 DB 가 계정까지 비면 알릴 총무팀이 DB 에 없어 알림이 안 섭니다.** 2026-09-13 의 사고가
정확히 그 모양이었습니다. 그때는 「알릴 총무팀 계정이 없습니다」 가 새벽 출력에만 찍히고,
그 출력은 AZ-c 가 「아무에게도 안 닿는다」 고 한 바로 그 자리입니다. 알림이 서는 때는
사람이 되돌린 뒤 다음 새벽이거나, 계정은 남고 회차만 빈 경우입니다.

CLAUDE.md 11-2 의 「앱 알림에 섭니다」 를 좁히고 봐둘것 AZ-c 의 「남는 것」 에 적었습니다.
**앱 밖 자리를 둘지는 사람이 정합니다**(사람칸 ①).

**그리고 이 알림은 `notify` 를 지나므로 웹 푸시로도 나가려 합니다** — 구독한 총무팀 기기로
새벽 3시에 갑니다. 알림함 알림이 푸시를 함께 시도하는 것은 원래 있던 길이지만, 백업
스크립트에서 바깥으로 나가는 길이 새로 생긴 것이라 적습니다(사람칸 ②).

## 3-4. 잰 것 (수용 기준 나 · 다 · 라 · 마)

| 시험 | 무엇을 보나 |
|---|---|
| `test44_b01_의심_판이_상한을_넘으면_의심_판부터_지우고_성한_판은_남는다` | 성한 셋 + 의심 `SUSPECT_KEEP+3` · 성한 셋 다 남음 · 최근 의심 `SUSPECT_KEEP` 남음 · 오래된 의심 셋 지워짐 |
| `test44_b02_상한을_안_넘으면_아무것도_안_지운다` | 의심 딱 `SUSPECT_KEEP` · 지운 것 없음 |
| `test44_b03_크기가_넘치면_의심_판을_성한_판보다_먼저_지운다` | 판 넷 중 하나를 빼면 들어가는 크기 · 성한 둘 남고 의심 하나만 남음 |
| `test44_b04_증거_판은_상한에도_크기에도_안_지워진다` | `keep=1 · max_total=1` 에서도 증거 판 남음 · 그 판이 실제로 의심으로 잡히는 모양인지(③) |
| `test44_c01_의심_판이_있으면_알림_화면에_그_말이_선다` | **관리자로 `/notifications` 를 받아 글을 읽어** 제목과 「그 앞 날짜의 성한 판으로」 가 있는지 |
| `test44_c02_같은_판으로_두_번_안_선다` | 두 번 불러 새로 선 수 1 → 0 · 알림 행 1 · 화면에 그 판 이름 한 번 |
| `test44_c03_의심_판이_없으면_아무_말도_안_한다` | `(0, None)` · 화면에 그 말 없음 |
| `test44_c04_증거_판으로는_알림을_안_세운다` | 증거 판이 의심인데도 `(0, None)` · 화면에 그 말 없음 |
| `test44_c05_알림_표가_없으면_까닭을_돌려준다` | 못 세우면 까닭이 빈 값이 아님 |

**수를 박지 않았습니다** — `SUSPECT_KEEP` · `KEEP` · `지키는판` 에서 끌어옵니다. 백업은
전부 `tmp_path` 에, 알림은 conftest 가 잡은 시험 DB 에 씁니다. **운영 백업 폴더에는 아무것도
안 썼습니다.**

**고장을 심었습니다** — 시험 파일 사본(`tests/` 에 잠깐 만들고 지움)에 고장 fixture 를 붙여:

```
② 의심 상한을 걷음                     -> b01 빨강
③ 크기 루프가 성한 판을 먼저 지움       -> b03 빨강
④ 지키는판을 셈에서 안 뺌               -> b04 빨강
⑤ dedupe_key 에 시각을 섞음             -> c02 빨강
⑥ 의심 판이 없어도 알림을 세움          -> c03 빨강
⓪ 안 심은 같은 사본                     -> 13 passed
```

**이 사본 여섯을 돌릴 때 `-p no:cacheprovider` 를 붙였습니다** — 12장.

**돌고 있는 서버에서 화면으로는 못 쟀습니다.** 운영의 의심 판은 증거 판 하나뿐이고 그 판은
알림을 안 세우므로 운영 알림 화면에는 설 말이 없습니다. 재려고 운영 백업 폴더에 가짜 빈
판을 넣는 것은 「백업을 손으로 건드리지 않는다」 에 걸려 안 했습니다. 화면 글은 시험의
관리자 클라이언트로 읽었습니다.

---

# 4. 앞 판이 남긴 것 (지시 4)

## 4-1. 「7/7 이 틀렸다」 — 검사로 닫았습니다 (지시 4-a)

앞 판의 틈은 글검사가 파일을 `git ls-files`(추적 중인 것만)로 모아서 **얼리려고 막 복사한
판이 검사 밖**이었던 것입니다. `check_stale` · `check_counts` · `check_names` 의 목록 명령에
`--others --exclude-standard` 를 더해 **gitignore 되지 않은 추적 전 파일도** 봅니다.

```
check_stale  옛 명령 -> 추적 전 파일을 못 봄 · 새 명령 -> 봄
check_counts 옛 명령 -> 추적 전 파일을 못 봄 · 새 명령 -> 봄
```

| 시험 | 무엇을 보나 |
|---|---|
| `test44_d01_추적_전_새_파일도_볼파일에_든다` (셋 각각) | 저장소 안에 추적 안 된 글 파일을 잠깐 만들어 `볼파일` 에 드는지 · 곧바로 지움 |
| `test44_d02_gitignore_된_것은_여전히_안_본다` | 넓혀도 gitignore 된 것(운영 자료)은 안 봄 · `git check-ignore` 판정 그대로 |

**이번에는 그 틈에 안 걸렸습니다** — 이 판은 처음부터 새 파일을 만든 뒤 글검사를 돌렸고,
실제로 `check_named` 가 추적 전 새 파일(`scripts/내프로세스.py`)을 가리키는 문서 줄에서
빨개져 `git add` 로 풀었습니다. **`check_named` 는 안 넓혔습니다** — 그 검사는 「문서가
가리키는 파일이 저장소에 있나」 를 보므로, 추적 전 파일을 있다고 치면 **커밋에서 빠뜨린
파일을 가리키는 문서**가 초록이 됩니다.

## 4-2. phone.js 의 「다음 판에 남습니다」 둘 (지시 4-b)

**이 판에서도 못 했습니다.** 고장을 더하면 11-3 이 자가시험을 두 계정으로 돌리라 하고,
그러려면 개발 서버에 로그인해야 하는데 이 창은 비밀번호를 칠 수 없습니다. 길이 새로
생기지 않았습니다. 봐둘것 AY-a 아래 그대로 남습니다.

## 4-3. 73% 에서 끊긴 전체 시험 (지시 4-c)

**이 판에서는 다시 안 났습니다.** 끝까지 돈 전체 시험은 1492 passed 로 요약 줄까지 나왔습니다(9장). 이 판에서 중간에 끊긴 전체 시험은 **이 창이 `scripts/내프로세스.py` 로 제 것을 골라 멈춘 한 번**뿐이고 까닭을 압니다(2-3 · exit 15). 앞 판의 exit 127 의 원인은 여전히 모릅니다 — 다시 나면 그 로그의 마지막 줄과 그 시각에 도는 다른 창을 함께 적습니다.

---

# 5. 문서 (11-3 3단계)

- **CLAUDE.md** — 11-2(성한 30칸 · 의심 상한 · 크기에서 먼저 · 지키는판 · 앱 알림과 그 한계 ·
  웹 푸시) · 11-3(멈출 때 내 것인지) · 14장(백업 줄 · 내프로세스 줄)
- **봐둘것** — AZ-c 처리함(+ 사고 모양에서 안 서는 것 · 푸시) · AZ-d 처리함(+ 운영이 정당하게
  크게 줄면) · AZ-e 좁힘(+ 자기와 조상 · 규칙의 한계) · AZ-a 표 칸의 옛말
- **옛말** — 「세지도 지우지도 않」 등 · 표기를 어미 앞까지 줄였습니다(검토 [C]: 봐둘것 표
  칸이 어미가 달라 안 걸렸습니다)
- **옛말-넘김** — 얼린 `2026-09-13-빈백업.md`
- **인계** — 머리 · 25번

---

# 6. 글검사 결과

```
━━━ 모아서 ━━━
  통과  check_names · check_stale · check_named · check_counts
  통과  check_handoff · check_placeholder · check_orders

글 검사가 전부 통과했습니다.
```

---

# 7. 돌고 있는 서버 (11-3 2단계)

**건너뛰었습니다** — 이번 커밋에 `app/` · `templates/` · `static/` 이 없습니다(스크립트 · 시험 ·
문서 · requirements 만). 앱은 PID 22972(07:24 시작)가 그대로 돕니다.

---

# 8. 수용 기준 — 각 줄이 무엇을 보나

| 기준 | 어디서 잰다 | 통과 |
|---|---|---|
| 가) 밖은 안 걸리고 안은 걸린다 · 같은 판 · 실제로 만들어 | 2-2 — `test44_a01`·`a02`(밖과 안에 실제 프로세스) · `a05`(CLI 를 부모로 감싸). 고장 ① ⑦ | O |
| 나) 의심 판이 있으면 화면에 말이 서고 없으면 안 선다 · 화면 글을 읽어 | 3-4 — `test44_c01`·`c03`·`c04`(관리자로 알림 화면을 받아 읽음). **운영 서버 화면으로는 못 쟀습니다**(3-4 끝) | O |
| 다) 같은 백업으로 두 번 안 선다 | 3-4 — `test44_c02` · 고장 ⑤ | O |
| 라) 상한을 넘으면 의심 판부터 · 성한 판 안 지워짐 · 수 안 박음 | 3-4 — `test44_b01`·`b02`·`b03` · 고장 ② ③ | O |
| 마) 증거 파일은 어떤 경우에도 안 지워짐 | 3-4 — `test44_b04`(`keep=1 · max_total=1`) · 고장 ④ | O |
| 바) pytest 옵션 없이 전부 · 글검사 7/7 · 얼린 판 git add 뒤 | 9장 · 6장. 이 판은 얼린 판을 새로 안 만들었습니다(앞 판의 것은 이미 추적 중) | O |
| 사) 검토자 원문 | 13장 | O — **원문을 실은 뒤에 검사와 시험을 다시 돌렸습니다** |

---

# 9. 시험

```
1492 passed, 3 warnings in 1575.95s (0:26:15)
```

**건너뜀 0 · 끝까지 돌았습니다.** 이 판에서 중간에 끊긴 전체 시험은 **검토를 받기 전 코드로 시작한 것을 이 창이 도구로 멈춘 한 번**뿐입니다(2-3). 위가 옵션 없이 처음부터 끝까지 돈 판입니다.

---

# 10. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| ① | 빈 백업을 안 지운다 | **했다** — `지키는판` · 손으로도 안 지움 |
| ② | SUSPECT_RATIO 10% 그대로 | **했다** — 안 건드림 |
| ③ | 빈 판 경고를 앱 화면에 | **했다** — 3장. 다만 계정까지 빈 모양에서는 안 섬(3-3) |
| ④ | 의심 판 상한 · 성한 판보다 먼저 | **했다** — 3-2 |
| 1-a · 1-b | (배경) 같은 모양 · 적는 것으로는 안 닫힘 | **했다** — 도구와 시험으로 |
| 1-c | 작업 폴더가 저장소 안인 것만 | **했다** — 2-1. 검토를 받고 자기·조상도 뺌 |
| 1-d | 멈추는 일이 도는 자리를 다 찾아 적기 · 그때그때 치는 것이면 규칙으로 | **했다** — 1장 첫 줄. 스크립트에는 없고 사고는 이 창의 명령 → 규칙+도구 · 규칙의 한계를 적음 |
| 1-e | 밖의 가짜는 안 걸리고 안은 걸림 · 같은 판 | **했다** — 2-2 |
| 1-f | 11-3 에 규칙과 까닭 | **했다** — 2-4 |
| 2-a | 관리자가 평소 지나는 자리 | **했다** — 사이드바 배지 · 알림 화면 |
| 2-b | 이미 있는 알림 자리 | **했다** — `notify` 재사용 · 앱 코드 안 고침 |
| 2-c | 같은 말 반복 안 함 | **했다** — `dedupe_key` |
| 2-d | 없으면 말 안 함 | **했다** |
| 2-e | 무엇을 하라는지 | **했다** — 3-1 |
| 2-f | 4-0 대로 | **했다** — 기존 14px 자리 |
| 3-a | 상한 · 성한 판보다 먼저 | **했다** |
| 3-b | 몇 개 · 까닭 · 성한 30과의 관계 | **했다** — 3-2 |
| 3-c | 넘으면 의심부터 · 안 넘으면 안 지움 · 같은 판 | **했다** — b01 · b02 · 고장 |
| 3-d | 증거 파일을 정리에서 빼고 까닭을 그 자리에 · 시험이 그 파일에 안 걸리는지 | **했다** — `지키는판` · b04 · 시험은 전부 tmp_path 라 운영 파일에 안 닿음 |
| 4-a | 추적 전 판 틈 · 검사로 닫을 수 있나 | **했다** — 4-1. 닫음 |
| 4-b | phone.js 둘 | **안 했다** — 로그인 못 함(4-2) |
| 4-c | 73% exit 127 | **했다** — 4-3 |
| 가)~사) | 수용 기준 | **했다** — 8장 |
| (끝줄) | 운영 자료는 읽기만 · 백업을 손으로 안 지움 | **했다** — 운영 DB·백업 폴더에 아무것도 안 씀 |
| (끝줄) | 앱을 고쳤으면 PID | **했다** — 앱을 안 고쳐 건너뜀(7장) |
| (끝줄) | 마무리는 11-3 | **다르게 했다** — 고장 심기 사본을 `-p no:cacheprovider` 로 돌려 「pytest 앞에 아무것도 끼우지 않는다」 를 글자로 어김(12장) |

---

# 11. 커밋

```
$ git log --reverse --oneline 9cf49c1..HEAD
58d86a7 멈출 프로세스를 작업 폴더가 이 저장소 안인 것만 고른다
f59a88c 빈 백업을 총무팀 앱 알림에 한 번 세우고, 의심 판에 상한을 두되 먼저 지운다
fed8540 글 검사가 추적 전 새 파일도 본다
9231783 멈출 범위 · 빈 판 알림 · 의심 판 상한 · 추적 전 파일을 실제로 만들어 잰다
a070ceb 무엇을 멈출 때 내 것인지 먼저 본다고 적고, 빈 판 알림과 상한을 문서에 옮긴다
6cbd8d2 멈출 프로세스를 고를 때 도구 자신과 그것을 부른 셸·런처를 뺀다
5ee99e2 빈 판 알림이 사고 모양에서는 안 선다는 것과 웹 푸시로도 나간다는 것을 적는다
(그리고 이 보고의 커밋)
```

---

# 12. 이 판이 어긴 것 — `-p no:cacheprovider`

3-4 의 고장 심기 사본 여섯을 돌리며 `-p no:cacheprovider` 를 붙였습니다. **그 옵션은 기본
플러그인 하나를 끄는 것이라 앱을 conftest 보다 먼저 읽히지 않고**, 운영 DB 비움의 입구가
아닙니다. 그래도 11-3 의 규칙은 글자로 예외를 두지 않습니다 — 그 규칙을 이 날 적은 창이
같은 날 어겼습니다. 안 심은 사본(⓪)과 그 뒤의 모든 실행은 옵션 없이 돌렸습니다.
**예외를 둘지는 사람이 정합니다**(사람칸 ③).

---

# 13. 검토자 원문

**커밋 전 검토를 받았고 답을 통째로 싣습니다.** 한 글자도 안 가렸습니다.

| 라벨 | 무게 | 어떻게 |
|---|---|---|
| [A] | 막아야 함 | **맞습니다.** 자기와 조상(`psutil.Process().parents()`)을 빼고, CLI 를 부모로 감싸 재는 `test44_a05` 를 더했습니다. 옛 코드 사본에서 5개를 고르고 새 코드는 2개 — 2-2 |
| [B] | 고쳐야 함 | CLAUDE.md 11-2 의 「섭니다」 를 좁히고 봐둘것 AZ-c 「남는 것」 에 적었습니다. 구조는 사람칸 ① |
| [C] | 고쳐야 함 | 봐둘것 표 칸과 `test_stage43` 머리를 고치고, 옛말 표기를 어미 앞까지 줄였습니다 |
| [D] | 봐 둘 것 | 12장 · 사람칸 ③ |
| [E] | 고쳐야 함 | `test44_d01` 에 `check_names` 를 더했습니다 |
| [F] | 봐 둘 것 | CLAUDE.md 11-2 · 봐둘것 AZ-c · 3-3 · 사람칸 ② |
| [G] | 봐 둘 것 | 훑을 때의 `Process` 를 넘기게 고쳤습니다 |
| [H] | 봐 둘 것 | 봐둘것 AZ-d 「남는 것」 에 적었습니다 |

```
━━━ 검토자 여기부터 ━━━ 범위 9cf49c1..a070ceb · 커밋 5개 · 파일 12개

돌린 것(읽기와 목록만. 멈춘 프로세스 없음)
  pytest tests/test_stage44.py tests/test_backup.py -> 22 passed (옵션 없음)
  scripts/글검사.py -> 7개 모두 exit 0
  내프로세스.py 를 멈춤 없이 한 번 돌림. 아무 프로세스에도 안 맞는 무늬를 줌 (아래 [A])
  data/backups 는 ls 로 파일 이름만 봄. 열지 않음

[A] 막아야 함 — 내프로세스.py 가 자기를 부른 셸과 자기 런처까지 "이 저장소 안" 으로 고른다
  scripts/내프로세스.py 훑는다() 51~66줄. 빼는 것이 os.getpid() 하나뿐이다
  재 본 것: 무늬 "zz-review-marker-7731" 로 돌림(그 무늬를 가진 진짜 프로세스는 없음)
    이 저장소 안 — 4개
      Git bash.exe -c ... (이 명령을 친 셸) 셋
      .venv\Scripts\python.exe scripts/내프로세스.py zz-review-marker-7731 (런처)
  무늬는 명령줄 인자로 들어가므로 부른 쪽 명령줄에 늘 들어 있다. 그 셸의 작업 폴더도
  이 저장소다. 그래서 --멈춤 을 붙이면 그 명령을 친 셸과 .venv 런처를 멈춘다.
  무늬가 "-m pytest" 여도 똑같다. 11-3(CLAUDE.md 신규 문단)이 이 도구를 안전한 길로
  가리키므로 규칙을 따른 사람이 맞는다. 이 판이 막으려던 "범위를 안 좁혔다" 와 같은 모양이다.
  시험이 못 잡은 까닭: test44_a01/a02 는 함수를 프로세스 안에서 부르고 무늬가 uuid 라서
  부른 쪽 명령줄에 무늬가 없다. CLI 로 부르는 모양은 한 번도 안 쟀다
  고칠 방향(판단은 구현자): 자기와 조상(psutil.Process().parents())을 빼기.
  시험은 CLI 를 subprocess 로 띄워, 그 부모가 "내 것" 에 안 드는지 재기
  문서도 같이 고칠 것: CLAUDE.md 11-3 과 봐둘것 AZe 는 못 가르는 것을 "이 저장소 안의
  다른 창" 뿐이라고 적었다

[B] 고쳐야 함 — 빈 판 알림이 정작 그 사고 모양에서는 안 선다
  scripts/backup.py 알린다() 466~474줄
  AZ-a 의 모양은 "표가 전부 빔, 스키마만 남음" 이다(봐둘것 2998줄). 그러면 users 가 0 이라
  "알릴 총무팀 계정이 없습니다" 를 돌려주고, 그 까닭은 새벽 작업 출력에만 찍힌다.
  그 출력은 AZ-c 가 "아무에게도 안 닿는다" 고 한 바로 그 자리다
  알림이 서는 때: 사람이 되돌린 뒤 다음 새벽(dedupe 라 남은 의심 판마다 한 번)이거나,
  계정은 남고 회차만 빈 경우뿐이다
  그런데 CLAUDE.md 11-2 신규 문단은 "빈 판이 뜨면 총무팀의 앱 알림에 섭니다" 라고 쓰고,
  봐둘것 AZ-c 는 "처리함" 이다. 이 한계가 어디에도 안 적혀 있다.
  고칠 것: 적어도 AZ-c 의 "남는 것" 에 한 줄 넣고, 11-2 에서 "…에 섭니다" 를 좁히기.
  구조를 바꿀지(점검 화면이 백업 폴더를 직접 읽는 등)는 사람에게 묻는 것 1

[C] 고쳐야 함 — 낡은 주장이 산 문서에 남았는데 옛말 검사가 못 잡았다(지시문 4-a 와 같은 모양)
  docs/봐둘것.md 3020줄(AZ-a 표 "주의" 칸)에 "빈 판은 세지도 지우지도 않고" 가 있다
  docs/옛말.md 452줄에 적은 표기는 "빈 판은 세지도 지우지도 않아" 로 어미가 달라 안 걸린다
  → check_stale 초록. 표기를 어미 앞까지 줄이거나 하나 더 적을 것
  tests/test_stage43.py 5줄 머리 독스트링 "의심스러운 판은 세지도 지우지도 않는지 잰다" 도
  낡았다(시험 자체는 통과함). 봐 둘 것

[D] 봐 둘 것 — 11-3 "pytest 앞에 아무것도 끼우지 않는다" 를 어겼다(구현자가 스스로 밝힘)
  고장 심기 사본을 -p no:cacheprovider 로 돌렸다. 그 플러그인은 app 을 먼저 읽히지 않으니
  AZ-a 입구는 아니다. 그래도 규칙은 글자로 예외를 두지 않는다.
  보고 본문에 어긴 사실과 까닭을 적어야 한다. 예외를 둘지는 사람이 정한다(묻는 것 3)

[E] 고쳐야 함(가벼움) — check_names 를 넓혔는데 그 시험이 없다
  scripts/check_names.py 370줄에 --others --exclude-standard 를 붙였는데,
  test44_d01 은 check_stale·check_counts 만 parametrize 한다.
  검토-원칙 2-③ "실제로 보나" 가 check_names 자리에서 안 재진다

[F] 봐 둘 것 — 새벽 백업이 이제 웹 푸시를 내보낸다
  알린다() → notify() → _try_push → push_notifications. 구독이 있는 총무팀 기기로 새벽 3시에 간다.
  알림함 알림이 푸시를 함께 시도하는 것은 원래 있던 모양이라 잘못은 아니다.
  다만 백업 스크립트에서 바깥으로 나가는 길이 새로 생겼는데 11-2·보고 어디에도 안 적혀 있다

[G] 봐 둘 것 — 멈춘다() 는 목록을 만든 뒤 PID 로 Process 를 다시 만든다
  내프로세스.py 77~84줄. 고르고 멈추기까지 사이에 그 PID 가 다른 프로세스로 다시 쓰이면
  엉뚱한 것을 멈춘다. 훑을 때 만든 Process 객체(create_time 을 가짐)를 넘기면 닫힌다

[H] 봐 둘 것 — 실제 DB 가 줄어든 경우(드묾)
  suspects() 의 중앙값 10% 기준에서, 운영이 정당하게 크게 줄면 새 판이 날마다 의심이 된다.
  그러면 SUSPECT_KEEP=7 만 돌고 옛 성한 30벌이 안 돌며, dedupe 가 판마다라 알림이 매일 하나씩 선다.
  ② SUSPECT_RATIO 10% 는 사람이 그대로 두기로 했으니 적어만 둔다

확인했고 문제 없음
  prune 크기 루프 `while 합() > max_total and (의심 or len(kept) > 1)`
    의심 판을 먼저 비우고, 성한 판은 마지막 하나를 남긴다. 성한 판만 있을 때는 전과 같다.
    달라진 점은 의심 판 크기도 합에 드는 것인데, 드는 즉시 먼저 지워지므로 성한 판을 더 밀지 않는다.
    지키는판 크기는 합에 안 든다(빈 판이라 작다)
  지키는판은 stamps_in 뒤 곧바로 빠져 개수·크기·의심 상한·알림 어디에도 안 든다. b04·c04 가 재고,
    ③ 도 있다(그 판이 실제로 의심으로 잡히는 모양인지). 수용 기준 마) 충족
  의심 상한은 SUSPECT_KEEP·KEEP 에서 끌어오고 수를 안 박았다(b01·b02). 라) 충족
  dedupe_key 는 판마다, 같은 사용자에게 두 번 안 만든다(app/notifications.py 123~129줄. 읽음 여부와 무관). c02. 다) 충족
  retreat_id None 알림은 배지(badge_unread_count)와 알림 목록 둘 다에 든다. .nbody 는 --fz-md·--ink-2 라 4-0 에 맞다
  /settings/checkup 경로가 있다. 점검 화면이 빈 판을 말하지 않는 것은 AZ-c "남는 것" 에 적혀 있다
  test44 가 운영 백업 폴더를 안 쓴다(tmp_path). _운영DB() 는 이름과 달리 conftest 가 잡은 시험 DB 다
    (이름이 오해를 부르지만 동작은 맞다)
  ls-files 확장은 gitignore 를 지킨다(d02). data/backups 의 비운판-* 은 app-*.db glob 밖이다
  실명·전화·계좌·토큰·바깥 주소: diff 에서 못 찾음

보고
  아직 없어서 사람칸·지시문 대조 표·고치기 전 훑기(검토-원칙 8-1·8-2·9)는 못 봤다.
  docs/review/최근.md 는 앞 판 것이다.
  보고 쓴 뒤 글검사·pytest 를 옵션 없이 다시 돌려야 한다. 이번에 나는 두 파일만 돌렸다.
  바) 의 "전부" 는 확인하지 않았다

사람에게 묻는 것
  1. [B] 운영 DB 가 계정까지 비는 사고 모양에서 알림이 못 선다.
     "적어 두고 둔다" 와 "앱 밖 자리(점검 화면이 백업 폴더를 읽는 등)도 둔다" 중 어느 쪽인가
  2. [F] 새벽 백업 알림이 웹 푸시로도 나가도 되는가
  3. [D] 11-3 "pytest 앞에 아무것도 끼우지 않는다" 에 app 을 안 읽히는 -p no:… 를 예외로 둘 것인가,
     글자 그대로 둘 것인가
━━━ 검토자 여기까지 ━━━
```
