사람이 할 것
노트북에서 직접: 없음 — 이번 판은 읽기만 했다(3장)
관리자 권한: 없음 — app/ · templates/ · static/ 을 안 건드려 재시작하지 않았다(8장)
정할 것: 다음 판의 갈래 — 업무를 어떻게 맞출지(ㄱ1·ㄱ2·ㄴ·ㄷ) · 회의록과 업무를 같은 판에 할지 · 외부 id 칸을 먼저 둘지 · 다음 회차에 복제된 run 도 범위에 넣을지(6장 · 인계 1))
막혀서 못 한 것: 없음

작성 시각: 2026-09-15 22:10 KST (검토 뒤 고침 22:50)
커밋: d612539 (앞 판 얼림) · 2ee8f87 (보고 · 인계) · 071bdff (검토 뒤 고침) · 두 번째 검토 뒤 고침은 그다음 커밋 — 문서만
작업: 노션 대조 1판 — 값 없이 모양만 비교

---

**값은 여기 없다.** 제목 · 날짜 · 사람 이름은 저장소 밖 `data/노션대조.real.md` (짝지은 표 · 노션만 · 앱만 · 구분 없음 목록)와
`data/노션업무.real.tsv` (노션에서 읽은 줄)에만 있다. 짝짓기 코드는 `data/노션대조.real.py` 로 남겨 다시 셀 수 있다
(검토 6번 · 인자로 저장소 경로를 준다 · 운영 DB 는 mode=ro). 셋 다 `data/*.real.*` 로 gitignore 된다.
tsv 에는 노션 페이지 id 와 편집 시각 칸이 없다.
여기에는 건수 · 속성 이름 · 구조만 적는다.

# 0. 고치기 전 훑기 (11-3 0단계) · 들여온 경로

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 없음 — 코드는 안 고친다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 |
| 그 값을 세어 둔 문서 | 인계 「진행 중 1) 데이터 동기화」 의 원본 수 줄 · `docs/review/2026-09-09-도막0.md`(얼린 판 — 안 고친다) · CLAUDE.md 13장의 노션 속성 목록(5장에서 어긋남 하나를 적었고 고치지 않았다) |

## 0a. 노션 자료가 앱에 들어온 길

| 무엇 | 언제 · 무엇으로 | 매핑 |
|---|---|---|
| **회의록** | 2026-09-02 (`0dd2285`) · `scripts/import_meetings.py` — **노션 API 가 아니라** 노션에서 내보낸 `.md` 파일(`data/notion-meetings`)을 읽는다. 미리보기 기본 · `--apply` · `--until` · `--undo`(묶음) | 파일 하나를 `app/domain/meeting_import.py` 의 `cut` 이 회의 여럿으로 자르고, 출처는 파일 이름(`source_ref`) — **노션 페이지 id 는 저장하지 않는다**(도막 0 의 0-(a)) |
| **업무** | **들여오는 스크립트가 없다.** 아래 0b | — |

`tests/test_meetings_import.py` 는 자르기 · 사람 평가 짚기 · 되돌리기를 잰다 — 노션 쪽과 다시 맞추는 길은 없다.

## 0b. 앱↔노션 동기화 · 들여오기 코드

**없다.** 저장소에 노션 API 를 부르는 코드가 없고, 업무를 들여오는 스크립트도 없다. 찾은 것: `git ls-files` 의 파일 이름에 노션이 든 것은 0 이고,
노션 낱말이 **내용에** 든 파일은 회의록 들여오기(`scripts/import_meetings.py` · `app/domain/meeting_import.py`) · `app/models.py` 의 출처 표기 ·
`scripts/check_dev_db.py` · 시드(`seed.py` · `seed_data.py` · `seed_library_data.py` — 손으로 옮긴 출처를 적은 것) · 시험 몇 개다 — 어느 것도 API 를 부르지 않는다.
업무가 앱에 선 길은 둘이고 둘 다 **사람이 손으로 옮긴 시드**다 — 4장.

# 1. 노션 쪽 (값 없이)

**업무 DB 한 개**(2026 여름수련회 업무 · data source 하나). 필터 없이 전부 읽었다.

| 무엇 | 건수 |
|---|---|
| 업무 DB 전체 줄 | 257 |
| 업무속성 Main | 87 |
| 업무속성 Sub | 74 |
| 업무속성 Sch | 62 |
| 업무속성 없음 | 34 (그중 **D-주차 표지 24** — 주차를 가르는 머리 줄) |
| Main 중 제목이 회의록 · 미팅 · 피드백 꼴 | 22 |
| 회의록 페이지 | 10 — **전부 마지막 편집 시각이 내보낸 파일의 수정 시각(2026-09-02 오후) 보다 앞** — 날짜가 아니라 시각으로 견줬다. 다만 한쪽 끝은 내보낸 시각이 아니라 파일이 디스크에 쓰인 시각이라, 내보낸 뒤 늦게 옮겨 담았다면 그 사이 고친 것은 0 으로 셈해진다 |

## 1a. 노션 속성과 앱 칸 — 짐작 매핑

| 노션 속성 | 종류 | 앱 칸 (**짐작**) |
|---|---|---|
| 할 일 | title | `task_library.title` |
| 업무속성 | select (Main · Sub · Sch) | `task_library.kind` (main · sub · schedule) |
| 담당팀 | select | `task_runs.department_id` → `departments.key` · 라이브러리의 기본 부서 |
| 관련업무 | relation (같은 DB) | `task_library.related_library_ids` |
| 일자 | date | `task_runs.start_date` · `end_date` (한 날짜) |
| 일자/기간 (타임라인용) | date (범위) | `task_runs.start_date` · `end_date` — **대조는 이것을 먼저, 없으면 일자** |
| 진행 상황 | select (To-do · W-I-P · Com-) | `task_runs.status` (대기 · 진행중 · 완료) |
| 행사 전후 | select (기획 · 준비 · 진행 · 후속) | 없음 |
| 사람 | person | `task_runs.assignee_id` — 앱은 계정에 잇고 노션은 노션 사용자라 **바로 못 잇는다** |
| 설명 | text | `task_library.rules` 또는 논의 — 갈린다 |
| 참여자or작성자 | text | 없음 |
| 파일과 미디어 | file | 첨부(`task_attachments`) — 회차별 |
| (페이지 본문) | — | 논의(`discussion_entries`) — **옮긴 적 없음**(4장) |

# 2. 앱 쪽 (운영 DB · mode=ro · 2026 회차)

**운영에 회차가 둘이다** — 2026 여름(retreat 1)과 그것을 복제한 다음 회차(retreat 2). 아래는 전부 **retreat_id 로 2026 여름만 걸러** 셌다.
다음 회차에도 run 이 서 있다(history 102 · included 96 + 제안 4) — 대조에는 안 넣었다.

| 무엇 | 건수 |
|---|---|
| 업무 run | 102 (included 96) |
| main · sub · schedule | 42 · 52 · 8 |
| run 의 라이브러리 origin — history · claude_suggestion | 102 · 0 (제안 4 의 run 은 전부 다음 회차에 있다) |
| 출처 회의록이 붙은 run | 0 |
| 이 회차 run 의 논의 | 28 |
| 회의록 | 53 — 노션 origin 50(출처 파일 10) · origin 빈 것 3(앱에서 만든 것) |
| 옛 `tasks` 표 | 175 — 아무 화면도 안 읽는다(4장) |

# 3. 대조표 (건수만)

**운영 자료는 읽기만 했다** — DB 는 `mode=ro`, 노션은 조회만(쓰기 도구를 안 불렀다), 개발 DB · 백업은 안 열었다.

장 이름을 지시문 번호에 맞췄다 — 3a 네 갈래 · 3b 저장소 밖 자료 · 3c 차이 수(검토 8번).

## 3a. 네 갈래

**업무** — 짝짓기: 제목에서 빈칸 · 괄호 · 문장부호를 떼고 소문자로 맞춘 뒤 ① 같은 것끼리 1:1 ② 남은 것 중 닮음 0.6 이상이거나 한쪽이 다른 쪽을 품는 것을 가장 닮은 순으로 1:1. 노션은 업무속성이 있는 223줄만 넣었다(구분 없는 34줄은 뺐다).

| 갈래 | 건수 | 노션 Main · Sub · Sch / 앱 main · sub · schedule |
|---|---|---|
| 노션만 | 143 | 55 · 44 · 44 |
| 앱만 | 22 | 17 · 5 · 0 |
| 둘 다 · 제목 다름 | 30 | — |
| 둘 다 · 제목 같음 | 50 | — |

셈 맞추기: 노션 143 + 30 + 50 = 223 · 앱 22 + 30 + 50 = 102.

**짝짓기의 한계** — 네 갈래 수와 3c 의 차이 수는 짝이 맞았다는 전제 위에 있다.

- **제목 다름 30** 은 닮음으로 이은 것이라 눈으로 보니 짝이 의심스러운 것이 몇 건 있다 — 사람이 확인하기 전에는 짝이 아니다
- **제목 같음 50** 도 틀릴 수 있다 — 노션에 같은 제목이 여러 줄이면(회차 · 단계마다 한 줄) 그중 첫 줄과 이었다. 다른 줄과 이었으면 날짜 · 구분 차이 수가 달라진다
- **같은 제목인 노션 뒷줄은 닮음 단계로 넘어간다** — 거기서 다른 앱 줄과 이어지면 「제목 다름 30」 이 부풀려진다
- **가장 닮은 순으로 1:1** 이라 앞에서 잘못 먹은 짝이 뒤의 맞는 짝을 밀어낼 수 있다
- **구분 없는 34줄을 뺐다** — D-주차 표지 24 를 뺀 10줄은 표지가 아니다. 그 안에 앱 업무와 짝이 되는 줄이 있으면 「앱만」 이 부풀려진다

**회의록**

| 갈래 | 건수 |
|---|---|
| 노션만 (페이지는 있는데 앱에 없음) | 0 |
| 앱만 (앱에서 만든 회의록) | 3 |
| 둘 다 (파일 이름으로 짝지음) | 10 — 노션 페이지 10 · 내보낸 파일 10 · 앱 출처 10. 파일 이름과 앱 출처는 글자 그대로 10/10 같다 |
| 둘 다 · 제목 같음 / 다름 | 세지 않음 — 노션 페이지 제목과 앱 글자를 견준 기록이 없다 |
| 내보낸 뒤 노션에서 고친 페이지 | 0 (1장 — 시각으로 견줌) |

회의록은 **페이지 단위로는 맞다.** 페이지 하나가 앱의 회의 여럿으로 잘려 50 이 됐다.

## 3b. 자세한 것 — 저장소 밖

`data/노션대조.real.md` 에 짝지은 표(노션 쪽 · 앱 쪽 구분 · 담당 · 시작 · 마감과 다른 칸 표시) · 노션만 목록 · 앱만 목록 · 구분 없음 목록.
저장소에는 이 보고와 인계의 건수만 들어간다.

## 3c. 짝지은 80건 안의 차이

| 차이 | 건수 |
|---|---|
| 구분 다름 | 33 |
| 담당 다름 | 12 (그중 노션 담당이 봉사팀 공통 — 앱에서 해체한 부서 — 10) |
| 시작일 다름 | 11 |
| 마감일 다름 | 15 |
| 앱에서 빼 둔 run 과 짝 | 0 |

위 한계 때문에 이 수에는 **짝 오류분이 섞였을 수 있다**(4장).

# 4. 원인 짐작 (고치지 않았다)

**앱의 업무 102건은 API 나 스크립트로 들여온 것이 아니다 — 사람이 노션을 재분류해 목업을 거쳐 손으로 적은 시드다.**
노션에서 온 것은 맞다: CLAUDE.md 6-9 가 「노션에서 정리한 준비 업무」 를 재료로 들고, 목업(`retreat-board-v4.html`) 안에도 노션 항목을 재분류한 까닭이 적혀 있다(검토 4번).
드러난 것은 **줄 단위로 노션과 잇는 열쇠 없이 옮겼다**는 데까지다. 근거:

| 커밋 | 무엇 |
|---|---|
| `f83b2c5` 2026-08-27 「실제 수련회 데이터 반영」 | `seed_data.py` 가 노션의 「전체팀 Main업무」 보기에서 업무 175건을 **손으로 옮겨** 옛 `tasks` 표에 넣었다(커밋 메시지와 파일 머리가 그 보기를 가리킨다). 운영의 옛 `tasks` 가 175 로 같다 |
| `730541a` 2026-08-29 「Phase 1 재설계 구현」 | `seed_library_data.py` 가 새 라이브러리 · run 을 세웠다. 파일 머리의 출처는 **목업**(`retreat-board-v4.html` · `retreat-setup.html`)이고, 줄마다 「재분류 사유」 칸이 있다(CLAUDE.md 4-2 의 재분류 원칙 · 2장의 봉사팀 공통 해체). 운영 라이브러리의 history 102 가 전부 이날 만들어졌다 |
| UI 개편 단계 3 | 옛 `/tasks` 화면을 걷었다 — 175건은 옛 표에 남았고 **지금 아무 화면도 안 읽는다**(CLAUDE.md 14장 옛 화면 표) |
| `0dd2285` 2026-09-02 | 회의록만 내보낸 파일에서 들여왔다. 업무 쪽 짝은 없다 |

**수가 다른 까닭 (짐작)**

- **노션이 더 잘다.** 노션만 143 중 Sch 44 · Sub 44 — 회차마다 · 단계마다 한 줄씩 있는 것을 앱 시드는 하나로 묶었다(4-2 「같은 의뢰건이 여러 Main 으로 흩어져 있으면 하나로 묶는다」)
- **노션 Main 에 업무가 아닌 줄이 섞였다** — 회의록 · 미팅 · 피드백 꼴 22, D-주차 표지 24(구분 없음). 앱은 회의록을 따로 표로 두고 주차는 계산한다
- **구분 다름 33** — 일부는 시드의 재분류로 보인다(참석만 하면 되는 것은 일정 · 순서가 있는 것은 상하위 뒤집기 — 4-2). 다만 `seed_library_data.py` 에서 「재분류 사유」 칸이 찬 줄은 16 이고 그 16 이 전부 분류 재분류는 아니다(부서를 옮긴 사유도 있어 보이나 세지 않았다) — **기록된 재분류가 33 보다 적다.** 나머지는 사유 없이 바꾼 것이거나 짝 오류분(3a 한계)이고, 이번 판은 둘을 가르지 않았다
- **담당 다름 12 중 10** — 봉사팀 공통을 해체해 특정 팀으로 옮긴 것으로 보인다(2장)
- **앱만 22 (main 17)** — 시드가 여러 줄을 묶어 새 이름을 붙였거나 목업에서 온 것으로 짐작. 짝짓기가 못 잡은 이름 바꿈과 구분 없는 10줄(3a 한계)도 섞였을 것이다. **제안 4 는 아니다** — 그 run 은 다음 회차에 있다(2장)
- **날짜 다름(시작 11 · 마감 15)** — 시드는 D-주차 · 요일 오프셋 · 기간으로 적어 개회일에서 다시 셈한다(14장). 노션의 절대 날짜와 갈릴 수 있고, 사람이 보드에서 옮긴 것(6-4)과 짝 오류분도 섞였을 수 있다 — 가르지 않았다
- **여러 회차가 섞였나** — 앱 쪽은 retreat_id 로 2026 여름만 걸러 셌으니 섞이지 않았다(운영에는 회차가 둘이다 — 2장). 노션 쪽은 **DB 이름**이 2026 여름이라는 것만 근거이고, 줄의 날짜 범위로 확인하지는 않았다
- **나중에 노션을 고쳤나** — 업무 DB 줄의 편집 시각은 **안 봤다.** 회의록 페이지는 내보낸 뒤 고친 것이 0 이다
- 인계의 원본 수(업무 313 · 일정 160)와 이번에 읽은 257 이 다르다 — 그 줄은 `266a352`(2026-09-09 · 인계를 처음 만든 커밋)가 넣었고, 그 커밋 본문과 도막 0 보고에도 무엇을 어떻게 셌는지는 없다. 거기까지 찾아봤고 **까닭을 모른다**

# 5. 문서와 어긋난 것 (적기만 했다)

- CLAUDE.md 13장이 노션 업무 DB 속성으로 「관련 팀」 을 든다 — 이번에 읽은 속성 목록에는 그 이름의 속성이 없다(짐작: 다른 보기나 옛 DB 의 것). 고치지 않았다

# 6. 다음에 정할 것 — 갈래

**업무를 어떻게 맞출지**

| 갈래 | 무엇 | 얻는 것 | 잃는 것 |
|---|---|---|---|
| **ㄱ. 노션을 원본으로 다시 들여오기** | 노션 줄을 외부 id 와 함께 세운다. 무게가 둘이다 — **ㄱ1** 앱 시드 102 는 「원본에 없음」 표시로 두고 노션 줄을 전부 새로 세운다(인계 1) 의 방향) · **ㄱ2** 짝이 확인된 것은 앱 run 에 id 만 달아 살리고 노션만 쪽만 새로 세운다 | 노션과 1:1 · 앞으로 대조가 기계로 된다 | 시드의 재분류 · 묶음 · 봉사팀 공통 해체를 줄마다 다시 정해야 한다(4-2 · 2장) — ㄱ1 은 전부, ㄱ2 는 노션만 쪽. 노션에는 업무가 아닌 줄(회의록 꼴 22 등)이 섞여 걸러야 한다. ㄱ1 은 이미 붙은 논의 28 · 첨부 · 담당자를 어느 run 에 둘지 정해야 한다 |
| **ㄴ. 앱을 원본으로 두고 어긋난 것만 맞추기** | 짝이 확인된 것에 외부 id 만 달고, 노션만 143 은 사람이 「넣음 · 안 넣음(묶임) · 버림」 을 고른다 | 재분류 결정이 산다 · 붙은 기록이 안 흔들린다 | 143 을 사람이 봐야 한다. **짝 확인을 건너뛰면 틀린 짝이 외부 id 로 굳는다**(3a 한계 — 제목 다름 30 만이 아니라 같음 50 도). 노션 쪽의 잘게 나뉜 줄이 앱에 없는 채 남는다 |
| **ㄷ. 맞추지 않고 노션을 닫기** | 2026 여름은 앱이 정본이라 선언하고(재정 7-1 과 같은 꼴) 노션은 보관만 한다 | 일이 가장 적다 | 노션과 잇는 열쇠가 영영 없다 — 다음 회차에 노션을 다시 볼 일이 생기면 이번 대조를 처음부터 다시 한다 |

**셋 모두에 같은 대가** — 노션 페이지 본문(논의)은 어느 갈래도 저절로 옮기지 않는다. 옮기지 않으면 다음 회차 담당자가 그 기록을 앱에서 못 본다(0장 둘째 원칙) — 아래 「함께 정할 것」 셋째 줄.

**함께 정할 것**

- **회의록과 업무를 같은 판에 할지** — 회의록은 페이지 단위로 이미 맞아 남은 일이 **페이지 id 를 다는 것**뿐이고, 업무는 위 갈래부터 정해야 한다. 이 창의 짐작은 **나눈다**(회의록이 먼저 · 작다)
- **외부 id 칸을 먼저 둘지** — ㄱ · ㄴ 둘 다 필요하다. 어느 표 어느 칸에 둘지는 인계 1) 에 이미 「미정」 으로 있다
- **노션 페이지 본문(논의)을 옮길지** — 이번에 안 셌다. 옮기면 CLAUDE.md 9장의 사람 평가 짚기를 지나야 한다

# 7. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 수가 비교됨 | O | 1 · 2장 |
| 나) 네 갈래 | O | 3a |
| 다) 원인 짐작 · 코드 이력 근거 | O | 4장 |
| 라) 다음 갈래 | O | 6장 |
| 마) 코드 diff 없음(문서만) | O | 9장 파일 목록 |
| 바) pytest 옵션 없이 · 글검사 7/7 | O | 9장 |
| 사) 검토자 원문 | O — 첫 검토 · 두 번째 검토 둘 다 | 10장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 머리 · 고치지 않고 들여오지 않는다 | 했다 | 9장 파일 목록이 문서뿐 · 노션 쓰기 도구를 안 불렀다 |
| 머리 · 보고와 덩어리에 값을 넣지 않는다 | 했다 | 값은 `data/*.real.*` 에만 · 검토자가 첫 커밋과 검토 뒤 고친 차이에서 값이 없음을 봤다(10장). 덩어리는 검토자가 못 본다 — 보고의 사본으로만 낸다 |
| 0a | 했다 | 0a — 노션 API 가 아니라 내보낸 파일이었다 |
| 0b | 했다 | 0b — 동기화 코드 없음 |
| 1 | 했다 | 1장 · 1a(매핑은 짐작 표시) |
| 2 | 했다 | 2장 — retreat 1 만 걸렀다 |
| 3a | 했다 | 3a — 업무 · 회의록 |
| 3b | 했다 | 3b — 값 파일은 저장소 밖 · 저장소에는 건수만 |
| 3c | 했다 | 3c — 날짜 · 담당 · 구분 차이 수 |
| 4 | 했다 | 4장 — 업무 DB 줄의 편집 시각은 안 봤다고 적었다 |
| 5 | 했다 | 6장 |
| 운영 자료 읽기만 · 노션 읽기만 | 했다 | 3장 머리 |
| 앱을 안 고치면 재시작 안 함 | 했다 | 8장 |
| 마무리는 11-3 대로 | 다르게 했다 | 첫 커밋(2ee8f87)을 **보고를 다 채우기 전에** 했고 수용 기준 바) · 사) 에 O 를 먼저 찍었다(검토 [H] 1). 검토 뒤 고침에서 바로잡았다. 마지막 pytest 는 보고를 다 채운 뒤 돌린다 — 결과는 9장 끝 줄 |

# 8. 재시작

**건너뛰었다** — 커밋에 `app/` · `templates/` · `static/` 이 없다(문서만). 운영 서버는 손대지 않았다.

# 9. 검사와 시험

- 커밋 전 `git status --short`: 첫 커밋 때 `docs/review/최근.md` · `docs/인계.md` · 얼린 판 · `docs/옛말-넘김.txt` 뿐 — `data/` 안의 것은 안 잡혔다(gitignore). 검토 뒤 고침에서도 같다
- 글검사: 첫 커밋 전 7/7(얼린 판이 옛말 검사에 한 번 걸려 넘김 한 줄) · 검토 뒤 고침(071bdff) 커밋 전 7/7 · 두 번째 검토 뒤 고침 커밋 전 7/7
- 바뀐 파일(`git diff --name-only 5255fda..HEAD`): `docs/review/최근.md` · `docs/review/2026-09-15-점검마무리.md`(앞 판 얼림) · `docs/옛말-넘김.txt` · `docs/인계.md`
- `docs/봐둘것.md` 를 열었다 — 노션 대조로 닫을 항목도 새로 미룰 항목도 없다(미룬 것은 인계 1) 에 적었다)
- 보고를 다 채운 뒤 돌린 pytest: `.venv\Scripts\python.exe -m pytest` → `1599 passed, 3 warnings in 1131.16s (0:18:51)`

# 10. 검토자 원문

## 10-1. 첫 검토 (범위 5255fda..2ee8f87)

```
━━━ 검토자 여기부터 ━━━ 범위 5255fda..2ee8f87 · 커밋 2개 · 파일 4개

대상: docs/review/최근.md(새 보고) · docs/인계.md · docs/옛말-넘김.txt · 얼린 판 2026-09-15-점검마무리.md
코드 diff 는 없다(마) 확인). 2ee8f87 은 아직 origin 에 없다(git branch -r --contains 가 비어 있음).
운영 DB 는 mode=ro 로 건수만 다시 셌다. data/*.real.* 은 갈래 머리와 구분 칸 건수만 봤다. 값은 옮겨 적지 않는다.

[H] 1. 돌리지 않은 검사에 O 를 찍었다 — docs/review/최근.md:175-176 · :201 · :203 · :207
    수용 기준 바)(pytest · 글검사 7/7)와 사)(검토자 원문)에 O 가 찍혀 있다.
    그런데 9장은 「결과는 검토 뒤 이 줄에 채운다」, 10장은 「커밋 뒤 검토자를 부르고」 라고만 적혀 있다.
    커밋 시점에는 그 O 를 받칠 근거가 없다. CLAUDE.md 14장이 check_placeholder 를 만든 까닭,
    곧 「수용 기준에 O 를 찍어 두고 그 장이 빈 자리」 와 같은 모양이다.
    이 판에서는 사람 말로 적혀 있어 check_placeholder 가 못 잡는다. 돌려 보니 rc=0 이었다.
    11-3 4단계 순서는 「보고를 다 채운 뒤 글검사 · pytest → 커밋」 이다.
    이 판은 채우기 전에 커밋했고, 보고 어디에도 git status 훑기 결과가 없다.
    푸시 전이므로 채우고 다시 돌린 뒤 커밋해야 한다.
    채우기 전까지는 두 칸을 O 가 아니라 「검토 뒤」 로 적어야 한다.

[M] 2. 원인 짐작이 DB 와 어긋난다: 「앱만 22 … 제안(claude_suggestion) 4 로 짐작」 — 최근.md:138 · :77
    운영 DB 에서 claude_suggestion 라이브러리 4건의 run 은 전부 retreat 2(2027 회차)에 있다. retreat 1 에는 0건이다.
    2장의 「origin history · claude_suggestion 102 · 4」 는 라이브러리 전체의 수라서 「2026 회차」 머리 아래 두면 틀린다.
    그러니 앱만 22 안에 제안 4건은 들어 있을 수 없다.

[M] 3. 「앱도 한 회차뿐이라 아니다」 가 사실과 다르다 — 최근.md:140
    운영 retreats 는 두 개다. 2027 회차에도 run 이 서 있다: history 102(included 96) + 제안 4.
    결론(섞이지 않았다)은 retreat 1 로 걸러 셌다면 맞을 수 있다. 그러나 적힌 근거가 틀렸다.
    2장 셈이 retreat_id 로 걸렀다는 사실을 한 줄 밝혀야 한다.
    노션 쪽 「한 회차의 DB」 도 DB 이름만이 근거다. 줄의 날짜 범위를 봤는지는 적혀 있지 않다.

[M] 4. 「노션에서 들여온 것이 아니다 — 목업에서 추려 손으로 적은 시드다」 가 근거보다 앞선다
       — 최근.md:123 · :128 · docs/인계.md:3
    CLAUDE.md 6-9 에는 「노션에서 정리한 준비 업무와 시간표에서 뽑은 실행 항목이 그 재료입니다」 라고 적혀 있다.
    목업 retreat-board-v4.html 안에도 노션 항목을 재분류한 까닭을 적은 문장이 있다(467행 언저리).
    f83b2c5(08-27) 가 노션 보기를 손으로 옮긴 뒤에 730541a(08-29) 가 목업을 출처로 적었다.
    그러니 드러난 것은 「API 나 스크립트로 들여온 것이 아니다 · 노션을 사람이 재분류해 옮긴 시드다」 까지다.
    「노션에서 온 것이 아니다」 로 읽히는 문장이 새 채팅이 먼저 읽는 인계 머리에까지 올라갔다.

[M] 5. 「구분 다름 33 — 시드가 재분류했다」 를 단정으로 적었다 — 최근.md:136
    seed_library_data.py LIBRARY 에서 재분류 사유 칸이 찬 줄은 96줄 중 16줄이다.
    이 16줄에는 담당 부서를 바꾼 사유도 섞였을 수 있다.
    기록된 재분류 수가 33보다 적다. 나머지는 사유 없이 바꾼 것이거나, 3a 가 스스로 의심한 닮음 짝 30 에서 생긴 짝 오류일 수 있다.
    같은 까닭으로 날짜 다름(시작 11 · 마감 15)도 짝 오류분이 섞였을 수 있다.
    이 둘을 가르지 않은 채 원인으로 붙였다.

[M] 6. 짝짓기의 한계가 덜 적혔다 — 최근.md:89 · :95
    적힌 한계는 「닮음 짝 30 중 의심스러운 것 몇 건」 하나뿐이다. 빠진 것:
      - 제목 같음 50 도 틀릴 수 있다. 노션에 같은 제목이 여러 줄이면(회차 · 단계마다 한 줄 — 4장이 스스로 말한다) 1:1 에서 어느 줄을 골랐는지에 따라 날짜 · 구분 차이 수가 달라진다
      - 구분 없는 34줄을 뺐다. 그 안에 앱 업무와 짝이 되는 줄이 있으면 「앱만」 이 부풀려진다. 표지 24 를 뺀 10줄은 표지가 아니다
      - 가장 닮은 순의 탐욕 1:1 이라, 앞에서 잘못 먹은 짝이 뒤의 맞는 짝을 밀어낼 수 있다
      - 짝짓기 코드와 문턱이 저장소에도 data/ 에도 남지 않아 다시 셀 수 없다
        (data/ 에 있는 것은 결과 md 와 tsv 뿐. tsv 에는 노션 페이지 id · 편집 시각 칸이 없다)

[M] 7. 갈래 표(6장)가 ㄴ 쪽으로 기울었다 — 최근.md:154-156
    ㄱ 의 「잃는 것」 은 넷, ㄷ 은 하나다.
    ㄱ 은 「시드 102 를 원본에 없음 표시로 둔다」 로 가장 무거운 꼴만 적었다. 짝지은 80 은 살리고 노션만 쪽만 새로 세우는 ㄱ 도 가능하다.
    「업무가 두 배 넘게 는다」 는 223 전부를 업무로 친 셈이다. 4장이 업무가 아니라고 한 회의록 · 미팅 꼴 22 와 Sch 62 가 들어 있다.
    노션 페이지 본문(논의)이 앱에 없다는 대가는 ㄷ 에만 적혔다. 실은 ㄱ · ㄴ 에도 같다 — 「함께 정할 것」 셋째 줄이 본문 옮기기를 따로 뺐다.
    ㄴ 의 「잃는 것」 에 짝 오류(6번)가 외부 id 로 굳어 버리는 위험이 없다.

[M] 8. 지시문 대조 표의 번호와 보고 장 이름이 서로 엇갈린다 — 최근.md:186-188 · :100 · :110
    지시문 3a(네 갈래) · 3b(자세한 것은 저장소 밖) · 3c(차이 수)는 표의 줄 번호와 맞는다.
    그런데 보고 장은 3b 에 「차이 수」(지시문 3c), 3c 에 「회의록」 을 담았다.
    그래서 표의 3c 줄 비고가 「3b — …」 이고, 3a 줄 비고가 「3a · 3c」 가 된다.
    읽는 사람은 3b · 3c 를 두 번 옮겨 읽어야 한다. 장 이름을 지시문 번호에 맞추면 풀린다.
    또 지시문 머리의 「아무것도 고치지 않고 들여오지 않는다 · 보고와 덩어리에 값을 넣지 않는다」 가 표에 줄로 없다.
    「운영 자료 읽기만」 줄이 일부를 덮을 뿐이고, 값 없음을 지켰다는 판정 줄이 없다.
    검토-원칙 8-1 에 따라 빠진 항목이다.

[L] 9. 0b 「git ls-files 에서 노션 이름이 든 파일 0」 이 글자 그대로는 틀린다 — 최근.md:36
    파일 이름 기준으로는 0 이 맞다. 그러나 app/models.py · app/domain/meeting_import.py · scripts/import_meetings.py · scripts/check_dev_db.py 에는 노션 낱말이 들어 있다.
    뜻은 「노션 API 를 부르는 코드가 없다」 로 맞다. 무엇을 찾았는지를 그 말로 적어야 한다.

[L] 10. 「회의록 페이지 전부 마지막 편집이 내보내기(2026-09-02) 전」 을 날짜 단위로 견줬는지 적혀 있지 않다 — 최근.md:51 · :117
    내보낸 파일의 수정 시각은 2026-09-02 오후다. 같은 날 내보낸 뒤에 고친 페이지가 있으면 날짜로만 견준 셈은 0 으로 나온다.
    무엇과 무엇을 어떤 단위로 견줬는지 한 줄이 필요하다.

[L] 11. 「인계의 313 은 기준 기록이 없어 까닭을 모른다」 — 최근.md:142
    그 줄은 266a352(2026-09-09) 가 넣었다. 코드 이력으로 거기까지는 짚을 수 있었다. 그 커밋 본문에도 기준은 없어 보인다.
    결론이 같더라도 「어디까지 찾아봤다」 를 적으면 다음 판이 같은 길을 되밟지 않는다.

[L] 12. 작성 시각 23:50 KST 가 커밋 시각(22:18 +0900)보다 늦다 — 최근.md:7

[L] 13. 인계에 넣은 줄이 끝나지 않았고 가리키는 곳이 덮인다 — docs/인계.md:114-115
    「…그 판 보고(다음 판이 날짜 이름으로 얼린다)에」 로 문장이 끊겼다.
    CLAUDE.md 13장과 어긋난 속성(5장)도 덮이는 최근.md 에만 적혔다.
    얼릴 이름을 미리 정해 두거나 봐둘것에 한 줄 두는 편이 안전하다.

값 누출: 보고 · 인계 · 옛말-넘김 어디에도 제목 · 날짜 값 · 사람 이름이 보이지 않는다.
  들어 있는 것은 속성 이름 · 부서 이름 · 커밋 해시 · 건수뿐이다.
  노션 보기 이름 하나(4장 표)는 f83b2c5 커밋 메시지에 이미 공개돼 있다.
건수 대조: data/노션업무.real.tsv 257줄 · 구분 칸 Main 87 · Sub 74 · Sch 62 · 빈 칸 34 로 보고와 같다.
  운영 retreat 1 의 run 102(included 96) · main 42 · sub 52 · schedule 8 · 옛 tasks 175 · 회의록 53(출처 10 · 노션 origin 50 · 빈 origin 3)도 보고와 같다.
  네 갈래 수는 결과 md 의 갈래 머리 줄 간격으로 대략 맞는 것만 봤고, 줄마다 세지는 않았다.
11-3: 0단계 훑기 있음 · 사람칸 다섯 줄 있음 · 앞 판 얼림(d612539)과 넘김 한 줄 있음 · 재시작 건너뜀 까닭 있음.
  빠진 것은 1번(검사 순서 · git status)과 3단계 봐둘것 언급이 없는 것.

사람에게 묻는 것
  - 앱 업무를 「노션을 사람이 재분류해 옮긴 것」 으로 볼지, 보고처럼 「노션에서 온 것이 아니다」 로 둘지 (4번 — CLAUDE.md 6-9 와 어느 쪽이 맞나)
  - 짝짓기 코드를 data/ 밖 어디에라도 남겨 다시 셀 수 있게 할지 (6번)
  - 갈래 ㄱ 을 「짝 80 은 살리고 노션만 쪽만 세우는」 꼴로도 적어 다시 볼지 (7번)
  - 2027 회차에 복제된 run 102 도 다음 판의 맞추기 범위에 넣을지 (3번)

━━━ 검토자 여기까지 ━━━
```

## 10-2. 첫 검토를 받아 고친 것

| 번호 | 받았나 | 고친 곳 |
|---|---|---|
| [H] 1 | 받음 | 수용 기준 바) 를 마지막 pytest 뒤에 채우는 칸으로 · 9장에 git status 훑기 · 지시문 대조 표 「마무리」 줄을 「다르게 했다」 로 |
| [M] 2 · 3 | 받음 | 2장 머리에 회차가 둘이고 retreat 1 만 걸렀다는 줄 · 제안 4 는 다음 회차 · 4장 「여러 회차」 근거를 고침 · 노션 쪽은 DB 이름만이 근거라고 |
| [M] 4 | 받음 | 4장 머리와 인계 맨 위 — 「스크립트로 들여온 것이 아니라 사람이 노션을 재분류해 손으로 적은 시드 · 줄마다 잇는 열쇠 없음」 |
| [M] 5 | 받음 | 구분 다름 · 날짜 다름에 기록된 재분류 수와 짝 오류분을 가르지 않았다고 |
| [M] 6 | 받음 | 3a 에 짝짓기 한계 넷 · 짝짓기 코드를 `data/노션대조.real.py` 로 남김(저장소 밖 — 사람에게 묻는 것 둘째는 이것으로 답함) |
| [M] 7 | 받음 | 6장 — ㄱ 을 ㄱ1 · ㄱ2 로 · 「두 배」 문장을 뺌 · 본문 대가를 셋 공통으로 · ㄴ 에 짝 오류가 id 로 굳는 위험 |
| [M] 8 | 받음 | 3장 절 이름을 지시문 번호에 맞춤(3a 네 갈래 · 3b 저장소 밖 · 3c 차이) · 대조 표에 머리 두 줄 |
| [L] 9 · 10 · 11 · 12 | 받음 | 0b 찾은 것 · 1장 시각으로 견줌 · 4장 `266a352` 까지 찾음 · 작성 시각 |
| [L] 13 | 받음 | 인계 1) 문장을 끝내고 값 · 코드 자리, 다음 회차 run, 13장 속성 어긋남을 인계에 옮김(덮이지 않는 자리) |

사람에게 묻는 것 넷 중 첫째 · 셋째 · 넷째는 사람칸 「정할 것」 과 6장 · 인계 1) 로 넘겼다.

## 10-3. 두 번째 검토 (범위 2ee8f87..071bdff)

```
━━━ 검토자 여기부터 ━━━ 범위 2ee8f87..071bdff · 커밋 1개 · 파일 2개 (docs/review/최근.md · docs/인계.md)

본 것: 차이 전체, data/노션대조.real.py(짝짓기 코드 · 값은 옮기지 않음), seed_library_data.py 의 재분류 사유 칸.
운영 DB 는 이번에 다시 열지 않았다(첫 검토의 셈 그대로). 글검사를 이 창에서 돌렸고 끝 줄은 전부 통과였다.
pytest 는 조율자가 돌리는 중이라 결과를 보지 않았다.

첫 지적이 풀렸나
  [H]1 절반만 풀렸다 — 아래 1번
  [M]2 · 3 · 4 · 5 · 6 · 7 · 8 풀렸다. 다만 6번과 4번을 고치다가 새로 틀린 곳이 생겼다 — 아래 3 · 4번
  [L]9 새로 틀렸다 — 아래 2번
  [L]10 · 11 · 12 · 13 풀렸다. 다만 인계 갈래 이름이 사람칸과 어긋난다 — 아래 6번

[M] 1. 아직 하지 않은 일을 한 것처럼 적었다 — 최근.md 지시문 대조 표 「마무리는 11-3 대로」 줄 · 9장 · 10-3 · 수용 기준 사)
    대조 표 비고: 「마지막 pytest 를 보고를 다 채운 뒤 돌렸다 — 9장」.
    그런데 9장은 pytest 도, 고친 뒤 글검사도 「아래 줄」 이라고만 적었고 그 아래에 줄이 없다.
    10-3 은 「두 번째 검토의 답을 여기에 싣는다」 뿐이다.
    수용 기준 사) 는 여전히 O 인데, 두 번째 검토 원문은 아직 없다.
    바) 를 채울 칸으로 바꾼 것은 맞다. 같은 모양이 대조 표 한 줄과 사) 에 남았다.
    check_placeholder 는 사람 말로 적힌 이 꼴을 못 잡는다(돌려 보니 통과).
    채우기 전까지는 과거형과 O 를 「채울 칸」 으로 두어야 한다.

[L] 2. 0b 의 새 문장이 다시 틀린다 — 최근.md 0b
    「노션 낱말이 내용에 든 코드는 … 뿐이다」 라고 적었다.
    git ls-files 로 보면 seed.py · seed_data.py · seed_library_data.py 와 tests 셋에도 그 낱말이 들어 있다.
    빠진 시드 파일들은 4장이 원인으로 드는 바로 그 경로다.
    「셋 다 API 를 부르지 않는다」 는 네 파일을 나열한 뒤의 말이라 수도 어긋난다.
    API 를 부르는 코드가 없다는 결론은 그대로 맞다.

[M] 3. 회의록 「둘 다 · 제목 같음 10 · 제목 다름 0」 이 같은 절 끝 문장과 부딪힌다 — 최근.md 3a 「회의록」 표와 그 아래 문단
    표는 제목이 같다고 판정했다.
    바로 아래 문단은 「노션 페이지 제목과 파일 이름은 … 글자 비교 결과를 따로 세어 두지는 않았다」 이다.
    잰 것은 「파일 이름 = 앱 출처 10/10」 뿐이다. 노션 제목과 앱의 글자는 견준 기록이 없다.
    네 갈래 꼴에 맞추려다 근거보다 앞선 칸이 생겼다.
    「파일 이름으로 짝지음 10 · 제목 비교는 세지 않음」 으로 적어야 한다.

[L] 4. 짝짓기 한계에 코드가 실제로 하는 일이 하나 빠졌다 — 최근.md 3a 「짝짓기의 한계」 · data/노션대조.real.py 30-54행
    같은 제목 단계는 노션 줄 순서대로 아직 안 쓴 앱 줄 하나를 가져간다(「첫 줄과 이었다」 는 맞다).
    같은 제목인 뒷 노션 줄은 닮음 단계로 넘어간다.
    닮음 단계에서 그 줄이 다른 앱 줄과 이어질 수 있다. 그러면 「제목 다름 30」 이 부풀려진다. 적힌 넷에는 이것이 없다.

[L] 5. 「재분류 사유 16 중에는 부서를 옮긴 사유도 섞였다」 로 단정했다 — 최근.md 4장 「구분 다름 33」
    첫 검토는 「섞였을 수 있다」 였다.
    낱말로 대강 세 보니 부서 쪽 낱말이 든 사유와 분류 쪽 낱말이 든 사유가 겹쳐 있다. 섞였다는 방향은 맞아 보인다.
    다만 보고에는 그것을 센 기록이 없다. 「16 이 전부 분류 재분류는 아니다」 쯤이 근거만큼이다.

[L] 6. 인계와 사람칸의 갈래 이름이 어긋난다 — docs/인계.md 맨 위 갱신 줄 · 1) 「미정」 줄 / 최근.md 사람칸 「정할 것」
    사람칸과 6장은 ㄱ1 · ㄱ2 · ㄴ · ㄷ 인데 인계 두 곳은 여전히 ㄱ · ㄴ · ㄷ 이다.
    새 채팅은 인계를 먼저 읽는다.

[L] 7. 대조 표 「값을 넣지 않는다 — 검토자가 따로 봤다(10장)」 가 받칠 수 있는 범위를 넘는다 — 최근.md 지시문 대조 표 머리 둘째 줄
    검토자가 본 것은 2ee8f87 의 보고와 인계다. 고친 뒤의 판과 덩어리는 보지 않았다.
    덩어리는 검토자가 볼 수 없다(검토-원칙 9).
    이번 판에 대해 말하면: 이 차이에도 제목 · 날짜 값 · 사람 이름은 보이지 않는다.
    10-1 에 실린 첫 검토 원문에도 값은 없다. 회차 이름의 연도 하나가 있을 뿐이다.

[L] 8. 1장 「시각으로 견줬다」 의 한쪽 끝은 내보낸 시각이 아니라 파일이 디스크에 쓰인 시각이다 — 최근.md 1장 회의록 줄
    파일을 내보낸 뒤 옮겨 담아 수정 시각이 늦어졌다면, 그 사이 노션에서 고친 것은 0 으로 셈해진다.
    그 한계를 한 줄 적어 둘 만하다.

값 누출: 이 차이에서는 찾지 못했다. data/노션대조.real.py 도 저장소 밖(gitignore 이름)이다.
근거보다 앞선 말: 새로 생긴 것은 3번(회의록 제목 같음)과 1번(pytest 를 돌렸다는 과거형)이다.
인계 문장: 1) 의 끊긴 문장은 끝났다. 남은 것은 6번뿐이다.

사람에게 묻는 것
  없음 — 첫 검토에서 넘긴 넷은 사람칸 「정할 것」 과 6장에 이미 올라가 있다.

━━━ 검토자 여기까지 ━━━
```

## 10-4. 두 번째 검토 뒤 고침 (다시 보이지 않았다 — 11-3)

| 번호 | 파일 | 고친 것 |
|---|---|---|
| [M] 1 | `docs/review/최근.md` | 대조 표 「마무리」 줄을 「돌린다 — 결과는 9장 끝 줄」 로 · 9장 글검사 줄을 실제 돌린 수로 · pytest 줄은 마지막 pytest 결과로 채움 · 사) 는 이 10-3 원문을 실은 뒤의 O |
| [L] 2 | `docs/review/최근.md` | 0b — 시드 셋 · 시험을 더하고 「어느 것도」 로 |
| [M] 3 | `docs/review/최근.md` | 3a 회의록 표 — 「파일 이름으로 짝지음 10 · 제목 같음/다름은 세지 않음」 · 부딪히던 끝 문장 뺌 |
| [L] 4 | `docs/review/최근.md` | 3a 한계에 「같은 제목인 노션 뒷줄이 닮음 단계로 넘어간다」 |
| [L] 5 | `docs/review/최근.md` | 4장 — 「16 이 전부 분류 재분류는 아니다 · 세지 않았다」 |
| [L] 6 | `docs/인계.md` | 맨 위 갱신 줄 · 1) 미정 줄의 갈래 이름을 ㄱ1 · ㄱ2 · ㄴ · ㄷ 로 |
| [L] 7 | `docs/review/최근.md` | 대조 표 머리 둘째 줄 — 검토자가 본 범위(첫 커밋 · 고친 차이)와 덩어리는 못 본다는 것 |
| [L] 8 | `docs/review/최근.md` | 1장 — 파일 수정 시각이 한쪽 끝이라는 한계 |
