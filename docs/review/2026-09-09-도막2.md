작성 시각: 2026-09-09 23:30 (KST)
커밋: fea9906 (그 위에 이 줄과 8장을 채운 커밋 하나)
작업: 도막 2 — 계정 정리 · 엑셀 칸 비우기 · 링크 정의 하나로

---

# 0. 먼저 셌습니다 (아무것도 안 고치고)

운영 `data/app.db` 를 읽기 전용(`mode=ro`)으로 열어 잰 값입니다.
**지우기 전의 셈**입니다 — 지운 뒤의 셈은 1-g 에.

## 0-(a) `users.id = 1` 이 관리자이고 활성인가 — **참**

| 물음 | 값 |
|---|---|
| `role` | `admin` |
| `is_active` | 1 |
| `first_seen_at` | 있음 (이 계정으로 화면을 연 적이 있음) |

참이므로 이 판을 계속합니다. (거짓이었으면 여기서 멈추고 0장만 냈습니다.)

**세다가 본 것 하나** — 도막 1 뒤에 `invite_tokens` 가 24 → 25 로 늘어
있었습니다. 최근 활동 기록을 보니 **이 판이 시작되기 전에 id 1 의 링크가
한 번 발급되고(서버에서) 그 링크가 쓰였습니다** — 사람이 그 계정으로
들어온 자국입니다. 이 판이 만든 것이 아니라 그대로 두고 셌습니다.

## 0-(b) 남길 계정을 뺀 나머지

| 무엇 | 값 |
|---|---:|
| `users` 전체 | 21 |
| **지울 계정** (id 1 을 뺀 것) | **20** |

도막 1 의 0-(e) 표를 **지울 계정을 가리키는 행**으로 다시 셌습니다. 남길
계정(id 1)이 가리키는 행은 뺐습니다.

| 표.칸 | `ondelete` | 값 있는 행 | id 1 | **지울 계정** |
|---|---|---:|---:|---:|
| `notifications.user_id` | CASCADE | 1,839 | 345 | **1,494** |
| `tasks.assignee_id` | SET NULL | 89 | 55 | **34** |
| `checklist_items.checked_by_id` | SET NULL | 19 | 0 | **19** |
| `invite_tokens.user_id` | CASCADE | 25 | 18 | **7** |
| `meeting_items.assignee_id` | SET NULL | 5 | 1 | **4** |
| `discussion_entries.author_id` | SET NULL | 12 | 10 | **2** |
| `user_retreat_states.user_id` | CASCADE | 3 | 1 | **2** |
| `task_attachments.uploaded_by_id` | SET NULL | 4 | 3 | **1** |
| `file_assets.created_by_id` · `file_versions.uploaded_by_id` · `invite_tokens.created_by_id` · `meetings.created_by_id` · `review_requests.requester_id` | SET NULL | 2 · 3 · 2 · 3 · 1 | 전부 id 1 | **0** |
| 나머지 아홉 (도막 1 표와 같음) | — | 0 | 0 | **0** |
| **합계 (FK 22개)** | | 2,007 | 444 | **1,563** |

**함께 사라질 행 1,503 (CASCADE) · 이름을 잃을 행 60 (SET NULL).**

| FK 가 없는 칸 | 값 있는 행 | id 1 | **지울 계정** |
|---|---:|---:|---:|
| `activity_logs.actor_id` | 42 | 36 | **6** |

## 0-(c) `actor_id` 가 비어도 기록을 읽을 수 있는가 — **있습니다**

| 물음 | 값 |
|---|---:|
| `activity_logs` 전체 | 51 |
| `actor_id` 는 있는데 `actor_name` 이 빈 행 | **0** |
| `actor_name` 이 빈 행 | 7 — 전부 `actor_id` 도 없는 행(앱이 뜰 때의 전환 2 · 서버에서 발급 등) |

`actor_id` 를 비워도 「누가 했는지」 는 `actor_name` 문자열이 그대로
집니다. 비운 뒤에 새로 못 읽게 되는 행은 없습니다.

## 0-(d) 「링크 안 받은 사람 N명」 은 어디서 어떻게 세는가

| 자리 | 파일:줄 |
|---|---|
| 수를 세는 곳 | `app/routers/admin_users.py:129` (`보낼사람`) → `:140` (`yet_count`) |
| 행마다 「살아 있는가」 | `app/routers/admin_users.py:96` → `invites.live_token` |
| `live_token` | `app/domain/auth.py:140` — **`problem_with` 가 None 인 토큰**을 찾습니다 |
| 「들어온 적 있는가」 | `invites.entered_ever` (`used_at` 이 있는 토큰) |
| 화면 | `app/templates/admin_users.html:81` |

**정의: 활성 계정 중 「살아 있는 링크가 없고 · 들어온 적도 없는」 사람.**
「살아 있다」 는 `live_token` → `problem_with` 이므로 **화면은 이미
`problem_with` 를 쓰고 있었습니다.** 제가 도막 1 에서 잘못 센 것은
화면이 아니라 제 손 셈이었습니다.

나란히 — 활성 계정 19명:

| 정의 | 수 |
|---|---:|
| **화면이 찍는 「링크 안 받은 사람」** (`live_token` 없음 · 들어온 적 없음) | **17** |
| 화면의 「보냈고 아직 안 들어옴」 | 1 |
| 화면의 「들어온 사람」 | 1 |
| [견줌] 토큰 행이 아예 없는 활성 계정 | 16 |
| [견줌] `used_at` 이 빈 토큰을 가진 활성 계정 (도막 1 의 제 실수) | 3 |
| [견줌] `problem_with` 가 None 인 토큰을 가진 활성 계정 | 1 |

세 정의가 각각 다른 수를 냅니다(16 · 3 · 1). 화면은 그중 맞는 것을 쓰고
있었습니다.

**그런데 `problem_with` 말고 「살아 있다」 를 스스로 정하던 자리가
둘 있었습니다** — 3장에서 없앴습니다.

| 자리 | 무엇을 스스로 봤나 |
|---|---|
| `app/domain/auth.py` 의 `revoke_all` | `used_at`·`revoked_at` 둘만 보고 **기한을 안 봤습니다** — 같은 파일 안에 「살아 있다」 가 두 뜻 |
| `scripts/관리자링크.py` 의 `마지막입장` | 토큰 표를 직접 뒤져 `used_at` 을 골랐습니다 (「쓴 적」 이라 뜻은 달라도, 보는 곳이 둘) |

## 0-(e) 계좌가 나가는 자리

| 자리 | 파일:줄 (고치기 전) |
|---|---|
| 엑셀 「지출 상세내역」 시트 | `app/domain/budget_xlsx.py:71` |
| 엑셀 「환급 대상자」 시트 | `app/domain/budget_xlsx.py:133` |
| 화면의 계좌 판정 | `app/templating.py:185` 의 `is_admin` (= `perm.can_manage_retreat`) — 지출 목록 `expenses.html:158` 이 부름 |
| 엑셀 칩 | `expenses.html:18` · `budget.html:25` — 같은 `is_admin` |
| 등록 폼의 직전 계좌 미리 채움 | `app/routers/expenses.py:166` — `perm.can_manage_retreat` (검토자가 잡음 — 처음 표에 빠져 있었습니다) |

**판정이 「총무팀인가」 와 한 함수였습니다.** 회차를 만들 수 있는가와
계좌를 볼 수 있는가는 다른 물음이라, 앞이 바뀌면 뒤가 따라 움직입니다.

---

# 1. `scripts/계정정리.py` — 사람이 고른 계정 하나만 남기고 지운다

- 인자로 남길 id · `--실행` 이 없으면 **미리보기만** · 남길 계정이
  관리자·활성이 아니면 거절
- 지우기 직전에 `scripts/backup.py` 의 `snapshot()`(`VACUUM INTO`)으로
  사본을 먼저 뜹니다 — **안 떠지면 지우지 않습니다**
- FK 가 없는 `activity_logs.actor_id` 는 스크립트가 직접 비웁니다.
  `actor_name` 은 그대로
- **FK 목록을 코드에 박지 않습니다** — `PRAGMA foreign_key_list` 로 스키마에서
  읽습니다(10장). FK 없는 칸만 `FK없는칸` 에 이유와 함께
- 지우기 전에 `PRAGMA foreign_keys` 가 켜져 있는지 봅니다 — 꺼져 있으면
  CASCADE 가 안 돌아 가리킬 곳 없는 행이 조용히 남습니다

## 1-b. 미리보기 (실제로 돌린 출력 — 이름 없음)

```
지울 계정 20개 (남길 id 1 은 그대로 둡니다)

  표.칸                                    어떻게 되나                        행
  notifications.user_id                  계정과 함께 사라집니다               1494
  tasks.assignee_id                      남되 누가 썼는지 모릅니다               34
  checklist_items.checked_by_id          남되 누가 썼는지 모릅니다               19
  invite_tokens.user_id                  계정과 함께 사라집니다                  7
  activity_logs.actor_id                 스크립트가 직접 비웁니다                 6
  meeting_items.assignee_id              남되 누가 썼는지 모릅니다                4
  discussion_entries.author_id           남되 누가 썼는지 모릅니다                2
  user_retreat_states.user_id            계정과 함께 사라집니다                  2
  task_attachments.uploaded_by_id        남되 누가 썼는지 모릅니다                1

  함께 사라지는 행 1503 · 이름을 잃는 행 60 · 직접 비우는 행 6

  **아직 아무것도 지우지 않았습니다.** 실제로 지우려면:
    python scripts/계정정리.py 1 --실행
```

exit 0 · 돌린 뒤 `users` **21 그대로** (수용 기준 2). 0-(b) 의 손 셈과
한 줄씩 같습니다.

## 1-g. 실행

```
계정 20개를 지웁니다. 남길 id 1.
  사본을 떴습니다: app-20260909-231055.db  (1,011,712 바이트)
  activity_logs.actor_id 을 6행 비웠습니다 (이름은 그대로 둡니다)
  지웠습니다. 남은 users 행: 1
```

**지운 뒤 다시 센 것:**

| 무엇 | 값 |
|---|---:|
| `users` 행 | **1** — `id=1 · admin · 활성` |
| FK 22개 중 **다른 id 를 가리키는 행** | **0** (표별로 전부 0) |
| id 1 을 가리키는 행 | **443** (지우기 전 444 — `invite_tokens.created_by_id` 가 2 → 1: id 1 이 **지울 계정에게** 발급해 준 토큰 행이 `user_id` 의 CASCADE 로 함께 사라졌습니다. 「가리키는 쪽」 으로는 0-(b) 가 맞지만 행 자체가 다른 칸으로 사라진 것입니다 — 검토자가 잡았습니다) |
| `activity_logs.actor_id` 가 **가리킬 곳 없는 행** | **0** (수용 기준 4) |
| `actor_id` 는 비고 `actor_name` 은 남은 행 | 8 (비운 6 + 원래 그렇던 2) |
| `activity_logs.target_id` 가 `target_type='user'` 이고 **없는 id 를 가리키는 행** | **8** — FK 없이 `users.id` 를 담는 **또 하나의 칸**입니다(검토자가 찾음). 비우면 「무엇에 대한 기록인가」 를 잃고 두면 없는 id 를 가리킵니다 — `summary` 에 그때의 이름이 있어 읽는 데는 지장이 없어 **두었고** `docs/봐둘것.md` 에 열어 두었습니다 |
| `invite_tokens` | 18행, 전부 `user_id=1` — 지울 계정의 7행은 CASCADE 로 함께 사라짐 |
| `notifications` | 345 (1,494 사라짐) |
| `계정_삭제` 활동 기록 | 1행 — `actor_id` 없음 · `actor_name` 없음 · 「계정 20개를 서버에서 지웠습니다 (남긴 계정 1개)」 |

**사본이 실행 전에 만들어졌는가** (수용 기준 3):

| 무엇 | 값 |
|---|---|
| 사본 | `data/backups/app-20260909-231055.db` |
| 사본의 수정 시각 | 2026-09-09 23:10:55 (KST) |
| `계정_삭제` 로그 시각 | 2026-09-09 14:10:55 UTC = 23:10:55 KST — 같은 초 |
| **사본 안의 `users` 행** | **21** — 지우기 전 상태입니다 |

시각이 같은 초라 순서를 시각으로는 못 가르므로, **사본을 열어 세었습니다** —
21행이면 지우기 전입니다. 순서를 지키는 것은 코드입니다(`지운다()` 가
`snapshot` 을 먼저 부르고 그 뒤에 `delete`) — 그리고 「사본이 안 떠지면
안 지운다」 는 `test24_a04` 가 잽니다. `test24_a03` 의 사본 파일은 순서의
근거가 **아닙니다**(시험이 미리 복사해 둔 파일이라 순서와 무관하게 참) —
검토자가 짚었습니다.

**남길 계정의 살아 있는 링크** (1-f) — 실행 시점에 id 1 은 **살아 있는
링크가 없었습니다**: 마지막 링크가 22:58 KST 에 **쓰였습니다**(0-a 의
그 자국). 남아야 할 것이 없었으므로 「남았다」 를 실측으로 말할 수는
없고, 18행이 그대로인 것과 `test24_a03`(살아 있는 링크를 심어 두고 지운
뒤 `live_token` 이 남는지)으로 잽니다.

---

# 2. 엑셀 — 계좌 칸만 비우고 부서 리더도 받게

| 자리 | 전 | 후 |
|---|---|---|
| `/export/expenses.xlsx` (`app/routers/export.py`) | `require_admin` | **`require_editor`** — 열람 전용만 403 |
| 계좌 판정 | `is_admin` = `can_manage_retreat` | **`permissions.can_see_account`** (`app/domain/permissions.py:25`) — **하나** |
| 엑셀 두 시트 (`budget_xlsx.py:69` · `:91` · `:143` · `:157` · `:163`) | 계좌 칸 늘 있음 | `계좌를_보인다` 가 거짓이면 **칸 자체를 안 만듭니다** (머리줄에도 없음) |
| 화면의 계좌 (`expenses.html:158`) | `is_admin(user)` | `계좌를_본다(user)` → 같은 함수 (`app/templating.py:186`) |
| 엑셀 칩 (`expenses.html:18` · `budget.html:25`) | `is_admin(user)` | `엑셀을_받는다(user)` — **받을 수 있는 사람**(열람 전용 아님) (`templating.py:189`) |
| 등록 폼의 직전 계좌 (`expenses.py:166`) | `can_manage_retreat` | `can_see_account` — 같은 함수 |

**칩은 계좌 판정이 아니라 「받을 수 있는가」 로 그립니다** — 둘은 다른
물음입니다. 도막 1 에서 칩을 계좌 판정으로 그려서, 표를 받을 수 있는
부서 리더에게 단추가 없었습니다.

**`budget_xlsx.write` 의 기본값은 「안 보임」 입니다.** 부르는 쪽이
빠뜨렸을 때 계좌가 새는 쪽으로 기울면, 빠뜨린 것을 아무도 눈치채지
못합니다. 라우터가 `perm.can_see_account(user.role)` 를 넘깁니다
(`export.py:44`).

`_style_header(ws3, 7)` 처럼 열 수가 숫자로 박혀 있던 자리를 `len(환급열)`
로 바꿨습니다 — 칸을 빼면 열이 하나 줄어, 박아 두면 머리줄만 남습니다.
환급 시트의 열 폭(`_widths(ws3, …)`)도 같은 모양이라 계좌 칸이 없을 때는
그 폭 하나를 빼게 했습니다(검토자가 찾음 — 계좌 폭이 지급여부에 가고
있었습니다).

## 2-d. 시험

| 시험 | 무엇을 잡나 | 결과 |
|---|---|---|
| `test23_g04_엑셀은_편집자가_받되_계좌_칸은_admin_만` | ① viewer 403 ② dept_lead 200 이고 두 시트 머리줄에 계좌 칸이 **없음** — 표는 그대로(「지출자」 는 있음) ③ admin 파일에는 칸이 있고 **값이 실제로 들어 있음** (xlsx 는 zip 이라 칸을 읽어 봄) | 통과 |
| `test23_g06_누를_수_없는_엑셀_단추를_그리지_않는다` | ① viewer 에게 칩 없음 ② **dept_lead 에게 칩 있음** ② admin 에게 칩 있음 | 통과 |

---

# 3. 링크 정의 하나로

| 자리 | 전 | 후 |
|---|---|---|
| `auth.revoke_all` (`auth.py:85`) | `used_at`·`revoked_at` 만 보고 기한을 안 봄 | **`problem_with` 가 None 인 것만** 취소 |
| `관리자링크.py` 의 「링크 쓴 때」 | 토큰 표를 직접 뒤짐 | `auth.last_used_at` (`auth.py:152`) 을 부름 — 보는 곳도 `domain/auth` 하나 |
| 설정 › 사용자의 「링크 안 받은 사람」 | 이미 `live_token` → `problem_with` | 그대로 (0-(d)) |

## 3-b. 시험

| 시험 | 무엇을 잡나 | 결과 |
|---|---|---|
| `test24_b01_취소됐거나_만료된_링크만_가진_사람은_안_받은_사람이다` | ① 취소만·만료만 가진 사람은 세어지고 ② 살아 있는 링크를 가진 사람은 안 세어지고 ③ 화면의 수가 그 정의로 센 수와 같고 그 둘이 실제로 들어 있음(≥2) | 통과 |
| `test24_b02_토큰_상태를_스스로_판정하는_코드가_없다` | `auth.py`·`models.py` 밖(**템플릿 포함**)에서 `used_at`·`revoked_at`·`expires_at` 을 **조건으로** 쓰는 줄이 0 (수용 기준 7) | 통과 |
| `test24_b02b_판정을_심으면_잡힌다` | ③ — `is not None` · 참거짓 · `not` · `bool()` · `.is_not(` · `<` 여덟 모양을 임시 파일에 하나씩 심어 전부 걸리는지, 값만 찍는 `.date()` 는 안 걸리는지. **처음 정규식은 이 중 다섯을 놓쳤습니다** — 검토자가 돌려 보고 잡았습니다 | 통과 |
| `test24_b03_revoke_all_도_같은_정의를_쓴다` | 만료된 것과 살아 있는 것을 두고 재발급 → 살아 있는 것만 취소(1) · 만료된 것은 안 건드림 | 통과 |

**수용 기준 7 의 「0줄」 은 이렇게 잽니다** — 값을 찍기만 하는
`token.expires_at.date()` (`admin_users.py:115` · `:356`)는 판정이 아니라
남깁니다. 그 `token` 은 이미 `live_token` 이 고른 것입니다.

---

# 4. 문서

- `docs/인계.md` — 2번(계정 정리)을 「끝난 것」 으로 옮기고 되돌리는 법과
  사본 파일 위치를 적었습니다. 갱신 줄 도막 2. **다음은 3번(다대다)** 을
  머리에. 「사람이 챙길 재료」 에서 「남길 계정 id」 를 뺐습니다
- `CLAUDE.md` 4-12 — 0장 예외의 도구가 `scripts/계정정리.py` 라는 한 문단
  (미리보기가 기본 · `--실행` · 사본 먼저 · FK 없는 칸은 직접)
- `docs/봐둘것.md` — K(엑셀 범위)를 **처리함** 으로
- `docs/옛말.md` — 「엑셀은 총무팀만 받는다」 를 낡은 주장으로 표기 넷과 함께
  (검토자 4 — 안 적어서 `check_stale` 이 초록이었습니다)
- `CLAUDE.md` 14장 표에 `계정정리.py` 한 줄, 4-12 에 **`merge_users.py` 가
  먼저**라는 순서 한 줄 (검토자 11)
- 지출·예산 화면의 낡은 주석 둘(「총무팀만 받습니다」)을 고쳤습니다
- README 의 로그인 절은 손대지 않았습니다 (인계 13번)

---

# 5. 계정정리.py 의 시험

| 시험 | 무엇을 잡나 | 결과 |
|---|---|---|
| `test24_a01_실행_없이는_아무것도_안_지운다` | ① `--실행` 없이는 `users` 가 그대로 · ③ 지울 것이 실제로 있음(≥4) | 통과 |
| `test24_a02_관리자가_아니거나_비활성이면_거절한다` | 부서 리더·비활성·없는 id 전부 exit 1 이고 `users` 그대로 | 통과 |
| `test24_a03_실행하면_하나만_남고_참조가_정리된다` | ② 하나만 남음 · CASCADE(알림·토큰) 사라짐 · 남길 계정의 살아 있는 링크 남음 · `actor_id` 고아 0 · `actor_name` 남음 · 남길 사람의 `actor_id` 그대로 · `계정_삭제` 한 줄(이름 없음) · 사본 파일이 생김 · ③ FK 가 켜져 있음을 먼저 잼 | 통과 |
| `test24_a04_사본이_안_떠지면_지우지_않는다` | `snapshot` 이 던지면 `users` 그대로 | 통과 |
| `test24_a05_여는_파일과_뜨는_파일이_다르면_지우지_않는다` | `DCB_DATABASE_URL` 로 세션의 파일과 사본을 뜰 파일이 갈리면 거절 · 사본도 안 뜸 (검토자 10) | 통과 |

---

# 6. 수용 기준

| # | 기준 | 결과 | 근거 |
|---|---|---|---|
| 1 | 0-a 가 참 | O | 0-(a) — admin · 활성 |
| 2 | `--실행` 없이는 안 지운다 (실제로 돌려 수가 그대로) | O | 1-b — 21 그대로 · `test24_a01` |
| 3 | 실행 뒤 `users` = 1 이고 id 1 · 사본이 실행 전에 만들어짐 | O | 1-g — 사본 안 `users` 21 · 파일명·시각 |
| 4 | 가리킬 곳 없는 `actor_id` 0행 | O | 1-g |
| 5 | 계좌 판정 함수가 하나이고 화면·엑셀·칩이 부른다 | O | `permissions.py:25` ← `templating.py:186` · `export.py:44` · 칩은 「받을 수 있는가」(`templating.py:189`) |
| 6 | 2-d · 3-b 시험이 있고 막히는 쪽·통과하는 쪽 둘 다 | O | 2-d · 3-b 표 |
| 7 | `problem_with` 말고 토큰 상태를 스스로 판정하는 코드 0줄 | O | `test24_b02` · 3장 |
| 8 | 덩어리와 최근.md 에 실명·전화·계좌·토큰 없음 | O | 글검사 · 이 파일에는 수와 파일명만 |
| 9 | pytest 옵션 없이 전부 통과 · 글검사 4/4 | O | 7장 |
| 10 | 검토자 원문이 덩어리와 최근.md 에 그대로 | O | 9장 |

---

# 7. 돌린 것

## pytest (옵션 없이)

```
1283 passed in 551.18s (0:09:11)
```

**검토자 원문을 실은 뒤, 검토자가 시킨 것을 다 고친 뒤에 돌린 값입니다.**
그 전 판은 `1 failed, 1280 passed` 였고 빨간 하나가 `test6_k05`(줄 번호
목록) — 검토자 1번입니다.

## 글검사 (`scripts/글검사.py`)

```
━━━ 모아서 ━━━
  통과  check_names (exit 0)
  통과  check_stale (exit 0)
  통과  check_named (exit 0)
  통과  check_counts (exit 0)

글 검사가 전부 통과했습니다.
```

**한 번 빨갰습니다** — `check_stale` 이 얼린 도막 1 보고 안의 검토자 문장
「파일은 통째로 admin」 을 이번 판이 적은 낡은 주장으로 잡았습니다. 얼린
판은 못 고치므로 `docs/옛말-넘김.txt` 에 이유와 함께 한 줄 — 도막 1 이
11-3 에 「얼릴 때마다 넘김 한 줄이 필요할 수 있다」 고 적어 둔 바로 그
자리입니다.

```
(위 줄은 넘김을 더한 뒤의 값)
```

## `docs/checks/drawer.js` — 세 화면 × 두 계정

개발 서버(8001 · 가명 자료 — 운영 DB 와 별개라 계정 삭제의 영향이
없습니다)에서, 해시 자리를 매번 바꾸고 `{cache:'reload'}` 로 받아
길이(59,095자)를 여섯 번 다 견줬습니다.

| 화면 | 관리자 | 부서 리더 |
|---|---|---|
| 보드 | 79/79 · 건너뜀 2 | 62/62 · 건너뜀 15 |
| 목록 | 92/92 · 건너뜀 4 | 78/78 · 건너뜀 16 |
| 달력 | 82/82 · 건너뜀 3 | 65/65 · 건너뜀 16 |

여섯 판 모두 **실패 0 · 못 쟀음 0 · 완주 참** — 도막 1 과 같은 수입니다.
이번 판이 고친 화면(지출·예산·설정)에는 상세 패널이 없어 이 점검이
닿지 않고, 그 자리는 `test23_g04`·`g06` 이 잽니다. 자가시험은 안 돌렸습니다
(점검 스크립트를 안 고쳤습니다).

## 서버 (11-3 2단계)

`app/` · `templates/` 가 바뀌었으므로 건너뛰지 않았습니다.

| 무엇 | 값 |
|---|---|
| 재시작 전 PID | 15204 |
| 재시작 후 PID | **26680** (성공 — PID 가 바뀌었습니다) |
| `GET /` | 303 (로그인으로) |
| `GET /login` | 200 |
| 화면이 물어 온 CSS | `retreat.bdf527e8.css` (내용 해시 — 이번 판은 CSS 를 안 고쳐 같은 값) |
| `GET /export/expenses.xlsx` (로그인 없이) | 303 — 로그인 문이 먼저. 편집자·열람 전용의 갈림은 `test23_g04` |

## 바뀐 파일 (`git diff --name-only 5979bcf..HEAD`)

```
CLAUDE.md
app/domain/auth.py
app/domain/budget_xlsx.py
app/domain/permissions.py
app/routers/expenses.py
app/routers/export.py
app/templates/budget.html
app/templates/expenses.html
app/templating.py
docs/review/최근.md
docs/봐둘것.md
docs/옛말-넘김.txt
docs/옛말.md
docs/인계.md
scripts/계정정리.py
scripts/관리자링크.py
tests/test_stage23.py
tests/test_stage24.py
tests/test_stage6.py
```

---

# 8. 커밋

`5979bcf..fea9906` — 여섯 판. 그 위에 이 8장과 머리의 해시를 채우고 이
보고를 얼린 커밋 하나가 더 붙습니다.

| 해시 | 무엇 때문에 |
|---|---|
| `3d98027` | 「링크가 살아 있다」 는 problem_with 하나가 정한다 |
| `d3fedc5` | 엑셀은 계좌 칸만 비우고 편집자도 받는다 — 판정은 한 함수 |
| `89fdf17` | 사람이 고른 계정 하나만 남기고 나머지를 지우는 도구 (0장의 예외) |
| `5ca317e` | 되돌릴 수 없는 일에는 시험이 셋 다 있어야 한다 |
| `f968f43` | 계정 정리를 끝난 것으로 옮기고, 도구 둘의 순서와 열어 둔 것을 적는다 |
| `fea9906` | 검토용 보고를 도막 2 로 덮어쓴다 |

**마지막 커밋에서 바꾼 것은 이 파일 · 그 얼린 사본 · `docs/옛말-넘김.txt` 의
한 줄뿐입니다** — 얼린 사본이 검토자 원문 안의 낡은 표기로 `check_stale` 에
걸려(도막 1 과 같은 자리) 이유와 함께 넘겼고, 바꾼 뒤 글검사와 문서를 읽는
시험을 다시 돌렸습니다(11-3).

---

# 9. 검토자 원문

익명화를 지난 그대로입니다. 「채팅에 있다」 로 가리키지 않습니다(11-3).

```
━━━ 검토자 여기부터 ━━━ 범위 5979bcf..작업트리(커밋 전) · 커밋 0개 · 파일 15개 스테이지 + 스테이지 밖 1개(tests/test_stage6.py)

읽은 것: CLAUDE.md(11-3 되짚기·10장·0장·4-12·7장) · docs/검토-원칙.md · docs/review/최근.md ·
git diff 5979bcf 전체 · 운영 data/app.db (mode=ro) · 사본 app-20260909-231055.db (mode=ro)
돌린 것: scripts/글검사.py (4/4 exit 0) · pytest 전체(옵션 없이, 23:21 시작) · tests/test_stage23+24 단독

먼저 말할 것 — 검토하는 동안 작업 트리가 움직였습니다
  · 처음 git status: docs/review/최근.md 가 스테이지 밖(" M"). 지금은 스테이지 안.
  · tests/test_stage6.py 가 23:24:55 에 바뀌었고 지금도 스테이지 밖(" M") — 지시문의
    「전부 스테이지됨」·「바뀐 파일」 목록 둘 다에 없습니다.
  아래 1번이 그 결과입니다.

━━ 막아야 함 ━━

1. 커밋에 tests/test_stage6.py 가 빠지면 pytest 가 빨간 채로 나갑니다
   · 근거: app/domain/permissions.py 에 can_see_account 14줄이 들어가
     `user_department_id == target_department_id` 가 47줄 → 61줄로 밀렸습니다.
     tests/test_stage6.py:158 의 ID_비교_허용 은 줄 번호를 박아 둔 목록이라
     47 → 61 로 고쳐야 k05 가 통과합니다. 그 한 줄 고침이 지금 스테이지 밖입니다.
   · 실측: 23:21 에 시작한 전체 pytest → `1 failed, 1280 passed`
     (FAILED test6_k05: 목록에 없는 부서 id 비교 ('app/domain/permissions.py', 61, …)).
     지금 트리(스테이지 밖 고침 포함)로 k05 단독 → 통과. stage23+24 → 21 passed.
   · 해야 할 것: tests/test_stage6.py 를 커밋에 넣고, 덩어리·최근.md 7장의 「바뀐 파일」
     과 pytest 줄을 그 상태에서 다시 뜬 것으로 채웁니다. 지금 최근.md §6 의
     9번 「O」 는 아직 근거가 없습니다(7장이 자리표시자).

━━ 고쳐야 함 ━━

2. 수용기준 5 「계좌 판정 함수가 하나」 — 적힌 대로는 아직 아닙니다
   · app/routers/expenses.py:166  `남이낸것 = perm.can_manage_retreat(user.role)`
     → 등록 폼에 직전 지출자의 payer_account 를 미리 채울지를 여기서 따로 판정합니다
     (168줄이 그 값으로 last.payer_account 를 넣습니다). 계좌가 나가는 입구인데
     can_see_account 가 아니라 can_manage_retreat 를 부릅니다.
   · 최근.md 0-(e) 「계좌가 나가는 자리」 표에 이 자리가 없습니다.
   · 오늘은 둘 다 role == ADMIN 이라 새지는 않습니다. 그러나 permissions.py:31-33 의
     can_see_account 독스트링이 바로 이 모양(「can_manage_retreat 를 그대로 쓰면 …
     계좌까지 함께 움직입니다」)을 이유로 함수를 가른 것이라, 한 자리가 남으면
     그 이유가 거짓이 됩니다. 166줄을 can_see_account 로 바꾸면 끝입니다.
     test23_g05 는 계속 통과합니다(판정값이 같습니다).

3. test24_b02 의 ③ 이 비어 있습니다 — 정규식이 자기 ③ 줄을 못 봅니다
   · tests/test_stage24.py:214 정규식
       (used_at|revoked_at|expires_at)\s*(is_\(|is_not\(|==|!=|&lt;|&gt;|\.is_)
     을 직접 돌려 본 결과:
       False  if token.revoked_at is not None:      ← :219 의 ③ 이 찾는 바로 그 줄
       False  if token.used_at is None:
       False  if token.used_at:
       False  if not t.revoked_at:
       False  bool(t.used_at)
       True   InviteToken.used_at.is_not(None)
       True   token.expires_at &lt; _now()
     즉 ③(:218-219 `revoked_at is not None` 이 auth.py 에 있다)은 스캐너와 다른
     모양을 확인하는 것이라 「검사가 볼 것을 실제로 보고 있는가」 를 증명하지
     않고, 가장 흔한 파이썬 판정(is None · 참거짓 · not)이 전부 새어 나갑니다.
     템플릿(.html)도 안 봅니다.
   · 지금 실제 상태는 0줄이 맞습니다 — app/ · scripts/ 를 따로 grep 했을 때
     auth.py · models.py 밖은 admin_users.py:115·356 의 `.expires_at.date()` 뿐
     (값 찍기). 수용기준 7 은 사실로는 성립하고, 시험이 그걸 못 지킬 뿐입니다.
   · 고칠 방향: 정규식에 `\bis\s+(not\s+)?None` 과 `if\s+(not\s+)?\S*\.(used_at|revoked_at|expires_at)\s*:`
     을 더하고, ③ 은 임시 파일에 판정 한 줄을 심어 잡히는지 재는 쪽으로
     (test6_k05b 가 하는 그 모양).

4. 고친 동작을 옮겨 적은 주석이 그대로입니다 (검토-원칙 4 · 11-3 3단계)
   · app/templates/budget.html:24
       {# 지출 화면과 같은 자리 — 총무팀만 받습니다 (파일에 계좌가 있습니다) #}
   · app/templates/expenses.html:16-17
       파일에는 계좌가 들어 있어 총무팀만 받습니다 — 남겨 두면 누른 사람이 403 화면을 봅니다.
     둘 다 이제 거짓입니다(편집자도 받고, 403 은 열람 전용만).
   · docs/옛말.md 에 이번 판이 바꾼 주장(「엑셀은 총무팀만 받는다」)이 안 들어갔습니다
     — 그래서 check_stale 이 초록입니다. 11-3 3단계가 「사람이 낱말을 고르지 않는다 —
     옛말.md 에 표기를 여럿 적는다」 고 정한 자리입니다.

5. 보고의 수 하나가 실측과 다릅니다
   · docs/review/최근.md:172  「id 1 을 가리키는 행 444 (지우기 전과 같음)」
   · 실측(22개 FK 칸에서 = 1 인 행 합): 사본 444 · 운영 지금 443.
     invite_tokens.created_by_id 가 2 → 1 — id 1 이 발급해 준 「지울 계정」 의
     토큰 행이 user_id 의 CASCADE 로 함께 사라졌습니다. 0-(b):47 의
     「created_by_id … 지울 계정 0」 은 「가리키는 쪽」 으로는 맞지만, 행 자체가
     다른 칸으로 사라진 것을 「지우기 전과 같음」 이 덮습니다. 「잰 것만 적습니다」.

━━ 봐 둘 것 ━━

6. FK 없이 users.id 를 담는 칸이 actor_id 말고 하나 더 있습니다 — activity_logs.target_id
   · 운영 DB 실측: target_type='user' 이고 users 에 없는 target_id → 8행
     (target_id 2·19·20·21 / 초대_링크_발급 · 초대_링크_사용 · 계정_변경).
     summary 문자열에 이름이 실려 있어 「누구 얘기였나」 는 여전히 읽힙니다.
   · scripts/계정정리.py:22-26 과 FK없는칸(:72) 은 actor_id 만 「FK 없는 자리」 로 적고
     있습니다. 폴리모픽 칸이라 비우면 「무엇에 대한 기록인가」 를 잃고, 두면 없는
     id 를 가리킨 채 남습니다 — 어느 쪽인지는 사람이 정할 것이라 여기 둡니다.
     (notifications.target_id 는 전부 'task' 345행 — 해당 없음.)
   · JSON 칸: activity_logs.before_value/after_value 에 id 꼴 키 0행 ·
     notification_logs.payload 0행 · 나머지 JSON 칸은 라이브러리·부서·run id 라
     오늘 운영 자료에는 user id 를 담은 JSON 이 없습니다.
   · models.py 전수(*_id 인데 users FK 아닌 것): activity_logs.actor_id ·
     activity_logs.target_id · notifications.target_id · discussion_entries.carried_from_run_id ·
     discussion_entries.source_meeting_id — 사람을 가리킬 수 있는 것은 앞의 둘뿐.

7. 운영 DB 고아 직접 셈 — 수용기준 1·3·4 실측 일치
   · users = [(1, 'admin', 활성 1)]
   · FK 22개 칸 전부 고아 0 (표별 값 있는 행: notifications 345 · tasks 55 · invite_tokens 18 · …)
   · activity_logs.actor_id 고아 0 · 전체 52 (사본 51 + 계정_삭제 1) ·
     actor_id 비고 actor_name 남은 행 8 — 보고 1-g 와 같음
   · 사본 app-20260909-231055.db 의 users 21 · actor_id 값 있음 42 — 지우기 전 상태
   · 계정_삭제 로그: actor_id·actor_name 없음 · target user/1 · 이름 없음 — 보고와 같음

8. budget_xlsx.py:170  `_widths(ws3, [12, 26, 12, 14, 12, 26, 10])` — 폭 7개가 숫자로 박혀 있음
   · 계좌 칸이 없을 때 6번째 열(지급여부)에 26(계좌 폭)이 가고 10 은 빈 열에 갑니다.
     보이는 것만의 문제. number_format 범위(ws 9-11·8열, ws3 4열)와 _style_header 는
     계좌가 맨 끝/끝에서 둘째라 안 밀립니다. :100 의 18개 폭도 마지막 하나가 빈 열 —
     무해. 열 이름으로 폭을 매기면 사라지는 자리입니다.

9. test24_a03 (:93-98) 의 사본은 순서를 증명하지 않습니다
   · tmp/app.db 는 정리() 를 부르기 전에 복사해 둔 것이라, snapshot 이 delete 뒤에
     오더라도 「사본 안 users = 지우기 전」 은 참입니다. 순서의 안전성은 a04
     (snapshot 이 던지면 안 지운다)가 지키고 있으니 그것으로 충분하되, 최근.md:189
     처럼 a03 을 「사본이 먼저」 의 근거로 적지는 않는 편이 맞습니다.

10. scripts/계정정리.py:138-140 은 config.DATA_DIR/app.db 를 뜨고, 세션은 config.DATABASE_URL 을 엽니다
   · app/config.py:26 — DCB_DATABASE_URL 로 따로 줄 수 있습니다. 갈리면 「다른 파일의
     사본」 을 뜨고도 「떴습니다」 가 찍힙니다. 관리자링크.py 와 같은 모양이라 지금
     새는 곳은 없고, 한 줄 확인(두 경로가 같은 파일인지)이면 닫힙니다.

11. 같은 목적의 도구가 둘인데 순서가 안 적혔습니다
   · CLAUDE.md:885 「이미 생긴 중복은 scripts/merge_users.py 로 … 비활성화만」
   · CLAUDE.md:3378 14장 표 「중복 계정 정리 (4-12) | merge_users.py — 비활성화만 한다」
   · CLAUDE.md:807 4-12 인용 블록에만 계정정리.py 가 있고 14장(「어디를 고쳐야 하는지」)엔 없음.
     merge(비활성) 다음에 계정정리(삭제)인지, 각각 언제인지 한 줄이 없습니다.

12. docs/인계.md:112 가 docs/review/2026-09-09-도막2.md 를 가리키는데 그 파일이 아직 없습니다
   · ls docs/review: 도막0 · 도막1 만. 도막 1 은 같은 판 안에서 얼렸습니다(커밋 0331a35).
     이번 판이 푸시 전에 같은 것을 안 하면 인계의 「재료가 어디」 가 죽은 길이 됩니다.

13. docs/review/최근.md 남은 자리
   · :2 커밋 · :298 __PYTEST__ · :304 __GEUL__ · :326 __SERVER__ · :331 __NAMES__ · :344 __RV__
     — 채울 때 1번(test_stage6.py 포함 · 그 상태의 pytest)과 5번(443)을 반영해야 합니다.
   · app/ · templates/ 가 바뀌었으므로 11-3 2단계(재시작·PID) 건너뛰면 안 됩니다 — 자리는 있음.

━━ 없다고 적는 것 ━━

  · 실명·전화·계좌·토큰: 최근.md 에 없음(수·id·파일명뿐). test_stage24 의 번호는 가짜 꼴.
    글검사 4/4 exit 0.
  · 계좌가 새는 다른 입구: app/ 전수 grep — 위 2번(폼 미리 채움) 말고는 없음.
    settings.html:25 는 본인 bank_account. JSON/API 로 payer_account 를 내보내는 곳 없음.
  · revoke_all 의 새 정의: issue() 가 revoke_all 을 먼저 부르고 새 토큰을 뒤에 넣어
    자기 것을 취소하지 않음(auth.py:70-80). last_used_at 은 entered_ever 와 같은 조건 — 어긋남 없음.
  · 엑셀을_받는다(templating.py:190) 와 require_editor(security.py:95) — 둘 다 perm.is_readonly 에서 나옴. 정의는 하나.
  · ORM 쪽 cascade 간섭: User 에는 department 관계뿐이라 db.delete(User) 가 자식 FK 를
    NULL 로 덮지 않음 — notifications 1,839 → 345 로 DB CASCADE 가 실제로 돌았음.

━━ 사람에게 묻는 것 ━━

  A. activity_logs.target_id (target_type='user') 8행 — 비울지(무엇에 대한 기록인지 잃음)
     그대로 둘지(없는 id 를 가리킴). 앞으로 계정을 지울 때마다 같은 모양이 생깁니다.
  B. merge_users.py(비활성) 와 계정정리.py(삭제) 의 순서·쓰임을 4-12/14장에 적을지.
  C. tests/test_stage6.py ID_비교_허용 이 줄 번호를 박는 방식 — 이번 판처럼 위쪽에
     함수 하나만 넣어도 빨개집니다. 값 대신 (파일, 식)으로 바꿀지는 이 판 밖의 결정.

━━━ 검토자 여기까지 ━━━
```

## 검토자를 받아 고친 것

| # | 무엇 | 어떻게 |
|---|---|---|
| 1 | `tests/test_stage6.py` 가 스테이지 밖이라 pytest 가 빨간 채로 나갈 뻔 | 커밋에 넣었고, 7장의 pytest 줄은 그 상태에서 다시 뜬 값입니다 |
| 2 | 등록 폼의 직전 계좌가 `can_manage_retreat` 로 따로 판정 — 「함수 하나」 가 거짓 | `can_see_account` 로. 0-(e) 표에도 그 자리를 더했습니다 |
| 3 | `test24_b02` 의 정규식이 `is not None` · 참거짓 · `not` 을 다 놓치고 ③ 이 비어 있었음 | 모양을 넓히고 템플릿까지 보며, `b02b` 가 여덟 모양을 심어 전부 걸리는지 잽니다 |
| 4 | 낡은 주석 둘 · `옛말.md` 미기재 | 주석을 고치고 낡은 주장을 표기 넷과 함께 적었습니다 |
| 5 | 「id 1 을 가리키는 행 444 (지우기 전과 같음)」 이 실측(443)과 다름 | 443 으로 고치고 **어느 행이 왜 사라졌는지** 적었습니다 |
| 6 | `activity_logs.target_id` — FK 없는 칸이 하나 더 | 코드는 안 건드리고 스크립트 머리·1-g·`봐둘것.md` 에 적었습니다 (사람이 정할 것) |
| 8 | 환급 시트 열 폭이 숫자로 박혀 계좌 폭이 지급여부에 감 | 계좌 칸이 없으면 그 폭도 뺍니다 |
| 9 | `a03` 을 「사본이 먼저」 의 근거로 적음 | 근거를 코드 순서와 `a04` 로 바꾸고, `a03` 은 아니라고 적었습니다 |
| 10 | 세션이 여는 파일과 사본을 뜰 파일이 갈릴 수 있음 | `make_url(...).database` 와 `DATA_DIR/app.db` 를 견줘 다르면 거절 · `test24_a05` |
| 11 | `merge_users.py` 와 `계정정리.py` 의 순서가 없음 | 4-12 에 「merge 가 먼저」 한 줄 · 14장 표에 한 줄 |
| 12 | `인계.md` 가 아직 없는 `2026-09-09-도막2.md` 를 가리킴 | 이 보고를 그 이름으로 얼립니다 (8장 뒤) |
| 13 | 자리표시자 | 전부 채웠습니다 |

**사람에게 묻는 것 셋** — A(`target_id`)는 열어 두고, B(순서)는 적었고,
C(`test_stage6` 의 줄 번호 방식)는 이 판 밖입니다 — 이번 판에서도 함수
하나 넣었다고 빨개졌으므로 `docs/봐둘것.md` 에 한 줄 더 남길 만합니다.
