사람이 할 것
노트북에서 직접: **전원을 통째로 내렸다 올리기** — `docs/배포-안내.md` 12-4 를 그대로. 그러고 「올렸습니다」 한 줄만 주시면 이 창이 나머지를 잽니다 (4장) · 사역자 열 분 계정(인계 18번) · 개기자 주소(인계 8번)
관리자 권한: 없음 — 이 판은 `app/`·`templates/`·`static/` 을 하나도 안 고쳤습니다 (7장)
정할 것: 새벽 3시에 노트북이 꺼져 있으면 그 날 백업이 없어지는 것을 그대로 둘지 (2-4 · 봐둘것 AX-a)
막혀서 못 한 것: **올라온 뒤에 재는 것**(지시 3) — 전원을 내리면 이 창도 같이 죽어 같은 판에 못 넣습니다 (4장)

작성 시각: 2026-09-12 15:40 (KST)
커밋: 6c7d1d9 재는 도구 · 6ed7503 없는 장 검사 · c28cce0 문서 · (이 보고가 마지막)
작업: 되살아나는지 — 전원이 내려갔다 올라와도 앱·터널·백업이 스스로 서는가 (내리기 전까지)

---

# 0. 한 줄 요약

**내리기 전 자국을 다 쟀고, 재면서 셋을 찾았습니다** — 기준 문서가 터널을
엉뚱한 자리에 적어 두었고(11-2), 새벽 3시에 꺼져 있으면 그 날 백업이
말없이 없어지며, **앱이 부팅으로 스스로 떴다는 자국은 아직 못 봤습니다**
(터널은 있습니다). 전원을 내렸다 올리는 것은 사람이 하고, 그 뒤를 재는 것은
다음 판입니다.

---

# 1. 고치기 전 훑기 (11-3 0단계)

## 1-1. 11-2 의 「터널을 무엇이 띄우나」 를 건드리면 — **넷 나옴**

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| 같은 주장을 적은 다른 문서 | `docs/배포-안내.md` 8-3 은 **처음부터 서비스로** 적혀 있었습니다 — 안 고쳤습니다. 낡은 쪽은 CLAUDE.md 하나였습니다 |
| 낡은 말 목록 | `docs/옛말.md` 에 한 줄 더했습니다 — 안 적으면 `check_stale` 은 다음에도 못 봅니다 |
| 그 말을 스스로 담게 되는 자리 | 고쳤다고 적는 문장이 옛 표기를 그대로 인용하면 **그 줄이 다시 걸립니다**(10장). 인용하지 않고 모양만 적었습니다 |
| 봐둘것 AM-f | 「스케줄러의 트리거를 한 번 볼 것」 이 열려 있었습니다 — 이 판이 재면서 함께 답했습니다(6-3 · AX-c) |

## 1-2. `check_placeholder` 를 넓히면 — **둘 나옴**

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| 그 파일의 독스트링이 적어 둔 「안 닫히는 것」 | 그 절이 **지금 무엇을 못 보는지**의 정본입니다. 넓힌 만큼 줄이고, 새 규칙의 경계를 그 자리에 적었습니다 |
| `tests/test_stage38.py` 의 머리 표 | 「잰 것」 이 셋이었는데 넓히면 넷이 됩니다. 표와 시험 이름을 함께 고쳤습니다 |

## 1-3. 검토가 시킨 고침도 훑었습니다 — **셋 나옴**

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| 「11-2 가 그러라고 했다」 고 적은 자리 | **셋이었습니다**(스크립트 머리말 · CLAUDE.md 14장 · 이 보고 7장). 규칙을 실제로 세우고 셋의 근거를 그 줄로 맞췄습니다 — 하나만 고치면 나머지 둘이 없는 규칙을 계속 가리킵니다 |
| 12-4 의 경고를 고치면 | 7장의 수용 기준 다) 판정이 같이 걸립니다. 둘이 서로를 부정하던 자리라 **재서** 한쪽으로 맞췄습니다 |
| `check_placeholder` 의 머리 판정을 넓히면 | 그 파일 독스트링의 「안 닫히는 것」 과 `test38_b03` 이 같이 바뀝니다. 셋을 한 번에 고쳤습니다 |

## 1-4. 훑기가 안 짚었으면 어땠을까

**11-2 는 짚였을 것입니다** — 지시 1-b 가 「앱·터널·백업이 다 있는지」 를
시켰고, 작업 목록에 터널이 없는 것은 그 자리에서 바로 보입니다.

**옛말 목록은 아니었을 것입니다.** 앞 판에서 정확히 그 자리를 놓쳤고
(보고가 스스로 「낡은 말 목록에 없으면 못 본다」 고 적어 놓고 안 넣었습니다)
커밋 전 검토가 잡았습니다. 훑기 표에 그 칸이 있어서 이번에는 적었습니다.

---

# 2. 내리기 전에 잰 것 (지시 1)

**재는 것은 `scripts/되살아나나.ps1` 하나입니다.** 내리기 전과 올라온 뒤에
**같은 것을 같은 방식으로** 재야 견줄 수 있어서, 명령을 그때그때 다시 짜지
않고 파일 하나로 두었습니다. **재기만 하고 아무것도 안 바꿉니다.**

## 2-1. 작업 스케줄러에 걸린 것 (수용 기준 가)

```
  대청부 백업       상태=Ready     켬=True  트리거=Daily        동작=python.exe
                 마지막실행=2026-09-12 오전 3:00:00 결과=0 다음=2026-09-13 오전 3:00:00
                 !! 그 시각에 꺼져 있었으면 건너뛰고 나중에 안 채웁니다 (StartWhenAvailable=False)
  대청부 알림       상태=Disabled  켬=False 트리거=Daily        동작=python.exe
                 마지막실행=1999-11-30 오전 12:00:00 결과=267011 다음=2026-09-13 오전 8:00:00
                 !! 배터리면 시작 안 함
  대청부 앱        상태=Running   켬=True  트리거=Boot         동작=serve.bat
                 마지막실행=2026-09-12 오후 2:22:22 결과=267009 다음=
```

| 서야 하는 것 | 어느 줄인가 | 지금 |
|---|---|---|
| 앱 | 작업 `대청부 앱` — **시스템 시작 시** 트리거 · `serve.bat` | 돌고 있음 (267009 = 「지금 돌고 있다」) |
| 백업 | 작업 `대청부 백업` — 매일 03:00 | **오늘 새벽치**가 결과 0 으로 끝났고 다음이 잡혀 있음 |
| 터널 | **작업 스케줄러에 없습니다** — 아래 2-2 | 서비스로 돌고 있음 |

**`대청부 알림` 은 꺼진 것이 맞습니다** — 4-11 이 「등록만 하고 사용 안 함으로
둔다」 고 정한 그대로입니다. 고장이 아닙니다.

## 2-2. 터널은 작업 스케줄러에 없었습니다 — **이 판이 찾은 첫째**

```
[터널 — 윈도우 서비스 Cloudflared]
  상태=Running 시작=Auto 계정=LocalSystem
  실행 경로에 --config 있음: True
  붙은 커넥터: 1개
    붙은 때: 2026-09-09T11:17:40Z (UTC)
```

**CLAUDE.md 11-2 는 터널까지 작업 스케줄러 쪽으로 적어 두고 있었습니다.**
터널은 그 목록에 없습니다 — `Cloudflared` 라는 윈도우 서비스입니다.
작업 스케줄러만 열어 보면 **터널이 통째로 안 보이고**, 그것이 곧 「안에서는
다 되는데 밖에서 안 열리는」 상태를 못 알아채는 자리입니다. 11-2 를 고치고
`docs/옛말.md` 에 넣었습니다.

**`docs/배포-안내.md` 8-3 은 처음부터 서비스로 적혀 있었습니다** — 낡은 것은
기준 문서 쪽 하나였습니다. 「같은 것이 두 곳에 있으면 갈린다」 의 그 모양입니다.

## 2-3. 더 있어야 하는 것이 있나 (지시 1-c)

**목록을 글로 세어 두지 않습니다**(10장). 스크립트는 **이름이 「대청부」 로
시작하는 작업 전부**를 훑고, 터널 서비스를 따로 봅니다 — 작업이 하나 늘면
고치지 않아도 저절로 나옵니다.

**빠뜨릴 수 있는 자리부터 세웠습니다.**

| 빠뜨릴 수 있는 곳 | 어떻게 했나 |
|---|---|
| 작업 스케줄러에 없는 것 | 실제로 터널이 그랬습니다(2-2). 서비스를 따로 봅니다 |
| 이 노트북의 **다른 프로그램**의 작업 | 이름으로 골라 섞이지 않게 했습니다 — 같은 노트북에 다른 시스템의 앱·터널·백업 작업이 따로 걸려 있습니다 |
| 파일로만 있는 것 | `data/secret_key.txt` · `data/vapid_private.pem` · `data/anthropic_key.txt` 는 전원과 무관합니다(디스크에 남습니다). **백업 대상**이라 11-2 가 이미 이름해 두었습니다 |
| 「등록돼 있다」 와 「그때 돈다」 | 마지막 실행 결과와 `StartWhenAvailable` 을 함께 찍습니다(2-4) |

## 2-4. 등록만 되고 안 도는 것이 있나 (지시 1-d) — **이 판이 찾은 둘째**

걸린 일마다 마지막 실행 결과가 정상이었습니다 — 백업 `0`, 앱 `267009`(지금
돌고 있다), 알림은 한 번도 안 돈 값(일부러 꺼 두었으므로 맞습니다).

**그런데 백업에 조용한 구멍이 있습니다.** `대청부 백업` 은 매일 03:00 이고
`StartWhenAvailable` 이 **꺼져** 있습니다 — 그 시각에 노트북이 꺼져 있거나
자고 있으면 **그 날은 건너뛰고 나중에 채우지 않습니다.** 오류도 안 나고
화면에도 안 뜹니다.

자국이 있습니다.

```
새벽 3시 백업: 5개
  가장 이른 것: app-20260908-030001.db
  가장 최근 것: app-20260912-030002.db
손으로 뜬 것까지 합쳐: 17개
```

**03시 파일은 `app-20260908-030001.db` 이 가장 이릅니다** — 그 앞에는
하나도 없고, 09-08 부터 09-12 까지 닷새가 연속입니다. **그 앞이 왜 비었는지는
이 자료로 못 가릅니다** — 그 시각에 꺼져 있었을 수도, 작업이 그때 아직
없었을 수도 있습니다. **작업이 언제 등록됐는지의 자국은 어디에도 없습니다**
(08-31 에 있는 파일은 09:00 에 손으로 뜬 것이라 그 자국이 아닙니다).
가른 척 적지 않습니다.

**고치지 않았습니다.** 운영 설정이고, 켜면 부팅 직후에 백업이 돌아 그때 쓰는
사람의 화면이 잠깐 느려집니다. 사람칸 「정할 것」 과 봐둘것 AX-a 에 넘겼습니다.

## 2-5. 지금 도는 것들의 자국 (지시 1-e · 수용 기준 나의 「전」)

| 무엇 | 내리기 전 값 |
|---|---|
| 마지막 부팅 | 2026-09-09 20:17:04 |
| 앱 — 포트를 쥔 PID | **13672** · 안에서 `GET /login -> 200` |
| 터널 — 붙은 커넥터 | 1개 · 붙은 때 `2026-09-09T11:17:40Z` |
| 백업 — 03시 파일 | 5개 · 가장 최근 `app-20260912-030002.db` |

**터널은 부팅으로 스스로 섰다는 자국이 이미 있습니다** — 커넥터가 붙은 때를
한국 시각으로 옮기면 **20:17:40**, 마지막 부팅(20:17:04)의 36초 뒤입니다.
사람이 붙인 것이 아니라 부팅이 붙인 것입니다.

**앱은 그 자국이 없습니다.** `data/server.log` 의 시작 줄에 **시각이 없어서**
언제 떴는지 못 가리고, 작업 스케줄러의 이력 로그는 그 부팅 **뒤**인
2026-09-10 에 켜졌습니다. 지금 도는 PID 13672 는 앞 판이 손으로 다시 켠
것입니다. **그래서 사람이 한 번 내렸다 올려야 합니다** — 이 판이 여기서
멈추는 이유가 그것입니다.

> **다음 부팅부터는 볼 자리가 있습니다.** 작업 이력 로그
> (`Microsoft-Windows-TaskScheduler/Operational`)가 켜져 있고, 오늘 재시작한
> 것이 거기에 `대청부 앱` 시작으로 남아 있는 것을 확인했습니다.

---

# 3. 사람이 내렸다 올린다 (지시 2)

**절차는 `docs/배포-안내.md` 12-4 에 새로 넣었습니다.** 사람칸은 그리로만
보냅니다 — 같은 말을 두 곳에 적으면 갈립니다(11-3 4단계).

| 지시 | 어디에 |
|---|---|
| 2-a 붙여 쓸 수 있는 한 벌 | 12-4 의 ①~⑤. 붙여넣을 명령은 파워셸 두 줄 |
| 2-b 내리기 전 사본 | **떴습니다** — 아래 |
| 2-c 무엇을 보면 「섰다」 인가 | 12-4 의 표. **밖에서 열리는 것까지**이고, 집 와이파이로 열면 안 된다고 적었습니다 |
| 2-d 안 서면 무엇을 보나 | 12-4 의 둘째 표 — 앱·터널·백업마다 먼저 볼 곳 |

**사본**(11-2 대로 미리 세고 · 돌리고 · 다시 셌습니다):

```
돌리기 전  app-*.db 16개 · 폴더 전체 31개
$ scripts/backup.py
백업했습니다: app-20260912-151859.db (0.7MB)
  VAPID 키도 함께: vapid-20260912-151859.pem
  업로드 폴더도 함께: uploads-20260912-151859.zip (0.5MB · 바뀐 것이 없어 지난 것에 이어 붙였습니다)
돌린 뒤    app-*.db 17개 · 폴더 전체 34개 (새로 생긴 것 셋)
```

**절전이 아니라 「시스템 종료」 라고 적었습니다** — 절전은 프로세스가 그대로
살아 있어서 아무것도 재지 못합니다. 그리고 **로그인하기 전에 밖에서 먼저**
열어 보라고 적었습니다: 앱은 「시스템 시작 시」, 터널은 서비스라 **둘 다
로그인 전에 서야 맞습니다.** 로그인부터 하면 「로그인해야 서는 것」 과
구별이 안 됩니다.

---

# 4. 올라온 뒤에 잴 것 (지시 3) — **이 판에서는 못 합니다**

**전원을 내리면 이 창도 같이 죽습니다.** 그래서 지시 3 은 같은 판에 못
들어갑니다 — 사람이 「올렸습니다」 를 알리면 다음 판이 2-5 의 표와 견줍니다.

| 볼 것 | 섰다의 뜻 |
|---|---|
| 앱 | 포트를 쥔 PID 가 **13672 가 아니다** · 안에서 200 |
| 터널 | 커넥터가 하나라도 붙어 있고 **붙은 때가 이번 부팅 뒤**다 |
| 백업 | 다음 03시가 지난 뒤 가장 최근 03시 파일이 그 날짜로 바뀐다 |

**밖에서 열린 것까지 봐야 「섰다」 입니다**(수용 기준 다). 이 판에서도
밖에서 한 번 열어 잰 값이 있습니다 — 7장.

**하나라도 안 서면** 왜 안 섰는지 다음 판이 찾고, **고친 뒤에 다시 내렸다
올려 보는 것은 사람 몫**이라 그때 사람칸에 남깁니다. 「고쳤다」 와 「고친
것이 섰다」 는 다릅니다.

---

# 5. 이 판이 안 재는 것 (지시 4)

| 안 잰 것 | 왜 |
|---|---|
| 다른 컴퓨터에서 서는지 | 이 노트북 한 대만 봅니다 (지시 4-a) |
| 인터넷이 끊겼다 돌아올 때 터널이 다시 붙는지 | 전원과 다른 이야기입니다. **만들지 않고 봐둘것 AX-b 에 적었습니다** (지시 4-b) |
| 앱이 **지난** 부팅으로 스스로 떴는지 | 자국이 없습니다(2-5). 없는 것을 있다고 적지 않습니다 |
| 3시에 꺼져 있던 이레가 왜 비었는지 | 두 갈래를 이 자료로 못 가릅니다(2-4) |

**안 재는 것을 재는 것처럼 적지 않습니다**(지시 4-c). 위의 것들은 「안 된다」 가
아니라 **「재 본 적이 없다」** 입니다.

---

# 6. 앞 판이 남긴 자잘한 것 (지시 5)

## 6-1. 얼린 판과 덩어리의 PID — 정본은 보고입니다 (지시 5-a)

앞 판은 보고 6장에 `35340 → 33768` 을 적고 얼렸는데, 검토가 `app/` 을 두 줄
더 고치게 해서 **덩어리에는 `33768 → 13672`** 가 실렸습니다. 같은 커밋을 두
값이 가리켰습니다.

**정본은 보고입니다 — 그래서 보고를 쓰기 전에 재시작합니다.** 보고를 쓴 뒤에
`app/`·`templates/`·`static/` 을 또 고쳤으면 재시작을 다시 하고 **보고의 그
줄을 고칩니다.** 덩어리는 보고의 사본이라 다른 값을 적지 않습니다.
**CLAUDE.md 11-3 2단계 한 곳**에만 적었습니다.

## 6-2. 「없는 장」 을 검사가 볼 수 있나 (지시 5-b) — **볼 수 있어서 만들었습니다**

**첫째 규칙으로는 못 봅니다.** `check_placeholder` 는 자리표시자 **낱말**을
찾는데, 앞 판의 탈은 빈 자리가 아니라 **없는 자리**였습니다 — 수용 기준에
O 를 찍어 두고 그 O 가 가리키는 11장이 아예 없었습니다.

**볼 수 있는 길이 있었습니다 — 보고가 스스로 가리킨 것만 보면 됩니다.**
수용 기준 표의 칸이 `11장` 으로 시작하면 그 장이 보고에 있어야 합니다.
**있어야 할 장의 목록을 두지 않습니다**(10장) — 가리키지 않으면 볼 것도
없고, 판마다 고칠 목록도 안 생깁니다.

앞 판의 보고에서 11장을 없애고 돌려 봤습니다 — **검토자가 [B] 로 짚은 바로
그 줄(292줄)을 집습니다.**

```
['292줄: 수용 기준이 11장을 가리키는데 보고에 그 장이 없습니다']
```

**경계도 적었습니다**(그 파일의 독스트링 · 9장의 시험) — 칸의 맨 앞만 보고,
수용 기준 표 밖은 안 보며, 다른 문서의 장을 가리키는 것과는 **못 갈립니다**
(그때는 `CLAUDE.md 11-3` 처럼 문서 이름을 앞에 적습니다).

## 6-3. 마감일만 고친 것도 「옮김」 으로 (지시 5-c)

사람이 2026-09-12 에 정했습니다 — **늘려 둔 기간이 말없이 되돌아가지 않는
쪽**입니다. 코드가 이미 그 모양이라 **고친 것은 없고**, 정한 것과 날짜를
**CLAUDE.md 6-4** 에 적었습니다. 봐둘것에는 **AX-d** 를 그 파일의 「정함」
꼴(제목에 취소선 · 머리에 정한 날과 결론)로 두고 **6-4 를 가리키게만**
했습니다 — 규칙을 두 곳에 적으면 갈리고, 이 파일은 원래 닫힌 것을 그렇게
남기는 자리입니다.

봐둘것에는 **이 판이 잰 것**도 넣었습니다 — AX-a(백업이 3시에 건너뛴다) ·
AX-b(회선 끊김은 안 쟀다) · AX-c(재시작 설정을 재어 AM-f 가 열어 둔 물음에
답했다: 트리거는 부팅 하나가 맞고, 그 밖에 「실패하면 다시 시작」 이 켜져
있다 — **그것이 그때의 원인이라고는 적지 않았습니다**).

---

# 7. 돌고 있는 서버에서 확인한 것 (11-3 2단계)

**재시작을 건너뛰었습니다** — 이번 커밋에 `app/` · `templates/` · `static/` 이
하나도 없습니다. 바뀐 것은 문서 · 시험 · 스크립트뿐입니다(11장).

**그래도 밖에서 한 번 열어 쟀습니다**(수용 기준 다) — 이 판이 「밖에서
열리는 것까지 봐야 섰다」 를 규칙으로 적었으므로, 그 말을 적은 판이
한 번도 안 재고 넘어가면 안 됩니다.

```
밖에서 GET /login -> 200
cf-ray 있음: True
server: cloudflare
본문에 로그인 폼: True
```

**`127.0.0.1` 이 아니라 터널 주소로 받았습니다** — 응답에 Cloudflare 의
표시가 붙어 있으므로 요청이 밖으로 나갔다 터널을 타고 돌아온 것입니다.
**주소 자체는 여기 적지 않습니다**(11-2) — 어디를 열었는지는
`docs/배포-안내.md` 11장 ④ 가 말합니다.

**상세 패널 점검(`docs/checks/drawer.js`)도 건너뛰었습니다** — 이 판은 그
스크립트도 화면도 한 줄도 안 고쳤고, 같은 코드를 앞 판이 세 화면 × 두 계정
여섯 판으로 재어 보고에 실었습니다. **건너뛴 사실을 적어 두는 것이 규칙이라**
여기 적습니다.

---

# 8. 수용 기준 — 각 줄이 무엇을 보나

| 기준 | 어디서 잰다 | 통과 |
|---|---|---|
| 가) 작업 목록이 있고 앱·터널·백업이 각각 어느 줄인지 짚혔다 · 없으면 없다고 | 2-1 · 2-2 | O — **터널은 「없다」 쪽이었습니다**(작업이 아니라 서비스) |
| 나) 내리기 전 자국과 올라온 뒤 자국을 나란히 · 앱 PID · 터널 · 백업 | 2-5 가 「전」, 4장이 「뒤에 볼 것」 | **반만** — 「전」 은 다 쟀고 「뒤」 는 사람이 올린 뒤입니다. 못 잰 것을 잰 것처럼 적지 않습니다 |
| 다) 밖에서 실제로 열린 것을 잰다 | 7장 | O |
| 라) 사람이 할 일이 붙여 쓸 수 있는 꼴로 사람칸에 | 3장 — 사람칸은 `docs/배포-안내.md` 12-4 로 보내고 붙여넣을 명령은 거기 있습니다 | O |
| 마) 얼린 판과 덩어리의 PID 중 어느 쪽이 정본인지 11-3 에 한 곳으로 | 6-1 | O |
| 바) pytest 옵션 없이 전부 통과 · 글검사 7/7 · 새 파일은 `git add` 뒤에 한 번 더 | 9장 | O |
| 사) 검토자 원문이 최근.md 에 그대로 | 12장 | O — **원문을 실은 뒤에 찍습니다** |

---

# 9. 검사와 시험

```
1443 passed, 3 warnings in 1296.60s (0:21:36)
```

**보고를 다 채운 뒤에 돌린 값입니다**(11-3) — 12장(검토자 원문)까지 넣고
나서입니다. 앞 판이 1439 였고 넷 늘었습니다 — `test38_b01`~`b03`(수용 기준이
가리킨 장) 과 `test6_g05`(`.ps1` 의 BOM).

> **그 앞 판에서는 `1 failed, 1441 passed` 였습니다.** 빨간 하나가
> `test38_a03` 이었고, 그 셋째 단언이 **살아 있는 보고가 초록인가**를
> 재고 있었습니다 — 보고를 쓰는 중이라 빨간 것이 맞는데, 그 빨강이
> 코드가 깨진 것과 겉이 같습니다. 검토가 [봐3] 으로 짚었고 그 단언을
> 걷었습니다(12장).

```
━━━ 모아서 ━━━
  통과  check_names · check_stale · check_named · check_counts
  통과  check_handoff · check_placeholder · check_orders

글 검사가 전부 통과했습니다.
```

**`check_placeholder` 가 이 판에서 네 번 빨갰고 네 번 다 맞았습니다** —
보고의 결과 자리 셋(pytest · 글검사 · 바뀐 파일)과 **아직 없던 12장**입니다.
자리표시자를 일부러 그 꼴로 적어 두고 채워 나갔습니다: 「꼴이 다른 빈 자리는
못 본다」 가 그 검사의 한계라(14장) **꼴을 맞춰 적는 것이 쓰는 쪽의 몫**입니다.

**새 파일이 둘입니다**(`scripts/되살아나나.ps1` · 시험은 기존 파일에 붙였습니다) —
`git add` 뒤에 한 번 더 돌렸습니다. `check_named` 가 그 전에는 둘을
집었고(`.ps1` 이라는 확장자와 윈도우 쪽 설정 이름), **저장소의 이름이 아니라서**
`docs/이름-넘김.txt` 에 까닭과 함께 넘겼습니다.

## 9-1. 새 시험

| 시험 | 무엇을 재나 |
|---|---|
| `test38_b01_수용_기준이_가리킨_장이_없으면_잡는다` | 온전한 판은 초록 · 가리킨 장을 없애면 빨강 · **절(`3-1`)도 같은가** · 몇 줄인지 말하는가 |
| `test38_b02_수용_기준_표_밖은_안_본다` | 대조 표의 「11장」 은 안 잡는다 · **그 줄이 실제로 그 판에 있는가**(없으면 아무것도 안 보는 시험) · **같은 말을 수용 기준 표로 옮기면 잡는가** |

---

# 10. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 0 | 왜 이 판인가 (까닭) | **했다** — 시킨 일이 아니라 배경입니다. 2-5 가 「재시작으로는 못 재는 것」 에 답합니다 |
| 1-a | 걸린 일을 전부 읽어 적기 (이름·언제·무엇을·켜졌나) | **했다** — 2-1. **작업이 어느 계정으로 도는지와 비밀번호는 안 적었습니다**(터널 서비스의 `LocalSystem` 은 붙박이 이름이고, 로그인 전에 서는 까닭 자체라 2-2 에 남깁니다) |
| 1-b | 앱·터널·백업이 다 있는지 · 없으면 그것이 이 판의 고장 | **했다** — 셋 다 있고, **터널이 작업 스케줄러에 없다**는 것이 나왔습니다(2-2). 없는 것을 만든 것이 아니라 **문서가 엉뚱한 자리를 가리키고 있었습니다** |
| 1-c | 셋 말고 더 있어야 하는 것 · 빠뜨릴 수 있는 자리부터 | **했다** — 2-3. 목록을 글로 세지 않고 스크립트가 이름으로 훑습니다 |
| 1-d | 실제로 도는 모양인지 · 마지막 실행 결과 | **했다** — 2-4. 결과는 다 정상이었고 대신 **백업의 조용한 구멍**이 나왔습니다 |
| 1-e | 지금 도는 것들의 자국 (앱 PID · 터널 · 백업) | **했다** — 2-5 |
| 2-a | 사람이 할 일을 한 벌로 · 사람칸에 · 붙여 쓸 수 있게 | **했다** — 배포-안내 12-4 이고 사람칸이 그리로 보냅니다 |
| 2-b | 내리기 전 사본 · 뜬 파일 이름을 적기 | **했다** — 3장. `app-20260912-151859.db` 와 같은 때의 둘 |
| 2-c | 올라온 뒤 무엇을 보면 「섰다」 인지 · 밖에서 열리는 것까지 | **했다** — 12-4 의 표. 집 와이파이로 열면 안 된다는 것까지 |
| 2-d | 안 서면 무엇을 보라는 것까지 | **했다** — 12-4 의 둘째 표 |
| 3-a~c | 올라온 뒤에 재기 | **안 했다** — **못 합니다.** 전원을 내리면 이 창도 죽습니다. 4장에 무엇을 어떻게 잴지 적어 두었고 사람칸 「막혀서 못 한 것」 에 올렸습니다 |
| 4-a | 다른 컴퓨터는 안 잰다 | **했다** — 5장 |
| 4-b | 회선 끊김은 안 잰다 · 볼 값이 있으면 봐둘것에 | **했다** — 봐둘것 AX-b. 만들지 않았습니다 |
| 4-c | 안 재는 것을 재는 것처럼 적지 않기 | **했다** — 5장이 「안 된다」 가 아니라 「재 본 적이 없다」 로 적습니다 |
| 5-a | PID 정본을 정해 11-3 에 한 곳으로 | **했다** — 6-1 |
| 5-b | 「없는 장」 을 볼 수 있는지 재고 · 되면 만들고 안 되면 왜인지 | **했다** — 볼 수 있어서 만들었습니다(6-2). 앞 판의 그 줄을 실제로 집는 것까지 확인했습니다 |
| 5-c | 사람의 판단과 날짜를 6-4 와 봐둘것에 | **했다** — CLAUDE.md 6-4 가 정본이고, 봐둘것 AX-d 는 그 파일의 「정함」 꼴로 **가리키기만** 합니다(6-3). 같은 규칙을 두 곳에 적으면 갈립니다 |
| 가) | 작업 목록 · 셋이 어느 줄인지 | **했다** — 2-1 · 2-2 |
| 나) | 전과 뒤의 자국을 나란히 | **다르게 했다** — 「전」 만 있습니다. 「뒤」 는 사람이 올린 뒤라 이 판에 못 넣습니다(8장의 그 줄도 「반만」 입니다) |
| 다) | 밖에서 열린 것을 잰다 | **했다** — 7장 |
| 라) | 사람칸이 붙여 쓸 수 있는 꼴 | **했다** — 3장 |
| 마) | PID 정본을 11-3 한 곳에 | **했다** — 6-1 |
| 바) | pytest · 글검사 · `git add` 뒤 한 번 더 | **했다** — 9장 |
| 사) | 검토자 원문 그대로 | **했다** — 12장 |

**안 한 것이 하나입니다** — 지시 3(올라온 뒤에 재기). 못 한 것이고, 지시문
자신이 「3 은 사람이 올린 뒤에 하는 것이라 같은 판에 못 넣는다」 고 적어
두었습니다. 사람칸에 올렸습니다.

---

# 11. 바뀐 파일

**커밋한 뒤에 이 명령이 아래를 냅니다** — 이 보고를 쓸 때는 아직 커밋 전이라
스테이징에서 셌습니다(`git diff --name-only --cached d5665b9`).

```
$ git diff --name-only d5665b9..HEAD
CLAUDE.md
docs/review/최근.md
docs/배포-안내.md
docs/봐둘것.md
docs/옛말.md
docs/이름-넘김.txt
docs/인계.md
scripts/check_placeholder.py
scripts/되살아나나.ps1
tests/test_stage38.py
tests/test_stage6.py
```

(얼린 판이 하나 더 붙습니다 — `docs/review/2026-09-12-되살아나는지.md`)

---

# 12. 검토자 원문

**커밋 전 검토를 받았고 답을 통째로 싣습니다** — 11-3 4단계가 「채팅에 있다」
로 가리키지 말라고 한 자리입니다. **한 글자도 안 가렸습니다**(`check_names`
가 이 장에서 잡은 것이 없습니다).

**막아야 함은 없었고, 고쳐야 함 여섯과 봐 둘 것 여섯이 나왔습니다.**
어떻게 했는지 먼저 적습니다.

| 검토 | 어떻게 했나 |
|---|---|
| [고1] 없는 규칙을 11-2 라고 가리켰다 | **규칙을 실제로 세웠습니다.** 11-2 의 「저장소와 실명」 에 「보고와 덩어리에는 바깥 주소를 적지 않는다」 를 넣고, **저장소 전체 금지가 아니라는 경계**를 함께 적었습니다 — 안내서(`docs/배포-안내.md`)에는 열 곳이 적혀 있어야 합니다. 가리키던 세 자리의 근거를 그 줄로 맞췄습니다 |
| [고2] 「작업이 잡힌 08-31」 은 잰 값이 아니고 구간이 두 문서에서 다르다 | **괄호를 뺐습니다.** 「03시 파일은 09-08 이 가장 이르다」 로만 두고, **작업이 언제 등록됐는지의 자국은 어디에도 없다**고 적었습니다. 봐둘것 AX-a 도 같은 말로 맞췄습니다 |
| [고3] 「어제치」 가 하루 어긋난다 | **「오늘 새벽치」** 로 고쳤습니다 |
| [고4] 훑기가 짚은 AM-f 를 실제로는 안 고쳤다 | **AM-f 본문에 한 줄 붙였습니다** — 트리거는 AX-c 에서 쟀고, 원인은 여전히 못 찾아 **열어 둔 채**라고 적었습니다 |
| [고5] 12-4 의 경고와 7장의 O 가 서로를 부정한다 | **재서 갈랐습니다** — 터널 주소는 사설 대역으로 풀리지 않습니다. 그러니 집 와이파이에서 열어도 터널을 탑니다. 12-4 의 경고를 **`127.0.0.1` · `192.168.…` 로 열지 말라**로 고치고, 처음 것이 재 보지 않고 적은 것이었다고 그 자리에 남겼습니다 |
| [고6] 못 잰 터널과 안 붙은 터널이 같은 출력이었다 | **갈랐습니다** — `$LASTEXITCODE` 가 0 이 아니면 「**못 쟀습니다**」 로 따로 내고 「안 붙은 것이 아니다」 를 함께 찍습니다 |
| [봐1] 둘째 규칙이 조용히 꺼지는 자리 둘 | **고쳤습니다** — 같은 급 이상의 머리에서만 닫으므로 소제목이 있어도 보고, 수용 기준을 절로 둔 판도 봅니다. `test38_b03` 이 그 둘을 잽니다 |
| [봐2] 실패 문구가 사정을 안 말한다 | **문구에 붙였습니다** — 「다른 문서를 가리킬 생각이면 문서 이름을 앞에 적습니다」 |
| [봐3] `test38_a03` 의 ③ | **걷었습니다.** 그 자리에 왜 걷었는지를 적었습니다 — 글검사가 이미 돌리는 것을 한 번 더 돌리는 값과, **보고를 쓰는 동안 전체 시험이 빨개 코드 회귀와 겉이 같아지는** 값을 견줬습니다. ①②(보는 파일이 하나인가 · 그 말을 담은 얼린 판이 실재하는가)가 남습니다 |
| [봐4] PID 하나로는 「부팅이 띄웠다」 가 안 된다 | **시작 시각을 함께 찍습니다.** 지금 PID 13672 의 시작이 `2026-09-12 14:22:40` 으로 나오고, 마지막 부팅(09-09 20:17)보다 뒤라 **손으로 켠 것임이 그 자리에서 보입니다** — 2-5 가 자국이 없다고 적은 그 자국이 다음 판에는 생깁니다 |
| [봐5] BOM 을 지키는 것이 없다 | **시험을 넣었습니다** — `test6_g05`. 이름을 세지 않고 저장소의 `.ps1` 전부를 보고, BOM 을 떼어 실제로 빨개지는 것까지 확인했습니다 |
| [봐6] 「계정과 비밀번호는 안 적었다」 가 `LocalSystem` 과 부딪혀 보인다 | **좁혔습니다** — 「작업이 어느 계정으로 도는지」 로 |

**검토자가 사람에게 물은 넷 중 둘은 이 판에서 정했습니다** — [고1](규칙을
세우되 안내서는 규칙 밖) · [고5](재서 갈랐습니다). [봐3] 은 값을 견주어
걷었습니다. **AX-a(새벽 3시 백업)만 사람에게 남깁니다** — 운영 설정이고
어느 쪽이 나은지가 사람의 판단입니다.

```
━━━ 검토자 여기부터 ━━━ 범위 d5665b9..작업트리 · 커밋 전 · 파일 10개

읽은 것 — CLAUDE.md(0·2·6-4·10·11-2·11-3·14장) · docs/검토-원칙.md ·
바뀐 파일 전부의 diff · docs/봐둘것.md AM-f/AN-a/AX-a~d ·
docs/배포-안내.md 3·8-3·11·12-4 · 얼린 판 2026-09-12-일하는방식.md.
돌린 것 — 글검사(6/7, check_placeholder 만 빨강) · pytest stage38+docs_shell
(1 failed 16 passed) · check_placeholder 를 앞 판 얼린 판에 직접 걸어 봄.
고친 것 없음 (읽기만).


■ 먼저, 물어보신 넷에 대한 답

1) 되살아나나.ps1 이 재기만 하는가 — 예. 부르는 것이 전부 Get-* 이고
   cloudflared 는 `tunnel info`(읽기), HTTP 는 GET 하나입니다. 쓰는 자리
   없음. 다만 아래 [고6] 하나가 걸립니다.

   11-2 가 두지 말라 한 값을 찍는가 — 안 찍습니다(경로는 Split-Path -Leaf,
   서비스 경로는 `--config` 여부만 bool, 커넥터는 개수와 붙은 때만).
   **그런데 그 「11-2 가 두지 말라 했다」 가 사실이 아닙니다 — [고1].**

2) check_placeholder 둘째 규칙 — 넘치는 쪽은 대체로 잘 좁혔습니다.
   실제로 재 봤습니다: 앞 판 얼린 판(2026-09-12-일하는방식.md)에 그대로
   걸었더니 `[]` 이고, 지금 살아 있는 보고에서는 319줄(사) 12장)을
   집습니다. 네 자리 날짜·대조 표·판정 칸 전부 안 걸립니다.
   구멍 쪽으로 둘 — [봐1][봐2].

3) 잰 것과 안 잰 것의 갈림 — 두 대목 중 **「앱이 부팅으로 스스로 떴다는
   자국이 없다」 는 깨끗합니다**(2-5 · 5장 · 인계 20번이 같은 말). 터널은
   UTC 11:17:40 + 9h = 20:17:40 이 부팅 20:17:04 의 36초 뒤로 맞습니다.
   **「3시 백업이 이레 비었다」 쪽은 안 깨끗합니다 — [고2].**

4) 지시문 대조 표(10장) — 번호를 지시문과 하나씩 맞춰 봤습니다.
   0 · 1-a~e · 2-a~d · 3-a~c · 4-a~c · 5-a~c · 가)~사) **전부 있습니다.**
   빠진 번호 없음. 판정과 본문도 어긋나지 않습니다(5-c 는 작업트리에서
   「다르게 했다」→「했다」 로 바뀌었고, AX-d 가 실제로 서 있으니 맞습니다).
   번호 없는 마무리 제약 한 줄(「운영 자료는 이 판에서 바꾸지 않는다」)만
   표에 없는데, 번호가 없어 막지는 않습니다 — 3장이 11-2 순서(세고·돌리고·
   다시 셈)를 지킨 것으로 답이 돼 있습니다.


■ 막아야 함 — 없습니다

(보고 9·11장의 자리표시자와 12장 없음은 미리 알려 주신 대로이고,
check_placeholder 가 그 셋 + 12장을 이미 집고 있습니다. 채우면 닫힙니다.)


■ 고쳐야 함

[고1] 없는 규칙을 11-2 라고 세 곳에서 가리키고, 지키려는 값은 이미
      저장소에 있습니다.

      scripts/되살아나나.ps1 9~12줄
        "11-2 가 저장소에 두지 않기로 한 값들입니다"
      CLAUDE.md 4044줄 (14장 새 줄)
        "바깥 주소·터널 id·나가는 IP 는 일부러 안 찍는다(11-2)"
      docs/review/최근.md 299줄
        "주소 자체는 여기 적지 않습니다(11-2)"

      CLAUDE.md 11-2 의 「저장소와 실명」 표에 있는 것은 app.db · *.real.json ·
      대응표 · secret_key · vapid · uploads 뿐입니다. 바깥 주소·터널 id·IP 는
      그 장 어디에도 없습니다(11-2 구간을 훑어 확인했습니다).

      그리고 **바깥 주소는 이미 공개 저장소에 평문으로 있습니다** —
      docs/배포-안내.md 145줄과 148줄, 그리고 929줄. 터널 이름도 136·379·
      470줄에 있습니다.

      안 찍는 것 자체는 맞는 판단입니다(출력이 채팅 덩어리로 나갑니다).
      틀린 것은 **근거를 이미 있는 규칙인 것처럼 단 것**입니다. 다음 사람은
      11-2 를 열어 그 줄을 못 찾고, 배포-안내에서 주소를 발견하고, 어느 쪽이
      정본인지 못 정합니다. 둘 중 하나입니다 — 11-2 에 그 줄을 실제로
      더하거나(그러면 배포-안내 145줄을 어떻게 할지도 같이 정해야 합니다),
      아니면 세 자리의 「(11-2)」 를 빼고 「이 출력은 채팅으로 나가므로」
      라고만 적는 것. **사람이 정할 자리라 여기서 정하지 않습니다.**

[고2] 백업이 빈 구간 — 같은 셈이 세 군데에서 다르고, 「못 가른다」 고
      적은 문단이 바로 앞에서 단정합니다.

      docs/review/최근.md 133~135줄
        "그 앞(작업이 잡힌 08-31 부터 09-07 까지)에는 03시 파일이 하나도
         없습니다. 그 이레 동안 …"
      docs/봐둘것.md 2783줄
        "그 앞(09-01~09-07)에는 하나도 없습니다. 그 이레 동안"

      셋이 안 맞습니다 — 08-31~09-07 은 여드레지 이레가 아니고, 봐둘것은
      09-01~09-07(이레)로 적어 두 문서가 다른 구간을 말합니다.

      더 무거운 것은 괄호 안입니다. **「작업이 잡힌 08-31」 은 잰 값이
      아닙니다.** data/backups 의 그 날 파일은 app-20260831-090050.db —
      09:00 에 손으로 뜬 것이고, 작업이 언제 등록됐는지의 자국이 아닙니다.
      바로 다음 문장이 "작업이 그때 아직 없었는지는 이 자료로 못 가릅니다"
      라고 적는데, 괄호가 이미 있었다고 못박고 있습니다. 물어보신 「갈라
      적었는가」 가 여기서 한 번 무너집니다. 괄호를 빼고 「03시 파일은
      09-08 이 가장 이릅니다」 로만 두면 두 문제가 같이 닫힙니다.

[고3] 2-1 표의 「어제치」 — 같은 화면의 출력과 안 맞습니다.

      docs/review/최근.md 62줄 (출력)  마지막실행=2026-09-12 오전 3:00:00
      docs/review/최근.md 74줄 (표)    "어제치가 결과 0 으로 끝났고"

      작성 시각이 2026-09-12 15:40 이므로 그것은 **오늘 새벽치**입니다.
      2-4 와 봐둘것 AX-a 는 "09-12 까지 닷새 연속" 으로 오늘을 세고 있어
      74줄만 혼자 하루 어긋납니다.

[고4] 고치기 전 훑기가 짚은 AM-f 를 실제로는 안 고쳤습니다.

      docs/review/최근.md 32줄
        "봐둘것 AM-f — … 이 판이 재면서 함께 답했습니다(6-3 · AX-c)"
      docs/봐둘것.md 2109줄 — AM-f 항목 본문에는 **아무 줄도 안 붙었습니다.**

      AX-c 는 AM-f 를 가리키는데 AM-f 는 AX-c 를 안 가리킵니다. AM-f 가
      열어 둔 "스케줄러의 트리거를 한 번 볼 것" 은 이 판이 실제로 재어
      답했는데(트리거는 부팅 하나 · 그 밖에 「실패하면 다시 시작」), AM-f 만
      읽는 다음 사람은 그것을 다시 잽니다. 11-3 3단계가 "닫고도 안 적으면
      다음 사람이 열린 줄 알고 다시 봅니다" 로 못박은 그 자리이고,
      검토-원칙 8-2 의 "훑기가 짚은 자리를 실제로 고쳤나" 에 걸립니다.
      AM-f 에 한 줄이면 됩니다 — 원인은 여전히 못 찾았으므로 제목은
      그대로 두고 「트리거는 AX-c 에서 쟀다」 만.

[고5] 12-4 의 경고와 7장의 판정이 서로를 부정합니다. 둘 다 설 수 없습니다.

      docs/배포-안내.md 826줄
        "집 와이파이로 열면 안 됩니다. 그러면 같은 공유기 안이라 터널이
         안 서 있어도 열릴 수 있습니다"
      docs/review/최근.md 286~298줄 (7장)
        같은 노트북에서 터널 주소로 받아 200 · cf-ray · server: cloudflare
        → 수용 기준 다) O

      터널 주소는 Cloudflare 로 해석되므로 같은 공유기 안에서 열어도
      요청은 밖으로 나갔다 돌아옵니다 — 7장의 cf-ray 가 그것을 보인
      값입니다. 공유기 안이라 터널 없이 열리는 것은 `192.168.x.x:8000`
      쪽이지 터널 주소가 아닙니다.

      그러니 826줄이 맞으면 7장의 O 가 근거를 잃고, 7장이 맞으면 826줄이
      사람에게 없는 위험을 경고하는 것입니다. **어느 쪽인지는 이 댁의 DNS·
      공유기 설정을 봐야 알 수 있어 제가 정하지 않습니다.** 다만 「휴대폰
      데이터로 열어라」 는 지시 자체는 어느 쪽이든 남겨 두는 편이 낫고,
      까닭만 「터널 주소가 아니라 내부 IP 로 열면 터널을 안 탄다」 로
      바꾸면 둘이 같이 섭니다.

[고6] 터널을 못 잰 것과 안 붙은 것이 같은 출력으로 나옵니다.

      scripts/되살아나나.ps1 61~66줄
        $글 = (& $cf tunnel info daecheongbu 2>$null | Out-String)
        … "붙은 커넥터: $($붙음.Count)개"
        if ($붙음.Count -eq 0) { "!! 비어 있으면 … 밖에서는 못 들어옵니다" }

      stderr 를 버리고 종료 코드를 안 봅니다. `tunnel info` 는 사용자
      폴더의 cert.pem 이 있어야 도는데(배포-안내 3·8-3), 다른 계정·권한·
      관리자 창에서 돌리거나 cert 가 없으면 **명령이 실패해도 「0개」 로
      나오고 「!! 안 붙었습니다」 가 찍힙니다.** 그러면 사람은 12-4 의
      「안 서면 볼 곳」 표를 따라 멀쩡한 터널을 다시 시작하러 갑니다.

      이 저장소가 4-11 과 12장에서 되풀이해 적은 「구별되지 않는 실패가
      가장 비싸다」 그대로입니다. $LASTEXITCODE 가 0 이 아니면 「못 쟀습니다」
      로 따로 내면 닫힙니다 — 값을 더 찍지 않아도 됩니다.


■ 봐 둘 것

[봐1] check_placeholder 둘째 규칙이 조용히 꺼지는 자리가 둘 있습니다.
      scripts/check_placeholder.py 109~113줄

          if 줄.startswith("#"):
              장머리인가 = bool(장머리.match(줄))
              기준안 = 장머리인가 and 기준머리 in 줄
              continue

      ㄱ. 수용 기준을 `## 8-1. 수용 기준` 처럼 **절**로 두면 장머리가
          아니라 영영 안 봅니다.
      ㄴ. 수용 기준 **장 안에 소제목을 하나라도 두면** 그 아래 표가
          안 보입니다(어떤 `#` 줄이든 기준안을 끕니다).

      지금 보고는 `# 8. 수용 기준 …` 한 장에 표 하나라 안 밟습니다.
      11-3 이 보고의 장 구성을 못박아 두지는 않았으므로, 다음 판이
      모양을 조금만 바꾸면 검사가 말없이 아무것도 안 봅니다 —
      검토-원칙 7 의 「…할 때만」 꼴입니다. 독스트링 48~61줄이 경계를
      셋 적었는데 이 둘은 없습니다. 적어 두기만 해도 값이 있습니다.

      (표가 아니라 목록으로 적은 수용 기준도 안 봅니다 — 같은 자리.)

[봐2] 칸 맨 앞에 CLAUDE.md 의 장·절을 적으면 오탐입니다.
      scripts/check_placeholder.py 95줄 `칸앞`

      `4-11 이 정한 그대로` · `12-4 의 표` 처럼 쓰면 "보고에 그 절이
      없습니다" 가 뜹니다. 독스트링 57~61줄이 경계로 적어 두었고
      「문서 이름을 앞에 적으라」 는 해법도 옳습니다. 다만 **실패 문구가
      그 사정을 말해 주지 않아서**, 처음 밟는 사람은 자기 보고에 그 절을
      만들려고 합니다. 문구에 「다른 문서를 가리킬 생각이면 문서 이름을
      앞에」 한 조각만 붙이면 됩니다(가리킨자리() 79~85줄).

[봐3] test38_a03 ③ — 물어보신 자리입니다. **설계로는 성립하지만 값이
      겹칩니다.** tests/test_stage38.py 132줄

      「나」 가 재려는 것은 "얼린 판을 안 본다" 인데, 그것은 ①(보는 파일이
      최근.md 하나인가)이 이미 재고 ②(그 말을 담은 얼린 판이 실재하는가)가
      받칩니다. ③ 은 그것을 안 재고 **check_placeholder 를 pytest 에서
      한 번 더 돌리는 것**입니다. 그 검사는 이미 글검사에 들어 있고
      test38_a04 가 들어 있음을 지킵니다.

      값이 없지는 않습니다 — pytest 를 더 자주 돌리므로 한 겹 더입니다.
      값을 치르는 자리는 둘입니다. ㄱ) 보고를 쓰는 동안 전체 시험이
      빨갛고, 그 빨강이 코드 회귀와 겉이 같습니다. ㄴ) 11-3 4단계가
      허락한 「막혀서 멈춤」 판은 보고가 덜 쓰인 채로 끝나는데, 그 판은
      정의상 pytest 를 초록으로 낼 수 없습니다.

      **사람이 정할 것으로 남깁니다.** 남긴다면 11-3 이 「보고를 다 채운
      뒤에 시험을 다시 돌린다」 고 이미 적어 둔 그 줄 옆에 "그전까지는
      이 한 줄이 빨간 것이 정상" 을 붙여 두는 편이 낫습니다 — 안 적으면
      다음 사람이 빨간 것을 보고 코드를 뒤집니다.

[봐4] 「섰다」 의 뜻이 PID 하나인데, 이 판이 같이 적은 AX-c 가 그것을
      부정합니다.
      docs/봐둘것.md 2826~2828줄
        "「PID 가 바뀌었다」 가 재시작의 증거가 아니게 되는 경우 — 는 이
         설정 때문에 실제로 있을 수 있습니다. … PID 만 보지 말고 그 자리의
         시각도 함께 봅니다"
      docs/review/최근.md 203줄 · docs/배포-안내.md 12-4 표
        "포트를 쥔 PID 가 내리기 전과 다르다"

      「실패하면 다시 시작」 이 켜져 있으므로 재부팅 없이도 PID 는
      바뀔 수 있습니다. 되살아나나.ps1 이 프로세스 **시작 시각**을 한 줄
      더 찍으면(74~76줄에서 이미 PID 를 쥐고 있습니다) 그것이 마지막
      부팅 시각 뒤인지로 갈립니다 — 2-5 가 "앱이 부팅으로 스스로 떴다는
      자국이 없다" 고 적은 바로 그 자국이 다음 판에는 생깁니다.

[봐5] .ps1 의 BOM 을 지키는 것이 아무것도 없습니다.
      CLAUDE.md 4044줄이 "BOM 을 붙여 저장한다 … 없으면 파싱부터 실패한다"
      고 적었고 실제로 붙어 있는 것을 확인했습니다(EF BB BF).
      그런데 devserve.bat 은 tests/test_stage6.py 367줄이 cp949 로 읽어
      암묵적으로 지키는 반면, 이 파일에는 그런 줄이 없습니다. 편집기가
      한 번 UTF-8 로 다시 저장하면 조용히 깨집니다. 한 줄이면 닫힙니다.

[봐6] 대조 표 1-a 의 "계정과 비밀번호는 안 적었습니다" 와 2-2 의
      `계정=LocalSystem` 이 겉으로 부딪혀 보입니다.
      docs/review/최근.md 84줄 · 348줄
      1-a 는 작업 스케줄러 쪽 지시였고 스크립트는 작업의 Principal 을
      안 찍으므로 실제로는 맞습니다. LocalSystem 은 붙박이 이름이라
      샌 것도 아니고, **터널이 로그인 전에 서는 까닭 자체**라 값이
      있습니다. 다만 348줄을 「작업 쪽 계정은 안 적었습니다」 로 좁히면
      읽는 사람이 두 번 안 멈춥니다.


■ 아니라고 확인한 것 (억지로 안 적으려고, 본 것만)

- 사람칸 — 보고 1~5줄에 다섯 줄이 그 순서로 있고 네 값이 다 찼습니다.
  「없음」 을 쓴 자리도 값입니다. check_handoff 5/5. ✓
- 막는 코드의 세 겹 — b01 이 ①②, b02 가 ③("그 줄이 실제로 그 판에
  있는가" → 없으면 아무것도 안 보는 시험이라고 못박음), b03 이 넘침 쪽 +
  "그러고도 진짜는 여전히 잡는가". 검토-원칙 2 를 세 겹 다 채웠습니다. ✓
- 문서의 값이 시험에 박혔는가 — `자리표시자` 를 검사의 정규식에서
  지어내고(26줄), 기준판은 합성 글입니다. ✓
- 실명·연락처·계좌·토큰 — 바뀐 열 파일에서 못 찾았습니다.
  PID·파일 이름·UTC 시각뿐입니다. check_names 통과. ✓
  (터널 이름이 스크립트에 새로 박혔지만 배포-안내 136·379·470줄에
   이미 있어 새로 새는 값은 아닙니다 — [고1] 에 함께 둡니다.)
- 옛말·이름 넘김 — 새 두 줄(.ps1 · StartWhenAvailable)에 까닭이 있고
  이유가 넘기는 말을 다시 인용하지 않습니다. check_named 671개 · 통과. ✓
- AX-d 의 「정함」 꼴 — AN-a(봐둘것 2837줄 언저리)와 같은 모양이라
  기존 관례를 따릅니다. 6-4 를 가리키기만 하는 것도 맞습니다. ✓
- 얼린 판에 새 규칙을 걸어 봤습니다 — 2026-09-12-일하는방식.md 는
  `[]` 입니다. 넘치는 쪽 오탐이 실제 판에서는 안 납니다. ✓


■ 사람에게 묻는 것

1. [고1] — 「바깥 주소·터널 id·나가는 IP 를 저장소에 두지 않는다」 를
   11-2 에 **규칙으로 세울지**, 아니면 세 자리의 「(11-2)」 를 빼고
   그냥 습관으로 둘지. 세운다면 docs/배포-안내.md 145·148줄의 주소와
   136줄의 터널 이름을 어떻게 할지가 같이 따라옵니다.

2. [고5] — 집 와이파이에서 터널 주소를 열면 터널을 타는지 안 타는지.
   이것이 정해져야 12-4 의 경고와 7장의 O 중 어느 쪽을 고칠지 갈립니다.

3. [봐3] — 보고를 다 채우기 전까지 pytest 가 한 줄 빨간 것을 받아들일지
   (test38_a03 의 ③ 을 남길지). 남긴다면 11-3 에 「그게 정상」 을
   한 줄 적어 두는 것까지.

4. 봐둘것 AX-a 의 물음(새벽 3시에 꺼져 있으면 그 날 백업이 없는 것)은
   이 판이 사람칸 「정할 것」 으로 올린 그대로입니다 — 제가 정하지
   않습니다.

━━━ 검토자 여기까지 ━━━
```

---
