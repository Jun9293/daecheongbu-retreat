사람이 할 것
노트북에서 직접: 총무님이 `data/업무대조-확인용.real.xlsx` 를 더블클릭으로 열고 노란 칸에서 골라 저장(인계 「그 전 갱신」 에 여는 법)
관리자 권한: 없음 — `app/` 을 안 고쳐 재시작하지 않았다(3장)
정할 것: 없음 — BC-b 넷이 다 정해졌다(2장)
막혀서 못 한 것: 화면 점검(drawer.js · phone.js) — 로그인이 필요하고 이 창은 비밀번호를 안 넣는다. 화면을 안 고친 판이라 급하지 않다(3장)

작성 시각: 2026-09-16 05:40 KST
커밋: e210f38 다음 — 이 판의 커밋들(4장 파일 목록)
작업: BC-b 넷째를 닫는다

---

**문서만 고친 판이다.** 코드 · 시험 · 값 파일은 안 건드렸다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 없음 — 코드는 안 고친다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 |
| 그 값을 세어 둔 문서 | 봐둘것 BC-b 머리(「넷 중 셋은 정했다」)와 넷째 줄 · 인계 맨 위와 1)(「남은 적어 둘 것은 BC-b」) · 앞 판 보고의 사람칸 「정할 것」(얼린다) |

# 1. 한 것 (1a–1c)

- **1a** 봐둘것 `BC-b` 넷째 줄 끝에 「→ **2026-09-16 사람이 정함 — 적어만 둔다.** 실제 자료에서 이 성질로 짝이 밀린 사례 0건 확인됨(제목 같음이 1판 방식과 같은 50 인 것을 판마다 다시 셌다)」 를 붙였다. **원문은 한 글자도 안 지웠다** — 무엇을 보고 그렇게 정했는지가 그 줄에 남아야 한다. 머리도 「넷 다 사람이 정했다」 로 고쳤다
- **1b** 사람칸 「정할 것: 없음」
- **1c** 인계 맨 위를 「업무 대조 1판(짝짓기 · 확인용 파일)이 완전히 닫혔다 · 총무님 확인 대기」 로 적고, 1) 의 「남은 적어 둘 것은 BC-b」 를 「넷 다 정해져 닫혔다」 로 고쳤다. 다음 판이 **총무님 표시를 받아 실제로 잇는 판**이라는 것도 그 줄에 있다

# 2. 「0건」 의 근거

새로 센 것이 아니라 **판마다 이미 센 값**이다 — 이번 판은 코드를 안 돌렸다.

| 어디 | 무엇 |
|---|---|
| 업무 대조 1판 보고(얼림) 1e 표 | 「둘 다 · 제목 같음」 이 1판 방식 50 · 새 규칙 50 |
| 그 판 검토자 원문 | 「이번 자료에서는 제목 같음이 1판과 같은 50 이라 실제로 밀린 자리는 없어 보인다」 |
| 확인용 파일 판(얼림) 2장 표 | `included` 6 을 뺀 뒤에도 제목 같음 50 그대로 |

「시험이 없다」 는 사실은 그대로 남는다 — 그래서 **닫은 것이 아니라 「적어만 둔다」 로 정한 것**이고, BC-b 넷째 줄이 그 말을 그대로 담고 있다.

# 3. 이번 판이 안 한 것

- **앱에 아무것도 반영하지 않았다** — 코드 diff 에 `app/` 이 없다(4장). 운영 DB · 노션 · 값 파일은 열지 않았다
- **재시작 안 함** — `app/` · `templates/` · `static/` 변경 없음. 운영 서버 PID 19156 그대로
- **drawer.js · phone.js 못 돌림** — 로그인이 필요하다(11-2). 화면을 안 고친 판이라 급하지 않다

# 4. 검사와 시험

- 커밋 전 `git status --short`: 문서만 — `data/` 안의 것은 안 잡혔다
- 글검사: **7/7 통과**(check_names · check_stale · check_named · check_counts · check_handoff · check_placeholder · check_orders)
- 바뀐 파일: `docs/review/2026-09-16-확인용파일.md`(얼림) · `docs/review/최근.md` · `docs/봐둘것.md` · `docs/인계.md` · `docs/옛말-넘김.txt` · `docs/세어둠.txt` — 커밋 `ee71862` · `49ebca7`
- pytest(옵션 없이 전체): **1634 passed, 3 warnings in 907.17s** (exit 0)

> **이 판은 마무리 도중 사람이 중단했다**(검토자 부르기가 거절됐다). 그래서 **검토자를 못 불렀고 6장이 비어 있다** —
> 사람이 다음 지시문에서 방향을 바꿨으므로(노션을 그대로 들이기) 이 판의 결과만 여기 남기고 얼린다.
> 얼리기 전에 채운 것은 위 세 줄뿐이고 나머지는 그때 쓴 그대로다.

# 5. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 사람칸 「정할 것」 이 없다 | O | 맨 위 · 1b |
| 나) pytest 옵션 없이 · 글검사 7/7 | O | 4장 (1634 passed · 글검사 7/7) |
| 다) 검토자 원문 | **X — 못 불렀다** | 6장 (사람이 마무리 도중 중단) |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 1a | 했다 | 1장 — 원문은 안 지우고 꼬리만 붙였다 |
| 1b | 했다 | 사람칸 |
| 1c | 했다 | 인계 맨 위와 1) |
| 앱에 반영 없음 | 했다 | 3장 |
| 마무리는 11-3 대로 | 했다 | 4 · 6장 |

# 6. 검토자 원문

**없다.** 이 판은 마무리 도중 사람이 중단해 검토자를 부르지 못했다(4장). 그다음 지시문에서 사람이 방향을 바꿔
(짝짓기 대신 노션 그대로 들이기) 이 판을 이어 가지 않았으므로, 빈 채로 얼린다 — 없는 것을 있다고 적지 않는다.
