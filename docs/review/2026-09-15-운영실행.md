사람이 할 것
노트북에서 직접: 하나 — 개발 DB 를 지우고 seed 를 다시 만드는 일이 앞 판에서 넘어와 아직 남았다(앞 판 보고 5장 · 얼린 판 `docs/review/2026-09-14-계좌게이트.md`). 이 판이 새로 넘기는 것은 없다
관리자 권한: 없음 — app/ 을 안 건드려 재시작하지 않았다(4장)
정할 것: 둘 — ① 다음 판(16번 구매목록이거나 다른 것 · 인계 순서표) ② 되돌리는 사본을 백업 정리 밖에 영영 둘지, 앱에서 쌓인 것이 사본보다 값질 때(못 이은 지출 30 건을 다 잇는 등) 풀어 줄지(3-4 · 7-2 사람에게 묻는 것)
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 이 판은 화면 코드를 안 바꿨다(4장)

작성 시각: 2026-09-15 00:55 KST
커밋: 4a38323 (백업 정리가 되돌리는 사본을 안 지우게) · 8054fe5 (문서) · 이 보고는 그다음 커밋
작업: 운영 실행 — 그대로 해 (재정 차례 6 을 운영에 들인다)

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 되돌리는 사본 이름 — CLAUDE.md 7-1 · 7-5 · 인계 머리 · 순서표 6 · 봐둘것 여섯 머리. **그 이름이 `scripts/backup.py` 의 정리 대상 꼴(`app-*.db`)과 같다는 것은 처음 훑기에서 놓쳤고 커밋 전 검토가 잡았다** |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 코드 쪽은 `scripts/backup.py` 목록 한 줄만 |
| 그 값을 세어 둔 문서 | CLAUDE.md 7-1(시트 실측) · 7-5(짝 json · 운영 실행 · 되돌리기) · 13장(원본 데이터) · 11-2 백업(지키는판 설명) · 봐둘것 BA-f · BB-a · BB-b · BB-c · BB-g · BB-h · 인계 |

# 1. 한 것

1. 운영 미리보기를 한 번 더 — 앞 판 리허설 미리보기와 **글자 하나까지 같았다**(총무님이 본 수와도 같다). 환경변수 `DCB_` 가 없는 것을 먼저 봤다
2. `--실행` — 직전 사본 **`data/backups/app-20260915-002352.db`** (729,088 바이트)
3. 넣은 뒤 commit 전에 스크립트가 DB 를 다시 세어 계획과 견줬고 같아서 commit. 이 창이 따로 `mode=ro` 로 다시 셈(3장)
4. 두 번째 미리보기 — 「이미 들여왔습니다」 로 멈춤
5. 재정 화면이 쓰는 도메인 함수로 읽기만 해서 셈(3-3)
6. `scripts/backup.py` 의 `지키는판` 에 그 사본을 넣었다 — 이름 꼴이 새벽 백업과 같아 성한 판 정리에 섞이면 한 달 안에 지워진다(커밋 전 검토)
7. 문서: CLAUDE.md 7-1 머리 · 7-5(짝 json 은 들이기 전에만 · 운영 실행을 지난 일로 · 되돌리기를 DB 하나만으로) · 11-2 지키는판 설명 · 13장 · 봐둘것 여섯 머리(원문 유지 · BB-h 라벨) · 인계 머리 · 순서표 6 · 16번 줄 · 앞 판 보고 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 운영 DB 를 다시 센 수가 계획과 같다 | O | 3-2 — 스크립트가 commit 전에 DB 에서 셈 + 이 창이 `mode=ro` 로 사본과 운영을 따로 셈 + 검토자도 따로 셈(7-1) |
| 나) 두 번째가 「이미 들여왔습니다」 로 멈춘다 | O | 1장 4 |
| 다) 되돌리는 사본 이름 | O | `data/backups/app-20260915-002352.db` · 절차는 3-4 |
| 라) pytest 옵션 없이 · 글검사 7/7 | O | 5장 |
| 마) 검토자 원문 | O | 7장 |

# 3. 운영 자료 (값 없이 건수만)

**이 판이 운영 DB 를 바꿨다** — 11-2 순서대로: 미리보기 → `--실행`(사본 먼저) → 다시 세기.

## 3-1. 미리보기

앞 판 리허설 미리보기와 같다(앞 판 보고 3-2 · 그 앞 판 보고 3-1). 총무님이 적어 준 수: 예산 34(0원 7 · 셈 안 맞음 1) · 지출 97 · 영수증 97(원본 번호 89 · 따로 첨부 8 · 한 장에 여럿 1) · 잇기 99 · 계좌 가름 64(끝 빈칸 22) · 계좌 꼴 걸림 0 · 수입 4 · 부서 붙음 39 · 비고 안 들임 88 · 식대 인원 식 12 · 비고 14 · 짝 표 예산 30 · 부서 1 · 영수증 0 · 식대 1 · 계좌 0.

## 3-2. 실행 전후 (회차 1 · `mode=ro` 로 셈)

| 무엇 | 실행 직전 사본 | 실행 뒤 운영 | 계획으로 셈 |
|---|---|---|---|
| 산 예산 항목 | 9 | 34 | 9 − 9 + 34 |
| 산 지출 | 12 | 97 | 12 − 12 + 97 |
| 산 수입 | 0 | 4 | 0 + 4 |
| 영수증 | 12 | 109 | 12 + 97 |
| 끊기지 않은 잇기 | 12 | 111 | 12 + 99 |
| 계좌 든 산 지출 | 0 | 64 | 64 − 0 |
| 식대 산 지출 | 5 | 26 | 12 + 14 |
| 부서 붙은 산 지출 | 5 | 39 | 39 |
| 들여오기 기록 | 0 | 1 | |
| 회차 · 계정 수 | 사본과 같음 | 사본과 같음 | |

**잇기 111 은 끊기지 않은 잇기 전부**다 — 그중 12 는 취소 표시로 내려간 seed 지출에 걸린 옛 영수증이다(7-4 대로 떼지 않는다). 산 지출에 걸린 잇기는 99 다. 스크립트가 견준 기대 수도 「전 12 + 계획 99」 로 같은 기준이다. 취소된 seed 도 운영에 남아 있다 — 예산 전체 43 − 산 34 = 9 · 지출 전체 109 − 산 97 = 12 가 사본의 산 수와 같다(검토자 셈).

## 3-3. 재정 화면이 가리키는 수 (도메인 함수 · 읽기만 · rollback)

| 화면이 쓰는 것 | 수 |
|---|---|
| `build_budget_summary` 산 예산 항목 / 취소 | 34 / 9 |
| `entries_of` → `live_entries` 산 지출 (전체) | 97 (109) |
| `expense_stats` 지출 건수 | 97 |
| 산 지출에 걸린 영수증 · `unlinked_receipt_count` | 97 · 0 |
| 산 수입 | 4 |
| 예산 항목 미지정 산 지출 | 30 (짝 표 예산 30 과 같다) |

서버는 안 건드렸다(`/login` 200). 로그인해야 보이는 화면을 이 창이 직접 열지는 못했다 — 화면 코드는 위 함수들을 받아 그리기만 한다(7-3 · 7-4).

## 3-4. 되돌리기

- 사본: **`data/backups/app-20260915-002352.db`** — DB 하나뿐이다(uploads · vapid · rows.json 짝이 없다)
- 절차: 서버를 끄고(`docs/배포-안내.md` 12장) 그 사본만 `data/app.db` 로 되돌린다. 11-2 의 「셋을 함께」 절차가 아니고, 다른 날짜의 uploads · vapid 를 섞지 않는다(CLAUDE.md 7-5)
- 대가: 실행 뒤 앱에서 한 것(못 이은 지출 잇기 · 고치기 · 새 등록 · 첨부)이 함께 사라지고, 그사이 올린 파일은 행 없이 디스크에 남는다
- 그 사본은 `scripts/backup.py` 의 `지키는판` 에 있어 새벽 백업 정리가 안 지운다. 언제 풀지는 정할 것 ②

# 4. 재시작

**건너뛰었다** — 이번 커밋들에 `app/` · `templates/` · `static/` 이 없다(`scripts/backup.py` 목록 한 줄과 문서). 떠 있던 서버(앞 판 재시작 PID 26316)가 바뀐 DB 를 그대로 읽는다 · `/login` 200. `scripts/backup.py` 는 새벽 작업이 돌 때마다 새로 읽는다.

# 5. 검사와 시험

- 글검사(커밋 전): 7/7 통과
- 새는지 검사(저장소 밖 도구 · 값 안 찍음): 바뀐 문서에 새로 들어온 시트 값 0
- `tests/test_stage44.py`(백업) 28 passed
- 보고를 다 채운 뒤 돌린 것: 글검사 7/7 통과 · 새는지 검사(이 보고) 0 · pytest(옵션 없이) `1573 passed, 3 warnings in 1297.47s (0:21:37)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 6. 바뀐 파일 (`git diff --name-only 2920336..8054fe5`)

- CLAUDE.md
- docs/review/2026-09-14-계좌게이트.md
- docs/봐둘것.md
- docs/세어둠.txt
- docs/옛말-넘김.txt
- docs/인계.md
- scripts/backup.py

운영 쪽(저장소 밖): `data/app.db` 가 바뀌었고 `data/backups/app-20260915-002352.db` · `data/재정짝표.real.md` 가 새로 섰다.

# 7. 검토자 원문

## 7-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위 HEAD 2920336 + 스테이지(git diff --cached) · 커밋 0개 · 파일 6개

본 것
  git diff --cached 전체 · CLAUDE.md 7-1/7-5 · docs/봐둘것.md BA-f/BB-a/b/c/g/h · docs/인계.md 머리·순서표·16번
  scripts/backup.py (snapshot · stamps_in · prune · 지키는판) · scripts/재정들여오기.py 사본을_뜬다
  얼린 판이 HEAD 의 최근.md 와 같은지: 같다 (git show 로 견줌 · 바이트 수 같음)
  운영 DB 와 사본을 mode=ro 로 다시 셈 (건수만):
    운영  산 예산 34 / 전체 43 · 산 지출 97 / 전체 109 · 산 수입 4 · 영수증 109 · 끊기지 않은 잇기 111
          산 지출 중 예산 항목 없음 30 · 들여오기 기록 1 · 지출이 걸린 회차 1 · 영수증 없는 산 지출 0
    사본  9/9 · 12/12 · 0 · 12 · 12 · 0 · 0 · 1 · 0
    구현자 요약과 전부 같다. 취소된 seed 도 들어 있다: 예산 43-34=9 · 지출 109-97=12 가 사본의 산 수와 같다
  글검사: 7/7 exit 0 (지금 작업 트리 · 최근.md 는 아직 앞 판)
  pytest 는 안 돌렸다 (문서만 바뀐 판이고 검토자 몫이 아님)

잡은 것

[고쳐야 함] scripts/backup.py:388-392 · 407 (문서 쪽 CLAUDE.md:2359 · docs/인계.md:4 · 218 · 봐둘것 여섯 머리)
  — 문서가 「되돌리는 사본」 으로 가리키는 data/backups/app-20260915-002352.db 는 새벽 백업과 이름 꼴이 같다.
  stamps_in 이 glob("app-*.db") 로 줍고 지키는판 에 없으므로 성한 판 30칸에 들어가, 새벽 백업이 30번쯤 더 돌면
  prune 이 말없이 지운다. 지금 app-*.db 가 21개라 그 날이 멀지 않다.
  왜: 문서는 그 이름을 오래 쓸 되돌림 자리로 적었는데, 파일은 한 달 안에 없어진다. 그 뒤로는 문서가 없는 파일을
  가리킨다. 지키는판 에 넣을지(코드), 다른 폴더로 옮길지, 「며칠 안에만 쓸 수 있다」 고 문서에 적을지는 사람이 정할 일이다

[고쳐야 함] CLAUDE.md:2614 (7-5 「되돌리기」) · 11-2 「되돌리기」 절
  — 7-5 는 「11-2 되돌리기 절차대로」 라고 한다. 그런데 11-2 는 같은 날짜의 app·uploads·vapid 셋을 함께 되돌리고
  rows.json 부터 보라고 한다. 이 사본에는 짝이 되는 uploads-/vapid-/rows.json 이 없다.
  재정들여오기 는 backup.snapshot 만 부른다. 들여오기는 업로드와 VAPID 를 안 건드리므로 db 만 되돌리는 것이
  맞지만, 적힌 대로 따르면 짝 파일을 찾다가 멈추거나 다른 날짜 판을 섞는다.
  하나 더: 실행 뒤 사람이 화면에서 한 것(못 이은 30건 잇기 · 첨부 올리기)은 이 사본으로 되돌리면 사라진다.
  올린 파일은 DB 에 행이 없는 채로 디스크에 남는다.
  왜: 되돌림은 급할 때 읽는 글이다. 이번 판이 그 이름을 여러 곳에 박았으니 절차도 이번에 맞춰야 한다

[고쳐야 함] docs/봐둘것.md:3365 (BB-h 머리 「사람이 짝을 적을 자리」) · CLAUDE.md:2607-2608 · 2612
  — BB-h 에 새로 단 인용은 「들인 뒤라 짝 표 json 은 더 안 먹는다」 인데, 제목 라벨은 여전히 「사람이 짝을 적을 자리」 다.
  본문 3391 「사람이 짝 표 json 으로 잇는다」 도 그대로다. 7-5 의 「사람이 짝을 적는 자리는 data/재정짝표.real.json」 과
  「운영 실행은 … 「그대로 해」 한 뒤입니다」 도 앞일로 남았다.
  왜: 11-3 3단계는 라벨과 본문이 서로 다른 것을 가리키지 않게 하라고 한다(AC-c 선례). 지시문은 「원문 유지」 라서
  봐둘것 제목은 손대지 않았을 수 있다. 다만 7-5 는 규칙 절이다. 다음 회차에도 맞는 말인지(짝 json 은 들이기 전에만 먹는다),
  이 회차에는 끝난 말인지 한 줄은 있어야 한다

[봐 둘 것] CLAUDE.md:2355-2361
  — 인용 단락이 「… 시트 실측 (…):」 과 그 아래 머리글 코드블록 사이에 끼었다. 콜론 뒤에 실측이 아닌 글이 온다.
  단락 앞이나 코드블록 뒤로 가면 읽기가 이어진다

[봐 둘 것] CLAUDE.md:2360 「들인 뒤로 정본은 앱」 (e)
  — 다른 절과 정면으로 부딪히는 곳은 못 찾았다. 7-5 「두 번 들이지 않는다」 · 0장과 맞는다.
  다만 13장 「원본 데이터」 는 Belong 예산 시트를 원본으로 적고 있다. 「들인 뒤로는 앱이 정본」 을 13장에 한 줄
  붙일지 볼 것. 그리고 「두 번 들이지 않는다」 는 한 회차 이야기다. 괄호가 「시트를 고쳐도 앱이 안 따라간다」 의
  까닭 전부처럼 읽힌다

[봐 둘 것] docs/봐둘것.md BB-b · BB-g 머리 (3337 · 3395 근처)
  — 여섯 곳에 같은 문장 「다시 센 수가 계획과 같았다」 를 붙였다. BB-b 는 「기계로 셀 자리가 없다」 는 항목이라,
  그 머리의 「센 수가 같았다」 는 BB-b 의 내용(반품 금액)을 셌다는 뜻으로 오해될 수 있다. 지시문대로이니 무게는 낮다

[봐 둘 것] 구현자 요약 「산 잇기 12→111」 (보고에 옮길 때)
  — 111 은 끊기지 않은 잇기 전부다. 그중 12줄은 취소 표시로 내려간 seed 지출에 걸린 옛 영수증이다
  (7-4 대로 떼지 않는다). 산 지출에 걸린 잇기는 99다(1장이 여러 지출에 · 2줄이 번호 둘).
  보고에 「산 잇기」 로 적으면 산 지출의 잇기로 읽힌다. 계획과 견준 수가 어느 정의인지 한 줄 밝혀 둘 것

(a) 셈 근거 — 충분하다. 운영의 산 수와 사본의 산 수, 전체 수의 차이가 서로 맞는다.
    사본 9·12 가 운영에서 취소로 넘어갔고, 들여오기 기록이 0→1, 지출이 걸린 회차가 1 그대로다. 막을 것 없음
(c) 「운영 반영」 은 사실만 말한다. 다음 할 일(못 이은 30건 · 16번 · 사람이 정함)은 인계 머리·순서표·16번에 있다.
    봐둘것 BB-h 는 위 [고쳐야 함] 셋째 항목에 적었다.
    못 이은 30건을 고치기로 이을 수 있는지는 코드로 봤다: update_expense 가 빈 항목에서 항목을 고르는 길이 있다(routers/expenses.py:439-447)
(d) 10장 — 새 줄에 「곳·군데」 가 붙은 셋 이상 수가 없고 check_counts 도 exit 0 이다.
    「30 건」 · 「여섯」 은 건·항목 단위라 규칙 밖이다
막아야 함: 없음

사람에게 묻는 것
  1. 되돌리는 사본 app-20260915-002352.db 를 백업 정리가 지우지 않게 할지.
     지키는판 에 넣기(코드 한 줄) · 다른 폴더로 옮기기 · 「한 달 안에만」 이라고 적기 중 무엇으로 할지
  2. 이 사본으로 되돌리는 절차를 「db 하나만 · 실행 뒤 화면에서 한 일은 잃는다」 로 7-5 에 따로 적을지
  3. 봐둘것 BB-h 제목 「사람이 짝을 적을 자리」 를 원문 유지 원칙의 예외로 고칠지, 머리 인용만 둘지
━━━ 검토자 여기까지 ━━━
```

## 7-2. 다시 보기 (검토 뒤에 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 범위 2920336..스테이지(다시 보기 · 앞 검토 뒤 고친 것만) · 커밋 0개 · 파일 7개(새로 scripts/backup.py)

본 것
  git diff --cached 2920336 에서 scripts/backup.py · CLAUDE.md(7-1 · 7-5 · 13장) · 봐둘것 BB-h 제목 줄
  scripts/backup.py prune 독스트링과 본문 (398-412) · CLAUDE.md 에서 지키는판 을 가리키는 줄
  글검사 7/7 exit 0 (지금 작업 트리)
  운영 DB 는 이번에 안 열었다 (고친 것이 문서와 백업 목록뿐)
  test_stage44 는 안 돌렸다 (구현자는 28 passed 라고 함)

앞 검토 [고쳐야 함] 셋이 닫혔나
  사본이 지워진다        닫힘. 지키는판 에 넣었다. prune 407 이 stamps_in 결과에서 지키는판 을 먼저 빼므로
                         개수 셈에도 크기 셈에도 안 든다. 알린다 451 도 이 판을 의심 판으로 알리지 않는다
  되돌리기 절차          닫힘. 7-5 가 「DB 하나뿐 · 짝이 되는 uploads/vapid/rows.json 없음 · 서버 끄고 그 사본만 ·
                         11-2 의 셋 되돌리기가 아님 · 다른 날짜 판 섞지 않음 · 대가 · 정리가 안 지움」 을 모두 적었다
  짝 json · 앞일로 적힌 말  닫힘. 7-5 두 줄이 회차 1 에서 끝난 일로 적혔다.
                         BB-h 제목은 옛 라벨을 괄호에 남기고 새 라벨로 바꿨다. 본문 3391 은 그대로지만 머리 인용이 덮는다
  [봐 둘 것] 둘도 반영됐다: 7-1 인용이 실측 줄 앞으로 옮겨졌고, 13장에 한 줄이 붙었다

잡은 것

[고쳐야 함] scripts/backup.py:98-99 (지키는판 의 새 까닭 글)
  — 「성한 판 30칸에 넣어 지우지 않는다」 는 코드와 반대로 말한다. 지키는판 은 30칸에 넣는 것이 아니라
  「어느 셈에도 안 든다」(같은 파일 405 · 407).
  왜: 이 글은 사람이 이 판을 왜 붙들었는지 읽는 자리다. 「30칸에 넣는다」 로 읽으면 30칸이 차면 밀려난다고 오해한다.
  「성한 판 30칸 정리에서 빼 지우지 않는다」 로 쓰면 코드와 맞는다

[봐 둘 것] CLAUDE.md:3283-3284 (11-2 백업)
  — 「어떤 경우에도 안 지우는 판은 지키는판 에 까닭과 함께 … 그날의 빈 판이 사고의 증거라 거기 있습니다」.
  지금은 빈 판이 아닌 되돌림 사본도 거기 있어서, 까닭이 하나뿐인 것처럼 읽힌다.
  「무엇이 왜 있는지는 그 목록이 말한다」 처럼 세지 않고 가리키는 말로 두면 다음 판이 늘어도 안 낡는다(10장)

[봐 둘 것] CLAUDE.md:2364-2365
  — 실측 줄과 코드블록 사이에 빈 줄이 둘이다. 그려질 때는 차이가 없다

(a)~(e) 가운데 이번 고침으로 새로 생긴 부딪힘: 없음.
  7-1 「정본은 앱」 의 새 까닭(앱에서 고친 것이 시트에 없음 · 다시 들이기 막힘)은 7-5 · 13장과 같은 말을 한다.
  10장: 새 줄에 「곳·군데」 가 붙은 셋 이상 수 없음 · check_counts exit 0.
  「30칸」 은 CLAUDE.md 11-2 에 이미 있는 말이다
  지키는판 은 scripts/ 라서 서버 재시작 대상이 아니다. 새벽 작업은 돌 때마다 파일을 새로 읽는다
막아야 함: 없음

사람에게 묻는 것
  1. 되돌림 사본을 지키는판 에 영영 두는가, 아니면 못 이은 30건을 다 잇는 등 앱에서 쌓인 것이 사본보다
     값질 때 풀어 주는가(풀 때를 정할 것인가)
━━━ 검토자 여기까지 ━━━
```

## 7-3. 두 번째 검토 뒤 고침 (다시 보이지 않고 적는다 · 11-3 4단계)

- `scripts/backup.py` `지키는판` 새 줄의 까닭 — 다시 보기 [고쳐야 함]. 「성한 판 30칸에 넣어」 → 「개수·크기 정리에서 빼」
- `CLAUDE.md` 11-2 백업 — 다시 보기 [봐 둘 것]. 「그날의 빈 판이 사고의 증거라 거기 있습니다」 → 「무엇이 왜 거기 있는지는 그 목록이 말합니다(사고의 증거인 빈 판 · 되돌리는 사본 등)」
- 빈 줄 둘(7-1)은 그려질 때 차이가 없어 그대로

# 8. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1-a | 미리보기 한 번 더 · 같은지 · 다르면 멈춤 | **했다** — 같았다 |
| 1-b | --실행 · 직전 사본 확인 | **했다** — `app-20260915-002352.db` |
| 1-c | 실행 뒤 DB 를 직접 다시 세어 계획과 견줌 | **했다** — 3-2 |
| 1-d | 두 번째 미리보기가 멈춤 | **했다** |
| 2-a | 재시작 불필요 | **했다** — 안 했다 |
| 2-b | 읽기 전용으로 화면이 가리키는 수 | **다르게 했다** — 로그인해야 보이는 화면을 직접 열 수 없어, 화면이 부르는 도메인 함수로 읽기만 해서 셌다(3-3) |
| 3-a | 사본 파일 이름 · 되돌리는 법 | **다르게 했다** — 이름을 적었고, 11-2 의 셋 되돌리기가 아니라 DB 하나만 되돌리는 절차로 7-5 를 고쳤으며, 백업 정리가 그 사본을 지우지 않게 `scripts/backup.py` 에 한 줄 더했다(커밋 전 검토) |
| 4-a | 인계 차례 6 끝남 · 다음은 16번이거나 사람이 정함 | **했다** |
| 4-b | 봐둘것 여섯을 운영 반영으로 닫기 · 원문 유지 | **했다** — BB-h 는 제목 라벨도 바꾸고 옛 라벨을 괄호에 남겼다 |
| 4-c | CLAUDE.md 7-1 을 이 판 뒤 사실로 | **했다** — 13장에도 한 줄 |
| 가~마 | 수용 기준 | **했다** — 2장 |
| 끝줄 | 운영 DB 를 실제로 바꾼다 · 11-3 마무리 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
