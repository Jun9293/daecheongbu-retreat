사람이 할 것
노트북에서 직접: 둘 — ① 총무님이 운영 미리보기 수를 직접 본다(명령은 3-3 · DB 는 안 바꾸고 짝 표 파일만 data/ 에 쓴다) ② 개발 DB 를 지우고 seed 를 다시 만든다 — 이 판의 계좌 꼴 검사가 옛 seed 의 지어낸 계좌에 걸려 devserve 가 지금 개발 DB 로는 안 뜬다(5장)
관리자 권한: 없음 — 재시작은 이 창에서 됐다(4장)
정할 것: 둘 — ① 운영 미리보기 수가 3-1 과 같으면 「그대로 해」 로 운영에 들일지(3장) ② 계좌 꼴 찾기(check_names)를 docs/ 밖(scripts · tests · seed)으로 넓힐지 — 넓히면 지어낸 번호를 전부 지어낸 표시 꼴로 바꿔야 한다(6장)
막혀서 못 한 것: docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인해야 도는데 이 창은 로그인 칸에 비밀번호를 넣지 않는다(11-2). 화면에서 바뀐 것은 계좌번호 칸의 안내 글자 둘뿐이다(4장)

작성 시각: 2026-09-14 23:45 KST
커밋: 383a13c (게이트 · 검사 · 시험) · d41172e (문서) · 이 보고는 그다음 커밋
작업: 계좌 꼴 게이트 — 운영 실행 전 마지막 판

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 계좌 여섯 칸(`payer_bank` · `payer_account_number` · `payer_account_holder` · `bank_name` · `account_number` · `account_holder`)에 값을 넣는 곳 — `routers/expenses.py` 등록·고치기 · `routers/settings.py` `update_me` · `scripts/재정들여오기.py`. 다른 라우터·clone·seed 는 쓰지 않는다(첫 검토 5 가 한 번 더 훑음) |
| 같은 부분 템플릿을 쓰는 화면 | 계좌번호 입력 안내가 `expenses.html` · `settings.html` 에 「적힌 그대로」 로 있었다 — **처음 훑기에서 놓쳤고 첫 검토 3 이 잡았다** |
| 그 값을 세어 둔 문서 | CLAUDE.md 7-4 · 7-5 · 11-2(개발 DB 게이트 문단) · 14장 · 봐둘것 BB-c · BB-h · 인계 머리 · 순서표 6. seed_data 의 지어낸 계좌(첫 검토 1) |

# 1. 한 것

- **판정 하나** — `app/domain/budget.py` 의 `account_problem(은행, 번호, 예금주)` → 까닭 또는 None
  - 막는 것: 번호가 있는데 은행이 빔 · 번호에 숫자와 하이픈 말고 다른 글자(빈칸 · 앞뒤 · 겹친 하이픈 · 전각 숫자 포함) · 숫자 10 개 미만이나 16 개 초과 · 글자 없는 은행 · 20 자 넘는 은행 · 30 자 넘는 예금주
  - 지나는 것: 셋 다 빈 것 · 은행이나 예금주만 있는 것(계좌를 안 적은 것)
- **꼴의 수** — `app/account_shape.py`(아무것도 import 안 함 · 검사가 앱을 안 읽고 쓴다)
- **지나는 자리** — 지출 등록(늘) · 지출 고치기와 내 정보(계좌 칸이 바뀔 때만 — 옛 값이 비고·이름 고치기를 막지 않게) · 시트 들여오기(가른 뒤 · 걸리면 계좌 없이 들이고 짝 표 「계좌」 · 미리보기 「지출 · 계좌 꼴에 걸려 계좌 없이 들임」)
- **검사** — `check_dev_db` 는 개발 DB 의 볼 칸 전부에서(대응표가 없어도), `check_names` 는 `docs/` 글에서 계좌번호 꼴을 찾는다. 값은 안 찍고 자리만. 날짜 머리 · 01 로 시작하는 10~11 자리 · 지어낸 표시(999 머리 · 한 숫자 되풀이 · 이어 오름)는 뺀다. `check_names` 는 저장소 전체를 볼 때 docs 파일을 0 개 보면 실패
- 계좌번호 칸 안내 「적힌 그대로」 → 「숫자와 하이픈(-)」 (두 화면)
- seed 의 지어낸 계좌를 지어낸 표시 꼴로
- 시험: `tests/test_stage49.py` 새로(12) · `tests/test_stage48.py` 합성 시트 세 줄(식대 손 셈 · 번호 겹침 · 꼴 걸림) + c03 · `tests/test_web_flows.py` 의 여섯 자리 번호를 꼴에 맞게
- 문서: CLAUDE.md 7-4 · 7-5 · 11-2 · 14장 · 봐둘것 BB-c(처리함 · 원문 유지) · BB-h(계좌 걸림 줄 · 사람이 정한 넷) · 인계 머리와 순서표 6 · 앞 판 보고를 `docs/review/2026-09-14-차례6.md` 로 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 이상한 꼴을 막고 시트 계좌가 전부 지난다 | O | `test49_a01`(막을 꼴 10 · 지날 꼴 4 · 빈 것 · 전각은 `d06`) · **같은 판에 시트를 재어** 계좌 줄 64 전부 게이트 통과 · 64 전부 검사가 계좌로 봄(3-1) |
| 나) 걸린 지출은 계좌 없이 · 짝 표 · 전체 안 멈춤 | O | `test48_c03` — 은행 없는 번호와 글자 섞인 번호 두 줄이 계좌 없이 들어가고 지출 수 = 계획 · 짝 표에 까닭만(값 없음) |
| 다) 폼이 게이트 뒤에도 돈다 | O | `test49_c01`(등록 · 계좌 없이도) · `c02`(고치기 · 옛 값이 있어도 비고 고치기 통과) · `c03` · `c04`(내 정보 · 옛 값이 있어도 이름 고치기 통과) · 기존 stage46 · 47 · web_flows 그대로 통과 |
| 라) 두 검사가 계좌 꼴을 본다 | O | `test49_d01`~`d05` · 리허설 사본에 `check_dev_db` → 계좌번호 칸 64 걸림(3-2) |
| 마) 리허설 수 = 앞 판(걸린 것 빼고) | O | 3-2 — 걸린 것 0 이라 전부 같음 |
| 바) 식대 짝 · 번호 겹침이 합성 시험에서 걸림 | O | `test48_b01`(식대 짝 [3] · 짝 표 md) · `test48_d01`(잇기 줄을 직접 세어 2). **고장 셋(겹침 한 번만 보기 걷기 · 식대 짝 안 적기 · 게이트 끄기)을 따로 심어 각각 빨개짐** — 첫 심기에서 겹침은 안 걸려 관계 대신 잇기 줄을 직접 세게 고쳤다 |
| 사) pytest · 글검사 7/7 | O | 6장 |
| 아) 새는지 검사 0 | O | 6장 — 바뀐 파일에 새로 들어온 시트 값 0 |
| 자) 검토자 원문 | O | 7장 |

# 3. 운영 자료 (값 없이 건수만)

**운영 DB 는 이 판에서 안 열었다.** 리허설은 앞 판 리허설이 떠 둔 **들이기 직전 사본**(이 창의 임시 폴더)을 다시 복사해 돌렸다 — 앞 판과 같은 사본이다.

## 3-1. 시트 계좌 실측 (게이트 규칙을 정하기 전)

- 계좌 칸이 글자인 줄 64 · 빈 줄 33 · **숫자로 저장된 칸 0**(첫 검토 5)
- 64 줄 전부 한글 은행 이름(2~5 자) · 숫자 12~14 개 · 숫자만 38 줄 / 하이픈 둘이나 셋 26 줄
- 규칙을 넣은 뒤: 게이트 통과 64 / 64 · 검사가 계좌로 봄 64 / 64

## 3-2. 리허설 (같은 사본)

| 무엇 | 앞 판 | 이 판 |
|---|---|---|
| 미리보기 수 | 3-1 표(앞 판 보고) | 같음 — 요약 줄에 「계좌 0」 이 더 붙음 |
| 계좌 꼴에 걸려 계좌 없이 들인 줄 | (없던 칸) | 0 |
| 실행 뒤 산 예산 항목 · 산 지출 · 산 수입 · 영수증 · 산 잇기 | 9→34 · 12→97 · 0→4 · 12→109 · 12→111 | 같음 |
| 계좌 든 지출 · 은행 없는 번호 | 64 · (재지 않음) | 64 · 0 |
| 두 번째 | 「이미 들여왔습니다」 | 같음 |

리허설 뒤 그 사본에 `check_dev_db` 를 돌리면 `expense_entries.payer_account_number` 가 64 칸 걸린다 — 운영 계좌가 개발 DB 로 옮겨지면 게이트가 서버를 안 띄운다.

## 3-3. 총무님이 운영 미리보기를 보는 법

저장소 폴더의 PowerShell 에서(DB 는 안 바꾼다 · `data/재정짝표.real.md` 만 새로 쓴다):

```
.venv\Scripts\python.exe scripts/재정들여오기.py data/Belong예산.real.xlsx --회차 1
```

마지막 줄이 「바꾸지 않았습니다」 이고, 수가 앞 판 보고 3-1 과 이 판 3-2(계좌 0)와 같으면 「그대로 해」. 다르면 그 수를 채팅에 적어 주세요 — 다음 판은 다르면 `--실행` 을 안 돈다(11-2).

## 3-4. 되돌리기

앞 판 보고 3-4 와 같다 — `--실행` 직전 사본(`data/backups/app-날짜.db`)으로 11-2 「되돌리기」.

# 4. 재시작

`app/` 이 바뀌어 두 번 했다(두 번째는 첫 검토 뒤 템플릿을 고쳐서).

- 1회: PID 32968 → 7348 · 「성공 — PID 가 바뀌었습니다」 · 2026-09-14 23:21:09
- 2회: PID 7348 → **26316** · 「성공 — PID 가 바뀌었습니다」 · 2026-09-14 23:32:31 · `/login` 200
- 그 뒤 고친 것은 `scripts/check_names.py` · 시험 · 문서라 서버가 읽지 않는다
- 게이트가 운영 서버에서 400 을 내는지는 로그인이 필요해 못 봤다 — pytest 가 그 길을 잰다. drawer.js · phone.js 도 같은 까닭(사람칸)

# 5. 판단이 들어간 자리

- **규칙의 폭** — 시트 계좌가 12~14 자리인데 10~16 으로 넓혔다. 시중 계좌 10~14 · 가상계좌 16 까지. 은행은 이름 목록으로 막지 않았다(목록은 낡는다)
- **고치기·내 정보는 계좌를 바꿀 때만** — 게이트 전 값(운영 사용자 계좌 · 앞으로의 옛 행)이 다른 칸 고치기를 막지 않게. 대가는 옛 값이 꼴에 안 맞는 줄에서 예금주만 고쳐도 번호 쪽 까닭으로 막히는 것(첫 검토 6)
- **내 정보도 게이트에** — 지시문 목록(등록·고치기·들여오기) 밖이지만 계좌를 쓰는 자리라 넣었다
- **검사는 docs/ 만** — 보고와 덩어리가 나가는 자리다. 코드·시험·seed 는 지어낸 번호를 담아, 넓히면 그것부터 바꿔야 한다(정할 것 ②)
- **지어낸 표시 꼴을 빼는 것** — 그 꼴(999 머리 · 되풀이 · 이어 오름)이나 날짜 머리 · 01 머리 10~11 자리인 실제 계좌는 **검사가 놓친다.** 입력 게이트는 안 놓친다. 이 판이 잰 시트 계좌 64 줄은 전부 걸렸다
- **개발 DB** — 지금 개발 DB 의 옛 칸(`payer_account`)에 옛 seed 가 넣은 지어낸 계좌 5 칸이 있어 계좌 꼴 검사가 서버를 막는다. seed 는 고쳤지만 이미 든 값은 남는다. 개발 DB 를 지우는 것은 데이터 삭제라 이 창이 안 하고 사람칸에 두었다:

```
Remove-Item "$env:TEMP\dcb-dev\app.db" -Force
```

  그 뒤 devserve 를 켜면 새 seed 로 만들어진다

# 6. 검사와 시험

- 글검사(커밋 전): 7/7 통과 · `check_names` 「docs/ 글 파일 61개에 계좌번호 꼴 없음」
- 새는지 검사(저장소 밖 도구 · 값은 안 찍음): 바뀐 파일마다 **이 판에 새로 들어온 시트 값** 0. 시험 파일에 앞 판부터 있던 우연 겹침(둥근 합성 금액)은 그대로다. 시험의 금액 하나가 시트의 네 자리 수를 품어 바꿨다. 이 보고에는 검토자 원문(7-1) 한 줄의 흔한 세 글자 낱말이 시트 글 조각과 우연히 같아 1 이 걸렸다 — 계좌·이름꼴이 아니고 원문이라 그대로 두었다
- 영향 받는 시험 7 파일 109 passed(첫 검토 뒤)
- 보고를 다 채운 뒤 돌린 것: 글검사 7/7 통과 · pytest(옵션 없이) `1573 passed, 3 warnings in 1342.08s (0:22:22)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위 02f2977..(스테이지+작업 트리) · 커밋 0개 · 파일 16개

기준으로 본 것: CLAUDE.md 10장 · 11-2 · 11-3 「마치기 전에 되짚는 것」 · 7-4 · 7-5 · 14장, docs/검토-원칙.md
직접 돌린 것(읽기만 · 값은 안 찍음):
  check_names.py → exit 0, 「docs/ 에 계좌번호 꼴 없음」
  check_dev_db.py 를 지금 개발 DB(%TEMP%\dcb-dev\app.db)에 → exit 1 (아래 [고쳐야 함] 1)
  같은 DB 에 계좌 꼴 검사만 끄고 → {} (이 판 전에는 통과하던 DB)
  account_shape 로 저장소 전체 글 파일을 셈 → docs 밖 7개 파일에서 걸림, 그중 seed_data.py 5줄
  얼린 판 2026-09-14-차례6.md 의 인덱스 사본 = HEAD 의 최근.md (줄끝 CR 차이만 · 내용 같음)

── 막아야 함 ──
없음

── 고쳐야 함 ──

1. scripts/check_dev_db.py:185 · :204-208 — 이 판 뒤로 이 노트북의 개발 서버가 안 뜸 · 안내문도 틀림
   지금 개발 DB 에서 게이트가 「expense_entries.payer_account: 5칸」 으로 멈춤. 계좌 꼴 검사를
   끄면 같은 DB 가 통과하므로, 멈추게 한 것은 이 판의 계좌 꼴 검사.
   그 값의 출처는 seed_data.py:587-591 의 MEAL_EXPENSES 계좌(999 로 시작하지 않는 지어낸 번호).
   지금 seed.py:302 는 그 값을 안 쓰지만(_account) 옛 seed 가 옛 칸에 넣어 둔 것으로 보임.
   걸린 게 틀린 것은 아님(계좌 꼴 값이 DB 에 있음). 문제는 멈출 때의 말:
   「익명화 이전 자료가 남은 것입니다」 는 이 경우 사실이 아님 — 지어낸 seed 값임.
   고칠 것 둘: 계좌 꼴로만 걸렸을 때의 안내 문장, 그리고 seed_data.py 의 그 번호들을
   지어낸 표시 꼴(999 머리)로 바꿀지. 보고에 「devserve 가 지금 DB 로는 안 뜬다 ·
   지우고 다시 seed 」 를 적어야 함 — 구현자 요약에 이 사실이 없음.

2. scripts/check_names.py:332-356 — 계좌 꼴 검사가 본 파일 수를 안 찍음 (11-3 ③)
   출력이 「docs/ 에 계좌번호 꼴 없음」 한 줄. docs/ 를 몇 개 봤는지 안 나와서
   startswith("docs/") 판정이 어긋나 0개를 봐도 똑같이 초록.
   바로 위 이름 검사는 「파일 350개를 봤습니다」 를 찍음. 같은 꼴로 수를 찍고,
   인자 없이 돌 때 0개면 실패로 둘 것. test49_d02 는 한 파일을 직접 넘기므로 이 자리는 못 봄.

3. app/templates/expenses.html:88 · app/templates/settings.html:28 — 입력 안내가 게이트와 반대로 말함
   계좌번호 칸의 placeholder 가 「적힌 그대로」. 이제 빈칸·글자가 섞이면 400 이 남.
   안내는 그대로 적으라 하고 서버는 막아서, 사람은 입력한 걸 잃고 까닭만 보게 됨(0단계 훑기에서
   「같은 이름을 쓰는 데」 로 잡혔어야 할 자리).

4. app/routers/settings.py:573-578 — 내 정보의 「바뀔 때만」 에 시험이 없음
   expenses 쪽은 test49_c02 가 「옛 값이 있어도 비고만 고치면 지난다」 를 잼.
   내 정보는 c03 이 막히는 쪽과 새 값만 잼 — 옛 계좌가 꼴에 안 맞는 사람이 이름만 고칠 때
   통과하는지(②)는 안 잼. 지금은 계좌 쓴 사용자가 0행이라 안 밟히지만 같은 규칙을 둘로 적었으니 둘 다 잴 것.

── 봐 둘 것 ──

5. (a) 게이트를 우회하는 길 — 못 찾음
   app/ · scripts/ · seed*.py 에서 계좌 여섯 칸에 값을 넣는 곳을 다 봄:
   expenses.py create(299) · update(474-476) · settings.py update_me · 재정들여오기.py(406-413 → 512).
   다른 라우터·clone·seed 에는 쓰는 곳이 없음. 옛 칸(_legacy_*)은 모델에서 막혀 있음.
   다만 재정들여오기.py:400 은 계좌 칸이 글자가 아니면(시트에 숫자로 저장된 칸) 원 = None 이 되어
   게이트도 짝 표도 안 거치고 조용히 계좌 없이 들어감. 이 판에서 생긴 건 아니고, 64줄이 전부 글자였다는
   실측이 있으면 운영에서는 안 밟힘. 보고에 「숫자로 된 계좌 칸 0」 을 한 줄 적어 두면 닫힘.

6. (b) 「바뀔 때만」 은 우회가 아님
   expenses.py:474 는 새값에 있는 계좌 칸이 하나라도 바뀌면 셋 전체를 검사함.
   남의 계좌를 못 보는 사람은 계좌 칸이 빠지므로(466-468) 쓰는 길 자체가 없음.
   바꾸지 않은 옛 값은 DB 에 그대로 남을 뿐 새로 들어가진 않음.
   대가: 옛 값이 꼴에 안 맞는 줄에서 예금주만 고쳐도 번호 쪽 까닭으로 막힘. 일부러 그렇게 둔 걸로 읽힘.

7. (c) 제외 규칙이 놓치는 크기와 docs/ 경계
   - 제외(날짜 머리 · 01 로 시작하는 10~11자리 · 999 머리 · 한 숫자 되풀이 · 이어 오름)는 검사에서만 씀.
     입력 게이트(account_problem)는 안 씀 — 막는 쪽은 안 새고, 찾는 쪽만 놓침.
     놓치는 꼴: 4-2-2 로 끊고 19/20 으로 시작하는 번호, 01 로 시작하는 10~11자리 계좌.
     둘 다 드물지만 0 은 아님. 보고에 적힌 「시트 64줄 전부 검사에 걸림」 이 그 크기를 재는 것.
   - docs/ 만 보는 경계는 11-2 「막는 것은 기본이 전부이고, 사람이 적는 것은 안 보는 이유」 와 반대 방향.
     저장소 전체로 세면 docs 밖에서 seed_data.py 5줄과 시험 여럿이 걸림 — 넓히면 지어낸 값부터
     999 꼴로 바꿔야 함. 경계를 어디 둘지는 사람이 정할 일.
   - app/account_shape.py:18 · :21 의 \d 와 isdigit 는 전각 숫자도 숫자로 봄.
     「번호에 숫자·하이픈 외 글자」 를 막겠다면 re.ASCII 가 필요. 영향은 작음.

8. 「…할 때만」 꼴 (검토-원칙 7)
   scripts/check_dev_db.py:148-149 — 대응표가 없으면 {} 로 먼저 돌아가서 계좌 꼴 검사도 같이 꺼짐.
   계좌 꼴은 대응표와 상관없는 검사인데 거기 묶였음. tests/test_stage49.py:156-157 도 같은 조건으로 skip.
   이 노트북에는 대응표가 있어 지금은 돔.

9. (d) 합성 시험에 운영 수가 박혔나 — 아님
   test_stage48 은 합성 시트 안의 수(식대 짝 [3] · 계좌 {6,8} · 잇기 줄 2)만 잼. 64 는 test_stage49 독스트링에만 있음.
   식대 짝 · 번호 겹침 · 계좌 게이트를 각각 심었을 때 빨개지는지는 구현자 요약의 주장이고, 검토자는 다시 안 돌림.

10. (e) 문서
   - CLAUDE.md 7-4 추가 줄의 「64 줄」 은 날짜가 붙은 실측값이라 10장 · check_counts 에 걸리지 않음.
     다만 다음 시트에서는 낡음. 「시트의 계좌 줄 전부」 로 적으면 안 낡음.
   - CLAUDE.md 14장 새 줄 「check_dev_db 는 개발 DB 글자 칸 전부에서」 — 실제로는 제외칸을 빼고,
     대응표가 있을 때만 돎(8번). 11-2 의 개발 DB 게이트 문단에는 계좌가 아직 없음(장소·연락처만).
   - docs/봐둘것.md BB-c — 「원문 유지」 라 했는데 원래 머리 「게이트는 적어만 둔다 · 미룬 둘은 닫혔다」 가 지워졌고,
     첫 인용 줄은 굵게 표시가 빠진 채 「(아래는 그 앞의 머리)」 뒤로 옮겨짐. 라벨을 바꾸는 건 관례지만
     옛 머리 글자는 인용 안에 남겨 두는 편이 지시에 맞음.

11. 보고(8-1 · 8-2 · 9)는 아직 없어서 못 봄
   최근.md 는 앞 판 그대로. 새 보고를 쓴 뒤 사람칸 다섯 줄 · 지시문 대조 표(1a~4b) · 고치기 전 훑기가
   있는지는 다음 검토나 사람이 봐야 함. 특히 1-d 「되돌아가 깨지는 자리」 에 3번(placeholder)과
   1번(개발 서버)이 들어가야 함.

── 사람에게 묻는 것 ──
ㄱ. 계좌 꼴 찾기를 docs/ 밖(scripts · tests · seed)으로 넓힐까요?
    넓히면 seed_data.py 의 지어낸 계좌와 시험 값을 999 꼴로 바꿔야 합니다. 지금은 docs/ 만 봅니다.
ㄴ. 개발 DB 를 지우고 다시 seed 할까요, 아니면 옛 칸 payer_account 를 제외칸에 이유와 함께 넣을까요?
    지금 개발 DB 로는 devserve 가 안 뜹니다.
ㄷ. 검사를 대응표가 없는 컴퓨터에서도 돌릴까요? 지금은 대응표가 없으면 계좌 꼴 검사도 같이 꺼집니다.
ㄹ. 전각 숫자로 적은 계좌번호도 막을까요?
━━━ 검토자 여기까지 ━━━
```

## 7-2. 다시 보기 (검토 뒤에 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 · 02f2977..스테이지 · 첫 검토 뒤 바뀐 파일만

직접 돌린 것(읽기만 · 값은 안 찍음):
  check_names.py app/templates → exit 1, 「계좌 꼴 검사가 docs/ 글 파일을 하나도 못 봤습니다」 (아래 1)
  check_dev_db.py 를 지금 개발 DB 에 → exit 1, 옛 칸 payer_account 5칸, 새 안내문이 나옴
                                   (seed 를 999 꼴로 바꿨어도 옛 DB 에 들어 있던 값이라 그대로 걸림 — 사람칸 몫)
  첫 검토 1~4 · 7 · 8 · 10 이 diff 에 반영됐는지 줄마다 대조함

── 막아야 함 ──
없음

── 고쳐야 함 ──

1. scripts/check_names.py:351-354 (새 0개 가드) — 경로를 주거나 --staged 로 돌리면 docs 가 없어도 실패함
   가드가 「docs/ 판정이 어긋나서 0」 과 「애초에 docs 파일을 안 넘겨서 0」 을 구별하지 못함.
   볼파일(args)(391-413)은 경로 인자(`check_names.py app/templates`)나 --staged 일 때 docs/ 가 아닌 파일만
   넘길 수 있고, 그때 실명 검사는 초록인데 전체가 exit 1 이 됨 — 위에서 실제로 재현함.
   글검사.py:73 은 인자 없이 부르므로 기본 경로(저장소 전체)는 멀쩡함.
   깨지는 것은 11-2 가 「docs/ 로 무언가를 커밋하기 전에」 손으로 돌리라고 한 그 사용법과 --staged.
   고칠 방향: 0개 실패는 기본 경로(저장소 전체를 볼 때)에만 걸고, 경로·--staged 일 때는
   「docs/ 글 파일 0개 — 계좌 꼴 검사 해당 없음」 으로 찍고 통과.
   test49_d05 는 빈 목록만 재므로 이 갈래(②, 막히면 안 되는 것)가 없음.

── 봐 둘 것 ──

2. docs/봐둘것.md BB-c 머리 인용(3330 언저리) — 「check_dev_db(개발 DB 글자 칸 전부)」 가 남아 있음
   14장은 「볼 칸(제외칸을 뺀 글자 칸 · 대응표가 없어도)」 으로 고쳐졌음. 뜻은 같은 쪽이라 가벼움.

3. tests/test_stage49.py c04 — 로그인한 관리자와 `role == "admin"` 의 첫 행이 같은 사람이라는 전제에 기댐
   다르면 요청이 계좌를 바꾸는 것이 되어 400 이 나고 시험이 빨개짐(조용히 통과하는 쪽은 아님).
   지금 통과한다는 것은 둘이 같다는 뜻이라 새는 곳은 아님.

4. app/account_shape.py:8 독스트링의 「64 줄」 — CLAUDE.md 는 「계좌 줄」 로 바꿨고 여기는 날짜가 붙은 실측으로 남음.
   봐둘것 BB-h 표의 64 도 같은 성격(그때 잰 값). 둘 다 그대로 둬도 됨.

반영 확인(문제 없음):
  - seed_data.py MEAL_EXPENSES 가 999 머리 꼴로 바뀜. seed.py:302 는 여전히 그 값을 안 씀.
    다른 코드·시험이 그 글자를 찾는 곳 없음(test_web_flows:376 은 따로 적은 값)
  - check_dev_db: 대응표가 없으면 names·phones 를 빈 것으로 두고 계좌 꼴만 봄. 대응표가 깨졌거나 빈 경우는
    전처럼 멈춤. test49_d04 가 잼 · d03 skip 은 걷힘. 안내문이 seed 경우를 말함
  - account_shape: [0-9] 와 ASCII 로만 셈. 게이트(칸꼴 · 숫자수)와 검사가 같이 바뀜. d06 은 게이트 쪽만 잼
  - 두 placeholder 가 게이트와 같은 말이 됨
  - test49_c04 가 내 정보의 「바꿀 때만」 ② 를 잼
  - CLAUDE.md 7-4 · 11-2 문단 · 14장, BB-c 옛 라벨 · 옛 머리 굵게 · 「남긴 것」 이 들어감

── 사람에게 묻는 것 ──
ㄱ. 지금 개발 DB 를 지우고 다시 seed 할지 (첫 검토 ㄴ · 구현자가 사람칸으로 올림)
ㄴ. 계좌 꼴 찾기를 docs/ 밖으로 넓힐지 (첫 검토 ㄱ · 보고 「정할 것」 으로 올린다고 함)
━━━ 검토자 여기까지 ━━━
```

## 7-3. 두 번째 검토 뒤 고침 (다시 보이지 않고 적는다 · 11-3 4단계)

- `scripts/check_names.py` — 다시 보기 1. 0 개 실패를 저장소 전체를 볼 때만 걸고, 경로·`--staged` 일 때는 「해당 없음」 으로 통과(`계좌꼴검사(파일들, 전체=…)`). `check_names.py app/templates` 가 exit 0 인 것을 돌려 봤다
- `tests/test_stage49.py` — `d05` 에 막히면 안 되는 쪽(`전체=False` → 0) 한 줄
- `docs/봐둘것.md` BB-c 머리 — 다시 보기 2. 「개발 DB 글자 칸 전부」 → 「개발 DB 의 볼 칸 · 대응표가 없어도」
- 다시 보기 3 · 4 는 안 고쳤다(새는 곳이 아니고, 64 는 날짜가 붙은 실측)

# 8. 바뀐 파일 (`git diff --name-only 02f2977..d41172e`)

- CLAUDE.md
- app/account_shape.py
- app/domain/budget.py
- app/routers/expenses.py
- app/routers/settings.py
- app/templates/expenses.html
- app/templates/settings.html
- docs/review/2026-09-14-차례6.md
- docs/봐둘것.md
- docs/세어둠.txt
- docs/옛말-넘김.txt
- docs/인계.md
- scripts/check_dev_db.py
- scripts/check_names.py
- scripts/재정들여오기.py
- seed_data.py
- tests/test_stage48.py
- tests/test_stage49.py
- tests/test_web_flows.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1-a | 계좌를 넣는 모든 자리가 지나는 한 곳 · budget.py | **다르게 했다** — 판정은 budget.py 하나이고, 꼴의 수만 `app/account_shape.py` 에 두었다(검사가 앱을 안 읽고 같은 꼴을 보려고). 내 정보도 지나게 했다(목록 밖) |
| 1-b | 무엇을 막나 · 시트 64 줄 먼저 확인 | **했다** — 3-1 · 1장 |
| 1-c | 들여오기는 게이트 지난 값만 · 걸리면 짝 표 · 계좌 없이 · 안 멈춤 · 미리보기에 수 | **했다** — test48_c03 |
| 1-d | 등록·고치기도 같은 게이트 · 되돌아가 깨지는 자리 | **했다** — 입력 안내 둘을 고쳤고(첫 검토 3), 개발 서버가 옛 개발 DB 로 안 뜨는 것은 사람칸(첫 검토 1) |
| 1-e | check_dev_db · check_names 에 계좌 꼴 | **했다** — check_names 는 docs/ 만(정할 것 ②) |
| 2-a | 같은 사본 리허설 · 걸린 수 | **했다** — 0 · 3-2 |
| 2-b | 앞 판 수와 견줌 | **했다** — 같음 |
| 3-a | 시험 빈 자리 둘을 합성 시트로 | **했다** — 고장을 심어 걸리는지까지 |
| 3-b | 14장 줄 확인만 | **다르게 했다** — 확인했더니 돌린다 로 맞았고, 이 판의 게이트 줄을 더하느라 그 줄 끝에 한 구절을 붙였다 |
| 4-a | BB-c 닫기 · 원문 유지 | **했다** |
| 4-b | 인계에 게이트 섰음 · 「그대로 해」 기다림 | **했다** — 머리와 순서표 6 |
| 가~자 | 수용 기준 | **했다** — 2장 |
| 끝줄 | 운영 자료 읽기만 · 리허설은 사본 | **했다** — 이 판은 운영 DB 를 안 열었다 |
| 끝줄 | --실행 은 「그대로 해」 뒤 | **했다** |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
