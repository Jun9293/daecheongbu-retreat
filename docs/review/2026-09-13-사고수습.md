사람이 할 것
노트북에서 직접: 없음 — 되돌리기는 이 창이 돌렸습니다(3장). 사역자 열 분 계정(인계 18번)과 개기자 주소(인계 8번)는 그대로 남아 있습니다
관리자 권한: 없음 — 앱을 멈추고 켜는 것은 이 창에서 됐습니다(3-1)
정할 것: 검토자가 물은 셋 — ① 지시 4 보다 넓게 담은 커밋 범위(새 시험 · 독스트링 한 줄 · HEAD 를 초록으로 만드는 넘김 두 줄 · 4장) ② 시험과 고장 재현을 운영 DB 와 같은 기계에서 계속 돌릴지(봐둘것 AZ-b) ③ 운영 파일을 직접 여는 표본 시험을 그대로 둘지(AZ-b · 11장 [E])
막혀서 못 한 것: 없음
작성 시각: 2026-09-13 KST — 검토를 받고 고친 판
커밋: (이 보고와 함께 나갑니다 — 10장)
작업: 사고 수습 — 시험이 운영 DB 를 못 지우게 막고, 운영 DB 를 15:18 백업으로 되돌림

---

# 0. 한 줄 요약

**막는 것을 먼저 넣고, 재고, 그다음 되돌렸습니다.** 시험이 운영 DB 를
지운 입구(`-p` 로 app 을 conftest 보다 먼저 읽힌 것)를 막지 않고 **지우는
자리**를 막았습니다 — `tests/conftest.py` 의 `_시험DB가_아니면_멈춘다` 가
`drop_all` 앞마다, 그리고 시험이 하나라도 돌기 전에 엔진이 가리키는 파일이
그 conftest 가 만든 임시 폴더 안인지 보고, 아니면 무엇을 열었고 왜 안
되는지 말한 뒤 세션을 멈춥니다.

그다음 운영 DB 를 2026-09-12 15:18 백업으로 되돌렸습니다. **표 37개의 행
수가 되돌리기 전 백업 · 복사 직후 · 앱이 뜬 뒤 셋 다 같습니다.** 밖에서
로그인 화면이 200 으로 뜹니다.

**앞 판이 작업 트리에 남긴 파일은 안 섞었습니다** — 한 가지 예외는 4장.

---

# 1. 고치기 전 훑기 (11-3 0단계)

| 무엇이 같이 바뀌나 | 어떻게 했나 |
|---|---|
| `drop_all` 을 부르는 데 | `tests/conftest.py` 의 `client` 와 `seed.py`. `seed.py` 는 사람이 운영 명령으로 돌리는 도구라 시험 경로가 아니어서 안 건드렸습니다 |
| `app.db` 의 엔진을 쓰는 시험 | `client` · `admin_client` · `login_as` · `make_user` · `app_session` 이 전부 그 엔진입니다. 지우는 것은 `client` 뿐이지만 **쓰는 것은 여럿이라** 세션 첫머리(`pytest_sessionstart`)에서도 같은 검사를 부릅니다 |
| 운영 파일을 직접 여는 시험 | `tests/test_meetings_import.py` 의 표본 회의 시험 하나 — **읽기만** 합니다. 이번 사고를 드러낸 자리라 봐둘것 AZ-b 에 적었고 안 고쳤습니다 |
| 「conftest 가 먼저 돌아 운영에 안 닿는다」 를 적어 둔 문서 | conftest 자신의 독스트링 · `tests/test_attachments.py` 독스트링 — 둘 다 고치고 옛말.md 에 표기로. 얼린 검토 보고 둘(`2026-09-11-로그인판` · `2026-09-11-비품도막4`)에도 같은 주장이 있으나 **그때의 기록이라 표기로 안 적었습니다** |
| 이 값을 세어 둔 문서 | 없음 |

---

# 2. 막는 것 (지시 1)

## 2-1. 무엇을 넣었나 (지시 1-b · 1-c · 1-f)

`tests/conftest.py`

- `_시험DB가_아니면_멈춘다(engine)` — 엔진이 **sqlite 파일이고 `_TMP` 안**이면
  지나갑니다. 메모리 DB 도 지나갑니다(지울 파일이 없습니다). 그 밖은 전부
  `pytest.exit(..., returncode=3)` 입니다 — **sqlite 가 아닌 것도 멈춥니다**
  (임시 폴더 안인지 알 길이 없으니까).
- **기준을 「`%TEMP%` 아래」 로 두지 않았습니다.** 운영과 같은 꼴의 경로를
  `%TEMP%` 에 만들어 재면 넓은 기준은 그것을 통과시킵니다 — 2-2 의 a01 이 바로
  그 자리입니다.
- 부르는 자리: `client` 의 **`drop_all` 두 줄 바로 앞마다**, 그리고
  `pytest_sessionstart`(시험 하나라도 돌기 전).
- 멈출 때 찍는 말 — 연 DB 의 주소 · 시험용 임시 폴더 · 왜(app 이 conftest 보다
  먼저 import 됨 — `-p` · PYTHONSTARTUP · IDE 플러그인을 끼웠는지) · 어디를
  읽을지(11-3 · 봐둘것 AZ-a).
- **입구를 안 막고 지우는 자리를 막은 까닭**을 파일 머리 독스트링에 적었습니다 —
  입구는 셀 수 없고 하나를 막으면 다음 것이 옵니다(11-2 「볼 목록이 불완전하다」).

## 2-2. 잰 것 (지시 1-e · 수용 기준 가)

**운영 파일에 대고 재지 않았습니다.**

`tests/test_stage42.py` — 한 판에서 둘 다 봅니다. (a07 은 검토를 받고 더했습니다)

| 시험 | 무엇을 보나 | 결과 |
|---|---|---|
| `test42_a01_운영꼴_경로면_멈춘다` | `tmp_path/data/app.db` (운영과 같은 꼴 · `%TEMP%` 아래 · `_TMP` 밖) → Exit · returncode 3 · 말에 그 경로와 까닭 | 통과 |
| `test42_a02_시험용_임시폴더면_지나간다` | `_TMP/test.db` → 안 멈춤 | 통과 |
| `test42_a03_지금_앱의_엔진은_지나간다` | 이 판이 실제로 쓰는 `app.db.engine` → 안 멈춤 · 그 파일이 `_TMP` 아래 | 통과 |
| `test42_a04_메모리DB는_지나간다` | `sqlite://` → 안 멈춤 | 통과 |
| `test42_a05_파일이_아닌_DB는_멈춘다` | postgresql 주소 → Exit | 통과 |
| `test42_a06_지우는_자리마다_검사가_앞에_선다` | conftest 의 AST 에서 `drop_all` 부르는 문장마다 **바로 앞 문장**이 검사인가 · 하나도 못 찾으면 실패(③) | 통과 |
| `test42_a07_세션_첫머리에서도_검사가_돈다` | `pytest_sessionstart` 가 있고 그 몸이 검사를 부르는가 — **입구를 실제로 막은 것이 이 훅**이라(검토 [B]) | 통과 |

**사고의 입구를 그대로 따라가 봤습니다 — 가짜 운영 파일에 대고.** 백업
사본을 scratch 의 `fakeprod/data/app.db` 에 두고 `DCB_DATABASE_URL` ·
`DCB_DATA_DIR` 을 거기로 준 채, app 을 먼저 import 하는 플러그인을 `-p` 로
끼워 `tests/test_shell.py` 를 돌렸습니다.

```
가짜 운영 파일  전: tables 37 total rows 1985
-p 로 앞질러 돌림 -> Exit: 시험이 연 DB 가 시험용 임시 폴더 밖입니다 — 아무것도 지우지 않고 멈춥니다.
                     연 DB : sqlite:///…/scratchpad/fakeprod/data/app.db
                  exit=3
가짜 운영 파일  후: tables 37 total rows 1985
같은 판, -p 없이 -> 15 passed
운영 app.db     : 크기·수정 시각 전후 같음 (그때는 빈 판이었습니다)
```

**이 재현은 `pytest_sessionstart` 에서 멈췄습니다** — 시험이 돌기 전이라
`drop_all` 앞의 검사까지는 안 탔습니다. 그 자리는 a01(함수가 멈추는가)과
a06(모든 `drop_all` 앞에 서 있는가)으로만 잽니다.

---

# 3. 되돌리기 (지시 2)

## 3-1. 순서

```
되돌리기 전 백업을 표마다 셈     (읽기 전용)
멈추기 전 PID: 26052
포트가 비었습니다
WAL/SHM 없음 확인
복사: data\backups\app-20260912-151859.db -> data\app.db
  SHA256 f2568c3b…4c2bc  (백업과 같음)
복사 직후 표마다 셈 -> 표 37개 전부 같음
Start-ScheduledTask '대청부 앱'
다시 켠 뒤 PID: 22972      (2026-09-13 07:24:33)
성공 — PID 가 바뀌었습니다
안에서 GET /login -> 200
앱이 뜬 뒤 표마다 셈 -> 표 37개 전부 같음
```

**WAL 을 먼저 봤습니다** — 빈 판의 `app.db-wal` 이 남아 있었다면 복사한 파일을
여는 순간 그것이 다시 덮였을 것입니다. 없었습니다.

## 3-2. 행 수 — 나란히 (지시 2-d · 수용 기준 나)

**백업 쪽 수를 시험에 박지 않고 그때 세어 견줬습니다.** 세 번 다 같은
스크립트(`sqlite3` · `mode=ro` · 표 이름 순)로 셌고 `diff` 가 비었습니다.

| 표 | 백업 | 되돌린 뒤 (앱이 뜬 뒤) |
|---|---|---|
| `activity_logs` | 63 | 63 |
| `auth_codes` | 2 | 2 |
| `budget_categories` | 9 | 9 |
| `checklist_items` | 31 | 31 |
| `checklists` | 6 | 6 |
| `departments` | 18 | 18 |
| `discussion_entries` | 58 | 58 |
| `discussion_entry_runs` | 58 | 58 |
| `draft_submissions` | 0 | 0 |
| `equipment_items` | 174 | 174 |
| `equipment_runs` | 188 | 188 |
| `expense_entries` | 12 | 12 |
| `expense_receipts` | 12 | 12 |
| `file_assets` | 2 | 2 |
| `file_versions` | 3 | 3 |
| `income_items` | 0 | 0 |
| `invite_tokens` | 18 | 18 |
| `meeting_items` | 13 | 13 |
| `meetings` | 53 | 53 |
| `notification_logs` | 0 | 0 |
| `notifications` | 380 | 380 |
| `program_items` | 180 | 180 |
| `programs` | 61 | 61 |
| `push_subscriptions` | 0 | 0 |
| `retreat_drafts` | 0 | 0 |
| `retreats` | 2 | 2 |
| `review_requests` | 1 | 1 |
| `schedule_days` | 4 | 4 |
| `schedule_items` | 136 | 136 |
| `site_settings` | 2 | 2 |
| `task_attachments` | 4 | 4 |
| `task_library` | 106 | 106 |
| `task_runs` | 208 | 208 |
| `tasks` | 175 | 175 |
| `user_departments` | 4 | 4 |
| `user_retreat_states` | 1 | 1 |
| `users` | 1 | 1 |
| **합** | **1985** | **1985** |

## 3-3. 안 건드린 것 (지시 2-e · 2-f · 수용 기준 라)

- `data/uploads/` · `data/vapid_private.pem` · `data/secret_key.txt` — 안 건드렸습니다
- `data/backups/비운판-20260913-0050.db` — 그대로입니다
  `SHA256 ddb715e9…cb6ec3` (이 판 처음과 끝이 같음 · 6장)

## 3-4. 밖에서 (지시 2-g · 수용 기준 다)

```
밖에서 GET /login -> 200 · <title>로그인 · 대청부 수련회</title> · 아이디 칸 있음
```

주소는 적지 않습니다(11-2).

---

# 4. 앞 판의 파일과의 경계 (지시 4)

**앞 판이 작업 트리에 남긴 파일은 스테이징도 내용도 그대로입니다** — 한 가지를
빼고.

**다르게 한 것 하나 — 넘김 두 줄.** `docs/세어둠.txt` 와 `docs/옛말-넘김.txt` 의
한 줄씩(둘 다 `docs/review/2026-09-12-휴대폰.md` 를 넘기는 줄)을 이번 커밋에
함께 담았습니다. **HEAD(5c3c517) 가 그 판을 얼리면서 넘김을 빠뜨려 HEAD 자체가
글검사에서 빨갛습니다** — 이번 판 변경만 얹은 트리에서 돌려 보니
`check_stale` · `check_counts` 가 그 얼린 판에 걸려 5/7 이었습니다. 수용 기준
마)(7/7)를 지키려면 그 두 줄이 있어야 하고, **앞 판이 적어 둔 줄과 글자 그대로
같은 줄**을 담아 작업 트리에 두 벌이 생기지 않게 했습니다. 그래서 그 두 파일은
커밋 뒤 `git status` 에서 빠집니다(16 → 14).

**문서 넷(CLAUDE.md · 봐둘것 · 옛말 · 인계)은 앞 판도 고친 파일입니다.** 커밋에는
「HEAD 판 + 이번 판 덩어리」 만 담고, 작업 트리에는 「앞 판의 변경 + 이번 판
덩어리」 를 둡니다. 두 덩어리가 글자 그대로 같은지 3-way merge 뒤 `diff` 로
견줬습니다(넷 다 같음). 커밋 뒤 작업 트리의 그 파일들의 diff 는 **앞 판의 것만**
남습니다.

**`docs/review/최근.md`** — 커밋에는 이 보고가 가고, 작업 트리에는 **앞 판의
보고를 되돌려** 둡니다(다음 판이 그것을 씁니다 · 인계 23번). 이 보고는
`docs/review/2026-09-13-사고수습.md` 로 얼립니다.

---

# 5. 문서 (지시 3)

- **`docs/봐둘것.md`** — AZ-a(사고 · 언제 · 무엇 · 입구 · 잃은 것/안 잃은 것 ·
  되돌린 법 · 막은 법 · 못 막은 것) · AZ-b(같은 기계에서 시험을 돌린다 — **사람이
  정할 것** · 고치지 않음)
- **AZ-a 에 하나 더 적었습니다 — 오늘 03:00 정기 백업은 빈 DB 를 떴습니다**
  (`app-20260913-030001.db` · 348KB). 「가장 최근 백업」 으로 되돌리면 빈 판으로
  갑니다. 15:18 판은 30벌 규칙으로 한 달쯤 뒤 밀려납니다
- **`CLAUDE.md` 11-3** 「마치기 전에 되짚는 것」 — `pytest` 옵션 문단 바로 아래에
  「pytest 앞에 아무것도 끼우지 않는다」 와 까닭
- **`docs/옛말.md`** — 「시험의 DB 보호는 지우는 자리의 검사다」 · 표기 둘
- **`docs/인계.md`** — 머리 갱신 · 23번(앞 판의 커밋이 다음 판) · 24번(이 판 · 끝난 것)
- **`tests/test_attachments.py`** — 독스트링의 옛 주장 한 줄

---

# 6. 돌고 있는 서버에서 확인한 것 (11-3 2단계)

**이 판은 `app/` · `templates/` · `static/` 을 안 고쳤습니다** — 재시작은 코드 반영이
아니라 **되돌리기의 한 걸음**으로 했습니다(3-1: 26052 → 22972 · 07:24:33).
화면 점검(`docs/checks/drawer.js` · `phone.js`)은 **안 돌렸습니다** — 커밋에 화면이
닿는 파일이 없고, 11-3 2단계가 건너뛰는 조건(서버가 읽는 것이 안 바뀜)에 맞습니다.


---

# 7. 수용 기준 — 각 줄이 무엇을 보나

| 기준 | 어디서 잰다 | 통과 |
|---|---|---|
| 가) 임시 폴더 밖 DB 에 멈춘다 · 정상 경로에선 안 멈춘다 · 같은 판 · 운영 파일에 안 댐 | 2-2 — `test42_a01`(운영꼴 경로 · 멈춤) 과 `a02`·`a03`(시험 임시 폴더 · 지금 엔진 · 안 멈춤)이 한 파일 한 판. 입구 재현은 가짜 운영 파일에 대고 exit 3 · 행 그대로, 같은 판 `-p` 없이 통과 | O — **`drop_all` 앞의 검사는 e2e 가 아니라 함수·자리 시험으로만** 탔습니다(2-2 끝) |
| 나) 되돌린 뒤 표마다 행 수가 백업과 같다 · 박지 않고 그때 세어 | 3-2 — 백업/복사 직후/앱이 뜬 뒤 셋을 같은 스크립트로 세어 `diff` | O |
| 다) 밖에서 200 · 로그인 화면 | 3-4 | O |
| 라) 비운판 사본이 그대로 | 3-3 · SHA256 처음과 끝 | O |
| 마) pytest 옵션 없이 전부 통과 · 글검사 7/7 · 1 을 넣은 뒤 | 8장 | O |
| 바) 검토자 원문이 최근.md 에 그대로 | 11장 | O — **원문을 실은 뒤에 글검사를 다시 돌렸습니다** |

---

# 8. 검사와 시험

**이번 커밋과 같은 트리에서 돌렸습니다** — HEAD 위에 이 판의 변경만 얹은 git
worktree(scratch). 앞 판의 작업 트리 파일이 섞이면 무엇이 통과한 것인지 흐려지기
때문입니다. 그 트리에는 `data/app.db` 가 없어 표본 회의 시험이 건너뜁니다 — 그것이 「1 skipped」 입니다(AZ-b 의
그 시험 — **운영 DB 를 안 놓아 둔 것이 이 판의 뜻과 맞습니다**).

**첫 판은 9 failed 였고 까닭은 그 worktree 였습니다.** 실패 아홉이 전부
`FileNotFoundError` — 시험이 `ROOT/.venv/Scripts/python.exe` 로 검사 스크립트를
부르는데 worktree 에는 `.venv` 가 없었습니다(gitignore). 본 저장소의 `.venv` 를
이어 붙이고(junction) **보고를 다 채운 뒤** 처음부터 다시 돌렸습니다. 아래가
그 판입니다.

```
1450 passed, 1 skipped, 3 warnings in 1103.10s (0:18:23)
```

```
━━━ 모아서 ━━━
  통과  check_names · check_stale · check_named · check_counts
  통과  check_handoff · check_placeholder · check_orders

글 검사가 전부 통과했습니다.
```

---

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 1-a | 막는 것이 되돌리기보다 먼저 | **했다** — 2장이 3장보다 앞. 되돌리기는 07:23 · 검사 재기는 그 전 |
| 1-b | 자기가 연 DB 가 임시 폴더인지 보고 아니면 drop_all 앞에서 멈춘다 · 매번 | **했다** — 2-1 |
| 1-c | 멈출 때 어느 파일을 왜 | **했다** — 2-1 · 2-2 의 출력 |
| 1-d | 넣기 전에는 pytest 안 돌림 | **했다** — 이 창에서 처음 돌린 시험이 넣은 뒤의 `test_stage42.py` |
| 1-e | 운영꼴 경로로 막히는 것·안 막히는 것을 같은 판에서 | **했다** — 2-2 |
| 1-f | 입구가 아니라 drop_all 자체 · 까닭을 파일에 | **했다** — 2-1 · conftest 머리 |
| 2-a | 앱을 멈춘다 | **했다** — 3-1 |
| 2-b | 백업을 app.db 로 복사 | **했다** — 3-1 · 해시 같음 |
| 2-c | 앱을 켠다 | **했다** — 3-1 · PID 바뀜 |
| 2-d | 전후 행 수를 나란히 · 다르면 멈춤 | **했다** — 3-2 · 다른 것 없음 |
| 2-e | 업로드와 VAPID 안 건드림 | **했다** — 3-3 |
| 2-f | 비운판 안 지움 | **했다** — 3-3 |
| 2-g | 밖에서 로그인 화면 | **했다** — 3-4 |
| 3-a | 봐둘것에 사고 | **했다** — AZ-a |
| 3-b | 같은 기계라는 사실 · 고치지 않음 | **했다** — AZ-b |
| 3-c | 11-3 에 한 줄 · 왜 | **했다** — 5장 |
| 3-d | 옛말.md | **했다** — 표기 둘. 얼린 판 둘은 기록이라 안 적음(1장) |
| 3-e | 인계에 이 판과 다음 판 | **했다** — 23 · 24번 |
| 4-a | 작업 트리 파일 안 건드림 | **다르게 했다** — 넘김 두 줄을 같은 글자로 커밋에 담아 그 두 파일이 status 에서 빠짐. 문서 넷에는 이 판 덩어리가 더해짐(앞 판 diff 는 그대로). `최근.md` 는 커밋 뒤 앞 판 것으로 되돌림 — 4장 |
| 4-b | 스테이징 안 된 채로 둔다 | **했다** — `tests/test_stage41.py` 만 스테이징된 그대로 |
| 4-c | 커밋은 conftest 와 문서만 | **다르게 했다** — 새 시험 `tests/test_stage42.py` 와 `tests/test_attachments.py` 독스트링 한 줄이 더 들어갑니다. 막는 코드에는 막히는 쪽 시험이 함께 있어야 합니다(11-3) |
| 가) | 멈춘다 · 안 멈춘다 · 같은 판 | **했다** — 7장 |
| 나) | 표마다 같다 | **했다** — 7장 |
| 다) | 밖에서 200 | **했다** — 7장 |
| 라) | 비운판 그대로 | **했다** — 7장 |
| 마) | pytest · 글검사 7/7 | **했다** — 8장 |
| 바) | 검토자 원문 | **했다** — 11장 |
| (끝줄) | 커밋하고 푸시 · 마무리는 11-3 | **했다** — 10장 · 이 보고와 덩어리 |

---

# 10. 바뀐 파일

```
$ git diff --name-only 5c3c517..HEAD
CLAUDE.md
docs/review/2026-09-13-사고수습.md
docs/review/최근.md
docs/봐둘것.md
docs/세어둠.txt
docs/옛말-넘김.txt
docs/옛말.md
docs/인계.md
tests/conftest.py
tests/test_attachments.py
tests/test_stage42.py
```

## 10-1. 검토를 받고 고친 것

| 라벨 | 무게 | 어떻게 |
|---|---|---|
| [A] | 고쳐야 함 | a06 독스트링을 「conftest 안에서」 로 좁히고, 대입 안의 부름·별칭은 못 본다고 적었습니다 |
| [B] | 고쳐야 함 | `test42_a07_세션_첫머리에서도_검사가_돈다` 를 더했습니다. 고장을 심어 봤습니다 — ① 훅을 지움 ② 훅이 검사를 안 부름: **둘 다 빨개짐**. 같은 판에 ③ 판정을 늘 지나가게 하면 a01 이 빨개지는 것도 봤습니다 |
| [C] | 고쳐야 함 (절차) | 커밋을 `git add` 로 만들지 않았습니다 — 4장. `git show --stat HEAD` 와 커밋 뒤 `git status` 는 채팅 덩어리에 싣습니다(이 보고는 그 커밋 안에 있어 스스로를 못 적습니다) |
| [D] | 봐 둘 것 | 2-2 끝과 7장 가) 에 갈라 적었습니다 — **e2e 로 막은 것은 `pytest_sessionstart`, `drop_all` 앞 검사는 함수·자리 시험으로만** |
| [E] | 봐 둘 것 | CLAUDE.md 11-3 의 그 줄을 「그 검사는 app 의 엔진만 봅니다 — 시험이 따로 연 엔진이나 업로드 폴더는 안 봅니다」 로 고쳤습니다. 표본 시험은 안 고쳤습니다(사람칸 · AZ-b) |
| [F] | 봐 둘 것 | 그대로 둡니다. 살아 있는 파일에서 다른 말로 쓴 같은 주장은 검토자도 못 찾았습니다 |
| [G] | 봐 둘 것 | 얼린 판 `docs/review/2026-09-13-사고수습.md` 를 커밋에 담았습니다 — 10장 목록 |
| [H] | 봐 둘 것 | 앞 판 보고의 사본을 `data/최근-회차넘나듦판.real.md`(gitignore)에 떴습니다. 작업 트리의 `최근.md` 와 바이트가 같은 것을 `cmp` 로 봤습니다 |
| [I][J] | 봐 둘 것 | 9장 대조 표에 「다르게 했다」 로 적었습니다. **HEAD 가 글검사에서 빨간 것은 이 창이 돌려 봤습니다** — 4장 |
| [K] | 사소 | a01 의 `or` 를 빼고, 연 파일 경로가 말에 있는지만 봅니다. 나머지 셋은 그대로 |

---

# 11. 검토자 원문

**커밋 전 검토를 받았고 답을 통째로 싣습니다.** 한 글자도 안 가렸습니다.

```
━━━ 검토자 여기부터 ━━━ 대상: 5c3c517 위 사고 수습 판(worktree) · pytest 안 돌림 · 운영 DB 안 엶

막아야 함 — 없음

고쳐야 함

[A] tests/test_stage42.py a06 독스트링이 실제보다 넓게 말합니다 (고쳐야 함)
  「새 drop_all 이 검사 없이 생기면 여기서 빨개진다」 라고 적었는데, 훑는 것은
  inspect.getsource(conftest) 하나뿐입니다. 다른 시험 파일에 drop_all 이 생기면
  초록입니다. 범위를 넓히는 것은 답이 아닙니다: 시험 파일들은 tmp_path 엔진에
  drop_all 을 부를 수 있고 그 경로는 _TMP 밖이라 멈춥니다. 독스트링을
  「conftest 안의」 로 좁히는 것이 맞습니다. 대입 안의 부름(x = ...drop_all())이나
  별칭은 ast.Expr 로 안 걸리는데, 이것도 같은 줄에 적어 두면 됩니다.

[B] pytest_sessionstart 를 재는 시험이 없습니다 (고쳐야 함 · 가볍게)
  test_stage42 의 여섯 개는 전부 함수와 drop_all 앞 자리만 봅니다.
  tests/conftest.py 에서 pytest_sessionstart 를 지워도 모두 초록입니다.
  그런데 사고의 입구(-p)를 실제로 막은 것은 그 훅이었습니다(바깥 재현이 exit 3).
  SessionLocal·init_db 로 운영에 쓰는 시험을 막는 것도 그 훅뿐입니다.
  a06 과 같은 방식으로 한 줄 더 두기를 권합니다: conftest.pytest_sessionstart 가
  있고 그 몸에 _시험DB가_아니면_멈춘다 부름이 있는가.
  11-3 「③ 검사가 볼 것을 실제로 보고 있는가」 의 자리입니다.
  tests/conftest.py 가 첫 conftest(initial conftest)로 읽히는지는 코드로 확인했습니다.
  pytest.ini 의 testpaths=tests 이고, 인자를 줘도 그 조상 폴더의 conftest 는
  첫 conftest 로 읽힙니다. 그래서 지금 훅이 불리는 것은 맞습니다.
  모자란 것은 「누가 지우면 아무도 모른다」 는 점입니다.

[C] 커밋 절차: 인덱스에 tests/test_stage41.py 가 올라가 있습니다 (고쳐야 함 · 절차)
  본 저장소에서 git diff --cached --name-only 를 돌리면 tests/test_stage41.py 가 나옵니다.
  「HEAD 판 + 이번 덩어리」 로 트리를 따로 만들어 커밋하는 계획이면 괜찮습니다.
  그러나 도중에 평범한 git add / git commit 을 쓰면 앞 판의 시험이 섞입니다(지시 4).
  커밋한 뒤 git show --stat HEAD 에 test_stage41 이 없는지 보고에 적어 주세요.
  같은 까닭으로 커밋 뒤 git status 도 적어 주세요.
  conftest·test_stage42·test_attachments 는 작업 트리와 HEAD 가 같아야 하고,
  앞 판 파일만 M 으로 남아야 합니다.
  작업 트리의 conftest 가 HEAD 와 다르게 남으면 다음 판이 옛 conftest 로 돌 수 있습니다.
  지금 본 저장소 작업 트리에는 새 conftest 가 들어 있어서 그 위험은 없어 보입니다.

봐 둘 것

[D] 수용 기준 가) 는 「같은 판에서 잰다」 로는 채워졌습니다. 다만 drop_all 앞 검사는
  끝에서 끝까지(e2e) 한 번도 안 탔습니다.
  운영꼴 경로로 멈추는 것(a01)과 정상 경로로 지나가는 것(a02·a03·a04)이 한 판에 있습니다.
  그런데 client 픽스처 안의 검사는 sessionstart 가 이미 통과시킨 엔진만 봅니다.
  그래서 지금 구조에서는 세션 도중에 app.db.engine 이 바뀌어야만 발동합니다.
  그렇게 바꾸는 시험은 찾지 못했습니다. grep 한 결과는 이렇습니다.
  - setattr(...engine) 없음
  - importlib.reload 없음
  - test_stage24 는 스크립트 모듈의 config.DATABASE_URL 을 tmp_path 로 바꿀 뿐입니다
  - test_calendar 의 DCB_ 목록은 README 대조용 문자열입니다
  보고에 「drop_all 앞 검사는 함수와 자리로만 쟀고, e2e 로 막은 것은 sessionstart」 라고
  갈라 적으면 됩니다. 한 덩어리로 「막혔다」 고 적으면 읽는 사람이 둘을 구별하지 못합니다.

[E] 검사 밖에서 운영 파일을 직접 여는 시험이 있습니다
  tests/test_meetings_import.py 1392~1399 가 ROOT/data/app.db 에 따로 엔진을 만듭니다.
  읽기만 하고 AZ-b 에도 적혀 있습니다.
  _시험DB가_아니면_멈춘다 는 app.db.engine 만 보므로 이런 자리는 원리상 못 봅니다.
  CLAUDE.md 11-3 에 더한 줄은 「그 검사는 DB 만 봅니다」 라고 적었습니다.
  더 정확히는 「app 의 엔진만 봅니다」 입니다. 고칠지는 구현자 판단입니다.

[F] 옛말.md 의 새 항목은 표기가 글자 그대로라 좁습니다
  「테스트가 건드리지 않도록)」 · 「임시 경로로 잡아 두었으므로」 는 옛 문장을
  그대로 옮긴 것이라 다른 말로 다시 쓰면 안 걸립니다. 살아 있는 파일을 grep 했더니
  같은 주장은 README.md 46 에만 걸리는데, 그건 devserve 이야기라 다른 주장입니다.
  얼린 판 둘을 표기로 안 적은 판단은 맞습니다. 적었으면 넘김 두 줄이 필요했을 것입니다.

[G] AZ-a · 인계 23/24번이 가리키는 docs/review/2026-09-13-사고수습.md 가 아직 트리에 없습니다
  커밋할 때 얼릴 계획으로 읽힙니다. check_named 는 CLAUDE.md 만 보므로 봐둘것·인계에서
  빠진 경로는 아무 검사도 못 잡습니다. 커밋에 그 파일이 들어갔는지 확인해 주세요.

[H] 앞 판의 보고가 작업 트리에만 있습니다
  계획대로 작업 트리의 최근.md 를 앞 판 보고로 되돌려 두면, 그 보고는 커밋되지 않은
  유일본입니다. checkout·stash·restore 한 번에 사라집니다(0장 「기록을 남긴다」 와
  같은 자리). data/*.real.md 처럼 gitignore 되는 자리에 사본을 하나 떠 두기를 권합니다.

[I] 가져간 넘김 두 줄은 앞 판의 그 파일 변경 전부입니다
  본 저장소에서 git diff HEAD 로 확인했습니다. 두 파일 모두 앞 판 변경은 그 한 줄뿐이고
  worktree 의 줄과 글자가 같습니다. 그래서 「부분만 섞였다」 는 없고, 커밋 뒤 두 파일의
  작업 트리 차이가 깨끗이 사라집니다. 까닭(HEAD 가 글검사에서 빨갛다)도 섞음보다 무겁습니다.
  다만 지시 4 에 부딪히는 것은 맞으므로 보고의 지시문 대조 표에 「다르게 했다」 로 적어야 합니다.
  HEAD 가 정말 빨간지는 제가 글검사를 안 돌려서 확인하지 못했습니다.
  (check_names 가 data/ 의 DB 를 읽으려 할 수 있어 이번 판 조건상 피했습니다.)

[J] 커밋 범위가 지시 4 「conftest 와 문서만」 보다 넓습니다
  tests/test_stage42.py 는 1-e(같은 판에서 잰다)가 요구하는 것이라 빼면 그쪽이 깨집니다.
  tests/test_attachments.py 는 독스트링 고침이고, 옛 주장을 걷은 것이라 3-d 와 같은 결입니다.
  둘 다 대조 표에 「다르게 했다」 와 까닭을 적으면 됩니다. 뒤집을 까닭은 못 찾았습니다.

[K] 사소한 것
  - a01 의 `str(운영꼴.parent) in 말 or "app.db" in 말` 은 앞 조건이 없어도
    뒤 조건만으로 늘 참입니다. 연 파일 경로를 확인하려는 뜻이면 or 를 빼는 편이 정확합니다.
  - `from tests import conftest`: tests/__init__.py 가 있어서 같은 모듈이 돌아옵니다.
    import 방식이 importlib 로 바뀌면 conftest 가 다시 실행돼 _TMP 와 환경변수가
    새로 잡힙니다. 그때는 a03 이 빨개지므로 조용히 새지는 않습니다.
  - 멈춤 문구가 전체 DB 경로를 찍습니다. 보고에 옮길 때 사용자 폴더 이름만 조심하면 됩니다.
    바깥 주소나 비밀값은 없습니다.
  - scripts/backup.py 의 prune 은 app-*.db 만 고르므로 비운판-*.db 증거 사본은 자동으로 안 지워집니다.
    AZ-a 「안 지운다」 와 맞습니다.

없음으로 확인한 것
  - 판정의 경계: _TMP 로 좁힌 것, sqlite 메모리 DB 는 통과, sqlite 가 아니면 멈춤,
    returncode=3 — 코드와 문서가 같은 말을 합니다
  - pytest.exit 가 sessionstart·픽스처 준비·픽스처 정리 어디서 나도 세션이 멈추는 것은 pytest 규약상 맞습니다
  - CLAUDE.md 가 백틱으로 적은 _시험DB가_아니면_멈춘다 · drop_all 은 git ls-files 안의 코드에 있습니다(check_named 통과 조건)
  - 실명·전화·계좌·토큰·바깥 주소: 이번 diff 에서 못 찾았습니다

사람에게 묻는 것
  1. 지시 4 「커밋은 conftest 와 문서만」 에 test_stage42.py · test_attachments.py ·
     앞 판의 넘김 두 줄(HEAD 가 빨개서)을 함께 담는 것을 허락하는가 ([I][J])
  2. AZ-b — 시험과 고장 재현을 운영 DB 가 있는 기계 · 계정 · 폴더에서 계속 돌릴 것인가
  3. 운영 파일을 직접 여는 표본 시험(test_meetings_import)을 그대로 둘 것인가 ([E])
━━━ 검토자 여기까지 ━━━
```
