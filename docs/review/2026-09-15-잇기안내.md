사람이 할 것
노트북에서 직접: 둘 — ① 총무님이 운영 지출 화면에서 못 이은 30 건을 잇는다(`docs/재정-잇기-안내.md` · 다 이으면 이은 수와 미지정으로 둔 수를 알려 준다) ② 개발 DB 를 지우고 seed 를 다시 만드는 일이 앞 판에서 넘어와 아직 남았다(얼린 판 `docs/review/2026-09-14-계좌게이트.md` 5장)
관리자 권한: 없음 — 재시작은 이 창에서 됐다(4장)
정할 것: 넷 — ① 등록 폼의 금액 칸도 1원 단위로 열지(지금 등록 10원 · 고치기 1원으로 갈림 · 5장) ② 잇기로 덮이는 시트의 구분·항목·세부항목-1 옛 글자를 활동 기록에 남기게 할지, 시트·짝 표·사본을 그 자리로 둘지(짝 표 파일은 백업 밖 · 5장) ③ 되돌리는 사본을 얼마나 지킬지 — 코드는 기한 없이, 문서는 「잇기가 끝날 때까지」(5장) ④ 다음 판(16번 구매목록이거나 다른 것)
막혀서 못 한 것: 둘 — ① 수용 기준 가) 의 「개발 서버 화면을 실제로 열어 잰다」 를 못 했다 — 이 창은 도는 서버(개발 포함)의 로그인 칸에 비밀번호를 안 넣고(11-2), 개발 DB 는 계좌 꼴 게이트에 걸려 devserve 가 안 뜬다. 앱을 그 자리에서 만드는 시험으로 서버 경로와 그려진 폼 HTML 까지만 쟀다(2장 · 3장) ② docs/checks/drawer.js · phone.js 를 못 돌렸다 — 로그인이 필요하다. 이 판은 지출 고치기 폼의 금액 칸 속성 하나를 바꿨다(4장)

작성 시각: 2026-09-15 10:25 KST
커밋: ad62699 (고치기 금액 칸 · 시험) · af1a473 (안내문 · 문서) · 이 보고는 그다음 커밋
작업: 못 이은 지출 30 건을 화면에서 실제로 잇는다

---

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 「예산 항목 미지정」 — 지출 목록 묶음 머리(`expenses.html`)뿐이고 필터 이름에는 없다(`budget.FILTERS`). 고치기 저장은 `routers/expenses.py` 의 `update_expense` 하나. 금액 칸 `step` 은 등록 폼과 고치기 폼 둘(고치기만 바꿈) |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 지출 화면 한 곳 |
| 그 값을 세어 둔 문서 | CLAUDE.md 7-4(고치기) · 7-5(짝 json 줄) · 봐둘것 BB-h · 인계 머리 · 순서표 6 |

# 1. 한 것

- **0-a 코드로 읽음** — 「예산 미지정」 필터 단추는 **없다.** 지출 화면 「전체」 에서 예산 항목으로 묶고, 항목이 없는 묶음 머리가 「예산 항목 미지정」 이다. 고치기 폼의 예산 항목 선택지는 산 예산 항목 전부 + 「미지정」
- **운영을 읽기만 해서 셈**(`mode=ro` · 건수만): 미지정 산 지출 30 · 30 전부 목록 제목이 「미지정」(세부항목-2 빔) · 30 전부 시트 구분 글자가 DB 에 남음(화면에는 안 그려짐) · 부서 붙음 0 · 식대 1 · (금액, 날짜) 겹침 0
- **0-b 시트를 다시 재어 꼴로 나눔**(건수만): ㄱ 같은 글자의 예산 줄이 둘 이상 4 · ㄴ 같은 구분·항목에 세부 글자가 겹침 2 · ㄷ 같은 구분·항목에 세부가 다름 10 · ㄹ 다른 구분·항목에 세부 글자가 겹침 5 · ㅁ 같은 구분만 9 · ㅂ 구분부터 없음 0 (합 30). 후보가 뚜렷한 꼴(ㄱ · ㄴ · ㄹ) 11 · 같은 항목 안에서 고르는 꼴(ㄷ) 10 · 항목이 없을 수 있는 꼴(ㅁ) 9
- **1 앱을 그 자리에서 만드는 시험**(`tests/test_stage50.py` · 11-2 가 로그인 규칙의 대상 밖으로 정한 꼴): 목록에 미지정 묶음과 그 지출의 고치기 폼이 그려지고 선택지에 산 항목이 있으며 미리 골라진 것이 없다 · 금액 칸이 10원 단위가 아닌 금액을 받는다 · 폼 값으로 저장하면 이어지고 부서·금액·지원금액은 그대로 · 이은 뒤 그 항목 묶음 머리 아래로 간다 · 부서 없는 지출도 총무팀이 잇는다
- **커밋 전 검토가 잡은 막힘을 고침** — 고치기 폼의 금액 칸이 `step="10"` 이라 10원 단위가 아닌 금액의 줄은 **브라우저가 저장을 막았다**(운영 셈: 못 이은 30 건 중 1 · 산 지출 전체 3). 고치기 폼만 `step="1"` 로 열었다. `update_expense` 는 안 건드렸다
- **3 부서 위험** — 없다. `update_expense` 는 부서를 받지 않는다. 대신 **항목이 바뀌면 구분·항목·세부항목-1 글자가 새 항목 글자로 덮이고, 그 옛 글자는 활동 기록에 안 남는다**(기록은 `budget_category_id` 만). 「미지정」 으로 되돌리면 그 칸이 빈다. 안내문에 굵게
- **2 안내문** `docs/재정-잇기-안내.md` — 준비(지출 화면 · 짝 표 · 원본 시트) · 한 건씩 순서 · 꼴별로 어디부터 볼지 · 항목이 없을 때 · 알아 둘 것 · 끝나면. 값은 없다
- **4 문서** — 인계 머리와 순서표 6 에 「재정짝표 30건 — 총무님이 화면에서 잇는 중」 · 봐둘것 BB-h 머리를 「사람이 실제로 잇기 시작함」(원문 유지 · 옛 라벨 괄호) · CLAUDE.md 7-5 짝 json 줄에서 안내문을 가리킴 · 운영 실행 판 보고 얼림

# 2. 수용 기준

| 기준 | 통과 | 무엇을 실제로 보나 |
|---|---|---|
| 가) 개발 서버에서 화면을 열어 항목을 골라 저장이 된다 | **△** | **개발 서버 화면은 못 열었다**(사람칸). 앱을 그 자리에서 만들어 **그려진 목록·폼 HTML**(`test50_a01`)과 **폼 값으로 저장하는 서버 경로**(`test50_b01` · `b02`)까지 쟀다. 브라우저의 제출 검사는 금액 칸의 `step` 하나만 HTML 에서 잰다 — `required` · `min` · 날짜 칸 같은 다른 제약은 안 잰다. 그 틈으로 `step="10"` 막힘이 검토에서야 드러났다 |
| 나) 부서가 같이 바뀌는지 코드로 · 안내문에 | O | `update_expense` 가 부서를 안 받음 · `test50_b01` 이 부서 그대로를 잼 · 안내문 5장 |
| 다) 안내문 | O | `docs/재정-잇기-안내.md` |
| 라) pytest 옵션 없이 · 글검사 7/7 | O | 6장 |
| 마) 검토자 원문 | O | 7장 |

# 3. 운영 자료

**읽기만 했다** — `data/app.db` 를 `mode=ro` 로 열어 건수만 셌고, 시트는 이 창의 스크립트 함수로 읽어 꼴만 셌다(값은 안 찍음). 운영의 30 건은 이 창이 잇지 않았다 — 총무님이 화면에서 한다.

# 4. 재시작

고치기 폼 템플릿이 바뀌어 12-1 대로 했다.

- PID 26316 → **17664** · 「성공 — PID 가 바뀌었습니다」 · 2026-09-15 10:05:38 · `/login` 200
- 템플릿을 고친 뒤에 재시작했다
- 보고를 쓴 뒤 `app/account_shape.py` 를 고쳐(7-3) 한 번 더: PID 17664 → **18592** · 「성공 — PID 가 바뀌었습니다」 · 2026-09-15 10:34:47 · `/login` 200. 그 뒤 고친 것은 이 보고뿐
- **돌고 있는 서버의 고치기 폼이 `step="1"` 인지는 못 봤다** — 지출 화면은 로그인이 필요하다. PID 가 바뀐 것과 운영에서 템플릿을 켤 때 한 번 읽는 것(11-2)으로 새 폼이 선다고 본다. 총무님이 첫 잇기에서 그 1 건이 저장되는지로 확인된다(안내문 5장)

# 5. 판단이 들어간 자리

- **지시문 밖 템플릿 한 줄** — 지시문은 「다시 만들지 않는다」 였지만, `step="10"` 이 그대로면 총무님이 안내문대로 해도 1 건이 저장되지 않는다. 고치기 폼의 속성 하나만 바꿨다. **등록 폼은 그대로 10원 단위다** — 새로 적는 영수증은 앱 규칙을 따를 수 있어서다. 두 폼이 갈린 까닭은 고치기 폼 옆 주석에만 있다(정할 것 ①)
- **옛 시트 글자 덮임** — 운영 DB 안에서는 되돌릴 길이 없다(활동 기록에 없고 앱에 그 칸을 넣는 곳이 없다). 원본 시트 · 짝 표 파일(백업 밖) · 지키는판의 사본에는 남는다. 들인 뒤로 앱이 정본이라(7-1) 옛 글자의 앱 밖 자리는 정본이 아닌 곳이다(정할 것 ②)
- **되돌리는 사본의 기간** — 코드(`지키는판`)는 기한이 없고, 안내문·인계·BB-h 는 「잇기가 끝날 때까지」 다. 지시문은 「이 판이 끝날 때까지 그대로」 라 이 판은 손대지 않았다(정할 것 ③)
- **꼴 나눔** — 건수와 어디부터 볼지만 적었고 「어느 항목이 맞는지는 총무님이 정한다」 를 붙였다. 「뜻이 같은 다른 이름일 때가 많다」 는 잰 근거가 없어 「일 수 있다」 로 낮췄다(첫 검토)
- **안내문에서 「필터」 대신 「전체」 의 묶음 머리** — 지시문은 「예산 미지정 필터를 누르면」 이었는데 그런 필터가 없다. 새로 만들지 않고 있는 자리로 안내했다

# 6. 검사와 시험

- 글검사(커밋 전): 7/7 통과
- 새는지 검사(저장소 밖 도구 · 값 안 찍음): 안내문 · 시험 · CLAUDE.md · 인계 · 봐둘것에 새로 들어온 시트 값 0
- `tests/test_stage50.py` 3 passed — 고치기 폼을 `step="10"` 으로 되돌려 심으면 빨개진다
- stage46 · 47 · typescale 과 함께 돌림 — 통과(첫 돌림에서 b01 의 머리 글자 비교가 HTML 이스케이프로 빨개 고침)
- 보고를 다 채운 뒤 돌린 것: 글검사 — 처음엔 `check_names` 가 이 보고의 백업 이름을 계좌 꼴로 잡아 빨갰고(7-3 에서 고침) 고친 뒤 7/7 통과 · 새는지 검사(이 보고) 0 · pytest(옵션 없이 · 그 고침 뒤) `1576 passed, 3 warnings in 1506.33s (0:25:06)` — 경고 셋은 test25_e01 의 sqlite datetime adapter. 이 줄을 채운 뒤 글검사만 한 번 더 돌렸다

# 7. 검토자 원문

## 7-1. 첫 검토

```
━━━ 검토자 여기부터 ━━━ 범위 HEAD 012ea14 .. 스테이지(커밋 전) · 커밋 0개 · 파일 8개

본 것: git diff --cached 전부(얼린 판은 머리만) · app/routers/expenses.py(update_expense · _my_entry ·
_category_or_none · _바뀐것 · expense_list) · app/templates/expenses.html(묶음 머리 · 고치기 폼) ·
app/security.py assert_can_edit_department · app/domain/permissions.py can_edit_department_key ·
app/domain/budget_xlsx.py · scripts/backup.py 지키는판 · tests/test_stage47.py(판 · _고침).
운영 DB 는 mode=ro 로 열고 건수만 셌다. 시트와 짝 표 파일은 열지 않았다.

[막아야 함] app/templates/expenses.html:167 · docs/재정-잇기-안내.md 2장 6번 · tests/test_stage50.py:24
  — 미지정 30건 중 1건은 화면에서 저장이 안 된다
  고치기 폼의 금액 칸이 type="number" min="0" step="10" 이고 폼에 novalidate 가 없다.
  운영을 ro 로 세어 보니 미지정 산 지출 30건 중 1건이 10원 단위가 아니다(식대 줄은 아니다).
  산 지출 전체로는 3건이다.
  이런 줄은 브라우저가 값이 step 과 안 맞는다며 제출을 막는다. 안내문 2장 6번의 「다른 칸은 건드리지 말고
  저장」 을 그대로 따르면 그 줄은 저장이 안 된다. 금액을 바꿔 넘기면 지원금액을 다시 센다(7-4).
  시험은 이것을 못 잡는다. _고침 은 폼 HTML 이 아니라 DB 값으로 바로 POST 하고, 지어낸 금액도 23_450 이라
  step 에 맞는다.
  이것이 수용 기준 가) 「화면을 실제로 열어 잰다」 를 시험으로 바꿨을 때 새는 자리다. 브라우저가 막는 것은
  서버 시험에 안 보인다.
  고치는 곳은 코드(차례 5 의 폼 · 이 판 지시문은 update_expense 를 다시 만들지 말라고 했다)이거나 안내문이다.
  어느 쪽인지는 사람이 정한다. 적어도 안내문과 보고는 이 1건을 말해야 한다.

[막아야 함] docs/review/최근.md(스테이지에 없음) — 보고가 아직 이 판으로 안 쓰였다
  지금 파일은 앞 판(운영 실행) 그대로다. 그래서 사람칸 다섯 줄 · 지시문 대조 표 · 고치기 전 훑기 ·
  검토자 원문을 볼 수 없었다(검토-원칙 8-1 · 8-2 · 9). 커밋 전에 들어가야 한다.
  대조 표에 들어가야 할 것:
    1a 는 「다르게 했다」 — 개발 서버를 못 열었고 앱을 그 자리에서 만드는 시험으로 쟀다.
      까닭은 11-2 와 계좌 꼴 게이트로 devserve 가 안 뜬 것이다.
    가) 는 「통과」 가 아니다 — 위의 step 1건이 그 증거다.
    0a 의 「예산 미지정 필터」 는 없다는 사실.
  사람칸 「막혀서 못 한 것」 에 넣을 것:
    · 화면 실측을 못 했다
    · 개발 DB 를 다시 만드는 일이 남아 있다(앞 판에서 넘어옴)
  구현자 요약의 (a) 서술 방향은 정직하다. 11-2 는 conftest 의 login_as 처럼 앱을 그 자리에서 만들고
  값이 지어낸 것이면 대상 밖으로 정했고, test_stage50 은 그 꼴이다.
  다만 「시험으로 가) 를 쟀다」 가 아니라 「가) 는 못 쟀고 서버 경로만 쟀다」 로 적혀야 한다.

[고쳐야 함] docs/재정-잇기-안내.md 머리 셋째 줄 — 「틀리게 이어도 다시 고치면 됩니다 — … 「미지정」 을 고르고 저장합니다」
  expenses.py:480-484: 항목이 바뀌면 level1/level2/level3a 를 새 항목 글자로 덮는다. 「미지정」 을 고르면
  category 가 None 이라 셋이 전부 None 이 된다.
  그래서 잘못 이은 뒤 미지정으로 되돌리면 시트 글자가 안 돌아온다. 결산 엑셀 지출 상세내역
  (budget_xlsx.py:91)의 구분·항목·세부1 칸이 빈 채로 남는다. 활동 기록에도 없다(_바뀐것 은 새값에 든 칸만 적는다).
  「다시 고치면 된다」 는 예산 항목 연결만 참이다. 5장의 굵은 줄과 머리 줄이 서로 다른 말을 한다.

[고쳐야 함] docs/재정-잇기-안내.md 머리 둘째 줄 — 「부서 리더 계정으로는 고치기가 안 뜰 수 있습니다」
  permissions.py:218 은 대상 부서 키가 없으면 admin 이 아닌 사람에게 False 를 준다. expenses.html:152·162
  can_edit 도 같은 판정이다.
  운영 ro 셈으로 30건 전부 부서가 없다(부서 붙음 0).
  그러니 「안 뜰 수 있다」 가 아니라 「안 뜬다」 다. 흐리게 적으면 리더가 시도하고 헤맨다.

[봐 둘 것] (c) 덮임과 기록 없음이 되돌릴 수 없는 손실인가
  운영 DB 안에서는 되돌릴 길이 없다. 활동 기록에 옛 글자가 없고, 앱에 옛 글자를 다시 넣는 칸도 없다(폼에 level1/2/3a 없음).
  바깥에는 셋이 남는다.
    · 원본 시트(구글)
    · data/재정짝표.real.md — 저장소 밖이고 11-2 백업 대상 표에도 없다
    · 지키는판의 20260915-002352 사본
  시트가 살아 있는 한 잃는 것은 아니다. 다만 짝 표 파일은 백업되지 않는다.
  「막아야 할 일」 인지는 사람이 정한다.
    · 막는 길: 옛 글자를 기록에 남기기
    · 막지 않는 길: 시트를 정본으로 남겨 두기
  시트가 정본인가: CLAUDE.md 13장은 「들인 뒤로는 앱이 정본」 이라 적는다. 그러면 옛 글자의 유일한 앱 밖
  자리가 정본이 아닌 곳이 된다. 이 점을 함께 보면 된다.

[봐 둘 것] scripts/backup.py:98 과 docs/재정-잇기-안내.md 6장 — 사본을 지키는 기간이 두 말이다
  코드 주석: 「개수·크기 정리에서 빼 지우지 않는다」(기한 없음).
  안내문과 인계·봐둘것 BB-h: 「잇기가 끝날 때까지」.
  지시문도 「이 판이 끝날 때까지」 다. 풀지 말지는 사람칸의 정할 것으로 이미 올라가 있으니, 코드 쪽 까닭 줄이
  그 결정 전까지 맞는 말인지만 보면 된다.

[봐 둘 것] (d) docs/재정-잇기-안내.md 3장 ㄷ 줄 — 「뜻이 같은 다른 이름일 때가 많습니다(예: 줄임말 · 띄어쓰기 · 순번)」
  꼴 나눔 자체는 건수와 어디부터 볼지만 말하고, 「어느 항목이 맞는지는 총무님이 정합니다」 를 적었다.
  사람 판단을 대신하지 않는다.
  다만 이 줄은 「많다」 는 경향을 주장하는데, 그 근거가 된 셈이 보이지 않는다. 사람이 이 줄을 믿고 비슷해
  보이는 이름을 고르게 된다. 잰 것이 아니면 빼거나 「일 수 있습니다」 로 낮추는 것이 맞다.
  구현자가 짝 표를 보고 적은 것이면 그대로 둬도 된다 — 물음이다.

[봐 둘 것] tests/test_stage50.py:53 — 「목록에서 미지정 묶음 사라짐」
  판에 미지정 지출이 하나뿐이라 성립하는 단언이다. 지금은 맞지만 fixture 가 늘면 뜻이 흐려진다.
  그 지출이 c2 묶음 아래에 섰는지 보는 단언이 더 곧다.

맞는지 확인한 것 — 안내문 사실(b):
  · 「예산 미지정」 필터가 없다: 맞다. FILTERS 에 없고, expense_list 가 budget_category_id 로 묶어 None 묶음
    머리가 「예산 항목 미지정」 이다(expenses.html:123).
  · 목록 제목 「미지정」: 맞다. 142줄이 level3b → 항목 이름 → 「미지정」 순이다. ro 로 level3b 빈 글자 0 · 공백 0.
  · 부서가 안 바뀐다: 맞다. update_expense 가 department_id 를 받지 않는다.
  · 다른 칸이 안 바뀐다: 조건부로 맞다. 폼이 지금 값을 채워 보내고 _바뀐것 이 같은 값은 건너뛴다.
    ro 로 30건의 계좌·지출자·비고 칸에 빈 글자·앞뒤 공백이 0 이라 계좌꼴 검사도 안 탄다.
    식대 1건은 인원이 있고 금액이 그대로면 다시 세지 않는다.
    예외는 위 step 1건이다.
  · 취소된 항목이 선택지에 없다: 맞다. 171줄이 summary.categories 만 싣고, 서버도 400 을 낸다.
  · 0원 항목의 잔액이 음수로 붉다: 맞다. 121줄 remaining<0 → overrun. 산 0원 항목이 운영에 이미 있다.
  · 엑셀 지출 상세내역에 level 칸: 맞다. budget_xlsx.py:91.
  · 「지출 97 건 중 30 건」 · 부서 0 · (금액·날짜) 겹침 0: 97 과 부서 0 은 ro 셈과 같다. 겹침 0 은 구현자 셈이다.

(e) 10장 수 규칙: 스테이지된 글에 셋 이상의 수에 「곳」「군데」 가 붙은 줄이 없다. 넘김 두 줄은 얼린 판 한 줄씩이고
  이유가 채워져 있으며 넘기는 말을 인용하지 않는다.
실명·값: 안내문·시험·인계·봐둘것·CLAUDE.md 에 이름·계좌·실제 금액이 없다. 시험 값은 지어낸 것이다. 새로 들어온
  시트 값은 보지 못했다.

사람에게 묻는 것
  1. step=10 에 걸리는 1건(산 지출 전체로는 3건)을 어떻게 할까.
     · 폼의 step 을 고친다 — 코드 · 이 판 밖
     · 안내문에 「이 줄은 금액을 바꾸지 말고 …」 우회를 적는다
     · 이 줄만 이 창이 다른 길로 잇는다
  2. 옛 시트 글자(구분·항목·세부1)가 덮이고 기록에 안 남는 것을 둘까.
     · 둔다: 시트와 짝 표를 옛 글자의 자리로 둔다. 짝 표 파일은 백업 밖이다
     · 막는다: 고치기가 옛 글자를 활동 기록에 남기게 한다
  3. 잘못 이은 줄을 「미지정」 으로 되돌리면 엑셀 구분 칸이 빈다. 안내문에 그대로 적을까, 되돌리지 말고 알려
     달라고 적을까.
  4. 되돌리는 사본의 지키는 기간 — 코드는 영구, 문서는 「잇기가 끝날 때까지」. 어느 쪽인가.
━━━ 검토자 여기까지 ━━━
```

## 7-2. 다시 보기 (검토 뒤에 고친 차이만)

```
━━━ 검토자 여기부터 ━━━ 다시 보기 · 범위 012ea14 .. 스테이지(커밋 전) · 앞 검토 뒤에 고친 것만 · 커밋 0개 · 파일 9개(새로 들어온 것 app/templates/expenses.html)

본 것: git diff --cached 012ea14 중 app/templates/expenses.html · tests/test_stage50.py · docs/재정-잇기-안내.md ·
docs/봐둘것.md BB-h 머리. tests · app · docs/checks · CLAUDE.md 에서 step="10" 을 grep 했다.
운영 DB 는 이번에 안 열었다(앞 검토의 셈으로 충분했다).

앞 검토의 발견이 닫혔나

  [막아야 함 1 · step=10] 닫힘
    expenses.html:168 의 고치기 폼 금액 칸이 step="1" 이 됐다. min="0" 이 step 의 기준이라 정수 금액은 전부 받는다.
    test50_a01(tests/test_stage50.py:43-46)은 그려진 폼 HTML 에서 금액 칸을 뽑고, 10원 단위가 아닌 금액이 step 에
    맞는지 잰다. step="10" 으로 되돌리면 빨개진다(구현자가 심어 확인했다고 적었다. 나는 돌리지 않았다).
    167줄 주석 안의 step="10" 글자는 Jinja 주석이라 그려진 HTML 에 안 나온다. 정규식이 잘못 잡을 자리가 없다.
    update_expense 는 안 건드렸다.

  [막아야 함 2 · 보고] 이 다시 보기 뒤에 쓴다고 했다
    그러니 아직 열린 채다. 사람칸 · 대조 표 · 원문이 들어간 뒤에 커밋해야 한다.
    가) 를 「서버 경로와 폼 HTML 까지만 쟀다」 로 적는 것이 맞다. 브라우저의 제출 검사는 여전히 기계로 안 잰다.
    a01 은 step 속성 하나만 본다. required · min · 날짜 칸 같은 다른 제약은 안 본다.

  [고쳐야 함 · 「미지정」 으로 되돌리기] 닫힘
    안내문 머리 11-12줄이 「맞는 항목을 다시 고르고 미지정으로 되돌리지 말 것 · 엑셀 칸이 빈다」 로 바뀌었다.
    코드(expenses.py:480-484 · category None → level 셋 None)와 맞는다.

  [고쳐야 함 · 리더] 닫힘
    「안 뜬다」 가 permissions.py:218 과 맞는다.

  [봐 둘 것 · ㄷ 「많습니다」] 닫힘
    「일 수 있습니다 … 아니면 4장」 으로 바뀌었다.

  [봐 둘 것 · b01 단언] 닫힘
    까닭 문구가 붙었고, c2 묶음 머리를 보는 단언이 더해졌다(tests/test_stage50.py:69-71).

새로 본 것

[봐 둘 것] docs/재정-잇기-안내.md 5장 셋째 항목 — 「2026-09-15 전에는 30 건 중 1 건이 저장되지 않았습니다 … 그날 고쳤습니다」
  들여온 날도 2026-09-15 라 「그날 전」 에는 그 30건이 아직 없었다. 날짜로 적으면 뜻이 틀린다.
  뜻은 「이 고침(재시작) 전에는」 이다. 봐둘것 BB-h 에 붙인 줄(「그 판에 고친 것 하나」)은 판으로 가리켜 괜찮다.

[봐 둘 것] app/templates/expenses.html:60 — 등록 폼은 step="10" 그대로다
  구현자가 일부러 남겼다고 적었다. 이 판의 30건과는 무관하다.
  다만 운영의 산 지출 전체에 10원 단위가 아닌 줄이 앞 셈으로 3건 있다. 그런 금액의 영수증을 앱에서 새로
  등록하면 브라우저가 막는다. 막는 것이 뜻인지(시트 관행) 두 폼이 갈린 것인지는 사람이 정할 일이다.
  같은 칸이 두 폼에서 다른 규칙인 까닭이 167줄 주석에만 있다.

[봐 둘 것] docs/재정-잇기-안내.md 5장 둘째 항목 — 「그 칸 하나만 바뀝니다」
  바로 아래 항목이 구분·항목·세부1 글자가 덮인다고 말한다. 한 절 안에서 두 말이다.
  앞 검토 때부터 있던 줄이다(이번에 고친 줄은 아니다). 「예산 항목(과 그에 딸린 구분 글자)만」 으로 맞추면 된다.

[봐 둘 것] 재시작 확인 — 템플릿이 바뀌어 12-1 재시작을 했다고 적었다
  8000 을 쥔 프로세스의 PID 는 읽혔다. 시작 시각은 이 창에서 안 읽혀(권한), 템플릿 수정 시각(10:03)보다 뒤에
  섰는지는 내가 확인하지 못했다.
  돌고 있는 서버의 고치기 폼이 step="1" 인지는 로그인이 필요해 이 창이 못 본다.
  보고에 전후 PID 와 무엇으로 확인했는지(또는 못 했는지)가 들어가야 한다(11-3 2단계).
  화면 코드가 바뀐 판이라 docs/checks/drawer.js · phone.js 를 못 돌린 것도 「막혀서 못 한 것」 에 남는다.

실명 · 값: 이번에 바뀐 줄에 이름 · 계좌 · 실제 금액이 없다. 「30 건 중 1 · 전체 3」 은 건수다.
10장 수 규칙: 셋 이상의 수에 곳 · 군데가 붙은 새 줄이 없다.

막아야 함: 보고가 아직 없는 것 하나(이 다시 보기 뒤에 쓰기로 한 것). 코드와 안내문에는 없다.

사람에게 묻는 것
  1. 등록 폼의 금액 칸도 1원 단위로 열까, 10원 단위를 등록 규칙으로 둘까(두 폼이 지금 갈려 있다).
  2. 옛 시트 글자가 기록에 안 남는 것 · 되돌리는 사본을 얼마나 지킬지 — 보고 「정할 것」 으로 간다고 했다. 그대로 사람 몫이다.
━━━ 검토자 여기까지 ━━━
```

## 7-3. 두 번째 검토 뒤 고침 (다시 보이지 않고 적는다 · 11-3 4단계)

- `docs/재정-잇기-안내.md` 5장 — 「2026-09-15 전에는 … 그날 고쳤습니다」 → 「이 안내를 만든 판의 고침(2026-09-15 · 앱 재시작) 전에는 … 그 판에 고쳤습니다」
- 같은 5장 — 「그 칸 하나만 바뀝니다」 → 「예산 항목과 그에 딸린 구분·항목·세부항목-1 글자(바로 아래)만 바뀝니다」
- `app/account_shape.py` · `tests/test_stage49.py` — 보고를 다 쓴 뒤 글검사의 `check_names` 가 이 보고(검토자 원문 7-1)의 백업 이름(날짜8자리-시각6자리)을 계좌번호 꼴로 잡았다. 원문은 못 고치므로 **날짜 머리에 그 꼴도 넣어 뺐다**(`20260915-002352` 를 빼는 조각 시험에 더함). 시트 계좌 64 줄은 여전히 전부 검사에 걸린다. `app/` 파일이라 재시작을 다시 했다(4장)
- 등록 폼 · 옛 글자 기록 · 사본 기간은 고치지 않고 사람칸 「정할 것」 으로 올렸다. 재시작 확인의 한계는 4장에 적었다

# 8. 바뀐 파일 (`git diff --name-only 012ea14..` 이 보고 커밋 · 보고 커밋에 `app/account_shape.py` · `tests/test_stage49.py` 가 함께 든다)

- CLAUDE.md
- app/account_shape.py
- app/templates/expenses.html
- docs/review/2026-09-15-운영실행.md
- docs/봐둘것.md
- docs/세어둠.txt
- docs/옛말-넘김.txt
- docs/인계.md
- docs/재정-잇기-안내.md
- tests/test_stage49.py
- tests/test_stage50.py

# 9. 지시문 대조

| 항목 | 무엇을 시켰나 | 어떻게 |
|---|---|---|
| 0-a | 「예산 항목 미지정」 필터로 30건을 코드로 읽음 | **다르게 했다** — 그런 필터가 없다. 「전체」 의 미지정 묶음이 그 자리이고, 운영을 ro 로 세어 30 건의 모양을 건수로 적었다 |
| 0-b | 짝 표로 후보와 못 잇는 것을 가름 · 건수만 | **했다** — 꼴 다섯(1장) |
| 1-a | 개발 서버 화면에서 항목을 골라 저장이 되는지 잼 · 다시 만들지 않음 | **다르게 했다** — 개발 서버 화면을 못 열었고(11-2 · 개발 DB 게이트), 앱을 그 자리에서 만드는 시험으로 서버 경로와 그려진 폼까지 쟀다. `update_expense` 는 안 건드렸고, 저장을 막던 고치기 폼 금액 칸 속성 하나를 고쳤다 |
| 1-b | 운영 30건은 총무님이 | **했다** — 이 창은 안 이었다 |
| 2-a | 어떤 순서로 보나 · 필터로 목록이 뜨나 코드로 | **다르게 했다** — 필터가 없어 「전체」 의 묶음 머리로 안내했다 |
| 2-b | 후보 / 못 잇는 것 안내 한 장 · 값 없이 | **했다** — 안내문 3장 · 4장 |
| 2-c | docs/재정-잇기-안내.md · 단계별 | **했다** |
| 3-a | 항목을 바꾸면 부서가 바뀌나 코드로 | **했다** — 안 바뀐다. 대신 시트 글자가 덮이는 것을 찾았다 |
| 3-b | 위험 굵게 · 없으면 없다고 | **했다** — 부서는 없다고, 시트 글자 덮임은 굵게 |
| 4-a | 인계에 「재정짝표 30건 — 총무님이 화면에서 잇는 중」 | **했다** — 머리와 순서표 6 |
| 4-b | 봐둘것 BB-h 「사람이 실제로 잇기 시작함」 · 원문 유지 | **했다** |
| 가~마 | 수용 기준 | **다르게 했다** — 가) 는 서버 경로와 폼 HTML 까지만(2장 △) · 나~마 는 했다 |
| 끝줄 | 운영 자료 읽기만 · 앱을 안 고치면 재시작 안 함 | **다르게 했다** — 운영은 읽기만 했고, 템플릿을 고쳐 재시작했다(4장) |
| 끝줄 | 마무리는 11-3 대로 | **다르게 했다** — drawer.js · phone.js 를 못 돌렸다(사람칸) |
