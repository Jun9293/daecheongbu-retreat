사람이 할 것
노트북에서 직접: 없음
관리자 권한: 없음
정할 것: phone.js 거짓 실패를 점검 쪽에서 고칠지 · 보드 · 달력에 main 을 둘지 (봐둘것 BE-a)
막혀서 못 한 것: 없음 — 계정 만들기 · 로그인은 사람이 했다 (1장)

작성 시각: 2026-09-17 18:00 (KST)
커밋: 260a653 다음 — 이 판의 커밋들(8장 파일 목록)
작업: 개발 서버 로그인 조치 · 화면 점검 (개발 환경 · 자유 조작)

---

**이 판은 코드를 안 고쳤다.** 운영 DB 는 안 건드렸고(수정 시각 15:20:10 · 활동 기록 66 · 지출 109 그대로 — 17:56 확인), 운영 앱은 재시작하지 않았다(PID 2776).
**계정의 아이디 · 비밀번호는 이 창이 모르고 어디에도 없다** — 계정은 사람이 만들고 로그인했다.

# 0. 고치기 전 훑기 (11-3 0단계)

| 무엇을 찾나 | 나온 것 |
|---|---|
| 같은 이름을 쓰는 데 | 없음 — 코드를 안 고쳤다 |
| 같은 부분 템플릿을 쓰는 화면 | 없음 — 화면을 안 고쳤다 |
| 그 값을 세어 둔 문서 | 「화면 점검이 남았다」 를 적은 자리 — CLAUDE.md 13장 · 인계 맨 위와 「남은 것」 표. 셋 다 결과로 맞췄다. CLAUDE.md 11-2 의 「phone.js 에 아직 안 심은 자가시험 둘」 은 이 판이 안 한 일이라 그대로 뒀다 |

# 1. 0 · 1단계 — 개발 서버와 계정

- **0a** — 127.0.0.1:8001 이 응답하지 않았다. 오늘 11:25 부팅 뒤 개발 서버가 안 떠 있었다(개발 서버는 스스로 서는 자리가 없다)
- **0c** — `scripts/check_dev_db.py` 를 개발 폴더로 돌려 exit 0. 9/15 의 계좌 게이트 문제는 재발하지 않았다
- **0b** — `.claude/launch.json` 의 「대청부수련회-개발」(devserve.bat)로 띄웠다. 로그인 화면 200
- **0d** — 게이트가 통과해 개발 DB 를 지우지 않았다
- **1a · 1b · 1c** — **이 창은 하지 않았다.** 지시문은 이 창이 개발 관리자를 만들고 그 비밀번호로 로그인하라고 했지만, 이 창의 고정 안전 규칙(계정을 만들거나 비밀번호로 로그인하지 않는다 · 사람이 허락해도)과 CLAUDE.md 11-2 가 막는다.
  멈추고 사람에게 넘겼고, **사람이 기존 개발 관리자로 로그인하고 부서 리더 계정을 만들어 로그인했다.** 이 창은 사람이 들어가 둔 Chrome 창에서만 조작했다(11-2 가 남겨 둔 길)
- 세션은 `localhost:8001` 의 Chrome 에만 있었다 — `127.0.0.1:8001` 과 이 창의 앱 안 브라우저에는 없었다(쿠키가 호스트마다 따로)

**개발 경로 확인 (2e)** — 8001 을 쥔 서버(PID 17104)와 그 워커의 환경변수가 `DCB_DATA_DIR`=개발 폴더 · 개발 쿠키 이름이다(psutil 로 읽음).
조작마다 개발 DB 를 `mode=ro` 로 세어 그 조작만큼 바뀐 것을 봤고, 운영 DB 는 매번 같았다. 개발 DB 는 계정 1 → 2(사람이 만든 리더) · 지출 2 → 3 · 영수증 1 → 2 · 활동 기록 14 → 32.

# 2. 2단계 — 화면 점검

**점검 스크립트를 어떻게 실었나** — 저장소의 `docs/checks/drawer.js` · `phone.js` 를 한 줄(`window.__점검 = …`)로 감싼 사본을 이 창의 임시 폴더에 두고,
임시 정적 서버(127.0.0.1:8765 · 끝나고 멈춤)에서 script 태그로 실었다. 저장소 파일은 그대로다. 회차는 `?retreat_id=1`, 창 1707px, 보이는 탭.

**2a — drawer.js** (실패 0 · 못 잼 0 · 전부 완주)

| 계정 | 화면 | 항목 | 건너뜀 |
|---|---|---|---|
| 관리자 | 목록 | 93 | 5 |
| 관리자 | 보드 | 80 | 3 |
| 관리자 | 달력(7월 · 범위 전체) | 83 | 4 |
| 부서 리더 | 목록(내 부서) | 85 | 7 |
| 부서 리더 | 목록(부서 전체) | 79 | 17 |
| 부서 리더 | 보드 | 63 | 16 |
| 부서 리더 | 달력(7월 · 범위 전체) | 66 | 17 |

- 관리자 판의 건너뜀에는 「못 바꾸는 상태 칸이 없어 건너뜀」 이 있었다. 리더로 **부서 전체** 목록(못 바꾸는 칸 86 · 바꾸는 칸 10)에서 돌려
  「상태 배지 → 행 열림 / 메뉴 안 뜸 (바꿀 수 없음)」 · 「상태 칸의 가장자리 → 행 열림 / 메뉴 안 뜸 (바꿀 수 없음)」 넷이 ✓ 로 재졌다
- 리더 판의 건너뜀은 「이 계정에는 그 칸이 없거나 죽어 있어」(고칠 수 없는 업무의 편집 칸)와 자료가 없는 것(옮긴 자리 · 고칠 논의 · 연결)이다 — 앞의 것은 관리자 판이 쟀다
- 달력은 처음에 이번 달 · 「내 것」 이라 열 점이 0 이었다(부서 없는 관리자) — 7월 · 전체로 바꿔 돌렸다
- 점검이 확인 요청을 보내는 항목이 있어 개발 DB 에 몇 건 쌓였다

**2a · 2d — phone.js** (같은 페이지 안의 400px 틀 · 폭 396 · Chrome 창 크기 바꾸기가 안 먹었다)

| 계정 | 목록 | 보드 | 달력 |
|---|---|---|---|
| 관리자 | 11 · 실패 0 · 건너뜀 2 | 10 · **실패 1** · 건너뜀 3 | 10 · **실패 1** · 건너뜀 3 |
| 부서 리더 | 11 · 실패 0 · 건너뜀 2 | 10 · **실패 1** · 건너뜀 3 | 10 · **실패 1** · 건너뜀 3 |

- 실패는 네 칸 모두 「본문이 그려졌다」 — **점검의 거짓 실패**다. 본문을 `main` 으로만 찾는데 보드 · 달력에는 `main` 이 없다.
  같은 틀에서 보드는 좁은 폭의 목록(`div.mlist` · 높이 679px)이, 달력은 `#calswap`(높이 1,594px)이 그려져 있었다
- 건너뜀은 손가락 기기 전용 조건(마우스가 있는 기기)이고, **보드 · 달력에서는 「본문의 링크가 덮여 있지 않은가」 도 건너뛰었다** — 본문을 못 찾아서다. 그 항목은 두 화면 · 두 계정에서 한 번도 안 재졌다(봐둘것 BE-a)
- 2d 의 「같은 것을 375/400px 에서 반복」 은 phone.js 만 400px 틀(폭 396)에서 돌렸다 — **375px 은 안 쟀고**, 재정 · 업무 클릭은 넓은 창에서만 했다(4장 대조 표)

**2b — 재정** (관리자 · 실제 클릭 · 개발 DB 로 저장 확인)

- 등록: 예산 항목 · 부서 · 금액 · 계좌 셋(은행 · 999 로 시작하는 지어낸 번호 · 예금주) · 메모 영수증 → 지출 1 · 영수증 1 · 잇기 1. 지원금액 = 금액(식대 아님)
- 영수증 잇기: 이미 있는 영수증 번호로 새 지출에 이음 → 한 장이 지출 셋에 걸림
- 고치기: 금액 · 비고를 바꿔 저장 → 지원금액도 다시 셈. 활동 기록 「지출_수정」 에 계좌 값 없음
- 취소(확인창 수락): 행 흐림(.45) · 집행액 36,000 → 21,000 원 · 그 행의 고치기 · 잇기 폼 사라짐 · 단추가 「되살리기」 · 잇기 줄 2 는 남음
- 되살리기: 취소가 풀리고 금액 그대로(「지출_되살림」)

**2c — 업무** (관리자)

- 목록 맨 아래 「이번 회차에서 뺀 업무 6건」 을 펴 한 줄을 누름 → 드로어 · 칩 아래 「이번 회차에서 뺀 업무입니다」(14px · 보임) · 상태 메뉴는 대기 · 진행중 · 완료(지연 없음)
- 뺀 업무의 상태를 진행중으로 → 막히지 않음(「업무_상태_변경」) → 대기로 되돌림
- 목록의 「담당자 없음」 칸 → 행이 안 열리고 그 자리에 메뉴 → 지정(「담당자_지정」) → 다시 비움
- **노션 233 은 개발 DB 에 없다** — 개발 DB 는 시드 102 라 「노션 233 이 보드 · 목록 · 달력에 보이는가」 는 잴 수 없었다(운영 사본을 부으면 실명 게이트가 막는다)

# 3. 3 · 4 · 5단계

**3 — 발견한 것 (심각도별 · 고친 코드 없음)**

| 갈래 | 수 | 무엇 | 어디 |
|---|---|---|---|
| 앱 결함으로 확인된 것 | 0 | — | — |
| 까닭을 모르는 것 | 1 | 첫 「되살리기」 누름이 서버에 안 닿고 두 번째에 됨(요청 없음 · 오류 화면 없음). 확인창 바꿔치기 탓일 수 있으나 추측이라 앱 결함 아님으로 치지 않는다 | 봐둘것 BE-a |
| 점검 스크립트 결함(낮음 · 다음 판) | 1 | phone.js 의 본문 판정이 `main` 만 본다 → 보드 · 달력 거짓 실패 · 그 탓에 본문 누름이 안 재짐. 함께: 두 화면에 `main` 랜드마크가 없음 | 봐둘것 BE-a |
| 점검 조작 쪽(앱 결함 아님) | 2 | 첫 「지출 등록」 누름은 폼이 접힌 채라 서버에 안 닿음(여닫기는 정상) · 닫힌 `details` 안도 `offsetParent` 가 있음 | 봐둘것 BE-a |

**4 — 정리** — 개발 DB 에 남은 것(사람이 만든 리더 계정 · 점검 지출 · 확인 요청 · 활동 기록)은 그대로 뒀다. 계정 값은 이 창이 모른다.
뺀 업무의 상태와 담당자는 원래대로 되돌렸다.

**5 — 문서** — 인계 맨 위 · 「남은 것」 표의 화면 점검 두 줄을 닫음 · CLAUDE.md 13장의 「화면 점검만 남았습니다」 를 결과로 · 봐둘것 BE-a.
앞 판 보고는 `docs/review/2026-09-17-시드줄기마무리.md` 로 얼렸다.

# 4. 수용 기준 · 지시문 대조

## 수용 기준

| 기준 | 통과 | 어디 |
|---|---|---|
| 가) 개발 서버가 정상으로 뜨고 로그인이 된다 | O | 1장 — 로그인은 사람이 했다 |
| 나) drawer.js · phone.js 가 끝까지 돌았다 | O | 2장 — 전부 완주. phone.js 보드 · 달력의 실패 1 은 거짓 실패이고 그 탓에 본문 누름은 안 재졌다(3장) |
| 다) 재정 · 업무를 실제 클릭으로 저장까지 | O | 2장 2b · 2c — 노션 233 표시는 개발 DB 에 없어 못 잼 |
| 라) 계정 정보가 저장소 · 보고에 안 남음 | O | 이 창이 값을 모른다 · 글검사 통과 |
| 마) 발견한 문제의 심각도별 갈래 | O | 3장 표 |
| 바) pytest 옵션 없이 전부 통과 · 글검사 7/7 | O | 8장 |
| 사) 검토자 원문이 최근.md 에 그대로 | O | 6장 |

## 지시문 대조 표

| 항목 | 판정 | 비고 |
|---|---|---|
| 0a | 했다 | 응답 없음 |
| 0b | 했다 | 미리보기(launch.json)로 띄움 |
| 0c | 했다 | 게이트 exit 0 |
| 0d | 안 했다 | 문제가 없어 지울 까닭이 없었다 |
| 1a | 안 했다 | 이 창의 고정 안전 규칙 · 11-2 — 사람이 계정을 썼다 |
| 1b | 안 했다 | 이 창은 로그인하지 않았다 — 사람이 로그인했고 이 창은 그 창을 썼다 |
| 1c | 다르게 했다 | 사람이 부서 리더 계정을 만들고 로그인했다 |
| 2a | 했다 | 2장 표 둘 |
| 2b | 했다 | 2장 |
| 2c | 다르게 했다 | 뺀 업무 · 상태 · 담당자는 했고, 노션 233 표시는 개발 DB 에 자료가 없어 못 쟀다 |
| 2d | 다르게 했다 | 좁은 폭은 phone.js 만(400px 틀 · 폭 396) — 375px 은 안 쟀고, 재정 · 업무 클릭은 넓은 창에서만 했다. 창 크기 바꾸기가 안 먹었다 |
| 2e | 했다 | 1장 끝 |
| 3a | 안 했다 | 고칠 수 있었지만 앱(`main` 을 둠)과 점검(후보를 넓힘) 중 어느 쪽을 고칠지 갈리고, 앱을 고치면 재시작 · 재점검이 따라와 이 판(코드 안 고침)의 범위를 넘는다 — 어느 쪽인지는 사람이 정한다(사람칸 · 봐둘것 BE-a) |
| 3b | 했다 | 3장 표 |
| 4a | 했다 | 그대로 둠 |
| 4b | 했다 | 값을 모른다 |
| 5a | 했다 | 인계 |
| 5b | 했다 | 건수로만 |
| 공통 규칙은 CLAUDE.md 그대로 | 다르게 했다 | 지시문의 「11-2 예외(이 창이 로그인)」 는 쓰지 않았다 — 1a |
| 검토자 원문을 그대로 싣는다 | 했다 | 6장 |
| 운영 DB 는 안 건드린다 | 했다 | 매 단계 확인 |
| 개발 코드를 고치면 적는다 | 했다 | 고친 코드 없음 |
| 마무리는 11-3 대로 | 했다 | 앱 코드를 안 고쳐 재시작 건너뜀 |

# 6. 검토자 원문

## 6-1. 커밋 전 검토

```
━━━ 검토자 여기부터 ━━━ 범위 260a653..작업 트리 · 커밋 0개(커밋 전) · 파일 5개

확인한 것
  - 얼린 사본: docs/review/2026-09-17-시드줄기마무리.md 는 git show 260a653:docs/review/최근.md 와 바이트까지 같다(cmp)
  - BE-a 의 판단은 맞다. docs/checks/phone.js:123 은 본문을 document.querySelector('main') 으로만 찾는다.
    templates/tasks.html:48 에는 <main> 이 있고, board.html · calendar.html 에는 <main> 이 없다
    (retreat_base.html 에도 없다. board.html 의 'main' 은 행 클래스일 뿐이다)
  - 운영 data/app.db 의 수정 시각은 15:20:10 이다. 보고 13줄과 같다
  - 계정 아이디·비밀번호, 실명, 바깥 주소는 diff 에서 못 찾았다. 나오는 주소는 localhost:8001 · 127.0.0.1:8001 · 127.0.0.1:8765 뿐이다

막아야 함
  1. docs/review/최근.md:149 · 153 · 157 · 163 · 165 · 167
     「@@검토1@@」 「@@검토2@@」 「@@고침@@」 「@@파일@@」 「@@글검사@@」 「@@pytest@@」 가 아직 채워지지 않았다.
     그런데 수용 기준 바)·사)(114·115줄)와 대조 표(140줄)는 이미 O / 했다 로 적혀 있다.
     scripts/check_placeholder.py:83 은 PLACEHOLDER 꼴만 찾는다. 그래서 이 @@…@@ 꼴은 글검사를 그냥 지나간다.
     커밋하기 전에 반드시 채워야 한다. 채우지 않으면 11-3 이 네 판 연속 겪은 「없는 것을 있다고 적음」이 되풀이되고,
     이번에는 기계도 못 잡는다. 표시를 PLACEHOLDER 꼴로 두었다면 검사가 잡았을 것이다.

고쳐야 함
  2. docs/review/최근.md:95 와 docs/봐둘것.md BE-a 의 「함께 적어 둔다」 가 서로 다른 것을 가리킨다.
     보고의 「점검 조작 쪽 3」 은 ① 첫 「지출 등록」 누름 ② 첫 「되살리기」 누름이 서버에 안 닿음 ③ details 의 offsetParent 이고,
     어디에 있는지를 「봐둘것 BE-a」 라고 적었다.
     BE-a 에 실제로 있는 셋은 400px 틀 · details 의 offsetParent · confirm 수락 바꿔치기다.
     ①·② 는 BE-a 에 없다.
  3. ② 첫 「되살리기」 누름이 서버에 안 닿은 일은 보고 스스로 「까닭 확인 못 함」 이라고 적었다.
     그런데 같은 표는 이것을 「앱 결함 아님」 으로 나누고, 인계 3줄과 보고 93줄은 「앱 결함 0」 이라고 적었다.
     앱 결함이 아니라는 근거가 없다. 「확인 못 함 1」 로 따로 세고 BE-a 에 옮겨 적어야 수가 사실과 맞는다.
     (confirm 을 바꿔치기한 때와 첫 누름의 앞뒤가 원인일 수는 있지만, 이것은 추측이다)
  4. BE-a(봐둘것 끝 절)는 거짓 실패만 적었고, 그 결과 빠진 측정을 적지 않았다.
     phone.js:131 은 본문이 null 이면 본문링크도 null 로 두고 「본문 누름은 건너뜀」 으로 넘어간다.
     그래서 보드·달력에서는 2026-09-12 회귀를 막으려고 둔 「본문의 링크가 덮여 있지 않은가」 가 두 계정 모두 한 번도 재지지 않았다.
     보고 64·65줄의 「10 항목 · 건너뜀 3」(목록은 11 · 2)이 바로 그것이다.
     BE-a 에 「보드·달력의 본문 누름은 안 재짐」 을 넣어야 한다.
     CLAUDE.md 13장 새 문장(4456줄 「세 화면 · 두 계정으로 돌았습니다」)과 인계 3줄 「화면 점검이 끝났다」 도 그만큼 줄여 적는 편이 맞다.
     같은 까닭으로 자가시험 ⑥(phone.js:323~326 · 심는 자리 main a)도 그 두 화면에서는 늘 「못 심음」 이다.
  5. docs/인계.md:263 에 「정식 11-3 점검(보이는 탭 · 두 계정)은 아직이다.」 가 그대로 남아 있다.
     같은 파일 3줄 · 271줄은 이제 돌았다고 말한다. 한 파일이 자기와 어긋난다.
     327줄 「화면 점검 (2026-09-15 …)」 절 머리도, 「남은 것」 표(271·272줄)가 그리로 보내는 자리인데 이번 판 결과를 가리키지 않는다.

봐 둘 것
  6. 대조 표 3a(133줄) 「했다 | 고친 것 없음」.
     3a 는 「고칠 수 있으면 고친다」 이다. BE-a 는 점검 스크립트 쪽이라 고칠 수 있었는데 다음 판으로 넘겼다.
     「안 했다 + 까닭(앱을 고칠지 점검을 고칠지 정할 일이라서)」 이 더 정확해 보인다.
     BE-a 가 「어느 쪽인지는 다음 판이 정한다」 로 남겼는데, 그것이 사람이 정할 일인지 다음 구현 판이 정할 일인지도 사람칸 「정할 것」(4줄)에서 흐리다.
  7. 대조 표 1b(126줄) 「다르게 했다」.
     실제로는 이 창이 로그인하지 않았으므로 1a 와 같이 「안 했다」 로도 읽힌다. 까닭은 적혀 있어 규칙상으로는 통과한다.
  8. 2d 는 「375/400px」 인데 실제로 잰 폭은 396 하나뿐이다(보고 60줄). 375 에서는 안 돌았다.
     대조 표 비고(131줄)에 375 를 안 잰 것이 적혀 있지 않다.
  9. CLAUDE.md 11-2 의 「phone.js 에 아직 안 심은 자가시험 둘」 문단은 그대로 두었다(보고 22줄).
     그 문단의 「이 창이 붙여넣어 돌리는 길은 남습니다」 와 이번 판의 방식은 맞는다. 어긋남은 없다.
  10. 보고 8줄 「커밋: 260a653 다음」.
     커밋한 뒤 짧은 해시로 바꾸는 것이 11-3 의 머리 모양이다.

없음
  - 코드 변경: 없음(diff 에 app/·templates/·static/ 없음 · 재시작 건너뜀 사유가 적혀 있음)
  - 막는 코드 · 시험 쌍: 이 판에는 해당 없음
  - 운영 DB 변경의 흔적: 없음

사람에게 묻는 것
  - BE-a 를 고칠 쪽(phone.js 의 본문 후보를 넓힐지 · board/calendar 에 <main> 을 둘지)을 사람이 정하나, 다음 구현 판이 정하나
  - 「첫 되살리기 누름이 서버에 안 닿음」 을 앱 쪽에서 다시 재 볼 것인가, 점검 조작의 탓으로 두고 닫을 것인가
━━━ 검토자 여기까지 ━━━
```

## 6-2. 검토 뒤 고침을 다시 본 것

```
━━━ 검토자 여기부터 ━━━ 범위 260a653..스테이징(git diff --cached) · 커밋 0개(커밋 전) · 파일 5개 · 검토 뒤 고침 다시 보기

앞 지적이 닫혔나
  [1] 아직 안 닫힘. 보고 끝의 채울 자리 셋이 남아 있다.
      docs/review/최근.md:217 「⟪검토2⟫」 · 251 「⟪글검사⟫」 · 253 「⟪pytest⟫」
      그런데 수용 기준 바)·사)와 대조 표 「검토자 원문을 그대로 싣는다」 는 이미 O / 했다 로 적혀 있다.
      이 ⟪…⟫ 꼴도 scripts/check_placeholder.py:83(PLACEHOLDER 꼴만 찾음)에 안 걸린다.
      커밋 전에 채우지 않으면 여전히 막아야 함이다. 7장이 약속한 「남은 표시를 손으로 찾았다」 도 8장에 결과가 적혀야 한다
  [2] 닫힘. 보고 3장 표의 「점검 조작 쪽」 이 2 로 줄었고, 두 줄 모두 BE-a 에 있다
  [3] 닫힘. 「까닭을 모르는 것 1」 로 따로 셌고, 인계 3줄과 보고 표가 「앱 결함으로 확인된 것 0」 으로 맞췄다. BE-a 에도 있다
  [4] 닫힘. 다만 새 줄에 번호가 틀린 곳이 하나 있다 — 아래 새 1
  [5] 닫힘. 인계 263줄과 09-15 절 머리 한 줄
  [6] 판정은 고쳐졌다(3a 안 했다 + 까닭). 넘긴 방식은 아래 새 3
  [7] 닫힘. 1b 가 「안 했다」 로 바뀌었다
  [8] 닫힘. 보고 2장과 대조 표 2d 에 「375px 은 안 쟀다」 가 들어갔다
  [9] 해당 없음
  [10] 7장의 까닭(보고가 담긴 커밋은 보고보다 뒤에 생긴다)을 받아들인다. 11-3 머리 모양과 부딪히지 않는다

새로 어긋난 것

고쳐야 함
  1. docs/봐둘것.md BE-a 「그 결과 안 재진 것」 줄의
     「③ 「본문의 링크가 덮여 있지 않은가」」 는 번호가 틀렸다.
     docs/checks/phone.js 에서 본문 링크 누름은 「② 화면이 그려졌는가」 블록 안에 있다(123~139줄).
     「③」 은 「메뉴 단추가 눌리는가」 다(141줄).
     자가시험 쪽에서는 이 항목이 「⑥ 본문의 링크가 덮여 있다」(323줄)다.
     다음 판이 번호를 따라 엉뚱한 항목을 찾게 된다

봐 둘 것
  2. docs/인계.md 327줄 아래에 새로 넣은 한 줄은 결과를 「보고 docs/review/최근.md(다음 판이 날짜 이름으로 얼린다)」 로 가리킨다.
     다음 판이 최근.md 를 덮으면 이 가리킴은 다른 판을 가리킨다.
     CLAUDE.md 0장이 「덮이는 최근.md 가 아니라 얼린 판을 가리킨다」 고 한 그 자리다.
     맨 위 갱신 줄과 BE-a 에 수가 있어 잃는 것은 적다. 다음 판이 얼린 이름으로 바꿔 적어야 할 곳이다
  3. 보고 7장 끝을 보면 앞 검토의 「사람에게 묻는 것」 둘을 사람칸에 올리지 않았다.
     구현 창이 「갈림이 작다」 고 보고 스스로 닫은 것이다.
     BE-a 에 「사람에게 올릴 만큼의 갈림은 아니다」 로 적힌 것도 구현 창의 판단이다.
     앱에 <main> 을 둘지는 화면 구조에 닿는 일이다. 사람이 이 판단을 받아들이는지 한 번 보는 편이 좋다
  4. 7장은 채울 자리 꼴을 검사가 못 잡는 점을 「검사를 넓히지 않고 손으로 찾는다」 로 두었다.
     11-3 「마치기 전에 되짚는 것」 의 「사람이 조심할 때를 정하지 말고 매번 도는 검사로」 와 결이 다르다.
     까닭은 적혀 있다. 판단은 사람 몫이다

없음
  - 스테이징된 파일은 5개이고 app/·templates/·static/ 은 없다. 재시작 건너뜀과 맞는다
  - 새로 들어온 줄에서 계정 값 · 실명 · 바깥 주소는 못 찾았다
  - CLAUDE.md 13장의 새 문장, 인계 맨 위 줄, BE-a 가 서로 같은 것을 말한다(위 1 의 번호만 빼고)
  - 보고 2장의 「(4장 대조 표)」 가리킴은 실제 장 번호와 맞다

사람에게 묻는 것
  - phone.js 거짓 실패를 고칠 쪽(점검 스크립트를 넓힐지, 보드·달력에 <main> 을 둘지)을 다음 구현 판에 맡겨도 되는가
  - 채울 자리 꼴을 check_placeholder 가 못 잡는 것을 손으로 찾는 채로 둘 것인가
━━━ 검토자 여기까지 ━━━
```

# 7. 검토 뒤 고침

검토 [1] 은 보고의 빈 자리 — 원문 · 파일 목록 · 검사 결과를 채우고 커밋한다(이 장과 8장). **안 고친 것**: 그 자리 표시 꼴을 `check_placeholder` 가 못 잡는다는 지적 — 이 창이 판마다 임시로 쓰는 표시라 검사를 넓히지 않고, 커밋 전에 남은 표시가 없는지 손으로 찾았다(8장).

| 검토 | 고친 것 | 파일 |
|---|---|---|
| [2] | 3장 표의 「점검 조작 쪽 3」 을 둘로 줄이고, BE-a 에 첫 「지출 등록」 누름을 옮겨 적었다 | 최근.md · 봐둘것.md |
| [3] | 첫 「되살리기」 를 「까닭을 모르는 것 1」 로 따로 세고 「앱 결함 0」 을 「앱 결함으로 확인된 것 0」 으로 바꿨다(보고 · 인계). BE-a 에 옮겨 적었다 | 최근.md · 인계.md · 봐둘것.md |
| [4] | BE-a 에 「보드 · 달력에서 본문 누름이 안 재짐 · 자가시험의 본문 링크 고장도 못 심음」 을 더했다. 보고 2장 · 수용 기준 나) · CLAUDE.md 13장 · 인계 맨 위를 그만큼 줄여 적었다 | 봐둘것.md · 최근.md · CLAUDE.md · 인계.md |
| [5] | 인계의 「정식 11-3 점검은 아직이다」 를 「2026-09-17 에 돌았다」 로 바꾸고, 09-15 「화면 점검」 절 머리에 정식 점검이 어디 있는지 한 줄을 더했다 | 인계.md |
| [6] | 대조 표 3a 를 「안 했다」 로 바꾸고 까닭을 적었다. 고칠 쪽은 다음 구현 판이 정한다고 적었다 — **두 번째 검토 뒤 사람칸 「정할 것」 으로 옮겼다**(8장 끝) | 최근.md · 봐둘것.md |
| [7] | 대조 표 1b 를 「안 했다」 로 | 최근.md |
| [8] | 2장 · 대조 표 2d 에 「375px 은 안 쟀다」 를 적었다 | 최근.md |
| [9] | 어긋남 없음 — 고친 것 없음 | — |
| [10] | 머리의 커밋 줄은 11-3 대로 「이 판의 커밋들」 을 가리킨다 — 보고가 담긴 커밋은 보고보다 뒤에 생겨 짧은 해시를 적을 수 없다. 덩어리 첫 줄이 해시를 준다 | — |

검토의 「사람에게 묻는 것」 중 고칠 쪽은 두 번째 검토 뒤 사람칸에 올렸다(8장 끝). 되살리기는 다음 재정 화면 점검 때 다시 본다고 BE-a 에 적었다.

# 8. 검사 · 시험

**바뀐 파일** (`git diff --name-only --cached 260a653`)

```
CLAUDE.md
docs/review/2026-09-17-시드줄기마무리.md
docs/review/최근.md
docs/봐둘것.md
docs/인계.md
```

`app/` · `templates/` · `static/` 이 없어 재시작은 건너뛰었다(11-3 2단계 · 운영 PID 2776 그대로).

**글검사** — 7개 전부 통과(check_names · check_stale · check_named · check_counts · check_handoff · check_placeholder · check_orders)

**남은 채울 자리** — 커밋 직전에 `⟪` · `@@` 로 시작하는 표시를 grep 으로 찾아 0 이었다(검토 [1] · 검사가 이 꼴을 못 잡는다).

**pytest** (옵션 없이 · 보고를 다 채운 뒤) — `1693 passed, 3 warnings in 1418.01s (0:23:38)`

## 두 번째 검토 뒤 고침

세 번째 검토는 없다(11-3).

| 두 번째 검토 | 고친 것 | 파일 |
|---|---|---|
| 새 1 | BE-a 의 항목 번호를 바로잡았다 — 본문 링크 누름은 ② 블록 안이고, 자가시험 쪽은 ⑥ | 봐둘것.md |
| 새 3 | 고칠 쪽(점검을 넓힐지 · 앱에 `main` 을 둘지)을 구현 창이 닫지 않고 사람칸 「정할 것」 에 올렸다. BE-a · 대조 표 3a 를 그에 맞췄다 | 최근.md · 봐둘것.md |
| 새 2 | 안 고침 — 인계의 09-15 절 머리가 가리키는 `최근.md` 는 다음 판이 이 보고를 얼린 이름으로 바꿔 적는다(맨 위 갱신 줄과 BE-a 에 수가 있다) | — |
| 새 4 | 안 고침 — 채울 자리 꼴을 검사로 잡을지는 7장의 까닭대로 두었다. 사람이 다르게 정하면 그 판에서 넓힌다 | — |
