# 문서에 이름이 나오지만 지키지 않는 백업 사본 — `사본 이름 | 왜 지키지 않는가`
#
# `tests/test_stage52.py` 가 씁니다. 규칙은 CLAUDE.md 11-2 백업 절 —
# **문서가 되돌리는 자리로 가리키는 사본은 `scripts/backup.py` 의 `지키는판` 에 넣습니다.**
# 여기는 그 반대, 문서가 **그 판의 기록으로** 적었을 뿐 되돌리는 자리로 가리키지 않는 이름입니다.
# 되돌리는 자리인지 기록인지는 사람이 가릅니다 — 이유 칸을 채울 때 한 번 봅니다.
# 뒤에 어느 문서가 되돌리는 자리로 다시 가리키면 여기서 빼고 지키는판에 넣습니다.

app-20260914-164215.db | 기록 — 영수증 잇기 운영 실행에서 뜬 직전 사본을 적은 것. 그 뒤 한 일은 지우거나 덮은 것이 없고 잇기 줄을 앱에서 뗄 수 있어 DB 로 되돌릴 일이 없다(덮인 글자가 DB 에만 있던 짝잇기 사본과 다르다) 근거: 봐둘것 BA-f · 인계 재정 차례 4
app-20260911-155104.db | 기록 — 비품 비고 옮기기 운영 실행에서 뜬 사본을 적은 것 근거: 인계 17번
app-20260911-171608.db | 기록 — 비품 묶음 합치기 즈음의 사본을 적은 것. 합치기의 사본인지도 확실치 않다고 같은 줄이 적었다 근거: 인계 17번
app-20260910-172758.db | 기록 — 비품 옮기기 도막 1 운영 실행에서 뜬 사본을 적은 것 근거: 인계 17번
