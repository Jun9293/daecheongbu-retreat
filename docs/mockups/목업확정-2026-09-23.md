# 목업 확정 값 — 2026-09-23

**CLAUDE.md 와 같은 효력의 시각 스펙입니다 · 사람이 2026-09-23 목업으로 정했습니다.**

다른 목업(`docs/mockups/*.html`)은 2026-08-31 판이고 그 뒤로 다시 그린 적이 없어
**색과 규칙은 CLAUDE.md 4-0 이 정본**입니다(그 파일 머리의 그 문단). 이 파일은
다릅니다 — **2026-09-23 에 사람이 정한 값 자체**라 옛 판이 아닙니다.
**정본은 CLAUDE.md 입니다** — 이 파일은 그 값의 출처이고, 둘이 어긋나면
여기서 사람이 무엇을 정했는지 확인해 **둘 다** 고칩니다(CLAUDE.md 머리의
그 문단). 그래서 아래에 꼬리를 단 자리가 있습니다.

**여기 없는 값은 지어내지 않습니다.** 지금 화면 값을 그대로 두고, 무엇을 그렇게
뒀는지 그 판의 보고에 적습니다.

**색은 같은 뜻의 기존 토큰으로 옮깁니다.** 새로 들이는 값은 셋뿐입니다 —
행 가로선 `#ECEDF1` · 수련회 음영 `#F3F2FF` · 기간 칠 `#FAF1D2`.

> **그중 `#FAF1D2` 는 이미 있는 값입니다** — CLAUDE.md 4-0 의 「비침」 이고
> `retreat.css` 의 `--span` 입니다(4-13 이 그 색을 「앰버(`--span`)」 로 부릅니다).
> 위 줄은 지시문에 적힌 그대로 옮긴 것이고, **여기 꼬리를 답니다** — 안 적으면
> 이 값을 쓰는 판이 같은 색에 토큰을 하나 더 만듭니다. 실제로 새로 들이는 것은
> `#ECEDF1`(`--row-line`)과 `#F3F2FF` 둘입니다.

---

## A. 사이드바

폭 176px · 안쪽 여백 14px 8px · 항목 13px(여백 5px 8px, 하위 항목 왼쪽 18px) ·
묶음 제목 12px 600 · 현재 항목 accent-soft 면 + accent 글자 600 · 머리줄 높이 52px.

## B. 드로어

- 폭 기본 460, 핸들로 360~760. 기기에 남긴 값이 범위 밖이면 범위 안으로 되돌린다.
- 열고 닫힘 0.22s, 끄는 동안 전환 없음. 핸들은 평소 옅은 선, 끄는 동안 accent.
- 밖 누름: 드로어 밖 아무 곳이나 누르면 닫힌다. 다른 업무를 누르면 그 업무로 바뀐다.
- 상태: 대기 · 진행중 · 완료 세 칸 붙은 버튼.
  - 켜진 칸: accent-soft 면, 진한 accent 글자 700, 앞에 점(완료 초록 · 진행중 accent · 대기 호박색 — 이 버튼 안에서만).
  - 꺼진 칸: 흐린 글자 500, 회색 점.
- 기간 줄: 「기간」(같은 날이면 「날짜」) + 2026.08.10 – 2026.08.15 + 「n일간」 또는 「하루」.
  지연이면 앞에 「마감 n일 지남 ·」을 지연색으로. 옆 달력 버튼(켜지면 accent 테두리·면)을
  누르면 292px 팝업이 뜬다.
- 달력 팝업:
  - ‹ 2026년 8월 ›. 일요일 · 공휴일은 빨간 날짜.
  - 고른 양끝은 accent 면 흰 글자(모서리 6px), 사이는 `#FAF1D2`.
  - 안내 줄 순서: 「시작일을 누르세요.」 → 「마감일을 누르세요 — 같은 날을 한 번 더 누르면 하루 업무입니다.」 → 「2026.08.10 하루 업무」 또는 「2026.08.10 – 2026.08.15 · 6일간 기간 업무」.
  - 둘 다 고른 뒤 누르면 시작일부터 다시 고른다. 시작보다 앞을 누르면 맞바꾼다.
  - 「취소」 「저장」. 저장을 눌러야 반영되고, 시작일이 없으면 저장이 흐려진다.
- 관련팀: 칩을 켜고 끄면 아래에 「저장」 「취소」가 뜬다. 저장을 누르면 어두운 막(검정 28%) 위에 340px 팝업이 뜬다.
  - 제목: 「어느 팀에 알릴까요?」
  - 설명: 「관련팀은 알림과 상관없이 저장하는 즉시 지정되어 그 팀의 업무 목록에 들어갑니다. 알림은 알리기만 합니다.」
  - 목록: 관련팀마다 36px 줄(체크 · 팀색 점 · 팀명 · 오른쪽 꼬리표 「새로 추가」(accent) 또는 「기존」(흐림)). 기본은 체크 없음.
  - 단추: 「돌아가기」와 확정 단추(체크 수에 따라 「n팀에 알리고 저장」 / 「알림 없이 저장」).

  확정하면 320px 결과 팝업이 뜬다.
  - 머리: ✓ 「관련팀을 저장했습니다」
  - 세 줄 표: 관련팀 = 팀들 또는 「없음」 / 알림 = 「팀들 — 보냄」 또는 「보내지 않음」 / 업무 목록 = 「위 팀들의 업무 목록에 추가됨」 또는 「—」
  - 「확인」으로 닫는다. `+`/`−` 표시 · 바뀐 것 글 · 초록 안내 글은 없다.
- 진단: 아래 고정 칸에 한 줄 — 색 점 + 4-10 판정 이름(굵게) + 첫 근거 한 줄(… 줄임) + ▼/▲.
  누르면 옅은 면 상자에 근거 전체. 판정과 근거는 지금 4-10 그대로다.
  「+ 하위 업무 추가」와 탭 구성은 지금 그대로다.

## C. 업무 추가 팝업

- 폭 640. 처음 위치는 가로 가운데 · 위 70px. 최대 높이는 화면 높이 − 40(안쪽 스크롤). 모서리 10 · 그림자.
- 윗줄 44px(사이드바 면): 「업무 추가」 14.5px 굵게 + 흐린 부제(「보드에서」 또는 「8/10 에 추가」) + 오른쪽 × 28px.
  윗줄 어디든 잡고 끌면 옮겨진다(× 는 끌기에서 뺀다). 가로 −560 ~ 폭−80, 세로 0 ~ 높이−44 로 묶는다.
- 첫 줄(흐림 12px): 「새로 만든 업무도 라이브러리에 남아 다음 회차의 후보가 됩니다.」
- 위 구역 「새 업무 만들기」(부제 「라이브러리에 없던 일 — 만들면 이번 회차에 바로 들어갑니다.」):
  - 업무 이름 · 담당 부서 · 분류(Main · 하위 · 일정 세 칸, 툴팁 「산출물이 남는 단위」 / 「Main 을 완성하기 위한 단계」 / 「날짜만 지키면 되는 것」).
  - 하위면 「상위 업무」 선택칸이 뜬다(첫 줄 「— Main 업무 고르기 —」).
  - 날짜 줄: 비었으면 「날짜 없음 — 달력 버튼으로 고르기」. 옆 달력 버튼은 B 와 같은 부품이다.
  - 오류 글: 「업무 이름을 적어 주세요.」 · 「하위 업무는 상위 업무를 골라야 합니다.」
  - 단추: 「새 업무 만들기」
- 아래 구역 「라이브러리에서 넣기」(부제 「이번 회차에 아직 없는 업무 — 하위 업무는 함께 딸려 옵니다.」):
  - 체크 목록, 줄마다 이름 · 분류 · 부서 점+이름 · D-주 · 날짜.
  - 「선택 n / N건」 + 「선택한 업무 추가」(0건이면 흐리고 안 눌림).

## D. 보드

- 왼쪽 고정 칸 폭 300 = 업무 칸 + 담당 칸 64(가운데 12px).
  머리: 「부서 · 업무」 13px 굵게 + 「Main ▸ 하위 ▸ 관련업무」 11px 흐림.
  왼쪽 칸 오른쪽은 진한 선, 업무·담당 사이는 옅은 선.
- 머리 두 줄: 구간 줄 22px(11px 600) + 날짜 줄 36px(위 12px 600 · 아래 11px, 숫자 고정폭).
- 구간: 「기획 · 주 단위 (D-주)」(D-16주보다 이른 주) · 「준비 · 주 단위 (D-주)」 · 「D-2주 · 하루 단위」 · 「수련회」 · 「후속 · 주 단위」.
  - 칸 폭: 주 118 · 하루 58 · 수련회 126 · 후속 118.
  - 면: 기획 · 후속은 사이드바 면, 수련회 칸은 머리부터 끝까지 `#F3F2FF`.
  - 세로선: 구간 안은 옅은 선, 경계는 진한 선.
  - 후속 칸: 수련회 다음 날부터 D+1, D+2 … 로 둔다. **오늘이 든 주와 가장 늦은 마감 중 늦은 쪽까지 가고, 최소 3칸 · 최대 D+26주다**(사람이 2026-09-23 에 정함).
- 오늘: 그 칸 머리를 accent-soft 면 + accent 글자로 칠하고 윗글에 「 · 오늘」을 붙인다. 본문에는 2px accent 세로선(불투명 0.55).
  범례 끝 「오늘」 단추(accent 면 흰 글자)는 오늘 선이 가운데 오게 부드럽게 옮긴다. 한눈에 보기(맞춤)는 없앤다.
- 범례 순서: Main · 하위 · 일정 · 완료 · ! 지연 · 관련업무 · 연결선.
- 가로선: 부서 줄 · 업무 줄 아래 `--row-line` 한 줄. 왼쪽 칸과 내용 칸이 같다.
- 부서 줄: 팀색 9% 면. ▶/▼ · 점 8px · 팀명 13.5px 굵게 · 바로 옆 「완료 n/n건」 12px,
  지연이 있으면 「 · 지연 n건」을 지연색 600 으로. 완료 막대 · 총 건수는 없다.
- 업무 줄:
  - 번호 11px 흐림(24px 칸) · 하위가 있으면 ▶/▼ 16px · 제목 13px(Main 600 · 하위 400 · 고스트는 기울임 흐림 + 담당팀 점+이름 11px).
  - 하위 줄은 26px 들이고 └ 를 붙인다.
- Main 바: 위 5 · 높이 20 · 글자 12px. 펼친 하위 줄 바: 위 8 · 높이 16 · 글자 11.5px.
- 바 글자: 밖으로 넘쳐도 되고, 같은 줄 다음 바 시작 − 6px 까지 쓴다. 넘으면 … 로 끊는다.
  끊긴 바와 합친 바는 F 의 팝업을 연다. 왼쪽 칸에서 … 로 끊긴 이름도 같은 팝업이다.

  > **`→` 가 붙은 바도 팝업을 엽니다** — 이 줄에는 그 조건이 없지만, 같은 사람이
  > 같은 날 정한 ①이 「상한 밖 업무는 … 팝업 메타에 실제 날짜를 쓴다」 이고
  > **제목이 짧아 안 끊긴 채 축 밖인 바**는 그 조건만으로는 팝업이 안 열려
  > 날짜를 말할 자리가 없어집니다. 규칙은 CLAUDE.md 4-1 에 있습니다.
  > **이 줄의 글은 그대로 두고 꼬리만 답니다**(`#FAF1D2` 와 같은 처리).
- 지연: 바 글자 앞 ! 하나(12px 800 지연색). 「지연 n일」 글은 바에 쓰지 않는다. 바 채움은 지금 `bar_style` 그대로다.
- 하위가 있는 Main 은 기본 접힘이다(펼침 상태는 기기에 남긴다). 접히면:
  - 하위 바를 Main 바 아래 한 줄에 둔다(위 29 · 높이 14 · 글자 11px).
  - 시작 칸과 끝 칸이 같은 하위끼리는 바 하나로 합치고 이름을 「, 」로 잇는다. **합친 바는 못 끈다**(사람이 2026-09-23 에 정함).
  - 일부만 겹치면 앞의 빈 줄에 넣고, 없으면 줄을 더한다(간격 18).
  - 행 높이 48 + (줄 수 − 1) × 18.
  - 왼쪽 이름 아래 「하위 n건」(11.5px 흐림, 누르면 펼침).
- 날짜 없는 업무: 내용 칸 왼쪽 8px 에 점선 작은 표 「날짜 없음 · 누르면 드로어에서 날짜를 정합니다」(11.5px 흐림), 누르면 드로어.
- 선택된 줄: 왼쪽 칸 accent-soft, 내용 칸 accent 7%. 보드 틀은 진한 선 1px · 모서리 6.

## E. 달력

- 머리: ‹ 2026년 8월 › · 「오늘」 · 범위 칩 · 미완료만.
  범례: 「업무 기간 (호버·터치)」(`#FAF1D2` 견본) · 「수련회」(`#F3F2FF` 견본) · ! 지연 · 완료.
- 칸:
  - 세로 옅은 선 · 가로 `--row-line`. 이 달 밖은 옅은 면, 수련회 날은 `#F3F2FF`.
  - 날짜 글자: 이 달 600, 밖은 흐림 400. 일요일 · 공휴일은 지연색.
  - 꼬리표: 「수련회 개회」 / 「수련회」(accent) / 공휴일 이름(지연색) / 「오늘」.
  - 1일과 격자 첫 칸은 「8/1」 꼴.
  - 날짜 줄 오른쪽 끝에 「+」.
- 한 칸의 업무는 전부 보이고 칸 높이가 는다. 순서는 지연 → 진행중·대기 → 완료.
- 칩: 한 줄 12px, 넘치면 … (드로어가 열리면 칸이 좁아진다).
  - 완료: 옅은 회색 면 · 흐린 글자 · 취소선.
  - 지연: 지연 면 · 옅은 지연 테두리 · 지연 글자 + 앞에 !.
  - 그 외: 지금 규칙 그대로다.
- 호버하면 시작~마감 칸 전체를 `#FAF1D2`(양끝 모서리 6px)로 칠한다. 칩이 끊겼으면 F 의 팝업을 「상세 열기」를 켜서 연다.
- 터치: 첫 탭은 칠함 + 팝업, 두 번째 탭이나 「상세 열기」는 드로어.
- 아래 「날짜가 없는 업무 n건」: 줄마다 「제목 · 담당팀」.
  안내 「날짜를 정하면 달력에 나타납니다 — 누르면 상세에서 날짜를 정합니다.」 누르면 드로어.
- 휴대폰 주 목록: 업무마다 기간 글자(7/12–7/26, 하루면 7/12)를 붙인다.

## F. 업무 팝업 (보드 · 달력 한 부품 · 사람이 2026-09-23 에 정함)

- 대상 아래 4px 에 뜨는 280px 상자(모서리 10 · 그림자).
- 항목마다 제목 전체 13px 600 + 「기간 · 상태(지연이면 지연 n일) · 담당팀」 11.5px 흐림.
  합친 바면 항목이 여럿이고 사이에 `--row-line`.
- 항목을 누르면 그 업무 드로어가 열린다. **달력에서만** 맨 아래 「상세 열기」.
- 팝업 안으로 마우스를 옮기면 유지되고, 벗어나면 닫힌다. 휴대폰은 첫 탭에 열리고, 다른 곳을 누르면 닫힌다.
