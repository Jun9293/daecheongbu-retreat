# 봐 둘 것

**고치지 않기로 한 것들입니다.** 무게가 「봐 둘 것」 이고, 지금 뚫린 곳은
없습니다. 다음에 그 언저리를 손댈 때 함께 봅니다.

`docs/이름-남은곳.txt` 와 뜻은 같습니다 — 남은 것을 사람의 기억이 아니라
목록에 넘깁니다. 고칠 때 그 줄을 지웁니다.

**다만 살아남는 방식이 다릅니다.** 그쪽은 `check_names` 가 돌 때마다
끝에 「아직 안 고친 곳 N곳」 을 찍어서, 검사를 돌리는 사람이 목록의
존재를 매번 봅니다. **이 파일에는 그런 자리가 없습니다** — 실명 검사에
성격이 다른 것을 얹지 않기로 했기 때문입니다. 대신 **CLAUDE.md 11-3 의
「마치기 전에 되짚는 것」 이 여기를 가리킵니다.** 그 문서는 덮이지 않고
작업을 마칠 때마다 읽는 자리입니다.

처음에는 「같은 성격입니다」 라고만 적었는데, 그때 이 파일을 가리키는
곳은 **매번 덮어쓰는 `docs/review/최근.md` 하나뿐**이었습니다 —
다음 판이 덮으면 여기 넘겨 둔 것을 통째로 잃는 상태였습니다.

2026-09-04 검토에서 나왔습니다.

---

## B1 · `표기수()` 를 부르는 곳이 없어졌습니다

`scripts/anonymize.py`

시험이 `표기여럿인줄이있나()` · `가명수()` 로 옮겨 가면서 남았습니다.
지금은 주석 안에서만 이름이 나옵니다.

**남길지 지울지**가 갈립니다 — 대응표를 밖에서 묻는 창구를 넓게 두는
편이 나은지, 부르는 곳이 없는 것은 지우는 편이 나은지.

## B2 · 대응표 안내와 주석에 새 모양이 없습니다

`scripts/anonymize.py` — 대응표가 없을 때 내는 안내, 그리고 dict 모양 주석

지금 적혀 있는 것은 옛 모양뿐입니다.

    {"names": [["실명", "가명"], ...],

표기마다 가명을 따로 두는 `["표기", "가명"]` 은 아래쪽 주석에만 있어서,
**대응표를 처음 만드는 사람에게는 안 보입니다.**

## B3 · 새 시험 둘이 `표기들()` 이 비면 조용히 통과합니다

`tests/test_anonymize_map.py` — `test_대응표_04` · `test_대응표_05`

    안맞 = [...] ; assert not 안맞
    겹침 = set(KEEP) & set(표기들()) ; assert not 겹침

대응표가 비면 둘 다 빈 목록이 되어 초록입니다. 같은 파일의
`test_익명화_03` 과 `main()` 의 0개 거절이 실제로 막고 있으므로
**지금 뚫리지는 않습니다.**

다만 11-3 의 「센 것이 0이면 성공이 아니라 실패」 자리입니다 — 막는
것이 다른 파일에 있으면, 그 다른 파일이 바뀔 때 이쪽이 조용해집니다.

## B4 · `KEEP` 의 근거가 「사용자 확인」 뿐입니다

`scripts/anonymize.py`

`KEEP` 은 익명화에서 영영 제외되는 자리라, 사람 이름이 하나 섞이면
**그 이름은 영영 안 바뀌고 `check_names` 도 안 봅니다.**
`test_대응표_05` 는 대응표에 같은 표기가 있을 때만 잡습니다.

**이것은 특정 항목의 문제가 아니라 `KEEP` 전체의 성질입니다** —
`헤브론`·`코람데오`·`셀라` 가 모두 같습니다. 각 항목에 왜 이름이
아닌지 한 줄을 적어 둔 것이 지금의 방어선이고, 그 이상은 사람이
훑습니다. **기계로 올릴 방법이 있는지**가 볼 자리입니다.

## B5 · 자리만 비워 둡니다

`docs/review/최근.md` 4줄의 커밋 해시였습니다. **그 판에서
처리했습니다** — 커밋한 뒤 그 자리에서 맞추는 것이라 남을 것이
없습니다. 번호만 비어 있으면 빠뜨린 것과 구별되지 않아 적어 둡니다.

## B6 · 시험 둘이 운영 `data/` 에 기대는데 결과가 다릅니다

`tests/test_check_names.py` 의 `test_표밖_04` — 없으면 **빨감**
`tests/test_meetings_import.py` 의 `test_y_13f` — 없으면 **건너뜀**

둘 다 독스트링에 적혀 있어 새로 생긴 규칙은 아닙니다. 다만
**새로 받은 사본에서 같은 원인이 다른 결과**를 냅니다 — 하나는 고장으로
보이고 하나는 안 보입니다. 어느 쪽으로 맞출지는 정해 두지 않았습니다.

---

2026-09-05 검토에서 더 나왔습니다. **이 판에서 고치지 않았습니다** —
「막아야 함」 이 아니었고, 고치면 그 자리에 또 시험을 얹어야 해서
판이 닫히지 않습니다.

## G-a · `[G1]` 이 절반만 닫혔습니다

`scripts/check_names.py`

DB 갈래가 값을 꺼내 `훑기({칸: 값})` 로 넘기는데, `훑기` 안의 문지기가
`JSON칸` 입니다. 그래서 **`db` 칸의 이름이 `json` 칸에도 있어야만**
실제로 세어집니다.

`담당칸` 에 `("db", "users", "name")` 처럼 **`json` 짝이 없는 칸**을
더하면 DB 에서 읽기는 읽고 **아무 말 없이 버립니다.**

**지금 새는 곳은 없습니다** — `assignee_name`·`host` 둘 다 우연히
`json` 칸에도 있습니다. 그래서 `test_표밖_05` 도 이것을 못 잡습니다.

「늘어나면 여기 더한다」 가 **아직 절반만 사실입니다.** 「보는 목록이
불완전했다」 의 **세 번째**입니다 — `host` 칸 · DB 갈래가 목록을 안
쓰던 것 · 그리고 이것.

## G-b · 이 파일을 가리키는 곳이 매번 덮이는 파일 하나뿐입니다 — 처리했습니다

> **2026-09-05 에 CLAUDE.md 11-3 「마치기 전에 되짚는 것」 이 여기를
> 가리키게 했습니다.** 아래는 그때 무엇이 문제였는지의 기록입니다.

`docs/review/최근.md` 는 11-3 이 **「매번 이번 작업으로 덮어쓴다」** 고
정한 파일입니다. 다음 판이 덮으면 이 목록으로 오는 길이 없어집니다.

**`docs/이름-남은곳.txt` 와 「같은 성격」 이라고 적은 것이 이 점에서는
사실이 아닙니다** — 그쪽이 살아남는 이유는 목록이어서가 아니라
`check_names` 가 돌 때마다 **끝에 그 이름을 찍기 때문**입니다.
이 파일에는 그런 자리가 하나도 없습니다.

4-12 의 「보이지 않으면 없는 것과 같습니다」 자리입니다.

**어디서 가리킬지**가 정할 것입니다 — CLAUDE.md 11-3 에 한 줄 ·
`check_names` 출력에 한 줄 · 항목 수를 보는 시험.

## G-c · 실명 검사가 아직 `git add` 안 한 파일을 못 봅니다

`scripts/check_names.py` — 저장소 전체를 `git ls-files` 로 훑습니다.

독스트링은 「**커밋될 것만 본다**」 고 적었는데, 새로 만들고 아직
추적되지 않는 파일은 **커밋될 것인데 안 봅니다.** 2026-09-05 에 실제로
걸렸습니다 — 새 파일 둘이 안 보여 192개였고, `git add` 뒤에 194개가
됐습니다(그때도 0건).

**「새 파일을 만든 판에서는 add 뒤에 한 번 더 돌린다」 로 적으면
11-3 이 스스로 경계한 「…할 때만」 꼴입니다.** 추적되지 않은 것도
보게 하는 편이 매번 도는 검사입니다.

## B-a · 이 목록에 B5 가 없습니다 — 채웠습니다

B5 는 「`최근.md` 4줄의 커밋 해시」 였고, **커밋한 뒤 그 자리에서
처리하는 것**이라 남을 것이 없어 넣지 않았습니다. 번호만 비어 있으면
빠뜨린 것과 구별되지 않아 여기 적어 둡니다.

## B-b · `test_dup_defs.py` 가 `tests/` 만 봅니다

`scripts/` 와 `app/` 도 같은 고장이 날 수 있습니다. 검토자가 밖에서
같은 `ast` 검사를 돌려 **지금은 둘 다 0건**임을 확인했습니다.

pytest 수집이 얽힌 `tests/` 만 유난히 조용하다는 판단이면 그대로 두는
것도 맞습니다 — 어느 쪽인지 정하는 것이 남았습니다.

## B-c · `test_검사_10` 이 아직 소스를 읽습니다 (이번에 생긴 것 아님)

`tests/test_check_names.py`

`check_names.py` 소스에서 「아직 안 고친 곳」 문자열을 찾습니다. 그
말이 지금은 `print` 안에만 있지만 **주석으로 옮겨도 초록입니다.**
같은 파일의 `test_표밖_03` 이 실제 출력으로 재는 길을 이미 보여 줍니다.

---

2026-09-05 「문서의 명령을 PowerShell 하나로」 판에서 나왔습니다.
**이 판에서 고치지 않기로 한 것들입니다.**

## S-a · `.bat` 안의 cmd 문법은 그대로 둡니다

`set` · `%VAR%` · `if exist` · `del` · `for %%F` · 경로 이동 스위치.

**cmd 가 읽는 파일이라 이게 맞습니다.** 11-2 가 「`.bat` 는 cp949 로
저장한다」 고 적어 둔 것과 같은 자리입니다. 시험도 `.bat` 는 안 봅니다.

**고칠 것이 아니라 「왜 안 고쳤는지」 를 남기는 항목입니다** — 다음에
누가 규칙을 들고 저장소를 훑을 때 이것들을 만나기 때문입니다.

**이름을 세지 않습니다** (10장 — 「자리를 세어 두지 않습니다」).
처음엔 둘이라고 적었는데 저장소에는 셋이었습니다.

    git ls-files "*.bat"

## S-b · 같은 명령이 회차 이름만 다르게 두 곳에 있었습니다 — 맞췄습니다

    CLAUDE.md 5-5              전:  2026 여름수련회 Belong
    import_programs.py 독스트링 전:  2026 여름수련회        <- 짧게 적혀 있었다
                               후:  둘 다 2026 여름수련회 Belong

줄 잇기는 양쪽 다 백틱으로 맞췄는데 **회차 이름이 달랐습니다.**
처음엔 「동작을 바꾸는 것이라」 두었는데, 그러면 CLAUDE.md 가 그 둘을
**「같은 것」 이라고 부르는 것이 사실이 아니게 됩니다** — 한 판 안에서
두 말이 갈립니다.

**맞췄습니다.** 실제 회차 이름이 `2026 여름수련회 Belong` 이고,
독스트링 쪽이 그냥 짧게 적혀 있던 것이었습니다.
그리고 `CLAUDE.md` 가 그 명령을 「위 14장」 이라고 가리키던 것을
**5-5** 로 고쳤습니다 — **한 곳입니다.** 검토가 「세 곳」 이라고 했고
제가 확인 없이 받아 적었는데, `da99d34` 의 「14장」 둘은 **둘 다
`docs/배포-안내.md` 14장을 가리키는 맞는 참조**였습니다.

## S-c · 보고에 붙는 기계 출력이 검사와 부딪힐 자리가 더 있을 수 있습니다

`tests/test_docs_shell.py` 가 트레이스백(`^` 자리 표시)과 URL
인코딩(`%EC%B5%9C`)은 갈라내지만, **11-3 이 보고에 무엇을 붙이라고
할지는 앞으로 늘어납니다.**

지금 거르는 조건은 둘뿐입니다 — `^` 가 하나이고 앞에 명령이 있는가,
`%` 사이가 두 글자 이상이고 hex 두 자리+`%` 가 아닌가.
**`%AB%` 처럼 두 글자 hex 로 된 변수는 일부러 놓칩니다** — 이 저장소에
없고, 놓치는 쪽이 보고를 못 적게 만드는 것보다 낫습니다.

다음에 새 종류의 출력을 붙이다 빨개지면 여기를 봅니다.

## S-d · 꺾쇠 자리표시자가 PowerShell 에서는 파스 오류입니다

`docs/배포-안내.md:215 · 249 · 492` — 따옴표 밖의 꺾쇠 자리표시자.
붙여넣으면 「'<' 연산자는 나중에 사용하도록 예약되어 있습니다」 로
**파스 단계에서 멈춥니다.**

문서가 「본인 것으로 바꿔서 치세요」 라고 적어 두었으니 시킨 대로 하면
됩니다. 다만 그 문서 5-6줄이 **「명령어를 몰라도 됩니다 · 붙여넣기만
하면 됩니다」** 라 갈립니다.

**이번 판이 만든 것은 아닙니다.** 699줄의 같은 꼴은 따옴표 안이라
무해하되, 문서의 다른 모든 곳은 경로를 그대로 박아 두어 그 줄만 모양이
다릅니다.

## S-e · 같은 정규식의 뜻이 세 곳에 있습니다

`tests/test_docs_shell.py` 의 주석 · `docs/봐둘것.md` 의 S-c ·
`docs/review/최근.md`.

**시험 주석이 정본입니다** — 값이 거기 있고 고칠 때 반드시 지나가는
자리이기 때문입니다. 다만 **어디가 정본인지 적혀 있지 않습니다.**

측정표 쪽은 이번에 정했습니다 — `tests/test_docs_shell.py` 머리말이
「정본은 CLAUDE.md 11-2 다」 라고 적습니다. 정규식 쪽도 같은 방식으로
한 줄 적으면 됩니다.

---

2026-09-05 3판 검토에서 나왔습니다. **「막아야 함」 은 없었고**, 아래는
그 아래 무게라 목록으로 넘깁니다.

## S-f · 「이 목록이 전부가 아니다」 가 정본에는 없습니다 — 처리했습니다

> **2026-09-05 에 그 문구를 CLAUDE.md 11-2 로 옮겼습니다.** 시험 주석과
> 한 글자도 다르지 않고, 어느 쪽이 정본인지 양쪽에 적었습니다.
> 아래는 그때 무엇이 문제였는지의 기록입니다.

`CLAUDE.md` 11-2 (정본) · `tests/test_docs_shell.py` (시험)

시험 주석은 이렇게 경계합니다 — 「이 목록이 전부가 아니다. 새 명령을
적을 때는 `Get-Command <이름> -All` 로 한 번 봐야 한다. 여기 없다고
안전하다는 뜻이 아니다.」

**정본에는 그 문장이 없습니다.** 셋을 나열해 두기만 해서
「이 셋만 조심하면 된다」 로 읽힙니다.

**규칙을 읽고 문서를 적는 사람은 시험 주석을 안 봅니다.**
11-3 의 「언제 조심할지를 정하는 규칙은 그 언제가 아닌 때에 조용히
뚫린다」 자리입니다.

## S-g · 측정표가 두 곳인데 둘이 같은지 재는 시험이 없습니다

`CLAUDE.md` 11-2 · `tests/test_docs_shell.py` 머리말.

이 판이 고친 고장이 정확히 「한 사실을 두 곳에 적고 한쪽만 고쳤다」
였는데, 고친 방법은 시험 머리말에 **「정본은 CLAUDE.md 11-2 다」** 를
적은 것입니다. **그건 사람이 기억해야 하는 규칙이지 검사가 아닙니다** —
두 블록이 갈려도 918 은 그대로 초록입니다.

S-e(같은 정규식 뜻이 세 곳)와 같은 갈래입니다.

## S-h · 설명 줄 가르기가 줄 전체에 걸립니다

`tests/test_docs_shell.py` 의 `설명줄`.

지금은 과하지 않습니다 — 실측으로 **531줄 중 넷**만 떨어지고 전부
의도한 측정표이며, 진짜 명령은 하나도 안 놓쳤습니다.

다만 조건이 **줄 전체**에 걸려서, 언젠가 설명이 붙은 명령 한 줄
(`python x.py   -> 이렇게 나옵니다`)을 적으면 **그 줄만 통째로 검사
밖**입니다.

## S-i · 브리핑이 「비공개 저장소 · 커밋 40개」 라고 적었습니다 — 처리했습니다

> **2026-09-05 에 기준일을 오늘로 올리고 넷을 고쳤습니다** — 기준일 ·
> 공개 여부 · 커밋 수 · 상세 패널 점검 항목 수. 「그때의 스냅샷」 이라고
> 적어 두면 읽는 사람이 언제 것인지 매번 계산해야 해서 그 길을 택하지
> 않았습니다. 아래는 그때 무엇이 문제였는지의 기록입니다.

`docs/현황-브리핑.md:9`

11-2 는 2026-09-01 에 **공개로 바꿨다**고 적고, 실제 커밋 수는
160 개입니다. 「비공개」 는 실명 판단의 전제라 숫자보다 무겁습니다.

**바로 한 줄 위(8줄)를 이번에 고쳤는데 이 줄은 못 봤습니다** —
11-3 3단계가 가리키는 자리입니다.

다만 이 문서는 머리에 **「기준일: 2026-09-01」** 을 달고 있어서,
숫자만 오늘 것으로 갈아 끼우는 것이 맞는지 자체가 갈립니다.
**기준일도 올릴지 · 그날 값으로 되돌릴지 · 「그때의 스냅샷」 이라고
한 줄 적을지** — 셋 다 말이 되고 사람이 정할 일입니다.
(어느 쪽이든 918 · 공개 저장소 · 커밋 160 을 함께 봐야 합니다.)

## S-j · 꺾쇠 자리표시자가 8-1 에도 있습니다

`docs/배포-안내.md:229` 는 **8-1 첫 관리자 계정 만들기**라 처음 하는
사람이 반드시 지나는 자리입니다. `<이름>` 은 따옴표 안이라 무해하고
**`<연락처>` 만 밖**입니다.

S-d 에 적은 「이번 판이 만든 것이 아니다」 는 맞습니다 — cmd 에서도
`<` 는 입력 리디렉션이라 원래 깨져 있었습니다. **깨지는 모양만
바뀝니다**(cmd 는 실행 중 실패, PowerShell 은 파스 단계에서 멈춤).

바로 위에 「본인 것으로 바꿔서 치세요」 가 있어 시킨 대로 하면 됩니다.

---

2026-09-05 「드로어가 드래그로 닫히는 것」 판에서 나왔습니다.

## S-k · 이 규칙을 pytest 가 안 지킵니다

`app/static/js/drawer.js` 의 down/up 판정을 지키는 것은 **사람이 콘솔에
붙여 넣는 `docs/checks/drawer.js` 뿐**입니다. 918건에는 아무 관계가
없습니다 — 시험 어디에도 `clickOrigin`·`pointerdown` 을 보는 곳이
없습니다.

**아무도 안 붙여 넣으면 아무것도 안 지켜집니다.** 이번 고장이 정확히
그렇게 두 번째까지 왔습니다.

헤드리스 브라우저(Playwright 등)를 붙이는 것이 답인데 **이 판보다
큽니다** — 의존성이 하나 늘고, CI 가 없는 이 저장소에서 누가 언제
돌릴지도 함께 정해야 합니다.

10장이 그 스크립트를 규칙으로 세워 둔 것이니 지금 상태가 이 프로젝트의
방식과 어긋나지는 않습니다. **다만 「막는 코드에는 막히는 쪽 시험이
함께 있다」(11-3)가 여기서는 사람의 손에 달려 있습니다.**

## S-l · 목업이 아직 400ms 방식입니다

`docs/mockups/retreat-board-v4.html` (646 · 1018 · 1023 · 1085 · 1092)

**고치지 않습니다.** 4-0 이 목업을 **시각 스펙**이라고 했습니다 —
배치·간격·상태 표현이 기준이지 동작이 기준은 아닙니다.
**동작까지 맞추면 목업이 두 번째 구현이 되고, 그때부터 갈립니다.**

다음에 목업을 다시 만들 일이 생기면 그때 함께 봅니다.

## S-m · 「기록수명」 을 걷어냈지만, 그 자리에 막히는 쪽 시험은 얕습니다

`app/static/js/drawer.js` · `docs/checks/drawer.js`

`click` 없이 끝난 포인터 뒤에 기록이 남는 문제를 `detail` 로 갈랐습니다
(포인터가 낸 클릭은 `detail >= 1`, 키보드로 낸 것은 `0`).

점검에는 **「우클릭 뒤 키보드 클릭」** 하나가 있는데, 그건 `contextmenu`
길만 밟습니다. **다른 두 길은 아무 항목도 안 밟습니다** —

- 창 밖에서 뗀 드래그 (`pointerup` 이 문서에 안 옴)
- 가운데 버튼 (`auxclick` 이라 `click` 도 `contextmenu` 도 없음)

**지금은 새지 않습니다** — `detail` 이 결정적이라 기록이 낡아도 키보드
클릭이 그것을 안 봅니다. 다만 11-3 의 「막는 코드에 시험이 없으면 그
경로를 처음 밟는 것은 실제로 막아야 할 순간」 자리입니다.

## S-n · 핸들 항목이 「안 닫히는가」 말고 다른 이유로 빨개질 수 있습니다

`docs/checks/drawer.js`

핸들 드래그는 **폭을 실제로 바꿉니다.** `check()` 는 뒤 스냅샷에서
스크롤이 4px 넘게 움직였는지도 보는데, 폭 변화가 보드 스크롤을 밀면
그 항목이 **재려는 것과 다른 이유로** 빨개집니다.

지금은 안 납니다. 폭을 되돌리는 것은 `끌어보기` 가 끝난 **뒤**라
그 항목 자신의 판정에는 못 미칩니다.

## S-o · `pointerdown` 이 버튼을 안 가립니다

`app/static/js/drawer.js`

오른쪽·가운데 버튼의 `pointerdown` 도 기록합니다.
`e.button === 0 && e.isPrimary` 만 기록하면 그 둘은 애초에 안 남습니다.

**지금 새지는 않습니다**(`detail` 이 막습니다). 기록을 좁히는 편이
읽기에 낫지만, 좁히면 S-m 의 「우클릭 뒤 키보드 클릭」 항목이 재는
대상도 함께 바뀌므로 둘을 같이 봐야 합니다.

## S-p · 채점 화면이 「표줄을 못 찾았다」 를 조용히 넘깁니다

`scripts/score.py` 의 `_채운다` · `main`

「놓친 것」 을 두 자리에 함께 적는데(회의 끝 줄 + 「다 채운 뒤」 표),
표줄을 못 찾으면 `if 이것["표줄"] is not None:` 이 **말없이 건너뜁니다.**
그러면 한 자리만 채워지는데, 그건 이 코드가 막으려고 만들어진 바로
그 상태입니다.

**뜰 때 세는 것이 제안 수뿐입니다.** 놓친표·종류줄·X이유줄·일판줄이
통째로 비어도 `회의 4건 · 제안 27개 · 걸음 32개` 가 그대로 찍힙니다.
일판줄이 없으면 `03.29 #1` 은 물어보지도 않고 걸음이 31개가 되는데
그것도 안 보입니다.

지금 실제 파일에서는 넷 다 이어져 있습니다(놓친표 키 == 짧은제목).
「제안 0개면 안 뜬다」 를 넣은 그 자리에 한 줄 더 있으면 닫힙니다.

## S-q · 읽은 줄과 쓰는 줄이 서로 다른 읽기입니다

`scripts/score.py` 의 `_채운다` · `쓴다`

`_채운다` 가 파일을 읽어 줄 번호를 정하고 `쓴다` 가 **다시 읽어** 그 번호에
넣습니다. 그 사이에 파일이 **밖에서** 바뀌면(사람이 채점표를 편집기로 열어
한 줄 더하고 저장) 엉뚱한 줄이 덮입니다.

브라우저 탭이 둘인 것은 안전합니다 — 칸만 갈아 끼우므로 줄 수가 안 변하고,
읽기부터 쓰기까지가 한 자물쇠 안입니다. **밖에서 고치는 경우만 남았습니다.**
「바꾸려는 줄이 아직 읽었던 그 줄인가」 를 견주면 닫힙니다.

## ~~S-r · 「무엇을 놓쳤나」 에 줄바꿈이 들어가면 표가 깨집니다~~ — **닫힘 (2026-09-06)**

> `칸글()` 이 줄바꿈과 탭도 바꿉니다(` ⏎ `). 여기 적어 둔 「개행을 공백으로
> 바꾸는 한 줄」 그대로입니다. **미룬 자리가 아니라 실제로 터진 자리였습니다** —
> 프롬프트를 넓히자 모델이 여러 줄짜리 대목을 인용하기 시작하면서 3판
> **22개 중 5개가 표에서 사라졌습니다.** `score.py` 의 「무엇을 놓쳤나」 도
> 같은 `SS.칸글` 을 쓰므로 함께 닫혔습니다.
>
> **지우지 않고 남깁니다** — 미룬 것이 나중에 어떻게 터졌는지가 이 목록의
> 값입니다. 아래는 그때 적어 둔 내용입니다.



`scripts/score.py` 의 `_채운다`

`SS.칸글()` 은 `|` 만 바꿉니다. 개행이 든 글을 한 번 저장하면 144줄이
145줄이 되고, 그 뒤로 놓친표가 4개에서 3개로 줄어 그 회의의 답은 표에
안 적힙니다 (S-p 와 같은 침묵입니다).

**화면으로는 못 넣습니다** — `input type=text` 라 개행이 안 들어갑니다.
API 로 직접 보낼 때만 납니다. 개행을 공백으로 바꾸는 한 줄이면 됩니다.

## S-s · 「첫째 회의록」 이 SQL 로 정해져 있지 않습니다

`scripts/score.py` 의 `회의록들` · `scripts/suggest_sample.py` 의 `main`

같은 제목의 회의록이 둘일 때(`26.07.05`) **제안을 낸 쪽과 채점이 보는 쪽이
같은 행이어야 합니다.** 그런데 양쪽 다 `order_by` 가 없고 질의 모양도
다릅니다 — 저쪽은 제목으로 좁혀 `.first()`, 이쪽은 회차 전체를 훑어
먼저 온 것을 씁니다.

지금은 둘 다 같은 행을 고릅니다(확인함). 정해 두는 것은
`order_by(Meeting.id)` 한 마디이고, 안 정하면 **인용 대조가 통째로
어긋납니다** — 실제로 한 번 그럴 뻔했습니다.

## S-t · 「새로 파싱하지 않는다」 를 글자로 봅니다

`tests/test_score_ui.py` 의 `test_z_02`

`"SS.제안줄인가" in 글` · `"def 제안줄인가" not in 글` 두 줄인데 주석과
독스트링을 안 걷어냅니다. 10장이 **네 번 당했다**고 적은 그 모양입니다.
지금 통과하는 것은 마침 독스트링에 `SS.` 가 안 붙어 있어서이지 규칙이
있어서가 아닙니다.

같은 파일의 `test_z_15c` 는 주석을 걷어내고 봅니다 — 그쪽으로 맞추거나,
`SS.제안줄인가` 를 잠깐 갈아 끼워 파싱 결과가 따라 바뀌는지 보면 값으로
묶입니다.

## S-u · 채점 화면의 백업은 한 판에 한 번입니다

`scripts/score.py` 의 `백업한다`

27개를 채우는 동안 S-q·S-r 같은 것으로 파일이 상하면 돌아갈 곳이 **첫 쓰기
전**이라 그때까지의 채점이 통째로 사라집니다. 한 번 쓰고 마는 도구이고
사람이 옆에서 보고 있어서 그대로 두었습니다.

같은 자리에 하나 더 — 「무엇을 놓쳤나」 는 `onblur` 로만 저장하므로 적고
나서 바로 창을 닫으면 그 글은 안 남습니다. `O`/`X` 를 누르면 블러가 먼저
나므로 실제 순서로는 거의 안 걸립니다.

## S-v · 「종류줄」 이 한 모듈 안에서 세 가지를 가리킵니다

`scripts/score.py`

| 이름 | 가리키는 것 |
|---|---|
| `_종류줄` | `"종류가 아쉬운 것"` 이라는 **표시 문자열** |
| `상태["종류줄"]` | 「다 채운 뒤」 **종류별 표**(논의·새 업무·결정사항·합계)의 줄번호 |
| `회의["종류줄"]` | 회의마다의 **「종류가 아쉬운 것」 줄**번호 (이번에 새로 생김) |

`test_z_01` 한 줄 안에 둘이 같이 나옵니다. 지금 코드는 맞지만 다음 사람이
헷갈릴 자리이고, `docs/봐둘것.md` 의 S-p 가 적어 둔 「종류줄」 은 **앞엣것**
입니다.

## S-w · 옛 채점표(1·2판)를 열면 「종류가 아쉽다」 단추가 조용히 꺼집니다

`scripts/score.py` 의 `main` · `걸음들`

1·2판 파일에는 「종류가 아쉬운 것」 줄이 없습니다. `--file` 로 그것을 열면
회의마다 `종류줄=None` 이 되고 단추가 비활성으로 뜹니다. **막히지는
않습니다** — 읽기·걸음·쓰기가 모두 `None` 을 지킵니다(2판으로 걸음 32개까지
나오는 것 확인).

다만 **뜰 때 아무 말도 안 합니다.** `main()` 이 세는 것은 제안 수뿐이라
「이 파일에는 그 자리가 없습니다」 가 안 나옵니다 — S-p 가 적어 둔 그
침묵에 자리가 하나 늘었습니다. 그리고 **「종류줄이 없는 채점표」 를 도는
시험이 없습니다**(표본은 셋 다 있습니다).

## S-x · 「채워졌나」 를 보는 기준이 두 곳에서 다릅니다

`scripts/score.py` 의 `읽는다` · `scripts/suggest_sample.py` 의 `채운곳`

```
score.py       if _종류줄 in 줄 and "→" in 줄
suggest_sample elif "종류가 아쉬운 것" in 줄 and 줄.split("→")[-1].strip()
```

가드 쪽은 `"→"` 가 없으면 `split` 결과가 **줄 전체**라 사람이 채운 것으로
봅니다. 지금 생성기가 늘 `→ ` 를 붙이므로 새지 않지만, **문구를 손대는 날
갈립니다.**

## S-y · `칸글()` 이 탭은 표시 없이 지웁니다

`scripts/suggest_sample.py`

독스트링은 「지우지 않고 `⏎` 로 보이게 남긴다 — 인용은 원문 그대로가 값이라
줄이 나뉘어 있었다는 사실도 함께 남아야 한다」 인데, **탭은 아무 표시 없이
공백이 됩니다.** 한 함수 안에서 원칙과 동작이 어긋납니다.

줄바꿈만큼 뜻을 지지 않아 그대로 두었습니다. 인용의 들여쓰기가 뜻을 갖는
자료가 나오면 그때 함께 봅니다.

## S-z · 3판이 넓힌 방향과 **반대로** 줄었습니다

`docs/review/제안-성적표.md` 의 3판 절

넓힌 셋(흐릿한 줄 보기 · 제목 버리지 않기 · 업무까지 내기)은 **모두 늘리는
쪽 지시**인데 네 회의 중 셋이 줄었습니다.

```
03.29   1 → 1        결정사항 12 → 11
05.24   9 → 6   ←    논의      8 → 7
07.05   3 → 2        새 업무   7 → 4   ← 가장 많이 줄었다
08.09  14 → 13
```

`05.24` 는 **사람이 놓친 것 10건을 적은 바로 그 회의**입니다.
「덜 낸 것이 정확해진 것인지」 는 채점 전이라 못 가르는 것이 맞지만,
**방향이 지시와 반대라는 사실 자체**는 채점하면서 볼 값입니다.

## S-aa · 「제작」 을 뺀 판단은 이 자료로는 아무것도 안 가릅니다

`scripts/score.py` 의 `_흔한낱말`

주석이 「실제 자료로 넣고 빼며 재 봤고 빼도 헛짚음이 늘지 않았다」 고
적었는데, **넣어도 안 늡니다**(짚힌 토막 3 → 3). 이 자료로는 어느 쪽이든
결과가 같습니다.

판단 자체는 논리로 서 있습니다(`큐시트 제작` 과 `현수막 제작` 은 다른 일).
**근거를 측정처럼 적은 것**만 봐 둘 것입니다 — 다른 자료가 생기면 그때
실제로 갈리는지 봅니다.

## S-ab · 「다 채운 뒤」 가 **1판과만** 견줍니다 — 4판 전에 고칩니다

`scripts/suggest_sample.py` 의 `다채운뒤` · `증감말` · `일판회의별`

두 함수가 **코드에 박힌 1판 상수**하고만 견줍니다.

```
일판 = {"논의": (8, 13), "새 업무": (6, 8)}      일판합계 = (14, 21)
일판회의별 = {"26.03.29 (1차)": (1, 1), …}
```

그래서 3판 파일의 「다 채운 뒤」 는 **3판 vs 1판**입니다. 정작 견줘야 할
직전 판(2판)과의 대조는 **보고에 사람이 손으로 적은 수**뿐입니다 —
`27 → 22` 도 `05.24 9 → 6` 도 제가 세어 적은 것이고, 문서와 파일 어디에도
계산으로 남지 않습니다.

**4판 전에 고칩니다.** 판이 하나 더 늘면 「1판과만 견주는 표」 가 셋이
되고, 손으로 적은 수가 세 판 몫으로 늘어납니다 — 그때부터는 어느 것이
잰 값이고 어느 것이 적은 값인지 갈리지 않습니다.

고치는 방향은 **직전 판 `.real.md` 를 읽어 회의별 수를 끌어오는 것**입니다.
`score.py` 의 `읽는다()` 가 이미 그 파일을 회의·제안으로 나눕니다 —
새로 파싱하지 말고 그것을 씁니다.

**없을 때를 함께 정해야 합니다.** `.real.md` 는 gitignore 라 **새 컴퓨터나
`data/` 를 새로 받은 판에는 없습니다** — `suggest_sample.py` 가 이미 그
경우(「`.real` 이 없고 공개본만 있을 때」)를 다루고 있습니다. 익명화
사본(`docs/review/제안-N판.md`)으로 물러설지, **말하고 건너뛸지**를 함께
적어야 다음 사람이 그 자리에서 다시 헤매지 않습니다.

**그리고 이 시한을 읽는 자리가 여기 하나뿐입니다.** 4판은 스크립트를
돌려서 만드는데, **스크립트를 돌리는 사람은 이 파일을 열지 않습니다** —
11-3 이 「마치기 전에」 여기를 가리키는 것과 시점이 다릅니다. 고칠 때
같은 말을 `다채운뒤()` 의 독스트링이나 **생성물 한 줄**에도 두세요.

> 위의 `27 → 22`·`05.24 9 → 6` 은 **S-z 와 성적표에도 손으로 적혀 있습니다.**
> 이 항목이 말하는 문제를 이 항목이 한 번 더 늘린 셈이라, 고칠 때 그
> 수들도 함께 계산으로 옮깁니다.

## S-ac · `03.29 #1` 을 3판 파일이 **다시 묻습니다**

`scripts/suggest_sample.py` 의 `다채운뒤`

```
**1판의 `03.29 #1` 도 여기서 정합니다.** … 지금 넷 중 하나로 정해 주세요.
→ `다른업무` / `할일아님` / `중복` / `기타` 중: ______
```

이 자리는 **2판에서 이미 답했습니다**(`기타` — 사람이 채운 40곳 중 하나).
그런데 3판 파일에도 빈 칸으로 또 나갑니다.

**두 답이 갈릴 수 있습니다.** 갈리면 어느 것이 사람의 뜻인지 알 수 없고,
성적표에는 이미 `기타` 로 옮겨 적혀 있습니다.

고치는 방향은 둘 중 하나입니다 — **2판에서만 내거나**, 이미 답한 것을
읽어 **답을 보여주고 안 묻거나**. 뒤엣것이 낫습니다: 3판을 채점하는
사람이 그 판단이 있었다는 것을 알아야 하고, 「2판에서만」 은 `…할 때만`
꼴이라 4판·5판에서 또 뚫립니다.

> **다만 그대로 하면 가드가 뒤집힙니다.** `채운곳()` 은 `→ … 중: X` 가
> 채워져 있으면 **사람이 채운 것**으로 셉니다. 스크립트가 답(`기타`)을
> 미리 써 넣으면 **갓 만든 3판이 `--replace` 로도 안 덮이고**,
> `막는다()` 가 「사람이 채운 것이 있습니다」 라고 **거짓을 말합니다.**
> `채운곳()` 의 독스트링이 적어 둔 그 함정입니다 — 「자기가 적은 것을
> 사람이 채운 것으로 오인해 영영 못 덮는다」.
>
> **보여주되 `채운곳()` 이 스크립트가 적은 것으로 알아보게** 해야 합니다.
> 「2판과의 대조」 표에서 이미 쓰는 방법이 있습니다 — 표 머리를 보고
> 사람 몫과 스크립트 몫을 가르거나, `O(도출)` 처럼 **꼬리표를 붙이는 것**.

## S-ad · 되붙인 다섯 줄을 `--replace` 가 지우지 않습니다

`scripts/suggest_sample.py` 의 `채운곳` · `data/제안-3판.real.md`

되붙임 표시 줄은 **일부러** 가드에 안 걸리게 적었습니다(수용 기준이 그것을
요구했습니다). 그 뜻은 **`--replace` 한 번에 그 줄이 사라진다**는 것입니다.

그런데 22줄은 **669원짜리 유일본**이고 그중 다섯은 손으로 되붙인 것입니다 —
지켜야 할 것의 성격이 「사람이 채운 판정」 과 다르지 않습니다.

**지금 뚫린 곳은 아닙니다** — `--replace` 없이는 `SystemExit 5` 로 막히고,
채점을 시작하면 판정이 들어가 그때부터 `--replace` 로도 안 덮입니다.
**채점 전에 다시 내는 경우**만 남습니다.

정할 것은 하나입니다 — **지킬 것인가, 안 지키는 것이 뜻인가.**
지킨다면 되붙임 표시도 `채운곳()` 이 보게 하고, 그러면 「갓 만든 3판을
다시 못 낸다」 가 따라옵니다(S-ac 와 같은 함정).

2026-09-07 단계 4 검토에서 나왔습니다. **이 판에서 고치지 않기로 한
것들입니다** — 「고쳐야 함」 여섯(보관 회차의 지연 제안 · 확인 요청 수신
키 비교 · 제목 편집 Esc · 색 정본 · 모두 읽음 숫자 · 모르는 키 거절)은
그 판에서 고쳤고, 아래는 그 아래 무게입니다.

## T-a · 이관 스크립트의 파일 복사가 커밋보다 먼저입니다

`scripts/migrate_files.py`

DB 는 마지막 commit 하나라 원자적이지만, 복사 도중 실패하면 이미 복사된
파일이 `ATTACHMENT_DIR` 에 **고아로** 남습니다(DB 에 없어 화면에는 안
보이고 백업에만 실립니다). 그 상태에서 재실행하면 이관 로그가 커밋 전이라
가드에 안 걸리고 새 임의 이름으로 또 복사합니다.

**운영 이관은 3파일 8KB 로 이미 끝났습니다** — 다시 돌 일 자체가
없습니다. 이관류 스크립트를 또 만들 때 「복사 목록을 만들어 두고 실패 시
지우기」 를 함께 넣는 자리로 남깁니다.

미리보기 모드도 `init_db()` 를 지나므로 부팅 마이그레이션(run_no 매김
등)은 실제로 돕니다 — 앱이 어차피 할 일이지만 「미리보기 = 아무것도 안
바뀜」 은 아닙니다. 스크립트가 그 말을 하지 않습니다.

## T-b · `next_run_no` 가 max+1 이라 동시 요청이 같은 번호를 받을 수 있습니다

`app/domain/library.py` — 유니크 제약 없음.

단일 프로세스 · 19명 규모라 확률은 낮지만, 번호는 「회의에서 부르는
손잡이」 라(4-14) 겹치면 비쌉니다. 유니크 인덱스(retreat_id, run_no)를
붙이면 닫히는데, 기존 SQLite 파일에 인덱스를 더하는 마이그레이션 자리를
정해야 해서 이 판에서는 미뤘습니다.

2026-09-07 단계 5 검토에서 나왔습니다. **이 판에서 고치지 않기로 한
것들입니다.**

## ~~U-a · 재정 편집 권한이 아직 부서 id 비교입니다~~ — **처리함 (마무리 판)**

> permissions 층에 `can_edit_department_by_key` 를 두고
> `security.assert_can_edit_department` 가 대상 부서 행을 열어 **키로**
> 견줍니다 — expenses·checklists 아홉 자리가 전부 이 문으로 갑니다. 화면
> 버튼(`can_edit_dept`)도 행을 받아 같은 축으로 고쳤습니다. 키 없는
> 부서(구설계)만 id 로 되돌아갑니다 — None==None 통과 방지.
> 남은 id 비교는 `test6_k05` 가 자리를 이름으로 지킵니다.
> 시험 test6_k01~k05. 아래는 그때 적어 둔 내용입니다.

`app/routers/expenses.py` 의 지출 등록·수정·영수증 붙이기 →
`security.assert_can_edit_department` → `domain/permissions.py`
(`user_department_id == target_department_id`). 화면 버튼(`can_edit_dept`)도
같은 비교입니다.

**새 회차가 열리면 부서 리더·팀원이 자기 부서 지출을 못 만집니다(403)** —
2장이 경고한 그 모양이고, 버튼도 함께 사라져 조용히 틀립니다. 단계 5에서
흐림(is_other_dept)과 확인 요청 수신은 키 비교로 옮겼는데, 이 자리는
`assert_can_edit_department` 를 쓰는 다른 화면(체크리스트 등)과 한 몸이라
**반쪽만 고치면 화면마다 규칙이 갈립니다.** 옮길 때는 permissions 층에서
한 번에 — 그 언저리를 손댈 때 먼저 엽니다.

## ~~U-b · 환급 판정이 지출자 표기 리터럴입니다~~ — **처리함 (마무리 판)**

> `is_refund_target` 이 **공백을 지우고** 견주고, 빈 값·None 은 환급
> 대상이 아닙니다 — 지출자를 안 적은 것이지 개인이 아닙니다. 입력 폼에는
> 「수련회계좌」 원클릭 단추(`payer-fill`)를 두어 변형 표기 자체를
> 줄였습니다. 시험 test6_r01(넷)·r02. 아래는 그때 적어 둔 내용입니다.

`domain/budget.py` 의 `RETREAT_ACCOUNT = "수련회계좌"`. 빈 지출자·
「수련회 계좌」(띄어쓰기) 같은 변형은 **환급 쪽으로** 떨어집니다 — 숨는
방향이 아니라 더 보이는 방향이라 두었습니다. 입력 폼이 자유 텍스트라
오타가 잡음이 되면 그때 공백 제거 비교로 좁힙니다.

## ~~U-c · /refunds 301 이 쿼리를 버립니다~~ — **처리함 (마무리 판)**

> 301 이 `request.url.query` 를 그대로 붙여 넘깁니다. 시험 test6_u01.
> 아래는 그때 적어 둔 내용입니다.

옛 즐겨찾기 `/refunds?retreat_id=N` 이 현재 회차의 필터로 떨어집니다.
필터 자체는 유지되므로 작습니다.

## ~~U-d · 로그인한 채 404·회차 없음 화면을 보면 머리가 헐렁합니다~~ — **처리함 (마무리 판)**

> mast 의 부제를 `{% if page_subtitle %}` 로 감싸 값이 없으면 「· 」 를
> 아예 내지 않습니다. 시험 test6_u02. 아래는 그때 적어 둔 내용입니다.

`retreat_base` 의 mast 가 `page_subtitle`·`retreats` 미전달로 「· 」 빈
부제가 남고 회차 드롭다운이 안 뜹니다. 화면은 안 죽습니다 — 외관만.

## ~~U-e · 이미지가 실명 검사의 볼 목록 밖입니다~~ — **처리함 (마무리 판)**

> 입구와 출구를 둘 다 막았습니다. 입구 — `scripts/check_dev_db.py` 가
> devserve 가 뜨기 **전에** 개발 DB 의 사람 이름 칸을 대응표 실명과
> 대조해, 하나라도 맞으면 띄우지 않고 멈춥니다(이름은 찍지 않고 자리·수만).
> 출구 — `check_names.py` 의 이미지검사() 가 docs/ 의 **새 이미지**
> (마지막 커밋에 없던 것)를 `docs/review/이미지-확인.md` 와 대조해, 사람이
> 눈으로 봤다고 적지 않은 것이 있으면 실패합니다. 규칙은 11-2.
> 시험 test6_g01~g04. 아래는 그때 적어 둔 내용입니다.

`scripts/check_names.py` 는 텍스트만 봅니다 — **스크린샷(PNG)은 아무도
기계로 안 봅니다.** 단계 5에서 실제로 샜을 뻔했습니다: 개발 DB
(`%TEMP%\dcb-dev\app.db`)가 익명화 전 자료를 들고 있었고, 그걸로 찍은
스크린샷 2장에 실명 7개가 들어간 채 커밋 직전까지 갔습니다 — **커밋 전
검토자가 잡았습니다.** 개발 DB 는 지우고 seed(가명)로 다시 만들었습니다.

「docs/ 에 새 이미지를 커밋할 때는 사람이 눈으로 본다」 를 규칙으로 적을지,
검사에 이미지 정지 장치를 더할지는 사람이 정할 일입니다.

2026-09-07 마무리 판 검토에서 나왔습니다. **새로 세운 방벽의 약한 곳**들이라
그 언저리를 손댈 때 먼저 봅니다. (검토의 「고쳐야 함」 둘 — 게이트 정규식의
M+조사, 깨진 대응표 fail-open — 은 그 판에서 바로 고쳤습니다.)

## ~~V-a · 덮어 찍은 이미지는 「새 이미지」 가 아닙니다~~ — **처리함 (화면 다듬기 판)**

> 확인 기록(이미지-확인.md)에 경로와 **내용 해시(md5 12자리)** 를 함께
> 적고, check_names 가 docs/ 이미지 **전부**를 해시까지 대조한다 — 같은
> 이름으로 다시 찍으면 해시가 갈려 다시 사람 확인을 요구한다. HEAD 비교는
> 걷었다(확인 파일이 유일한 기준). 시험 test6_g03. 아래는 그때 적어 둔 내용이다.

`check_names.py` 의 새이미지() 는 **경로 기준**(`git ls-tree HEAD`)입니다.
HEAD 에 이미 있는 경로에 이미지를 **다시 찍어 덮으면** 새 이미지가 아니라서
기계도 사람도 안 봅니다 — 재촬영은 실제로 일어나는 일입니다
(이미지-확인.md 의 「재촬영본」 두 건). 내용(blob) 비교로 넓힐지는
사람이 정할 일입니다.

## V-b · 이미지 확인 파일 대조가 substring 입니다

미확인이미지() 는 확인 파일 어디에든 경로가 적혀 있으면 통과합니다 —
「아직 못 봄: 경로」 같은 줄도 확인으로 칩니다. 줄 머리 형식을 강제하지
않은 선택이고 지금 새는 곳은 없습니다. 덧붙여 `.svg` 는 글인데
이미지꼴로만 취급됩니다(사람 확인은 받으니 보수적인 쪽).

## ~~V-c · k05 가드는 `department_id ==` 를 적은 파이썬만 봅니다~~ — **부분 처리 (화면 다듬기 판)**

> 허용 목록을 (파일, 줄, 식) → 이유로 조였다 — 같은 파일에 같은 식이 하나
> 더 생기거나 자리가 움직이면 빨개지고, 전수와 목록의 수가 같은지도 센다
> (0이면 실패). 가짜 비교를 심으면 잡히는 것을 test6_k05b 가 잰다.
> **템플릿과 다른 모양(`!=`·`in`·`.department.id`)은 여전히 밖이다** —
> 아래 원문 그대로 남는 한계다.

`test6_k05` 는 문자 그대로 `department_id` 가 든 `==` 비교를 `app/**/*.py`
에서만 봅니다. `user.department.id == target.id` · `!=` · `in dept_ids` ·
**Jinja 템플릿**은 범위 밖입니다 — 이번 판에 checklists.html 에서 고친 것이
정확히 템플릿 속 그 모양이었습니다. 허용 목록이 (파일, 식) 쌍이라 같은
파일에 같은 식이 하나 더 생겨도 초록입니다.

## V-d · /refunds 301 에 filter= 가 이미 있으면 중복 키가 됩니다

`?filter=refund&` 뒤에 옛 쿼리를 그대로 붙이므로 손으로 만든
`/refunds?filter=x` 는 `filter` 가 두 번 들어가고 Starlette 는 마지막 값을
취합니다. 옛 `/refunds` 에는 filter 가 없었으니 실제로는 안 일어납니다.

## V-e · 「수련회계좌」 가 두 곳에 있습니다

`domain/budget.py` 의 `RETREAT_ACCOUNT` 와 expenses.html 단추의
`data-value` 리터럴. 상수가 바뀌면 단추가 판정과 다른 값을 채웁니다
(환급 쪽으로 떨어져 눈에는 보입니다). 컨텍스트로 상수를 넘기면 한 곳이 됩니다.

## ~~V-f · 지출의 department_id 가 현재 회차 행인지 검사하지 않습니다~~ — **처리함 (화면 다듬기 판)**

> create_expense 가 부서 행을 열어 `retreat_id` 를 견주고, 다른 회차의
> 행이면 400 으로 거절한다. 운영 DB 를 세어 보니 회차가 다른 기존 행은
> **0건**이라 고칠 데이터는 없었다. 시험 test7_b01. 아래는 그때 적어 둔 내용이다.

`create_expense` 는 넘어온 부서 id 의 키만 봅니다. 키 비교 도입으로
「내 키의 아무 회차 부서 행」 이 권한을 통과하므로, 다른 회차의 부서 행
id 를 단 지출이 이번 회차에 생길 수 있습니다. 이 모양 자체는 키 비교
전부터 있었고 지금 새는 화면은 못 찾았습니다 — 지출 입구를 손댈 때
회차 소속 검사를 함께.

2026-09-07 화면 다듬기 판 검토에서 나왔습니다. **이 판에서 고치지 않기로
한 것들입니다** — 지금 새는 곳은 없습니다.

## ~~W-a · 이미지 확인 대조가 경로·해시를 같은 줄로 묶지 않습니다~~ — **처리함 (초대 링크 판)**

> 미확인이미지() 가 **같은 줄**에 경로와 해시가 함께 있어야 통과로 본다 —
> 두 이미지의 해시를 맞바꾸면 둘 다 걸린다. 시험 test8_w03.
> 아래는 그때 적어 둔 내용이다.

`check_names.py` 의 미확인이미지() — 경로와 해시가 확인 파일 **어디든**
있으면 통과라, 두 이미지의 내용을 맞바꾸는 극단이 지나갑니다. 실익 대비
줄 단위 대조로 조일지 판단할 자리입니다 (V-b 의 substring 한계와 같은 갈래).

## ~~W-b · 직전 입력값 제안이 취소된 식대 행도 잡습니다~~ — **처리함 (초대 링크 판)**

> _last_meal_defaults 가 canceled_at IS NULL 만 본다. 시험 test8_w01.
> 아래는 그때 적어 둔 내용이다.

`expenses.py` 의 `_last_meal_defaults` — 잘못 넣어 취소한 「모임 식사비-N」
이름이 다시 제안될 수 있습니다. 제안일 뿐이라 새지는 않습니다.

## ~~W-c · 취소된 지출 행에 지급 토글이 그대로 노출됩니다~~ — **처리함 (초대 링크 판)**

> 취소 행에는 지급 폼 대신 상태 배지만 낸다. 시험 test8_w02.
> 아래는 그때 적어 둔 내용이다.

의미 없는 조작이 가능합니다 — 합계에는 안 끼므로 숫자는 안 샙니다.
취소 행에서 지급 폼을 감출지 판단할 자리입니다.

## W-d · templating 의 live_title 주석이 옛 이름을 씁니다 — 처리함

> 「진행 화면의 이름」 → 「총무팀 일정(진행 화면)의 이름」 으로 그 판에서
> 바로 고쳤습니다.

## W-e · 예산 항목 행이 level2 비연속 반복에 두 번 섭니다

`budget.html` 의 l2row 는 「직전 행과 level2 가 다르면」 세웁니다.
sort_order 가 흐트러져 같은 level2 가 떨어져 있으면 같은 항목 행이 두 번
섭니다. 지금 자료로는 문제 없습니다.

2026-09-07 초대 링크 판 검토에서 나왔습니다. **이 판에서 고치지 않기로
한 것들입니다** — 지금 새는 곳은 없습니다.

2026-09-07 남은 것 닫기 판 검토에서 나왔습니다. **이 판에서 고치지 않기로
한 것들입니다** — 지금 새는 곳은 없습니다.

2026-09-07 검사 범위 맞추기 판 검토에서 나왔습니다. **이 판에서 고치지
않기로 한 것입니다** — 지금 새는 곳은 없습니다.

## Z-a · anonymize 의 바꾸기(swap)는 번호 변형을 안 바꿉니다

찾기(check_names·게이트)는 번호의 세 변형을 다 보지만, 바꾸기는 대응표
원표기만 치환합니다 — 3-4-4 실번호가 검사에 걸려도 미리보기·--apply 에는
안 나오고 사람이 손으로 고칩니다. 이름의 엄격/느슨 분리와 같은 설계이나
번호는 치환해도 낱말이 깨질 위험이 없어, 다음에 실제로 걸렸을 때
「미리보기에 왜 안 나오나」 를 겪기 전에 변형 치환을 붙일 자리입니다.

## ~~Y-a · 연락처 패턴의 두 비대칭~~ — **처리함 (검사 범위 맞추기 판)**

> 변형(숫자·3-4-4·공백)을 만드는 곳을 `anonymize.번호변형들` 하나로 —
> 문서 검사(표기들)와 게이트가 같은 것을 부른다. 하이픈으로 적어도
> 숫자를 먼저 모아 두 형이 다 나온다. 시험 test10_v01~v04(두 검사가
> 같은 문자열에 같은 판정). 아래는 그때 적어 둔 내용이다.

게이트(check_dev_db)는 대응표 번호가 **11자리 숫자일 때만** 3-4-4 변형을
만듭니다 — 나중에 누가 번호를 하이픈 붙여 대응표에 적으면 숫자-그대로
형이 조용히 빠집니다. 그리고 문서 쪽 check_names 는 표기들() 원문
그대로만 찾아 **문서에 3-4-4 로 적힌 실번호를 못 봅니다** — 게이트만
이번에 똑똑해졌고 문서 검사는 그대로라, DB 와 문서의 검사 범위가
갈라졌습니다. 연락처 검사를 손댈 때 둘을 한 규칙으로 모읍니다.

## ~~X-a · 배너의 u= 는 링크(k)와 묶여 있지 않습니다~~ — **처리함 (남은 것 닫기 판)**

> 검토자가 권한 근본 해소 그대로 — stash 가 (원문, 이름)을 한 키에 담고
> take 한 번에 둘 다 나온다. u= 는 만드는 곳도 읽는 곳도 없다(0곳).
> 시험 test9_t01(남의 id 를 붙여도 아무것도 안 바뀜)·t02(이름과 로그인
> 계정 일치). 아래는 그때 적어 둔 내용이다.

`admin_users.py` 의 issued_for — u= 는 이름 표시용 장식이라, 주소를 손으로
바꿔 열면 「B 님의 링크입니다」 아래 A 의 링크가 뜰 수 있습니다. 만들 수
있는 사람이 그 admin 자신뿐이라 실익이 작습니다. 근본 해소는 stash 에
(원문, 이름)을 함께 담아 u= 를 없애는 것 — take 한 번에 둘 다 나오므로
어긋날 자리가 사라집니다.

## ~~X-b · 장소 가명의 글자 수 규칙이 정해지지 않았습니다~~ — **처리함 (남은 것 닫기 판)**

> 글자 수 유지는 **사람 이름에만** — 이유(화면 폭·M 접미)가 사람 이름의
> 사정이다(11-2). test_대응표_04 가 장소를 빼고 재고, 04b 가 길이 다른
> 장소 쌍의 통과를 합성 자료로 잰다. 장소들() 의 이중 파싱도 걷었다 —
> load_map 이 셋째 값으로 내고 창구는 그것을 돌려준다.
> 아래는 그때 적어 둔 내용이다.

`test_anonymize_map` 의 글자 수 일치 검사가 장소 쌍에도 걸립니다. 지금은
4자↔4자라 통과인데, 길이가 다른 장소를 넣는 순간 이유(화면 폭·M 접미 —
사람 이름용) 없는 빨강이 뜹니다. 그때 장소를 예외로 할지 규칙을 지킬지
정합니다. `anonymize.장소들()` 이 load_map 밖에서 places 구조를 두 번째로
파싱하는 것도 같은 자리에서 봅니다 — 구조를 아는 곳이 한 파일 안에
둘입니다.

## X-c · 실제 장소명이 든 옛 스크린샷·글은 히스토리에 남습니다 — **고치지 않음 확정**

**히스토리에 남아 있고, 고치지 않습니다** (정책 — 되돌린다는 보장이
없는데 지금까지의 검토 주소가 전부 죽는다). 지금부터 막는 쪽입니다:
게이트가 글자 칸 전부를 기본으로 훑게 되어(제외만 이유와 함께) 같은
경로는 입구에서 잡힙니다.

## ~~T-c · 사이드바 배지가 확인 요청 하나를 둘로 셉니다~~ — **처리함 (단계 5)**

> 배지 = 안 읽은 **시스템** 알림 + 답 대기 요청 (4-0 원문 그대로) 으로
> 고쳤습니다 — 사람 발신은 앞항에서 빠지고 뒷항으로만 세어 +1 입니다.
> 「답을 기다리는 것」 칩이 배지의 뒷항과 **같은 함수**(pending_for_user)를
> 쓰게 되어 부서 없는 admin 의 배지-화면 불일치도 함께 닫혔고, 확인결과
> 항목의 바로가기 없음은 「내가 보낸 요청」 탭이 그 자리를 맡습니다.
> 시험 test5_c02. 아래는 그때 적어 둔 내용입니다.

`base.html` · `retreat_base.html` — 배지 = 안 읽은 알림 + 답 대기 요청.

확인요청은 받는 부서원에게 **알림 1건 + 대기 요청 1건**이라 배지가 +2 로
뜁니다. 4-0 문구(「안 읽은 시스템 알림 + 답 대기 중 요청」)대로라면
unread 쪽에서 사람 발신 kind 를 빼야 하는데, 합산 방식은 단계 4 이전의
retreat_base 그대로라 이 판에서는 안 건드렸습니다.

같은 언저리 둘 —

- **부서 없는 admin** 은 `pending_for_user` 가 전부 잡아 배지에는 뜨는데,
  알림 행은 부서원에게만 가므로 「받은 것」·「답을 기다리는 것」 칩에는
  아무것도 안 뜹니다. 옛 /reviews 는 admin 에게 들어온 요청 전부를
  보여줬는데 그 자리가 사라졌습니다
- **확인결과(사람 발신) 항목에 바로가기가 없습니다** — 템플릿이 시스템
  발신일 때만 링크를 냅니다

## ~~T-d · 확인 요청 취소가 하드 삭제입니다~~ — **처리함 (단계 5)**

> `status='취소'` 로 바꿨습니다 (0장 첫 원칙) — 행이 남고, 「내가 보낸
> 요청」 에 흐리게 보이며, 배지와 답 버튼에서는 빠집니다. 시험 test5_c01.
> 아래는 그때 적어 둔 내용입니다.

`app/routers/reviews.py` 의 `cancel` — `db.delete(review)`.

0장 「아무것도 삭제하지 않는다」 와 긴장합니다. 단계 4 이전부터 있던
동작인데, 확인 요청이 업무 이력(드로어 탭)에 뜨게 된 지금은 취소가
**이력을 지우는 셈**이 됐습니다. 상태를 「취소」 로 바꾸는 쪽이 원칙에
맞는데 REVIEW_STATUSES 와 화면 필터가 따라 움직여야 해서 미뤘습니다.

## T-f · 백업이 2026-08-31 ~ 09-06 이레 동안 산출물이 없습니다 — **닫힘 (2026-09-07)**

> 사람이 화면에서 시작 위치를 고친 뒤 손 실행이 **LastResult=0** 으로 끝나고
> `data/backups/` 에 당일 산출물(app db + uploads zip)이 생겼습니다.
> **그 이레의 공백 자체는 기록으로 남습니다** — 되돌릴 지점이 없는 구간입니다.

`data/backups/` 의 마지막 산출물이 2026-08-31 09:00 (그날 손으로 돌린
것으로 보임). 그 뒤 매일 03:00 작업은 시작 위치(WorkingDirectory)가
비어 `scripts/backup.py` 를 못 찾고 **exit 2** 로 실패해 왔습니다.

**그 이레 사이의 DB 는 되돌릴 지점이 없습니다** — 고치더라도 소급되지
않으므로 기록으로 남깁니다. 고치는 절차는 배포-안내 12-2-2 (암호가
필요해 사람이 합니다). 고쳐져서 오늘 날짜 산출물이 생기면 이 항은
「닫힘」 으로 바꿉니다.

## ~~T-e · late_in_base 의 경계가 옛 코드와 하루 다릅니다~~ — **결정으로 만듦 (단계 5)**

> 폐회일 당일 마감 미완료는 늦음이 아닙니다 — 그날은 아직 안 지났습니다.
> 4-10 에 적어 결정이 됐고, 시험 test5_c03 이 지킵니다.
> 아래는 그때 적어 둔 내용입니다.

`app/domain/suggestions.py`

옛 코드는 폐회일 **당일** 마감 미완료도 늦음으로 봤고(기준일+1 로 쟀음),
새 코드(overdue_by_date(run, 폐회일))는 안 봅니다 — end < close 라
폐회일 마감은 통과합니다. 폐회일에 마감을 두는 업무가 「수련회 당일
일」 이라 늦음으로 부르지 않는 쪽도 말이 되지만, **정한 것이 아니라
가드를 우회하다 따라온 값**입니다. 제안이 하나 덜 나오면 여기부터 봅니다.

## S-ae · 제안 표에 번호 칸이 없어 `#번호` 를 세어야 합니다

`scripts/suggest_sample.py` 의 `한줄` · 11-2 의 새 규칙

11-2 가 **`회의 · #번호`** 를 표준 가리킴으로 삼았는데, 정작 표에는 번호
칸이 없습니다. 몇 번째인지 **손으로 세야** 합니다 — 08.09 는 13개입니다.

이번 되붙임 표시(`#1`~`#5`)는 세어서 맞는 것을 확인했지만, **가리킴을
규칙으로 삼은 이상 세는 일이 계속 생깁니다.** 표에 번호 칸을 더할지는
**4판 때 함께 봅니다** — 칸을 더하면 `제안줄인가()` 가 보는 7칸이 바뀌므로
S-ab 과 한 판에서 같이 해야 합니다.
