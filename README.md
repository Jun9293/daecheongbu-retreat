# 대청부 수련회 총무팀 통합 관리 시스템

수련회를 **준비 · 진행 · 정산**하는 모바일 우선 웹앱입니다.
설계 배경은 [CLAUDE.md](CLAUDE.md), 화면 스펙은 [docs/mockups](docs/mockups)의 HTML 3개를 보세요.

현재 상태: **Phase 1 완료** — 로그인·권한 / 새 회차 세팅 마법사 / 준비 단계 보드.
(재정·회의록·파일·체크리스트 화면은 이전 설계에서 만든 것이 그대로 살아 있습니다. 재설계 대상입니다.)

---

## 실행 방법

### 가장 쉬운 방법 (Windows)

`run.bat` 파일을 더블클릭하세요. 처음 실행하면 필요한 것들을 자동으로 설치합니다.
검은 창이 뜨면서 아래 주소가 보이면 성공입니다.

    http://127.0.0.1:8000

### 직접 명령어로 실행

```bash
py -3 -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
.venv\Scripts\python.exe seed.py          # 데모 데이터 (처음 한 번만, 선택)
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 휴대폰에서 접속하기

PC와 휴대폰이 **같은 와이파이**에 있으면, PC의 IP 주소로 접속할 수 있습니다.

```bash
ipconfig
```

여기서 나온 IPv4 주소로 `http://192.168.0.x:8000` 처럼 접속하세요.
브라우저 메뉴에서 **홈 화면에 추가**를 누르면 앱처럼 쓸 수 있습니다(PWA).

---

## 들어 있는 데이터

`seed.py`가 **실제 자료**를 그대로 넣습니다.

| 출처 | 반영 내용 |
|---|---|
| Notion 업무 DB → `docs/mockups/retreat-board-v4.html` | 준비 업무 **102건** (Main·하위·일정, 담당팀·관련팀·연결·논의 내역) |
| Google Sheets "2026 여름수련회 일정표_Belong" | 봉사자 일차별 시간표 4일치(선발대·1~3일차) **136건** |

부서는 CLAUDE.md 2장의 **9개**입니다 — 총무M · 총무팀 · 선교사회 · 스케치 · 헤브론 ·
코람데오 · 재정 · 개기자 · 새친구팀. (봉사팀 공통은 해체했습니다.)
행정·현장관리·비품·음식·교역자는 부서가 아니라 총무팀 안의 **파트**로 다룹니다.

회차는 **2026 여름수련회 Belong 하나**입니다. 분류를 그럴듯하게 보이려고 과거 회차를
지어내지 않습니다 — 지어낸 이력이 "필수 23건" 같은 판단의 근거가 되면 안 되기 때문입니다.

그래서 마법사의 자동 분류는 쌓인 회차 수에 맞춰 표현을 바꿉니다. 1회차인 지금은
"지난 회차 실행 / 미실행"까지만 말하고, 3회차가 쌓이면 "필수·추천·후순위"가 됩니다.
그동안 구멍 방지를 맡는 것이 **업무 라이브러리(`/library`)의 필수 지정**입니다.

### 로그인 계정

개발 모드에서는 **실제 SMS 대신 인증번호가 화면에 바로 표시**됩니다.

| 전화번호 | 이름 | 역할 | 부서 |
|---|---|---|---|
| 010-0000-0001 | 박민준 | 총무팀(관리자) | 총무M |
| 010-0000-0002 | 정하윤 | 총무팀(관리자) | 총무M |
| 010-0000-0021 | 스케치 리더 | 부서 리더 | 4 스케치 |
| 010-0000-0022 | 헤브론 리더 | 부서 리더 | 5 헤브론 |
| 010-0000-0014 | 한지훈 | 열람 전용 | - |

부서 리더 계정으로 들어가면 **자기 부서만 펼쳐지고 나머지는 흐리게** 보입니다.
다른 부서 업무도 볼 수는 있지만 상태·논의는 고칠 수 없습니다.

> ⚠️ **전화번호는 모두 자리표시자입니다.** 실제로 쓰시려면
> `설정 > 사용자`에서 각자의 진짜 번호로 바꿔야 로그인할 수 있습니다.

> 데이터를 처음부터 다시 만들려면: `.venv\Scripts\python.exe seed.py --reset`
>
> 실제 사용을 시작할 때는 데모 데이터를 지우고(`--reset` 후 seed 없이 실행) 첫 로그인을 하세요.
> **DB가 비어 있을 때 처음 로그인한 번호가 자동으로 총무팀(관리자) 계정이 됩니다.**

---

## 화면 구성

### 지금 설계의 화면

| 경로 | 설명 |
|---|---|
| `/` `/board` | **준비 단계 보드** — 부서 → Main → 하위 트리 + D-주차 타임라인. 상태 변경, 논의 내역, 연결 강조, 관련업무 고스트, 날짜 필터 |
| `/setup` | **새 회차 만들기** — 4단계 마법사. D-주차 자동 계산, 명절 충돌, 업무 라이브러리 자동 분류, 구멍 방지 경고 |
| `/library` | **업무 라이브러리** — 회차를 넘어 남는 업무 목록. 손으로 **필수** 지정 (총무팀 전용) |
| `/draft` | **회차 준비** — 각 팀이 맡을 업무를 고르고 임시저장·제출. 총무팀이 모아서 회차를 엽니다 |
| `/board/add` | **업무 추가** — 회차를 연 뒤 라이브러리에서 넣거나 새로 만들기 |

### 이전 설계에서 만든 화면 (재설계 대상)

| 경로 | 설명 |
|---|---|
| `/dashboard` | 예전 홈 — 내 부서 할 일, 지연 경고, 예산 집행률 |
| `/schedule` | 봉사자 일차별 시간표 — 일자 탭 + 시각별 타임라인, 부서 필터 |
| `/tasks` | 할 일 — 부서/상태 필터, 상태 변경 |
| `/tasks/{id}` | 할 일 상세 — 선행 작업 지정, 확인 요청, 수정·삭제 |
| `/budget` | 예산 — 항목별 예산 대비 집행 현황 |
| `/expenses` | 지출 등록·내역 (영수증 첨부, 식대 자동 정산) |
| `/refunds` | 환급 대상자 — 지출자별 환급 합계와 지급 처리 |
| `/settings` | 회차·부서·사용자 관리, 내 정보, 변경 이력 |
| `/export/expenses.xlsx` | 엑셀 내려받기 (지출 상세내역 / 예산 대비 집행 / 환급 대상자 3개 시트) |
| `/more` | 아래 Phase 2 화면들의 모음 |
| `/notifications` | 알림함 + 휴대폰 푸시 알림 켜기 |
| `/reviews` | 부서 간 확인 요청 — 승인/반려 |
| `/files` | 작업 파일 — 버전 이력, 검토 상태 |
| `/checklists` | 비품·준비물 체크리스트 |
| `/meetings` | 회의록 — 액션아이템을 할 일로 전환 |

---

## 식대 정산 규칙

기존 "Belong 예산.xlsx"에서 실측 검증한 공식을 그대로 구현했습니다.

```
지원금액   = min(영수증 금액, 인원수 × 1인당 상한)
개인부담액 = 영수증 금액 - 지원금액
```

- **1인당 상한은 하드코딩하지 않고 회차별 설정값**입니다 (`설정 > 회차 설정`).
  여름수련회 8,000원, 소그룹지원비 규정 5,000~7,000원 등 규정에 맞춰 바꿔 쓰면 됩니다.
- 예산 집행액에는 **지원금액만** 반영됩니다. 초과분은 개인부담이므로 수련회 예산에서 나가지 않습니다.
- 기존에 비고 칸에 "9명 박민준 홍성헌…"처럼 적던 인원수·명단은 전용 입력칸으로 분리했습니다.
- 같은 부서가 "모임 식사비-1, -2, -3…"으로 반복 등록하는 흐름을 고려해, 직전 입력값
  (부서·결제자·계좌·참석자 명단)과 다음 항목명이 자동으로 채워집니다.

---

## 안전망 (Phase 2)

담당자가 놓쳐도 시스템이 대신 알아차리게 하는 부분입니다.

**선후행 의존성**
- 할 일에 "먼저 끝나야 하는 작업"을 지정하면 후행은 ⛔ 로 막힌 상태가 됩니다.
- 선행이 완료되면 후행이 ▶ **시작 가능**으로 자동 전환되고, 후행 담당자와 그 부서에 알림이 갑니다.
- 서로를 기다리는 순환 관계는 등록 단계에서 거부됩니다.

**자동 위험 점검** (1시간마다 자동 실행, `더보기 > 지금 위험 점검하기`로 수동 실행)
| 상황 | 알림 대상 |
|---|---|
| 기한이 지남 | 담당자 + 부서 + **총무팀** (상태도 자동으로 '지연'으로 전환) |
| 마감 D-3 / D-1 / 당일 | 담당자 + 부서 |
| 담당자 없이 마감 7일 이내 | 담당자 + 부서 + **총무팀** |
| 선행 작업이 지연됨 | 후행 담당자 + **총무팀** |

같은 상황으로 매일 중복 알림이 오지 않도록 중복 방지 키가 걸려 있고, 날짜가 바뀌면 다시 알립니다.
**열람 전용 계정에는 알림이 가지 않습니다.**

**부서 간 확인 요청**
- 할 일이나 파일에 대해 관련 부서에 "확인 요청"을 보내면 그 부서원에게 알림이 갑니다.
- 요청받은 부서만 승인/반려할 수 있고, 코멘트와 함께 요청자에게 결과가 통보됩니다.
- 파일에 대한 요청이 승인되면 파일 상태도 자동으로 '승인'이 됩니다.

**작업 파일**
- 새 버전을 올리면 v1, v2… 로 이력이 쌓이고 예전 버전도 계속 내려받을 수 있습니다.
- 승인된 파일에 새 버전을 올리면 상태가 다시 '작업중'으로 돌아갑니다 (예전 승인이 새 파일에 잘못 적용되지 않게).

**회의록 → 할 일**
- 액션아이템에 담당자·기한을 적고 "할 일로 등록"을 누르면 그대로 Task가 되고 담당자에게 알림이 갑니다.
- 같은 항목을 두 번 등록하지는 않습니다.
- 수련회와 무관한 일반 회의도 "연결 안 함"으로 기록할 수 있습니다.

### 웹 푸시 알림에 대해

`알림함` 화면에서 **알림 켜기**를 누르면 브라우저 푸시를 받습니다. 다만 웹 푸시는
브라우저 규격상 **HTTPS 또는 localhost 접속에서만** 동작합니다.
같은 와이파이에서 `http://192.168.x.x:8000` 으로 접속한 휴대폰에서는 켤 수 없고,
운영 배포에 HTTPS를 붙이면 해결됩니다.

**푸시를 켜지 않아도 알림은 항상 앱 알림함에 쌓입니다.** 푸시 발송이 실패해도
알림 자체는 남으므로, 알림 하나가 시스템의 단일 실패점이 되지 않습니다.

VAPID 키는 첫 실행 시 `data/vapid_private.pem` 에 자동 생성됩니다 (git 제외).
이 파일을 지우면 기존 구독이 모두 무효가 되니 옮길 때 함께 가져가야 합니다.

---

## 권한

| 역할 | 할 수 있는 일 |
|---|---|
| `admin` 총무팀 | 전체 조회·편집, 회차/부서/예산 항목/사용자 관리 |
| `dept_lead` 부서 리더 | 자기 부서 할 일·지출 편집, 나머지는 열람 |
| `member` 부서원 | 자기 부서 할 일·지출 편집, 나머지는 열람 |
| `viewer` 열람 전용 | 모든 화면 열람만 가능 |

**타 부서 항목은 숨기지 않고 흐리게 표시**됩니다. (존재는 인지되어야 한다는 원칙)

---

## 화면 설계

- **모바일**: 상단바 + 하단 5탭(홈·일정표·할 일·지출·더보기)
- **데스크톱(900px~)**: 좌측 고정 사이드바 + 넓은 본문, 타임라인 2단
- 아이콘은 이모지가 아니라 인라인 SVG — 기기마다 모양이 달라지지 않습니다
- 부서마다 고유 색을 부여해 일정·할 일·파일에서 한눈에 구분됩니다
- 타 부서 항목은 **흐리게**, 공통 일정은 **선명하게** (모두의 일정이므로)

---

## 기술 구성

- Python 3.14 + FastAPI + SQLAlchemy 2.x + SQLite
- Jinja2 서버 렌더링 + 순수 CSS (빌드 도구·Node.js 불필요)
- PWA (manifest + service worker) — Phase 2의 웹 푸시 기반
- 웹 푸시: pywebpush + VAPID
- 테스트: pytest 163개 (`.venv\Scripts\python.exe -m pytest`)

### 폴더 구조

```
app/
  main.py            앱 진입점, 라우터 연결, 오류 화면
  config.py          환경변수 설정
  models.py          데이터 모델 (CLAUDE.md 2장 스키마)
  security.py        세션 쿠키, 권한 가드
  domain/            순수 비즈니스 로직 (테스트 대상)
    meal.py          식대 정산 공식
    budget.py        예산 대비 집행 집계
    permissions.py   역할별 권한 규칙
    auth.py          전화번호 정규화, 인증코드 발급/검증
    clone.py         회차 복제
    dependencies.py  선후행 의존성, 순환 참조 검사
    escalation.py    위험 감지 규칙 (지연·기한임박·담당자 미지정)
  notifications.py   알림 생성 및 수신자 결정
  push.py            웹 푸시 발송, VAPID 키 관리
  routers/           HTTP 라우트
  templates/         Jinja2 화면
  static/            CSS · JS · PWA
  templates/partials/  아이콘(SVG)·공용 매크로
seed_data.py         실제 시간표·업무 원본 데이터
tests/               도메인 테스트 + 실제 HTTP 통합 테스트
data/                SQLite DB, 업로드된 영수증 (git 제외)
```

---

## 운영 배포 전에 해야 할 일

1. **SMS 벤더 연동** — 현재는 개발 모드라 인증번호가 화면에 표시됩니다.
   `app/sms.py`에 벤더 Provider를 추가하고 `DCB_SMS_PROVIDER` 환경변수를 바꾸면 됩니다.
   추천 순서: Solapi(가입 즉시 사용, 건당 ~20원) → NHN Cloud SMS → 알리고.
   어느 벤더든 **발신번호 사전등록**(통신사 규제)이 필요합니다.
2. **`DCB_SECRET_KEY` 설정** — 세션 쿠키 서명 키. 기본값은 개발용이라 반드시 바꿔야 합니다.
3. **`DCB_DEV_MODE=0`** — 인증번호가 화면에 노출되지 않게 합니다.
4. **DB 이전** — 여러 명이 동시에 쓰는 운영 환경에서는 SQLite 대신 PostgreSQL을 권장합니다.
   `DCB_DATABASE_URL`만 바꾸면 됩니다 (`postgresql+psycopg://...`).
5. **호스팅** — Railway / Render / Fly.io 중 하나를 권합니다(파일 업로드용 디스크가 필요).
   Vercel은 서버리스라 SQLite·업로드 파일 저장에 맞지 않습니다.

### 환경변수

| 이름 | 기본값 | 설명 |
|---|---|---|
| `DCB_DATABASE_URL` | `sqlite:///data/app.db` | DB 연결 문자열 |
| `DCB_DATA_DIR` | `./data` | DB·업로드 파일 저장 위치 |
| `DCB_SECRET_KEY` | (개발용 값) | 세션 쿠키 서명 키 — **운영 시 필수 변경** |
| `DCB_SMS_PROVIDER` | `console` | `console`이면 서버 로그로만 발송 |
| `DCB_DEV_MODE` | `1` | `1`이면 인증번호를 화면에 표시 |
| `DCB_PUSH_CONTACT` | `mailto:admin@example.com` | 웹 푸시 VAPID 연락처 — 운영 시 실제 주소로 변경 |
| `DCB_RISK_SCAN_INTERVAL` | `3600` | 자동 위험 점검 주기(초). `0`이면 자동 점검 끔 |
| `DCB_MAX_ASSET_MB` | `50` | 작업 파일 업로드 최대 크기(MB) |
